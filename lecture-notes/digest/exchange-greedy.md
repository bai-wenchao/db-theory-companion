# Chapter digest: exchange-greedy
n_papers_with_tool: 16
PODS exemplars are from the PODS'26 issue inside PACMMOD Vol 4 — mark them ^PODS.

## sigmod26-053 [PODS] — Coresets for Robust Query Optimization  (score 44)
domain: query-optimization
usage: Hoeffding plus union bounds (Lemmas 3.1–3.2); greedy coverage with exponential termination (Lemma 3.4); global↔local risk transfer (Lemma 4.2); Markov argument (Lemma 4.6).
note-quote: "There exists an algorithm that samples O((κ*/δ²) ln(N) ln(δ⁻¹)) queries from W and computes ... a set C ⊆ Π of size O(κ* ln δ⁻¹) such that, with high probability, C is a (c₁ε, τ, c₂δ|W)-coreset for risk." (Thm 3.5)
  Lemma 3: 1 and 3.2 f are accurate estimates of failure probability and risk, respectively. The latter show that 𝑝e𝑟 and Risk implies that the greedy cover procedure defined using proxy risks behaves almost as if it had access to the true risks on the sampled queries. Using these two lemmas, we prove two structural claims about the algorithm. In Lemma 3.3, we show that whenever the algorithm terminates in round 𝑟 , the set C returned in that round satisfies 𝑝 (3𝜀, C) ≤ 3𝛿, implying that C is a valid (3𝜀, 
  Lemma 3.1: Let 𝜀 ′ ∈ (0, 1) and 𝑟 ≥ 1. Then 𝑝e𝑟 (𝜀 ′, F ) − 𝑝 (𝜀 ′, F ) ≤ 𝛿/4 holds simultaneously for all subsets F ⊆ Π of size at most b𝑟 with probability at least 1 − 𝑁 −𝑂 (1) .
  Lemma 3.2: For any round 𝑟 , any 𝜋 ∈ Π and any query 𝑄𝑖 ∈ W −𝑂 (1) with probability at least 1 − 𝑁 .
  Lemma 3.3: Assuming the conditions of Lemmas 3.1 and 3.2 hold in every round, the algorithm terminates with 𝑝 (3𝜀, C) ≤ 3𝛿.
  Lemma 3.4: Assuming the conditions of Lemmas 3.1 and 3.2 hold in every round, the algorithm terminates with 𝑘𝑟 ≤ 2𝜅 ∗ .
  Theorem 3.5: Let W be a workload distribution. Given a set of plans Π of size |Π| = 𝑁 and parameters 𝜀 ∈ (0, 1], 𝜏 ≥ 1, and 𝛿 ∈ (0, 1), let 𝜅 ∗ denote the size of the smallest (𝜀, 𝜏, 𝛿 |W)–coreset for risk. Fix any pair of constants 𝑐 1, 𝑐 2 > 1. There exists an algorithm that samples 𝑂 (𝜅 ∗ /𝛿 2 ) ln(𝑁 ) ln(𝛿 −1 )  queries from W and computes in time 𝑂 (𝜅 ∗ /(𝜀 2𝛿 2 )) 𝑁 ln3 (𝑁 ) ln(𝛿 −1 ) , a set C ⊆ Π of size  𝑂 𝜅 ∗ ln 𝛿 −1 such that, with high probability, C is a (𝑐 1𝜀, 𝜏, 𝑐 2𝛿 |W)–coreset for risk. 3
  Theorem 4.1: Let 𝜀, 𝛿 ∈ (0, 1) and 𝜏 ≥ 1 be parameters. Let W be a workload distribution over Q, and let F ⊆ Π be a (𝜀, 𝜏, 𝛿 | W)-coreset for risk. Let 𝑄 ∼ W. Suppose LocalInference(𝑄, F ) returns a plan ˆ 𝜋. Then, with probability at least 1 − 2𝛿, Risk( ˆ 𝜋, 𝑄 ; 𝜏 2 ) ≤ 2 Risk★ (𝑄 ; 𝜏) + 4𝜀. Moreover, the algorithm uses Θ(|F |(ln |F | + ln(1/𝛿))/𝜀 2 ) time. Here is a high-level overview of the proof of Theorem 4.1. Let F ⊆ Π be an arbitrary set of plans. For a query 𝑄, let 𝑟 = min𝜋 ∈ F Risk(𝜋, 𝑄 ; 𝜏) denote
  Lemma 4: 3 shows that the empirical quantity L-Risk approximates the true local risk L-Risk F . Next, Lemma 4.4 combines the guarantees of Lemmas 4.2 and 4.3 to show that LocalInference returns a plan whose global 𝜏 2 -risk is at most 2𝑟 + 2𝜀. Finally, we consider the case when F is an (𝜀, 𝜏, 𝛿 | W)-coreset for risk. In this case, with probability at least 1 − 𝛿, we have 𝑟 ≤ Risk★ (𝑄 ; 𝜏) + 𝜀. Combining this bound with the guarantee in Lemma 4.4 yields Theorem 4.1. We now give the proof in detail through
  Proof: Observe that for any set of plans F ⊆ Π, the random variable 𝑝e𝑟 (𝜀 ′, F ) is an unbiased estimator of 𝑝 (𝜀 ′, F ). Since each 𝑄𝑖 is drawn i.i.d. from W, by Hoeffding’s inequality [35],     Pr 𝑝e𝑟 (𝜀 ′, F ) − 𝑝 (𝜀 ′, F ) > 𝛿/4 ≤ 2 exp −2𝔫𝑟 (𝛿/4)2 = 2 exp −𝛿 2 𝔫𝑟 /8 . Taking a union bound over all sets of plans of size up to b𝑟 and using the fact that the number of subsets of plans of size at most b𝑟 is bounded by (b𝑟 + 1)𝑁 b𝑟 , we obtain    Pr ∃ F ⊆ Π with |F | ≤ b𝑟 : 𝑝e𝑟 (𝜀 ′, F ) − 𝑝 (𝜀 ′, F ) > 𝛿/4 ≤ 2(b + 1)𝑁 b𝑟 exp −𝛿 2 𝔫𝑟 /8 , where b𝑟 = 𝑐 3𝑘𝑟 ln(𝛿 −1 ) and 𝔫𝑟 = (𝑐 1𝑘𝑟 /𝛿 2 ) ln(𝑁 ) ln(𝛿 −1 ). By a suitable choice of the constants 𝑐 1 and 𝑐 3 , the right-hand side is 𝑁 −𝑐 for an
  Proof: For a fixed plan 𝜋 ∈ Π and 𝑄𝑖 ∈ W𝑟 , let 𝑌𝑖,𝑗 = 1{Pnl(𝜋, 𝑄𝑖,𝑗 ) > 𝜏 }. Then Risk(𝜋, 𝑄𝑖 ) = 1 Í𝔪𝑟 𝑗=1 𝑌𝑖,𝑗 and E[𝑌𝑖,1 ] = Risk(𝜋, 𝑄 𝑖 ). By Hoeffding’s inequality [35], 𝔪𝑟 f f i h  f 𝑄𝑖 ) − Risk(𝜋, 𝑄𝑖 ) > 𝜀/10 ≤ 2 exp −𝔪𝑟 𝜀 2 /50 . Pr Risk(𝜋, Proc. ACM Manag. Data, Vol. 4, No. 2 (PODS), Article 100. Publication date: May 2026. 100:8 Rahul Raychaudhury, Haibo Xiu, Pankaj K. Agarwal, Stavros Sintos, & Jun Yang By plugging in 𝔪𝑟 = (𝑐 2 /𝜀 2 ) ln(𝑁 ) ln(𝔫𝑟 ) from the algorithm and taking a union bound over all f𝑟 , the failure probability is at most |Π| · 𝔫𝑟 = 𝑁 · 𝔫𝑟 pairs (𝜋, 𝑄𝑖 ) ∈ Π × W 2𝑁 𝔫𝑟 exp(−𝑐 2 ln(𝑁 ) ln(𝔫𝑟 )/50) = 2𝑁 𝔫𝑟 𝑁 − (𝑐 2 /50) ln(𝔫𝑟 ) . This is at most 𝑁 −𝑂 (1) for a sufficient

## sigmod26-200 — Querying Cohesive Subgraphs in Temporal Graphs  (score 29)
domain: graph-db
usage: Union-find with inverse-Ackermann amortization; Kruskal-style cycle edge-exchange for the decomposition; induction for correctness; priority search trees for the range queries.
note-quote: "The construction of BTAS runs in O(m log m) and BTAS-Index costs O(m) space."
  Definition 2: 1 (Non-overlapping). A CSM P is said to be non-overlapping if and only if for any graph 𝐺 = (𝑉 , 𝐸) and any two subgraphs 𝐺 [𝑉1 ] ∈ P (𝐺), 𝐺 [𝑉2 ] ∈ P (𝐺), the vertex sets 𝑉1 and 𝑉2 are disjoint, i.e., 𝑉1 ∩ 𝑉2 = ∅. Clearly, for a non-overlapping CSM P, P (𝐺) consists of at most |𝑉 | different subgraphs. Proc. ACM Manag. Data, Vol. 4, No. 3 (SIGMOD), Article 226. Publication date: June 2026. 226:4 Yinyu Liu, Kaiqiang Yu, Shengxin Liu, Cheng Long, and Xun Zhou
  Lemma 2.3: The connected component, 𝑘-core, and connected 𝑘-core are non-overlapping and monotonic.
  Definition 3: 1 (𝑢-𝑣-TW). Given a temporal graph G = (𝑉 , E) and a CSM P. Let 𝑢 and 𝑣 be two vertices in 𝑉 , and [𝑡 1, 𝑡 2 ] be a time window. [𝑡 1, 𝑡 2 ] is a 𝑢-𝑣 time window (or simply, 𝑢-𝑣-TW) w.r.t. P iff 𝑢 and 𝑣 belong to the same subgraph in P (𝐺 [𝑡1,𝑡2 ] ). Besides, a 𝑢-𝑣-TW [𝑡 1, 𝑡 2 ] is said to be minimal iff for any sub-interval [𝑡 1′ , 𝑡 2′ ] ⊂ [𝑡 1, 𝑡 2 ], 𝑢 and 𝑣 belong to the different subgraphs in P (𝐺 [𝑡1′ ,𝑡2′ ] ). Clearly, there could be multiple minimal 𝑢-𝑣-TW between [0, 𝑡 max ]. To illus
  Lemma 3.2: Let [𝑡 1, 𝑡 2 ] be a (minimal) 𝑢-𝑣-TW w.r.t. P. Any time window [𝑡 1′ , 𝑡 2′ ] with [𝑡 1, 𝑡 2 ] ⊆ ′ [𝑡 1, 𝑡 2′ ] is a 𝑢-𝑣-TW w.r.t. P.
  Lemma 3.4: For a vertex subset 𝑆 of 𝑉 with |𝑆 | ≥ 2, 𝑆 induces a subgraph 𝐺 [𝑡𝑠 ,𝑡𝑒 ] [𝑆] in P (𝐺 [𝑡𝑠 ,𝑡𝑒 ] ) if and only if 𝑆 induces a CC 𝐺 [𝑡𝑠 ,𝑡𝑒 ] [𝑆] of 𝐺 [𝑡𝑠 ,𝑡𝑒 ] . Based on the above lemma, we can answer a Historical-CSM query by solving a unified query formulation called Spanning-CC query, formally defined as follows. Spanning-CC query: Given an affiliated graph G and a time window [𝑡𝑠 , 𝑡𝑒 ], the spanning-CC query over G aims to find all vertex sets, called spanning-CCs, where each vertex set 𝑆 
  Lemma 3.6: Given a spanning edge 𝑒 = (𝑢, 𝑣, 𝑡 1, 𝑡 2 ). Let 𝜏 (𝑒) be the timestamp such that 𝑒 ∈ F𝑡1,𝜏 (𝑒 ) and 𝑒 ∉ F𝑡1,𝜏 (𝑒 )+1 . An affiliated forest (𝑉 , F𝑥,𝑦 ) built by BuildAF contains 𝑒 if and only if 0 ≤ 𝑥 ≤ 𝑡 1, 𝑡 2 ≤ 𝑦 ≤ 𝜏 (𝑒). (4)
  Lemma 3.7: AF-3DT-Query answers a query in 𝑂 (𝑛 + log2 𝑚).
  Lemma 3.8: BuildAF runs in 𝑂 (𝛼 (𝑚) + 𝛼 (𝑛𝑡 max
  Proof: 𝑘-core. We show that the 𝑘-core is a non-overlapping and monotonic CSM. The 𝑘-core of a graph 𝐺 is defined as a maximal subgraph where every vertex has degree at least 𝑘 within the subgraph. To show that the 𝑘-core is non-overlapping, we prove that the 𝑘-core is unique in a given graph 𝐺. Assume to the contrary that 𝑔1 and 𝑔2 are two distinct 𝑘-cores in a graph 𝐺. By the definition of 𝑘-core, the union of 𝑔1 and 𝑔2 , i.e., 𝐺 [𝑉 (𝑔1 ) ∪ 𝑉 (𝑔2 )], where 𝑉 (𝑔) denotes the vertex set of subgraph 𝑔, is clearly 𝑘-core, which implies that there exists a larger 𝑘-core, contradicting the maximality of the 𝑘-core. Thus, 𝑘-core is a non-overlapping CSM. We next prove the monotonic property. For any sup
  Proof: This can be verified by the monotonic property of P (Definition 2.2). Assume that 𝑢 and 𝑣 belong to a subgraph 𝑔 in P (𝐺 [𝑡1,𝑡2 ] ). We note that there exists a subgraph 𝑔′ of P (𝐺 [𝑡1′ ,𝑡2′ ] ) such that 𝑔 is contained in 𝑔′ (i.e., 𝑉 (𝑔) ⊆ 𝑉 (𝑔′ ) and 𝐸 (𝑔) ⊆ 𝐸 (𝑔′ )) since 𝐺 [𝑡1′ ,𝑡2′ ] is a supergraph of 𝐺 [𝑡1,𝑡2 ] and P satisfies the monotonic property. Therefore, 𝑢 and 𝑣 belong to subgraph 𝑔′ in P (𝐺 [𝑡1′ ,𝑡2′ ] ) and [𝑡 1′ , 𝑡 2′ ] is a 𝑢-𝑣-TW. □

## sigmod26-031 — Balancing Global and Local: Representative Sampling for Large-Scale Vector Data  (score 28)
domain: sampling | ann-vector-search
usage: Gamma-sum moment identities; (1−p)^m ≤ e^{-mp}; contraction-unrolling greedy analysis; parsimonious k-median reduction.
note-quote: "Let B be the budget and let A_gre be the size-B output of (lazy-)greedy in Algorithm 2. Then F(A_gre) ≥ (1 − 1/e) max_{|A|≤B} F(A)."
  Lemma 1: Under (A1)-(A2) at 𝑠 ∈ S, let 𝐷 = 𝐷 (𝑠) and 𝑚 = 𝑚 S (𝑠). The sample-based LID estimator admits the representation  𝑚 ∑︁  −1 i.i.d. d S (𝑠) = 𝐷 · 1 LID 𝑌 𝑗 , 𝑌 𝑗 ∼ Exp(1), 𝑚 𝑗=1 with the following moments:   d S (𝑠) | 𝐷, 𝑚 = 𝐷 · 𝑚 (bias = 𝐷 ); for 𝑚 = 1, the expectation is infinite. • For 𝑚 > 1, E LID 𝑚−1 𝑚−1  𝑚2 d S (𝑠) | 𝐷, 𝑚 = 𝐷 2 • For 𝑚 > 2, Var LID ; for 𝑚 = 2, the variance is infinite. 2 (𝑚−1) (𝑚−2) Consequently, for 𝑚 ≥ 3,   d S (𝑠) − 𝐷) 2 | 𝐷, 𝑚 = 𝐷 2 MSE(𝑚) = E ( LID 𝑚+2 (𝑚 − 1) 
  Lemma 2: Let MSE(𝑚) be as in Lemma 1, and let MSEref denote the MSE of LID For 𝜏 > 0 and 𝜙 (𝑢) = 𝑢/(1 + 𝑢), ! √︁ √   MSE(𝑚) + MSEref , 𝑚 ≥ 3, E ℓ𝑠 (S) | 𝑚 ≤ 𝜙 𝜏 and the RHS is strictly decreasing in 𝑚. d S (𝑠) − 𝐷 and 𝑒 D = LID d D (𝑠) − 𝐷. Then | LID d S (𝑠) − LID d D (𝑠)| ≤ |𝑒 S | + |𝑒 D |.
  Lemma 3: Under (A1)-(A2), for 𝑚 = 𝑚 S (𝑠) i.i.d. neighbors with uniform directions on S𝑘 (𝑠 ) −1 , the probability that none falls into the cap of half-angle 𝜑 equals (1 − 𝜇𝑘 (𝑠 ) (𝜑))𝑚 and is bounded by exp(−𝑚 𝜇𝑘 (𝑠 ) (𝜑)).
  Theorem 1: Let 𝐷 (𝑠) denote the local intrinsic dimension at 𝑠, 𝑘 (𝑠) = ⌈𝐷 (𝑠)⌉, and 𝑑𝑠 = dist(𝑠, 𝑞). Define   𝜏geo (𝑠, 𝑞, 𝜑) 𝐷 (𝑠 ) 𝑝 geom (𝑠, 𝑞, 𝜑) = 𝜇𝑘 (𝑠 ) (𝜑) . 𝑅 D (𝑠) Under (A1)-(A3), the one-layer progress probability at pivot 𝑠  𝑝 prog (𝑠; 𝑞, 𝜑) ≜ P ∃ available neighbor 𝑠 ′ : dist(𝑠 ′, 𝑞) < 𝑑𝑠 admits the lower bound  𝑝 prog (𝑠; 𝑞, 𝜑) ≥ 1 − exp − 𝛼 𝑚 S (𝑠) 𝑝 geom (𝑠, 𝑞, 𝜑) . For a beam/frontier B with best pivot 𝑠 ★ = arg min𝑢 ∈ B dist(𝑢, 𝑞) and 𝑑 ★ = dist(𝑠 ★, 𝑞), the beam-wise progress probab
  Corollary 1: Fix 𝜑 ∈ (0, 𝜋/2]. The exponent in Theorem 1 grows linearly in 𝑚 S (𝑠). Moreover, 𝜙 by Lemma 2, E[ℓ𝑠 (S) | 𝑚] decreases strictly with 𝑚 for 𝑚 ≥ 3. Therefore, driving down CostLID systematically tends to increase local neighbor counts at the aligned scale (suppressing 𝑚 ∈ {0, 1, 2} events) and thus amplifies the layer-wise progress probability for both greedy (𝑏 = 1) and beam (𝑏 ≥ 1) expansions, even when trajectories are non-monotone. Note. If 𝑘 (𝑠) = ⌈𝐷 (𝑠)⌉ < 2, one may simply replace 𝑘 (𝑠) by 
  Theorem 2: Problem 1 (Representative Sampling) is NP-hard.
  Lemma 4: For 𝑚 ≥ 3, MSE𝑠 (𝑚) in (4) is strictly decreasing in 𝑚, and Δ𝑔𝑠 (𝑚) = MSE𝑠 (𝑚) − MSE𝑠 (𝑚+1) = is strictly positive and decreasing in 𝑚. Hence 𝑔𝑠 (𝑚) = Z ≥0 . 𝐷 (𝑠) 2 (𝑚 + 6) 𝑚 (𝑚 − 1) (𝑚 − 2) Í𝑚−1 𝑟 =0 Δ𝑔𝑠 (𝑟 ) is nondecreasing and concave on
  Lemma 5: Let 𝐹 be given by (3) with each 𝑔𝑠 nondecreasing and concave. Then 𝐹 : 2 U → R ≥0 is nondecreasing and submodular.
  Proof: By (A1)-(A2), 𝑈 𝑗 = (dist(𝑠, 𝑠 𝑗 )/𝑅 D (𝑠)) 𝐷 ∼ Unif [0, 1], hence 𝑌 𝑗 = − ln 𝑈 𝑗 ∼ Exp(1) i.i.d. Í d Let 𝑍 = 𝑚 𝑗=1 𝑌 𝑗 ∼ Γ(𝑚, 1). Then E[ LID S (𝑠)] = 𝐷 E[𝑚/𝑍 ] = 𝐷 · 𝑚/(𝑚 − 1) for 𝑚 > 1, and 2 2 d Var( LID S (𝑠)) = (𝐷𝑚) Var(1/𝑍 ) = 𝐷 𝑚 2 /[(𝑚 − 1) 2 (𝑚 − 2)] for 𝑚 > 2 (standard identities for 𝑍 ∼ Γ(𝑚, 1)). The MSE expression follows by Var + bias2 , and its monotonicity in 𝑚 is immediate. d S (𝑠)| ≤ E| d d S (𝑠) − 𝐷 |, Finally, by the triangle inequality, E| d LID D (𝑠) − LID LID D (𝑠) − 𝐷 | + E| LID where the first term depends on 𝑘 > 2 but not on 𝑚, and the second term is upper-bounded by ref √︁ MSE(𝑚), decreasing in 𝑚 for 𝑚 ≥ 3. □
  Proof: Write 𝑒 S = LID d Since max{LID D (𝑠), 𝜏 } ≥ 𝜏, √︁ √ MSE(𝑚) + MSEref E|𝑒 S | + E|𝑒 D | E[𝑟𝑠 | 𝑚] ≤ ≤ , 𝜏 𝜏 √ where we used Cauchy-Schwarz for E|𝑒 | ≤ E𝑒 2 . 𝜙 is increasing and concave on [0, ∞), hence by Jensen, E[ℓ𝑠 (S) | 𝑚] = E[𝜙 (𝑟𝑠 ) | 𝑚] ≤ 𝜙 (E[𝑟𝑠 | 𝑚]), giving the claim. Monotonicity in 𝑚 follows from the strict decrease of MSE(𝑚) for 𝑚 ≥ 3. □

## sigmod26-009 — Accelerating Maximum Common Subgraph Computation by Exploiting Symmetries  (score 17)
domain: graph-db
usage: Swap/exchange arguments map any pruned branch to an earlier lexicographic branch; the automorphism characterization identifies symmetry classes.
note-quote: "Variable symmetry breaking does not violate the correctness or completeness of the MCIS solution."
  Definition 2: 1 (Maximum Common Induced Subgraph). The maximum common induced subgraph (MCIS) problem for two graphs 𝐺 and 𝐻 is to find induced subgraphs 𝐺 [𝑉 ′ ] ⊆ 𝐺 and 𝐻 [𝑈 ′ ] ⊆ 𝐻 such that 𝐺 [𝑉 ′ ]  𝐻 [𝑈 ′ ] and |𝑉 ′ | = |𝑈 ′ | is maximized. The MCIS problem is NP-hard and serves as a fundamental model for identifying structural similarity between graphs. Most exact approaches employ a branch-and-bound strategy that incrementally builds vertex correspondences while pruning infeasible or suboptimal branc
  Definition 3: 1 (Properties of MCIS Algorithms). An algorithm for the MCIS problem must satisfy the following properties: • Correctness: Only mappings between isomorphic induced subgraphs of 𝐺 and 𝐻 are considered valid. • Completeness: At least one mapping of maximal cardinality is guaranteed to be found. The rest of this section is structured as follows. First, we discuss our symmetry detection technique, which we call modular symmetry. We then individually discuss variable and value symmetry. Following thi
  Theorem 3: 1 (Mutual Exclusiveness). A vertex 𝑣 cannot participate in both negative and positive symmetry relationships.
  Theorem 3.2: Variable symmetry breaking does not violate the correctness or completeness of the MCIS solution.
  Theorem 3.3: Value symmetry breaking does not violate the correctness or completeness of the MCIS solution.
  Lemma 1: Two vertices 𝑢, 𝑣 ∈ 𝑉 are modular symmetric if and only if the permutation that swaps 𝑢 and 𝑣 is an automorphism of 𝐺. 3.7 Complexity Analysis In this section, we discuss the data structures and implementation details used to understand the SymSplit algorithm efficiently. Symmetry Identification. Fast symmetry identification is crucial to our algorithm’s performance; otherwise, the overhead of symmetry detection negates the benefits of symmetry breaking. Our symmetry detection mechanism addresse
  Lemma 2: The time complexity of symmetry detection is 𝑂 (𝑛 2 ), determined by adjacency matrix traversal and hash operations. A naïve approach would compare neighborhoods for all vertex pairs, requiring 𝑂 (𝑛 3 ) time: 𝑂 (𝑛 2 ) to compute each vertex’s neighborhood, followed by pairwise comparisons. Instead, we employ a hash-based approach. For each vertex, we compute a hash value of its neighborhood using an injective hash function designed to avoid collisions on our datasets. Computing neighborhoods for
  Proof: Suppose 𝑣, 𝑢, 𝑤 ∈ 𝑉 (𝐺), and assume 𝑣 and 𝑢 are positively symmetric, i.e., 𝑁 + (𝑣) = ∈ 𝐸 (𝐺). Now suppose 𝑣 and 𝑤 are negatively symmetric, i.e., 𝑁 − (𝑣) = 𝑁 − (𝑤), and by definition of negative symmetry, (𝑣, 𝑤) ∉ 𝐸 (𝐺). Since 𝑣 and 𝑤 have identical negative neighborhoods, and 𝑣 is adjacent to 𝑢, it follows that 𝑤 must also be adjacent to 𝑢. However, as (𝑣, 𝑤) ∉ 𝐸 (𝐺), it follows that 𝑣 is not adjacent to 𝑤, but 𝑢 is adjacent to 𝑤. This contradicts 𝑁 + (𝑣) = 𝑁 + (𝑢). □
  Proof: Let the conflicting edges be labeled (𝑣 1, 𝑢 2 ), (𝑣 2, 𝑢 1 ) where possibly 𝑢 2 = ⊥, for a branch 𝑏 with mapping 𝑀. By performing a swap of (𝑣 1, 𝑢 2 ), (𝑣 2, 𝑢 1 ) to (𝑣 1, 𝑢 1 ), (𝑣 2, 𝑢 2 ) we can create a symmetrical mapping 𝑀 ′ with corresponding hypothetical search branch 𝑏 ′ . We call 𝑏 ′ hypothetical since it may have been pruned as well, due to a different 𝐺-conflict. However, since search trees branch on all possible mappings of 𝑣 at the same node, 𝑏 ′ must have been considered before 𝑏, assuming values are consider in the same arbitrary but fixed order used in Rule 1 (more on this assumption later). Thus repeated swapping of 𝐺-conflicting edges will lead to earlier and earlier hy

## sigmod26-224 — Skyline Retrieval meets Set-Cover Chunk Merging for Long-Context LLM QA  (score 16)
domain: llm-db
usage: WSC reduction proves hardness, motivating greedy merging; an averaging argument against an optimal cover bounds each greedy step.
note-quote: "The meta-query merge problem is NP-hard via a polynomial-time, cost-preserving reduction from Weighted Set Cover (WSC)."
  Definition 2: 1 (Closed-book LLM QA). Given 𝑄 = {𝑞 𝑗 }𝑛𝑗=1 , closed-book QA answers each query using only the model’s parametric knowledge without external documents as 𝐴 𝑗 = LLM(𝑞 𝑗 ). In domain-specific, up-to-date, and private scenarios, closed-book LLM QA is insufficient. To address this limitation, existing works supplement LLMs with external documents. However, due to constraints in the input context window, it is necessary to first define the concept of a chunk and its associated token costs.
  Definition 3: 1 (Query Decomposition). Let 𝑄 denote the query space and M as the space of decomposed queries. A decomposed query is defined as a tuple M (𝑞) = (𝐸 (𝑞), 𝑑, 𝑜), where 𝐸 (𝑞) is an extraction meta-query, 𝑑 is a target extraction data structure type, and 𝑜 is an optional operation specification. Extraction meta-query 𝐸 (𝑞) is instantiated under one of four predefined extraction schemas {(key, attribute), (head, relation, tail), (timestamp, value), raw text}, which correspond to structured extraction
  Lemma 3: 4 (Complementarity of Dimensions). Presence and semantic dimensions capture orthogonal aspects of retrieval. In particular, for any chunk 𝐶𝑖 and query 𝑞, 𝜙 (𝐶𝑖 , 𝐸 (𝑞)) = 0 ⇏ 𝜔 (𝐶𝑖 , 𝑞) = 0. That is, we can still judge a section to be relevant (high 𝜔) even if no explicit keyword appears (low 𝜙).
  Theorem 3: 8 (Problem 3 is NP-Hard). The meta-query merge problem is NP-hard via a polynomialtime, cost-preserving reduction from Weighted Set Cover (WSC) [9, 17].
  Lemma 3.10: (Efficiency) For every 𝑡, 𝐺𝑡 +1 (𝐶𝑡 +1 ) ≥ OPT . Í Ð
  Proof: Because the number of keywords within 𝐸 (𝑞) is finite, we can form a paraphrase 𝐶 ★ of 𝑞 in which each 𝑚 ∈ 𝐸 (𝑞) is replaced by a synonymous expression not contained in 𝐸 (𝑞). Then no element of 𝐸 (𝑞) appears in 𝐶 ★, so by Definition 3.2 we have 𝜙 (𝐶 ★, 𝐸 (𝑞)) = 0. Yet 𝐶 ★ is semantically equivalent (or highly aligned) with 𝑞, hence by the assumed property of 𝜔 we obtain 𝜔 (𝐶 ★, 𝑞) > 0. Taking 𝐶𝑖 = 𝐶 ★ yields the claim. □
  Proof: Weighted Set Cover (WSC). Given a universe 𝑈 and a family S = {𝑆 1, . . . , 𝑆𝑚 } with weights 𝑤𝑖 ≥ 0, the goal is to pick S ′ ⊆ S covering 𝑈 with minimum total weight: ∑︁ Ø min 𝑤 s.t. 𝑆𝑖 = 𝑈 . (3) 𝑖 ′ S ⊆S 𝑆𝑖 ∈ S ′ 𝑆𝑖 ∈ S ′ Ð Reduction. From 𝐼 SC = ⟨𝑈 , S, w⟩, we build an instance 𝐼 Merge = ⟨{𝐸 (𝑞 𝑗 )}, 𝑗 𝑆 (𝐸 (𝑞 𝑗 )), T⟩ of Problem 3 as follows. For each meta-query 𝐸 (𝑞 𝑗 ), the Pareto optimal chunk list 𝑆 (𝐸 (𝑞 𝑗 )) is selected by Algorithm 2. Treat each chunk 𝐶 as a subset of meta-queries: it covers all 𝐸 (𝑞) with 𝐶 ∈ 𝑆 (𝑞). Set its cost 𝑇 (𝐶) to match the corresponding subset weight 𝑤𝑖 . Then, the objective in Equation (5) is exactly the WSC objective with the coverage constraint that ea

## sigmod26-229 — Stress-Testing Causal Claims via Cardinality Repairs  (score 15)
domain: data-cleaning
usage: NP-completeness motivates influence-scored greedy heuristics (SubCure-tuple/pattern) with sampling and periodic influence recomputation instead of exact optimization.
note-quote: "Problem 1 and Problem 2 are NP-complete for AVG and ATE."
  Definition Tuple Influence: The influence of a tuple $t $ on the ATE of $T$ on $O$ is defined as: \[ influence (t) = ATE_ (T, O) - ATE_ \ t\ (T, O) \] where $T$ and $O$ denote the treatment and outcome variables, respectively, and $ATE_ (T, O)$ represents the ATE of $T$ on $O$ computed over the dataset $ $, while adjusting for confounding variables (omitted from the notation for clarity).
  Lemma : Consider a database $ $ and its identifier augmentation $ _2$. For every set of tuples $I \ 1,…, n\ $, there exists a pattern $ _I$ such that $ ( _I)=I$.
  Definition DB Causality \cite{meliou2010causality: Let $t ^n$ be an endogenous tuple, and $r$ a possible answer to query $q$.
  Proposition : Problem [ref] and Problem [ref] are NP-complete for AVG and ATE.
  Definition \brit{TODO: Give a name: Let $t _d^n$ be an endogenous tuple, $T $ is and $t_i (T)$ is
  Theorem : The pattern-deletion problem is NP-complete for AVG and ATE.
  Proposition : The \ problem is NP-complete.
  Proposition : Problem [ref] is NP-complete.
  Proof: [Proof of Lemma [ref] ]
  Proof: [Proof of Proposition [ref] ] We show a reduction from SUBSET-SUM to the decision version of . Recall that in SUBSET-SUM we are given a set of numbers $S = \ x_1, …, x_n\ $ and a target $k$, and the problem is to decide whether there exists a subset $S' S$ such that $Σ_ x S' x = k$. Recall that in \ we are given a database instance $ $ over a schema $ $, a binary treatment variable $T $, an outcome variable $O $, a desired value $ATE_d$ and $ε>0$. In the decision version of this problem we are also given an additional bound $n$, and we need to decide whether there is a subset of tuples $ $ s.t. $| | n$ and it holds that $ATE_ (T,O) [ATE_d - ε, ATE_d + ε]$. We turn to describe the reduction. 
  bound: Given a desired causal estimate $ATE_d$ and a threshold $ε > 0$, find a minimal size subpopulation defined by a pattern $ $ such that: $$ATE_ ( ) (T,O) [ATE_d -ε, ATE_d + ε]$$ In order to analyze the complexity of the problems, we implicitly refer to their decision-problem versio
  bound: To nevertheless assess how closely \ approximates the optimal solution, we designed controlled experiments using synthetic data where an upper bound on the number of deletions is known, thus bounding the search space OPT.
  bound: We then injected 20\% noisy records into the data, making the number of noisy tuples an upper bound on the solution size.
  bound: Both algorithms produced solutions smaller than the upper bound for up to 1.3k noisy tuples.
  bound: Proof of prop:problem_is_np_hard_pattern We now turn to prove prop:problem_is_np_hard_pattern , showing that prob:patterns is NP-hard.
  bound: We can assume without loss of generality that there are no confounding variables, as the problem remains NP-hard in this case, as we show in the proof of prop:problem_is_np_hard (this assumption only simplifies the writing, but has no meaningful impact on the construction).
