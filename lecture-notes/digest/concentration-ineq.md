# Chapter digest: concentration-ineq
n_papers_with_tool: 38
PODS exemplars are from the PODS'26 issue inside PACMMOD Vol 4 — mark them ^PODS.

## sigmod26-175 [PODS] — On Sketching Trimmed Statistics  (score 72)
domain: streaming | sketching
usage: Geometric level sets with random threshold ζ; dyadic subsampling isolates Θ(log n/ε²) survivors; CountSketch/heavy-hitter identification; Chernoff plus union bound over levels.
note-quote: "there exists a linear sketch that uses poly(log n/ε) bits of space and estimates Σ_{i=1}^k |a_i|^p up to a (1±ε) multiplicative factor with high constant probability."
  Definition 2.1: Given parameters 𝜃, 𝑘, a HeavyHitter data structure D that receives a stream of updates to the frequency vector 𝒇 and provides a set 𝑇 ∈ [𝑛] of heavy hitters of 𝒇 , where (1) 𝑖 ∈ 𝑇 if 𝑓𝑖 ≥ 𝜃 ||𝒇−𝑘 || 2, 9 (2) For every 𝑖 ∈ 𝑇 , we have 𝑓𝑖 ≥ 10 · 𝜃 ||𝒇−𝑘 || 2 where 𝒇−𝑘 is the frequency vector excluding the top 𝑘 frequencies. Moreover, for every 𝑖 ∈ 𝑇 , D can estimate 𝑓𝑖 up to 𝑘1 𝒇−𝑘 2 additive error. Turnstile streaming model. Our input is an 𝑛-dimensional frequency vector 𝒙, initially all zeros. 
  Theorem 4: 1 (𝐹𝑝 of top-𝑘 vector for 0 ≤ 𝑝 ≤ 2). Given 𝒙 ∈ Z𝑛 , 𝑝 ∈ [0, 2], 𝑘 ≥ 0, and 𝜀 ∈ (0, 1), suppose that we have ∥𝒙 −𝑘 ∥ 22 𝑎𝑘2 ≥ (𝜀/log 𝑛)𝑐 · . (2) 𝑘 for some constant 𝑐. Then there exists a linear sketch that uses poly(log 𝑛/𝜀) bits of space (where the Í exponent depends on the value of 𝑐) and estimates 𝑘𝑖=1 |𝑎𝑖 |𝑝 up to a (1 ± 𝜀) multiplicative factor with high constant probability. Remark 4.1. When 𝑘 ≥ polylog𝑛, condition (2) is always met. Therefore, this directly gives us the result for modera
  Definition 4.2: Suppose that 𝑚 is an upper bound on ∥𝒙 ∥ ∞ and 𝑡 = 1 + log1+𝜀 𝑚. Let 𝜁 be a uniform random variable in [1/2, 1]. Define the level set 𝑆 𝑗 for 𝑗 ∈ [1, log1+𝜀 𝑚] to be 𝑆 𝑗 = {𝑖 ∈ [𝑛] : |𝑥𝑖 | ∈ [𝜁 (1 + 𝜀)𝑡 − 𝑗 −1, 𝜁 (1 + 𝜀)𝑡 − 𝑗 )} and 𝑠 𝑗 = |𝑆 𝑗 |. We say the level set 𝑆 𝑗 “contributes" if ∑︁ |𝑥𝑖 |𝑝 ≥ 𝑖 ∈𝑆 𝑗 𝑘 𝜀 2 ©∑︁ 𝑝 ª |𝑎𝑖 | ® . log2 𝑛 𝑖=1 « ¬ ∥𝒙 −𝑘 ∥ 22 𝑡 − 𝑗 ≥ |𝑎 | and 𝑘 𝑘 . For any 𝑗 such that 𝜁 (1 + 𝜀) 2 𝑡 − 𝑗 −1 2 𝑐+5 𝑆 𝑗 “contributes", taking 𝑣 = 𝜁 (1 + 𝜀) we have 𝑣 · 𝑠 𝑗 ≥ (𝜀/log 𝑛) · 𝒙 
  Lemma 4.3: Suppose that 𝑎𝑘2 ≥ (𝜀/log 𝑛)𝑐 · is the rank of 𝑣 in array 𝒙 (i.e., the number of entries in 𝒙 with value greater than 𝑣).
  Lemma 4.4: Consider some level set 𝑆 𝑗 with 𝑠 𝑗 = |𝑆 𝑗 |. Consider  the subsampling stream P where 𝐶 log 𝑛 each coordinate is sampled with probability 𝑟 = min 1, 𝑠 𝑗 𝜀 2 for some constant 𝐶 and suppose that 𝑆 𝑗 has 𝑧 survivors in this stream. Let 𝑠˜𝑗 = 𝑧/𝑟 , then with probability 1 − 1/poly(𝑛) we have (1 − 𝜀)𝑠 𝑗 ≤ 𝑠˜𝑗 ≤ (1 + 𝜀)𝑠 𝑗 .
  Lemma 4.5: Consider some level set 𝑆 𝑗 with 𝑠 𝑗 = |𝑆 𝑗 |. Consider the subsampling stream P/2 𝐶 log 𝑛 where each coordinate is sampled with probability 𝑟 = min 1, 𝑠 𝑗 𝜀 2 · 21 for some constant 𝐶. With probability at least 1 − 1/poly(𝑛) there are less than 𝑐 log 𝑛/𝜀 2 survivors from 𝑆 𝑗 .
  Lemma 4.6: Suppose that 𝑎𝑘2 ≥ (𝜀/log 𝑛)𝑐 · −𝑘 and 𝑆 𝑗 contributes where 𝜁 (1 + 𝜀)𝑡 − 𝑗 ≥ |𝑎𝑘 |. 𝑘 Then, with probability at least 1 − poly(𝜀/log 𝑛), we have 𝑠˜𝑗 ∈ [(1 − 𝜀)𝑠 𝑗 , (1 + 𝜀)𝑠 𝑗 ].
  Lemma 4: 6 suggests that our current algorithm requires (log 𝑛/𝜀)𝑐+9 buckets in the CountSketch data structure. However, this dependence can be further improved. For example, (1) for 𝑝 < 2, we can save an log 𝑛 when summing over the different level sets in Lemma 4.3. (2) Our current algorithm estimates each level-set size to (1 ± 𝜀) accuracy. However, if a level is “tiny” (e.g., contributes only an 𝜀-fraction of total 𝐹𝑝 ), such precision is unnecessary: an 𝑂 (1) constant-factor estimate suffices. Using 
  Proof: Since 𝑆 𝑗 contributes, we have 𝑝 𝑠 𝑗 𝑣 𝑝 ≥ (𝜀/log 𝑛) 2𝑘𝑎𝑘 . Proc. ACM Manag. Data, Vol. 4, No. 2 (PODS), Article 111. Publication date: May 2026. 111:8 Honghao Lin, Hoai-An Nguyen, and David P. Woodruff Algorithm 1 Level-Set-Estimator (𝜀 ∈ (0, 1]) Require: (𝑖) A subsampling scheme 𝑃 such that the 𝑖-th level has subsampling probability 𝑟𝑖 = 2−𝑖 to each coordinate of the underlying vector; (𝑖𝑖) an upper bound on each coordinate 𝑚; (𝑖𝑖𝑖) 𝐿 + 1 HeavyHitter structures D0, . . . , D𝐿 with parameter 𝜃 = (𝜀/log 𝑛)𝑐+9 where 𝐿 = log 𝑛 and D𝑖 corresponds to the 𝑖-th substream. 1: 𝑡 0 ← 𝐾 log(𝜀 −1 log 𝑚) where 𝐾 is a large constant. 2: 𝜁 ← uniform random variable between [1/2, 1]. 3: 𝑡 ← log1+𝜀 (𝑚) + 1.
  Proof: Denote the coordinates in 𝑆 𝑗 as 𝑢𝑖 for 𝑖 ∈ [𝑠 𝑗 ]. Let 𝑋𝑖 for 𝑖 ∈ [𝑠 𝑗 ] denote an indicator random Í variable which is 1 if 𝑢𝑖 was sampled in P and 0 otherwise. We have that E[𝑋𝑖 ] = 𝑟 and E 𝑖 𝑋𝑖 = 𝑠 𝑗 𝑟 . Then, from Chernoff’s bound we have that " # ∑︁ Pr 𝑋𝑖 − 𝑠 𝑗 𝑟 ≥ 𝜀𝑠 𝑗 𝑟 ≤ 2 exp(−𝜀 2𝑠 𝑗 𝑟 /3) ≤ 2 exp(−𝐶 log 𝑛/3) ≤ 1/poly(𝑛). □

## sigmod26-314 — Finding Heavy-Hitters with Optimal State Changes  (score 63)
domain: streaming | sketching
usage: Morris/Morris+ counters with sub-gamma tails (Lemma 22, exp(−cR)); competing-pairs counting plus Markov for counter survival; dyadic subsampling (Lemmas 13/14); exponential max-stability (Prop 15) for Fk tracking; lower bound from crafted streams with one repeated item.
note-quote: "Let k ∈ [1, ∞). Suppose that an algorithm solves the (ε, ℓk)-heavy-hitter problem with probability at least 3/4. Then this algorithm must use at least Ω(n^{1−1/k} ε^{-1} log(1/ε)) state changes." (Thm 6)
  Theorem 2: Let ℎ ∈ [𝑛] be a (9/10, ℓ2 )-heavy-hitter. If 2∥ 𝑓1 ∥ ≤ 𝑝 ≤ ∥ 𝑓2∥ . Let 𝑓ˆ be the count 2 2 estimates returned by the algorithm. Then with at least constant nonzero probability, 𝑓ˆℎ ≥ 15 ∥𝑓 ∥ 2 . Moreover, 𝑓ˆ𝑥 ≤ 𝑓𝑥 holds deterministically for all 𝑥. This algorithm uses 𝑂 (log 𝑚(log 𝑚 + log 𝑛)) space and 𝑂 (∥𝑓 ∥ 2 ) state changes. Additionally, we remark that by running this algorithm for 𝑝 at 𝑂 (log 𝑚) different scales, we obtain the same guarantee with no assumption that ∥𝑓 ∥ 2 is known to the 
  Theorem 3: There is an algorithm √ that solves the (0.9, ℓ2 )-heavy-hitters problem with 3/4 probability using 𝑂 (log3 𝑛) space and 𝑂 ( 𝑛 log3 𝑛) state changes. For the upper bounds, we ask whether it is possible to achieve the optimal state-change bound for ℓ𝑘 heavy hitters, while retaining polylog(𝑛) space. We show that we can get surprisingly close; for all 1 < 𝑘 ≤ 2 our number of state changes is optimal to within log log 𝑛 factors.
  Theorem 4: For 𝑘 ∈ [1, 2], there is an algorithm that solves the (𝜖, ℓ𝑘 )-heavy-hitters problem using 𝑂 ( 𝜖12 (log 𝜖1 ) log6 𝑛 log log 𝑛) space and 𝑂 ( 𝜖1 (log 𝜖1 )𝑛 1−1/𝑘 poly log log 𝑛) state changes. In the appendix, we show that a small change to this argument solves heavy-hitters for 𝑘 ≥ 2.
  Theorem 5: For 𝑘 ≥ 2, there is an algorithm that solves the (𝜖, ℓ𝑘 )-heavy-hitters problem using 𝑂˜ ( 𝜖12 𝑛 1−2/𝑘 ) space and 𝑂 ( 𝜖1 (log 𝜖1 )𝑛 1−1/𝑘 poly log log 𝑛) state changes. One might wonder if the additional log 𝜖1 factors are necessary in the above state change bounds. This factor shows up within the space bound of many heavy-hitter algorithms, including BPTree Proc. ACM Manag. Data, Vol. 3, No. 5 (PODS), Article 278. Publication date: November 2025. 278:4 William Swartworth and David P. Woodruff 
  Theorem 6: Let 𝑘 ∈ [1, ∞) Suppose that an algorithm solves the (𝜖, ℓ𝑘 )-heavy-hitter problem with probability at least 3/4. Then this algorithm must use at least Ω(𝑛 1−1/𝑘 𝜖1 log 𝜖1 ) state changes. 1.3 Existing Work Our work is closely connected to [14] who introduced the state-change model in the context of streaming algorithms. While they obtain optimal bounds for ℓ𝑘 -heavy-hitters up to polylog(𝑛) factors, these polylog(𝑛) factors are quite large – roughly log11+3𝑘 if their results are applied directly
  Theorem 7: Let ℎ ∈ [𝑛] be a (9/10, ℓ2 )-heavy-hitter. If 2∥ 𝑓1 ∥ ≤ 𝑝 ≤ ∥ 𝑓2∥ . Let 𝑓ˆ be the count 2 2 estimates returned by the algorithm. Then with at least constant nonzero probability, 𝑓ˆℎ ≥ 15 ∥𝑓 ∥ 2 . Moreover, 𝑓ˆ𝑥 ≤ 𝑓𝑥 holds deterministically for all 𝑥. This algorithm uses 𝑂 (log 𝑚(log 𝑚 + log 𝑛)) space and 𝑂 (∥𝑓 ∥ 2 ) state changes.
  Corollary 8: Suppose that ∥ 𝑓 ∥ 2 is promised to lie in [𝑎, 𝑏]. Then there is an algorithm using 𝑂 (log(𝑏/𝑎) log 𝑚(log 𝑛 + log 𝑚)) space and 𝑂 (log(𝑏/𝑎) ∥𝑓 ∥ 2 ) state changes that achieves the guarantee of Theorem 7 with 3/4 probability. 1𝑓 [𝑖,𝑇 ] (ℎ) means the frequency count of ℎ on the stream interval [𝑖,𝑇 ]. Proc. ACM Manag. Data, Vol. 3, No. 5 (PODS), Article 278. Publication date: November 2025. 278:8 William Swartworth and David P. Woodruff
  Theorem 9: There is an algorithm √ that solves the (0.9, ℓ2 )-heavy-hitters problem with 3/4 probability using 𝑂 (log3 𝑛) space and 𝑂 ( 𝑛 log3 𝑛) state changes.
  Proof: For stream indices 𝑖 < 𝑗 ∈ [𝑚], we say that (𝑖, 𝑗) is a competing pair for ℎ if 𝑥𝑖 = ℎ and there exists 𝑇 ≥ 𝑗 such that 𝑓 [ 𝑗,𝑇 ] (𝑥 𝑗 ) ≥ 21 𝑓 [𝑖,𝑇 ] (ℎ).1 We first count the competing pairs for ℎ. Let 𝑓 = [𝑓1, . . . , 𝑓𝑛 ] be the frequency vector. Note that #{𝑖 : ∃𝑗, 𝑥 𝑗 = 𝑘 and (𝑖, 𝑗) is a competing pair} ≤ 2𝑓𝑘 . (1) To form a competing pair (𝑖, 𝑗) with 𝑥 𝑗 = 𝑘, there are at most 𝑓𝑘 ways to choose 𝑗, and by the Í above fact there are at most 2𝑓𝑘 ways to choose 𝑖. That means that are at most 2 𝑘 𝑓𝑘2 = 2 ∥𝑓 ∥ 22 competing pairs. Therefore the average update to ℎ is part of at most 2 ∥𝑓 ∥ 22 /𝑓ℎ competing pairs. Say that an 2𝑓 update to ℎ is bad, if it is part of at least 2𝑀 ∥𝑓 ∥ 22 /𝑓ℎ comp
  Proof: Recall that Theorem 7 only provides underestimates of the true frequency values. We may therefore run this algorithm on power-of-two increments with 𝑝 = 𝑎, 2𝑎, 4𝑎, . . . 𝑏. Since one of these values of 𝑝 is in the range of Theorem 7, we may simply take the maximum frequency count entrywise over runs, so that the guarantee holds with constant nonzero probability. By repeating the entire procedure a constant number of times (and again taking maxes) we may boost the overall success probability to 3/4. □

## sigmod26-196 — Privacy Loss of Noise Perturbation via Concentration Analysis of A Product Measure  (score 56)
domain: privacy-dp
usage: Radon–Nikodym product-measure lemma; exponent-optimized Markov bound; AMP sensitivity/Jacobian bounds; linear DP composition.
note-quote: "if σ_M ≥ (Δ₂f/ε)·t, then the product noise achieves (ε,δ)-DP on the perturbed f(x) ... the expected magnitude of our product noise is reduced by a factor of Θ(log 1/δ) relative to the classic Gaussian noise."
  Theorem Distribution-Independent Composition~\cite{he2021tighter: Suppose an iterative algorithm $ A $ has $T$ steps: $\ W_i (s)\ _ i=0 ^ $, where $W_i$ is the learned hypothesis after the $i$-th iteration. Suppose all the iterators are ($ε, δ$)-differentially private. Then, the algorithm $ A $ is ($ε^ , δ^ $)-differentially private that & ε^ = T (e^ ε -1)ε e^ ε +1 + √ 2 log ( 1 δ ) T ε^2 , \\ & δ^ = 2 - ( 1-e^ε δ 1 + e^ε )^ ε^ ε ( 1- δ 1 + e^ ε )^ T - ε^ ε - ( 1 - δ 1 + e^ε )^ T + δ^ , where $ δ $ is an arbitrary positive real constant, and $δ^ $ is defined a
  Theorem : Given a function $f( x ) ^M$ ($M>2$) with $l_2$ sensitivity $ _2f$ and any constant $k>1$, if we set σ_M^2 ≥ ( _2f)^2 ε^2 t^2 and t^2 = 2 k^ 4 M ( M 4 + 3 2 )^ (1+ 4 M ) e^ (1+ 2 M ) , then the product noise in ( [ref] ) achieves $(ε,δ)$-DP on the perturbed $f( x )$, where $ε>0$. By setting $λ = _2f σ_M $, the corresponding $δ$ is given by δ= e^ - λ^ 2 2 k√ ( _1F_1 ( M 4 + 1 2 ; 1 2 ; λ^2 2 ) + √ 2 λ _1F_1 ( M 4 +1 ; 3 2 ; λ^2 2 ) ) √ M-1 √ M 2 - 3 2 √ M 2 + 3 4 , where $_1F_1(·,·,·)$ is the con
  Lemma Product Measure~\cite{durrett2019probability: Let $ (X, A )$ \(\) and $ ( (Y, B ) )$ be measurable spaces, let $μ_1$ and $ _1$ be finite measures on $ ( X, A )$, and $μ_2$ and $ _2$ be finite measures on $ (Y, B )$. If $ ( _1 μ_1 )$ and $ ( _2 μ_2 )$, then $ ( _1 × _2 μ_1 × μ_2 )$, and $ d ( _1 × _2) d (μ_1 × μ_2) (x, y) = d _1 d μ_1 (x) d _2 d μ_2 (y)$, for $μ_1 × μ_2$ -almost everywhere , where $ $ stands for absolutely continuous, $ d _1 d μ_1 (x)$ denotes the Radon-Nikodym derivative of $ _1$ with respect to $μ_1$ evaluated at $x$, simi
  Definition : [cite] A function $f: R ^M R : $ ; is a convex function if for all $ _1$, $ _2 R ^M$, $f( _1)-f( _2) f( _2), _1 - _2 $. ; is a $ $-strongly convex function if for all $ _1$, $ _2 R ^M$, $f( _1) f( _2) + f( _2), _1 - _2 + 2 \| _1 - _2 \|_2^2 $ or equivalently, $ f( _1)- f( _2), ( _1 - _2) \| _1 - _2 \|_2^2 $. ; has $L$-Lipschitz constant $L$ if for all $ _1, _2 R ^M$, $\ | f( _1) $ $ - f( _2) \ | ≤ \| _1 - _2 \|_2$. ; is $β$-smooth if for all $ _1$, $ _2 R ^M$, $ \|f( _1) - f( _2) \| ≤ β · \| _1 
  Observation Higher Utility under the Same or Stricter Privacy Guarantees: The model using product noise consistently achieves higher test accuracy in all considered tasks for convex ERM under the same privacy parameter. Notably, on specific datasets, the performance even surpasses that of the non-private baseline. For some datasets, even under stricter privacy guarantees (i.e., smaller $ε$), the product noise-based method achieves higher accuracy than the other noise in convex ERM (ref. Section~ [ref] and~ [ref] ).
  Definition PLRV~\cite{bun2016concentrated: Let $ x , x ' N ^ | X | $ be two neighboring datasets, $ M : N ^ | X | S $ be a randomized mechanism, and $P$ and $Q$ be the distributions of $ M ( x )$ and $ M ( x ')$, respectively. PLRV associated with $ M $ and a randomized output $ s $ is defined as $ _ M (s) = ln ( d P( M ( x )= s ) d Q( M ( x ')= s ) )$, where $ d P( M ( x )= s ) d Q( M ( x ')= s ) $ denotes the Radon-Nikodym derivative of $P$ with respect to $Q$ evaluated at $s S $.
  Lemma : If $X_ 1 ,X_ 2 ,...$ are random points sampled from $ S ^ M-1 $ (the unit sphere embedded in in $ R ^ M $) uniformly at random, $M>2$. Let $Θ_ ij $ be the angle between $ OX_ i $ and $ OX_ j $ and $x_ ij =cosΘ_ ij $ for any $i ≠ j$. Then $ \ x_ ij ; 1≤ i < j ≤ n \ $ are pairwise independent and identically distributed with density function f_ X (x) = 1 √ ( M 2 ) ( M-1 2 ) (1 - x^2)^ M-3 2 for |x| < 1, where $x$ denotes $ θ$.
  Definition Beta distribution and Beta Prime distribution~\cite{johnson1995continuous: A random variable $X$ has a Beta distribution, i.e., $X~ (α,β)$, if it has the following probability density function f_ X (x)= 1 B(α,β) x^ α-1 (1-x)^ β-1 , where $α>0, β>0, 0 ≤ x≤ 1$, and $B(α, β) = (α) ( β) ( α+β) $ is the Beta function. A random variable $X$ has a Beta Prime distribution, i.e., $X~ (α,β)$, if it has the following probability density function f_ X (x)= 1 B(α,β) x^ α-1 (1+x)^ -α-β , where $α>0, β>0, x>0$.
  Proof: Proposition~ [ref] can be proved by using transformation of random variables. First, we recall the following Lemma from [cite] . If $X_ 1 ,X_ 2 ,...$ are random points sampled from $ S ^ M-1 $ (the unit sphere embedded in in $ R ^ M $) uniformly at random, $M>2$. Let $Θ_ ij $ be the angle between $ OX_ i $ and $ OX_ j $ and $x_ ij =cosΘ_ ij $ for any $i ≠ j$. Then $ \ x_ ij ; 1≤ i < j ≤ n \ $ are pairwise independent and identically distributed with density function f_ X (x) = 1 √ ( M 2 ) ( M-1 2 ) (1 - x^2)^ M-3 2 for |x| < 1, where $x$ denotes $ θ$. Lemma~ [ref] applies to two random unit vectors and also holds when one vector is fixed while the other is randomly sampled from the unit sphe
  Proof: The $q$-th moment of random variable $U$ is & E [U^q] \\ =& _ 1 ^ ∞ u^ q f_ u (u)du (a) = 2 √ ( M 2 ) ( M-1 2 ) _ 1 ^ ∞ u^ q (u^2-1)^ - 1 2 u^ 1-M du \\ =& 2 √ ( M 2 ) ( M-1 2 ) _ 1 ^ ∞ u ^ q-M+1 (u^2-1 )^ - 1 2 1 2 u^ -1 d u^2 \\ =& 1 √ ( M 2 ) ( M-1 2 ) _ 1 ^ ∞ u ^ q-M (u^2-1 )^ - 1 2 d u^2 \\ =& 1 √ ( M 2 ) ( M-1 2 ) _ 1 ^ ∞ (t+1) ^ q-M 2 t^ - 1 2 d t let $t=u^2-1$ \\ =& 1 √ ( M 2 ) ( M-1 2 ) B ( 1 2 , M-q-1 2 ) _ 0 ^ ∞ 1 B ( 1 2 , M-q-1 2 ) t^ - 1 2 (t+1)^ q-M 2 dt _ =1, \ integrand is the kernel of \\ t~ ( 1 2 , M-q-1 2 ) \\ =& 1 √ ( M 2 ) ( M-1 2 ) B ( 1 2 , M-q-1 2 ) \\ =& 1 √ ( M 2 ) ( M-1 2 ) ( 1 2 ) ( M-q 2 - 1 2 ) ( M-q 2 ) \\ =& ( M 2 ) ( M-1 2 ) ( M-q 2 - 1 2 ) ( M-q 2 ) ( $1<q<
  bound: Solving for $δ = min _ q δ_1δ_2$ delta = min delta1 delta2 Since $δ = δ_1δ_2$ involves the confluent hypergeometric function, which is cumbersome to work with, we first consider an approximation of $δ$ by deriving it upper bound.
  bound: As a result, an upper bound of $δ$ in ( [ref] ) takes the following form size [display] Since for any $1<q<M- 3 2 $, we have $1>√ 2 λ$, $λ^2q> √ 2 λ^3 3 q$, $λ^2> 2√ 2 3 λ^3$.
  bound: Then we set $ σ _ M_1 2L n · √ 2 k^ 2 M ( M 4 + 3 2 )^ ( 1 2 + 2 M ) ε_3 e^ ( 1 2 + 1 M ) $ for $ε_3 < ε_1$, we can get the proof of the Lemma from the privacy guarantee of product noise in Theorem [ref] . [ [cite] ] For any pair of neightporing datasets \( D, D^ D ^n\), and \( ε
  bound: To guarantee that $ L ( ; D)$ is $ n $-strongly convex function, it is required that $ n \| n _1 \|_2 σ _ M_1 √ r $ (where $r$ is the minimum of $M$ and twice the upper bound on the rank of $ $'s Hessian) and $ n \| n _2 \|_2 L σ _ M_2 $ for $ L ( ; D)$.
  bound: ( [ref] )). [Utility Guarantee of Product Noise-Based AMP (i.e., Algorithm [ref] )] Let $ = min _ R ^M 1 n Σ_ i=1 ^ n ( ; d_i)$, and $r = min\ M, 2 · ( upper bound on rank of 's $ $ Hessian ) \ $.
  bound: The PLRV of perturbation using the product noise proposed in ( [ref] ) has the following upper bound [display] where $R~ _1$ and $θ$ is the random angle formed by a random noise $ n $ and $ v $.

## sigmod26-259 — A Bouquet of Results on Maximum Range Sum: General Techniques and Hardness Reductions  (score 46)
domain: spatial
usage: Grid-shifted random sampling of points with Chernoff plus union bound for weighted-depth concentration; guard-point encodings reduce (min,+)-convolution to batched MaxRS; trapezoidal maps for exact colored enumeration.
note-quote: "Assuming that (min,+)-convolution on sequences of length n requires Ω(n²) time, the batched MaxRS problem on n points and m query interval lengths requires Ω(mn) time."
  Lemma : Let $C$ be a $d$-ball, and let $ ' $ be a collection of $k$ weighted $d$-balls, such that the $(d -1 )$-dimensional measure of $ C B$ relative to the $(d -1 )$-dimensional measure of $C$ is $ 1 2 -Θ( )$, for all $B '$ In $ ^2$, $C$ will be a disk and any disk in $ '$ will cover at least $ ( 1 2 -Θ( ) )$ fraction of $C$'s circumference. We also remark that the lemma holds in a more general setting. . Let $S=\ p_1,p_2,…,p_t\ $ be a collection of points such that each point in $S$ is independently 
  Theorem : For dynamic MaxRS with $Q$ being a $d$-ball, there is a randomized $( 1 2 - )$-approximation algorithm with amortized update time $O( ^ -2d-2 log n)$, for $0 < < 1/2$. The approximation factor holds with high probability, i.e., $1-n^ -Ω(1) $. Here $n$ is the number of points of $P$ in a given round.
  Definition Monotone $(\min,+)$-convolution: Given two sequences \( D = (D_0, D_1, , D_ n-1 ) \) and \( E = (E_0, E_1, , E_ n-1 ) \), such that \( D_0>D_1>…> D_ n-1 \) and \( E_0> E_1> …> E_ n-1 \), the monotone $(min,+)$-convolution produces a new sequence \( F \), where for each $k \ 0,…,n-1\ $, \[ F_k = min_ i + j = k (D_i + E_j). \]
  Lemma : Let $B$ be a unit radius $d$-ball and $C$ be a $d$-ball of radius $ $. Suppose $ B $ passes through a point that lies at distance $ ^2$ from the center of $C$. Then the $(d - 1)$-dimensional measure of $B C $ relative to the $(d - 1)$-dimensional measure of $C$ is at least $ 1 2 -Θ( )$.
  Theorem : For colored MaxRS with $Q$ being a $2$-ball, there is a randomized $(1- )$-approximation algorithm with expected running time $O( ^ -2 nlog n)$, for $0 < < 1$. The approximation factor holds with high probability.
  Theorem : For static MaxRS with $Q$ being a $d$-ball, there is a randomized $( 1 2 - )$-approximation algorithm with running time $O( ^ -2d-2 nlog n)$, for $0 < < 1/2$. The approximation factor holds with high probability.
  Lemma : (Theorem 2 in [cite] ) The trapezoidal map a set of $n$ circular $x$-monotone arcs with $k$ intersection points can be computed in $O(nlog n + k)$ expected time. The map has $O(n+k)$ vertices, edges and faces.
  Theorem : Assuming that $(min,+)$-convolution on sequences of length $n$ requires $Ω(n^2)$ time, the batched MaxRS problem on $n$ points and $m$ query interval lengths requires $Ω(mn)$ time.
  Proof: We will use an indirect argument to establish the lemma. Consider any ball $B '$. We will first establish that with high probability at least $ ( 1 2 -Θ( ) )t$ points of $S$ will lie inside $B$. For $1≤ i ≤ t$, let $X_i$ be the indicator random variable which is equal to $1$ if $p_i B$. Let $Y_B=Σ_ i=1 ^t X_i$ be the number of points in $S$ contained in $B$. Then, $ E [Y_B] ≥ ( 1 2 -Θ( ) )t$. Since $X_1,X_2,…, X_t$ are independent random variables, via standard application of Chernoff bound, with high probability (w.h.p.), we have $Y_B ≥ (1- ) ( 1 2 -Θ( ) )t ≥ ( 1 2 -Θ( ) )t$. Next, by union bound on $k≤ n$ balls, $Y_B ≥ ( 1 2 -Θ( ) )t$ holds w.h.p. for all $B '$. Now we will connect the ran
  Proof: Via suitable rotations of the $d$-balls, we can assume that the center of $C$ is at the origin and the center of $B$ is $(0,0,…,0,1+ ^2)$. See Figure~ [ref] (a). For any point $x = (x_1, ,x_d) B C $, $ x = ^2$ and $ x-(0, ,0,1+ ^2) = 1$. Expanding these two quantities, we get the following equations: Σ_ i=1 ^ d x_i^2 = ^2 and Σ_ i=1 ^ d-1 x_i^2 + (x_d - 1 - ^2)^2 = 1, which solves to $x_d = 3 ^2 + ^4 2+2 ^2 $. We denote the expression on the right by $b$. It can be verified that $ ^2 ≤ b ≤ 2 ^2$ for all $ (0,1)$. In $d=2$, suppose $B C $ subtends an angle $2θ$ at the center of $C$ (ref Figure~ [ref] (a)). Since $ θ = b/r$, the arc length of $B C $ relative to the circumference of $C$ is 2θ r
  bound: We establish a conditional lower bound of $Ω(mn)$ time for this problem, assuming the hardness of $(min,+)$-convolution problem.
  bound: Interestingly, this implies that the trivial upper bound of $O(mnlog n)$ for batched MaxRS in $ ^2$ is almost-tight.
  bound: A similar lower bound is established for a related problem of batched smallest $k$-enclosing interval . ; In the colored MaxRS problem in $ ^d$, each point in $P$ is assigned a color from $\ 1,2,…,m\ $ and the goal is to find the placement of a $d$-ball $Q$ that maximizes the num
  bound: Specifically, we establish an $Ω(mn)$ time conditional lower bound for batched MaxRS in $ ^1$ assuming hardness of the popular $(min,+)$-convolution problem.
  bound: We establish a conditional lower bound of $Ω(n^2)$ for this problem as well.
  bound: Recall that even (uncolored) MaxRS for $Q$ being a $2$-ball has a conditional lower bound of $Ω(n^2)$~ [cite] (and in fact we conjecture a conditional lower bound of $Ω(n^d)$ for colored MaxRS for $Q$ being a $d$-ball).

## sigmod26-012 — Acyclic Graph Pattern Counting under Local Differential Privacy  (score 44)
domain: privacy-dp | graph-db
usage: Parallel/basic DP composition, Laplace concentration, Chernoff on effective degrees, Chebyshev for sampling error; random marking reduces path counting to walk counting.
note-quote: "The k-round mechanism satisfies E[M(G,ε)] = W_k, and with probability at least 1−β, |M(G,ε) − W_k| ≤ kγ√N(d(G)+γ)^{k−1} = Õ(√(N·d(G)^{k−1}))."
  Definition 3: 1 (Differential Privacy). A randomized mechanism M : G → K satisfies 𝜀-DP if, for any pair of neighboring graphs 𝐺, 𝐺 ′ and any measurable subset S ⊆ K, it holds that Pr[M (𝐺) ∈ S] ≤ 𝑒 𝜀 Pr[M (𝐺 ′ ) ∈ S]. Two graphs 𝐺 and 𝐺 ′ are said to be node-neighboring if they differ by exactly one node and all of its incident edges. Similarly, they are edge-neighboring if they differ by exactly one edge. Applying these notions to the DP definition yields the concepts of node-differential privacy (node-DP) 
  Lemma 3: 2 (Post-Processing). Let M : G → K be a randomized mechanism that satisfies 𝜀-DP. Then for any (possibly randomized) function F : K → Z, the mechanism F ◦ M : G → Z also satisfies 𝜀-DP. Proc. ACM Manag. Data, Vol. 4, No. 3 (SIGMOD), Article 129. Publication date: June 2026. 129:6 Yihua Hu, Kuncan Wang, and Wei Dong
  Theorem 4.1: For any graph 𝐺, walk length 𝑘, and privacy budget 𝜀, Mk-wk satisfies 𝜀-edge-LDP.
  Theorem 4.2: For any graph 𝐺, walk length 𝑘, and parameters 𝜀 and 𝛽, the 𝑘-round mechanism Mk-wk satisfies E[Mk-wk (𝐺, 𝜀)] = 𝑊𝑘 , and with probability at least 1 − 𝛽, √ √ |Mk-wk (𝐺, 𝜀) − 𝑊𝑘 | ≤ 𝑘𝛾 𝑁 (𝑑 (𝐺) + 𝛾)𝑘 −1 = 𝑂˜ ( 𝑁𝑑 (𝐺)𝑘 −1 ), √︁ where 𝛾 = 2𝑘 8 log(2𝑘𝑁 /𝛽)/𝜀 = 𝑂˜ (1). Proofs of all lemmas and theorems in this paper are provided in Appendix A.3 of the full version [29]. By Theorem 4.2, Mk-wk successfully reduces the error from typically 𝑂 (𝑁 𝑘 ) to √ 𝑂˜ ( 𝑁𝑑 (𝐺)𝑘 −1 ) for general 𝑘-line walk counting
  Theorem 4.3: For any graph 𝐺, walk length 𝑘, and privacy budget 𝜀, the optimized mechanism Mk-wk satisfies 𝜀-edge-LDP.
  Theorem 4.4: For any graph 𝐺, walk length 𝑘, and parameters 𝜀 and 𝛽, the optimized (𝑘 − 1)-round mechanism Mk-wk satisfies E[Mk-wk (𝐺, 𝜀)] = 𝑊𝑘 , and with probability at least 1 − 𝛽, √ √ |Mk-wk (𝐺, 𝜀) − 𝑊𝑘 | ≤ 𝑘𝛾 𝑁 (𝑑 (𝐺) + 𝛾)𝑘 −1 = 𝑂˜ ( 𝑁𝑑 (𝐺)𝑘 −1 ), Proc. ACM Manag. Data, Vol. 4, No. 3 (SIGMOD), Article 129. Publication date: June 2026. Acyclic Graph Pattern Counting under Local Differential Privacy 129:11 √︁ where 𝛾 = 2𝑘 8 log(2𝑘𝑁 /𝛽)/𝜀 = 𝑂˜ (1). 5 LDP Path Counting Now, we extend our LDP solution for wal
  Lemma 5.1: For any graph 𝐺, path length 𝑘, and parameter 𝛽, the 𝑘-round non-private algorithm ck-pt satisfies E[M ck-pt (𝐺)] = 𝑃𝑘 , and with probability at least 1 − 𝛽, M √︁ √ ck-pt (𝐺) − 𝑃𝑘 | ≤ (𝑘 + 1) 𝑁 𝛽 −1 (𝑑 (𝐺) + 𝑘 + 1)𝑘 = 𝑂 ( 𝑁𝑑 (𝐺)𝑘 ). |M 5.2 Integrating DP and Efficiency Optimization With Algorithm 3, we successfully reduce the problem of 𝑘-line path counting to that of 𝑘-line walk counting. We can then utilize the idea of the edge-LDP mechanism Mk-wk to privatize node outputs from Algorithm 3. To
  Theorem 5.2: For any graph 𝐺, path length 𝑘, and privacy budget 𝜀, the mechanism Mk-pt satisfies 𝜀-edge-LDP. For utility analysis, the total error |Mk-pt (𝐺, 𝜀) −𝑃𝑘 | consists of two parts. The first is the sampling error incurred by random marking, as shown in Lemma 5.1. For the second part, the DP noise introduced by the Laplace mechanism, we show that it slightly differs from that of 𝑘-line walk counting but admits the same asymptotic upper bound.
  Proof: We first show that the computation in each round ℓ ∈ {1, . . . , 𝑘 } satisfies 𝜀/𝑘-edge-DP. Let the differing edge between edge-neighboring graphs 𝐺 and 𝐺 ′ be (𝑣𝑖 , 𝑣 𝑗 ). In each round ℓ, the computations at nodes 𝑣𝑖 and 𝑣 𝑗 are affected by this edge: the presence of (𝑣𝑖 , 𝑣 𝑗 ) contributes an additional X𝑗(ℓ −1) to 𝑣𝑖 ’s result, and X𝑖(ℓ −1) to 𝑣 𝑗 ’s result. Therefore, the per-node global sensitivity in round ℓ is upper bounded by ∥X (ℓ −1) ∥ ∞ , which is the maximum of all nodes’ absolute results at round ℓ−1. Applying the Laplace mechanism, adding a Laplace noise of scale 2𝑘 ∥X (ℓ −1) ∥ ∞ /𝜀 as in Line 6 of the Algorithm 1 ensures 𝜀/2𝑘-edge-DP for each node at each round. Since a singl
  Proof: At round ℓ ∈ {1, . . . , 𝑘 }, for any 𝑖, ∑︁ 2𝑘 X𝑖(ℓ ) = X𝑗(ℓ −1) + Lap( ∥X (ℓ −1) ∥ ∞ ). 𝜀 (1) 𝑗 ∈ N (𝑖 ) It follows immediately that ∑︁ X𝑗(ℓ −1) ≤ 𝑑 (𝐺) ∥X (ℓ −1) ∥ ∞ . 𝑗 ∈ N (𝑖 ) Starting from the first round, we allocate a failure probability of 𝛽 ′ /𝑁 per node per round to upper√︁ bound the added Laplace noise. Let 𝛾 = 2𝑘 8 log(2𝑁 /𝛽 ′ )/𝜀, applying the Laplace concentration bound (Lemma 3.6), with probability at least 1 − 𝛽 ′ /𝑁 , the Laplace noise added at any node in round ℓ satisfies 2𝑘 Lap( ∥X (ℓ −1) ∥ ∞ ) ≤ 𝛾 ∥X (ℓ −1) ∥ ∞ . 𝜀 (1) Thus, ∥X ∥ ∞ = 𝑑 (𝐺) + 𝛾, and we conclude by induction that ∥X (ℓ ) ∥ ∞ ≤ (𝑑 (𝐺) + 𝛾) ℓ (2) for any ℓ ≤ 𝑘 − 1 with probability at least 1 − (𝑘 − 1)𝛽 ′ . 
  bound: Therefore, by summing the variances over all oriented 𝑘-line walks consisting of distinct nodes (and hence distinct edges), we obtain a lower bound on the variance of 𝑊ˆ 𝑘 as follows: ∑︁ Var(𝑊ˆ 𝑘 ) ≥ Var(𝑍ˆ𝑤 ) 𝑤:| Edist (𝑤 ) |=𝑘 ≥ 𝑒 𝜀𝑘 · Ω(𝑁 𝑘+1 ) (𝑒 𝜀 − 1) 2𝑘 = Ω(𝑁 𝑘+1 ).

## sigmod26-079 — DP-S4S: Select-Join-Aggregate Query Processing with User-Level DP  (score 41)
domain: privacy-dp | aqp
usage: Rényi-divergence convexity and Bernstein/Chernoff bounds drive amplification and error proofs; SVT searches the truncation threshold.
note-quote: "Algorithm 2 satisfies (ε,0)-DP; with probability 1−β its error is Õ(√(n/q) + 1/q + τ*(T) + τ*(T)/(qε*))."
  Definition Differential Privacy~\cite{DBLP:journals/fttcs/DworkR14: For $ >0$ and $δ≥ 0$, a randomized mechanism $ : I O $ is $( ,δ)$-DP, if for any pair of neighboring database instances $ I ~ I '$ and any subset of outputs $O O $, it holds that \[ [ ( I ) O]≤ e^ · [ ( I ') O]+δ\,. \] When $δ=0$, we say the mechanism satisfies pure DP . Otherwise (i.e., when $δ>0$), we say the mechanism satisifes approximate DP . Note that in the above definition, the concept of ``neighboring'' database instances $ I ~ I '$ is left abstract, which we will clarify with concrete 
  Lemma : Let $ Err _ M _ (B( I ); ,δ,β)$ be the error of a local-sensitivity mechanism $ M _ $ for function $F$ under $( ,δ)$-DP, given bound $B( I )≥ _F( I )$ and $|B( I )-B( I ')|$ for neighboring datasets. The noise that PMSJA adds to each query is \[ σ_ PMSJA = O ( ^*( I )· Err ( 1 ; ,δ,β ) ) \] for $ ^*( I ):=max_u \| x _u\|_2$, where $ x _u=(x_ 1,u , ,x_ |F|,u )$ is the contribution of user $u$ to all queries. Specially, when $ M _ $ is the privately bounding local sensitivity mechanism~ [cite] , i
  Lemma Gaussian R\'enyi divergence~\cite{DBLP:journals/isci/GilAL13: Let $ N ( μ , Σ )$ denote the multivariate Gaussian distribution, then & D _α( N ( _1, Σ_1)\| N ( _2, Σ_2))\\ =& α 2 ( _1- _2)^T[Σ_α]^ -1 ( _1- _2)- 1 2(α-1) ln |Σ_α| |Σ_1|^ 1-α |Σ_2|^α if $αΣ_1^ -1 +(1-α)Σ_2^ -1 $ is positive definite, otherwise infinite, where $Σ_α=(1-α)Σ_1+αΣ_2$. In particular, when the noise added to each dimension is i.i.d., we have & D _α( N ( _1, σ_1^2 I _d)\| N ( _2, σ_2^2 I _d))\\ =& α 2 · \| _1- _2\|_2^2 (1-α)σ_1^2+ασ_2^2 + d 2(α-1) ln σ_1^ 2(1-α) σ_2^ 2α (1-α)σ_1^2+ασ
  Lemma Smooth-sensitivity Mechanism~\cite{DBLP:conf/stoc/NissimRS07: Given $F: I R ^d$ and its smooth-sensitivity function $ SS _F: I × R R $ as in Lemma~ [ref] , the smooth-sensitivity mechanism is defined as \[ _ SS ( I ):=F( I )+ · SS _F( I ,γ)· Z\,. \] Its privacy guarantee is as follows. ; If $γ= 6d $, $ = 6 $ and $Z~ Cauchy ^d(0,1)$, then $ _ SS-C $ satisfies $( ,0)$-DP. ; If $γ = 4(d+ln(2/δ)) $, $ = 2 $ and $Z~ ^d(0,1)$, then $ _ SS-L $ satisfies $( ,δ)$-DP. ; If $γ = 4(d+ln(2/δ)) $, $ = 5√ 2ln(2/δ) $ and $Z~ N (0, 1 _d)$, then $ _ SS-G $ satisfies $( ,δ)$
  Lemma : Algorithm~ [ref] satisfies $( ,0)$-DP. Let $n$ be the number of records, $m$ be the number of users and $q$ be the sample rate. With probability $1-β$, its error is \[ | f (T)-f(T)|= O (√ n q + 1 q + 1 _ i^* ( ^*(T)+√ ^*(T) q ) )\,, \] where $ O (·)$ omits constants and polylogarithmic factors in $m$ and $1/β$; $ ^*(T):=max_ u U Σ_ t T 1 [u t]$ is the maximum number of tuples a user contribute to in $T$; and $ _ i^* ≥ /L$ is the allocated noise‑calibration budget where $L= +1$.
  Lemma Simplified Computation: Algorithm~ [ref] satisfies $ '$-user-level DP for '( , , ,q) & = max \ ( logcdf ( , ,q_1) + · log A_1) ( logsf ( , ,q)+ ),\\ & - (( logcdf ( , ,q_2)+ · log A_2) ( logsf ( , ,q)- ) ) \ \,, where $ $ denotes the $ LogSumExp $ function, $ logcdf $ and $ logsf $ are the cumulative distribution function and survival function for the binomial distribution respectively, $A_1=1-q+q ( / )$, $q_1=q· exp( / )/A_1$; $A_2=1-q+q (- / )$, $q_2=q· exp(- / )/A_2$.
  Corollary : Assuming each user can have at most $C$ collaborators, let $λ_k: T T $ be the Sample-and-Explore process for $k$ sampled users, and let $ : T O $ be an $( ,δ)$-user-level DP mechanism. Then, $ λ_k$ satisfies $( ',δ')$-user-level DP for $ '=ln(1+(1-P)·(e^ 2 -1))$ and $δ'=(1-P)(e^ +1)δ$, where $P=0$ if $k>m/(C+1)$ and $P= m-kC k / m k $ otherwise. When $δ=0$, it suffices to take $ '=ln(1+(1-P)·(e^ -1))$.
  Lemma RDP Smooth Sensitivity Mechanism: Given $F: I R ^d$ and its smooth-sensitivity function $ SS _F: I × R R $ as in Lemma~ [ref] , the RDP-SS mechanism defined as \[ _ RDP-SS ( I ):=F( I )+σ( I )· N (0, 1 _d) \] satisfies $(α, )$-RDP for \[σ( I )= SS _F( I ,γ) 1-α+α e^ -2γ · √ α \,,\] where $γ:= 1 2 lnmin\ t_1^ -1 , t_2\ $, and $0<t_1<1<t_2$ are solutions to \[ h(t):=t^α-α e^ (α-1) d t +(α-1)e^ (α-1) d =0\,. \]
  Proof: Denote by $f(S):=Σ_ t S w_t$. Note that $f(S)=Σ_ t T 1 [t is sampled ]· w_t$. Therefore \[ E [f(S)]=q· f(T), and Var [f(S)]=q(1-q)Σ_ t T w_t^2≤ qn\,. \] We have $ [ | ( _i _i ) |≤ _i _i ln 3L β ]>1- β 3L $. By a union bound, this holds for all iterations with probability at least $1-β/3$. Conditioned on this happening, $ f (S, _i)$ is always an underestimate since $ f (S, _i)≤ f (S, _i)≤ f(S)$ and $ f (S, _ i^* )≥ f(S) - 2( _ i^* / _ i^* )ln(3L/β)$. Let $ ^*(S):=max_ u U Σ_ t S 1 [u t ]$ be the maximum number of records contributed by a user in the sample. For any $ ≥ ^*(S)$, the optimal solution to problem [ref] assigns $x_t=w_t$ so that $ f (S, )=f(S)$. In particular, this holds for a powe
  Proof: Consider $ I ~ I '$, let $σ( I )=σ_1$ and $σ( I ')=σ_2$. By the smoothness of $ SS _F$, we have \[ e^ -2γ ≤ σ_2^2 σ_1^2 = SS _F( I ',γ)^2 SS _F( I ,γ)^2 ≤ e^ 2γ \] According to Lemma~ [ref] , the divergence between the outputs is \[ D_α( ( I )\| ( I '))= α 2 · \|F( I )-F( I ')\|_2^2 (1-α)σ_1^2+ασ_2^2 + d 2(α-1) ln (σ_2/σ_1)^ 2α α(σ_2/σ_1)^2+1-α \] when $α(σ_2/σ_1)^2+1-α>0$, which can be satisfied when \[ σ_2^2 σ_1^2 ≥ e^ -2γ > α-1 α \,, \] or $γ< 1 2 ln α α-1 $. For the first term, we have α 2 · \|F( I )-F( I ')\|_2^2 (1-α)σ_1^2+ασ_2^2 &≤ α 2 · ( I )^2 σ_1^2· (α(σ_2/σ_1)^2+1-α) \\ &≤ 2 · 1-α+α e^ -2γ 1-α+α (σ_2^2/σ_1^2) \\ &≤ 2 \,. For the second term, we require that \[ d 2(α-1) ln (σ_2/σ_1
  bound: The upper bound on the number of collaborators $C$ for S\&E can also be computed from $D$.
  bound: This leads to a favorable privacy-accuracy trade-off, and DP-S4S consistently outperforms S\&E . fig/amazon2-eps fig/c2a-eps fig/amazon2-d Degree upper bound $D$ (equivalently $C$ and $ $) .
  bound: For graph queries, there are two reasons that account for a large $ $: on the one hand, the degree upper bound $D$ should be a data-independent worst-case upper bound.
  bound: With truncation, the query engine can impose an upper bound, e.g., up to 10 edges are counted for each user, which reduces the query sensitivity dramatically, leading to a lower noise requirement to satisfy DP, as we explain in Section~ [ref] .
  bound: Prior work R2T ~ [cite] addresses this by considering all possible truncation thresholds that are a power of two, which guarantees asymptotically optimal result accuracy, as we elaborate further in Section~ [ref] .
  bound: Since DP offers information-theoretic privacy guarantees, we do not rely on any assumption on the computational power of the adversary.
