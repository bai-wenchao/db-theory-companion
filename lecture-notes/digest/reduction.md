# Chapter digest: reduction
n_papers_with_tool: 50
PODS exemplars are from the PODS'26 issue inside PACMMOD Vol 4 — mark them ^PODS.

## sigmod26-136 [PODS] — Improved Lower Bounds for Privacy under Continual Release  (score 72)
domain: privacy-dp | streaming
usage: Gadgets embed inner-product queries into graph streams; packing/reidentifiable-distribution arguments yield sample-complexity bounds; binary-tree mechanisms plus composition give near-matching upper bounds.
note-quote: "Any (ε,δ)-DP incremental mechanism ... for maintaining the size of the maximum matching, degree histogram, or core number of a vertex must have additive error α = Ω(min{T^a, n^b})."
  Theorem 1: Let ε ∈ [0, 1] and δ ∈ [0, 1/3]. Any (ε, δ)-DP incremental mechanism in the exact setting for maintaining the size of the maximum matching, degree histogram, or core number of a vertex0 must have additive error α = Ω(min{T a , nb }), for problem-specific constants a, b ∈ [1/8, 1/2] given in Table 1. Our polynomial lower bounds for the real-valued graph problems are tight in the sense that relaxing to either the static setting or to small multiplicative approximations admit private mechanisms wit
  Theorem 2: There exist (ε, δ)-DP incremental mechanisms in the exact setting for maintaining the size of the eε,δ (min{T c , nd }) maximum matching, degree histogram, or core number of a vertex with additive error1 α = O for problem-specific constants c, d ∈ [1/6, 1/2] which are given in Table 1. Norm estimation problem. We show that our lower bound techniques also extend from graph mechanisms to a standard problem for data streams, namely the incremental norm estimation problem. This problem requires main
  Theorem 3: Let ε ∈ [0, 1] and δ ∈ [0, 1/3]. Any incremental (ε, δ)-DP mechanism in√the exact setting for √ TopK that runs with n elements for T timesteps must have additive error α = Ω(min{ 4 T , n}). This is the first lower-bound for this problem, even in the fully-dynamic setting. We complement this result by showing that the problem admits polylogarithmic additive error both in the static setting and 0 1 These problems are formally defined in Section 2. e Here Oε,δ hides poly(1/ε, log(1/δ)) factors, and
  Theorem 4: Let ε >  0 and ζ ∈ (0, 1/2].  There exists an ε-DP (1 + ζ)-approximate mechanism for IncSNE 6 1 e with additive error O ε2 ζ 5 · log (nT ) · L(e1 ) for any monotone symmetric norm L, where L(e1 ) is the norm of the first standard basis vector.2 Question 2. As our final result, we show that the item-level lower bounds of [RS25] cannot be circumvented by allowing multiplicative error: we give a polynomial lower bound on the product of the additive and multiplicative errors. This implies that the
  Theorem 5: Let ε, δ ∈ [0, 1]. Any (ε, δ)-DP mechanism in the item-level fully dynamic setting with multiplicative approximation η and additive error α for maintaining the size of the maximum matching, subgraph counts, degree histogram, mincut, s-t mincut, or core number of a vertex on n-node graphs for T ≥ Ω(1/δ) time steps must have ( √ Ω(min{ T , n}) when δ = 0 √ η·α= e Ω(min{ 3 T , n}) when 0 < δ < o(1/T ) √ √ for edge count, the lower bounds are Ω(min{ T , n2 }) for δ = 0 and Ω(min{ 3 T , n2 }) for δ ∈
  Lemma 1: Let ε ∈ [0, 1] and δ ∈ [0, 1/3], and let g be a graph problem that admits an extendable bitwise AND gadget with v(d) vertices and t(d) edges. For any (ε, δ)-DP mechanism for the event-level setting that is (i) solving g in the exact setting and (ii) running on n-node graphs for T timesteps with additive error α, it holds that  np o p α = Ω min t− (T ), v − (n) , where f − is the generalized inverse of a function f : N → N defined as f − (x) = sup{d | f (d) ≤ x}.
  Lemma 2: There exist extendable bitwise AND gadgets for the following problems and parameters: IncMatching : v(d) = O(d2 ) and t(d) = O(d2 ), IncDegHist : v(d) = O(d) and t(d) = O(d2 ), IncKCore : v(d) = O(d2 ) and t(d) = O(d4 ). We prove this lemma in Section 3.1. Combining Lemma 2 with Lemma 1, we get Theorem 1. 17 Figure 1: Lower bound for IncMatching. The core idea of the reduction is to note that in Qjx , there is an augmenting path for the pink matching if and only if both xi and qij are set to 1. 
  Lemma 3: There exist extendable bitwise AND gadgets for IncMatching with v(d) = O(d2 ) and t(d) = O(d2 ).
  Proof: Let A be a mechanism that is solving g in the exact setting with additive error α. We denote by A(G) the query value from the mechanism on the graph G. The accuracy guarantee tells us that with probability 2/3, for any graph G generated during the execution of the mechanism, ∥g(G) − A(G)∥ ≤ α. We condition on the event that the accuracy guarantee holds, and given an instance of InnerProduct of dimension d, we show how to privately answer it with an additive error of at most 2α using the 3 This is because t(d) is a positive and increasing function. 16 mechanism for solving g in the exact setting. The reduction is given in Algorithm 1. We choose d to be d = min {t− (T ), v − (n)}, so n′ = v(d)
  Proof: We present the gadget in Figure 1. Formally, Gadget construction. 1. Hinit has vertex set (U j ∪V j )j∈{0}∪[m] ∪U m+1 with |V j | = |U j | = d for all j, and a perfect matching between V 0 and U 1 given by ∪i∈[d] (vi0 , u1i ). 2. ei = (u0i , vi1 ) for all i ∈ [d]. 3. S j = ∪i:qj =1 (uji , vij ), which consists of a matching of all edges between U j and V j whose index i corresponds to the index of a query bit that is set to 1.     j j j , which consists of a perfect matching between V j 4. T j = ∪i∈[d] (vij , uj+1 ) ∪ ∪ (u , v ) \ S i∈[d] i i i and U j+1 and additionally edges between U j and V j to form a perfect matching between them as well. Properties of Hxj and Qjx . Note that for a

## sigmod26-289 — Clustering with Set Outliers and Applications in Relational Clustering  (score 66)
domain: + clustering-outliers
usage: LP feasibility plus greedy rounding with weight-charging arguments; Set-Cover reductions via line-point encodings; coresets shrink instances; randomized repetition over join samples.
note-quote: "If the Unique games conjecture is true, there exists no (1, f−ε, γ)-approximation algorithm that runs in polynomial time for the problem, where f = o(log n)."
  Theorem : Given a set $P$ of $n$ points in $ ^d$, a set $ $ of $m$ hyper-rectangles in $ ^d$, where $d$ is a constant, such that every point in $P$ belongs to one hyper-rectangle from $ $, positive integer parameters $k, z$, and a small constant $ (0,1)$, there exists a $(2+ , 2,O(1))$-approximation algorithm for the $ (P, ,k,z)$ problem that runs in $O(((n+m)log^ d-1 n +kmlog n+β_1log(β_1)+(k+z)(β_2+β_3)log^ d+2 (β_2)) n)$ time, where $β_1=min\ n,km\ $, $β_2=min\ n,kz\ $, and $β_3=min\ m,kz\ $.
  Definition $\gprob(P,\setrects,k,z)$: Given a set $P$ of $n$ points in $ ^d$, a set $ $ of $m$ hyper-rectangles in $ ^d$, where $d$ is a constant and every point in $P$ lies in at least one hyper-rectangle in $ $, and two integer positive parameters $k, z$, the goal is to compute a set of centers $C P$ with $|C|≤ k$ and a set $ $ of hyper-rectangles as outliers with $| |≤ z$ and $C ( _ P)= $, such that the cost excluding points covered by outliers $ (C,P ( _ P))$ is minimized.
  Theorem : For an acyclic join query $ $ of $d$ attributes, a database $ $ of input size $N$ with $| ( )|= (N)$, integer positive parameters $k, z$, and a small constant $ (0,1)$, there exists an $(1,1+ ,3+ )$-approximation for the $ ( , ,k,z)$ problem with probability at least $1- 1 N $ that runs in $O (Nlog N + k^2 δ log^3 N )$ time, where $δ= z | ( )| $. If $| ( )|=O(N)$, then the running time is bounded by $O(kNlog^2 N)$.
  Definition $\prob(P,\mathcal{H},k,z)$: Given a set $P$ of $n$ elements, a family $ H $ of $m$ subsets of $P$, where every element in $P$ belongs to at least one set in $ H $, and two integer positive parameters $k, z$, the goal is to compute a set of centers $C P$ with $|C|≤ k$ and a family $H H $ of outlier sets with $|H|≤ z$ and $C ( _ h H h)= $ such that the cost excluding the points covered by outliers $ (C,P ( _ h H h))$ is minimized.
  Theorem \cite{arora2012multiplicative: Given a feasibility problem as defined above, a parameter $ $, a $ $- ORACLE implemented in procedure $ Oracle (·)$, and an update procedure $ Update (·)$, there is an algorithm which either finds an $ $ such that $ i$, $A_i _i≥ b_i- $ or correctly concludes that the system is infeasible. The algorithm makes $O( (m')/ ^2)$ calls to procedures $ Oracle (·)$ and $ Update (·)$.
  Corollary : Given a set of $n$ elements $P$ in a metric space with bounded doubling dimension, a family of $m$ disjoint subsets $ H $ over $P$, and positive integer parameters $k,z$, there exists a $(2,2,O(1))$-approximation algorithm for the $ (P, H ,k,z)$ that runs in $O(nk+k^2m+β_1^2+(β_2+β_3)^ log β_1)$ time, where $β_1=min\ n,km\ $, $β_2=min\ n,kz\ $, and $β_3=min\ m,kz\ $.
  Theorem : For an acyclic join $ $ of $d$ attributes, a database $ $ of input size $N$, parameters $ , k, z$, a set $S ( )$ of size $k$ and a set $C ( )$ of size $|C|≤ (1+ )^2δ | ( )|$ can be computed with probability at least $1- 1 | ( )|^2 $, such that $ (S, ( ) C)≤ 3√ d _ k,z ( ( ))$, where $δ= z | ( )| $. The running of the algorithm is $ O ( k ^2δ N+ k^3 ^4δ^2 )$.
  Theorem : For a database schema $ $ with $g$ tables $R_1,…, R_g$, an acyclic join query $ $ of $d$ attributes, a database $ $ with $| |=N$, integer positive parameters $k, z$, and a small constant $ (0,1)$, there exists a $(2+ ,2,O(1))$-approximation algorithm for the $ ( , ,k,z)$ problem, with probability at least $1- 1 N $, that runs in $O(k^2N^2log N)$ time.
  Proof: [Proof of Lemma~ [ref] ] Starting from the input $X, Y $ of the $ SC (X, Y )$ problem we construct in polynomial time a set $P$ and a family of sets $ H $ that will serve as input to the $ $ problem. For every $x_i X$, we place a point $p_i$ on the real line at coordinate $i$. We define $ P = _ x_i X p_i$. Then for an arbitrary positive integer number $k$ such that $k≤ O(n'^2)$, we add $k$ additional points $ Q =\ q_1, …, q_k\ $ in $P$. Each point $q_j$ is placed at coordinate $2n'+j$. Overall $P= P Q $. For every set $y Y $, we create the set $ y =\ p_i x_i y\ $. Let $ Y =\ y y Y \ $. Furthermore, for every $j [k]$ we define a set $h_j=\ q_j\ $. Let $ H =\ h_j j [k]\ $. Overall $ = Y H $. L
  Proof: [Proof of Lemma~ [ref] ] For simplicity, in the correctness proof we consider that $x_i^*, y_j^*$ are the values of the variables $x_i, y_j$ in the last feasible LP solution we found. We first show i). Equivalently, we will prove that if $r≥ ^*_ k,z (P, H )$ then the corresponding IP is feasible. Since $r≥ ^*_ k,z (P, H )$, there exists the optimum set of $k$ centers $C^* P$ and a family of $z$ sets $H^* H $ such that $ (C^*,P _ h H^* h)= ^*_ k,z (P, H )$. Based on this optimum solution, we construct a solution to the IP as follows. If $p_i C^*$ then we set $ x _i=1$, otherwise we set $ x _i=0$. If $h_j H^*$ we set $ y _j=1$, otherwise $ y _j=0$. Since $|C^*|=k$ and $|H^*|=z$, the first two 
  bound: The bound on $C$ and the approximation ratio hold with probability at least $1- 1 | ( )|^2 $.

## sigmod26-350 — Query Answering Under Volume-Based Diversity Functions  (score 53)
domain: + diverse-query-answering
usage: Measure-algebra submodularity proof; combinatorial reductions; marginal diversity evaluated as a tropical-semiring sum-product query; ranked enumeration with compatible rankings for preprocessing.
note-quote: "Let Ω be any volume assignment over a universe U of possible solutions. Then δ_Ω is always monotone and submodular."
  Definition \cite{deep2025ranked: Let $(T, )$ be a tree decomposition of a full CQ $Q$. We say that a ranking function is compatible with $(T, )$ if for every node $t$ it is $( (T_t) key (t))$-decomposable conditioned on $ key (t)$ Recall that $ (T_t)$ are all the variables that are in the bag of $v$ or a decedent of $v$ and $ key (t)$ are the variables in the bag of $t$ that it shares with its parent (or $ $ if $t$ is the root). .
  Definition \cite{deep2025ranked: Let $ $ be a ranking function ($ T _ \ R\ R $) over $R( )$-tuples and $ $. In the following, $g$ is a arbitrary homomorphism over $ $. We say that $ $ is $ $-decomposable if there exists a total order $ $ for all homomorphisms over $ $, such that for any two homomorphisms $h,h'$ over $ $ we have: $$ h h' g, (R((h g)( ))) = (R((h' g)( ))) \\ or \\ g, (R((h g)( ))) > (R((h' g)( ))) $$
  Theorem \cite{deep2025ranked: Let $(T, )$ be a tree decomposition of the full CQ $Q( )$ and $ $ a ranking function for $Q$-tuples that is compatible with $(T, )$. Then, enumerating $ Q (D)$ in the order induced by $ $ is possible with $O(|D|^ fhw(T, ) )$ preprocessing time and $O(log|D|)$ delay in data complexity. In particular, a solution maximizing $ $ can be found in time $O(|D|^ fhw(T, ) )$.
  Definition : Let $ = ( , μ, β)$ be a volume assignment, $R( )$ be an atom over $Σ$ with variables $ $, and $ y x $. We say that $β$ is $ y $-decomposable (w.r.t.\ $R$) if for every pair of homomorphisms $h, h'$ over $ y $ and homomorphisms $g,g'$ over $ x y $ we have: β(R((h g)( x ))) β(R((h' g)( x ))) = β(R((h g')( x ))) β(R((h' g')( x ))).
  Lemma : Let $Q( )$ be a full CQ and let $ =( , μ, β)$ be a volume assignment over $ $ such that $β$ is $ y $-decomposable conditioned on $ z $ (w.r.t.\ $Q$). Then, the ranking function for $Q$-tuples $ _ ,S Q (D) R , Q(h( x )) δ_ (S \ Q(h( x ))\ ))$ is $ y $-decomposable conditioned on $ z $ for every $S Q (D)$.
  Definition \cite{deep2025ranked: Let $ $ be a ranking function over $R( )$-tuples, and $ , $ such that $ = $. We say that $rank$ is $ $-decomposable conditioned on $ $ if for every homomorphism $f$ over $ $, the ranking function $rank_ f (R_ ( h ( ))) := rank(R(( f h )( )))$ defined over $R_ ( )$-tuples is $ $-decomposable.
  Lemma : Let $(T, )$ be a tree decomposition of the full CQ $Q( )$ and let $ =( , μ, β)$ be a volume assignment over $ $ such that $β$ is compatible with $(T, )$. Then, the ranking function $ _ ,S Q (D) R , Q(h( x )) δ_ (S \ Q(h( x ))\ ))$ is compatible with $(T, )$ for every $S Q (D)$.
  Lemma : Let $ =( , μ, β)$ be a volume assignment over $ $ such that $β$ is $ y $-decomposable conditioned on $ z $ (w.r.t.\ $R( )$). Then, then ranking function $ T _ \ R\ R , R( f ( )) μ(β(R( f ( ))))$ is $ y $-decomposable conditioned on $ z $.
  Proof: [Proof sketch] For given multi-attribute diversity function $v_λ$, defining an equivalent volume-based diversity function $v_ $ is straightforward. More precisely, we set $ =( , μ, β)$ with $ = 2^ 2^X $, $β (x) = \ A X x A\ $, and $μ( B ) = Σ_ A B λ_A $ for $ B $. The other direction is more involved and only works for finite universe $ $. In particular, we set $λ_A = μ ( _ a A β(a) _ x X A β(x) )$.
  Proof: In~ [cite] , Nemhauser, Wolsey, and Fisher showed that for every monotone submodular set function $f: ( ) $ and $k≥ 1$ one can compute in polynomial time a $k$-subset~$A$ of $ $ such that $f(A) ≥ (1- 1 e ) · max_ B : |B| = k f(B)$. Since $δ_ $ is submodular and monotone and $Q$ is fixed, one can compute the set $ Q (D)$ in polynomial time over $D$ and then apply the result in~ [cite] to retrieve a $(1- 1 e )$-approximation of $δ_ $ over $ Q (D)$ restricted to subsets of~size~$k$. In Algorithm~ [ref] , we depict this procedure for $δ_ $ which follows a greedy strategy: starting from $S = $; in every iteration it finds a tuple $t Q (D)$ that maximizes the marginal diversity of $δ_ $, namely, $
  bound: Nevertheless, one must compare the approximation ratio obtained for volume-based diversity functions with that of the best approximation found for distance-based analogs.
  bound: For $ $ under the Hamming or Euclidean metrics, the best approximation ratio is $(1 - ( 1 2 + ε))$, and the running time of the algorithms depends on $ε$.
  bound: For $ $, the best approximation ratio is $(1 - 2 k )$ for Hamming distance (for Euclidean distance it is $(1- 1 2 )$) but the running time depends on $k^3$.
  bound: Instead, the approximation ratio for volume-based diversity functions is $(1- 1 e )$ and works for every volume assignment that can be computed in polynomial time (in particular, for most of the examples presented in Section~ [ref] ).
  bound: It is well-known that the maximum coverage problem is hard to approximate beyond $(1- 1 e )$-approximation ratio, unless $ = $~ [cite] .
  bound: Nevertheless, even in the cases where $ $ is NP-hard, one might still be able to get a reasonable approximation algorithm.

## sigmod26-357 — Representation Obliviousness and Pseudodeterminism in Streaming Algorithms  (score 53)
domain: streaming | sketching
usage: Reduction chain from ROPD F0 estimator to ROPD Promise-Union protocol to PD Disj protocol, contradicting CC(Disj)=Ω(m); canonical-value and output-distribution arguments.
note-quote: "Let ε, δ ≤ 1/3. Any ROPD streaming algorithm that (ε, δ)-approximates F0 requires Ω(n) space (where n is the size of the universe)." (Thm 3.1)
  Definition 2.1: Let 𝜖, 𝛿 > 0. A streaming algorithm A is said to (𝜖, 𝛿)-approximate F𝑘 if for every universe size 𝑛 ∈ N and for every stream D ∈ [𝑛] ∗ Pr (|A𝑛 (D) − F𝑘 (D)| ≤ 𝜖F𝑘 (D)) ≥ 1 − 𝛿 For simplicity, in the rest of the paper we fix 𝛿 = 1/3. We will also use the following additional notation: For a set 𝑆 ⊆ [𝑛], D (𝑆) denotes the stream representation of 𝑆 in lexicographic order. For any two streams D1 and D2 , let D1 D2 denote the concatenation of the two streams. Proc. ACM Manag. Data, Vol. 3, No. 5 (PO
  Definition 2: 2 (Pseudodeterministic algorithms [16]). A randomized algorithm A is pseudodeterministic (PD for short) if for all inputs 𝑥, there is a canonical value 𝑔(𝑥) such that Pr(A (𝑥) = 𝑔(𝑥)) ≥ 23 . In the context of streaming algorithms for F𝑘 , a pseudodeterministic algorithm for a given input stream should output a canonical approximation for F𝑘 with high probability. We next introduce the notion of representation oblivious algorithms. Let 𝑘 ∈ N \ {0}, note that the frequency moment F𝑘 is “oblivious”
  Definition 2.4: [Representation Oblivious Algorithm for F𝑘 ] Let 𝑘 ∈ N. Two streams D1 and D2 are R𝑘 -equivalent if 𝑓 𝑘 (D1 ) = 𝑓 𝑘 (D2 ). A streaming algorithm A for F𝑘 is said to be representation oblivious if for every universe size 𝑛, A𝑛 is R𝑘 -oblivious. Recall that A𝑛 is the streaming algorithm A instantiated with the universe as [𝑛]. By combining the notions of pseudodeterminism and representation obliviousness, we arrive at the following definition.
  Definition 2.5: A streaming algorithm for F𝑘 is Representation Oblivious Pseudodeterministic (ROPD) if it is both representation oblivious and pseudodeterministic. The above definition means the following: Suppose A is an ROPD algorithm for F𝑘 . Since A is pseudodeterministic for every D there is a canonical value 𝑔(D) (which is an 𝜖-approximation of F0 (D)) such that A outputs 𝑔(D) with probability at least 2/3. Representation obliviousness imposes that if D1 and D2 belong to the same R𝑘 -equivalence class, th
  Definition 2.7: A streaming algorithm for F𝑘 is 𝛼-Representation Oblivious Pseudodeterministic (𝛼-ROPD) if it is both 𝛼-representation oblivious and pseudodeterministic. Note that 0-ROPD is the same as ROPD. We remark that our lower bound results extend to 𝛼-ROPD setting. Proc. ACM Manag. Data, Vol. 3, No. 5 (PODS), Article 282. Publication date: November 2025. 282:6 2.3 Michael Chen, Sourav Chakraborty, A. Pavan, and N. V. Vinodchandran Communication Complexity For our lower bound proofs, we will need results 
  Definition 2.10: Let 𝜓 : {0, 1}𝑚 × {0, 1}𝑚 ⇀ {0, 1} be a partial function and 𝑇 be an equivalence relation over {0, 1}𝑚 × {0, 1}𝑚 . A communication protocol P for 𝜓 is 𝑇 -oblivious pseudodeteriministic (𝑇 -OPD), if it is 𝑇 -oblivious and pseudodeterministic. We now define some communication problems that we use in this paper.
  Definition 2.13: Let 𝑚 ∈ N. Two input instances to Promise-Union𝑚 (𝐴1, 𝐵 1 ) and (𝐴2, 𝐵 2 ) are 𝜌𝑚 -equivalent if 𝐴1 ∪ 𝐵 1 = 𝐴2 ∪ 𝐵 2 . For the rest of the paper, we are interested in the above relationship 𝜌𝑚 . From now on, an ROPD protocol for Promise-Union𝑚 refers to a 𝜌𝑚 -OPD protocol. 3 F0 Lower bound In this section, we prove an Ω(𝑛) space lower bound for estimating F0 over the universe [𝑛] for ROPD one-pass streaming algorithms.
  Theorem 3.1: Let 𝜖, 𝛿 ≤ 31 . Any ROPD streaming algorithm that (𝜖, 𝛿)-approximates F0 requires Ω(𝑛) space (where 𝑛 is the size of the universe). Our proof approach shall be to show that if an ROPD F0 estimator using 𝑜 (𝑛)-space exists, then there is a 𝑜 (𝑚) communication protocol to solve Disj𝑚 problem via the reductions depicted below. This is a contradiction because the communication complexity of Disj𝑚 is Ω(𝑚) [26, 34]. ROPD F0 estimator ROPD communication protocol for Promise-Union Communication protocol
  Proof: Let 𝑛 ∈ N. Let A𝑛 be a ROPD streaming algorithm that ( 13 , 13 )-approximates F0 over a universe [𝑛] and uses space 𝑠. We now define the protocol P𝑛 to solve the Promise-Union𝑛 : Alice receives 𝐴 ⊆ [𝑛] and Bob receives 𝐵 ⊆ [𝑛]. They convert their input sets to streams D𝐴 = D (𝐴) and D𝐵 = D (𝐵), respectively. Alice runs the streaming algorithm A𝑛 on D𝐴 and then communicates the state at the end of her execution to Bob (requiring only 𝑠 bits of communication). On receiving this state, Bob continues the execution with his part of the stream D𝐵 . Let D = D𝐴 D𝐵 . At the end of the simulation, Bob has A𝑛 (D). Bob outputs 1 if A𝑛 (D) is at least 𝑛/2; otherwise, Bob outputs 0. We now prove that P𝑛 (
  Proof: For a stream D over [𝑛], let 𝑔(D) be the canonical value of A𝑛 , i.e., Pr(A𝑛 (D) = 𝑔(D)) ≥ 23 . For 𝐴, 𝐵 ⊆ [𝑛], define ( 1 if 𝑔(D (𝐴)D (𝐵)) ≥ 𝑛2 ℎ(𝐴, 𝐵) = , 0 if 𝑔(D (𝐴)D (𝐵)) < 𝑛2 so that, Pr(P (𝐴, 𝐵) = ℎ(𝐴, 𝐵)) ≥ 23 . Thus, ℎ testifies that P𝑛 is pseudodeterministic. We emphasize that the pseudodeterminism holds for all inputs. □

## sigmod26-264 — A Unifying Algorithm for Hierarchical Queries  (score 49)
domain: provenance | uncertain-data
usage: Variable-elimination rules (private variables, duplicate atoms) with invariant induction; homomorphism φ commuting with monoid operations transfers provenance to each application; bipartite-clique reduction for hardness.
note-quote: "Then, the output of Algorithm [hierarchical] on Q and D_φ is the φ-mapping of the output of Algorithm [hierarchical] on Q and D₊."
  Proposition Elimination Procedure for Hierarchical Queries: A $Q$ is hierarchical if and only if the following elimination procedure transforms $Q$ into a query with a single nullary atom, i.e., $Q() R()$. Repeatedly, apply either one of the following two rules, until neither rule applies: ; (Rule 1) Eliminate a ``private'' variable $Y$: Find a variable $Y (Q)$ that occurs in only one atom $R( X)$ in $Q$, and replace $R( X)$ with $R'( X \ Y\ )$ where $R'$ is a new relation symbol. ; (Rule 2) Eliminate a ``duplicate atom'': Find two distinct atoms $R_1( X
  Definition Provenance Tree: Let $Σ$ be a set of symbols. A provenance tree is a rooted tree whose leafs are labeled with symbols from $Σ \ , \ $ and whose internal nodes are labeled with either $ $ or $ $. A provenance tree is called decomposable if its leaves have distinct labels, including the labels $ $ and $ $. The labels $ $ and $ $ do not need to appear in any trees except for the two trivial trees: the tree that consists of a single node which is $ $, and the tree that consists of a single $ $ node. This is because 
  Definition Provenance \twomonoid: Let $Σ$ be a set of symbols. The provenance is a $ K = ( K , , )$ defined as follows. ; The domain $ K $ is the set of all provenance trees over $Σ \ , \ $. ; The $ $ operator takes two provenance trees $x$ and $y$ and returns a new provenance tree $z$ that has a root labeled with $ $ and whose children are the roots of $x$ and $y$. ; The $ $ operator takes two provenance trees $x$ and $y$ and returns a new provenance tree $z$ that has a root labeled with $ $ and whose children are the roots of 
  Definition \bagsetmax: The problem is an optimization problem that is parameterized by a self-join-free Boolean conjunctive query $Q$, which is interpreted under bag-set semantics ~ [cite] . The input to the problem is a tuple $( , , θ)$ where: ; $ $ is a set database instance, which is the input instance that we want to repair. ; $ $ is another set database instance, called the repair database , which contains the facts that we are allowed to add to $ $ in order to repair $ $. ; $θ$ is a natural number representing a
  Theorem Algorithm~\ref{alg:hierarchical} is correct: Let $Q$ be a hierarchical and $ $ be a database instance where every fact is annotated with a unique symbol from a set of symbols $Σ$. Let $ K = ( K , , )$ be the provenance from Definition~ [ref] where $ K $ is the set of provenance trees over $Σ \ , \ $, and let $ 0 $ and $ 1 $ be the identities. Let $ K = (K, , )$ be another with identities $ 0$ and $ 1$. Suppose we are given a function $ : K K$ that maps provenance trees to values in $K$ and satisfies the following properties, for all $x$ an
  Definition Shapley value of a fact with respect to a query and a database: Let $Q$ be a Boolean Conjunctive Query and $ $ be a database instance. The instance $ $ is divided into two parts: ; Exogenous part , denoted $ $. These are facts that are fixed. ; Endogenous part , denoted $ $. These are facts that can be added or deleted from $ $. Suppose that we pick a permutation of the facts in $ $ uniformly at random, and we begin to insert those facts one by one into $ $. The Shapley value of a given fact $f$ with respect to $Q$ and $ $ is the probability of adding the fa
  Definition \twomonoid for computing $\sat_{Q, \Dexo, \Dendo}(k)$: We define a specific $ K = (K, , )$ as follows. Let $ \ , \ $. The domain $K$ is $ ^ × $, i.e., the set of all vectors of natural numbers that are indexed by pairs of natural numbers and Boolean values. The $ $ and $ $ operations are defined as follows for any pair of vectors $ x_1, x_2 K$: ( x_1 red x_2)(i, b) & Σ_ i_1, i_2 : i_1 + i_2 = i Σ_ b_1, b_2 : b_1 red b_2 = b x_1(i_1, b_1) × x_2(i_2, b_2), & i , b \\ ( x_1 red x_2)(i, b) & Σ_ i_1, i_2 : i_1 + i_2 = i Σ_ b_1, b_2 : b_1 red b_2 = b x_1(
  Definition Size of a $\bm K$-annotated database: Let $ K=(K, , )$ be a where $ $ and $ $ have identities $ 0$ and $ 1$ respectively. Given a $ K$-annotated relation $R( X)$, the support of $R( X)$, denoted $ (R( X))$, is the set of all facts $R( x)$ in $R( X)$ whose $ K$-annotation is not $ 0$. The size of $R( X)$ is the size of its support, i.e., $ (R( X)) $. The support of a $ K$-annotated database instance $ $, denoted $ ( )$, is the union of the supports of its relations. The size of a $ K$-annotated database instance $ $, denoted $ $, is 
  Proof: Membership in $ $ follows from Proposition~ [ref] . We now prove $ $-hardness. Let $Q$ be an arbitrary non-hierarchical . Since $Q$ is not hierarchical, $Q$ must have the form: \[ Q() R(A, X ), S(A,B, Y ), T(B, Z ), P_1( D _1), , P_n( D _n), \] where $A, B$ are distinct variables and $ X $, $ Y $, $ Z $, $ D _1, , D _n$ are sets of variables, $A Z $ and $B X $, and $n≥ 0$. We give a polynomial-time reduction from to for $Q$. Let $(G, k)$ be an input instance to where $G=(V,E)$ is an undirected graph without self-loops and $k$ is a positive integer. We construct an input instance $( , , θ, )$ for the problem for $Q$ as follows: Let $ $ be the set of vertices $V$, and fix an arbitrary vertex $
  Proof: [Proof of Proposition~ [ref] ] To prove Proposition~ [ref] , we will prove the following claims: Given a hierarchical $Q$ that does not have the form $Q() R()$, either Rule 1 or Rule 2 must be applicable to $Q$, and the resulting query must be hierarchical. Given a non-hierarchical $Q$, if either Rule 1 or Rule 2 is applicable to $Q$, then the resulting query must be non-hierarchical. [Proof of Claim~ [ref] ] Claim~ [ref] follows from Proposition~ [ref] . In particular, consider a hierarchical $Q$ that does not have the form $Q() R()$. If $ (Q) = $, then all connected components of $Q$ must have the form $Q_i() R_i()$, in which case we can apply Rule 2 on them. On the other hand, if $ (Q) ≠ 
  bound: The input to the problem is a tuple $( , , θ)$ where: ; $ $ is a set database instance, which is the input instance that we want to repair. ; $ $ is another set database instance, called the repair database , which contains the facts that we are allowed to add to $ $ in order to 

## sigmod26-375 — Tractability Frontiers of the Shapley Value for Aggregate Conjunctive Queries  (score 47)
domain: provenance
usage: #Set-Cover and permanent reductions via databases whose Shapley values encode coverage counts; Shapley linearity/symmetry/dummy axioms to decompose; injective transformations shifting value functions across aggregation.
note-quote: "If Q is [hierarchical], then φ(f, A_α) is computable in polynomial time ... Otherwise, there is a localized [value function] such that computing φ(f, A_α) is #P-complete."
  Lemma : Consider an of the form $α $. Let $Q_0$ be a CQ without self-joins, such that $Q_0$ is but not q-hierarchical. There is a value function $ _0$, having the form $ i $, and a polynomial-time algorithm that, given an input database $D$ for $ $, constructs an input database $D_0$ for $Q_0$, a function $h:D D_0 $, and a function $g: (D) Q_0(D)$ such that: ; $h$ and $g$ are both bijections; ; For every $E D $ it holds that: ; $g( (D E)) = Q_0(D_0 h(E))$ where $h(E) h(f) f E $ and, for every set $T$ of
  Observation : Let $Q$ be a CQ without self-joins, $1 ≤ i ≤ (Q)$ and let $x_i$ is denote the $i$th variable in the head of $Q$. Furthermore, let $γ $ be injective. Consider the following transformation $ $ of facts in the schema of $Q$: For each fact $f = R( a)$ let $ (f)$ be the fact $ (f) = R( a')$ defined by \[ a'[j] γ( a[j]), & if x_i appears at position j in the atom R( x) of Q,\\ a[j], & otherwise, \] Then, for each database $D$, we have $(γ i Q)(D) = ( i Q)( (D))$.
  Observation : Let $B=(X,μ)$ be a bag of real numbers, $α$ an aggregation function and $γ $. ; If $α , $ and $γ$ is monotonically increasing, then $(α γ)(B) = (γ α)(B)$ ; If $α = _q$ for some $q (0,1)$ and $γ$ is monotonically increasing define $γ'$ by $γ'(x) = γ(x)$ for all $x X$ with $μ(x) > 0$ and by linear interpolation between these points (and $0$ elsewhere). Then $(α γ)(B) = (γ' α)(B)$.
  Theorem : Let $Q$ be a CQ without self-joins, $q (0,1)_ Q $ fixed, and $α , _q $. Denote by $A_ $ the aggregate query $α Q$. ; If $Q$ is , then $ (f,A_ )$ is computable in polynomial time, given a database $D$ and a fact $f$, for every localized $ $. ; Otherwise, there is a localized $ $ such that computing $ (f, A_ )$, given a database $D$ and a fact $f$, is -complete.
  Theorem : Let $Q$ be a CQ without self-joins and $α , , $. Denote by $A_ $ the aggregate query for $α Q$. ; If $Q$ is , then $ (f,A_ )$ is computable in polynomial time, given a database $D$ and a fact $f$, for every localized $ $. ; Otherwise, there is a localized $ $ such that computing $ (f, A_ )$, given a database $D$ and a fact $f$, is -complete.
  Lemma : Let $Q$ be a CQ without self-joins and $ $ be localized on the atom $R( z)$. For a database $D$ and $a $, let $D_a$ denote the database that is obtained from $D$ by removing all facts $R( b)$ with $ ( b) ≠ a$. Furthermore, let $ () Q( x)$. Then \[ (D_a) = 1 a ( Q)(D). \]
  Lemma \protect{\cite[Proof~of~Theorem~6.3: ] For an input $( , )$ to the \#Set-Cover problem with $ = Y_1, ,Y_m $, consider the cooperative game with player set $P = 1, …, m $ and utility \[ (C) = 1 & if _ i C = \\ 0 & otherwise. \] Then, computing $ (i, )$ for $( , )$ and an index $i$ of $ $ is -complete
  Lemma : Let $Q$ be a CQ without self-joins, such that $Q$ is but not q-hierarchical. There is a localized value function $ $ such that for $A= Q$, computing $ (f, A)$, given a database $D$ and a fact $f$, is -complete.
  Proof: This claim follows from the linearity property of the Shapley value. To see that we can apply this property, let $C D $. Since $α$ is constant per singleton, $A$ returns $α( c )$ if $Q(C D )$ is nonempty and $0$ otherwise. In the same way, $ $ returns $1$ if $Q(C D )$ is nonempty and $0$ otherwise, so we have $A(C D ) = α( c ) · (C D )$.
  Proof: We can infer this formula directly from the properties of the Shapley value presented in apx:shapley . Before considering $ = A$, let us first consider $ _a _a Q$ for $a ( Q)(D)$. Each fact $R( x)$ with $ ( x) ≠ a$ is a null player for this utility function, and all other players are interchangeable and therefore have equal Shapley values by symmetry. Since the sum of all Shapley values for $ _a$ is $1$ by efficiency, we obtain $ (R( t), _a)(D) = 1 R( t') \, \, ( t') = ( t) $ if $ ( t) = a$ and $0$ otherwise. Writing $ = Σ_a _a$ and using linearity yields the claim.
  bound: (We will give the complete argument in the extended version of this manuscript.) Importantly, it is necessary to restrict $ $ (e.g., being a monotonic monoid) to extend our upper bound for min and max; it is insufficient to assume only that $ $ is computable in polynomial time (a
