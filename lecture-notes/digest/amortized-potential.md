# Chapter digest: amortized-potential
n_papers_with_tool: 11
PODS exemplars are from the PODS'26 issue inside PACMMOD Vol 4 — mark them ^PODS.

## sigmod26-177 [PODS] — Optimal Enumeration of Regular Pattern Matches  (score 52)
domain: indexing
usage: Left/right-canonical embeddings; epoch partition of intervals; suffix array/tree ranges, radix sort, range-max structure for O(1) delay; counting bound Σ(|O_i|−1) ≤ min{kn,|S|} (Lemma 3.4); segment tree for the dynamic case.
note-quote: "We can enumerate the set S of all valid embeddings of α in T with constant delay, after a preprocessing running in O(n + |S|) time. The used space is O(n + min{|S|, n^k})." (Thm 1.1)
  Theorem 1.1: We can enumerate the set 𝑆 of all valid embeddings of 𝛼 in 𝑇 with constant delay, after a preprocessing running in O (𝑛 + |𝑆 |) time. The used space is O (𝑛 + min{|𝑆 |, 𝑛𝑘 }). Further, we present an algorithm that still reports all matches of 𝛼 to 𝑇 in O (𝑛 + |𝑆 |) time, but has the additional properties that the preprocessing time is O (𝑛) and that for all 𝑠 ≤ |𝑆 |, the first 𝑠 embeddings are reported in O (𝑛 + 𝑠) time; in other words, this algorithm works with amortized O (1) delay.
  Theorem 1.2: We can enumerate the set 𝑆 of all valid embeddings of 𝛼 in 𝑇 with amortized constant delay, after a preprocessing running in O (𝑛) time. The used space is O (𝑛 + min{|𝑆 |, 𝑛𝑘 }). Finally, we give an algorithm for the considered problem that works with preprocessing time O (𝑛) and constant delay. It is worth noting that employing the algorithms from [2, 19] to the restricted class of regular patterns that we consider would lead to enumeration algorithms with a delay that depends (polynomially) on
  Theorem 1.3: We can enumerate the set 𝑆 of all valid embeddings of 𝛼 in 𝑇 with worst-case constant delay, after a preprocessing running in O (𝑛) time. The used space is O (𝑛 min{|𝑆 |, 𝑛𝑘 }). While Theorem 1.3 improves both previous results, we prefer stating our results in this incremental fashion, which allows us to introduce our tools and their functionality gradually. The algorithms presented here heavily take advantage, on the one hand, that the strings separating the variables are fixed (not described b
  Theorem 1.4: We can preprocess the text 𝑇 in O (𝑛 log 𝑛) time, so that when given a pattern 𝛼 with 𝑘 variables, after an additional processing running in O (|𝛼 | + 𝑘 log 𝑛) time, the set 𝑆 of all valid embeddings of 𝛼 in 𝑇 can be enumerated with constant delay. Overview of our algorithms. Our four algorithms are based on a common idea. We can identify, greedily, a left-canonical valid embedding (ℓ1, . . . , ℓ𝑘 ) and a right-canonical valid embedding (𝑟 1, . . . , 𝑟𝑘 ), such that for every other valid embeddi
  Lemma 2: 1 ([29]). Let 𝑤,𝑇 ∈ Σ∗ with |𝑤 | = 𝑚 and |𝑇 | = 𝑛. After O (𝑚) time preprocessing, the minimal 𝑖 such that 𝑇 [𝑖 − 𝑚 + 1 : 𝑖] = 𝑤, can be found in O (𝑖 + 𝑚) time. Further, all 𝑖 such that 𝑇 [𝑖 − 𝑚 + 1 : 𝑖] = 𝑤 can be enumerated, in increasing order, in O (𝑛) time. Radix sort. For a sequence of 𝑛 keys (𝑥 1, . . . , 𝑥𝑛 ), each key being a 𝑘-tuple over [1 : 𝑚], radix sort is a non-comparison-based sorting algorithm that sorts the keys lexicographically by processing their components from least signi
  Lemma 2.2: Let 𝑇 [1 : 𝑛] be a text and let 𝑤 1, . . . , 𝑤𝑘 be strings with 𝑘𝑖=1 |𝑤𝑖 | ≤ 𝑛. Then, in total O (𝑛) time, we can compute, for every 𝑖 ∈ [𝑘], the range 𝑆𝐴[𝑎 𝑤𝑖 : 𝑏 𝑤𝑖 ] containing exactly the positions 𝑗 such that 𝑇 [ 𝑗 : 𝑗 + |𝑤𝑖 | − 1] = 𝑤𝑖 .
  Lemma 2.4: A permutation of [𝑛] stored in an array 𝐴[1 : 𝑛] can be preprocessed in O (𝑛) time for the following queries: given integers 𝑎, 𝑏, 𝑥 ∈ [𝑛] with 𝑎 ≤ 𝑏, enumerate all elements of the set {𝑖 ∈ [𝑎 : 𝑏] | 𝐴[𝑖] ≥ 𝑥 } with worst-case constant delay.
  Observation 2.1: By combining Lemmas 2.2 and 2.4 we obtain the following primitive: Let 𝑇 [1 : 𝑛] Í be a text and let 𝑤 1, . . . , 𝑤𝑘 be strings with 𝑘𝑖=1 |𝑤𝑖 | ≤ 𝑛. After O (𝑛) preprocessing, given an index 𝑖 and a position 𝑥, we can enumerate all occurrences of 𝑤𝑖 starting at position 𝑥 or later with O (1) worst-case delay. Figure 1 illustrates Observation 2.1. 3 Warm-up: Computing All Valid Embeddings We recall that a match of 𝛼 to 𝑇 is represented as a 𝑘-tuple ( 𝑗1, . . . , 𝑗𝑘 ) called valid embedding, where
  Proof: We build the suffix tree and preprocess its node to store the corresponding ranges of the suffix array. Then, we lexicographically sort all strings 𝑤 1, . . . , 𝑤𝑘 with radix sort for variable length Í keys in O (𝑛 + 𝑘𝑖=1 |𝑤𝑖 |) = O (𝑛) time. Next, for every 𝑖 ∈ [𝑘], we want to find the node of the suffix tree corresponding to 𝑤𝑖 (the node might be explicit or implicit) and return its corresponding range (if a node is implicit, we return the range of its nearest explicit descendant). To find the node corresponding to 𝑤𝑖 , we first go up from the already known node corresponding to 𝑤𝑖 −1 to reach the node corresponding to the longest common prefix of 𝑤𝑖 −1 and 𝑤𝑖 , and then we navigate down w
  Proof: The preprocessing amounts to constructing the RMQ structure from Theorem 2.3 in O (𝑛) time. After receiving the integers 𝑎, 𝑏, 𝑥 ∈ [𝑛] with 𝑎 ≤ 𝑏, we proceed as follows. We maintain a stack storing intervals [𝑎𝑖 , 𝑏𝑖 ] such that RMaxQ𝐴 (𝑎𝑖 , 𝑏𝑖 ) ≥ 𝑥. We first check if RMaxQ𝐴 (𝑎, 𝑏) ≥ 𝑥 and if so push the interval [𝑎, 𝑏] onto the stack. Then, as long as the stack is non-empty we pop its topmost element [𝑎𝑡 , 𝑏𝑡 ]. We compute 𝑝 := RMaxQ𝐴 (𝑎𝑡 , 𝑏𝑡 ) and report 𝑝 as the next element of the set. Then, if 𝑎𝑡 < 𝑝 and RMaxQ𝐴 (𝑎𝑡 , 𝑝 − 1) ≥ 𝑥 we push [𝑎𝑡 , 𝑝 − 1] onto the stack. Similarly, if 𝑝 < 𝑏𝑡 and RMaxQ𝐴 (𝑝 + 1, 𝑏𝑡 ) ≥ 𝑥 we push [𝑝 + 1, 𝑏𝑡 ] onto the stack. It is straightforward to verify that

## sigmod26-015 — AeroSketch: Near-Optimal Time Matrix Sketch Framework for Streams  (score 42)
domain: streaming | sketching
usage: FD decomposability composes per-site sketches; iteration error bounds give the whp guarantee; amortization over every-ℓ-updates SVD yields the time bound.
note-quote: "ML-AeroSketch solves [sliding-window matrix sketch tracking] with space O((d/ε)·log R) and amortized time O((d/ε)·log d·log R) per update, with success probability at least 99/100."
  Theorem Probabilistic error bounds and time complexity of Simultaneous Iteration, the main lemma proven in~\cite{musco2015randomized: Given a matrix $ A R ^ d × $, a target rank $k$, and an error parameter $ _ SI $, after executing Algorithm~ [ref] , we obtain matrices $ Z R ^ d× k $ and $ Σ R ^ k× k $. With high probability (at least $99/100$), the following guarantees hold: ; $\| A - Z Z ^ A \|_F (1 + _ SI ) \| A - A _k\|_F$, where $ A _k$ is the best rank-$k$ approximation of $ A $. If the singular value decomposition of $ A $ is $ U Σ V ^ $, then $ A _k = U _k Σ _k V _k^ $. Specifically, for $k = 0$, we define $ A _0 = 0 $
  Theorem : Algorithm~ [ref] solves the Tracking Approximate Matrix Multiplication over Sliding Window (Problem~ [ref] ) with space complexity $O ( d_x+d_y log R )$ and amortized time complexity $O ( d_x+d_y log d log R )$ per update, with success probability at least that given in Eq.~ [ref] . Alternatively, using the similar like probability amplification of Algorithm~ [ref] , it achieves amortized time complexity $O\! ( d_x+d_y log d log^2 1 δ )$ per update while guarantee success probability at least $1
  Theorem : Algorithm~ [ref] solves the Tracking Distributed Matrix Sketch over Sliding Window (Problem~ [ref] ) with communication complexity $O ( md log \| A \|_F^2 )$ and amortized time complexity $O ( d log d )$ per update, with success probability at least that given in Eq.~ [ref] . Alternatively, using the probability amplification of Algorithm~ [ref] , it achieves amortized time complexity $O\! ( d log d log^2 1 δ )$ per update to guarantee success probability at least $1-δ$, where $δ$ is a tunable p
  Theorem : (Algorithm~ [ref] ) solves the Distributed Matrix Sketch problem (Problem~ [ref] ) with communication complexity $O ( md log \| A \|_F^2 )$ and amortized time complexity $O ( d log d )$ per update, with success probability at least that given in Eq.~ [ref] . Alternatively, using the probability amplification of Algorithm~ [ref] , it achieves amortized time complexity $O\! ( d log d log^2 1 δ )$ per update while guarantee success probability at least $1-δ$, where $δ$ is a tunable parameter that c
  Theorem : Algorithm~ [ref] solves the ATTP Matrix Sketch (Problem~ [ref] ) with space complexity $O\! ( d log \| A \|_F^2 )$ and amortized time complexity $O\! ( d log d )$ per update, with success probability at least that given in Eq.~ [ref] . Alternatively, using the probability amplification of Algorithm~ [ref] , it achieves amortized time complexity $O\! ( d log d log^2 1 δ )$ per update while guarantee success probability at least $1-δ$, where $δ$ is a tunable parameter that can be chosen arbitraril
  Proposition : Let $ B $ be the sketch of the current sliding window $(T - N,T]$ returned at line 6 of Algorithm~ [ref] (i.e., from $ Query (lb = T - N,\ ub = T)$). This is our target sketch. We show that, with success probability at least \( 99 100 (1- 2 √ e · 1 √ d log_2 d )\), the following error bound holds: \| A _ T - N, T ^ A _ T - N, T - B ^ B \|_2 O(1)\, \, \| A _ T - N, T \|_F^2, where $ A _ T - N, T $ is the matrix formed by stacking the vectors $ a _i$ for $i = T - N+1,…,T$.
  Lemma Decomposability: Given a matrix $ A R ^ d× n $, we decompose $ A $ into $m$ submatrices, i.e., $ A ^ = [ A _1, A _2, , A _m]$, where $ A _i R ^ d× n_i $ and $Σ_ i=1 ^m n_i = n$. If we construct a matrix sketch with an $ε_i$ covariance error guarantee for each submatrix $ A _i$, denoted as $ B _i$, such that A _i A _i^ - B _i B _i^ _2 ε_i· A _i _F^2. Then $ B = [ B _1, B _2,…, B _m]$ is an approximation of $ A $ and the error bound is A A ^ - B B ^ _2 Σ_ i=1 ^mε_i· A _i _F^2.
  Corollary Probabilistic error bound and time complexity of Power Iteration, a simplified corollary of Theorem~4.1(a) in~\cite{kuczynski1992estimating: For Algorithm~ [ref] , if we set $k = _ 2 d +1$, then the probability that the estimated top squared singular value $ σ _1^2$ exceeds half of the true value $σ_1^2 / 2$ is bounded by: [ σ _1^2 σ_1^2/2 ] 1- 2 √ e · 1 √ d log_2 d . Power Iteration requires only matrix-vector multiplications. The total computational time cost of Algorithm~ [ref] is $O(d k)$. If we set $k = _2 d +1$, then the time cost becomes $O(d log d)$.
  Proof: [Proof of Corollary~ [ref] ] We set the number of iterations $k= _2 d + 1$. Plugging $k$ in [ref] , we have [ | σ _1^2-σ_1^2 σ_1^2 |> ] 0.354 √ · log_2 d √ d (1- )^ log_2 d + 1 / 2 . Then we set $ = 1/2$, we have & = [ σ _1^2<σ_1^2/2 ] = [ σ _1^2-σ_1^2 σ_1^2 <- 1 2 ] [ | σ _1^2-σ_1^2 σ_1^2 |> 1 2 ] \\ & = [ | σ _1^2-σ_1^2 σ_1^2 |> ] 0.354 √ _2 d √ d (1- )^ log_2 d + 1 / 2 \\ & = 0.354 √ (log_2 d)/2 √ d · ( 1 2 )^ log_2 d + 1 / 2 = 0.354 √ d log_2 d , where the numerical constant corresponds to the numerical evaluation of $ 192 105 · 1 √ e ≈ 0.354$, and it holds that $ 192 105 · 1 √ e 2 √ e $. Therefore, we have [ σ _1^2<σ_1^2/2 ] 2 √ e · 1 √ d log_2 d . Hence, we have [ σ _1^2 σ_1^2/2 ] 1- 2
  Proof: We denote the matrix $ B $ before the execution of eigen decomposition at line~5 of Algorithm~ [ref] over the window $(T-N, i]$ as $ B _i$. Formally, B _i= C _i^ C _i +Σ_ j=T-N+1 ^ i ( Z _j Z _j^ C _j^ ^ C _j^ + C _j^ ^ C _j^ Z _j Z _j^ - Z _j Z _j^ C _j^ ^ C _j^ Z _j Z _j^ ). In fact, not every index \( j \) explicitly corresponds to the matrices \( Z _j \) and \( Z _j^ C _j^ ^ C _j^ \) stored in the queue \( S \); in such cases, we set \( Z _j = 0 \) here. We introduce the per-update error as: _ i = a _ i ^ a _ i + B _ i-1 - B _ i . \\ Here $ a _i R ^ 1× d $ is a row vector. Then summing over the window yields \|Σ_ i=T-N+1 ^ T _i \|_2 & Eq.~ [ref] = \| (Σ_ i=T-N+1 ^ T a _ i ^ a _ i )- B _ 
  bound: It is also difficult to derive an upper bound for this error.
  bound: At initialization, requires the input vector dimension $d$, the upper bound on the relative covariance error $ $, and a dump threshold $θ$.
  bound: ML- (Algorithm~ [ref] ) solves the Tracking Approximate Matrix Sketch over Sliding Window (Problem~ [ref] ) with space complexity $O ( d log R )$ and amortized time complexity $O ( d log d log R )$ per update, with success probability (the probability that the output sketch satis
  bound: We plot the function graph of the success probability lower bound Eq.~ [ref] with respect to the vector dimension \(d\) in Figure~ [ref] .
  bound: As the dimension $d$ increases, the success probability lower bound of the algorithm quickly approaches \( 99 100 \).
  bound: This indicates that when \( d \) is not very small, the success probability lower bound is close to \( 99 100 \).

## sigmod26-180 — ORDER: Optimal Routing with Path Indexing in Exchange Graph  (score 22)
domain: + DeFi exchange order routing
usage: Contradiction proofs on the global weight ordering validate lazy maintenance; epoch decomposition (scan + heap build + per-selection heap ops) yields the amortized bound.
note-quote: "For any two indices i < i′, no path in B_i′ has a strictly smaller ordering key than a path in B_i."
  Definition 3: 1 (Ordered Edge Group). For a token pair (𝑢, 𝑣), let 𝐸𝑔 (𝑢, 𝑣) denote the ordered edge group that contains all parallel edges from 𝑢 to 𝑣: 𝐸𝑔 (𝑢, 𝑣) = [𝑒 0, 𝑒 1, . . . , 𝑒𝑚−1 ]. Each edge 𝑒 ℓ corresponds to a feasible swap and is associated with a weight–capacity pair (𝑤 ℓ , 𝑐 ℓ ). Edges in 𝐸𝑔 (𝑢, 𝑣) are ordered by nondecreasing weight. If multiple edges have the same weight, ties are broken arbitrarily but consistently. We denote the current front (active price level) of the group by 3 In produ
  Lemma 3: 4 (On-Demand Bottleneck Update). Let 𝜏 be the selection time of path 𝜋𝑖 . Define 𝐶b(𝜏) = min𝑔∈𝜋𝑖 𝑐𝑔 (𝜏), where 𝑐𝑔 (𝜏) is the residual capacity at the head of front(𝐸𝑔 ) at time 𝜏. Then 𝐶b(𝜏) equals the true bottleneck capacity for executing 𝜋𝑖 at time 𝜏. Therefore, maintaining a cached C𝑖 across selections is unnecessary.
  Theorem 3: 9 (Amortized Complexity). Consider the iterative path selection algorithm on |P | paths with active bucket iterations. The amortized time per selection iteration is  size  𝐾 for 𝑇 selection   |P| 𝐾 E[per-iteration] = 𝑂 𝑇 + 𝑂 1 + 𝑇 log 𝐾 . Proc. ACM Manag. Data, Vol. 4, No. 3 (SIGMOD), Article 220. Publication date: June 2026. 220:14 Bingqiao Luo et al.
  Definition 4: 1 (Prioritized Buckets). Let B = (𝐵 0, 𝐵 1, . . . , 𝐵𝐿−1 ) denote an ordered sequence of buckets, where 𝐿 ∈ N is the number of levels. Bucket indices encode execution priority: buckets are accessed strictly in increasing index order (lower indices are considered earlier). By construction, 𝐵 0 coincides with the active bucket 𝐵 act , while 𝐵 ℓ for ℓ ≥ 1 are reserve buckets activated on demand when higher-priority buckets are exhausted. Data Structure for Prioritized Bucket. To generalize the rese
  Lemma 4: 2 (No-Cross-Bucket Domination). Given the prioritized buckets in Definition 4.1, for any two indices 𝑖 < 𝑖 ′ , no path in 𝐵𝑖 ′ has a strictly smaller ordering key than a path in 𝐵𝑖 . Equivalently, if 𝜋𝑝 ∈ 𝐵𝑖 and 𝜋𝑞 ∈ 𝐵𝑖 ′ with 𝑖 < 𝑖 ′ , then W(𝜋𝑝 ) ≤ W(𝜋𝑞 ).
  Proof: At time 𝜏, each edge 𝑔 ∈ 𝜋𝑖 has residual capacity 𝑐𝑔 (𝜏). By definition, 𝐶b(𝜏) = min𝑔∈𝜋𝑖 𝑐𝑔 (𝜏). Assume, for contradiction, that 𝐶b(𝜏) is not the true bottleneck at time 𝜏. Then one of the following must hold: (1) There exists 𝑔★ ∈ 𝜋𝑖 with 𝑐𝑔★ (𝜏) < 𝐶b(𝜏). Then some edge has capacity smaller than the alleged bottleneck, contradicting that 𝐶b(𝜏) is the minimum along 𝜋𝑖 . (2) For all 𝑔 ∈ 𝜋𝑖 , 𝑐𝑔 (𝜏) > 𝐶b(𝜏). Then every edge exceeds 𝐶b(𝜏), so the path minimum would be larger than 𝐶b(𝜏), again contradicting its definition. Proc. ACM Manag. Data, Vol. 4, No. 3 (SIGMOD), Article 220. Publication date: June 2026. 220:12 Bingqiao Luo et al. Select π2 (S → C → T) Δ = 600 𝑬𝒈 𝑺, 𝑪 Capacity only Before 
  Proof: Suppose, for contradiction, that a rebuild is triggered while |𝐵 act | > 0 and some 𝜋𝑖 ∈ 𝐵 act has C𝑖 > 0. Let 𝜏 be the most recent admission threshold. By the maintenance invariants, 𝑊𝑖 ≤ 𝜏 for all 𝑖 ∈ 𝐵 act and 𝑊 𝑗 ≥ 𝜏 for all 𝑗 ∈ 𝐵 res . Thus a feasible in-bucket candidate satisfies 𝑊𝑖 ≤ 𝜏 ≤ 𝑊 𝑗 , so no reserve item can dominate it. Rebuilding is therefore unnecessary, leading to a contradiction. □

## sigmod26-363 — SplineSketch: Even More Accurate Quantiles with Error Guarantees  (score 21)
domain: sketching
usage: Potential function over bucket counters bounds splits; rank error charged along split history; coefficient-feasibility argument for the spline lemma.
note-quote: "for any threshold σ_i of the final sketch, ε^n(σ_i) ≤ O(log α·n/k)"
  Definition : We say that a bucket $i$ is splittable if one of the following conditions holds: [label=( )] ; bucket $i$ violates the bucket bound~ [ref] , i.e., $b_i > C_b· n/k$, or ; there are at least $k/3 + 2$ joinable pairs of buckets, $b_i > 2n/k$, and the heuristic error~ [ref] of bucket $i$ is $γ > 1$ times larger than the heuristic error of a joinable pair of buckets (after joining) not overlapping bucket $i$.
  Definition : We say that a pair $i, i+1$ of adjacent buckets is joinable if both of the following conditions hold: [label=( )] ; the counter of the bucket resulting from the join satisfies $b_i + b_ i+1 0.75· C_b · n/k$, and ; threshold $ _i$ is not protected .
  Lemma : Let \( P(x) \) be a cubic polynomial such that \( P(0) = 0 \), \( P(1) = 1 \), and \( P(x) \) is non-decreasing on the interval \( [0,1] \). Then, $P ( 1 2 ) [β, 1 - β] ≈ [0.067, 0.933],$ where $β = 1 2 - √ 3 4 ≈ 0.067$.
  Lemma : For any $j$-node $t$ of the merge tree $T_0$ with $j > 0$ (i.e., containing buckets), the sketch represented by $t$ contains a joinable pair of adjacent buckets, according to Definition~ [ref] .
  Lemma : For any time $t$ during processing the stream after initialization of buckets by Algorithm~ [ref] , SplineSketch contains a joinable pair of adjacent buckets, according to Definition~ [ref] .
  Lemma See \Cref{lem:existsBucketToMergeStreaming,lem:existsBucketToMerge} in Appendix~\ref{sec:analysis} for proofs in the streaming and mergeability settings, respectively.: After initialization of buckets by Algorithm~ [ref] , SplineSketch always contains a joinable pair of adjacent buckets, according to Definition~ [ref] .
  Observation : For any epoch $j > 0$, the $j$-nodes of $T_0$ form a disjoint union of paths.
  Proof: We maintain buckets in a doubly linked list $L$. We use a queue $Q_S$ to keep track of buckets that need to be split due to violating bucket bound. We also use two heaps: a max-heap $H_S$ to keep track of buckets that might get split due to the heuristic error~ [ref] , ordered by their heuristic errors, and a min-heap $H_J$ for joinable pairs of buckets, ordered by their heuristic errors after temporarily joining the pair. In both heaps, every node contains a pointer to the corresponding bucket (or pair of buckets) in $L$. Conversely, every bucket in $L$ contains a (constant size) list of pointers to nodes of $H_S$ and $H_J$ containing that bucket. Both heaps can be initialized in $O(k)$ tim
  Proof: We prove that $P( 1 2 ) ≤ 1 2 + √ 3 4 $. The other inequality then holds by symmetry, that is, considering the polynomial $Q(x) = 1-P(1-x)$ which is also non-decreasing and $Q( 1 2 ) = 1-P( 1 2 )$. From now on, we focus on proving that $P( 1 2 ) ≤ 1 2 + √ 3 4 $. Let us denote the coefficients of $P$ by $a, b, c$ and $d$, that is, \[ P(λ) = a λ^3 + b λ^2 + c λ + d \,. \] Because $P(0) = 0$, we have that $d=0$. At the same time, because $P(1) = 1$, we have that $a+b+c = 1$. From the assumption that the polynomial is non-decreasing, we also have that its derivative is non-negative, meaning that for any $λ [0,1]$, \[ 3 a λ^2 + 2b λ + c ≥ 0\,. \] At $1/2$, the polynomial evaluates to \[ f(a,b,c) 
  bound: For deterministic comparison-based algorithms, there is a gap of $O(log n)$ between the merge-and-prune algorithm using space $O( ^ -1 · log^3 n)$~ [cite] and the lower bound of $Ω( ^ -1 · log^2 n)$~ [cite] .
  bound: Efficient randomized sketches with relative error have only appeared recently, namely, ReqSketch with space $O( ^ -1 · log^ 1.5 n)$~ [cite] and its ``elastic`` version achieving space close the information-theoretic lower bound of $Ω( ^ -1 · log n)$~ [cite] ; however, mergeabilit
  bound: We also depict an upper bound on the theoretically optimal error bound of $O(1/k)$ in purple (loosely dotted), where $k$ is the sketch size; the hidden constant factor is based on the maximum error of KLL and GKAdaptive.
  bound: Combining the lower bound on the decrease due to a split with the upper bound on the total potential, it follows that the number of splits during $(t', t]$ is at most $$ t β· C_b· t 2.5· k = 2.5· k β· C_b \,.$$ We choose $C_b$ based on $β$ so that $2.5· k / (β· C_b) k/18$.
  bound: Second, we show an upper bound on the error only for the thresholds of the final sketch.
  bound: The lower bound, i.e., $ ^n( _i) -O(log α· n/k)$, can be obtained in a symmetric way.

## sigmod26-274 — Approximate Query Processing under Updates  (score 13)
domain: aqp
usage: Splitting the join tree into exact connex and approximated parts trades ratio for update speed; a Boolean-semiring reduction imports the dynamic lower bound.
note-quote: "For any b > 1, any free-connex query Q(H,F), all auxiliary views can be updated in amortized O(log_b M) time under an insertion-only update sequence."
  Lemma 4.3: The total size of all the auxiliary views is 𝑂 ( 𝑒 |𝑅𝑒 |). We then analyze the approximation guarantee.
  Theorem 4.4: For any relation 𝑅 ∈ T − 𝑆, the following inequality holds for all 𝑡: 𝐽𝑅 (𝑡) ≤ 𝐴𝑅 (𝑡) ≤ 𝑏 | T𝑅 | −1 ⊗ 𝐽𝑅 (𝑡). We can replace 𝐽𝑅𝑑 (𝜋key(𝑅𝑑 ) 𝑡) with 𝐴𝑅𝑑 (𝜋key(𝑅𝑑 ) 𝑡) for all 𝑅𝑑 ∈ 𝐷 in equation (1), which results in the following approximation: ! ! Ì Ì 𝑄˜ (𝑡) = 𝑅 ′ (𝜋var(𝑅 ′ ) 𝑡) ⊗ 𝐴𝑅𝑑 (𝜋key(𝑅 ) 𝑡) . (2) 𝑑 𝑅 ′ ∈𝑆 𝑅𝑑 ∈𝐷 Note that this is exactly the formula we used in the example above to compute the approximate annotation of the tuple (4). 
  Theorem 4.5: 𝑄˜ is a 𝑏 | T | − |𝑆 | − |𝐷 | -approximate result of 𝑄. Remark 1. For a 𝑏-approximation, we can find the largest 𝑏ˆ such that 𝑏ˆ | T | − |𝑆 | − |𝐷 | ≤ 𝑏 and replace 𝑏 with 𝑏ˆ when constructing the approximation views. Remark 2.
  Theorem 4: 5 assumes that every relation in T − 𝑆 − 𝐷 might receive updates. If we know that only 𝜅 of these relations are being updated, then the exponent |T | − |𝑆 | − |𝐷 | can be replaced by 𝜅. 4.3 Updates Let 𝑢𝑖 = (𝑅𝑢 , 𝑡𝑢 , Δ𝑢 ) be the 𝑖-th update. We use superscripts 𝑖 and 𝑖 + 1 to denote the relations and views before and after applying 𝑢𝑖 , respectively. As the auxiliary views are defined recursively, updates are also processed recursively. We calculate the changes Δ𝑅𝑢 , Δ𝐻𝑅𝑢 , Δ𝐴𝑅𝑢 , and Δ𝑃𝑅𝑢 for 
  Theorem 4.7: For any 𝑏 > 1, any free-connex query 𝑄 (H, F ), all auxiliary views can be updated in amortized 𝑂 (log𝑏 𝑀) time under an insertion-only update sequence, where 𝑀 = max𝑖,𝑒 max𝑡 ∈𝑅𝑒𝑖 𝐴𝑖𝑅𝑒 (𝑡). Applying this theorem to maintaining the join size of a 3-way join, which has 𝑀 = 𝑂 (𝑁 2 ), the amortized update cost is thus 𝑂 (log𝑏 𝑁 ). More generally, under the usual word-RAM assumption that the word size is 𝑂 (log 𝑁 ) bits and each annotation fits in a word, we always have 𝑂 (log 𝑀) = 𝑂 (log 𝑁 ). Note t
  Lemma 4.8: Let 𝑅 be any relation in 𝑆 and let 𝑡 be any tuple. If 𝑄 (𝑡) > 0, then 𝐻𝑅 (𝜋var(𝑅) 𝑡) = T. With the help of Lemma 4.8, we can start from the tuples in the helper view of the root relation 𝑅𝑟 and use a similar algorithm as in [44] to enumerate the set {𝑡 | 𝑄˜ (𝑡) > 0}. More precisely, for each 𝑡𝑟 ∈ 𝐻𝑅𝑟 , we call FullEnum(𝑅𝑟 , 𝑡𝑟 , T ) in Algorithm 3.
  Theorem 4.9: Algorithm 3 enumerates the set {𝑡 | 𝑄 (𝑡) > 0}, hence the full approximate query result 𝑄˜ = {(𝑡, 𝑄˜ (𝑡)) | 𝑄˜ (𝑡) > 0}, with constant delay. Example 4.10. Suppose we perform a full enumeration in Figure 6(a). Since 𝐻 [𝑋3 ] (4) = T, we invoke Algorithm 3 by calling FullEnum( [𝑋 3 ], (4), T ). Since C( [𝑋 3 ]) ∩ 𝑆 = ∅, it yields tuple Proc. ACM Manag. Data, Vol. 3, No. 6 (SIGMOD), Article 295. Publication date: December 2025. Approximate Query Processing under Updates 295:15 Algorithm 3: FullEnum
  Lemma 4.11: For the root relation 𝑅𝑟 , 𝐿𝑅𝑟 (𝑡𝑟 ) = T if and only if 𝐻𝑅𝑟 (𝑡𝑟 ) = T. For any non-root relation 𝑅𝑒 , 𝐿𝑅𝑒 (𝑡𝑒 ) = T if and only if 𝐻𝑅𝑒 (𝑡𝑒 ) = T and 𝐿par(𝑅𝑒 ) ⋉ 𝑡𝑒 ≠ ∅. The live views can be maintained during the delta enumeration process. 4.6.2 Algorithm. Our strategy is to identify specific tuples during the update process, referred to as witness tuples. Each witness tuple is associated with a relation, and we denote the set of witness tuples associated with a relation 𝑅 as 𝑊𝑅 . After the upda
  bound: For a fully-dynamic update sequence with both insertions and deletions, the 𝑂 (1) update cost can only be achieved√ for q-hierarchical queries [12, 24], which are a very restrictive query class, and there is an Ω( 𝑁 ) lower bound for non-q-hierarchical queries, where 𝑁 is the tot
  bound: Nevertheless, this theoretical lower bound only holds in pathological situations, while empirical results suggest that existing algorithms work well under realistic update sequences [24, 44].
  bound: IVM√𝜖 leverages the heavy/light data partitioning technique [26–29] to achieve an update time of 𝑂 ( 𝑁 ) for queries on three relations, which is worst-case optimal, and even 𝑂 (1) for specific query structures.
  bound: Approximate Query Processing under Updates 295:11 Algorithm 1: Approx(𝑦, 𝑏, ˜ 𝑦) Input : New value 𝑦, approximation ratio 𝑏 > 1, and the current approximation ˜ 𝑦; Output : An updated ˜ 𝑦; 1 if 𝑦˜ < 𝑦 or 𝑏 ⊗ 𝑦 < 𝑦˜ then 2 𝑦˜ ← 𝑏 ⊗ 𝑦; 3 return ˜ 𝑦; Leaf relation 𝑅3 ∈ 𝐷, so 𝐻𝑅3 = 𝑅
  bound: This is actually an inevitable consequence due to the lower bound result in [12], which says that (the √ Boolean version of) any non-q-hierarchical CQ must incur an amortized update cost of Ω( 𝑁 ) to maintain under a fully dynamic update sequence.
  bound: Note that any multiplicative approximation of the query result on the semiring (R ≥0, +, ×) yields an exact query result on the Boolean semiring, so the lower bound of [12] also holds for approximate query processing on (R ≥0, +, ×).

## sigmod26-203 — RadixGraph: A Fast, Space-Optimized Data Structure for Dynamic Graph Storage  (score 8)
domain: graph-db | storage-compression
usage: The occupancy model yields the space objective; dynamic programming over layer splits minimizes it; amortized counting of compaction costs bounds update time.
note-quote: "Under a single-threaded execution model, the amortized time complexity of edge insertion, update and deletion is O(1)."
  Theorem : Under a single-threaded execution model, the amortized time complexity of edge insertion, update and deletion is $O(1)$.
  Lemma : (Proof in Appendix A) There exists an optimal solution $(s_0^*,s_1^*,…,s_ l-1 ^*)$, such that $s_ l-1 ^*=x$.
  Theorem : The time complexity of computing $g(l-1,x)$ is $O(nlx^2)$ and the space complexity is $O(lx)$.
  Theorem : (Proof in Appendix A) The optimal value of $f$ is equal to $g(l-1,x)$.
  Proof: To compute $g(l-1,x)$, we need to compute $g(i,j)$ for all $0≤ i<l-1,0≤ j≤ x$, which contains $O(lx)$ subproblems. To compute each $g(i,j)$, we need to enumerate $k$ for all $0≤ k≤ j$ (i.e., $O(x)$ subproblems) to find the optimal transition. Each transition requires $O(n)$ multiplications and divisions. Therefore, the total time complexity is $O(nlx^2)$. For the space complexity, we need to store $g(i,j)$ for all $O(lx)$ subproblems and the previous optimal subproblem $g(i-1,k)$ contributing to $g(i,j)$, such that we can find a path for optimal transition to $g(l-1,x)$ which recovers the optimal $a_i$.
  Proof: [Proof Sketch] We prove the theorem by discussing the two cases of edge operations: (1) when the log segment is not full, appending an edge log to the segment clearly costs $O(1)$ time; (2) when a compaction is required, the compaction process costs $O(d)$, and is amortized to $O(1)$ for each edge operation. The complete proof can be found in Appendix B.
