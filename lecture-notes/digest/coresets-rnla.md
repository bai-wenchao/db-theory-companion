# Chapter digest: coresets-rnla
n_papers_with_tool: 8
PODS exemplars are from the PODS'26 issue inside PACMMOD Vol 4 — mark them ^PODS.

## sigmod26-289 — Clustering with Set Outliers and Applications in Relational Clustering  (score 66)
domain: + clustering-outliers
usage: LP feasibility plus greedy rounding with weight-charging arguments; Set-Cover reductions via line-point encodings; coresets shrink instances; randomized repetition over join samples.
note-quote: "If the Unique games conjecture is true, there exists no (1, f−ε, γ)-approximation algorithm that runs in polynomial time for the problem, where f = o(log n)."
  Theorem : Given a set $P$ of $n$ points in $ ^d$, a set $ $ of $m$ hyper-rectangles in $ ^d$, where $d$ is a constant, such that every point in $P$ belongs to one hyper-rectangle from $ $, positive integer parameters $k, z$, and a small constant $ (0,1)$, there exists a $(2+ , 2,O(1))$-approximation algorithm for the $ (P, ,k,z)$ problem that runs in $O(((n+m)log^ d-1 n +kmlog n+β_1log(β_1)+(k+z)(β_2+β_3)log^ d+2 (β_2)) n)$ time, where $β_1=min\ n,km\ $, $β_2=min\ n,kz\ $, and $β_3=min\ m,kz\ $.
  Definition $\gprob(P,\setrects,k,z)$: Given a set $P$ of $n$ points in $ ^d$, a set $ $ of $m$ hyper-rectangles in $ ^d$, where $d$ is a constant and every point in $P$ lies in at least one hyper-rectangle in $ $, and two integer positive parameters $k, z$, the goal is to compute a set of centers $C P$ with $|C|≤ k$ and a set $ $ of hyper-rectangles as outliers with $| |≤ z$ and $C ( _ P)= $, such that the cost excluding points covered by outliers $ (C,P ( _ P))$ is minimized.
  Theorem : For an acyclic join query $ $ of $d$ attributes, a database $ $ of input size $N$ with $| ( )|= (N)$, integer positive parameters $k, z$, and a small constant $ (0,1)$, there exists an $(1,1+ ,3+ )$-approximation for the $ ( , ,k,z)$ problem with probability at least $1- 1 N $ that runs in $O (Nlog N + k^2 δ log^3 N )$ time, where $δ= z | ( )| $. If $| ( )|=O(N)$, then the running time is bounded by $O(kNlog^2 N)$.
  Definition $\prob(P,\mathcal{H},k,z)$: Given a set $P$ of $n$ elements, a family $ H $ of $m$ subsets of $P$, where every element in $P$ belongs to at least one set in $ H $, and two integer positive parameters $k, z$, the goal is to compute a set of centers $C P$ with $|C|≤ k$ and a family $H H $ of outlier sets with $|H|≤ z$ and $C ( _ h H h)= $ such that the cost excluding the points covered by outliers $ (C,P ( _ h H h))$ is minimized.
  Theorem \cite{arora2012multiplicative: Given a feasibility problem as defined above, a parameter $ $, a $ $- ORACLE implemented in procedure $ Oracle (·)$, and an update procedure $ Update (·)$, there is an algorithm which either finds an $ $ such that $ i$, $A_i _i≥ b_i- $ or correctly concludes that the system is infeasible. The algorithm makes $O( (m')/ ^2)$ calls to procedures $ Oracle (·)$ and $ Update (·)$.
  Corollary : Given a set of $n$ elements $P$ in a metric space with bounded doubling dimension, a family of $m$ disjoint subsets $ H $ over $P$, and positive integer parameters $k,z$, there exists a $(2,2,O(1))$-approximation algorithm for the $ (P, H ,k,z)$ that runs in $O(nk+k^2m+β_1^2+(β_2+β_3)^ log β_1)$ time, where $β_1=min\ n,km\ $, $β_2=min\ n,kz\ $, and $β_3=min\ m,kz\ $.
  Theorem : For an acyclic join $ $ of $d$ attributes, a database $ $ of input size $N$, parameters $ , k, z$, a set $S ( )$ of size $k$ and a set $C ( )$ of size $|C|≤ (1+ )^2δ | ( )|$ can be computed with probability at least $1- 1 | ( )|^2 $, such that $ (S, ( ) C)≤ 3√ d _ k,z ( ( ))$, where $δ= z | ( )| $. The running of the algorithm is $ O ( k ^2δ N+ k^3 ^4δ^2 )$.
  Theorem : For a database schema $ $ with $g$ tables $R_1,…, R_g$, an acyclic join query $ $ of $d$ attributes, a database $ $ with $| |=N$, integer positive parameters $k, z$, and a small constant $ (0,1)$, there exists a $(2+ ,2,O(1))$-approximation algorithm for the $ ( , ,k,z)$ problem, with probability at least $1- 1 N $, that runs in $O(k^2N^2log N)$ time.
  Proof: [Proof of Lemma~ [ref] ] Starting from the input $X, Y $ of the $ SC (X, Y )$ problem we construct in polynomial time a set $P$ and a family of sets $ H $ that will serve as input to the $ $ problem. For every $x_i X$, we place a point $p_i$ on the real line at coordinate $i$. We define $ P = _ x_i X p_i$. Then for an arbitrary positive integer number $k$ such that $k≤ O(n'^2)$, we add $k$ additional points $ Q =\ q_1, …, q_k\ $ in $P$. Each point $q_j$ is placed at coordinate $2n'+j$. Overall $P= P Q $. For every set $y Y $, we create the set $ y =\ p_i x_i y\ $. Let $ Y =\ y y Y \ $. Furthermore, for every $j [k]$ we define a set $h_j=\ q_j\ $. Let $ H =\ h_j j [k]\ $. Overall $ = Y H $. L
  Proof: [Proof of Lemma~ [ref] ] For simplicity, in the correctness proof we consider that $x_i^*, y_j^*$ are the values of the variables $x_i, y_j$ in the last feasible LP solution we found. We first show i). Equivalently, we will prove that if $r≥ ^*_ k,z (P, H )$ then the corresponding IP is feasible. Since $r≥ ^*_ k,z (P, H )$, there exists the optimum set of $k$ centers $C^* P$ and a family of $z$ sets $H^* H $ such that $ (C^*,P _ h H^* h)= ^*_ k,z (P, H )$. Based on this optimum solution, we construct a solution to the IP as follows. If $p_i C^*$ then we set $ x _i=1$, otherwise we set $ x _i=0$. If $h_j H^*$ we set $ y _j=1$, otherwise $ y _j=0$. Since $|C^*|=k$ and $|H^*|=z$, the first two 
  bound: The bound on $C$ and the approximation ratio hold with probability at least $1- 1 | ( )|^2 $.

## sigmod26-053 [PODS] — Coresets for Robust Query Optimization  (score 44)
domain: query-optimization
usage: Hoeffding plus union bounds (Lemmas 3.1–3.2); greedy coverage with exponential termination (Lemma 3.4); global↔local risk transfer (Lemma 4.2); Markov argument (Lemma 4.6).
note-quote: "There exists an algorithm that samples O((κ*/δ²) ln(N) ln(δ⁻¹)) queries from W and computes ... a set C ⊆ Π of size O(κ* ln δ⁻¹) such that, with high probability, C is a (c₁ε, τ, c₂δ|W)-coreset for risk." (Thm 3.5)
  Lemma 3: 1 and 3.2 f are accurate estimates of failure probability and risk, respectively. The latter show that 𝑝e𝑟 and Risk implies that the greedy cover procedure defined using proxy risks behaves almost as if it had access to the true risks on the sampled queries. Using these two lemmas, we prove two structural claims about the algorithm. In Lemma 3.3, we show that whenever the algorithm terminates in round 𝑟 , the set C returned in that round satisfies 𝑝 (3𝜀, C) ≤ 3𝛿, implying that C is a valid (3𝜀, 
  Lemma 3.1: Let 𝜀 ′ ∈ (0, 1) and 𝑟 ≥ 1. Then 𝑝e𝑟 (𝜀 ′, F ) − 𝑝 (𝜀 ′, F ) ≤ 𝛿/4 holds simultaneously for all subsets F ⊆ Π of size at most b𝑟 with probability at least 1 − 𝑁 −𝑂 (1) .
  Lemma 3.2: For any round 𝑟 , any 𝜋 ∈ Π and any query 𝑄𝑖 ∈ W −𝑂 (1) with probability at least 1 − 𝑁 .
  Lemma 3.3: Assuming the conditions of Lemmas 3.1 and 3.2 hold in every round, the algorithm terminates with 𝑝 (3𝜀, C) ≤ 3𝛿.
  Lemma 3.4: Assuming the conditions of Lemmas 3.1 and 3.2 hold in every round, the algorithm terminates with 𝑘𝑟 ≤ 2𝜅 ∗ .
  Theorem 3.5: Let W be a workload distribution. Given a set of plans Π of size |Π| = 𝑁 and parameters 𝜀 ∈ (0, 1], 𝜏 ≥ 1, and 𝛿 ∈ (0, 1), let 𝜅 ∗ denote the size of the smallest (𝜀, 𝜏, 𝛿 |W)–coreset for risk. Fix any pair of constants 𝑐 1, 𝑐 2 > 1. There exists an algorithm that samples 𝑂 (𝜅 ∗ /𝛿 2 ) ln(𝑁 ) ln(𝛿 −1 )  queries from W and computes in time 𝑂 (𝜅 ∗ /(𝜀 2𝛿 2 )) 𝑁 ln3 (𝑁 ) ln(𝛿 −1 ) , a set C ⊆ Π of size  𝑂 𝜅 ∗ ln 𝛿 −1 such that, with high probability, C is a (𝑐 1𝜀, 𝜏, 𝑐 2𝛿 |W)–coreset for risk. 3
  Theorem 4.1: Let 𝜀, 𝛿 ∈ (0, 1) and 𝜏 ≥ 1 be parameters. Let W be a workload distribution over Q, and let F ⊆ Π be a (𝜀, 𝜏, 𝛿 | W)-coreset for risk. Let 𝑄 ∼ W. Suppose LocalInference(𝑄, F ) returns a plan ˆ 𝜋. Then, with probability at least 1 − 2𝛿, Risk( ˆ 𝜋, 𝑄 ; 𝜏 2 ) ≤ 2 Risk★ (𝑄 ; 𝜏) + 4𝜀. Moreover, the algorithm uses Θ(|F |(ln |F | + ln(1/𝛿))/𝜀 2 ) time. Here is a high-level overview of the proof of Theorem 4.1. Let F ⊆ Π be an arbitrary set of plans. For a query 𝑄, let 𝑟 = min𝜋 ∈ F Risk(𝜋, 𝑄 ; 𝜏) denote
  Lemma 4: 3 shows that the empirical quantity L-Risk approximates the true local risk L-Risk F . Next, Lemma 4.4 combines the guarantees of Lemmas 4.2 and 4.3 to show that LocalInference returns a plan whose global 𝜏 2 -risk is at most 2𝑟 + 2𝜀. Finally, we consider the case when F is an (𝜀, 𝜏, 𝛿 | W)-coreset for risk. In this case, with probability at least 1 − 𝛿, we have 𝑟 ≤ Risk★ (𝑄 ; 𝜏) + 𝜀. Combining this bound with the guarantee in Lemma 4.4 yields Theorem 4.1. We now give the proof in detail through
  Proof: Observe that for any set of plans F ⊆ Π, the random variable 𝑝e𝑟 (𝜀 ′, F ) is an unbiased estimator of 𝑝 (𝜀 ′, F ). Since each 𝑄𝑖 is drawn i.i.d. from W, by Hoeffding’s inequality [35],     Pr 𝑝e𝑟 (𝜀 ′, F ) − 𝑝 (𝜀 ′, F ) > 𝛿/4 ≤ 2 exp −2𝔫𝑟 (𝛿/4)2 = 2 exp −𝛿 2 𝔫𝑟 /8 . Taking a union bound over all sets of plans of size up to b𝑟 and using the fact that the number of subsets of plans of size at most b𝑟 is bounded by (b𝑟 + 1)𝑁 b𝑟 , we obtain    Pr ∃ F ⊆ Π with |F | ≤ b𝑟 : 𝑝e𝑟 (𝜀 ′, F ) − 𝑝 (𝜀 ′, F ) > 𝛿/4 ≤ 2(b + 1)𝑁 b𝑟 exp −𝛿 2 𝔫𝑟 /8 , where b𝑟 = 𝑐 3𝑘𝑟 ln(𝛿 −1 ) and 𝔫𝑟 = (𝑐 1𝑘𝑟 /𝛿 2 ) ln(𝑁 ) ln(𝛿 −1 ). By a suitable choice of the constants 𝑐 1 and 𝑐 3 , the right-hand side is 𝑁 −𝑐 for an
  Proof: For a fixed plan 𝜋 ∈ Π and 𝑄𝑖 ∈ W𝑟 , let 𝑌𝑖,𝑗 = 1{Pnl(𝜋, 𝑄𝑖,𝑗 ) > 𝜏 }. Then Risk(𝜋, 𝑄𝑖 ) = 1 Í𝔪𝑟 𝑗=1 𝑌𝑖,𝑗 and E[𝑌𝑖,1 ] = Risk(𝜋, 𝑄 𝑖 ). By Hoeffding’s inequality [35], 𝔪𝑟 f f i h  f 𝑄𝑖 ) − Risk(𝜋, 𝑄𝑖 ) > 𝜀/10 ≤ 2 exp −𝔪𝑟 𝜀 2 /50 . Pr Risk(𝜋, Proc. ACM Manag. Data, Vol. 4, No. 2 (PODS), Article 100. Publication date: May 2026. 100:8 Rahul Raychaudhury, Haibo Xiu, Pankaj K. Agarwal, Stavros Sintos, & Jun Yang By plugging in 𝔪𝑟 = (𝑐 2 /𝜀 2 ) ln(𝑁 ) ln(𝔫𝑟 ) from the algorithm and taking a union bound over all f𝑟 , the failure probability is at most |Π| · 𝔫𝑟 = 𝑁 · 𝔫𝑟 pairs (𝜋, 𝑄𝑖 ) ∈ Π × W 2𝑁 𝔫𝑟 exp(−𝑐 2 ln(𝑁 ) ln(𝔫𝑟 )/50) = 2𝑁 𝔫𝑟 𝑁 − (𝑐 2 /50) ln(𝔫𝑟 ) . This is at most 𝑁 −𝑂 (1) for a sufficient

## sigmod26-303 — Effective Clustering for Large Multi-Relational Graphs  (score 41)
domain: graph-db
usage: Neumann series with Lidskii/Courant–Fischer eigenvalue arguments; Sinkhorn–Knopp scaling (Birkhoff–von Neumann) for a symmetric doubly stochastic affinity; CountSketch for scalable norm estimation.
note-quote: "If S_ω is doubly stochastic and Z=Y^T, optimizing [the relaxed objective] is equivalent to optimizing Σ_k Σ_{v_i∈C_k} ‖z_i − c^(k)‖²₂, where c^(k) is the center of cluster C_k."
  Corollary : Let $ R ^ M × m $ be a count-sketch matrix and $ ^ (r) = ^ (r) $, where $m=O(rε^ -4 log (r/εδ) · (r+log (1/εδ) ))$, $ε$ is an error threshold and $r$ is the rank of $ ^ (r) $. Then, \| ^ ^ (r) \|^2_F = (1 ε)^2· trace ( ^ ( - ^ (r) ) ) holds with a probability of at least $1-δ$.
  Theorem : If $ $ is doubly stochastic and $ = ^ $, optimizing Eq.~ [ref] is equivalent to optimizing $ _1,…, _K min Σ_ k=1 ^KΣ_ v_i _k \| _i- c ^ (k) \|^2_2 $, where $ c ^ (k) =Σ_ v_j _k _j | _k| $ stands for the center of cluster $ _k$.
  Lemma \cite{horn2012matrix: Let $ $ be a matrix whose dominant eigenvalue $λ$ satisfies $|λ|<1$. Then, $ - $ is invertible, and its inverse $( - )^ -1 $ can be expanded as a Neumann series: $( - )^ -1 =Σ_ =0 ^∞ ^ $.
  Lemma Lidskii Inequality~\cite{horn1962eigenvalues,lidskii1982spectral: Suppose $ $ is a random matrix, and let $λ( )$ denote the largest eigenvalue of $ $, For any Hermitian matrices $ $ and $ $, the following inequality holds: λ( + ) ≤ λ( ) + λ( )
  Theorem \cite{horn2012matrix: Let $ $ be a matrix whose dominant eigenvalue $λ( )$ satisfies $|λ( )|<1$. Then, the inverse $( - )^ -1 $ can be expanded as a Neumann series: $( - )^ -1 =Σ_ =0 ^∞ ^ $.
  Definition $(\ell_1,\ell_2)$-Order Maximum Eigengap: Let $λ_ i ( )$ be the $i^ th $ eigenvalue of $ $. The $( _1, _2)$-order maximum eigengap is $μ_ _1, _2 = 1 i N max | λ_ i ( ) ^ _1 - λ_ i ( ) ^ _2 | $.
  Theorem : $\| - ^ \|_F Σ_ =L+1 ^ ∞ α^ (1+α)^ +1 \| ^ - ^L \|_2·\| \|_F$, which can be upper bounded by $ ( α 1+α )^ L+1 · \| \|_F· 1 max \ μ_ L,L+ $.
  Lemma : If $ $ is required to be an $N× K$ NCI as in Eq.~ [ref] , then min_ D ( , ) max_ trace ( ^ ) .
  Proof: [ Proof of Lemma~ [ref] ]
  Proof: [ Proof of Eq.~ [ref] ] Let $s_i=\| _i\|_1$. By the definition of the DE, we can rewrite $ D ( , )$ in Eq.~ [ref] as follows: D ( , ) =& 1 2 Σ_ v_i,v_j _ i,j · \| _i / √ s_i - _j / √ s_j \|^2_2 \\ =& 1 2 Σ_ k=1 ^KΣ_ v_i,v_j _ i,j · ( _ i,k / √ s_i - _ j,k / √ s_j )^2 \\ =& 1 2 Σ_ k=1 ^KΣ_ v_i, v_j _k _ i,j | _k| · ( 1 √ s_i - 1 √ s_j ) ^2 \\ & + 1 2 Σ_ k=1 ^KΣ_ v_i _k, v_j _k _ i,j · 1 | _k|· s_i . If we assume that $s_i=s_j\ v_i,v_j $, we can derive that the minimization of $ D ( , )$ is equivalent to minimizing $$Σ_ k=1 ^KΣ_ v_i _k, v_j _k _ i,j | _k| .$$
  bound: Note that the N-cut problem has been proven to be NP-hard~ [cite] .
  bound: Particularly, SNEM-Rounding iteratively seeks an orthogonal transformation matrix $ $ that most closely maps $ $ to NCI $ $ in terms of spectral norm: [display] As such, \( trace ( ^ ) ≈ trace ( ^ ^ ) = trace ( ^ ) \), meaning that the loss \( trace ( ^ ) \) is close to the maxim
  bound: Basic Idea As remarked in Figure~ [ref] , relies on a partial eigendecomposition of the $N× N$ dense affinity matrix $ $ to approximately solve the NP-hard problem in Eq.~ [ref] , which takes $O(N^2· (d+K))$ time and is still prohibitively expensive.
  bound: As analysed in [cite] , the above objective is equivalent to finding an NCI optimizing the following trace minimization problem: [display] which is an NP-hard problem given the constraint in Eq.~ [ref] on $ $. [display] A common way is to compute an approximate solution by relaxi

## sigmod26-349 — Proximity Graphs for Similarity Search: Construction, Lower Bounds, Euclidean Separation  (score 36)
domain: ann-vector-search
usage: Covering-net decompositions yield sparse constructions; adversarial point sets give size lower bounds; a log-drop distance-descent lemma bounds greedy query paths.
note-quote: "there is a set P of Θ(n) points ... such that any 2-PG for P must have Ω(n log Φ) edges, regardless of the query time allowed."
  Theorem : The following statements are true: ; For any integers $ $ and $n$ that are powers of 2 satisfying $n 2$ and $n^2 2 2^n$, there is a set $P$ of $Θ(n)$ points with aspect ratio $ $ from a metric space whose doubling dimension is 1, such that any 2-PG for $P$ must have $Ω(n log )$ edges, regardless of the query time allowed. ; Given any integers $s 2$, $t 1$, and constant $d 1$, there is a set $P$ of $n = s^d · t$ points with aspect ratio $ = O(n)$ from a metric space whose doubling dimension $λ$ i
  Theorem : Given any set $P$ of $n$ points in the metric space $( ^d, L_2)$ where $d = O(1)$, there is a $(1+ )$-PG that has $O((1/ )^ λ · n)$ edges and ensures a query time of $O((1/ )^λ · log^2 + (1/ )^ d-1 log n · log^2 )$, where $n$ and $ $ are the size and aspect ratio of the data input, respectively, and $λ = O(d)$ is the doubling dimension of $( ^d, L_2)$. W.h.p., we can build such a graph in $(1/ )^ λ · n (n )$ time.
  Lemma : Fix an arbitrary point $q $, and an arbitrary point $p^ P$ that is not a $(1+ )$-ANN of $q$. Define p^+_ = the out-neighbor of $p^ $ closest to $q$. Both of the following statements are true: ; $D(p^+_ , q) < D(p^ , q)$. ; [The log-drop property] Let $ $ be any point in $P$ satisfying $D( , q) D(p^+_ , q)$. If $ $ is not a $(1+ )$-ANN of $q$, then log D( , p^*) < log D(p^ , p^*) where $p^*$ is an exact NN of $q$.
  Theorem : For a metric space with a constant doubling dimension $λ$, there is a $(1+ )$-PG that has $O((1/ )^λ · n log )$ edges and $O((1/ )^ λ · log^2 )$ query time, where $n$ and $ $ are the size and aspect ratio of the data input, respectively. We can construct such a graph in $(1/ )^ λ · n (n )$ time.
  Lemma : Let $x$ and $y$ be two points that are on the surfaces of $B(q, r)$ and $B(q, (1+ )r)$, respectively. If $L_2(p, x) = L_2(p, y)$, then the angle between the rays $ _ p, x $ and $ _ p, y $ is strictly larger than $ /8$.
  Fact : Let $a, b,$ and $c$ be three distinct points in $ ^d$ such that the angle $γ$ between rays $ _ a, b $ and $ _ a, c $ satisfies $0 < γ < /2$. If $L_2(a, b) = L_2(a, c) = l > 0$, then $L_2(b, c)< l · γ$.
  Lemma : The following inequalities are correct: l &<& (2+ ) · r \\ L_2(p^*, v) & & l · γ \\ L_2(v, _ ) &<& l · (1 - γ) \\ L_2( _ , p_ ) & & l · γ.
  Fact : If a point $p P$ is not a $(1+ )$-ANN of $q$, either $log D(p, p^*) α - 1$ or $D(p, q) > D(y^ , q)$.
  Proof: Fix any point $p P$. If $p Y_i$ for some $i [0, h]$, there is a point $y Y_i$ with $D(p, y) 2^i$ due to the covering property. This $y$ must be an out-neighbor of $p$. Next, we assume that $p Y_i$ for all $i [0, h]$. Denote by $j$ the highest value of $i$ satisfying $|Y_i| 2$. Let $y$ be any point in $Y_i$ different from $p$. We argue that $y$ must be an out-neighbor of $p$, i.e., $D(p, y) · 2^j$. Indeed, this is true if $j = h$ because $D(p, y) (P) 2^h$. Consider now $j < h$. It follows from the definition of $j$ that $|Y_ j+1 | = 1$, in which case the covering property tells us $D(p, y) 2^ j+1 < · 2^j$.
  Proof: [Proof of Fact~ [ref] ] Suppose that $D(p, p^*) > 2^ α - 1 $ and $D(p, q) D(y^ , q)$ hold simultaneously. We argue that in this case $p$ must be a $(1+ )$-ANN of $q$, which will then validate Fact~ [ref] . Consider first $β = 0$. From [ref] , we know $D(p^*, y^ ) 1$, implying that $y^ = p^*$ (the inter-point distance in $P$ is at least 2). The condition $D(p, q) D(y^ , q)$ asserts that $p$ must be an exact NN of $q$. The subsequent discussion assumes $β = α - - 1 > 0$. By $D(p, p^*) > 2^ α-1 $ and $D(y^ , p^*) 2^β = 2^ α- -1 $, we obtain && D(y^ , p^*) < D(p, p^*) / 2^ \\ && D(p,q) D(y^ , q) D(y^ , p^*) + D(p^*, q) < D(p, p^*) / 2^ + D(p^*, q). On the other hand, the triangle inequality show
  bound: The hard inputs used in our lower-bound arguments are non-geometric, thus prompting the question of whether improvement is possible in the Euclidean space (a key subclass of metric spaces).
  bound: It is worth mentioning that the parameter $t$ in Statement (2) permits the lower bound to hold for a wide range of $ $.
  bound: A Size Lower Bound under $ = 1 $ This section serves as a proof of Statement (1) of Theorem~ [ref] .
  bound: A Size Lower Bound under Small $ $ This section serves as a proof of Statement (2) of Theorem~ [ref] .
  bound: When $t = 1$, the set $P$ degenerates into a hard input used in [cite] to prove a lower bound on the query time of ANN data structures.
  bound: Generalizing that hard input to establish a size lower bound for $(1+ )$-PGs under a wide range of $ $ demands additional ideas, as we have shown above.

## sigmod26-052 — Constrained Shortest Path Finding on Terrain Surfaces  (score 34)
domain: spatial
usage: Contradiction and induction for spanner lemmas; triangle-inequality charging; Kruskal MST plus pre-order traversal gives the factor 2.
note-quote: "It holds that dg(u,v) ≤ dG(u,v) ≤ (1+ε)·dg(u,v), where dG(u,v) is the shortest distance between u and v on G."
  Definition 2: 1 (Geodesic Shortest Path). The geodesic shortest path between point 𝑠 and point 𝑡, denoted by Π𝑔 (𝑠, 𝑡), is the path on 𝑇 with the minimum length from 𝑠 to 𝑡. In Figure 1, the geodesic shortest path between 𝑠 and 𝑡 is shown in dotted line segments. The length of Π𝑔 (𝑠, 𝑡), denoted by 𝑑𝑔 (𝑠, 𝑡), is called the geodesic distance between 𝑠 and 𝑡. Building upon the definition of the geodesic shortest path for two given points, we extend it to trips that involve multiple points-of-interest.
  Theorem 2: 3 (Hardness). Problem CPTS is NP-hard.
  Theorem 3: 3 (Distance on the Spanning Graph). The spanning graph G is a connected graph. That is, for any pair of distinct vertices 𝑢 and 𝑣 on G, there exists a path from 𝑢 to 𝑣 on G. In addition, it holds that 𝑑𝑔 (𝑢, 𝑣) ≤ 𝑑 G (𝑢, 𝑣) ≤ (1 + 𝜖) · 𝑑𝑔 (𝑢, 𝑣), where 𝑑 G (𝑢, 𝑣) is the shortest distance between 𝑢 and 𝑣 on G.
  Lemma 3.4: For any pair of distinct vertices 𝑢 and 𝑣 on G, there is an edge (𝑢, 𝑥) in G such that 𝑑 (𝑢,𝑥 ) 𝑑𝑔 (𝑥, 𝑣) ≤ 𝑔 𝜎 .
  Lemma 3: 5 is for the concept of maximum weight of 𝐿. 𝑑 (𝑢,𝑣)
  Lemma 3.5: The maximum weight 𝑤 𝐿𝑖 of 𝐿 is at most (𝜎𝑔 −1) 𝑖 .
  Lemma 3.6: There are at most ⌈log𝜎 −1 min𝑜,𝑜 ′ ∈𝑃𝑔∪{𝑠,𝑡 } 𝑑𝑔 (𝑜,𝑜 ′ ) ⌉ iterations in our iterative procedure. Proc. ACM Manag. Data, Vol. 4, No. 3 (SIGMOD), Article 154. Publication date: June 2026. Constrained Shortest Path Finding on Terrain Surfaces 154:11 𝑑 (𝑢,𝑣)
  Lemma 3.7: Each terminal pair (𝑜, 𝑜 ′ ) (i.e., an edge on G) inserted into 𝐿 in the 𝑖-th iteration in 1 𝑖 our iterative procedure has the weight smaller than or equal to 𝜎 · 𝑑𝑔 (𝑢, 𝑣) · ( 𝜎 −1 ).
  Proof: We prove that CPTS is NP-hard by transforming the renowned Euclidean Travelling Salesman Problem (ETSP) [24] (which has been proved to be NP-hard [23]) into CPTS. We first present the decision version of the CPTS problem. Given a terrain surface 𝑇 , two points 𝑠 and 𝑡 on the surface of 𝑇 , a set 𝑃 of points-of-interest on the surface of 𝑇 , and a non-negative real number 𝑟 , the decision version of the CPTS problem is to determine if there is a path Π(𝑠, 𝑡 |𝑃) on the surface of 𝑇 such that (1) the path starts from 𝑠 and ends with 𝑡, (2) the path passes through all points in 𝑃, and (3) the length of the path is at most 𝑟 . The Euclidean Travelling Salesman Problem (ETSP) is defined as follows
  Proof: We first prove the following lemma. Proc. ACM Manag. Data, Vol. 4, No. 3 (SIGMOD), Article 154. Publication date: June 2026. 154:10 Victor Junqiu Wei, Min Xie, and Weicheng Wang

## sigmod26-127 — HeteroFedSyn: Differentially Private Tabular Data Synthesis for Heterogeneous Federated Settings  (score 18)
domain: privacy-dp
usage: zCDP additivity plus post-processing for budget allocation across marginals; expectation calculations strip projection and noise bias; random projection compresses 2-way marginals before release; greedy selection trades noise error against dependency error.
note-quote: "z_{a,b} − ‖z_{a∗b}‖²₂ − [kασ₂² + ασ₁²(s_b‖M̂_a‖²₂ + s_a‖M̂_b‖²₂) − s_a s_b α²σ₁⁴] is an unbiased estimator of the square of InDif²_{a,b}."
  Definition : ( ($ε$, $δ$)-Differential Privacy ). An algorithm $ M $ satisfies ($ε,δ$)-DP,where $ε, δ ≥ 0$, if and only if for any neighboring datasets $D$ and $D'$ that differ in one element, and any possible outputs $ R Range( M )$, we have \[ [ M (D) R ] ≤ e^ ε [ M (D') R ] + δ.\]
  Theorem : ~ [cite] For any pair of attributes $(A_i, A_j)$, the KL divergence of conditional estimation is no larger than that of independent estimation: KL \! (P_ ij \,\|\, P_ ij ) \; \; KL \! (P_ ij \,\|\, P_i P_j ), where \[ P_ ij = Σ_ A P( A)\, P(A_i A)\,P(A_j A). \]
  Definition : (Zero-Concentrated Differential Privacy (zCDP)). A randomized mechanism $ M $ satisfies $ $-zCDP with parameter $ > 0$ if, for all neighboring datasets $D$ and $D'$ differing in a single element, and for all $α > 1$, we have: \[ D_ α ( M (D) \| M (D')) ≤ α, \]
  Theorem : (Unbiased estimation of $ InDif2 _ a,b $). $ \|z_ a,b -z_ a*b \|_2^2 - [ kασ_2^2 + ασ_1^2 (s_b\| M_a\|_2^2+s_a\| M_b\|_2^2 ) - s_a s_b α^2 σ_1^4 ]$ is an unbiased estimator of the square of $ InDif2 _ a,b $, where $α= Σ_i n_i^2 n^2 $.
  Definition : (Gaussian Mechanism). Given a function \( f: D R ^d \), the Gaussian Mechanism adds Gaussian noise to ensure differential privacy. Specifically, the Gaussian Mechanism is defined as: \[ M (D) = f(D) + N (0, σ^2 I), \]
  Theorem : (Privacy Guarantee for 2-Way Marginals) Let $ M_ a,b ^ c_i P_ a,b -M_ a,b ^ c_i' P_ a,b _2 _2, a,b \ 1,..., d\ , b>a, i \ 1,..., c\ $. Then for any $ _2>0$, $z_ a,b $ is $ _2$-zCDP if $σ_2 _2√ d(d-1) 4 _2 $.
  Definition : (Composition of zCDP~ [cite] ) For a sequence of mechanisms \( M _i\) satisfying \( _i\)-zCDP for \(i = 1, 2, , k\), their composition $ M = ( M _1, M _2, , M _k)$ satisfies $Σ_ i=1 ^ k _i -zCDP $.
  Theorem : (Privacy Guarantee for 1-Way Marginals). Let $ M_a^ c_i -M_a^ c_i' _2 _1, a \ 1,..., d\ $, $i \ 1,..., c\ $. Then for any $ _1>0$, $ M_a$ is $ _1$-zCDP if $σ_1 _1√ d 2 _1 $.
  Proof: Denote the count of items on attribute $a$ as $a_1,..., a_ s_a $. Assume that we add one sample to the dataset of participant $c_i$, the value of attribute $a$ belongs to the $j^ th $ item $a_j$. Additionally, the number of sample held by each participant $n_i$ should larger than $1$. Then we have & _1=max M_a^ c_i - M_a^ c_i' _2 \\ &=max √ Σ_ i=q, q≠ j ^ s_a ( a_q n_i - a_q n_i+1 )^2+( a_j n_i - a_j+1 n_i+1 )^2 \\ &=max √ Σ_ i=q, q≠ j ^ s_a a_q^2 n_i^2(n_i+1)^2 + (a_j-n_i)^2 n_i^2(n_i+1)^2 \\ &=max 1 n_i(n_i+1) √ Σ_ i=q ^ s_a a_q^2+n_i(n_i-2a_j) \\ & √ 2 n_i 1
  Proof: Denote the count of items on attribute $a$, $b$ as $a_1,..., a_ s_a $ and $b_1,..., b_ s_b $, respectively. Then the domain size of $2$-way marginal on attribute $a$ and $b$ is $s_as_b$, denote the count of items as $c_1,...,c_ s_as_b $. Assume we add one sample to the $i^ th $ user's datasets, the value of attribute $a$ and $b$ belong to $i_0^ th $ and $j_0^ th $ items, respectively. We have _2&=max M_ a,b ^ c_i P_ a,b -M_ a,b ^ c_i' P_ a,b _2 \\ & max M_ a,b ^ c_i -M_ a,b ^ c_i' _2 _ 1 i s_as_b √ Σ_ j=1 ^k P[i, j]^2 (based on [cite] ) \\ &=√ Σ_ (i,j)≠(i_0,j_0) ( c_ i,j n_i - c_ i,j n_i+1 )^2+( c_ i_0,j_0 n_i - c_ i_0,j_0 +1 n_i+1 )^2 \\ & _ 1 i s_as_b √ Σ_ j=1 ^k P[i, j]^2 max_ 1 i s_as_b 
  bound: Next, we provide the upper bound on the sensitivity $ _1$ of the $1$-way marginal in the:delta_1 .
  bound: We provide an upper bound on the sensitivity $ _2$ in the:delta_2 .
