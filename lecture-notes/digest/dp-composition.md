# Chapter digest: dp-composition
n_papers_with_tool: 18
PODS exemplars are from the PODS'26 issue inside PACMMOD Vol 4 — mark them ^PODS.

## sigmod26-298 — Differentially Private Explanations for Clusters  (score 61)
domain: privacy-dp
usage: Composition/post-processing calculus assembles the end-to-end privacy proof; direct sensitivity analyses (L1 histogram algebra, binary-entropy limits) justify replacing standard divergences with low-sensitivity scores.
note-quote: "Given a clustering function f: ..., alg:gen_global_explanation is (ε_CandSet + ε_TopComb + ε_Hist)-DP."
  Proposition : The following holds for DP [cite] : ; Sequential Composition: Let $ _1: $ and $ _2: × $. Suppose $ _1$ satisfies $ _1$-DP and for every $y $, $ _2(·,y)$ satisfies $ _2$-DP (as a function of its first input). Define $ _3: $ by $ _3(D) = _2(D, _1(D))$. Then, $ _3$ satisfies $( _1+ _2)$-DP ; Parallel Composition: Suppose $ _1: $ satisfies $ _1$-DP and $ _2: $ satisfies $ _2$-DP. Let $ _1, _2 (R)$ be disjoint subsets of the input domain. Define $ ': × $ by $ '(D)=( _1(D _1), _2(D _2))$. Then, $ '$ s
  Definition : An HBE mechanism $ (D,f)$ is an algorithm that takes a dataset $D$ and a clustering function $f: (R) C$, and outputs a global HBE ( def:global-candidate ) for the clustering of $D$ defined by $f$. We say that $ $ satisfies $ _ exp $-DP if for any clustering function $f$, for any pair of neighboring datasets $D D'$, and any set of explanations $S Range( )$, it holds $ [ (D,f) S] e^ _ exp · [ (D',f) S]$.
  Definition Pair Diversity: For a dataset $D$ and a clustering function $f$, the diversity score of a pair of attributes $A_c, A_ c' $, where $A_c$ (respectively $A_ c' $) is a candidate attribute for explaining $D_c = t f(t)=c $ (respectively $D_ c' $), is &d(D, f, c, c' , A_c, A_ c' ) = min |D_c|, |D_ c' | × \\ & 1 & A_c ≠ A_c \\ 1 2 Σ_ a (A) _ A=a (D_c) max D_c ,1 - _ A=a (D_ c' ) max |D_ c' |,1 & A_c=A_ c'
  Definition Global Score: Let $ λ = (λ_ , λ_ , λ_ )$ be non-negative parameters that sum to $1$. For a dataset $D$ and a clustering function $f$, define the overall quality score of an attribute combination $ $ as _ λ (D,f, ) &= λ_ · (D, f, )+λ_ · (D, f, ) \\ & + λ_ · (D, f, ) where we extend $ (D, f, ) = 1 Σ_ c C ( D, f, c, (c))$ and $ (D, f, ) = 1 Σ_ c C ( D, f, c, (c)). $
  Definition Jensen-Shannon Divergence \cite{lin1991divergence: For two distributions $p$ and $q$ over the same domain, their Jensen–Shannon divergence is defined as \[ JSD(p,q) = H ( p+q 2 )- 1 2 H(q) - 1 2 H(q) \] where $H(·)$ is the Shannon entropy and $ p+q 2 $ is the mixture distribution of $p$ and $q$. The Jensen–Shannon distance , denoted $d_ JS $, is the square root of the Jensen–Shannon divergence.
  Corollary : Let $D$ be a dataset, $f$ a clustering function, $A$ an attribute, and $D_ c , D_ c' $ non-empty clusters such that $ D_c D_ c' $. The pairwise-diversity score ( def:pairwise_diversity ) is \[ d(D, f, c, c' , A, A) = 1 2 _A(D_c) - |D_c| |D_ c' | _A (D_ c' ) _1 \]
  Definition Single-Cluster Score: Let $γ = (γ_ ,γ_ )$ be a pair of non-negative parameters that sum to $1$. For a dataset $D$ and a clustering function $f: (R) C$, define the quality score $A $ as a candidate attribute for a cluster $c C$, as _ γ (D,f,c,A) = γ_ · (D,f,c,A) +γ_ · (D,f,c,A) .
  Definition Global Diversity: For a dataset $D$ and a clustering function $f: (R) C$, define the diversity score of an attribute combination $ $ as (D, f, ) = 1 2 Σ_ c,c' C d(D, f,c,c', (c), (c')) where the sum is over all distinct pairs of cluster labels.
  Proof: [Proof Sketch] The execution of alg:topk-single satisfies $ _ CandSet $-DP by prop:topk-single privacy . Since the function $ _ λ $ has sensitivity $1$ ( prop:global_score-sensitivity ), the execution of the exponential mechanism in gen_global_explanation:line 5 guarantees $ _ TopComb $-DP, applying thm:exponential_mech . We assume that each execution of $ _ hist (·, )$ satisfies $ $-DP. Therefore, by sequential composition, the computation of $ h _A$ for all $A '$ satisfies overall $ _ Hist /2$-DP. Since the clusters are disjoint, the computation of all noisy histograms $ h ^c$ satisfies overall $ _ Hist /2$-DP by parallel composition. Note that the computation of each $ h^ -c $ in gen_glob
  Proof: Let $D D'$ be two neighboring datasets. By the triangle inequality, f(D) - f(D') &= Σ_ i=1 ^m α_i · f_i(D) - Σ_ i=1 ^m α_i · f_i(D') \\ & Σ_ i=1 ^m α_i · f_i(D) -f_i(D') \\ & Σ_ i=1 ^m α_i _i.
  bound: By the triangle inequality, [display] Interestingness We prove next the high sensitivity of the total variation distance interestingness measure [ref] , restated below for convenience. [display] Since the range bound is standard (see, e.g., [cite] ), we focus on the sensitivity l
  bound: Since $ D'_c D' $ holds as well, we have [display] Since $ _a _1 =1$ and $ _A(D) _1=|D|$, we conclude [display] For the range upper bound, we use lemma:interest_vector to obtain [display] Sufficiency We revisit the notion of sufficiency from prior work and provide its sensitivity
  bound: If either of $D_c$ or $D_ c' $ is empty, then by claim:divers-upperbound , we have $d(D_c,D_ c' , A) = 0$, and $d(D_c,D'_ c' , A) 1$, and so \[ d(D, c, c', A)- d(D', c, c', A) 1. \] Hence, we proceed with the assumption that both are not empty.
  bound: For the range bound, note that $d(D, f, c, c', A_c, A_ c' )$ attains its upper-bound ( claim:divers-upperbound ), in particular, when $A_c≠ A_ c' $.
  bound: The range upper bound for $ $, $R_ $, is given by $ prop:sens-div $.

## sigmod26-178 [PODS] — Optimal Pure Differentially Private Sparse Histograms in Deterministic Linear Time  (score 54)
domain: privacy-dp
usage: Four-case distribution-dominance analysis over neighboring supports plus DP composition proves privacy; geometric-mechanism estimator accuracy carries through to per-coordinate utility.
note-quote: "The utility guarantee of Theorem 4.1 is an ℓ∞ bound and is asymptotically optimal, matching the lower bound Ω(min{1/ε ln d, n}) of Balcer and Vadhan."
  Theorem $(0, \delta)$-Approximate Time-Oblivious Sampler: Let $μ$ be a distribution with discrete support, let $δ (0, 1)$, and let $ δ / 2 $ denote a subset of minimum size such that $μ δ / 2 1 - δ / 2$. Assume access to the following two oracles: [label=$ $, leftmargin=0.7cm] ; The labeling oracle $ $, which assigns labels to the elements in $ δ / 2 $ from $0$ to $m - 1$. For each $i m $, $ (i)$ returns the corresponding element. ; The binary probability oracle $ $, which, given $x δ / 2 $ and $ _+$, returns the $ $-bit binary expansion of $μ(x)$ afte
  Theorem Private Sparse Histogram Circuit: \ Given integers $n, d, a_ , b_ , a_γ, b_γ _+$ that fit in a constant number of machine words, and $γ a_γ / b_γ 1 / n^ O(1) , 1 $, there exists a circuit with cost \[ O n + n · + n · + n · 1 · ln 1 + ln d γ + ln^2 d γ , \] where $ n $ is the circuit cost of sorting $n$ elements represented using $ $ bits, and $ $ is the circuit cost of multiplying two $ $-bit numbers. This circuit implements a mechanism that takes a dataset $ $ of $n$ user-contributed elements from domain $[d]$ as input, and out
  Theorem \ourAlgoName: Given integers $n, d, k, a_ , b_ , a_γ, b_γ _+$ that fit in a constant number of machine words, where $ a_ b_ _+$ and $γ a_γ / b_γ 1 / n^ O(1) , 1 $, and a dataset $ 1 , …, n [d]^n$, there exists an algorithm, denoted by $ $, that outputs a histogram $ n ^d$ approximating the original histogram $ $ of $ $, satisfying $ _0 O(n)$ and [leftmargin=0.4cm, label=$ $] ; Privacy Guarantee: $ $ satisfies $max 3 2 + 1 k + 1 , 2 $-differential privacy. ; Utility Guarantee: There exists a universal constant
  Theorem Purified Approximate Discrete Laplace Sampler: Let $n ^+$, $ _+$, and $γ _+ (0, 1)$. There exists a randomized algorithm $ n γ : n n $ with the following properties: [label=$ $, leftmargin=0.7cm] ; For each $t [n]$, $ n γ t - 1 $ and $ n γ t $ are $( , 0)$-indistinguishable. ; For each $t n $, $β > 2 · γ$, $ n γ t $ is $(α, β)$-accurate estimator of $t$, for $α 1 · ln 2 β - 2 + n 1 + n · γ O 1 · ln 1 β $ ; It has initialization time $O ( 1 · ln 1 , ln n, ln 1 γ )$ and memory usage $O ( ( 1 + ln 1 γ + ln n ) · ( ln 1 + ln 1 γ + ln n ) )$ bits
  Theorem Approximate Discrete Laplace Sampler: Let $ _+$ and $δ _+ 0, 1 $. There exists a $(0, δ)$-approximate time-oblivious sampler, denoted by $ δ $, for the discrete Laplace distribution $ e^ - $, with the following properties: [label=$ $, leftmargin=0.7cm] ; The sampler has initialization time $O 1 · ln 1 , ln 1 δ $, and memory usage $O ( 1 + ln 1 δ · ( ln 1 δ + ln 1 ) )$ bits, ; Each sample consumes at most $log_2 1 · ln 1 δ + 2 · log_2 1 δ + O(1)$ unbiased random bits, which runs in $O 1 · ln 1 + ln 1 δ $ time in the word-RAM model, w
  Theorem Relaxed Discrete Laplace, Informal version of \cref{thm:purified-approximate-discrete-laplace-sampler: Let $n _+$, $ _+$ and $γ _+ (0, 1)$, with representations that fit in a constant number of machine words. There exists a randomized algorithm $ n γ : n n $ with initialization time $ O ( 1 )$, memory usage $O ( 1 + ln 1 γ + ln n )$, deterministic per-query running time $O(1)$ after initialization. It satisfies the following privacy and utility guarantees: [label=$ $, leftmargin=0.7cm] ; For each $t [n]$, $ n γ t - 1 = i / n γ t = i [e^ - , e^ ]$. ; For each $t n $, $β > 2 · γ$, $ n γ t - t α β$ 
  Theorem Deterministic Approximate Sampler, Informal~\cref{thm:time-oblivious-distribution-sampler: Let $μ$ be a distribution over a discrete domain, let $δ (0,1)$, and let $ δ/2 $ denote a smallest subset satisfying $μ( δ/2 ) 1 - δ/2$. Then there exists a deterministic-time sampler for $μ$ with the following guarantees: [label=$ $, leftmargin=0.7cm] ; Memory: $O\! ( 1 \, δ/2 \, ( ln 1 δ + ln δ/2 ) )$, with $ $ the Word-RAM word size. ; Sampling time per draw: $ 1 \, log δ/2 + log 1 δ + O(1) $. ; Accuracy: The resulting distribution has total variation distance at most $δ$ from $μ$.
  Theorem Poisson Approximation Theorem~\citep{MU05: Suppose $m$ balls are thrown into $d$ bins independently and uniformly at random, and let $X_i$ denote the number of balls in bin~$i$, for $1 i d$. Let $Y_1, ,Y_d$ be independent Poisson random variables, each with mean $m/d$. For any nonnegative function $f(x_1,…,x_d)$, \[ f(X_1,…,X_d) \; \; e √ m \, f(Y_1,…,Y_d) . \] In particular, for any event $ E $, taking $f$ to be its indicator function yields \[ E under the exact model \; \; e √ m \, E under the Poisson model . \]
  Proof: [Proof of fact:discrete-laplace-tail ] For each $t 1$, X t &= 2 · 1 - e^ - 1 + e^ - · Σ_ i = t ^∞ e^ - · i = 2 · 1 - e^ - 1 + e^ - · e^ - · t 1 - e^ - = 2 · e^ - · t 1 + e^ - .
  Proof: [Proof of thm:time-oblivious-distribution-sampler ] Denote $m δ / 2 $ and $ log_2 2 δ + log_2 m $. For each $i m $, let $x_i i δ / 2 $. Since $μ$ has discrete support, we can list the remaining elements as $x_ m , x_ m + 1 , …$. In particular, if $μ$ has finite support, we set $x_i $ for all $i μ $. For each $i m $, let $p_i μ x_i , $ denote the $ $-bit binary approximation of $μ x_i $ after the fractional point, so that $0 μ x_i - p_i 2^ - $. Finally, adjust $p_0$ to ensure $p_0 1 - Σ_ i = 1 ^ m - 1 p_i$, so that the $p_i$'s sum to $1$. Hence p_0 - μ(x_0) &= 1 - Σ_ i = 1 ^ m - 1 p_i - 1 - Σ_ i _+ μ(x_i) \\ &= Σ_ i m μ(x_i) + Σ_ i = 1 ^ m - 1 μ(x_i) - p_i \\ &= Σ_ i m μ(x_i) + Σ_ i = 1 ^ m -
  bound: In the add/remove model, there is no fixed upper bound on the dataset size, so it is often referred to as the unbounded setting .
  bound: A commonly studied relaxation is the upper-bounded setting , where a public upper bound on the dataset size is assumed.
  bound: The pseudocode is given in alg:private-upper-bound-search , and the construction relies on two key components. [display] Construction.
  bound: Second, [cite] t ratliff2025securing show that, for given $β_1 (0,1)$, alg:private-upper-bound-search is an $ _1$-DP time-oblivious algorithm that computes an estimate $ n $ satisfying $n n c · n$ with probability at least $1 - β_1$.
  bound: To address this, alg:private-upper-bound-search truncates $ $ to its first $ n _k$ elements before constructing $ $ and invoking algo:tail-item-padding .
  bound: Analyzing the running time of alg:private-upper-bound-search in the word-RAM model requires more care.

## sigmod26-337 — N2E: Reducing Node-Differential Privacy to Edge-Differential Privacy for Graph Analytics  (score 40)
domain: privacy-dp | graph-db
usage: Sequential composition, group privacy, post-processing; SVT over sensitivity-1 monotone queries at logarithmic scale; LP rounding of the (x_v,y_e) relaxation.
note-quote: "The N2E framework satisfies (ε, δ)-node-DP."
  Theorem : Given any $ $, $β$, and $δ$, for any graph $G$, task $Q$, and edge-DP mechanism $ M _Q^ edge $, with probability at least $1-β$, the N2E framework outputs a node-DP result with error |Q(G) - Q( G )| + Err _Q^ edge ( G , /6 ^*, δ/4 ^*,β/3), where $ G $ is obtained by clipping edges of $O(log(log( deg (G))/ β)/ )$ nodes from $G$, and $ Err _Q^ edge \\ ( G , /6 ^*, δ/4 ^*,β/3)$ denotes the error of the edge-DP mechanism on task $Q$ given clipped graph $ G $, privacy budgets $ /6 ^*$ and $δ/4 ^*$, a
  Definition Differential Privacy: A randomized mechanism $ M : G R $ satisfies $( , δ)$-differential privacy if, for any two neighboring graphs $G, G' G $, and for any subset of outputs $ S $ in the output space of mechanism $ M $, the following holds: \[ [ M (G) S ] ≤ e^ [ M (G') S ] + δ. \] $ $ is usually a constant between $0.1$ to $10$ while $δ 1/N$. If $δ = 0$, the mechanism satisfies $( ,0)$-DP or simply $ $-DP.
  Lemma \cite{liu2024unleash: If the sequence of queries $\ Q_ \ $ used in SVT are sensitivity-monotonic, given any $ >0$, by setting $c=1$, SVT preserves $ $-DP. Moreover, given an error failure probability \(β\), if there exists some \(Q_k\) such that $ Q_k(G) ≥ T + 4 ln(2 / β)/ , $ then with probability at least \(1 - β\), SVT returns an index $ $ such that \[ ≤ k and Q_ (G) ≥ T - 4 ln 2k β . \]
  Definition Down Neighborhood Optimality: Given query $Q$, and let \( M \) denote the set of all \(( , δ)\)-node-DP mechanisms, a node-DP mechanism \( M _Q M \) is \(( , c)\)-down neighborhood optimal if, for any graph \( G \), \[ [| M _Q(G) - Q(G)| ≤ c · L (G, ) ] ≥ 2 3 , \] where \( c \) is the optimality ratio, and L (G, ) := &min_ M ' M max_ G' G ,d_ node (G, G') ≤ \ : [|M'(G') - Q(G')| ≤ ] ≥ 2 3 \ .
  Lemma Group Privacy: If a mechanism $ M $ satisfies $( , δ)$-differential privacy, then for any two graphs $G$ and $G'$ with $d(G, G') = λ$ (i.e., differing by $λ$ edges or nodes), and for any subset of outputs $ S R $, the following holds: \[ [ M (D) S ] ≤ e^ λ [ M (D') S ] + λδ. \] The mechanism $ M $ satisfies $(λ , λδ)$-differential privacy for the group of $λ$ edges or nodes.
  Lemma : $Q_ Del-Deg (G, )$ has the following properties: ; [1.] Monotonicity : $Q_ Del-Deg (G, )$ is monotonic and becomes $0$ when $ ≥ deg (G)$. ; [2.] Bounded Sensitivity : $Q_ Del-Deg (G, )$ has $ GS _Q = 1$ under edge-DP. ; [3.] Sensitivity-Monotonicity : For any two neighboring graphs $G ~_ edge G'$ and any $ $, $Q_ Del-Deg (G, ) ≥ Q_ Del-Deg (G', )$ if $G G'$.
  Lemma : $Q_ Del-N (G, )$ satisfies the following $3$ properties: ; [1.] Monotonicity : $Q_ Del-N (G, )$ is monotonic and becomes $0$ when $ ≥ deg (G)$. ; [2.] Bounded Sensitivity : $Q_ Del-N (G, )$ has $ GS _Q = 1$ under node-DP. ; [3.] Sensitivity-Monotonicity : For any two neighboring graphs $G ~_ node G'$ and any $ $, $Q_ Del-N (G, ) ≥ Q_ Del-N (G', )$ if $G G'$.
  Lemma : Given any $ $, $β$, and $δ$, for any graph $G$, the polynomial-time node-DP maximum degree approximation algorithm returns a $ ^*$ such that with probability at least $1-β$, ^* ≤ 6 deg (G) + 24 (4 deg (G)) + 48 ln 4 β + 12 lnmax( 1 δ , 2 β )+ 1, and N_ ^* (G) ≤ 24 (4 deg (G)) + 48 ln 4 β . Furthermore, with probability at least $1-δ$, ^*+ N_ ^* (G) ≤ 2 ^*.
  Proof: For $ ≥ deg (G)$, we have $Q_ Del-Deg (G, ) = 0 = T + 4 ln (2/β)/ $. By Lemma~ [ref] , with probability at least $1 - β$, Algorithm~ [ref] returns a $ $ such that $ ≤ deg (G)$ and Q_ Del-Deg (G, ) ≥ T - 4 ln 2 deg (G) β = - 4 ln deg (G) - 8 ln 2 β . Thus, the lemma follows.
  Proof: Let $ $ denote the output from SVT in Algorithm~ [ref] . When $ ≥ deg (G)$, $Q_ Del-N (G, ) = 0 = T + 8 ln (4/β)/ $. Since $ $ is evaluated on a logarithmic scale, the number of iterations required to reach $ ≥ deg (G)$ is bounded by $ln log (4 (G))$. Thus, by Lemma~ [ref] , with probability at least $1 - β/2$, we have $ ≤ 2 (G)$ and |Q_ Del-N (G, )| ≤ 8 ln log(4 (G)) + 16 ln 4 β . Next, $ $ is post-processed to $ ^*$ in Line~8 of Algorithm~ [ref] . By the tail bound of the Laplace distribution, with probability at least $1 - min(β/2, δ)$, 0 ≤ Lap ( 2 ) + 2 lnmax( 1 δ , 2 β ) ≤ 4 lnmax( 1 δ , 2 β ). Thus, under the same probability, ^* ≤ + |Q_ Del-N (G, )| + 1 + 4 lnmax( 1 δ , 2 β ), and ^* 
  bound: A straightforward solution based on group privacy divides the privacy budget by a given degree upper bound, but this leads to poor utility when the bound is set conservatively large to accommodate worst-case inputs.
  bound: Since we are only interested in $ $ candidates whose LP result exceeds the SVT threshold, we stop the computation early if the current LP upper bound of $ $ already falls below the threshold.
  bound: Since $ ^*$ contains the term $|Q_ Del-N (G, )|$, it follows directly that [display] For any $G$ and any $ $, [display] [display] To begin with, with the optimal solution of $Q_ LP-Del-N (G, )$, we can construct a subgraph of $G$ with a degree upper bound of $3 $ by Algorithm~ [r
  bound: Similarly to Lemma~ [ref] , with probability at least $1 - min(β/2, δ)$, [display] and [display] By Lemma~ [ref] , this directly implies that [display] Applying ( [ref] ) with $ = 3 $ and using Lemma~ [ref] , we obtain [display] By applying the union bound and incorporating the u
  bound: Both R2T and DLL-Histogram rely on a degree upper bound, which is set to the smallest power of $2$ exceeding the number of nodes for each dataset.
  bound: Optimality of $ ^*$. [display] [display] N2E relies on the maximum degree approximation $ ^*$ as the degree upper bound to clip the input graph.

## sigmod26-263 — A General Framework for Per-record Differential Privacy  (score 30)
domain: privacy-dp
usage: Doubling budget partition plus per-slice Laplace; union bounds; composition lemmas; down-neighborhood lower bounds gauge near-optimality.
note-quote: "|Q̂(D) − Q(D)| ≤ |Q(D)−Q(D')| + Err_M(D', ε_min(D)/4, β/2), where D' is obtained by removing O(1/ε_min(D)·log log(·/β)) records"
  Theorem : For any given privacy budget function $ E (·)$ and $β (0,1)$, as well as any DP mechanism $ M $ for query $Q$, Algorithm~ [ref] returns $ Q (D)$, such that with probability at least $1 - β$, we have | Q (D) - Q(D) | ≤ |Q(D) - Q(D')| _ I + Err _ M (D',\, _ min (D)/4,\, β/2 ) _ II , where $D'$ is obtained by removing $O ( 1 _ min (D) log log ( / ) β )$ records from $D$ and $ Err _ M (·,\, ·,\, ·)$ denotes the error introduced by the DP mechanism $ M $, which depends on the input dataset, privacy b
  Theorem : The sum estimation algorithm described in Section~ [ref] satisfies $ E $-PrDP and it returns $ Q _ sum (D) $, such that with at least $1 - β$ probability, we have & | Q _ sum (D) - Q _ sum (D)| \\ &≤ Σ_ i=1 ^ -1 |Q_ sum (D X _i) | + Σ_ i= ^ log ( / ) | Q_ sum (D X _i) - Q_ sum (D X _i) | \\ &≤ Σ_ i=k ^ -1 2 · max_ r X _i ( v_ Bal E (r) ) ln ( log ( / ) β ) + \\ &Σ_ i= ^ log ( / ) max_ r X _i ( v_ Bal E (r) ) ln ( log ( / ) β ). where $k$ is the index of the first non-empty privacy-specified doma
  Theorem : For any privacy budget function $ E (·)$, $β (0,1)$, and any LDP protocol $ M $, Algorithm~ [ref] returns an estimate $ Q (D)$ such that, with probability at least $1 - β$, | Q (D) - Q(D)| ≤ |Q(D) - Q(D')| _ (I) + Err _ M (D',\, _ min (D)/4,\, β/2 ) _ (II) , where $D'$ is obtained by removing $O ( √ n _ min (D) log log ( / ) β )$ records from $D$, and $ Err _ M (·,\, ·,\, ·)$ denotes the error introduced by the LDP protocol, which depends on the dataset, privacy budget, and failure rate.
  Definition : (Down-neighborhood Optimality) ~ [cite] . Let $ M $ be the class of all $ $-DP mechanisms. Given any query $Q$, the $ $-down neighborhood lower bound on dataset $D$ is defined as & L (D, ) := \\ & _ M' M _ D' D, \\ d(D,\, D') ≤ \ : [ | M' (D') - Q(D')| ≤ ] ≥ 2 3 \ , and a mechanism $ M $ is $( , c)$-down neighborhood optimal if, for any dataset $D$, [ | M (D) - Q(D)| ≤ c · L (D, \, ) ] ≥ 2 3 , where $c$ is a constant quantifying the level of optimality.
  Definition : (Per-record Local Differential Privacy) . Given a record-dependent privacy budget function $ E : [U]^ d [ , \, ]$, a mechanism $ M $ is $ E $-per-record local DP, or simply $ E $-PrLDP, if for arbitrary input record $r [U]^ d $ and for all possible outputs $y$, the following holds: e^ - E (r) [ M ( ) y] ≤ [ M (r) y] ≤ e^ E (r) [ M ( ) y].
  Definition : (Per-record Privacy (PrDP)) . Given a record-dependent privacy budget function $ E :[U]^d [ ,\, ]$, a mechanism $ M $ is $ E $-per-record differentially private, or simply $ E $-PrDP, if for any pair of neighboring datasets $D ~_r D'$ and for all possible outputs $y$, the following holds: [ M (D) = y] ≤ e^ E (r) · [ M (D') = y].
  Theorem : For any given privacy budget function $ E (·)$ and $β (0,1)$, the combination of Algorithm~ [ref] and Algorithm ~ [ref] returns $ _ $ and $ Q _ count (D)$, such that with probability at least $1 - β$, we have $ _ ≥ 1 2 _ min (D)$, and | Q _ count (D)-Q_ count (D) | = O ( √ n _ min (D) log ( log ( / ) β ) ).
  Lemma : (PrDP Parallel Composition) . Let $ R _1, , R _k$ be a set of disjoint subdomains. Let $\ M _1, , M _k\ $ be a series of $ E _i$-PrDP mechanisms. Then their parallel composition M (D) := ( M _1(D R _1), , M _k(D R _k) ) satisfies $ E '$-PrDP, where $ E '(r) = max\ E _i(r)\ _ i=1 ^k$ for all $r [U]^d$.
  Proof: The privacy and error-bound proofs can be derived similarly to those for Theorem~ [ref] , with the error bound now modified due to the new threshold.
  Proof: We analyze the error introduced by the algorithm, which consists of two components. The first component arises from omitting the results of the first $ -1$ privacy-specified domains. According to Lemma~ [ref] and the union bound over $ log ( / ) $ domains, with probability at least $1 - β$, we have | Q _ count (D X _i) - Q_ count (D X _i)| &≤ T_i \\ &= 1 2^ i-1 · ln ( log ( / ) β ), which holds for all domains. This means Algorithm [ref] will not stop at empty domains with $Q_ count (D X _i) = 0$, to be more specific, we have _ ≥ 1 2 _ min (D). ( [ref] ) also indicates that for each domain with $i < $, the number of elements within those domains is very limited even if they are discarded Q_ 
  bound: However, as a global lower bound on privacy budgets, $ $ is typically set to a conservatively small value.
  bound: Optimality of DP Errors The Laplace mechanism is known to achieve worst-case optimal error ~ [cite] .
  bound: Given any query $Q$, the $ $-down neighborhood lower bound on dataset $D$ is defined as [display] and a mechanism $ M $ is $( , c)$-down neighborhood optimal if, for any dataset $D$, [display] where $c$ is a constant quantifying the level of optimality.
  bound: For the example of estimating a bank's total deposits, a possible privacy budget function is defined as [display] where $α$ is a constant factor and $ $ is a predefined upper bound on the privacy budget.
  bound: Such a technique is widely used in standard DP sum estimation to reduce error dependence from a predefined domain upper bound $U$ to $ Max (D)$.

## sigmod26-073 — Differentially Oblivious Multi-way Join  (score 21)
domain: join-algorithms | privacy-dp
usage: FO join evaluation leaks only input size plus a DP-noisy join-size bound; RST/EST lemmas establish smoothness of the sensitivity bound; hybrid construction picks the cheapest valid smooth bound per subset.
note-quote: "Algorithm 3 runs in O((N^w + |Q(I)| + RRS(I)) · log N) time under data complexity, where w is the fractional hypertree width of the join query."
  Definition 2: 1 (Access Pattern [32, 33]). For a query Q, the access pattern of a join algorithm A  ℓ over an instance I of Q is the sequence Access A (Q, I) = ⟨op𝑖I , ad𝑖I ⟩ produced by A, where 𝑖=1 ℓ = |Access A (Q, I)| is the number of memory accesses. For a randomized algorithm, its access pattern is the distribution over the set of all possible sequences generated by the algorithm on the input instance.
  Definition 3: 1 (Advised FO). For a join query Q, an algorithm A is advised FO if for any two instances I and I′ of same input size, for any input advice Λ ≥ max{|Q (I)|, |Q (I′ )|}, Access A (Q, I, Λ) = Access A (Q, I′, Λ) If A taking as input an instance I and an advice Λ, then Leak A (I) comprises the input size |I| and the advice Λ. This is different from a FO algorithm A ′ , where Leak A ′ (I) comprises only the input size |I|. Putting everything together, we come to the meta algorithm:
  Theorem 3.2: Algorithm 1 is (𝜖1 + 𝜖2, 𝛿 1 + 𝛿 2 )-DO. The formal proof of Theorem 3.2 is deferred to the full version [3]. Intuitively, the output size of  across any instance. (𝜖1, 𝛿 1 )-DO algorithm is fixed, since it exclusively computes a single value OUT Proc. ACM Manag. Data, Vol. 4, No. 3 (SIGMOD), Article 162. Publication date: June 2026. Differentially Oblivious Multi-way Join 162:7 Algorithm 1: MetaDOJoin(Q, I)  ← an (𝜖2, 𝛿 2 )-DP upper bound on the join size |Q (I)| via an (𝜖1, 𝛿 1 )-DO algorith
  Lemma 3: 5 ([25]). Let T̂(·) be a smooth upper bound on the maximum boundary. Then, ∑︁ ˆ 𝑘 (I) = max max LS T̂ [𝑚]−{𝑖 }−𝐸 (I) · 𝑑𝐸 (I, I′ ) ′ ′ I :𝑑 (I,I )=𝑘 𝑖 ∈ [𝑚] (2) 𝐸 ⊆ [𝑚] − {𝑖 } is an upper bound on LS𝑘 (I), and ˆ 𝑘 (I) S(I) = max 𝑒 −𝛽𝑘 · LS (3) 𝑘 ≥0 is a smooth upper bound on local sensitivity. Proc. ACM Manag. Data, Vol. 4, No. 3 (SIGMOD), Article 162. Publication date: June 2026. 162:10 Zhiang Wu, Wei Dong, and Xiao Hu R1 x1 x2 R2 x2 x3 R3 x3 x4 R2 ./ R3 x2 x3 x4 a 1 b1 a 2 b2 b1 c1 c1 d1 b1 c1
  Definition 4: 1 (Acyclic Join [65]). A join Q = (x, {x𝑖 : 𝑖 ∈ [𝑚]}) is acyclic if one can organize its relations into a join tree T such that (1) there is a one-to-one correspondence between relations in Q and the nodes in T ; and (2) for each attribute 𝑥 ∈ x, the set of nodes containing 𝑥 forms a connected subtree.
  Lemma 4: 3 ([10]). For a free-connex join-max query maxy Q and any instance I, there is an FO e(|I|) time under data complexity. algorithm that computes maxy Q (I) in 𝑂 Example 4.4. For 𝐸 = {2, 3}, its induced join ({𝑥 2, 𝑥 3, 𝑥 4 }, {{𝑥 2, 𝑥 3 }, {𝑥 3, 𝑥 4 }, {𝑥 2, 𝑥 4 }}) is not acyclic and then𝑇𝐸 is not free-connex. For 𝐸 = {4},𝑇𝐸 is free-connex since both ({𝑥 4, 𝑥 5 }, {{𝑥 4, 𝑥 5, }}) and ({𝑥 4, 𝑥 5 }, {{𝑥 4, 𝑥 5 }, {𝑥 4 }}) are acyclic. It can be checked that as long as 𝐸 ≠ {2, 3}, 𝑇𝐸 is always free
  Lemma 4.6: For any instance I, any 𝐸 ⊊ [𝑚] and any pair of y, y′ ⊆ 𝜕𝐸 with y′ ⊆ y, T𝐸 (I) ≤ y′ y RST𝐸 (I) ≤ RST𝐸 (I). Proc. ACM Manag. Data, Vol. 4, No. 3 (SIGMOD), Article 162. Publication date: June 2026. 162:12 Zhiang Wu, Wei Dong, and Xiao Hu
  Lemma 4.7: For any 𝐸 ⊊ [𝑚], 𝑖 ∈ 𝐸, y ⊆ 𝜕𝐸, and two neighboring instances I, I′ such that ′ ′ y y y 𝑑 (𝑅𝑖I, 𝑅𝑖I ) = 1 and 𝑅 I𝑗 = 𝑅 I𝑗 for any 𝑗 ∈ 𝐸 − {𝑖}, RST𝐸 (I) − RST𝐸 (I′ ) ≤ RST𝐸−{𝑖 } (I). Besides, Lemma 4.7 guides the choice of y — the attributes to be dropped — over different subsets of relations in 𝐸. If an attribute 𝑥 is dropped from T𝐸 for some 𝐸 ⊊ [𝑚], then 𝑥 must be dropped from T𝐸 for each 𝐸 ′ ⊆ 𝐸 if 𝑥 ∈ 𝜕𝐸 ′ , to guarantee the smoothness. 4.3 Incorporating Degree Information Inspired by the co
  Proof: Line 1 invokes the FO algorithm [10] to compute |Q (I)|, hence this step only depends on (or reveals) the input size |I|. Line 2 runs within the trusted memory, hence does not incur any access to untrusted storage. Line 3 invokes FO algorithms in [10] to compute T̂𝐸 (I), hence this step only depends on (or reveals) the input size |I|. Lines 4-5 run within the trusted  which only depends on (or reveals) memory. Line 6 computes the join results with advice OUT,  the input size |I| and the advice OUT. Hence, the access pattern of Algorithm 3 only depends on |I| and  OUT, i.e., its leakage function Leak(I) consists of |I| and  OUT. As  OUT satisfies (𝜖, 𝛿)-DP, Algorithm 3 is (𝜖, 𝛿)-DO by [2
  Proof: For simplicity, we first consider acyclic joins for which 𝑤 = 1. Line 2 runs in 𝑂 (1) time from Lemma 4.13. Line 2 computes every maximum boundary T̂𝐸 (I) in 𝑂 (𝑁 log 𝑁 ) time. As there are 𝑂 (1) different subsets 𝐸 ⊊ [𝑚] under data complexity, this step takes 𝑂 (𝑁 log 𝑁 ) time in total. Line 4 computes RRS(I) according to Equations (2) and (3), which still takes 𝑂 (1) time according to [25]. Line 1 computes the true join size in 𝑂 (𝑁 log 𝑁 ) time [10]. Line 5 computes a noisy join size by calibrating noise based on RRS(I),   which only takes 𝑂 (1) time. Finally, the advised FO join algorithm runs in 𝑂 (𝑁 +  OUT) · log 𝑁 = 𝑂 ((𝑁 + |Q (I)| + RRS(I)) · log 𝑁 ) time, since  is proportional 

## sigmod26-214 — Scalable Privacy-Preserving Shortest Path via 2-Hop Labeling in MPC  (score 19)
domain: graph-db | privacy-dp
usage: Optimal-substructure induction over boundary decompositions proves distance preservation; truncated Laplace mechanism analysis gives privacy with guaranteed convergence.
note-quote: "Theorem 5.4 (Privacy Guarantee). The above mechanism satisfies (𝜖, 𝛿)-differential privacy."
  Definition 2: 1 (Distributed Graph Partition). A graph G = (V, E) is distributed across two parties 1 ∪𝐸 as {𝐺𝑖 (𝑉𝑖 , 𝐸𝑖 )}𝑖=0 𝑐𝑢𝑡 , where: Ð1 • V = 𝑖=0 𝑉𝑖 , with 𝑉𝑖 ∩ 𝑉𝑗 = ∅ for 𝑖 ≠ 𝑗, Ð1 • E = 𝑖=0 𝐸𝑖 ∪ 𝐸𝑐𝑢𝑡 , where 𝐸𝑖 = {𝑒 (𝑢, 𝑣) ∈ 𝐸 | P (𝑢) = P (𝑣)}, • 𝐸𝑐𝑢𝑡 = {(𝑢, 𝑣) ∈ 𝐸 | P (𝑢) ≠ P (𝑣)} denotes the set of cutting edges that connect vertices across the two subgraphs and is publicly known to both parties, • Each party 𝑃𝑖 privately holds its subgraph 𝐺𝑖 for 𝑖 ∈ {0, 1}. Based on this partitioning, we classify
  Definition 2.4: (Neighboring Graphs [23]) Two graphs 𝐺 = (𝑉 , 𝐸, 𝑤) and 𝐺 ′ = (𝑉 , 𝐸, 𝑤 ′ ) are neighboring (denoted 𝐺 ∼ 𝐺 ′ ) if their weight functions satisfy: ∑︁ ∥𝑤 − 𝑤 ′ ∥ 1 := |𝑤 (𝑒) − 𝑤 ′ (𝑒)| ≤ 1 𝑒 ∈𝐸 The Differential Privacy (DP) introduced by Dwork [22] is defined below.
  Lemma 2: 7 (Laplace Mechanism [22]). For any function 𝑓 : G → R𝑑 with sensitivity Δ𝑓 , the mechanism: M (𝐺) := 𝑓 (𝐺) + (𝑌1, ..., 𝑌𝑑 ), i.i.d. 𝑌𝑖 ∼ Lap(0, Δ𝑓 /𝜖) satisfies 𝜖-differential privacy. We employ differential privacy (DP) mechanisms into the pruning algorithm to ensure provable privacy guarantees during online query processing. Proc. ACM Manag. Data, Vol. 4, No. 1 SIGMOD, Article 81. Publication date: February 2026. 81:6 2.3 Huizhong Wang, Yuanyuan Zeng, Kun Chen, Wei Dong, & Chenhao Ma Cryptogr
  Definition 4: 1 (Subgraph-Index). For a subgraph 𝐺 (𝑉 , 𝐸), 𝐻 𝐵𝑆 contains a 2-hop index for each vertex. For any vertex 𝑣 ∈ 𝑉 and its label (𝑢, 𝑑 𝑣𝑢 ) ∈ 𝐿(𝑣), the following conditions hold: • 𝑟 (𝑢) ≥ 𝑟 (𝑣); • 𝑑 𝑣𝑢 = 𝑑𝑖𝑠𝑡𝐺 (𝑢, 𝑣); • 𝑑 𝑣𝑢 < 𝑑 𝑣𝑤 + 𝑑𝑢𝑤 with ∀𝑟 (𝑤) > max{𝑟 (𝑣), 𝑟 (𝑢)}. This implies that each vertex 𝑣 ultimately stores the shortest distances to all higher-ranked reachable bridging vertices. Based on 𝐻 𝐵𝑆 , the local shortest path distance of any internal vertex pair (𝑠, 𝑡) on the subgraph 𝐺 can be
  Lemma 4: 4 (Boundary Path Implication). Given a boundary graph 𝐺 𝐵 (𝑉𝐵 , 𝐸𝐵 ,𝑊 ), for any boundary vertex pair (𝑠, 𝑡) ∈ 𝑉𝐵 × 𝑉𝐵 , if its shortest path 𝑝 (𝑠, 𝑡) in subgraph 𝐺 passes through at least another boundary vertex 𝑤 ∈ 𝑉𝐵 \ {𝑠, 𝑡 }, then: • Even if 𝑒 (𝑠, 𝑡) ∉ 𝐸𝐵 , 𝑑𝑖𝑠𝑡𝐺𝐵 (𝑠, 𝑡) = 𝑑𝑖𝑠𝑡𝐺 (𝑠, 𝑡).
  Definition 5: 1 (Combined Boundary Graph). Given a graph G(𝑉 , 𝐸) partitioned into two parties 1 ∪𝐸 as {𝐺𝑖 (𝑉𝑖 , 𝐸𝑖 )}𝑖=0 𝑐𝑢𝑡 , and their respective HB-Indices 𝐻 𝐵 0 and 𝐻 𝐵 1 , the combined boundary graph 𝐺𝐶𝐵 (𝑉𝐶𝐵 , 𝐸𝐶𝐵 ) is defined as: • 𝑉𝐶𝐵 = 𝑉𝐵0 ∪ 𝑉𝐵1 • 𝐸𝐶𝐵 = 𝐸𝐵0 ∪ 𝐸𝐵1 ∪ 𝐸𝑐𝑢𝑡 Though 𝐺𝐶𝐵 provides a convenient abstraction for reasoning, it is never materialized in plaintext. Instead, the boundary graphs 𝐺 𝐵0, 𝐺 𝐵1 , and 𝐸𝑐𝑢𝑡 are maintained in secret-shared form to preserve privacy.
  Lemma 5: 2 (Distance Preservation in 𝐺𝐶𝐵 ). For any (𝑢, 𝑣) ∈ 𝑉𝐶𝐵 × 𝑉𝐶𝐵 , the shortest path distance satisfies: 𝑑𝑖𝑠𝑡𝐺𝐶𝐵 (𝑢, 𝑣) = 𝑑𝑖𝑠𝑡 G (𝑢, 𝑣).
  Theorem 5: 4 (Privacy Guarantee). The above mechanism satisfies (𝜖, 𝛿)-differential privacy.
  Proof: Let 𝑝 (𝑠, 𝑡) be the shortest path from 𝑠 to 𝑡. If 𝑝 (𝑠, 𝑡) does not pass through any other boundary vertices, then by Definition 4.2, boundary graph must contain the direct edge 𝑒 (𝑠, 𝑡), in which case 𝑑𝑖𝑠𝑡𝐺𝐵 (𝑠, 𝑡) = 𝑊 (𝑠, 𝑡) = 𝑑𝑖𝑠𝑡𝐺 (𝑠, 𝑡). If 𝑝 (𝑠, 𝑡) passes through 𝑤 ∈ 𝑉𝐵 \ {𝑠, 𝑡 }, according to the optimal substructure property of shortest paths, the path can be decomposed as: 𝑝 (𝑠, 𝑡) = 𝑝 (𝑠, 𝑤) ⊲⊳ 𝑝 (𝑤, 𝑡) where 𝑝 (𝑠, 𝑤) and 𝑝 (𝑤, 𝑡) are the shortest paths in their respective subpaths. By recursively applying this argument, we know that: Proc. ACM Manag. Data, Vol. 4, No. 1 SIGMOD, Article 81. Publication date: February 2026. Scalable Privacy-Preserving Shortest Path Distance Computat
  Proof: We consider all types of shortest paths in 𝐺𝐶𝐵 : (1) Intra-boundary paths: If 𝑢, 𝑣 ∈ 𝑉𝐵𝑖 and 𝑝 (𝑢, 𝑣) lies entirely within 𝐺 𝐵𝑖 : 𝐺 𝐵𝑖 𝑝 (𝑢, 𝑣) = 𝑢 ⇝ 𝑣 By Definition 4.2, 𝑊𝐺𝐶𝐵 (𝑢, 𝑣) = 𝑑𝑖𝑠𝑡𝐺𝑖 (𝑢, 𝑣). Thus: 𝑑𝑖𝑠𝑡𝐺𝐶𝐵 (𝑢, 𝑣) = 𝑑𝑖𝑠𝑡𝐺𝐵𝑖 (𝑢, 𝑣) = 𝑑𝑖𝑠𝑡 G (𝑢, 𝑣). (2) Single cut-edge paths: If 𝑢 ∈ 𝑉𝐵0, 𝑣 ∈ 𝑉𝐵1 and (𝑢, 𝑣) ∈ 𝐸𝑐𝑢𝑡 : 𝐸𝑐𝑢𝑡 𝑝 (𝑢, 𝑣) = 𝑢 → 𝑣 Then 𝑊𝐺𝐶𝐵 (𝑢, 𝑣) = 𝑑𝑖𝑠𝑡 G (𝑢, 𝑣) by construction. (3) Multi-hop inter-partition paths: For general paths crossing partitions multiple times, 𝐺 𝐸𝑐𝑢𝑡 𝐺 𝐸𝑐𝑢𝑡 𝐺 𝐵0 𝐵1 𝐵𝑖 𝑝 (𝑢, 𝑣) = 𝑢 ⇝ 𝑤1 → 𝑤2 ⇝ 𝑤3 → . . . ⇝ 𝑣 By the optimal substructure property of shortest paths: ∑︁ 𝑑𝑖𝑠𝑡𝐺𝐶𝐵 (𝑢, 𝑣) = 𝑑𝑖𝑠𝑡𝐺𝐵𝑖 (𝑤𝑘 −1, 𝑤𝑘 ) + 𝑑𝑖𝑠𝑡𝐸𝑐𝑢𝑡 (𝑤𝑘 , 𝑤𝑘+1 ) 𝑘 Since each 𝑑𝑖𝑠𝑡𝐺𝐵𝑖 (·, ·) a
