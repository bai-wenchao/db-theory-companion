# Chapter digest: communication-complexity
n_papers_with_tool: 4
PODS exemplars are from the PODS'26 issue inside PACMMOD Vol 4 — mark them ^PODS.

## sigmod26-115 [PODS] — Frequency Moments in Noisy Streaming and Distributed Data under Mismatch Ambiguity  (score 68)
domain: streaming | sampling
usage: Ordered p-clique sampling estimator with truncation controls expectation/variance (Hölder, Chebyshev, plus a Renyi-style inequality); DISJ communication-complexity reduction yields the space lower bound.
note-quote: "Any O(1)-pass ((ϵ + Cηp), 0.48)-approximation algorithm for computing Fp on any noisy data stream of length up to m needs Ω(m^{1−1/p}/ϵ^{1/p}) bits of space."
  Claim : The input $σ$ for $F_p$ after the input reduction has the following properties. ; For any $ [k]$ and $i [n]$, we have $q_ i,j ^ ( ) ~ q_ i,j' ^ ( ) $ for any $j, j' [ t k ]$ ($j ≠ j'$). In words, items created from the same element w.r.t.~the same player are pairwise dissimilar. ; For any $i [n]$ and any $ , ' [k]\ ( ≠ ')$ with $X_i^ ( ) = X_i^ ( ') = 1$, we have $q_ i,j ^ ( ) ~ q_ i,j' ^ ( ') $ for any $j, j' [ t k ]$. In words, if two players $ , '$ share an element $i$ (i.e., $X_i^ ( ) = X_i^
  Corollary : For any constants $p > 1$, $C 0$ and $ (0, 1 3 )$, any $(( + C _p), 0.48)$-approximation algorithm for computing $F_p$ in the coordinator model on any noisy dataset of up to $m$ items distributed over $k 10p(C+1)$ sites needs $Ω ( 1 ^ 1/p m^ 1-1/p )$ bits of communication, regardless the number of rounds.
  Definition $F_p$-mismatch-ambiguity ($p \ge 1$): For a noisy dataset $σ = (σ_1, …, σ_m)$ and its ground truth $ = ( _1, …, _m)$, the $F_p$-mismatch-ambiguity of $σ$ with respect to $ $ is defined as _p(σ, ) = 1 F_p( ) Σ_ i [m] ( B_i^ σ B_i^ ^ p-1 - B_i^ σ B_i^ ^ p-1 ). When there is no confusion, we simply write $ _p(σ, )$ as $ _p$.
  Theorem : For any constants $p > 1$, $C 0$ and $ (0, 1 3 )$, any $(( + C _p), 0.48)$-approximation algorithm for computing $F_p$ on any noisy dataset of up to $m$ items distributed over $k 10p(C+1)$ players needs $Ω ( 1 ^ 1/p m^ 1-1/p )$ bits of communication in the blackboard model.
  Theorem : For any constant $p 1$, given an input dataset of size $m$ with $ _p ≤ 0.4$ partitioned among $k$ sites in the coordinator model, Algorithm~ [ref] computes a $(( + O( _p)), 0.01)$-approximation of $F_p$, using two rounds and $O ( 1 ^2 km^ 1-1/p )$ words of communication.
  Theorem : For any constant $p 2$ and a noisy size-$m$ dataset $σ$ with $ _p ≤ ^p 4^ p+1 · k^ p-1 $, Algorithm~ [ref] computes an $(( + O( _p)), 0.01)$-approximation of $F_p$ in the coordinator model with $k$ sites, using three rounds and $O ( k^p ^ p+1 )$ words of communication.
  Definition Frequency Moments: Let $U=(u_1, …, u_n)$ be a finite universe and $ =( _1, …, _m) U^m$ be a noiseless dataset. For each element $u_j U$, define its frequency in $ $ as $$ f_j = \ i [m] _i = u_j\ . $$ The $p$-th frequency moment of $ $ is defined as $ F_p( ) = Σ_ j=1 ^n f_j^p. $
  Theorem : For any constant $p Z ^+$, given a noisy input data stream of length $m$ with $ _p ≤ 1 3(p!) $, Algorithm~ [ref] computes an $(( +O( _p), 0.01)$-approximation of $F_p$, using a single pass and $O ( 1 ^2 m^ 1-1/p )$ words of space.
  Proof: Let $n_ $ be the number of distinct elements in $ $, and $\ V_1, …, V_ n_ \ $ be the set of cliques in $G^ $. It is easy to see that for any $K_p _p^ $, all $p$ nodes in $K_p$ must belong to the same clique $V_k$ for some $k [n_ ]$. Therefore, the total number of ordered $p$-cliques is $Σ_ k [n_ ] V_k ^p = F_p$.
  Proof: We first bound the size of $ _p^ σ _p^ $. Consider an ordered $p$-clique $K_p = ( i_1 , , i_p )$ in $ _p^ σ _p^ $ with $i_1 = i$. There must exist a $k [p]$ such that $ i_k B_i^ σ B_i^ $. Therefore, the total number of ordered $p$-cliques in $ _p^ σ _p^ $ with $i_1 = i$ is at most $ B_i^ σ ^ p-1 - B_i^ σ B_i^ ^ p-1 ≤ B_i^ σ B_i^ ^ p-1 - B_i^ σ B_i^ ^ p-1 . $ Summing over $i [m]$, we have _p^ σ _p^ Σ_ i [m] ( B_i^ σ B_i^ ^ p-1 - B_i^ σ B_i^ ^ p-1 ) = _p F_p. We next bound the size of $ _p^ _p^ σ $. Let $n_i$ be the number of ordered $p$-cliques in $ _p^ $ that contains $i$ and at least one node from $B_i^ B_i^σ$. We have $$ n_i ≤ p ( B_i^ ^ p-1 - B_i^ σ B_i^ ^ p-1 ) ≤ p ( B_i^ σ B_i^ ^ p-1 - 
  bound: Within this framework, we focus on the frequency moments ($F_p$) problem and demonstrate that it is possible to approximate $F_p$ of the unknown ground-truth dataset using sublinear space in the data stream model and sublinear communication in the coordinator model, provided that
  bound: However, as we will demonstrate in this paper using $F_p$ as an example, it is feasible to approximate $f( )$ by introducing a parameter $ _f(σ, )$ into the approximation ratio.
  bound: The scaling factor is introduced for convenience; it transforms an additive error into a relative one for the ease of expressing the approximation ratio.
  bound: Accordingly, we assume that the algorithm is given $m$, which can be regarded as an upper bound on the number of distinct items (and hence the effective universe size), since elements absent from the stream can be ignored.
  bound: Notably, in the hard input constructions for the $F_p$ lower bound in the noiseless setting (e.g., [cite] ), it consistently holds that $n = Θ(m)$. 2mm Our Results.\ Our algorithmic result in the data stream model can be stated as follows: ; There is a one-pass $(( +O( _p)), 0.01
  bound: We supplement the algorithmic result with the following lower bound: ; For any constant $C 0$, any $O(1)$-pass $((ε + C _p), 0.48)$-approximation algorithm for $F_p\ (p > 1)$ over a data stream of $m$ items must use at least $ Ω ( 1 ε^ 1/p m^ 1 - 1/p )$ bits of space.

## sigmod26-243 [PODS] — Towards Output-Optimal Uniform Sampling and Approximate Counting for Join-Project Queries  (score 64)
domain: join-algorithms | sampling
usage: Hybrid heavy/light partition, Hoeffding intersection estimation, geometric search on OUT, Markov/Chebyshev median amplification, negative-hypergeometric rejection; coupon-collector reductions from listing hardness.
note-quote: "any randomized algorithm that ... outputs an O(1) approximation of OUT with high probability requires at least Ω(N/√OUT) time."
  Theorem : For a cyclic join-project query $ =( , , )$, there is an algorithm that can take as input an arbitrary instance $ $ of input size $N$, output size $ $ and full join size $ _ $, and builds an index in $O(N)$ time such that a uniform sample from the query result $ ( )$ can be returned in $$O ( N^ ^*( _ ) + N· _ + min_ w N^ Σ_ e w(e) ^ 1-w( ) )$$ expected time, where $ ^*( _ )$ is the fractional edge covering number of $ _ = ( , )$, and $w$ is the fractional edge cover of $ _ = ( , \ \ )$.
  Theorem : For the star query $ $, no combinatorial algorithm can preprocess an instance $ $ of input size $N$ and output size $ $ in $O(N · ^ 1 k -γ )$ time and generate a uniform sample from $ ( )$ in $O ( N ^ 1 k + γ )$ expected time, for any arbitrary small constant $γ > 0$, assuming the combinatorial hardness of listing star join results It is known that any combinatorial algorithm for listing all results of a star query requires $Ω(N · ^ 1-1/k )$ time. , under the property testing model.
  Theorem : For an acyclic join-project query $ =( , , )$, there is an algorithm that can take as input an arbitrary instance $ $ of input size $N$, output size $ $ and full join size $ _ $, and builds an index in $O(N)$ time such that a uniform sample from the query result $ ( )$ can be returned in $$O ( min \ _ + N^ Σ_ e w(e) · ^ w( )-1 , \ N · _ \ )$$ expected time, where $w$ is the fractional edge covering of $ _ = ( , \ \ )$.
  Theorem : For $ $, assuming the combinatorial hardness of Boolean matrix multiplication , given any nonnegative integers $N, $, there is no combinatorial algorithm that preprocesses an instance $ $ of input size $N$ and output size $ $ in $O(N · ^ 1 2 -γ )$ time and generates a uniform sample from $ ( )$ in $O ( N ^ 1 2 + γ )$ expected time, for any constant $γ > 0$.
  Lemma : In Algorithm~ [ref] , the following holds for an arbitrary input instance $ $ of input size $N$ and output size $ $: [leftmargin=*] ; When $ 1 2 · $, Algorithm~ [ref] returns $s≠ $ with probability at least $1-δ$; ; When $s≠ $ is returned, we always have $ [s] = $. ; When $s≠ $ is returned, $s$ is an $ε$-approximation of $ $ with probability at least $1-δ$.
  Proposition \cite{chen2020random: Suppose there is a uniform sampling algorithm $ A $ that can successfully return a query result with probability $ W $. If repeating $Y$ independent invocations of $ A $ and ending up with $X$ successes, then $ X· W Y $ is an unbiased estimator of $ $. If $Y = Ω( W ε^2 · )$, $ X· W Y $ is an $ε$-approximation of $ $ with constant probability.
  Theorem : For $ = _ A,D R_1(A,B) R_2(B,C) R_3(C,D)$, given any parameter $N, Z ^+$, any randomized algorithm that takes as input an arbitrary instance $ $ of input size $N$ and output size $ $, and returns a constant-approximation of $| ( )|$ with constant probability, requires at least $Ω(min\ N, N^2 \ )$ time, under the property testing model.
  Theorem : For $ = _ A,E R_1(A,B) R_2(B,C) R_3(C,D) R_4(D,E)$, given any parameter $N, Z ^+$, any randomized algorithm that takes as input an arbitrary instance $ $ of input size $N$ and output size $ $, and returns a constant-approximation of $| ( )|$ with constant probability, requires at least $Ω(N)$ time, under the property testing model.
  Proof: [Proof of Proposition~ [ref] ] By the Law of Total Probability and the independence of $U$ and $Y$: $ [U Y] = Σ_ y=1 ^M [U y] · [Y=y].$ Since $U$ is uniform on $[M]$, $ [U y] = y M $. Substituting this yields: $ [U Y] = 1 M Σ_ y=1 ^M y · [Y=y] = [Y] M $.
  Proof: [Proof of Lemma~ [ref] ] As mentioned, $B^ \ b (B): |R_2 b| √ \ $ always holds. It suffices to show that $\ b (B): |R_2 b| √ \ B^ $. Consider an arbitrary value $b (B)$ with $|R_2 b| √ $. In each attempt, the probability that no tuple in $R_2 b$ is sampled is $1- |R_2 b| |R_2| $. Then, in $k = N· log N δ √ $ independent attempts, the probability that no tuple in $R_2 b$ is sampled is $ (1- |R_2 b| |R_2| )^ N· log N δ √ (1- √ N )^ N· log N δ √ exp (-log N δ ) δ N $. So, $b$ is not detected with probability at most $ δ N $. As there are at most $N$ such values, by union bound, the probability that at least one of them is not detected is at most $δ$.
  bound: In this paper, we present the first asymptotically optimal algorithms for fundamental classes of join-project queries, including matrix, star, and chain queries.
  bound: While matrix and star queries admit efficient sublinear-time algorithms, we establish a significantly stronger lower bound for chain queries, demonstrating that sublinear algorithms are impossible in general. [500] Theory of computation~Database query processing and optimization 
  bound: While worst-case optimal join algorithms~ [cite] have settled the worst-case complexity up to the AGM bound~ [cite] , the join size can still be polynomially larger than the input size $N$ (i.e., the number of tuples in the database), reaching up to $N^ ^* $, where $ ^*$ is the f
  bound: Since the complexities of our algorithms are monotonically non-increasing with respect to $ $ (e.g., $O( N √ )$), the worst-case runtime upper bound occurs when $ =1$.
  bound: For simplicity, the lower bound shown in Figure~ [ref] assumes $O(N)$ preprocessing time.
  bound: We establish a matching lower bound of $ Ω ( N √ )$ for both uniform sampling and approximate counting.

## sigmod26-357 — Representation Obliviousness and Pseudodeterminism in Streaming Algorithms  (score 53)
domain: streaming | sketching
usage: Reduction chain from ROPD F0 estimator to ROPD Promise-Union protocol to PD Disj protocol, contradicting CC(Disj)=Ω(m); canonical-value and output-distribution arguments.
note-quote: "Let ε, δ ≤ 1/3. Any ROPD streaming algorithm that (ε, δ)-approximates F0 requires Ω(n) space (where n is the size of the universe)." (Thm 3.1)
  Definition 2.1: Let 𝜖, 𝛿 > 0. A streaming algorithm A is said to (𝜖, 𝛿)-approximate F𝑘 if for every universe size 𝑛 ∈ N and for every stream D ∈ [𝑛] ∗ Pr (|A𝑛 (D) − F𝑘 (D)| ≤ 𝜖F𝑘 (D)) ≥ 1 − 𝛿 For simplicity, in the rest of the paper we fix 𝛿 = 1/3. We will also use the following additional notation: For a set 𝑆 ⊆ [𝑛], D (𝑆) denotes the stream representation of 𝑆 in lexicographic order. For any two streams D1 and D2 , let D1 D2 denote the concatenation of the two streams. Proc. ACM Manag. Data, Vol. 3, No. 5 (PO
  Definition 2: 2 (Pseudodeterministic algorithms [16]). A randomized algorithm A is pseudodeterministic (PD for short) if for all inputs 𝑥, there is a canonical value 𝑔(𝑥) such that Pr(A (𝑥) = 𝑔(𝑥)) ≥ 23 . In the context of streaming algorithms for F𝑘 , a pseudodeterministic algorithm for a given input stream should output a canonical approximation for F𝑘 with high probability. We next introduce the notion of representation oblivious algorithms. Let 𝑘 ∈ N \ {0}, note that the frequency moment F𝑘 is “oblivious”
  Definition 2.4: [Representation Oblivious Algorithm for F𝑘 ] Let 𝑘 ∈ N. Two streams D1 and D2 are R𝑘 -equivalent if 𝑓 𝑘 (D1 ) = 𝑓 𝑘 (D2 ). A streaming algorithm A for F𝑘 is said to be representation oblivious if for every universe size 𝑛, A𝑛 is R𝑘 -oblivious. Recall that A𝑛 is the streaming algorithm A instantiated with the universe as [𝑛]. By combining the notions of pseudodeterminism and representation obliviousness, we arrive at the following definition.
  Definition 2.5: A streaming algorithm for F𝑘 is Representation Oblivious Pseudodeterministic (ROPD) if it is both representation oblivious and pseudodeterministic. The above definition means the following: Suppose A is an ROPD algorithm for F𝑘 . Since A is pseudodeterministic for every D there is a canonical value 𝑔(D) (which is an 𝜖-approximation of F0 (D)) such that A outputs 𝑔(D) with probability at least 2/3. Representation obliviousness imposes that if D1 and D2 belong to the same R𝑘 -equivalence class, th
  Definition 2.7: A streaming algorithm for F𝑘 is 𝛼-Representation Oblivious Pseudodeterministic (𝛼-ROPD) if it is both 𝛼-representation oblivious and pseudodeterministic. Note that 0-ROPD is the same as ROPD. We remark that our lower bound results extend to 𝛼-ROPD setting. Proc. ACM Manag. Data, Vol. 3, No. 5 (PODS), Article 282. Publication date: November 2025. 282:6 2.3 Michael Chen, Sourav Chakraborty, A. Pavan, and N. V. Vinodchandran Communication Complexity For our lower bound proofs, we will need results 
  Definition 2.10: Let 𝜓 : {0, 1}𝑚 × {0, 1}𝑚 ⇀ {0, 1} be a partial function and 𝑇 be an equivalence relation over {0, 1}𝑚 × {0, 1}𝑚 . A communication protocol P for 𝜓 is 𝑇 -oblivious pseudodeteriministic (𝑇 -OPD), if it is 𝑇 -oblivious and pseudodeterministic. We now define some communication problems that we use in this paper.
  Definition 2.13: Let 𝑚 ∈ N. Two input instances to Promise-Union𝑚 (𝐴1, 𝐵 1 ) and (𝐴2, 𝐵 2 ) are 𝜌𝑚 -equivalent if 𝐴1 ∪ 𝐵 1 = 𝐴2 ∪ 𝐵 2 . For the rest of the paper, we are interested in the above relationship 𝜌𝑚 . From now on, an ROPD protocol for Promise-Union𝑚 refers to a 𝜌𝑚 -OPD protocol. 3 F0 Lower bound In this section, we prove an Ω(𝑛) space lower bound for estimating F0 over the universe [𝑛] for ROPD one-pass streaming algorithms.
  Theorem 3.1: Let 𝜖, 𝛿 ≤ 31 . Any ROPD streaming algorithm that (𝜖, 𝛿)-approximates F0 requires Ω(𝑛) space (where 𝑛 is the size of the universe). Our proof approach shall be to show that if an ROPD F0 estimator using 𝑜 (𝑛)-space exists, then there is a 𝑜 (𝑚) communication protocol to solve Disj𝑚 problem via the reductions depicted below. This is a contradiction because the communication complexity of Disj𝑚 is Ω(𝑚) [26, 34]. ROPD F0 estimator ROPD communication protocol for Promise-Union Communication protocol
  Proof: Let 𝑛 ∈ N. Let A𝑛 be a ROPD streaming algorithm that ( 13 , 13 )-approximates F0 over a universe [𝑛] and uses space 𝑠. We now define the protocol P𝑛 to solve the Promise-Union𝑛 : Alice receives 𝐴 ⊆ [𝑛] and Bob receives 𝐵 ⊆ [𝑛]. They convert their input sets to streams D𝐴 = D (𝐴) and D𝐵 = D (𝐵), respectively. Alice runs the streaming algorithm A𝑛 on D𝐴 and then communicates the state at the end of her execution to Bob (requiring only 𝑠 bits of communication). On receiving this state, Bob continues the execution with his part of the stream D𝐵 . Let D = D𝐴 D𝐵 . At the end of the simulation, Bob has A𝑛 (D). Bob outputs 1 if A𝑛 (D) is at least 𝑛/2; otherwise, Bob outputs 0. We now prove that P𝑛 (
  Proof: For a stream D over [𝑛], let 𝑔(D) be the canonical value of A𝑛 , i.e., Pr(A𝑛 (D) = 𝑔(D)) ≥ 23 . For 𝐴, 𝐵 ⊆ [𝑛], define ( 1 if 𝑔(D (𝐴)D (𝐵)) ≥ 𝑛2 ℎ(𝐴, 𝐵) = , 0 if 𝑔(D (𝐴)D (𝐵)) < 𝑛2 so that, Pr(P (𝐴, 𝐵) = ℎ(𝐴, 𝐵)) ≥ 23 . Thus, ℎ testifies that P𝑛 is pseudodeterministic. We emphasize that the pseudodeterminism holds for all inputs. □

## sigmod26-070 [PODS] — Deterministic Lower Bounds for k-Edge Connectivity in the Distributed Sketching Model  (score 30)
domain: graph-db | distributed-query
usage: Probabilistic-method set families plus pigeonhole partitions create indistinguishable neighborhood pairs; reduction and a simulation of the sketching algorithm by the three parties transfer the hardness.
note-quote: "Every deterministic algorithm that decides k-edge connectivity on n-node graphs in the distributed sketching model has a worst case message length of Ω(k) bits."
  Theorem 1: Every deterministic algorithm that decides k-edge connectivity on n-node graphs in the distributed sketching model has a worst case message length of Ω(k) bits, for any super-constant √ k = k(n) ≤ γ n, where γ > 0 is a suitable constant. The proof of Theorem 1 requires us to overcome several technical challenges: • To obtain a graph that is hard for deciding k-edge connectivity in the distributed sketching model, we need to find a suitable graph construction. We cannot directly extend the constr
  Theorem 2: For any sufficiently large m, there exists an s = Θ (m) such that the deterministic one-way communication complexity of UniqueOverlapm,s in the simultaneous 3-party model is Ω (m), where each m-length input vector has support s. 1.4 Additional Related Work The distributed graph sketching model was first introduced by Becker, Matamala, Nisse, Rapaport, Suchan, and Todinca in [BMN+ 11], where they proved the hardness of several fundamental graph problems for deterministic sketching algorithms: In 
  Lemma 1: Every node vi ∈ V is either vσ , A-restricted, or B-restricted. Moreover, vi knows this information as part of its initial state.
  Lemma 2: Graph G ∈ Gk,n is k-edge connected if and only if (C1) holds.
  Lemma 3: Consider a set family W , any small ε > 0, and any positive integer d ≤ 2eε|W 2+4/ε . There  εd exists a set family S ⊆ W d such that, for any two distinct S1 , S2 ∈ S, we have |S1 ∩ S2 | ≤ 2 , and d−1 |S| ≥ e . Instantiating Lemma 3 with d = 2k − 1 and a suitable small constant ε > 0, shows that |S| ≥ e2k−2 . (4) Given an arbitrary set D, we say that {B1 , . . . , Bℓ } is a partition P of D and each set Bi ⊆ D is S called a block of P, if ℓi=1 Bi = D and all blocks are pairwise disjoint. For a
  Lemma 4: Suppose that we obtain the partition {A, B} by assigning each vertex in W uniformly at random to either A or B. Consider any vi ∈ V . Then, with probability at least 34 , there exist distinct S0 , S1 ∈ S that form an indistinguishable separated pair for vi .
  Claim 1: Let D be a set of size s, and let P1 , . . . , Pr be r partitions of D, such that the number of blocks in each of the partitions is at most n′ < s1/r+1 . Then, for every i ∈ [r], there exists a block Bi in Pi that contains at least s1−i/(r+1) elements that are in the same block in each of the partitions P1 , . . . , Pi .
  Corollary 1: For a given k-edge connectivity algorithm, let Vgood ⊆ V be the set of nodes for which there exist indistinguishable separated pairs (see Def. 2 on page 9). If each node sends at most o(k) bits, then there exists a partition {A∗ , B ∗ } of W such that |Vgood | ≥ 43 |V |. 4 The UniqueOverlap Problem We now introduce a communication complexity problem in a 3-party model with simultaneous messages, whose hardness turns out to be crucial for obtaining a lower bound for deciding k-edge connectivity i
  Proof: Let VA ⊆ V be the subset of nodes that are A-restricted, i.e., satisfy (E3), and also include vσ in VA . Similarly, we define VB ⊆ V to contain all B-restricted nodes, which satisfy (E4). Every vj ∈ VA has k parallel edges to uA , which itself has an edge to each node in A. Since A is a clique of size at least k, it follows that there are at least k edge-disjoint paths between every pair of nodes in G[A ∪ {uA } ∪ VA ], which implies that G[A ∪ {uA } ∪ VA ] is k-edge connected. A similar argument shows that B, uB , and VB induce a k-edge connected subgraph. By properties (E1)-(E5), any edge in the cut (A ∪ {uA } ∪ VA , B ∪ {uB } ∪ VB ) must be in E(vσ , B). Thus, the graph is k-edge connected
  Proof: We start by defining additional partitions P̃i,A and P̃i,B that we obtain from Pi,A and Pi,B , respectively, as follows: For each block B ∈ Pi,A , we define ϕ−1 A (B) = [ {S ∈ S | ϕA (S) = T }. T ∈B 4 This is Lemma 7 in the full version of their paper on arXiv [KNP+ 17b]. 9 Partition P̃i,A has exactly the same number of blocks as Pi,A , and we obtain B̃ ∈ P̃i,A from B ∈ Pi,A by defining it as the set containing all elements in ϕ−1 A (B). Analogously, we construct P̃i,B , which will have the same number of blocks as Pi,B , and, for each B ′ ∈ Pi,B , we get block B̃ ′ ∈ P̃i,B , which S ′ contains all elements in ϕ−1 B (B ) = T ∈B′ {S ∈ S | ϕB (S) = T }. Thus, the number of blocks in P̃i,A , P̃
