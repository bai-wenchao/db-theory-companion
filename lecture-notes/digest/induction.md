# Chapter digest: induction
n_papers_with_tool: 36
PODS exemplars are from the PODS'26 issue inside PACMMOD Vol 4 — mark them ^PODS.

## sigmod26-114 [PODS] — FPT Parameterisations of Fractional and Generalised Hypertree Width  (score 71)
domain: query-optimization
usage: MSO transductions lift Bojańczyk–Pilipczuk; elimination forests with a Reduced-Hypergraph Dealternation Lemma bound alternation depth; conflict-graph coloring and neutral-swap/pigeonhole arguments; induction over forests.
note-quote: "it is fixed-parameter tractable to check ghw(H) ≤ k and fhw(H) ≤ k parameterised by k, rank(H), Δ(H)."
  Theorem 1: 𝑓 -width-check is fixed parameter tractable for any manageable width function 𝑓 . We approach this problem by building on the transduction-based treewidth algorithm of Bojańczyk and Pilipczuk [5]. At a high level, their result shows how to express the process of turning a potentially suboptimal tree decomposition into an optimal one by an MSO transduction, based on deep combinatorial insights on the fine structure of so-called elimination forests (see Section 2). In our setting, we follow the sa
  Theorem 2: For each integer 𝛿 ≥ 𝑟𝑎𝑛𝑘 (𝐻 ), F𝛿 -width is fixed-parameter tractable parameterised by 𝑘, rank(𝐻 ), Δ(𝐻 ), and 𝛿. Theorem 2 leaves an interesting open question as to whether the discretized adaptive width can approximate the actual adaptive width. We provide some initial insight related to this question. We consider a relaxed version F𝛿∗ of F𝛿 consisting of all independent set functions where all the non-zero weights are at least 𝛿 (or, to put it differently, there are no small non-zero weights
  Definition 6: We call a width function 𝑓 manageable if it satisfies all of the following. Weak monotone separability. 𝑓 is weakly monotone separable, as in Definition 5. Bounded size. There exists a computable function 𝛽 such that for every  𝑘 ≥ 0, every hypergraph 𝐻 , and every 𝑈 ⊆ 𝑉 (𝐻 ), if 𝑓𝐻 (𝑈 ) ≤ 𝑘, then |𝑈 | ≤ 𝛽 𝑘, rank(𝐻 ) . We refer to any such 𝛽 as a size bound for 𝑓 . 104:8 Lanzinger, Razgon, and Unterberger Invariant locality. For all hypergraphs 𝐻 1, 𝐻 2 and all 𝑈 1 ⊆ 𝑉 (𝐻 1 ), 𝑈 2 ⊆ 𝑉 (𝐻 2 ), 
  Corollary 8: For input hypergraph 𝐻 and 𝑘 > 0, it is fixed-parameter tractable to check ghw(𝐻 ) ≤ 𝑘 and fhw(𝐻 ) ≤ 𝑘 parameterised by 𝑘, rank(𝐻 ), Δ(𝐻 ). In the rest of this section, we will provide an overview of the proof of Theorem 7. Subsequent sections will then expand on the technical details of the statements made here. 3.2 The Structure of Optimal Elimination Forests In this subsection, we develop the main combinatorial engine behind Theorem 7, which we call the Reduced Hypergraph Dealternation Lemma.
  Definition 9: For TF-pair t, 𝐹 , we define the split of the pair as the size of the largest maximal factorization of target sets of t, i.e., split (t, 𝐹 ) := max𝑈 ∈TS(t) |MF (𝑈 )|. The second aspect to this structural alignment is that the individual factors themselves follow a kind of top-down monotonicity. Formally, this means that we want to avoid the presence of many context-factors in our representation. For the definition, it will be helpful to extend our notation. We call 𝑈 ⊆ 𝑉 (𝐻 ) well-formed w.r.t. 
  Definition 10: For TF-pair t, 𝐹 we define the irregularity of a node 𝑥 of t as the number of children 𝑦 of 𝑥, such that 𝑐𝑚𝑝 (𝑦) is not well-formed w.r.t. 𝐹 . We denote the maximum irregularity of a node of 𝑇 by mir (t, 𝐹 ). These two measures of structural complexity, split and mir, are key to our framework. In key contrast to [5], we isolate split and irregularity as the only interface parameters necessary to build a modular, general theoretical framework for width checking using MSO transductions. It allows 
  Definition 12: Let t, 𝐹 be a tf-pair of 𝐻 and let 𝑢 ∈ 𝑉 (𝐻 ). The stain of 𝑢 (w.r.t. t, 𝐹 ), denoted by Stain(𝑢, (t, 𝐹 )), is the set consisting of mnt (𝑢) and the vertices of Pathmn (𝑢, 𝑣) for each child 𝑣 of 𝑢 in 𝐹 . That is, Ø Stain(𝑢, (t, 𝐹 )) = {mn(𝑢)} ∪ Pathmn (𝑢, 𝑣) 𝑣 ∈Child 𝐹 (𝑢 ) Note that Definition 12 is different from the definitions of irregularity and split above in that it considers a part of 𝐹 in the context of t rather than a part of t in the context of 𝐹 . Intuitively, the stain of a vertex 𝑢
  Lemma 13: There is an algorithm whose input is 𝑞 > 0 and the output is a transduction Tr of size 𝑂 (𝑞) whose input signature is 𝜏𝑡𝑑 and the output signature is 𝜏𝑒 𝑓 . Let t, 𝐹 be a TF-pair of hypergraph 𝐻 and let 𝑀 (𝐻, t) be the relational structure over 𝜏𝑡𝑑 induced by 𝐻 and t. Then the following is true regarding Tr(𝑀 (𝐻, t)). (1) Let 𝑀 ∗ ∈ Tr(𝑀 (𝐻, t)). Then 𝐻 (𝑀 ∗ ) = 𝐻 and 𝐹 (𝑀 ∗ ) is an elimination forest of 𝐻 . (2) If the chromatic number of CG(t, 𝐹 ) is at most 𝑞, then there is a 𝑀 ∗ ∈ Tr(𝑀 (𝐻, t))
  Proof: We analyse partitions (𝑅𝑒𝑑, 𝑋, 𝐵𝑙𝑢𝑒) of 𝑉 (𝐻 ), where 𝑋 separates 𝑅𝑒𝑑 from 𝐵𝑙𝑢𝑒 (i.e., no hyperedge has both a red and a blue vertex). In the application, 𝑋 is chosen as a subset of a bag 𝐵𝑥 of t, and hence |𝑋 | ≤ tw(t) + 1. We further consider only reduced elimination forests: an elimination forest 𝐹 is reduced if every parent–child edge (𝑢, 𝑣) of 𝐹 is witnessed by some hyperedge, i.e., there exists 𝑒 ∈ 𝐸 (𝐻 ) with 𝑢 ∈ 𝑒 and 𝑒 ∩ 𝑉 (𝐹 𝑣 ) ≠ ∅, where 𝐹 𝑣 is the subtree rooted at 𝑣. We show that one can transform every elimination forest into a reduced one of the same width, hence this restriction can be made without loss of generality. Fix a reduced optimal elimination forest 𝐹 and a coloured
  Proof: The algorithm first computes 𝑝 = 𝛽 (𝑘, 𝑟𝑎𝑛𝑘 (𝐻 )) and runs an off-theshelf algorithm for exact treewidth computation parameterized by 𝑝 − 1. We note that, due to the bounded size property, if 𝑓 -width(𝐻 ) ≤ 𝑘 then tw(𝐻 ) ≤ 𝑝 − 1. Therefore, if the treewidth computation algorithm rejects, our algorithm also rejects. Otherwise, we have a tree decomposition t = (𝑇 , 𝐵) of 𝐻 with treewidth at most 𝑝 − 1. We then compute 𝑝 1 = 𝛾 (𝑡𝑤 (t), 𝑘, 𝑟𝑎𝑛𝑘 (𝐻 ), Δ(𝐻 )) with 𝛾 as in Lemma 11, and 𝑞 = 𝜂 (𝑝 1, 𝑝 1, 𝑡𝑤 (t), 𝑟𝑎𝑛𝑘 (𝐻 )) with 𝜂 as in Theorem 14. Finally, we apply the algorithm as in Lemma 13 with input 𝑞 to create a transduction Tr as specified in the lemma.

## sigmod26-371 — The Space-Time Complexity of Sum-Product Queries  (score 71)
domain: join-algorithms
usage: Induction over trees and elimination orders; AGM/fractional-edge-cover bounds on intermediates; explicit witness-query constructions; heavy/light partitioning for sublinear-space plans.
note-quote: "For any ε > 0, no algorithm can solve this problem over the tropical semi-ring in space S=O(|E|^k4−ε) and time T=O(|E|^k2)."
  Definition : A recursive pseudo-tree (RPT) of an SPQ with input variables $(Q( X ), I )$ is recursively defined as follows: ; Base case. A PTCR $(P,C, )$ of $(Q( X ), )$ is an RPT of $(Q( X ), )$. ; Recursion. Let $(P,C, )$ be a PTCR of $(Q( X ), )$, and let $A_1, , A_l$ be vertices in $ V (P) $, such that $A_i (A_j) \, i≠ j$. Let $Q_i$ be the subquery of $Q( X )$ restricted to the variables $ (A_i) (A_i)$ and with head variables $ (A_i) (A_i)$. Let $( _i, _i, _i)$ be RPTs of $(Q_i, (A_i))$. Then, $( , , ):=
  Definition : Fix a query $Q$ and cardinality constraints $(cc)_i$, and let $ $ be a class of query plans for evaluating SPQs. The class of plans $ $ consists all pairs $(TD, )$, where $TD$ is a tree decomposition of $Q$, and $ $ is a function that maps each vertex $v$ in $TD$ to a plan $$ ^v (Q^v, ((cc_i / m)_i, ( ^*( Z ^w, Q, (cc_i)_i)/m)_w)$$ where $$m=max(\ cc_i i\ \ ^*( Z ^w, Q, (cc_i)_i) w\ ).$$ I.e., $ ^*( Z ^w, Q, (cc_i)_i)$ is the maximal possible size of the relation $Q^w( Z ^w)$ and $ ^v$ respects 
  Definition (Plan Domination).: Let $ _1, _2$ be two plans for the same query $Q$. We say $ _1$ improves over $ _2$, or that $ _1$ dominates $ _2$, denoted $ _1 _2$, if $t( _1) ≤ t( _2)$ and $s( _1) ≤ s( _2)$. We say that $ _1$ strictly dominates $ _2$, denoted $ _1 _2$, if $ _1 _2$ but not vice versa. A class of plans $ _1$ improves over ( dominates ) another class $ _2$, denoted $ _1 _2$, if for every query $Q$, it holds that $ \, _2 _2(Q)$, $ \, _1 _1(Q)$ such that $ _1 _2$. Notice that $ $ over classes of plans forms a pre
  Lemma : Let $( , , )$ be a RPT of the SPQ $Q$ and $A V ( )$ arbitrary. Then, during the execution of Algorithm [ref] , the function call $ solve (A, x )$ returns $$ Q[ (A) | x [ (A)] ]( (A)) .$$ Furthermore, after the call to $ fillCache (A, x [ (A) (A)])$ the cache $M_A$ is of the form \ ( , ) Q[ (A) | , ]( (A)) Q[ (A) | x [ (A)] ] \ . where $ = x [ (A)]$. Further, the function call $ solveReplaced (A,A', x )$ returns $$ Q[ (A') | x [ (A')] ]( (A')) .$$ Furthermore, after Line [ref] , the cache $M_A$ i
  Definition \cite{DBLP:conf/ijcai/FreuderQ85,DBLP:series/synthesis/2019Dechter: A pseudo-tree (PT) of $Q$ is a directed tree $P=( V ,E)$, satisfying: ; Every atom $R_i( X _i)$ is contained in a branch: formally, $ A V$ such that $ X _i (A)$. The term pseudo-tree was introduce by Freuder and Quinn in the context of constraint optimization~ [cite] , and studied extensively by Dechter~ [cite] . Pseudo-trees are a generalization of normal trees , also called Tr\'emaux trees used in graph theory~ [cite] , which are pseudo-trees where every tree edge is also an edge in the graph.
  Lemma : Let $( , , )$ be a RPT of the SPQ $Q$ and $A V ( )$ arbitrary. Then, for a run of Algorithm [ref] , let $ solve (A, x _1), solve (A, x _2)$ be two calls where the first happens before the latter. Then, $ x _1[ (A)]$ comes lexicographically before $ x _2[ (A)]$ (or they are equal). Further, let $ solveReplaced (A,A', x '_1), solveReplaced (A,A', x _2)$ be two calls where the first happens before the latter. Then, $ x '_1[ (A)]$ comes lexicographically before $ x '_2[ (A)]$ (or they are equal).
  Lemma : Let $( , , )$ be a RPT of the SPQ $Q$ and $A V ( )$ arbitrary. Let $Q( X ) _i R_i( X _i)$ be a SPQ and $( , , )$ a RPT of $Q( X )$. Then, for a run of Algorithm [ref] , the function $ solve (A, x )$ is only called for arguments $A V ( )$ that do not replace a $A'$ and $ x ^ (A) $ where $ x [ (A)] Q[ (A) ] $. Further, the function $ solveReplaced (A,A', x )$ is only called for arguments $A V ( )$ that replaced $A'$, $ x ^ (A') $ where $ x [ (A')] Q[ (A') ] $ and $ x [ (A)] Q[ (A) ] $.
  Definition : The class of query plans $ (Q)$ consists of pseudo-trees with caching $(P, C )$ of $Q$ and their space and time exponents are defined as: s(P, C ) := max_ A C ^*( (A) (A)), t(P, C ) := max_ A V(P) ^*( (B_A) [A, B_A] (A)), where Given a nonempty set $ S $ on some branch of the tree, i.e. $ S (A)$ for some $A$, we denote by $min( S )$ its smallest element, i.e. $min( S ) S $ and $ S (min( S ))$. $B_A:=min( C (A))$.
  Proof: [Proof Sketch.] $ $ follows from the fact that the 4-path query $R(AB) S(BC) T(CD)$ admits a $ $ query plan $ $ with $e( ) = (1,1)$, while time exponent 1 is not feasible in . $ $ is proved by showing that the 3-path query in Example~ [ref] has a plan $ $ with $e( ) = (0,1)$, while allows for plans $ '$ with $e( ') = (1,1)$ or $e( ') = (0,2)$ but not $(0,1)$; none dominate $e( )=(0,1)$. $ ^ $ follows as $Q^v=Q$ for the single-bag TD.
  Proof: [Proof Sketch.] Given a plan $ := ((T, ), ) ^ $, we construct a plan $ _0 := (P, C) $ such that $ _0 $. The construction is based on the variable elimination procedure for a tree decomposition~ [cite] , and proceeds by induction on the number of bags in $T$. If $T$ has a single bag, then $ $ is essentially a $ $ plan, and the claim follows from $ $. Otherwise, let $v$ be a leaf of $T$, and $p:= (v)$. We eliminate all variables $ A_1, …, A_k := (v) (p)$. Let $ Z$ be their neighbors, $ Z := B atom R_i( X_i), j, s.t. A_j, B X_i $. Let $Q'$ to be the query obtained from $Q$ by removing all variables $A_1, …, A_k$, and adding a new atom $R( Z)$. Let $ ' = ((T', ), )$ be the plan obtained from $ $
  bound: For a full CQ (query without projection), a worst-case optimal join (WCOJ) algorithm runs in time $O(|D|^ ^* )$, where $ ^*$ is the fractional edge cover number of $Q$; this time is proportional to the worst-case output size of the query~ [cite] .
  bound: GJ can easily be adapted to compute conjunctive queries with projections (i.e. non-full queries), however it is no longer guaranteed to be worst-case optimal.
  bound: The Generic Join (GJ) algorithm~ [cite] computes $Q(D)$ in worst-case optimal time given by the AGM bound $O(|D|^ ^*( (Q)) )$~ [cite] .
  bound: Pseudo-trees For a full query, any GJ plan is worst-case optimal, because the output size can be as large as $O(|D|^ ^*( (Q)) )$~ [cite] .
  bound: Readers familiar with free-connex tree decomposition may notice that condition ( [ref] ) is more restrictive, but that's OK for our purpose, because we only consider worst-case optimal algorithms, and do not consider constant-delay algorithms~ [cite] .
  bound: The latter can be asymptotically larger: if the bags are computed using Generic Join, or Pseudo-Trees, then we need to use the tighter upper bound expression $O(Π_i N_i^ w_i^* )$ rather than $O(|D|^ ^* )$, see Sec.~ [ref] .

## sigmod26-307 — Efficient Exact Resistance Distance Computation on Small-Treewidth Graphs  (score 64)
domain: graph-db
usage: Block-matrix inversion/Schur complements derive elimination formulas; induction on eliminated node sets; random-walk interpretation for the cut property; subtree-contiguous traversal for lookup.
note-quote: "Algorithm [index-building] has a time complexity of O(n · d_max · h_G^2)."
  Lemma : $ S _ | U _2 U _1| = [ L _ U _1 U _1 ^ -1 & 0 \\ 0 & 0 ]$. We will prove this by mathematical induction on the number of eliminated nodes. Base case: When no nodes have been eliminated ($i=0$), we have $ S _0 = L ^ -1 _ U _2 U _2 $. Inductive hypothesis: Assume that after eliminating $k$ nodes, the resulting matrix $ S _k$ has the form where all rows and columns corresponding to the eliminated nodes are zero, and the submatrix corresponding to the remaining nodes correctly represents their Schur
  Lemma Non-zero structure of resistance distance labelling: The resistance distance labelling $ S [v,u]$ has the following non-zero structures: (i) For each node $v$, all nodes $u$ in the subtree of $v$ are non-zero; (ii) For each node $u$, all nodes $v$ in the path from $u$ to the root node are non-zero. The non-zero structure of the resistance distance labelling can be derived from the vertex hierarchy property of the tree decomposition (Lemma~ [ref] ) and the cut property of resistance distance (Lemma~ [ref] ). For property (i), consider a node $v$ an
  Lemma Time Complexity Analysis: Algorithm~ [ref] has a time complexity of $O(n· d_ max · h_ G ^2)$. We analyze the time complexity by examining each component of the algorithm: First, initialization and DFS order computation (lines 1-2) require $O(n)$ time. The main outer loop (line 3) executes exactly $n$ iterations, processing each node $v_i$ sequentially. For each node $v_i$, the inner loop (line 5) iterates through its already processed neighbors, bounded by $| N (v_i) U | ≤ d_ v_i ≤ d_ max $. Within this loop, the while l
  Lemma : Let $ U _1 U _2 V $ be two subsets of the nodes of $ G $, then $ L ^ -1 _ U _1 U _1 $ is the Schur complement of $ L ^ -1 _ U _2 U _2 $ with respect to the node set $ U _1$. According to the block matrix decomposition formula, Let $S=A-BD^ -1 C$ be the Schur complement of $D$ with respect to $A$, we have: A & B \\ C & D ^ -1 = S^ -1 & -S^ -1 BD^ -1 \\ -D^ -1 CS^ -1 & D^ -1 +D^ -1 CS^ -1 BD^ -1 . We can prove this lemma using block matrix decomposition. First, let's partition $ L _ U _2 U _2 $ ac
  Lemma : Define $ U _i$ as the set of nodes that remain uneliminated at the point when node $v_i$ is being eliminated. Suppose that $ S [:,v_k]$ is the $v_k$-th column of $ S _i$ at the moment of $v_i$'s elimination, then we have: $ [ L _ U _ i U _ i ^ -1 & 0 \\ 0 & 0 ]=Σ_ k=1 ^i S [:,v_k] S [:,v_k]^T S [v_k,v_k] $. Specifically, we have: $ L _ U _ 2 U _ 2 ^ -1 - [ L _ U _ 1 U _ 1 ^ -1 & 0 \\ 0 & 0 ]=Σ_ k=| U _1|+1 ^ | U _2| S [:,v_k] S [:,v_k]^T S [v_k,v_k] $. The proof follows directly from the Gaussia
  Lemma Dependence property of resistance distance: In $ T _ min $ obtained from the heuristic tree decomposition, the resistance distance $r(s,t)$ depends only on the labels stored along the paths from nodes $s$ and $t$ to the root of $ T _ min $. Specifically, r(s,t) &= Σ_ v P _ s (s,t) ( S [v,s])^2 S [v,v] +Σ_ v P _ t (s,t) ( S [v,t])^2 S [v,v] \\ &+Σ_ v P _ (s,t) root ( S [v,s]- S [v,t])^2 S [v,v] . According to Lemma~ [ref] , we have $ L _v^ -1 = Σ_ v S [:,v] S [:,v]^T S [v,v] $. Substituting this into the resistance distance formula, we obt
  Lemma : $ S [u]. $ contains exactly $| T _ min . [u]|$ elements, where $ T _ min . [u]$ denotes the subtree rooted at $u$ in $ T _ min $. $ S [v,u]$ can be visited via $ S [u]. [ T _ min . [u] - T _ min . [v]]$. This result follows from the properties of traversal on trees. During a traversal, all nodes in the subtree rooted at any node $u$ are visited consecutively before the traversal backtracks to nodes outside this subtree. When performing a traversal on $ T _ min $, each node $v$ is assigned a posi
  Lemma Time Complexity of Single-Pair Queries: Algorithm~ [ref] has a time complexity of $| P _ s root |+| P _ t root |$, where $ P _ s root $ and $ P _ t root $ are the paths from $s$ and $t$ to the root in the tree decomposition, respectively. This complexity can further be bounded by $O(h_ G )$. The time complexity of Algorithm~ [ref] is determined by the number of nodes visited during the three traversal phases. In the first phase, we traverse from node $s$ to $ (s,t)$, which requires $| P _ s (s,t) |$ steps. Similarly, the second phase 
  Proof: According to the resistance computation formula, we have: r(s,t)&=( e _s- e _t)^T L _v^ -1 ( e _s- e _t)\\ &= e _s^T L _v^ -1 e _s+ e _t^T L _v^ -1 e _t-2 e _s^T L _v^ -1 e _t\\ &= e _s^T L _v^ -1 e _s+ e _t^T L _v^ -1 e _t\\ &=r(s,v)+r(v,t). Here, the third equality holds because $ e _s^T L _v^ -1 e _t=0$. This is because $ e _s^T L _v^ -1 e _t= _v[s,t] d_t $ [cite] , where $ _v[s,t]$ is the expected number of passes to $t$ in a random walk starts from $s$ and terminates when it hits $v$. Since $v$ is a vertex cut of $s$ and $t$, it is impossible for a random walk from $s$ to pass $t$ before it hits $v$. Thus, $ _v[s,t]=0$. The Lemma is established.
  Proof: According to the resistance distance formula in Theorem~ [ref] . We have: r(s,t) &= e _ s ^T L _ U U ^ -1 e _ s + e _ t ^T L _ U U ^ -1 e _ t -2 e _ s ^T L _ U U ^ -1 e _ t \\ & +( p _ s - p _ t )^T( L / V _ cut )^ ( p _ s - p _ t )\\ &= e _ s ^T L _ U U ^ -1 e _ s + e _ t ^T L _ U U ^ -1 e _ t \\ & +( p _ s - p _ t )^T( L / V _ cut )^ ( p _ s - p _ t )\\ &= e _ s ^T L _ V _1 V _1 ^ -1 e _ s + e _ t ^T L _ V _2 V _2 ^ -1 e _ t \\ & +( p _ s - p _ t )^T( L / V _ cut )^ ( p _ s - p _ t )\\ &=r_ G _ V _1 (s, _1)+r_ G _ V _2 (t, _2)\\ & +( p _ s - p _ t )^T( L / V _ cut )^ ( p _ s - p _ t ). Similar to the proof of Lemma~ [ref] , we can obtain that $ e _ s ^T L _ U U ^ -1 e _ t =0$ for $s$ and $
  bound: Given the labelling structure, the following lemma gives an upper bound on the label size: The label size of is $O(n· h_ G )$.
  bound: In practice, the tree height $h_ G $ is typically not very large, and the actual label size is often much smaller than the theoretical upper bound (see Section~ [ref] , Table~ [ref] ).
  bound: This gives us an upper bound of $O(d_ max · n · h_ G ^2)$ operations across all nodes, since $Σ_ i=1 ^n | T _ min .
  bound: In the worst case, when the tree is highly unbalanced, each subtree could contain up to $O(n)$ nodes, and the path length could be $O(h_ G )$, resulting in an upper bound of $O(n · h_ G )$ for the time complexity.

## sigmod26-153 — LSHAlign: All-Pair Near-Duplicate Text Alignment via Locality-Sensitive Hashing  (score 44)
domain: entity-resolution
usage: Monotone-stack skyline updates proved by induction; probability s/(s+1), s/(s+2) union-bound tightness computations; rectangle partition guarantees completeness (no missed near-duplicates).
note-quote: "Given a text T with length n, and a hash function g = (h1, . . . , hm) ∈ G, the expected number of tight intervals in T under g is O(nm)." (Thm 4)
  Definition 4: Given a hash function g = (h1, h2, . . . , h𝑚 ) ∈ G and a text T, an interval [𝑙, 𝑟 ] is • left-tight under g iff there exists 𝑖 ∈ [1, 𝑚] such that [𝑙, 𝑟 ] is left-tight under h𝑖 , • right-tight under g iff there exists 𝑖 ∈ [1, 𝑚] such that [𝑙, 𝑟 ] is right-tight under h𝑖 , • tight under g iff it is both left-tight and right-tight under g. Example 5. Consider text T and hash function g = (h1, h2 ) in Figure 2. Interval [1, 2] is tight under g: it is right-tight under g since h1 (T[3]) = 22 < min
  Lemma 1: Let S be the skyline at position 𝑟 . An interval [𝑙, 𝑟 ] is left-tight if and only if 𝑙 − 1 ∈ S \ {𝑟 }.
  Lemma 2: Let S be the skyline at position 𝑟 , for any position 𝑞 ∈ S \ {0}, let 𝑝 = max{𝑝 ∈ S | 𝑝 < 𝑞}. For any position 𝑥 such that 𝑝 < 𝑥 < 𝑞, 𝑞 dominates 𝑥.
  Lemma 3: Given an interval [𝑙, 𝑟 ]. Let S be the skyline at position 𝑟 , i.e., all positions in S are no more than 𝑟 . The interval is right-tight if and only if h(T[𝑟 + 1]) < h(T[𝑝]) where 𝑝 = min{𝑝 ∈ S | 𝑝 ≥ 𝑙 }.
  Lemma 4: Let S be the skyline at position 𝑟 . If h(T[𝑟 + 1]) > h(T[𝑝]) for a position 𝑝 ∈ S, then any interval [𝑙, 𝑟 ] where 𝑙 ≤ 𝑝 must not be right-tight.
  Lemma 5: Let S = (𝑝 1, 𝑝 2, . . . , 𝑝𝑠 ) be the ordered skyline at position 𝑟 . We have 𝑝𝑠 = 𝑟 and h(T[𝑝 1 ]) < h(T[𝑝 2 ]) < · · · < h(T[𝑝𝑠 ]). Proc. ACM Manag. Data, Vol. 4, No. 3 (SIGMOD), Article 206. Publication date: June 2026. 206:10 Yuheng Zhang, Zhencan Peng, Miao Qiao, Wei Zhang, Feifei Li, and Dong Deng
  Lemma 6: Let S(𝑟 ) = (𝑝 1, 𝑝 2, . . . , 𝑝𝑠 ) be the ordered skyline at position 𝑟 and 𝑖 = min{ 𝑗 ∈ [1, 𝑠] | h(T[𝑝 𝑗 ]) < h(T[𝑟 + 1])}. The ordered skyline at position 𝑟 + 1 must be S(𝑟 + 1) = (𝑝 1, 𝑝 2, . . . , 𝑝𝑖 , 𝑟 + 1).
  Theorem 1: All intervals generated by Algorithm 1 are tight intervals in T; all tight intervals in T are generated by Algorithm 1.
  Proof: We first prove for any left-tight interval [𝑙, 𝑟 ], 𝑙 − 1 ∈ S \ {𝑟 }. Since [𝑙, 𝑟 ] is an interval, 𝑙 ≤ 𝑟 . Thus 𝑙 − 1 ≠ 𝑟 and 𝑙 − 1 ∉ {𝑟 }. On the other hand, by the definition of left-tightness, h(T[𝑙 − 1]) < h(T[𝑥]) for every 𝑥 ∈ [𝑙, 𝑟 ]. Therefore, by the definition of dominance, no position 𝑥 ∈ [𝑙, 𝑟 ] dominates 𝑙 − 1. Thus, by the definition of skyline, 𝑙 − 1 ∈ S. As a result, we have 𝑙 − 1 ∈ S \ {𝑟 }. Next, we prove for every position 𝑝 ∈ S \ {𝑟 }, the interval [𝑝 + 1, 𝑟 ] must be left-tight. First of all, by the definition of skyline, for every position 𝑝 ∈ S, 𝑝 ≤ 𝑟 . Thus 𝑝 + 1 ≤ 𝑟 for every 𝑝 ∈ S \ {𝑟 } and [𝑝 + 1, 𝑟 ] must be a non-empty interval. Moreover, by the definition of sk
  Proof: Consider a position 𝑥 such that 𝑝 < 𝑥 < 𝑞. Let 𝑦 be the position in [𝑥 + 1, 𝑟 ] with the smallest hash value, i.e., 𝑦 = arg min𝑧 ∈ [𝑥+1,𝑟 ] h(T[𝑧]). Since 𝑥 ∉ S, by Definition 6 there is some position in [𝑥 + 1, 𝑟 ] that dominates 𝑥, thus h(T[𝑦]) < h(T[𝑥]), i.e., 𝑦 dominates 𝑥. Consider two cases. Case 1: 𝑦 > 𝑞. Because 𝑞 ∈ S, no position 𝑦 > 𝑞 can dominate 𝑞, so h(T[𝑦]) > h(T[𝑞]). Thus, h(T[𝑥]) > h(T[𝑦]) > h(T[𝑞]), hence 𝑞 dominates 𝑥. Case 2: 𝑥 < 𝑦 ≤ 𝑞. As 𝑦 is the position in [𝑥 + 1, 𝑟 ] with the smallest hash value, h(T[𝑦]) ≤ h(T[𝑞]). Thus, 𝑞 cannot dominate 𝑦. Because all positions in [𝑞 + 1, 𝑟 ] have hash values no less than h(T[𝑞]) — otherwise 𝑞 will not be in the skyline S, thus no p

## sigmod26-233 — Succinct Structure Representations for Efficient Query Optimization  (score 41)
domain: query-optimization
usage: Structural induction, ear-removal acyclicity, Prüfer-sequence tree counting; theorems justify restricting plan search to width-1/meta-decomposition space; experiments validate.
note-quote: "A conjunctive query Q has width-1 query plans if and only if Q is acyclic."
  Definition : Given a conjunctive query $Q$ and the associated hyergraph $H$, its meta-decomposition $M = (V(M), r(M), E(M), λ, , )$ is a labeled tree, where [leftmargin=*] ; $V(M)$ is the set of vertices, $r(M) V(M)$ is the root, and $E(M)$ is the set of edges, ; $λ : V(M) 2^ E(H) $ maps each vertex of $M$ to a set of hyperedges of $H$ (possibly empty), and ; $ : V(M) 2^ V(H) $ and $ : V(M) 2^ V(H) $ each maps each vertex of $M$ to a set of vertices of $H$, such that it satisfies [ref] , [ref] , and [label=(
  Lemma : Given a meta-decomposition $M$ of an acyclic hypergraph $H$, for all $v V(M)$, [leftmargin=*] ; if $λ(v) = $ and $ (v) = (v)$, then, for each join tree $T$ of $H$, there exists some $S = \ p V(M) λ(p) ≠ , (v) (p) \ $ such that the set $V_v^P = \ p V(M_ v ) λ(p) ≠ \ S$ induces a connected subtree $T_v$ of $T$; ; if $λ(v) ≠ $ or $ (v) ≠ (v)$, then, for each join tree $T$ of $H$, the set $V_v^P = \ p V(M_v) λ(p) ≠ \ $, i.e., physical nodes on the subtree $M_v$ rooted at $v$, induces a connected sub
  Definition : We say a query plan $ P $ is induced by a join tree $T$~if [leftmargin=*] ; for each node $p V(T)$, there exists $q V( P )$ with $Q(p) = Q(q)$, i.e., the induced query of node $p$ in the join tree is exactly the induced query of node $q$ in the query plan; and ; for each non-leaf node $q V( P )$ with two children $u$ and $v$, there exists some $s E(H_u)$ and $t E(H_v)$ such that $I(u) s$, $I(v) t$, and there exists an edge between $s$ and $t$ in $E(T)$.
  Lemma : Given a meta-decomposition $M$ of hypergraph $H$, for all $v V(M)$, let $V_v^P = \ p V(M_v) λ(p) ≠ \ $. Then, [leftmargin=*] ; if $λ(v) = $ and $ (v) = (v)$, then alg:enum enumerates all join trees of the hypergraph $H_v$ with $E(H_v) = λ(V_v^P) \ (v) \ $ and $V(H_v) = E(H_v)$; ; if $λ(v) ≠ $ or $ (v) ≠ (v)$, then alg:enum enumerates all join trees of the hypergrpah $H_v$ with $E(H_v) = λ(V_v^P)$ and $V(H_v) = E(H_v)$.
  Theorem : Consider star queries We note that, in some literature, e.g.,~ [cite] , such queries are called `` clique '' queries, because their query graphs are cliques. of the form \[ Q_ star R_1[ x _1] … R_n[ x _n], \] where for any $i, j [n]$, $ x _i x _j = x $, for some attribute $x$. There are $n^ n-1 $ possible join trees for such a star query with $n$ relations.
  Lemma : For all width-1 query plans $ P $ induced by a join tree $T$, for all non-leaf nodes $t V(T)$ with distinct children $c_1$ and $c_2$, if $p_1, p_2 V( P )$ are the nodes on $ P $ such that $Q(p_1) = Q(c_1)$ and $Q(p_2) = Q(c_2)$, then there exists no $q P $ such that $Q(q) = Q(p_1) Q(p_2) = Q(c_1) Q(c_2)$.
  Theorem : Given a Boolean conjunctive query $Q$ and a query plan $ P $ for the query, the maximum intermediate result size, i.e., $max_ p V(H_p) | _ I(p) Q(p)|,$ is upper-bounded by $O(N^ w( P ) )$ in the worst case, where $N$ is the maximum cardinality of any base relation.
  Proposition : Given an acyclic hypergraph $H$, if there exists a set of distinct ears $S = \ e_1, , e_n \ E(H)$ $(n ≥ 2)$ such that $o(e_1, H) = = o(e_n, H) = o$, then, the hypergraph $H'$, with $E(H') = (E(H) S) \ o \ $ and $V(H') = E(H')$, is acyclic.
  Proof: Suppose there is such a $q$, then by def:w1plan-induced-by-jt , there would have been an edge between $c_1$ and $c_2$ on $T$, contradicting with the assumption that $c_1$ and $c_2$ are distinct children of some node $t$.
  Proof: ($ $): $o(e_1, H) = e_1 ( (E(H) \ e_1 \ )) e_1$, and, similarly, $o(e_2, H) e_2$. So it follows that $o(e_1, H) = o(e_2, H) e_1 e_2$. ($ $): Since $e_1 E(H) \ e_2 \ $, we have $o(e_1, H) = o(e_2, H) = e_2 ( (E(H) \ e_2 \ )) e_1 e_2$.
  bound: In fact, finding the optimal join order of a query is well-known to be NP-hard in general~ [cite] .
  bound: The width of a query plan indicates an upper bound on the minimal intermediate results over all nodes $p$ during evaluation.
  bound: In the worst-case scenario, where all relations in $S$ share no common attributes and their join is a Cartesian product, the size of this join is $O(N^ |S| )$, which gives an upper bound of $O(N^ w( P ) )$.

## sigmod26-290 — Codd's Theorem for Databases over Semirings  (score 39)
domain: provenance
usage: Structural induction translating expressions ↔ formulas; multiplicity-growth counting (support ≤ n^{l(E)}, multiplicities ≤ p_E(n)·2^{l(E)}) against a 2^n division counterexample.
note-quote: "There is no expression of basic relational algebra BRA that defines the division operation ÷ on N-databases."
  Definition Operations on $\mathbb{K}$-relations: Let\/ $ K =(K,+,·, - , s, 0,1)$ be a semiring with monus and support. We define the following operations on $ K $-relations. ; Union: If $R_1$ and $R_2$ are two $n$-ary $ K $-relations, then the union $R_1 R_2$ is the $n$-ary $ K $-relation defined by $(R_1 R_2)( t ) = R_1( t ) + R_2( t )$, for all $ t D^n$. ; Difference: If $R_1$ and $R_2$ are two $n$-ary $ K $-relations, then the difference $R_1 R_2$ is the $n$-ary $ K $-relation defined by $(R_1 R_2)( t ) = R_1( t ) - R_2( t )$, for all $ t D
  Definition : Let $ =( R_1 ,… R_p )$ be a schema. The basic relational calculus language $ BRC ( )$ over $ $ is the set of all expressions defined recursively as follows: ; If $y_i$ and $y_j$ are variables, then the expression $y_i=y_j$ is a $ BRC ( )$-formula with $y_i$ and $y_j$ as its free variables. ; If $ R_i $ is a relation symbol of arity $r_i$ and if $y_1,…,y_ r_i $ are variables, then the expression $ R_i (y_1,…,y_ r_i )$ is a $ BRC ( )$-formula with $y_1,…,y_ r_i $ as its free variables. ; If $ $ is
  Definition Semantics of Basic Relational Algebra: Let $ K =(K,+,·, -, s , 0,1)$ be a semiring with monus and support, and let $ = ( R_1 , …, R_p )$ be a schema. ; A $ K $-database over $ $ is a finite sequence $ I =(R_1,…,R_p)$ of\/ $ K $-relations such that the arity of each $ K $-relation $R_i$ matches the arity of the relation symbol $ R_i $, $1≤ i≤ p$. We say that $ I$ is non-trivial if at least one of the relations $R_i$ of $ I$ has non-empty support. In what follows, we make the blanket assumption that all $ K$-databases considered are no
  Proposition : Let $ =( R_1 ,… R_p )$ be a schema. The following statements are true. ; Consider the atomic formula $ R_i (y_1,…,y_n)$, where $ R_i $ is one of the relation symbols of the schema $ $, $n$ is the arity of $ R_i $, and the $y_i$'s are distinct variables. If $ A =(A,R_1…,R_p)$ is a $ K $-structure, then $( R_i (y_1,…,y_n))^ A = R_i$. ; Let $ _1(y_1,…,y_n)$ and $ _2(y_1,…,y_n)$ be two $ BRC ( )$-formulas with the same free variables. If $ A =(A,R_1…,R_p)$ is a $ K $-structure, then ; $( _1 _2)^ A =
  Definition Basic Relational Algebra: Let $ = ( R_1 ,…, R_p )$ be a schema. The basic relational algebra language $ BRA ( )$ over $ $ is the set of all expressions defined recursively as follows: ; Each relation symbol $ R_i $ is a $ BRA ( )$-expression of arity $r_i$, $1≤ i≤ p$. ; If $E_1, E_2$ are $ BRA ( )$-expressions of the same arity $n$, then $E_1 E_2 $ and $E_1 E_2$ are $ BRA ( )$-expressions of arity $n$. ; If $E_1, E_2$ are $ BRA ( )$-expressions of arities $n, m$, respectively, then $E_1 × E_2 $ is a $ BRA ( )$-expression
  Definition : Let\/ $ K =(K,+, ·, -, s , 0,1)$ be a semiring with monus and support, let $ =( R_1 ,…, R_p )$ be a schema, and let $ $ be a $ BRC ( )$-formula. The semantics of $ $ on $ K $ is a function $\| \|$ that takes as input a $ K $-structure $ A =(A,R_1,…,R_p)$ and an assignment $α$ on $ A $, and returns a value $\| \|( A , α)$ in $K$ that is defined recursively and simultaneously for all assignments as follows: ; $\|x_i=x_j\|( A ,α) = 1 & if α(x_i) = α(x_j) ; \\ 0 & otherwise. $ ; $ \| R_i (x_ i_1 ,…,
  Definition $\mathbb{K}$-structures and assignments: Let\/ $ K =(K,+,·,-, s ,0,1)$ be a semiring with monus and support, and let $ = ( R_1 , …, R_p )$ be a schema. ; A $ K $-structure over $ $ is a tuple $ A = (A,R_1, , R_p)$, where $A$ is a non-empty subset of $D$, called the universe of $ A $, and each $R_i$ is a $ K $-relation on $A$ whose arity matches the arity of the relation symbol $ R_i $, $1≤ i≤ p$. We say that $ A $ is a finite $ K $-structure if its universe $A$ is a finite set. ; An assignment on $ A $ is a function $α V A$, assigning 
  Proposition : Let $ =( R_1 ,… R_p )$ be a schema. The following statements are true. ; Let $ _1(y_1,…,y_n)$ and $ _2(y_1,…,y_n)$ be two $ BRC ( )$-formulas with the same free variables. If $ _1$ and $ _2$ are domain independent $ BRC $-formulas, then $ _1 _2$ and $ _1 _2$ are domain independent. ; If $ _1(y_1,…,y_n)$ and $ _2(y_ n+1 ,…,y_ n+m )$ are two domain independent $ BRC ( )$-formulas,\\ where $y_1,…,y_ n+m $ are distinct variables, then $ _1 _2$ is domain independent. ; If $ _1(y_1,…,y_n)$ is a domain
  Proof: The first part about the atomic formula $ R_i (y_1,…,y_n)$ follows easily from Definitions~ [ref] and~ [ref] . For the second part, let $ _1$ and $ _2$ be two $ BRC ( )$-formulas with $y_1,…,y_n$ as their free variables, and let $ A $ be a $ K $-structure. To show that $( _1 _2)^ A = _1^ A _2^ A $, we have to show that for every tuple $(a_1,…,a_n) D^n$, we have $( _1 _2)^ A (a_1,…,a_n) = ( _1^ A _2^ A )(a_1,…,a_n) $. We distinguish two cases for the tuple $(a_1,…,a_n) D^n$, namely, whether or not $(a_1,…,a_n) A^n$, where $A$ is the universe of $ A $. If $(a_1,…,a_n) A^n$, then we have that $$ ( _1 _2)^ A (a_1,…,a_n)= \| _1 _2\|( A ,(a_1,…,a_n)) = \| _1\| ( A , (a_1,…,a_n))+ \| _2\| ( A , (a_
  Proof: For concreteness, let us assume that $ $ consists of a single binary relation symbol $ R $. The argument presented below extends easily to arbitrary schemas with only slight modifications. Let $E_ adom $ be the $ BRA ( )$-relational algebra expression $ ( _1( R ) _2( R ))$. We claim that for every $ K $-database, we have that $E_ adom ^ I = adom ( I )$. Let $ I =(R)$ be a $ K $-database and let $a$ be an element of the domain $D$. First, observe that, using the definitions of the semantics of $ BRA $-expressions, we have that $$( _1( R ) _2( R ))^ I (a) = (( _1( R ))^ I ( _2( R ))^ I )(a) = Σ_ R(a,b) ≠ 0 R(a,b)~~ + Σ_ R( b,a) ≠ 0 R(b,a).$$ If $a$ is in the active domain $ adom ( I )$ of $ I 
