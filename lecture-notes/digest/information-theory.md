# Chapter digest: information-theory
n_papers_with_tool: 5
PODS exemplars are from the PODS'26 issue inside PACMMOD Vol 4 — mark them ^PODS.

## sigmod26-185 [PODS] — PANDAExpress: A Simpler and Faster PANDA Algorithm  (score 42)
domain: join-algorithms | query-optimization
usage: LP duality picks optimal weights; a sub-probability-measure / geometric-mean construction turns each Shannon-inequality proof step into recursive hyperplane partitioning; potential |D|+|M|+3|S| bounds recursion depth.
note-quote: "PANDAExpress can compute a model Σout for the DDR (8) of size ∥Σout∥ = O(B) in time O(N log N + B log N) where B = ∏δ Nδ^{wδ}."
  Definition 2.1: Let Σ denote a set of unconditional monotonicity terms, and ∆ denote another set of monotonicity terms. Let λ = (λZ )Z∈Σ and w = (wδ )δ∈∆ be two sets of non-negative rational coefficients 3 with ∥λ∥1 = 1. A Shannon-flow inequality is a Shannon inequality of the form X X (4) λZ · h(Z) ≤ wδ · h(δ) Z∈Σ δ∈∆ Below is an equivalent characterization of Shannon-flow inequalities:
  Proposition 2: 2 ([6]). Inequality (4) holds for all polymatroids iff there are multisets Z, D, M, S over base sets Σ, ∆, MON, SUB respectively, such that X X X X (5) h(Z) = h(δ) − h(µ) − h(σ) Z∈Z µ∈M δ∈D σ∈S holds as an identity over symbolic variables h(X), ∅ ̸= X ⊆ V . Furthermore, the sizes of the multisets are functions of λ, w, and |V |.
  Definition 2.3: Given identity (5), we recover the corresponding Shannon-flow inequality (4) by dropping the negative terms on the right-hand side of (5), X X (6) h(Z) ≤ h(δ) Z∈Z δ∈D and dividing both sides by |Z|. We refer to inequality (6) an integral Shannon-flow inequality, that is parameterized by the multisets (Z, D), and witnessed by the multisets (M, S). The following two key results about (integral) Shannon-flow inequalities were shown in [5, 6]. They are both straightforward consequences of Propositio
  Theorem 2: 4 (The Proof Sequence Construction [6, 5]). Every integral Shannon-flow inequality (6) parameterized by (Z, D) with witness (M, S) can be proved by a sequence of steps X X X X h(δ) = h(δ) ≥ h(δ) ≥ · · · ≥ h(δ), with Dk ⊇ Z δ∈D where P δ∈Di h(δ) is turned into δ∈D0 δ∈D1 δ∈Dk P δ∈Di+1 h(δ) by applying exactly one of the following steps: • Submodularity: h(Y |X) → h(Y |XZ). • Composition: h(X) + h(Y |X) → h(XY ). • Decomposition: h(XY ) → h(X) + h(Y |X). • Monotonicity: h(XY ) → h(X). Furthermore, 
  Lemma 2: 5 (The Reset Lemma [6]). Given an integral Shannon-flow inequality (6) parameterized by (Z, D) and witnessed by (M, S), let W ∈ D be an unconditional monotonicity term. Then, we can construct multisets Z ′ , D′ , M′ , S ′ such that the following hold: (a) (Z ′ , D′ ) are parameters of another integral Shannon-flow inequality witnessed by (M′ , S ′ ), (b) Z ′ ⊆ Z and D′ ⊆ D − {W }, with |Z ′ | ≥ |Z| − 1. (c) |D′ | + |M′ | + 2|S ′ | ≤ |D| + |M| + 2|S| − 1. In English, the Reset lemma allows us to 
  Definition 2: 6 (DDR and its model). Given two schemas Σin and Σout , a disjunctive datalog rule (DDR) is the expression _ ^ (8) Q(Z) :R(X) Q(Z)∈Σout R(X)∈Σin Given a database instance over the input schema Σin , a model (or feasible output) of the DDR (8) is a database instance over the output schema Σout such that for every tuple t ∈ ⋊ ⋉Σin , there exists at least one atom Q(Z) ∈ Σout such that tZ ∈ Q(Z). Without loss of generality, we assume that if Q(Z1 ), Q(Z2 ) ∈ Σout then Z1 ̸= Z2 . We will also write 
  Theorem 2.4: (22) h(ABCDEF ) ≤ h(ABC) + h(DE|C) + h(F ) h(ABCDEF ) ≤ h(EF A) + h(BD|F ) + h(C) Assuming that h satisfies h(ABC), h(CDE), h(EF A), h(BDF ) ≤ 1, we seek a partition of the space of polymatroids such that, in each part, one of the two RHS in (22) is ≤ 2. However, no axis-parallel partition exists with this property. If it existed, it would correspond to partitioning the values of C and F into light and heavy, but then the first join derived from (22) joins the light-C with heavy-F , while the se
  Proposition 4.1: Given a sub-probability measure pXY , the following hold: (23) • The following marginal measure is a sub-probability measure: X def pX (x) = pXY (x, y) y∈DomY (24) • Given x ∈ DomX the following conditional measure is a sub-probability measure: ( pXY (x,y) if pX (x) > 0 def pX (x) pY |X=x (y) = 0 otherwise As is customary in probability theory, we write pY |X (y|x) instead of pY |X=x (y), with the implicit understanding that there is a measure for each x ∈ DomX .
  Proof: This follows trivially from the AM-GM inequality: ! k1 k k k X Y X1X 1 XX p(Ω) = pi (x) pi (x) = pi (x) ≤ 1 ≤ k i=1 k i=1 i=1 x∈Ω x∈Ω x∈Ω □
  Proof: Write λZ = mdZ and wδ = mdδ where mZ and mδ are positive integers, and d is the minimum common denominator of theP coefficients. By P multiplying both sides of (13) by d, we obtain the integral Shannon-flow inequality (6): Z∈Z h(Z) ≤ δ∈D h(δ) in which Z is a multiset of elements in Σout and D is a multiset of elements in ∆. Furthermore, elements Z ∈ Z appear with multiplicity mZ , and elements δ ∈ D appear with multiplicity mδ . We first claim that there exist sub-probability measures pZ , one for each copy of Z in Z for which Y Y (27) pZ (t) ≥ pδ (t), ∀t ∈ DomV Z∈Z δ∈D To prove this claim, our strategy is to examine the proof sequence of the integral Shannon-flow inequality from Theorem 2.4

## sigmod26-079 — DP-S4S: Select-Join-Aggregate Query Processing with User-Level DP  (score 41)
domain: privacy-dp | aqp
usage: Rényi-divergence convexity and Bernstein/Chernoff bounds drive amplification and error proofs; SVT searches the truncation threshold.
note-quote: "Algorithm 2 satisfies (ε,0)-DP; with probability 1−β its error is Õ(√(n/q) + 1/q + τ*(T) + τ*(T)/(qε*))."
  Definition Differential Privacy~\cite{DBLP:journals/fttcs/DworkR14: For $ >0$ and $δ≥ 0$, a randomized mechanism $ : I O $ is $( ,δ)$-DP, if for any pair of neighboring database instances $ I ~ I '$ and any subset of outputs $O O $, it holds that \[ [ ( I ) O]≤ e^ · [ ( I ') O]+δ\,. \] When $δ=0$, we say the mechanism satisfies pure DP . Otherwise (i.e., when $δ>0$), we say the mechanism satisifes approximate DP . Note that in the above definition, the concept of ``neighboring'' database instances $ I ~ I '$ is left abstract, which we will clarify with concrete 
  Lemma : Let $ Err _ M _ (B( I ); ,δ,β)$ be the error of a local-sensitivity mechanism $ M _ $ for function $F$ under $( ,δ)$-DP, given bound $B( I )≥ _F( I )$ and $|B( I )-B( I ')|$ for neighboring datasets. The noise that PMSJA adds to each query is \[ σ_ PMSJA = O ( ^*( I )· Err ( 1 ; ,δ,β ) ) \] for $ ^*( I ):=max_u \| x _u\|_2$, where $ x _u=(x_ 1,u , ,x_ |F|,u )$ is the contribution of user $u$ to all queries. Specially, when $ M _ $ is the privately bounding local sensitivity mechanism~ [cite] , i
  Lemma Gaussian R\'enyi divergence~\cite{DBLP:journals/isci/GilAL13: Let $ N ( μ , Σ )$ denote the multivariate Gaussian distribution, then & D _α( N ( _1, Σ_1)\| N ( _2, Σ_2))\\ =& α 2 ( _1- _2)^T[Σ_α]^ -1 ( _1- _2)- 1 2(α-1) ln |Σ_α| |Σ_1|^ 1-α |Σ_2|^α if $αΣ_1^ -1 +(1-α)Σ_2^ -1 $ is positive definite, otherwise infinite, where $Σ_α=(1-α)Σ_1+αΣ_2$. In particular, when the noise added to each dimension is i.i.d., we have & D _α( N ( _1, σ_1^2 I _d)\| N ( _2, σ_2^2 I _d))\\ =& α 2 · \| _1- _2\|_2^2 (1-α)σ_1^2+ασ_2^2 + d 2(α-1) ln σ_1^ 2(1-α) σ_2^ 2α (1-α)σ_1^2+ασ
  Lemma Smooth-sensitivity Mechanism~\cite{DBLP:conf/stoc/NissimRS07: Given $F: I R ^d$ and its smooth-sensitivity function $ SS _F: I × R R $ as in Lemma~ [ref] , the smooth-sensitivity mechanism is defined as \[ _ SS ( I ):=F( I )+ · SS _F( I ,γ)· Z\,. \] Its privacy guarantee is as follows. ; If $γ= 6d $, $ = 6 $ and $Z~ Cauchy ^d(0,1)$, then $ _ SS-C $ satisfies $( ,0)$-DP. ; If $γ = 4(d+ln(2/δ)) $, $ = 2 $ and $Z~ ^d(0,1)$, then $ _ SS-L $ satisfies $( ,δ)$-DP. ; If $γ = 4(d+ln(2/δ)) $, $ = 5√ 2ln(2/δ) $ and $Z~ N (0, 1 _d)$, then $ _ SS-G $ satisfies $( ,δ)$
  Lemma : Algorithm~ [ref] satisfies $( ,0)$-DP. Let $n$ be the number of records, $m$ be the number of users and $q$ be the sample rate. With probability $1-β$, its error is \[ | f (T)-f(T)|= O (√ n q + 1 q + 1 _ i^* ( ^*(T)+√ ^*(T) q ) )\,, \] where $ O (·)$ omits constants and polylogarithmic factors in $m$ and $1/β$; $ ^*(T):=max_ u U Σ_ t T 1 [u t]$ is the maximum number of tuples a user contribute to in $T$; and $ _ i^* ≥ /L$ is the allocated noise‑calibration budget where $L= +1$.
  Lemma Simplified Computation: Algorithm~ [ref] satisfies $ '$-user-level DP for '( , , ,q) & = max \ ( logcdf ( , ,q_1) + · log A_1) ( logsf ( , ,q)+ ),\\ & - (( logcdf ( , ,q_2)+ · log A_2) ( logsf ( , ,q)- ) ) \ \,, where $ $ denotes the $ LogSumExp $ function, $ logcdf $ and $ logsf $ are the cumulative distribution function and survival function for the binomial distribution respectively, $A_1=1-q+q ( / )$, $q_1=q· exp( / )/A_1$; $A_2=1-q+q (- / )$, $q_2=q· exp(- / )/A_2$.
  Corollary : Assuming each user can have at most $C$ collaborators, let $λ_k: T T $ be the Sample-and-Explore process for $k$ sampled users, and let $ : T O $ be an $( ,δ)$-user-level DP mechanism. Then, $ λ_k$ satisfies $( ',δ')$-user-level DP for $ '=ln(1+(1-P)·(e^ 2 -1))$ and $δ'=(1-P)(e^ +1)δ$, where $P=0$ if $k>m/(C+1)$ and $P= m-kC k / m k $ otherwise. When $δ=0$, it suffices to take $ '=ln(1+(1-P)·(e^ -1))$.
  Lemma RDP Smooth Sensitivity Mechanism: Given $F: I R ^d$ and its smooth-sensitivity function $ SS _F: I × R R $ as in Lemma~ [ref] , the RDP-SS mechanism defined as \[ _ RDP-SS ( I ):=F( I )+σ( I )· N (0, 1 _d) \] satisfies $(α, )$-RDP for \[σ( I )= SS _F( I ,γ) 1-α+α e^ -2γ · √ α \,,\] where $γ:= 1 2 lnmin\ t_1^ -1 , t_2\ $, and $0<t_1<1<t_2$ are solutions to \[ h(t):=t^α-α e^ (α-1) d t +(α-1)e^ (α-1) d =0\,. \]
  Proof: Denote by $f(S):=Σ_ t S w_t$. Note that $f(S)=Σ_ t T 1 [t is sampled ]· w_t$. Therefore \[ E [f(S)]=q· f(T), and Var [f(S)]=q(1-q)Σ_ t T w_t^2≤ qn\,. \] We have $ [ | ( _i _i ) |≤ _i _i ln 3L β ]>1- β 3L $. By a union bound, this holds for all iterations with probability at least $1-β/3$. Conditioned on this happening, $ f (S, _i)$ is always an underestimate since $ f (S, _i)≤ f (S, _i)≤ f(S)$ and $ f (S, _ i^* )≥ f(S) - 2( _ i^* / _ i^* )ln(3L/β)$. Let $ ^*(S):=max_ u U Σ_ t S 1 [u t ]$ be the maximum number of records contributed by a user in the sample. For any $ ≥ ^*(S)$, the optimal solution to problem [ref] assigns $x_t=w_t$ so that $ f (S, )=f(S)$. In particular, this holds for a powe
  Proof: Consider $ I ~ I '$, let $σ( I )=σ_1$ and $σ( I ')=σ_2$. By the smoothness of $ SS _F$, we have \[ e^ -2γ ≤ σ_2^2 σ_1^2 = SS _F( I ',γ)^2 SS _F( I ,γ)^2 ≤ e^ 2γ \] According to Lemma~ [ref] , the divergence between the outputs is \[ D_α( ( I )\| ( I '))= α 2 · \|F( I )-F( I ')\|_2^2 (1-α)σ_1^2+ασ_2^2 + d 2(α-1) ln (σ_2/σ_1)^ 2α α(σ_2/σ_1)^2+1-α \] when $α(σ_2/σ_1)^2+1-α>0$, which can be satisfied when \[ σ_2^2 σ_1^2 ≥ e^ -2γ > α-1 α \,, \] or $γ< 1 2 ln α α-1 $. For the first term, we have α 2 · \|F( I )-F( I ')\|_2^2 (1-α)σ_1^2+ασ_2^2 &≤ α 2 · ( I )^2 σ_1^2· (α(σ_2/σ_1)^2+1-α) \\ &≤ 2 · 1-α+α e^ -2γ 1-α+α (σ_2^2/σ_1^2) \\ &≤ 2 \,. For the second term, we require that \[ d 2(α-1) ln (σ_2/σ_1
  bound: The upper bound on the number of collaborators $C$ for S\&E can also be computed from $D$.
  bound: This leads to a favorable privacy-accuracy trade-off, and DP-S4S consistently outperforms S\&E . fig/amazon2-eps fig/c2a-eps fig/amazon2-d Degree upper bound $D$ (equivalently $C$ and $ $) .
  bound: For graph queries, there are two reasons that account for a large $ $: on the one hand, the degree upper bound $D$ should be a data-independent worst-case upper bound.
  bound: With truncation, the query engine can impose an upper bound, e.g., up to 10 edges are counted for each user, which reduces the query sensitivity dramatically, leading to a lower noise requirement to satisfy DP, as we explain in Section~ [ref] .
  bound: Prior work R2T ~ [cite] addresses this by considering all possible truncation thresholds that are a power of two, which guarantees asymptotically optimal result accuracy, as we elaborate further in Section~ [ref] .
  bound: Since DP offers information-theoretic privacy guarantees, we do not rely on any assumption on the computational power of the adversary.

## sigmod26-231 — Sublime: Sublinear Error & Space for Unbounded Skewed Streams  (score 38)
domain: streaming | sketching
usage: Geometric-series epoch analysis over expansions; Markov/Chebyshev for confidence; Jensen for encoding length; entropy incompressibility argument proves the lower bound.
note-quote: "Such a sketch must use at least N/E(N) · [Ω(log 1/δ + log log(N/E(N))) − Θ(1)] bits of memory at some point in time while processing a stream."
  Lemma : Like CS, provides unbiased estimates. Moreover, letting~$V(F)$ denote its estimation variance when using a size function~$W(·)$ and one array on a stream with squared~$ _2$ - norm $F = Σ_y (f(y))^2$, we have at the end of epoch~$i$ that $$ V(F_ i+1 ) ≤ 1 W_i · Σ_ j=0 ^i 2^ i-j · (F_ j+1 - F_j), $$ where~$F_j$ denotes the squared~$ _2$ - norm of the stream at the start of epoch~$j$.
  Theorem : When using a sublinear power size as the function~$W(N)$, , has a memory footprint of~$≈ d · W(N) · [ 4.41 + 1.26 · log_2 N/W(N) ]$. With a linear size function, uses~$≈ d · W(N) · [4.41 + 1.26 · log_2(N log_2 N/W(N)) ]$ bits of memory. The same bounds hold for with~$W(F)$ instead of $W(N)$ and with an additive overhead of one bit per counter.
  Lemma : Letting~$E(N)$ denote the estimation error of when using a size function~$W(·)$ and one array on a stream with a total of~$N$ keys, we have at the end of epoch~$i$ that $$ E(N_ i+1 ) ≤ 1 W_i · Σ_ j=0 ^i 2^ i-j · (N_ j+1 - N_j), $$ where~$N_ j $ for $j≥ 0$ denotes the total number of keys in the stream at the start of epoch~$j$.
  Lemma : Letting~$E(N)$ denote the error of when using a size function~$W(·)$ on a stream of total of~$N$ keys, we have at the end of epoch~$i$ that $$ E(N_i) ≤ Σ_ j=0 ^i N^ res (k) _j-N^ res (k) _ j-1 W(N_j)+1-k , $$ where~$N^ res (k) _j$ for $j≥ 0$ is the total count of the keys not among the top~$k$ at the end of epoch~$j$.
  Lemma : Letting~$E(N)$ denote the estimation error of when using a size function~$W(·)$ on a stream with a total of~$N$ keys, we have at the end of epoch $i$ that $$ E(N_i) ≤ Σ_ j=0 ^i N_j - N_ j-1 W(N_j)+1 ,$$ where~$N_ j $ for $j≥ 0$ denotes the total number of keys in the stream at the end of epoch~$j$.
  Theorem : Consider an FE sketch with only overestimation errors that processes growing streams with an error bound of~$E(N)$ and a confidence of~$1-δ$. Such a sketch must use at least~$N/E(N) · [Ω ( log 1/δ + loglog (N/E(N)) ) - Θ(1)]$ bits of memory at some point in time while processing a stream.
  Lemma : Consider a counter array of size~$w$ and let~$k$ be the sum of its counters' absolute values. When the counters are unsigned, \ encodes the array in~$w · [ 4.41 + 1.26 · log_2 (k/w) ]$ bits. When the counters are signed, it uses one extra bit per counter to encode their signs.
  Lemma : When working with unsigned counters and stubs of length~$s$ bits, \ encodes a counter value~$v$ in $s$ bits if $v < 2^s$ and in at most $4 + 1.26 · log_2 v$ bits otherwise. Signed counters use one more bit for encoding the sign in both cases.
  Proof: If~$v < 2^s$, it fits in its stub and occupies~$s$ bits. Otherwise, its~$s$ lower-order bits are stored in its stub and its higher-order bits encoding~$ v/2^s $ are stored in an extension. This extension has~$ log_3 v/2^s + 2$ fragments to represent the digits and the delimiter. Thus, it occupies $$ 2 · ( log_3 v/2^s + 2) ≤ 2 · (log_3 2 · log_2 v/2^s + 2) ≤ 4 - s + 1.26 · log_2 v $$ bits. This, added to the~$s$ bits of space consumed by the stub, yields the result.
  Proof: Applying lemma:single_counter_space to every counter within the array and summing results in a total memory footprint of~$m ≤ Σ_i max(s, 4+1.26 · log_2 |v_i|)$ bits. To simplify this expression, we bound the stub length~$s$ based on the total absolute counter values. Recalling 's tuning mechanics from sec:accommodating_skew , \ uses stubs that are slightly longer than the length of the average counter. In fact, it never sets~$s$ to be more than~2 bits longer than the average counter length. Formally, this corresponds to~$s ≤ 2 + 1 w · Σ_i log_2 |v_i|$, where~$v_i$ denote the signed value of the~$i$ - th counter in the array. Since the logarithm is a concave function, we use Jensen's inequali
  bound: (the Appendix~ [cite] ). ; We prove a lower bound on the minimum space required for any expandable frequency estimation sketch to achieve a target error.
  bound: We now incorporate \ into the analysis to prove an upper bound on 's memory footprint in bits ( thm:all_counters_space ).
  bound: We also prove a memory footprint lower bound for any FE sketch that processes a growing stream without knowledge of its ultimate length (Theorems~ [ref] and [ref] ).
  bound: We observe that 's upper bound is within a small logarithmic factor of this lower bound, demonstrating 's ability to give rise to FE sketches that attain a desired level of accuracy with a close-to-optimal memory~footprint.
  bound: Plugging these values into the bound of~ thm:counter_array_per_counter_space and multiplying by~$d$ proves the result. [display] Lower Bound.
  bound: We prove a memory footprint lower bound for FE sketches by reducing them to filters.

## sigmod26-279 — Benchmarking Differentially Private Tabular Data Synthesis: [Experiments & Analysis]  (score 14)
domain: privacy-dp | benchmark
usage: RDP accounting assigns budgets across pipeline stages; KL convexity/chain-rule lemma proves the estimation-ordering theorem; no proofs in extract — statements imported from prior work.
note-quote: "the KL divergence error of conditional estimation is always no larger than that of independent estimation"
  Theorem : For any pair of attributes $(A_i, A_j)$, the KL divergence error of conditional estimation is always no larger than that of independent estimation. Formally, we have ( [A_i, A_j] \|\; [A_i, A_j] . ) ≤ ( [A_i, A_j] \|\; [A_i] [A_j] . ) Here $ [A_i, A_j]$ is defined in Equation~( [ref] ).
  Definition R\'enyi DP~\cite{mironov2017renyi: We say that an algorithm $ A $ satisfies $(α, )$-R\'enyi DP ($(α, )$-RDP) if and only if for any two neighboring datasets $D$ and $D'$ \[ D_ α ( A (D)|| A (D')) ≤ , \] where $D_ α (Y||N) = 1 α-1 ln E _ x ~ N [ Y(x) N(x) ]^ α $.
  Definition Differential Privacy: An algorithm $ A $ satisfies $( ,δ)$-differential privacy ($( ,δ)$-DP) if and only if for any two neighboring datasets $D$ and $D'$ and any $T Range ( A )$, we have $$ A (D) T ≤ e^ \, A (D') T + δ. $$
  Theorem Composition: Let $f: D R _1$ be $(α, _1)$-RDP and $g: R _1 × D R _2$ be $(α, _2)$-RDP respectively. Then the mechanism defined as $(X,Y)$, where $X ~ f(D)$ and $Y ~ (D, f(D))$, satisfies $(α, _1 + _2)$-RDP.
  Lemma : Assuming that \[ P_1(z) = Σ_x p(x)P_1(z|x) \;\;\; and \;\;\; P_2(z) = Σ_x p(x)P_2(z|x), \] we have \[ (P_1(z) \;\|\; P_2(z) ) ≤ Σ_x p(x) (P_1(z|x) \;\|\; P_2(z|x) ) \]
  Definition Sensitivity: Let $f: D R ^k$ be a vector-valued function of the input data, then the $ _2$ sensitivity of $f$ is defined as \[ _f = max_ D D' f(D) - f(D') _2 \]
  Theorem Post-Processing: Let $f: D R _1$ is $(α, )$-RDP, and $g: R _1 R _2$ is an arbitrary randomized mapping. Then $g f$ is also $(α, )$-RDP.
  Theorem : If $f$ is an $(α, )$-RDP mechanism, then it also satisfy $ ( + log 1/δ α - 1 , δ )$-DP for any $0 < δ < 1$.
  bound: Formally, this method can be expressed as $ Uniform Bin (x) = x - x_ h $, where $x_ $ is the lower bound of the attribute's domain, and $h$ is the length of the uniform interval determined by the bin number.

## sigmod26-237 — The Case For Language Model Approximated LIKE Predicate  (score 3)
domain: llm-db
usage: Entropy measures predicate difficulty; capacity ratio M/H(W) links model scale to success probability, gating when LM approximation is viable.
note-quote: "M represents an abstracted upper bound on the total task complexity H(W) that the model can reliably internalize ... a tractable, albeit simplified, link between model scale and task complexity."
  Theorem 5: 1 (Entropy of a LIKE Pattern). Given a LIKE pattern of length 𝑥 with 𝑝 underscore ("_") and 𝑞 percent ("%") wildcards over alphabet Σ, no two percent signs adjacent, and a maximum generated-string length of 𝑚, the pattern entropy 𝐻 (P) is: ! 𝑚−𝑥+𝑞 ∑︁ 𝑠 + 𝑞 − 1 𝑠 𝐻 (P) = 𝑝 ln |Σ| + ln |Σ| (2) 𝑞−1 𝑠=0 The proof is detailed in Appendix I.1.
  bound: Within our framework, 𝑀 represents an abstracted upper bound on the total task complexity 𝐻 (W) that the model can reliably internalize.
  bound: This leads to the following lower bound on the success probability:     𝑀 𝛼 |𝜃 | 𝑝 ≥ 𝑐 · min 1, = 𝑐 · min 1, (4) 𝐻 (W) 𝐻 (W) This assumption provides a tractable, albeit simplified, link between model scale and task complexity, enabling the derivation of sampling bounds.
