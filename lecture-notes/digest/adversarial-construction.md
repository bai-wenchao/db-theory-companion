# Chapter digest: adversarial-construction
n_papers_with_tool: 21
PODS exemplars are from the PODS'26 issue inside PACMMOD Vol 4 — mark them ^PODS.

## sigmod26-166 [PODS] — Near-Optimality for Single-Source Personalized PageRank  (score 60)
domain: graph-db
usage: Monte-Carlo discovery with Chernoff and union bound; martingale-difference error control with Azuma–Hoeffding; lower bounds via r-padded hard instances with hidden splits, Bayes likelihood-ratio switching.
note-quote: "DistWalks solves the SSPPR-R query within O(min{log(n)/δ, m + n·log n·log(mδ/n)}) queries in expectation."
  Definition 1.1: SSPPR with Relative Error Guarantee (SSPPR-R). Given a graph 𝐺, a decay factor 𝛼, a source node 𝑠, a relative error parameter 𝑐 ∈ (0, 0.5), an overall failure probability parameter 𝑝 𝑓 and choosing a targeting PPR threshold 𝛿 ∈ (0, 1), SSPPR-R query requires an estimated SSPPR vector 𝜋ˆ (𝑠, ·) such that with at least 1 − 𝑝 𝑓 probability, |𝜋ˆ (𝑠, 𝑡) − 𝜋 (𝑠, 𝑡)| ≤ 𝑐 · 𝜋 (𝑠, 𝑡) holds for all node 𝑡 with its PPR value 𝜋 (𝑠, 𝑡) ≥ 𝛿. Note that the threshold 𝛿 can be chosen arbitrary close to 0 or 1, w
  Definition 1.2: SSPPR with Additive Error Guarantee (SSPPR-A). Given a graph 𝐺, a decay factor 𝛼, a source node 𝑠, an additive error parameter 𝜀 ∈ (0, 1) and an overall failure probability parameter 𝑝 𝑓 , SSPPR-A query requires an estimated SSPPR vector 𝜋ˆ (𝑠, ·) such that with at least 1 − 𝑝 𝑓 probability, |𝜋ˆ (𝑠, 𝑡) − 𝜋 (𝑠, 𝑡)| ≤ 𝜀 holds for all node 𝑡.
  Definition 1.3: Single Target Personalized PageRank Query (STPPR). Given a graph 𝐺, a decay factor 𝛼, a target node 𝑠, a relative error parameter 𝑐 ∈ (0, 0.5), an overall failure probability parameter 𝑝 𝑓 and choosing a targeting PPR threshold 𝛿 ∈ (0, 1), STPPR query requires an estimated STPPR vector 𝜋ˆ (𝑠, ·) such that with at least 1 − 𝑝 𝑓 probability, |𝜋ˆ (𝑠, 𝑡) − 𝜋 (𝑠, 𝑡)| ≤ 𝑐 · 𝜋 (𝑠, 𝑡) holds for all node 𝑠 with its PPR value 𝜋 (𝑠, 𝑡) ≥ 𝛿. 1.2 Existing Results for SSPPR-A, SSPPR-R and STPPR Upper Bounds. 
  Theorem 1: 4 [Improved upper bound of SSPPR-R]. We present DistWalks (Algorithm 3), which   log(𝑛) solves the SSPPR-R query within O 𝑚 + 𝑛 log(𝑛) log( 𝑚𝛿 ) queries in expectation. DistWalks fundamentally overcomes the limitations of prior state-of-the-art methods that rely on deterministic push operations for initialization. Instead, it introduces a novel decomposition of PPR by accumulating contributions from already discovered effective paths, thereby avoiding redundant random walks over previously vis
  Definition 2: 1 [multigraph and edge multiplicity [30]]. A multigraph is a graph that may contain parallel edges (multiple edges between the same pair of nodes); a simple graph contains no such parallel edges. The edge multiplicity of a multigraph U is defined as MUL(𝐺) = max { 𝑒 ∈ 𝐸 U | 𝑒 = (𝑢, 𝑣)} , 𝑢,𝑣 ∈𝑉 representing the maximum number of parallel edges between any node pair.
  Lemma 3.1: (Proof in Appendix A.) With probability ≥ 1−𝑝 𝑓 , the node set 𝑉out returned by Algorithm 1 satisfies: 𝑉≥𝛿 ⊆ 𝑉out ⊆ 𝑉≥𝛿/(5𝑒 ) . Proc. ACM Manag. Data, Vol. 4, No. 2 (PODS), Article 110. Publication date: May 2026. 110:8 Xinpeng Jiang, Haoyu Liu, Siqiang Luo, and Xiaokui Xiao Based on this discovered candidate set from Algorithm 1, we can then estimate SSPPR values only for nodes in 𝑅out using the standard MC. For SSPPR-A query, we further run   2 1 2 𝑇 = 256[ log 𝛿/(5𝑒 ) + log 𝑝 ]/[𝑐 (𝛿/(5𝑒))]
  Lemma 3: 2 [Decomposition]. (Proof in Appendix B.1.) Given graph 𝐺, subset 𝑋 and source node 𝑠 ∉ 𝑋 , we have Í 𝐺𝑋 𝐺 (𝑢), (𝑢)/𝑑𝑜𝑢𝑡 𝜋𝐺 (𝑠, 𝑡) = 𝛼1 𝑢 ∈ 𝑋 𝜋𝐺 (𝑠, 𝑢)𝜋𝐺𝑋 (𝑢, 𝑡)𝑑𝑜𝑢𝑡 (3) where 𝐺𝑋 is constructed by: (A). 𝑉𝐺𝑋 = 𝑋 ∪ 𝑋 ∪ {𝑣 𝑋 }, where 𝑣 𝑋 is a new auxiliary node. (B). For nodes 𝑢 ∈ 𝑉 and 𝑡 ∈ 𝑋 , if directed edge (𝑢, 𝑡) ∈ 𝐸, add this edge to 𝐸𝐺𝑋 . (C). For nodes 𝑡 ∈ 𝑋 and 𝑢 ∈ 𝑉 /𝑋 , if directed edge (𝑡, 𝑢) ∈ 𝐸, add edge (𝑡, 𝑣 𝑋 ) to 𝐸𝐺𝑋 . Intuitively, Step (A). eliminates any new dangling nodes intro
  Lemma 4.1: To allow for more flexible control over the graph’s parameters like node/edge count, we next introduce a modified structure, the 𝑟 -padded instance. Intuitively, information of the graphs is encoded by edges between 𝑌 and 𝑋 . We can add some padding edges from nodes in 𝑌 (or to nodes in 𝑋 ) to confuse the algorithm. ∀U ∈ U (𝑛, 𝐷, 𝑑), we define the 𝑟 -padded instance of U, denoted as U(𝑟 ) = (𝑉𝑈 (𝑟 ) , 𝐸𝑈 (𝑟 ) ) in the below by involving two independent node sets 𝑍𝑋 and 𝑍𝑌 : (1) 𝑉𝑈 (𝑟 ) = 𝑉𝑈 ∪ 𝑍𝑋
  Proof: With 𝑜 (𝑛𝐷) known edges, for most nodes 𝑥𝑖 ∈ 𝑋 , we only know a small fraction of their in-edges, as per the “Traversal is Hard” intuition. For the split SP, there exists a subset 𝑋 ′ = 𝑋 1′ ∪ 𝑋 2′ , where 𝑋 1′ ⊂ 𝑋 1 , 𝑋 2′ ⊂ 𝑋 2 , and |𝑋 1′ |, |𝑋 2′ | ∈ Ω(𝑛), such that each 𝑥𝑖 ∈ 𝑋 ′ appears at most 𝑜 (𝐷) times in 𝑒 1, · · · , 𝑒𝑇 . We then use the ‘Random Walk is Hard’ intuition to show that, for nodes in 𝑋 ′ , it is hard for the algorithm to determine their class in the split. For each pair 𝑥𝑖1 ∈ 𝑋 1′, 𝑥𝑖2 ∈ 𝑋 2′ , consider a "switched" split SP′ created by setting 𝑥𝑖1 ∈ 𝑋 2 and 𝑥𝑖2 ∈ 𝑋 1 , while fixing the split for all other nodes. Using a Bayesian approach, we can bound the target succes
  Proof: 1 Fact A.1 [Chernoff bound]. Let 𝑋 1, . . . , 𝑋𝑛 be independent random variables taking values in [0, 1]. Let Í 2  𝑋 := 𝑛𝑖=1 𝑋𝑖 and 𝜇 := E[𝑋 ]. Then, for any 𝑐 > 0, (1) if 𝑐 ∈ [0, 1], then P[𝑋 > (1+𝑐)𝜇] ≤ exp − 𝑐3 𝜇 ;  2  (2) if 𝑐 ∈ [0, 1], then P[𝑋 < (1 − 𝑐)𝜇] ≤ exp − 𝑐2 𝜇 ; (3) if 𝑐 ≥ 1, then P[𝑋 > (1 + 𝑐)𝜇] ≤ exp − 𝑐3 𝜇 .

## sigmod26-306 — Efficient Defective Clique Enumeration and Search with Worst-Case Optimal Search Space  (score 38)
domain: graph-db
usage: Branching-vector induction over search-tree instances; Moon–Moser-style witness graphs for the output lower bound; colorful-degree core arguments for size bounds.
note-quote: "The worst-case output size of maximal k-defective clique enumeration is Ω(3^{n/3}·n^k) when k is a constant."
  Definition Colorful degeneracy ordering: Given a graph $G=(V, E)$ and a coloring $ $ of $G$, an ordering $(v_1, v_2, …, v_n)$ of its vertices $V$ is a colorful degeneracy ordering with respect to $ $ if, for each $1 i n$, $v_i$ is a vertex with the smallest colorful degree with respect to $ $ in the subgraph of $G$ induced by $\ v_i, v_ i+1 , …, v_n\ $.
  Definition Degeneracy ordering~\cite{matula1983smallest: Given a graph $G=(V, E)$, an ordering $(v_1, v_2, …, v_n)$ of its vertices $V$ is a degeneracy ordering if, for each $1 i n$, $v_i$ is a vertex with the smallest degree in the subgraph of $G$ induced by vertices $\ v_i, v_ i + 1 , …, v_n\ $.
  Lemma : Given a graph $G=(V, E)$, a coloring $ $ of $G$, and positive integers $k$ and $q$, let $S$ be a $k$-defective clique of size at least $q$. Then, every vertex $u S$ has colorful degree $d^ _ S (u) q - k - 1$ in the subgraph induced by $S$.
  Definition Colorful degree: Given a graph $G=(V, E)$ and a coloring $ $ of $G$, the colorful degree of $u V$ in $G$ with respect to $ $, denoted by $d^ _G(u)$, is the number of distinct colors among $u$'s neighbors. Formally, $d^ _G(u) = |\ (v): v N_G(u) \ |$.
  Lemma $k$-defective clique pivoting: Given an instance $(S, C, X)$ of a recursive call where $N_C(S) ≠ $, let $p$ be a vertex in $N_C(S)$. Every maximal $k$-defective clique that includes $S$ and is included by $S C$ must contain at least one vertex in $ _C[p]$.
  Definition Colorful $s$-core: Given a graph $G=(V, E)$, a coloring $ $ of $G$ and an integer $s$, the colorful $s$-core of $G$ with respect to $ $ is the maximal subgraph $g$ of $G$ in which every vertex $u$ in $g$ has colorful degree $d^ _ g (u) s$.
  Lemma : Given a graph $G=(V, E)$, a coloring $ $ of $G$, and positive integers $k$ and $q$, every $k$-defective clique of size at least $q$ is contained within the colorful $(q - k - 1)$-core of $G$ with respect to $ $.
  Definition $k$-defective clique: Given a graph $G=(V, E)$ and an integer $k$, a vertex subset $S$ of $V$ is a $k$-defective clique in $G$ if $G[S]$ misses at most $k$ edges (i.e., $G[S]$ has at least $ |S| 2 - k$ edges).
  Proof: Let $S$ be a $k$-defective clique of size $q$. By definition, $S$ contains at most $k$ missing edges. Consider the set of vertices incident to these missing edges. By removing one endpoint from each missing edge, we remove at most $k$ vertices from $S$, and the remaining vertices form a clique of size at least $q - k$. Therefore, a $k$-defective clique of size $q$ contains a clique of size $q - k$.
  Proof: Suppose not. Then, there exists a maximal $k$-defective clique $S'$ that includes $S$ but does not contain any vertex in $ _C[p]$. Since $p$ is in $N_C(S)$, all the vertices in $S'$ are adjacent to $p$. Therefore, $S' \ p\ $ is also a $k$-defective clique, and thus $S'$ is not maximal. This contradiction proves the lemma.
  bound: myproc Procedure clique reduce reduce KwIn Input KwOut Output Continue continue output output report report update refine upperbound upper-bound A graph $G=(V, E)$ and two integers $k, q$ Every maximal $k$-defective clique in $G$ of size $ q$ $ , V, $ \; $S, C, X$ $C X = $ and $|
  bound: Since we aim to find $k$-defective cliques of size at least $q$, we compute an upper bound on the size of a $k$-defective clique that includes $S$ and is included by $S C$ (Line~ [ref] ).
  bound: Whenever this upper bound is less than $q$, the remaining recursive calls are pruned, as they cannot generate any solution of size at least $q$.
  bound: This upper-bound-based pruning is applied to $(S, C)$ in Line~ [ref] , in order to reduce the search space efficiently.
  bound: However, the recursive calls of $M_2$ are pruned since the upper bound of $(\ u_1, u_2\ , \ u_3, u_4, u_8\ )$ is $|S| + c + r = 2 + 0 + 1 = 3$, which is less than $q=4$.
  bound: After backtracking to $M_1$, $u_2$ is moved from $C$ to $X$, and the upper bound of $(\ u_1\ , \ u_3, u_4, …, u_8\ )$ is computed in Line~ [ref] .

## sigmod26-349 — Proximity Graphs for Similarity Search: Construction, Lower Bounds, Euclidean Separation  (score 36)
domain: ann-vector-search
usage: Covering-net decompositions yield sparse constructions; adversarial point sets give size lower bounds; a log-drop distance-descent lemma bounds greedy query paths.
note-quote: "there is a set P of Θ(n) points ... such that any 2-PG for P must have Ω(n log Φ) edges, regardless of the query time allowed."
  Theorem : The following statements are true: ; For any integers $ $ and $n$ that are powers of 2 satisfying $n 2$ and $n^2 2 2^n$, there is a set $P$ of $Θ(n)$ points with aspect ratio $ $ from a metric space whose doubling dimension is 1, such that any 2-PG for $P$ must have $Ω(n log )$ edges, regardless of the query time allowed. ; Given any integers $s 2$, $t 1$, and constant $d 1$, there is a set $P$ of $n = s^d · t$ points with aspect ratio $ = O(n)$ from a metric space whose doubling dimension $λ$ i
  Theorem : Given any set $P$ of $n$ points in the metric space $( ^d, L_2)$ where $d = O(1)$, there is a $(1+ )$-PG that has $O((1/ )^ λ · n)$ edges and ensures a query time of $O((1/ )^λ · log^2 + (1/ )^ d-1 log n · log^2 )$, where $n$ and $ $ are the size and aspect ratio of the data input, respectively, and $λ = O(d)$ is the doubling dimension of $( ^d, L_2)$. W.h.p., we can build such a graph in $(1/ )^ λ · n (n )$ time.
  Lemma : Fix an arbitrary point $q $, and an arbitrary point $p^ P$ that is not a $(1+ )$-ANN of $q$. Define p^+_ = the out-neighbor of $p^ $ closest to $q$. Both of the following statements are true: ; $D(p^+_ , q) < D(p^ , q)$. ; [The log-drop property] Let $ $ be any point in $P$ satisfying $D( , q) D(p^+_ , q)$. If $ $ is not a $(1+ )$-ANN of $q$, then log D( , p^*) < log D(p^ , p^*) where $p^*$ is an exact NN of $q$.
  Theorem : For a metric space with a constant doubling dimension $λ$, there is a $(1+ )$-PG that has $O((1/ )^λ · n log )$ edges and $O((1/ )^ λ · log^2 )$ query time, where $n$ and $ $ are the size and aspect ratio of the data input, respectively. We can construct such a graph in $(1/ )^ λ · n (n )$ time.
  Lemma : Let $x$ and $y$ be two points that are on the surfaces of $B(q, r)$ and $B(q, (1+ )r)$, respectively. If $L_2(p, x) = L_2(p, y)$, then the angle between the rays $ _ p, x $ and $ _ p, y $ is strictly larger than $ /8$.
  Fact : Let $a, b,$ and $c$ be three distinct points in $ ^d$ such that the angle $γ$ between rays $ _ a, b $ and $ _ a, c $ satisfies $0 < γ < /2$. If $L_2(a, b) = L_2(a, c) = l > 0$, then $L_2(b, c)< l · γ$.
  Lemma : The following inequalities are correct: l &<& (2+ ) · r \\ L_2(p^*, v) & & l · γ \\ L_2(v, _ ) &<& l · (1 - γ) \\ L_2( _ , p_ ) & & l · γ.
  Fact : If a point $p P$ is not a $(1+ )$-ANN of $q$, either $log D(p, p^*) α - 1$ or $D(p, q) > D(y^ , q)$.
  Proof: Fix any point $p P$. If $p Y_i$ for some $i [0, h]$, there is a point $y Y_i$ with $D(p, y) 2^i$ due to the covering property. This $y$ must be an out-neighbor of $p$. Next, we assume that $p Y_i$ for all $i [0, h]$. Denote by $j$ the highest value of $i$ satisfying $|Y_i| 2$. Let $y$ be any point in $Y_i$ different from $p$. We argue that $y$ must be an out-neighbor of $p$, i.e., $D(p, y) · 2^j$. Indeed, this is true if $j = h$ because $D(p, y) (P) 2^h$. Consider now $j < h$. It follows from the definition of $j$ that $|Y_ j+1 | = 1$, in which case the covering property tells us $D(p, y) 2^ j+1 < · 2^j$.
  Proof: [Proof of Fact~ [ref] ] Suppose that $D(p, p^*) > 2^ α - 1 $ and $D(p, q) D(y^ , q)$ hold simultaneously. We argue that in this case $p$ must be a $(1+ )$-ANN of $q$, which will then validate Fact~ [ref] . Consider first $β = 0$. From [ref] , we know $D(p^*, y^ ) 1$, implying that $y^ = p^*$ (the inter-point distance in $P$ is at least 2). The condition $D(p, q) D(y^ , q)$ asserts that $p$ must be an exact NN of $q$. The subsequent discussion assumes $β = α - - 1 > 0$. By $D(p, p^*) > 2^ α-1 $ and $D(y^ , p^*) 2^β = 2^ α- -1 $, we obtain && D(y^ , p^*) < D(p, p^*) / 2^ \\ && D(p,q) D(y^ , q) D(y^ , p^*) + D(p^*, q) < D(p, p^*) / 2^ + D(p^*, q). On the other hand, the triangle inequality show
  bound: The hard inputs used in our lower-bound arguments are non-geometric, thus prompting the question of whether improvement is possible in the Euclidean space (a key subclass of metric spaces).
  bound: It is worth mentioning that the parameter $t$ in Statement (2) permits the lower bound to hold for a wide range of $ $.
  bound: A Size Lower Bound under $ = 1 $ This section serves as a proof of Statement (1) of Theorem~ [ref] .
  bound: A Size Lower Bound under Small $ $ This section serves as a proof of Statement (2) of Theorem~ [ref] .
  bound: When $t = 1$, the set $P$ degenerates into a hard input used in [cite] to prove a lower bound on the query time of ANN data structures.
  bound: Generalizing that hard input to establish a size lower bound for $(1+ )$-PGs under a wide range of $ $ demands additional ideas, as we have shown above.

## sigmod26-123 — GPM: Gaussian Pancake Mechanism for Undetectable DP Backdoors  (score 32)
domain: privacy-dp | security
usage: Covertness by reduction to hCLWE hardness (SIVP/GapSVP vs BQP); Gaussian c.d.f. analysis of peak misalignment along w; Weyl equidistribution over random w for the average-case bound.
note-quote: "With constant probability over sampling w ←$ S^{d−1}, M is not (ε,δ)-DP where ε ∈ Θ(1/β²) for any 0 < δ ≤ 0.5."
  Definition 2: 1 (differential privacy [1, 2]). Given ε>0 and 0≤δ <1, a mechanism M:D →Y is (ε,δ)-differentially private (DP) if, for any pair of neighbouring databases D ≃D′ that differ in a single row, and subset S ⊆Y,   any measurable   Pr M(D)∈S ≤eεPr M(D′)∈S +δ. (1) 2.3 Gaussian Mechanism The Gaussian mechanism (GM) [2] is one of the most prevalent DP mechanisms. We state its definition and provide its DP guarantee in Definition 2.2 and Theorem 2.3, respectively.
  Theorem 2.3: For any 0 < δ ≤ 0.5 such that ε = 2σ (δ), where ∆ := 2 − σ Φ ′ supD≃D′ ∥q(D)−q(D )∥ is the sensitivity of query q, and Φ is the cumulative distribution function (c.d.f.) of the standard Gaussian distribution, M∗σ (·) is (ε,δ)-DP [2]. Note that, compared with Theorem 2.3, a more commonly used yet looser closedq form upper bound for ε is ∆ 2ln 1.25 σ δ for 0<ε,δ <1. However, due to the extreme values of ε involved in our analysis, we do not restrict ourselves to ε<1 in this study. 2.3.1 Numerical 
  Lemma 2.6: For any w ∈Sd−1 and β,γ >0, the p.d.f. of the hCLWE distribution 5 Hw,β,γ (as defined in Definition 2.5) can  be expressed as  X γz (6) ψw,β,γ (y)∝ aβ,γ,z ·ϕ y; 2 2 w,Σw,β,γ β +γ z∈Z       2 γ2 γz 1 ⊤ I − where aβ,γ,z :=exp − β2πz and Σ := ww , such that ϕ ·; w,Σ 2 2 2 2 2 w,β,γ w,β,γ +γ 2π β +γ   β +γ γz is the p.d.f. of the multivariate Gaussian distribution N β2 +γ2 w,Σw,β,γ [25]. The hCLWE distribution and the multivariate Gaussian distribution are considered computationally indist
  Theorem 2: 7 states that, under the aforementioned hardness assumptions, no such PPT adversary exists.
  Theorem 2.7: Under the assumption that SIVP ∈ / BQP or GapSVP ∈ / BQP, √ for 2 d ≤ γ(d) ≤ dO(1), β(d) = d−O(1), and n(d) ∈ dO(1), and any family of PPT adversaries {Ad}d∈N, there exists µ(d)∈negl(d) such that     Ad(sd)=1: Ad(sd)=1:  ≤µ(d),  w ←$ Sd−1 (7) Pr ⊗n(d) −Pr √ d ⊗n(d) sd ←$ N (0,Id) sd ←$ 2πHwd ,β(d),γ(d) where ⊗n(d) represents the Cartesian product of n(d) identical distributions. Equivalently, each observation ri(1≤i≤n(d)) is independently sampled from the identical √ distribution of either
  Definition 3: 1 (Gaussian pancake mechanism). Given a query q :D →Rd and hyperparameters σ >0, w ∈Sd−1, and β,γ >0, the Gaussian pancake mechanism is given by √ Mσ,w,β,γ (D)=q(D)+ 2πσ·Hw,β,γ . (8) We denote the p.d.f. of Mσ,w,β,γ (D) as fσ,w,β,γ (·;D). √ Note that the multiplicative factor of 2π in the hCLWE noise ensures that the scale remains consistent with GM. In the rest of this section, we formalize a backdoor attack based on GPM, while formalizing its undetectability by any party other than the backdoo
  Theorem 3: 4 (Covertness of GPM √ backdoor). For any family of PPT adversaries {Ad}d∈N, any n(d)∈dO(1), any 2 d≤γ(d)≤dO(1), any β(d)=d−O(1), there exists µ(d)∈negl(d) such that for any D1,D2,...,D  n(d) ∈Dd,  Ad(od)=1: Ad(od)=1: Pr −Pr ≤µ(d), (9) od ←$ GMSeq(d) od ←$ GPMSeq(d) where GMSeq(d) and GPMSeq(d) are defined as Figure 3. In Theorem 3.4, Ad represents any party (e.g., the server and downstream users) trying to distinguish between the outputs of GM and GPM, without knowledge of the backdoor key 
  Theorem 4.2: Although computationally indistinguishable from GM, GPM’s actual privacy costs exceed ε = 104 even when δ = 0.1, which is infeasible for practical applications. Note that the lower and upper bounds of GPM’s privacy costs still change with respect to δ, although at smaller relative scales. and state the key technical Lemma 4.1 for analyzing the behaviour of GPM in the direction of w. As exemplified in Figure 4, T and t correspond to the integer and decimal parts of the ratio between the √ project
  Proof: Assume the opposite. Then an efficient distinguishing adversary could distinguish between samples from the multivariate Gaussian distribution and the hCLWE distribution, thereby breaking the hardness assumption. Similar to Theorem 3.4, for the RPA without knowledge of the backdoor key w, GPM maintains the same level of privacy guarantee, which is formalized in Appendix A.1. 4 Analysis on the Statistical Privacy Leakage to the BPA By Theorem 3.4, GPM is computationally indistinguishable from GM and achieves the same level of privacy computationally, i.e., against a PPT adversary without knowledge of the backdoor key w. However, as shown in Figure 4, in the direction of w, the probability dens
  Proof: For neighbouring databases D ≃ D′, when q(D) and q(D′) differ by T +t in the direction of w, the projected distributions of Mσ,w,β,γ (D) γ and Mσ,w,β,γ (D′) are H1,β,γ and H1,β,γ +(T +t) β2 +γ , respectively. Therefore, by  2   

## sigmod26-150 — LMSC: Local Sketch Modularity for Size-Constrained Community Search  (score 26)
domain: graph-db
usage: Algebraic comparison of LM/LSM gain differences; worst-case log-ratio bounding for the τ threshold; explicit clique counterexamples for non-monotonicity/non-submodularity.
note-quote: "LSM does not suffer from local free-rider effect when the identified community is a weak sense community and τ > log_{1+1/h}( h(h−1)/2 )."
  Definition 1: (Newman Modularity [29]). Given a graph 𝐺 = (𝑉 , 𝐸) and a set of nodes C ⊆ 𝑉 ,   𝑑 C2 1 NM(𝐺, C) = 2|𝐸 | 2𝑙 C − 2|𝐸 | , where 𝑙 C denotes the sum of internal edge weights, 𝑑 C is the sum of all node weights, and |𝐸| is the total number of edges. While foundational, maximising Newman modularity (NM) is NP-hard [3] and suffers from the well-known resolution limit [12], which causes small communities to be merged into larger ones as the graph size increases. To address the detection of smaller an
  Definition 2: (Density Modularity [21]). Given a graph 𝐺 = (𝑉 , 𝐸) and a set of nodes C ⊆ 𝑉 ,   𝑑2 DM(𝐺, C) = 2|1C | 2𝑙 C − 2|𝐸C | , where |C| is the number of nodes in C. Density modularity extends classical modularity for the community search problem by explicitly incorporating community size. Wang et al. [38] further refined DM with a cohesiveness factor 𝜏 which enables adjustable control over the preference for a smaller or denser community:
  Definition 3: (Density Sketch Modularity [38]). Given a graph 𝐺 = (𝑉 , 𝐸), a set of nodes C ⊆ 𝑉 ,   𝑑2 and 𝜏 ∈ R ≥0 , DSM(𝐺, C) = 2| C1 |𝜏 2𝑙 C − 2|𝐸C | . Despite the efforts to adapt modularity to community search, these measures fundamentally depend on global graph statistics, such as the total number of edges or the degrees of all nodes. This reliance restricts their scalability to large-scale or partially observable networks. Moreover, as the graph size increases, the score becomes increasingly dominate
  Definition 4: (Luo Modularity [25]). Given a graph 𝐺 = (𝑉 , 𝐸) and a set of nodes C ⊆ 𝑉 , LM(𝐺, C) = 𝑜𝑙 CC , where 𝑜 C denotes the sum of external edge weights. Proc. ACM Manag. Data, Vol. 4, No. 1 (SIGMOD), Article 52. Publication date: February 2026. LMSC: Local Sketch Modularity Optimisation for Size-Constrained Community Search in Networks 52:5 Unlike modularity-based measures that require global graph information and null models, LM operates purely on local structure, relying only on the internal and ext
  Definition 5: (Local Sketch Modularity). Given a graph 𝐺 = (𝑉 , 𝐸), a sketch threshold 𝜏 ∈ R ≥0 , and a set of nodes C ⊂ 𝑉 , the local sketch modularity (LSM) is defined as follows: LSM(𝐺, C, 𝜏) = 𝑙C 1 · 𝑜 C |C|𝜏 Observe that the minimum value of LSM is 0 when there are no internal edges within the community. Conversely, the maximum value of LSM is achieved when 𝜏 = 0 and a community contains all the nodes except the one with the lowest sum of edge weights. In Section 4, we compare the properties of LSM with 
  Theorem 1: The LMSC problem is NP-hard. We establish NP-hardness by a polynomial-time reduction from Set Cover. Given a Set Cover instance with a set of items and a collection of itemsets, we construct a weighted graph in which nodes represent items and itemsets, with edges encoding set membership. Additional nodes and edge weights are introduced so that minimising the number of selected sets in Set Cover corresponds to maximising the LSM objective when 𝜏 = 0. Thus, any optimal solution to the LMSC instanc
  Definition 6: (Local free-rider effect [17, 40]). Given a graph 𝐺 = (𝑉 , 𝐸), query nodes 𝑄, the goodness function 𝑓 . Let 𝐺 [𝐻 ] denote the identified community based on the function 𝑓 and 𝐺 [𝐻 ′ ] represent a query-independent local optimum subgraph. The function 𝑓 suffers from the local free-rider effect if 𝑓 (𝐻 ∪ 𝐻 ′ ) ≥ 𝑓 (𝐻 ). 4.2.1 Comparison Between LM and LSM. In the following, we theoretically analyse the local free-rider effects of LM and LSM.
  Theorem 2: LSM is less prone to the local free-rider effect than LM.
  Proof: Let 𝐻 be the set of nodes of an identified subgraph and 𝐻 ′ be the set of nodes the optimal subgraph. Let 𝑙𝐻,𝐻 ′ be the sum of edge weights between 𝐻 \ 𝐻 ′ and 𝐻 ′ \ 𝐻 , 𝑙𝑑𝑢𝑝 denote the sum of duplicated internal edge weights between 𝐻 and 𝐻 ′ , 𝐻𝑑𝑢𝑝 represent the set of duplicated nodes between 𝐻 and 𝐻 ′ . We further define 𝑙𝐻𝑑𝑢𝑝 ,𝐻 ′ \𝐻 and 𝑙𝐻𝑑𝑢𝑝 ,𝐻 \𝐻 ′ as the sum of edge weights between the duplicated nodes 𝐻𝑑𝑢𝑝 and the non-overlapping nodes in 𝐻 ′ and 𝐻 , respectively. To simplify the notation, we denote 𝑙𝑐𝑟𝑜𝑠𝑠 = 𝑙𝐻𝑑𝑢𝑝 ,𝐻 ′ \𝐻 + 𝑙𝐻𝑑𝑢𝑝 ,𝐻 \𝐻 ′ . We now formulate the equations of 𝐿𝑀 (𝐻 ), 𝐿𝑀 (𝐻 ∪ 𝐻 ′ ), 𝐿𝑆𝑀 (𝐻 ), and 𝐿𝑆𝑀 (𝐻 ∪ 𝐻 ′ ). • LM(𝐻 ) = 𝑜𝑙𝐻𝐻 𝑙 +𝑙 ′ −𝑙 +𝑙 ′ 𝑑𝑢𝑝 𝐻 ,𝐻 • LM(𝐻 ∪ 𝐻 ′ ) =
  Proof: To guarantee that LSM does not suffer from the free-rider effect, it must satisfy the following inequality. LSM(𝐻 ) > LSM(𝐻 ∪ 𝐻 ′ ) ⇒ 0 > 𝑜 𝐻 · (𝑙𝐻 + 𝑙𝐻 ′ − 𝑙𝑑𝑢𝑝 + 𝑙𝐻,𝐻 ′ ) (𝑜 𝐻 + 𝑜 𝐻 ′ − 𝑙𝑐𝑟𝑜𝑠𝑠 − 2𝑙𝐻,𝐻 ′ ) · 𝑙𝐻 − (|𝐻 | + |𝐻 ′ | − |𝐻𝑑𝑢𝑝 |)𝜏 |𝐻 |𝜏 To derive a lower bound on 𝜏, we rewrite the following inequality in terms of 𝜏. ′ 𝑙𝐻 ∪𝐻 𝑙𝐻 log 𝑜 𝐻 + log 𝑙𝐻 ∪𝐻 ′ − log 𝑙𝐻 − log 𝑜 𝐻 ∪𝐻 ′ − log 𝑜𝐻 + log 𝑜𝐻 ∪𝐻 ′ 𝜏> = |𝐻 ′ | − |𝐻𝑑𝑢𝑝 | log (|𝐻 | + |𝐻 ′ | − |𝐻𝑑𝑢𝑝 |) − log |𝐻 | log (1 + ) |𝐻 | The inequality above provides an exact expression for the lower bound of the sketch threshold 𝜏 required to eliminate the free-rider effect in LSM. To make this condition more interpretable and Proc. ACM Manag. Da

## sigmod26-179 — Order-based Algorithms for Core Maintenance in Bipartite Graphs  (score 20)
domain: graph-db
usage: Order-position necessary/sufficient conditions (lack values, monotone paths in the order) prune candidate sets; the adversarial construction separates insertion from deletion complexity.
note-quote: "Updating these bi-cores after inserting an edge (u,v) is unbounded under the model of locally persistent algorithms."
  Theorem 3.1: Given a bipartite graph 𝐺 and all its bi-cores, updating these bi-cores after inserting an edge (𝑢, 𝑣) is unbounded under the model of locally persistent algorithms.
  Theorem 3.2: Given a bipartite graph 𝐺 and all its bi-cores, updating these bi-cores after deleting an edge (𝑢, 𝑣) is bounded.
  Theorem 3: 1 and 3.2 highlight the asymmetry of the bi-core maintenance problem: handling edge deletions is significantly simpler than handling edge insertions. This asymmetry distinguishes Proc. ACM Manag. Data, Vol. 4, No. 1 (SIGMOD), Article 60. Publication date: February 2026. Order-based Algorithms for Efficient Core Maintenance in Large Bipartite Graphs 60:7 bi-core maintenance from other incremental problems, such as incremental maximum cardinality matching in bipartite graphs [45], where both inser
  Theorem 3.3: When handling an inserted edge (𝑢, 𝑣), Algorithm 1 costs 𝑂 (𝑚·max{𝑑 (𝑢, 𝐺), 𝑑 (𝑣, 𝐺)}) time. When handling a deleted edge (𝑢, 𝑣), the time complexity of Algorithm 1 is 𝑂 (|CHANGED| + |𝑁 1 (CHANGED, 𝐺)|). As shown in Theorem 3.3, the time complexity for an edge insertion is 𝑂 (𝑚·max{𝑑 (𝑢, 𝐺), 𝑑 (𝑣, 𝐺)}) = Ω(|CHANGED| · max{𝑑 (𝑢, 𝐺), 𝑑 (𝑣, 𝐺)}), which indicates that the time cost is unbounded. In contrast, Proc. ACM Manag. Data, Vol. 4, No. 1 (SIGMOD), Article 60. Publication date: February 2026. 
  Lemma 5.1: Let 𝐺 be a bipartite graph, 𝑂 𝛼=𝑖 an associated order, and (𝑢, 𝑣) an inserted edge with 𝑢 ⪯𝛼=𝑖 𝑣. Then, a c-pair update may occur for vertices in the original (𝑖, ·)-cores only if 𝐿(𝑖, 𝑢, 𝑂 𝛼=𝑖 ) = 1. In this case, 𝑢 is the first vertex added to the candidate set. Proc. ACM Manag. Data, Vol. 4, No. 1 (SIGMOD), Article 60. Publication date: February 2026. 60:12 Qiaoyuan Yang, Wensheng Luo, Yixiang Fang, and Yuanyuan Zeng Example 3. Consider the edge insertion (𝑢 4, 𝑣 2 ) in Figure 1, under 𝛼 = 2.
  Theorem 5: 1 establishes a necessary condition for triggering updates after an edge insertion. Specifically, inserting (𝑢, 𝑣) decreases 𝐿(𝑖, 𝑢, 𝑂 𝛼=𝑖 ) by at most one, as it introduces only a single new neighbor for 𝑢. If 𝑢 still lacks sufficient neighbors appearing after it in 𝑂 𝛼=𝑖 to support an update (𝐿(𝑖, 𝑢, 𝑂 𝛼=𝑖 ) > 0 after it is decreased), no vertices in the original (𝑖, ·)-cores will be added to the candidate set. Otherwise, 𝐿(𝑖, 𝑢, 𝑂 𝛼=𝑖 ) = 0, 𝑢 is added to the candidate set, triggering further
  Lemma 5.2: Given a bipartite graph 𝐺, an order 𝑂 𝛼=𝑖 , and an insertion edge (𝑢, 𝑣) with 𝑢 ⪯𝛼=𝑖 𝑣, a vertex 𝑤 ∈ 𝐺 can belong to the candidate set only if the following conditions hold simultaneously: (1) 𝐴𝛼=𝑖 (𝑢, 𝐺) ≤ 𝐴𝛼=𝑖 (𝑤, 𝐺) ≤ 𝐴𝛼=𝑖 (𝑣, 𝐺), and (2) there exists a path 𝑤 0, 𝑤 1, . . . , 𝑤𝑡 such that 𝑤 0 = 𝑢, 𝑤𝑡 = 𝑤, and for each 0 ≤ 𝑝 < 𝑡, (𝑤 𝑝 , 𝑤 𝑝+1 ) ∈ 𝐸 with 𝑤 𝑝 ⪯𝛼=𝑖 𝑤 𝑝+1 . Example 4. Consider the graph 𝐺 in Figure 1 with the inserted edge (𝑢 4, 𝑣 2 ) and order 𝑂 𝛼=2 . We have 𝑢 4 ⪯𝛼=2 𝑣 2 , 𝐴𝛼=2 
  Lemma 5.3: Given a graph 𝐺, an inserted edge (𝑢, 𝑣), the target c-pair (𝑖, 𝑗), and the order 𝑂 𝛼=𝑖 , for any vertex 𝑤 belongs to the candidate set, the updated 𝐿(𝑖, 𝑤, 𝑂 𝛼=𝑖 ) is not less than its value before the update. Based on this result, we state the following theorem regarding the correctness of the entire algorithm.
  Proof: We prove this by contradiction. We begin by constructing a bipartite graph 𝐺 with 𝑛 vertices and defining two updates, each consisting of a single edge insertion, denoted as Δ𝐺 1 and Δ𝐺 2 , which will be applied to 𝐺, and satisfy the following properties: for both Δ𝐺 1 and Δ𝐺 2 , the corresponding Δ𝑅 is empty; after applying Δ𝐺 1 and Δ𝐺 2 sequentially to 𝐺, the |Δ𝑅| is Ω(𝑛). Assuming a bounded LP algorithm 𝑀 exists, the workload for 𝑀 to process either 𝐺 ⊕ Δ𝐺 1 or 𝐺 ⊕ Δ𝐺 2 would be 𝑂 (1). However, based on the properties of the constructed graph, we can prove that the total maintenance cost for any LP algorithm 𝑀 to handle 𝐺 ⊕ Δ𝐺 1 and 𝐺 ⊕ Δ𝐺 2 separately is Ω(𝑙), where 𝑙 is a non-constant v
  Proof: During the bi-core maintenance for edge deletion, if the current vertex is to be removed from the original bi-core, it is added to CHANGED, leading to a visit to its neighbors. Otherwise, if it remains in the bi-core, neighbor visits are skipped. Thus, the maintenance time cost is 𝑂 (|CHANGED| + |𝑁 1 (CHANGED, 𝐺)|), making it bounded. The complete proof is in our full paper [52].
