# Chapter digest: spectral-matrix
n_papers_with_tool: 13
PODS exemplars are from the PODS'26 issue inside PACMMOD Vol 4 — mark them ^PODS.

## sigmod26-038 [PODS] — Bounding the Fragmentation of B-Trees Subject to Batched Insertions  (score 57)
domain: indexing
usage: Spectral (Jordan/Perron–Frobenius) analysis proves convergence; eigenvector arithmetic yields the fullness bounds; regime analysis selects even/uneven/TargetSplit algorithms.
note-quote: "For r ∈ (B/(2i), B/(2i−1)], the expected final fullness of the deferred even split strategy is 2ir(H₂ᵢ − Hᵢ)/B, where Hₖ is the k-th harmonic number."
  Lemma : Let $A R ^ d× d $ be an irreducible Metzler matrix, and assume there exists a positive (entry-wise) $w$ such that $w^ T · A=r· w^ T $ for some $r>0$, then: ; $r$ is a simple eigenvalue of $A$, and there exists a positive eigenvector corresponding to it. ; For every other eigenvalue $λ σ (A )$, we have $ R e (λ )<r$, where $ R e(·)$ denotes the real part of a complex number.
  Lemma : Let $n_ 0 ,r$ be positive integers, let $λ C $, and let $N_ λ $ be a nilpotent matrix ($N_ λ ^ d =0$). Then: \[ |Π_ k= |λ | ^ m-1 (I+ N_ λ n_ 0 +k· r+λ ) |≤ C_ N_ λ · (ln (m ) )^ d \] for sufficiently large $m$, where $C_ N_ λ $ is a constant depending only on $N_ λ $ and independent of $m$ (where $ |· |$ refers to the standard operator norm over complex matrices).
  Theorem : Assume a matrix recurrence: $ n≥ n_ 0 :v_ n+r = (I+ 1 n · A )v_ n $ where $r σ (A )$ is a simple positive eigenvalue, and all other eigenvalues $λ$ satisfy $ R e (λ )<r$. Then $ _ m ∞ v_ n_ 0 +m· r n_ 0 +m· r =P_ r ^ A ( v_ n_ 0 n_ 0 )$ where $P_ r ^ A $ is the spectral projection operator onto $A$'s generalized eigenspace corresponding to eigenvalue $r$.
  Lemma : Assume we call TargetSplit during an insertion batch, and at least $f_L+f_R-B-1$ additional insertions remain in the batch (i.e the next $f_L+f_R-B-1$ insertions will take place between $k$ and $k_ j+1 $ in ascending order). Then, following the call to TargetSplit , $A_L$ contains $f_L$ keys and $A_R$ contains $f_R$ keys.
  Definition : For a square matrix $A R ^ d× d $, we define the underlying directed graph of $A$, denoted by $G [A ]$, as a directed graph on $V= \ 0,...,d-1 \ $ in which for every $i,j \ 0,...,d-1 \ $, there exists a directed edge from $i$ to $j$ in $G [A ]$ if and only if $A_ i,j ≠0$.
  Lemma : Suppose $B$ and $i$ are positive integers. For $r (B/(2i),\,B/(2i - 1) ]$, the expected final fullness of the deferred even split strategy is \[ 2ir B \,(H_ 2i - H_ i ) . \] where $H_k = 1 + 12 + … + 1k$ is the $k$-th harmonic number.
  Lemma : Let $w= (d,d+1,...,B )$, and let $u$ be any eigenvector corresponding to eigenvalue $r$ for $A (B,r )_ S $. Then the expected average block size converges to at least $ u,w_ S u, 1 $, where $ · $ denotes the standard inner product.
  Lemma : Let $n_ 0 ,r$ be positive integers, and let $λ C $. Then: \[ |Π_ k= |λ | ^ m-1 (1+ λ n_ 0 +k· r ) |≤ m^ R e (λ ) r ·α_ λ,n_ 0 ,r \] where $α_ λ,n_ 0 ,r $ is a constant depending only on $λ,n_ 0 ,r$.
  Proof: If the condition at (2) is satisfied, then we make the split such that $A_L$ contains $f_L$ elements. Further, since $j≥ f_L$, $k_ f_L+1 ≤ k_ j+1 $, i.e $k_ j+1 $ is in $A_R$. So we can insert $k$ and the next keys in the batch into $A_R$. Following the insertions of $k$ and the following $B-f_L-f_R-1$ keys, $A_L$ contains $f_L$ keys, and $A_R$ contains $B-f_L+B+f_L+f_R-B=f_R$ keys. We can similarly show that this also holds if the conditions in (5) or (8) are met instead.
  Proof: First, since deferred even splitting partitions the total number of keys evenly into the minimum required number of blocks, it is a trivial invariant that at the end of each batch of insertions all blocks are at least half full. So for every batch, starting from the second, we have a total of $N≥ B 2 +r$ keys to partition into $k= N/B $ bins. The minimum number of keys in a block at the end of the batch is therefore at least $ N/k $, and our goal now is to lower bound this quantity. First, since $N≤ r+B$, we have $k≤ r B +1 $. Therefore, $N/k≥ B/2+r r/B+1 $ which implies $ N/k ≥ B/2+r r/B+1 -1$. We now proceed to show $ N/k ≥ 2 3 B-1$. First, if $k=2$, since $N>1.5B$, then $ N/k ≥ 4 3 B$. If
  bound: The minimum number of keys in a block at the end of the batch is therefore at least $ N/k $, and our goal now is to lower bound this quantity.
  bound: This means there exists some scalar $c$ such that $u^ =c· u$, therefore the limit equals: \[ c· u,w_ S c· u, 1 = u,w_ S u, 1 . \] Our goal is thus now to characterize an arbitrary eigenvector in the eigenspace corresponding to $r$ to in order to provide a lower bound for: \[ u,w_
  bound: Now observe that it suffices to lower bound the term of interest, i.e. $ Σ_ k S k· u_ k Σ_ k S u_ k $, when restricting the sums to some class in a partition of $S$, as formulated by the following lemma: Let $C_ 1 ,...,C_ $ be some partition of $S$.
  bound: According to lem:AveragePartitioning , it suffices to set such a class $C$ and provide a lower bound for $ Σ_ i=0 ^ L j_ i · u_ j_ i Σ_ i=0 ^ L u_ j_ i $.
  bound: The first lower bound is favorable to the others when $r<0.0058B$.
  bound: The second lower bound is favorable when $r<0.21B$, and the third lower bound is favorable in the rest of the regime we analyze for even splitting, i.e. $0.21B≤ r<0.5B$.

## sigmod26-303 — Effective Clustering for Large Multi-Relational Graphs  (score 41)
domain: graph-db
usage: Neumann series with Lidskii/Courant–Fischer eigenvalue arguments; Sinkhorn–Knopp scaling (Birkhoff–von Neumann) for a symmetric doubly stochastic affinity; CountSketch for scalable norm estimation.
note-quote: "If S_ω is doubly stochastic and Z=Y^T, optimizing [the relaxed objective] is equivalent to optimizing Σ_k Σ_{v_i∈C_k} ‖z_i − c^(k)‖²₂, where c^(k) is the center of cluster C_k."
  Corollary : Let $ R ^ M × m $ be a count-sketch matrix and $ ^ (r) = ^ (r) $, where $m=O(rε^ -4 log (r/εδ) · (r+log (1/εδ) ))$, $ε$ is an error threshold and $r$ is the rank of $ ^ (r) $. Then, \| ^ ^ (r) \|^2_F = (1 ε)^2· trace ( ^ ( - ^ (r) ) ) holds with a probability of at least $1-δ$.
  Theorem : If $ $ is doubly stochastic and $ = ^ $, optimizing Eq.~ [ref] is equivalent to optimizing $ _1,…, _K min Σ_ k=1 ^KΣ_ v_i _k \| _i- c ^ (k) \|^2_2 $, where $ c ^ (k) =Σ_ v_j _k _j | _k| $ stands for the center of cluster $ _k$.
  Lemma \cite{horn2012matrix: Let $ $ be a matrix whose dominant eigenvalue $λ$ satisfies $|λ|<1$. Then, $ - $ is invertible, and its inverse $( - )^ -1 $ can be expanded as a Neumann series: $( - )^ -1 =Σ_ =0 ^∞ ^ $.
  Lemma Lidskii Inequality~\cite{horn1962eigenvalues,lidskii1982spectral: Suppose $ $ is a random matrix, and let $λ( )$ denote the largest eigenvalue of $ $, For any Hermitian matrices $ $ and $ $, the following inequality holds: λ( + ) ≤ λ( ) + λ( )
  Theorem \cite{horn2012matrix: Let $ $ be a matrix whose dominant eigenvalue $λ( )$ satisfies $|λ( )|<1$. Then, the inverse $( - )^ -1 $ can be expanded as a Neumann series: $( - )^ -1 =Σ_ =0 ^∞ ^ $.
  Definition $(\ell_1,\ell_2)$-Order Maximum Eigengap: Let $λ_ i ( )$ be the $i^ th $ eigenvalue of $ $. The $( _1, _2)$-order maximum eigengap is $μ_ _1, _2 = 1 i N max | λ_ i ( ) ^ _1 - λ_ i ( ) ^ _2 | $.
  Theorem : $\| - ^ \|_F Σ_ =L+1 ^ ∞ α^ (1+α)^ +1 \| ^ - ^L \|_2·\| \|_F$, which can be upper bounded by $ ( α 1+α )^ L+1 · \| \|_F· 1 max \ μ_ L,L+ $.
  Lemma : If $ $ is required to be an $N× K$ NCI as in Eq.~ [ref] , then min_ D ( , ) max_ trace ( ^ ) .
  Proof: [ Proof of Lemma~ [ref] ]
  Proof: [ Proof of Eq.~ [ref] ] Let $s_i=\| _i\|_1$. By the definition of the DE, we can rewrite $ D ( , )$ in Eq.~ [ref] as follows: D ( , ) =& 1 2 Σ_ v_i,v_j _ i,j · \| _i / √ s_i - _j / √ s_j \|^2_2 \\ =& 1 2 Σ_ k=1 ^KΣ_ v_i,v_j _ i,j · ( _ i,k / √ s_i - _ j,k / √ s_j )^2 \\ =& 1 2 Σ_ k=1 ^KΣ_ v_i, v_j _k _ i,j | _k| · ( 1 √ s_i - 1 √ s_j ) ^2 \\ & + 1 2 Σ_ k=1 ^KΣ_ v_i _k, v_j _k _ i,j · 1 | _k|· s_i . If we assume that $s_i=s_j\ v_i,v_j $, we can derive that the minimization of $ D ( , )$ is equivalent to minimizing $$Σ_ k=1 ^KΣ_ v_i _k, v_j _k _ i,j | _k| .$$
  bound: Note that the N-cut problem has been proven to be NP-hard~ [cite] .
  bound: Particularly, SNEM-Rounding iteratively seeks an orthogonal transformation matrix $ $ that most closely maps $ $ to NCI $ $ in terms of spectral norm: [display] As such, \( trace ( ^ ) ≈ trace ( ^ ^ ) = trace ( ^ ) \), meaning that the loss \( trace ( ^ ) \) is close to the maxim
  bound: Basic Idea As remarked in Figure~ [ref] , relies on a partial eigendecomposition of the $N× N$ dense affinity matrix $ $ to approximately solve the NP-hard problem in Eq.~ [ref] , which takes $O(N^2· (d+K))$ time and is still prohibitively expensive.
  bound: As analysed in [cite] , the above objective is equivalent to finding an NCI optimizing the following trace minimization problem: [display] which is an NP-hard problem given the constraint in Eq.~ [ref] on $ $. [display] A common way is to compute an approximate solution by relaxi

## sigmod26-235 — TaCo: Subspace Collision for High-dimensional Approximate Nearest Neighbor Search  (score 23)
domain: ann-vector-search
usage: Poincaré separation (Cauchy interlacing) proves the eigensystem allocation solves the log-det problem; projection non-expansiveness plus Chernoff bound justify collision-based candidate selection in k-ANNS.
note-quote: "∥B⊤(𝑜𝑖−𝑜𝑧)∥² ≥ (1−𝜀)∥𝑜𝑖−𝑜𝑧∥² ... yields ∥B⊤(𝑜𝑖−𝑜𝑗)∥² < ∥B⊤(𝑜𝑖−𝑜𝑧)∥²."
  Lemma Local Distance Preservation for Algorithm~\ref{eigensystem_allocation: Let $ D=\ o_i\ _ i=1 ^n R ^d$ be a dataset, and let $B R ^ d × (N_s · s) $ denote the transformation returned by Algorithm~ [ref] , i.e., $B= B_1 & B_2 & … & B_ N_s $, with $B_j R ^ d × s $ and $N_s · s ≤ d$. For any point $o_i D $ and its neighbor $o_j$, assume that the local difference vector satisfies \|(I_d - B B^ )(o_i - o_j)\|^2 \, \|o_i - o_j\|^2, for some $ (0,1)$. Then, the distance after transformation satisfies The assumption of the form in [ref] has been established for random data p
  Definition Query Discriminability: Given a query $q$ and the number of subspaces $N_s$. As a consequence of the uniformity of the subspaces after the subspace-oriented data transformation in Theorem~ [ref] , we denote $p^*$ the collision probability such that subspace collision happens for a true nearest neighbor $o^* R ^*= \ o^*_1,…,o^*_k\ $, and $p$ the probability that subspace collision happens for a non-neighbor $o R ^*$, with $p^* > p$. The Discriminability Gap of $q$ is defined as $ (q) = p^* - p > 0$.
  Definition Subspace Collision: Given a dataset $ D$ of $n$ data points in $d$-dimensional space, a query $q R ^d$, and a collision ratio $α (0, 1 )$. We randomly select $s$ dimensions from all $d$ dimensions as a subspace $ R ^s$ ($s<d$). The dataset $ D$, the data point $o$, and the query point $q$ in this subspace are denoted as $ D^ $, $o^ $, and $q^ $. If a point $o D$ satisfies: $o^ $ is one of the $(α · n)$-NNs of $q^ $ in $ D^ $, we say that $o$ collides with $q$ in the subspace $ R ^s$.
  Definition SC-score: Given a dataset $ D$ of $n$ data points in $d$-dimensional space, a query $q R ^d$, $N_s$ $s$-dimensional subspaces, a collision ratio $α (0, 1 )$. Probe collisions of $q$ in $N_s$ subspaces with the collision ratio $α$. For a data point $o D$, its SC-score is the number of subspaces where it collides with $q$. Therefore, SC-score is an integer in $ [0, N_s ]$.
  Lemma SC-score Separation: For a given query $q$ and its discriminability gap $ (q)$ as in Definition~ [ref] , denote $o^*$ a true nearest neighbor of $q$ and $o R ^*$ a non-neighbor. Then, the optimal Type-I and Type-II errors both decay at least at the rate $exp ( - N_s ^2(q) 8p(1-p) )$. That is, both errors decay exponentially fast to zero as $N_s$ or $ (q)$ becomes large.
  Definition $k$-Approximate Nearest Neighbor Search, $k$-ANNS: Given a dataset $ D$ of $n$ data points in $d$-dimensional Euclidean space $ R ^d$, a query $q R ^d$, an approximation ratio $c > 1$, and an integer $k$. Let $o^*_i$ be the $i$-th exact nearest neighbor of $q$ in $ D$. A $k$-ANNS returns $k$ points $o_1,o_2,…,o_k$. For each $o_i D$ satisfying $ \|q,o_i \| ≤ c · \|q,o^*_i \|$, where $i [1,k]$.
  Definition Subspace Sampling: Given a dataset $ D$ of $n$ data points in $d$-dimensional space, we adopt a multi-round sampling strategy to obtain $N_s$ subspaces. In round $i$, a number of $s= d N_s $ dimensions are uniformly sampled without replacement to form a subspace $S_i$, $i=1,2,…,N_s-1$. For the last subspace $S_ N_s $, it simply pick up all remaining dimensions.
  Theorem Relative Neighborhood Ordering Preservation for Algorithm~\ref{data_transformation: Under the notations and conditions of Lemma~ [ref] , consider two points $o_j, o_z D $ such that \|o_i - o_j\|^2 < (1- )\,\|o_i - o_z\|^2. Then, their relative ordering with respect to $o_i$ is preserved after the transformation through $B$, in the sense that \|B^ (o_i - o_j)\| < \|B^ (o_i - o_z)\|.
  Proof: [Proof of Theorem~ [ref] ] To show that Eigensystem Allocation in Algorithm~ [ref] solves the optimization problem in [ref] , we first focus on the inner maximization problem: Denote $λ_1 > … > λ_d$ and $μ_1 ≥ … ≥ μ_s$ the eigenvalues of $ Σ$ and $B_j^ Σ B_j$, respectively (in descending order), we have, by the Poincaré separation theorem (also known as general Cauchy interlacing theorem) that λ_i ≥ μ_i ≥ λ_ d - s + i , i = 1, …, s, that is, each eigenvalue $μ_i$ of $B_j^ Σ B_j$ lies between two eigenvalues of $ Σ$. Since the logarithm function is monotonically increasing, maximizing the (log-)determinant in [ref] is equivalent to maximizing the product of eigenvalues. We formally have, by [
  Proof: [Proof of Lemma~ [ref] ] Recall from Algorithm~ [ref] that $B R ^ d × (N_s · s) $ contains eigenvectors of $ Σ$ as its columns, so that $B B^ $ is an orthogonal projection matrix. As a consequence, $o_i - o_j R ^d$ can be decomposed as the sum of the projection $B B^ (o_i - o_j)$ and the corresponding residue $(I_d - B B^ )(o_i - o_j)$, with \|o_i - o_j\|^2 = \|B^ (o_i - o_j)\|^2 + \|(I_d - B B^ )(o_i - o_j)\|^2. Rearranging~ [ref] gives \|B^ (o_i - o_j)\|^2 = \|o_i - o_j\|^2 - \|(I_d - B B^ )(o_i - o_j)\|^2. By~~ [ref] , the second term on the right-hand side is bounded by $ \|o_i - o_j\|^2$, which yields the lower bound in [ref] . The upper bound follows directly from the non-expansiveness
  bound: In this paper, we address these limitations from two aspects: first, we design a subspace-oriented data transformation mechanism by averaging the entropies computed over each subspace of the transformed data, which ensures balanced subspace partitioning (in an information theoret
  bound: A $k$-NNS returns $k$ points $ R=\ o_1,o_2,…,o_k\ $, satisfying $ R= B$. [$k$-Approximate Nearest Neighbor Search, $k$-ANNS] Given a dataset $ D$ of $n$ data points in $d$-dimensional Euclidean space $ R ^d$, a query $q R ^d$, an approximation ratio $c > 1$, and an integer $k$.
  bound: In practice, many $k$-ANNS implementations do not explicitly rely on an approximation ratio $c$ during query processing; instead, they constrain the quality of the results through evaluation metrics~ [cite] .
  bound: As a consequence, $o_i - o_j R ^d$ can be decomposed as the sum of the projection $B B^ (o_i - o_j)$ and the corresponding residue $(I_d - B B^ )(o_i - o_j)$, with [display] Rearranging~ [ref] gives [display] By~~ [ref] , the second term on the right-hand side is bounded by $ \|o
  bound: The upper bound follows directly from the non-expansiveness of orthogonal projection.

## sigmod26-190 — PJsim: Towards Precise and Scalable Graph Similarity  (score 18)
domain: graph-db
usage: Kronecker-sum eigenvalue analysis, Neumann expansion, and real Schur decomposition replace direct O(n^6) vectorized inversion.
note-quote: "The vectorized PJsim similarity matrix has the direct closed-form solution: Vec(𝑆^PJ) = 2(1−𝑐)(𝑋⊕𝑋)^{−1}Vec(𝐼𝑛)"
  Theorem 3: 1 (PJsim’s Vectorized Lyapunov Eqation). The PJsim’s Equation 3 can be equivalently expressed in vectorized form as: (𝑋 ⊕ 𝑋 ) Vec(𝑆 PJ ) = 2 (1 − 𝑐) Vec(𝐼𝑛 ) (4) Proc. ACM Manag. Data, Vol. 4, No. 3 (SIGMOD), Article 223. Publication date: June 2026. 223:8 Prajjwal Nijhara, Jainan Tandel, Rohit Prajapati, and Dip Sankar Banerjee
  Corollary 3.1: 1 (PJsim Direct Closed-form Solution). The vectorized PJsim similarity matrix has the direct closed-form solution: Vec(𝑆 PJ ) = 2 (1 − 𝑐) (𝑋 ⊕ 𝑋 ) −1 Vec(𝐼𝑛 ) (10) We establish the validity of our PJsim formulation by proving that the required matrix inverse exists.
  Proof: We apply the 𝑉 𝑒𝑐 operator to both sides of our PJsim Equation 3 and use the property that Vec(𝐴𝐵𝐶) = (𝐶𝑇 ⊗ 𝐴)Vec(𝐵). We have: Vec(𝑋𝑆 PJ + 𝑆 PJ𝑋 𝑇 ) = Vec(2(1 − 𝑐)𝐼𝑛 ) 𝑇 (5) Vec(𝑋𝑆 PJ ) + Vec(𝑆 PJ𝑋 ) = 2(1 − 𝑐)Vec(𝐼𝑛 ) (6) (𝐼𝑛 ⊗ 𝑋 )Vec(𝑆 PJ ) + (𝑋 ⊗ 𝐼𝑛 )Vec(𝑆 PJ ) = 2(1 − 𝑐)Vec(𝐼𝑛 ) (7) [(𝐼𝑛 ⊗ 𝑋 ) + (𝑋 ⊗ 𝐼𝑛 )]Vec(𝑆 PJ ) = 2(1 − 𝑐)Vec(𝐼𝑛 ) (8) (𝑋 ⊕ 𝑋 )Vec(𝑆 PJ ) = 2(1 − 𝑐)Vec(𝐼𝑛 ) (9) where we used the definition of the Kronecker sum: 𝐴 ⊕ 𝐵 = (𝐴 ⊗ 𝐼 ) + (𝐼 ⊗ 𝐵). □
  Proof: We analyze the eigenvalues of 𝑋 and subsequently those of 𝑋 ⊕ 𝑋 for our PJsim method. The eigenvalues of 𝑋 = 𝐼𝑛 − 𝑐𝑄 lie in the range [1 − 𝑐, 1], since the eigenvalues of a backward transition matrix 𝑄 are bounded by [0, 1], and we have 𝜆𝑖 (𝑋 ) = 1 − 𝑐𝜆𝑖 (𝑄). The eigenvalues of the Kronecker sum 𝑋 ⊕ 𝑋 are given by {𝜆𝑖 (𝑋 ) + 𝜆 𝑗 (𝑋 ) : 𝑖, 𝑗 = 1, . . . , 𝑛}, which are the pairwise sums of eigenvalues of 𝑋 . Therefore, the eigenvalues of 𝑋 ⊕ 𝑋 lie in the range [2(1 − 𝑐), 2]. Since 𝑐 ∈ (0, 1), we have 2(1 − 𝑐) > 0, ensuring that all eigenvalues of 𝑋 ⊕ 𝑋 are strictly positive. Hence, 𝑋 ⊕ 𝑋 is invertible, validating that the solution of Equation 10 exists. □

## sigmod26-101 — Fair Data Pre-Processing with Imperfect Attribute Space  (score 11)
domain: fairness
usage: Conditioning on each X₄=j slice yields per-slice latent-class models; tensor-decomposition identifiability plus the shared mixing distribution forces one consistent global permutation; θ₄ read from observed conditionals.
note-quote: "the local parameters 𝜃𝵢 of each 𝑋𝵢 are generically identifiable up to a global permutation of the latent states."
  Theorem : Consider a latent causal model with one latent attribute $X_0$ and $d ≥ 4$ observed attributes. The observed attributes are partitioned into four nonempty groups $X_1, X_2, X_3, X_4$, such that conditional on $(X_0,X_4)$ the groups $X_1, X_2, X_3$ are mutually independent, and moreover $X_0 X_4$. The latent attribute $X_0$ takes $ 2$ states with strictly positive mixture weights. For each group $X_i$ with $i \ 1,2,3,4\ $, define its group cardinality as \[ _i := Π_ j X_i k_j, \] where $k_j$ is t
  Proposition \cite{salimi2019interventional,zheng2025causalpre: Let $ D $ be a database instance and $ G $ its attribute graph over the attribute set $ V $. Under attribute specification, $ V $ is fully partitioned into five disjoint subsets: $ S $ (sensitive), $ I $ (inadmissible), $ A $ (admissible), $ W $ (additional), and $ Y $ (label). Here $ W $ refers to additional attributes that are neither sensitive nor causally relevant to fairness. If, in $ G $, every edge directed toward the label attribute in $ Y $ originates exclusively from attributes in $ A 
  Definition Generic Identifiability up to Label Swapping~\cite{allman2009identifiability: Let $ $ be the set of all permutations of the state labels of $ L $, and for $ $, let $ (θ)$ denote the parameter vector obtained by applying $ $ to the state labels of $ L $ in all relevant CPTs. A latent causal model is said to be generically identifiable up to label swapping if there exists a subset $Ω Θ$ of Lebesgue measure zero such that \[ θ, θ' Θ Ω, P [ V ; θ] = P [ V ; θ'] \ \ such that θ' = (θ). \] That is, for almost all parameter values, the observed distribution determines the parame
  Definition Identifiability~\cite{allman2009identifiability: Consider a latent causal model parameterized by $θ Θ$, where $Θ$ is the parameter space. Let $ P [ V , L ;θ]$ denote the joint distribution induced by $θ$, and let $ P [ V ;θ]$ denote the corresponding observed distribution obtained by marginalizing out $ L $. The model is said to be identifiable if \[ θ, θ' Θ, P [ V ; θ] = P [ V ; θ'] \ \ θ = θ'. \] Equivalently, the mapping $θ P [ V ;θ]$ is injective, so the parameters are uniquely determined by the observed distribution.
  Definition $\mathcal{K}$-fairness~\cite{salimi2019interventional: For a subset $ K X S $, we say that $ M $ is $ K $-fair with respect to the sensitive set $ S $ if, for every assignment $ K = $, intervening on $ S $ does not affect the distribution of the outcome once $ K $ is fixed: & P [ O = o do ( S = $ S $ _0), do ( K = $ K $ ) ] \\ = \;& P [ O = o do ( S = $ S $ _1), do ( K = $ K $ ) ].
  Definition Generic Identifiability~\cite{allman2009identifiability: A latent causal model is said to be generically identifiable if there exists a subset $Ω Θ$ of Lebesgue measure zero such that \[ θ, θ' Θ Ω, P [ V ; θ] = P [ V ; θ'] \ \ θ = θ'. \] Equivalently, the mapping $θ P [ V ;θ]$ is injective almost everywhere, except on a measure-zero subset of $Θ$.
  Definition Justifiable Fairness~\cite{salimi2019interventional: Let $ A X $ denote the collection of admissible attributes , i.e., features that are permitted to influence $O$ even if they are themselves affected by sensitive attributes. A classifier $ M $ is said to satisfy justifiable fairness if it is $ K $-fair for every $ K $ with $ A K X $.
  Corollary \cite{salimi2019interventional: Let $ G $ be the attribute graph derived from a dataset. If all causal pathways from sensitive attributes to the label pass through at least one admissible attribute, then any reasonable classifier trained on this dataset is regarded as justifiably fair.
  Proof: Let $θ_i$ denote the local parameter at $X_i$ for $i \ 0,1,2,3,4\ $. We aim to show that the local parameters $θ_i$ of each $X_i$ are generically identifiable up to a global permutation of the latent states. Fix any value $j \ 1,…, _4\ $ of $X_4$. Conditioning on $X_4=j$, we obtain a latent class model with three observed attribute groups $(X_1, X_2, X_3)$ and one latent variable $X_0$. For $i=0,1,2,3$, denote these conditioned attributes or groups by $X_i^ (j) $ and let $θ_i^ (j) $ denote their associated local parameters. By assumption on the group cardinalities, at least two of $ _1, _2, _3$ are not smaller than $ $. Without loss of generality, assume $ _1, _2 ≥ $. Then, for each conditio
  bound: To empirically validate this property, we test whether the learned $L$ exhibits any residual dependence on $ S $ from both information-theoretic and statistical perspectives, using Normalized Mutual Information (NMI) and the chi-square test.
  bound: In addition to identifiability, Theorem~ [ref] provides a practical upper bound on the number of latent states.
  bound: To address this, we introduce an auxiliary distribution $ P [L V , θ ]$ with fixed parameter $ θ $ and apply Jensen's inequality to derive a tractable lower bound: [display] where $H[·]$ denotes entropy.
  bound: Since the second term is constant with respect to $θ$, maximizing the lower bound reduces to [display] where [display] The validity and convergence of this reformulation follow the standard analysis in~ [cite] .

## sigmod26-265 — Accelerating High-Dimensional ANN Search via Skipping Redundant Distance Computations  (score 11)
domain: ann-vector-search
usage: JL distortion bounds turn distance thresholds into cheap projected-space comparisons; orthogonal-transform invariance enables multiplication-free iterative refinement.
note-quote: "If δ(p,q) < τ, then Pr(‖A(p−q)‖²₂ < (1+ε)d/D·τ) > p1; if δ(p,q) > τ, then Pr(‖A(p−q)‖²₂ < (1+ε)d/D·τ) < p2."
  Definition 2: 1 (k-Nearest Neighbor Search). Given a set of points 𝑃 with each points p ∈ R𝐷 , a query point q ∈ R𝐷 , and distance function 𝛿 (p, q), find a set 𝐾 ⊂ 𝑃 with 𝑘 points, where for any point p ∈ 𝐷 \ 𝐾, 𝛿 (p, q) ≥ maxp∈𝐾 𝛿 (p, q). Proc. ACM Manag. Data, Vol. 3, No. 6 (SIGMOD), Article 288. Publication date: December 2025. Accelerating High-Dimensional ANN Search via Skipping Redundant Distance Computations 288:5 Approximate nearest neighbor search allows for retrieval of approximate, rather than exa
  Lemma 2: 2 (distributional JL lemma [42, 45]). Let 𝜖, 𝛽 ∈ (0, 1), there exists a probability  distribution 𝐹 over linear functions 𝑓 : R𝐷 → R𝑑 , where 𝑑 = Θ 𝜖 −2 log 𝛿1 , such that for any fixed x ∈ R𝐷 ,   Pr ∥𝑓 (x) ∥ 22 − ∥x∥ 22 ≤ 𝜖 ∥x∥ 22 ≥ 1 − 𝛽. (2) 𝑓 ∼𝐹 Let S be a random d-dimensional subspace, let xi′ be the projection of xi ∈ 𝑃 into S, we set 𝜌 = ∥xi′ − xj′ ∥ 22 to be the square 𝑙 2 norm, and 𝜇 = (𝑑/𝐷) ∥xi − xj ∥ 22 , then we have the following inequality [21]:   𝑑 (𝜖 2 /2 − 𝜖 3 /3) . (3) Pr 
  Theorem 2.5: For any two points x and y, we have the following properties [36]: • 𝑀𝑆𝐿(x′, y′, 𝑚) monotonically increases with 𝑚. • 𝛿 (x, y) ≥ 𝑀𝑆𝐿(x′, y′, 𝑚) for all 0 ≤ 𝑚 ≤ 𝐷. • 𝛿 (x, y) == 𝑀𝑆𝐿(x′, y′, 𝑚) when 𝑚 = 𝐷. Those properties enable us to use the MS-distance as a lower bound for the Euclidean distance. We can gradually increase dimensions to obtain a better lower bound until exact distance is calculated. 2.4 DCO and Lower Bound Pruning Distance comparison operation (DCO) [29] is a key step in the sea
  Theorem 4.1: Given a point p ∈ 𝑃 from the dataset, a query point q, and a parameter 𝜖 ∈ (0, 1). Let A ∈ R𝑑 ×𝐷 be the first 𝑑 rows of a random orthogonal matrix A𝐷 ×𝐷 , and let 𝜏 represent the farthest square distance in the ef-queue. The dimensions 𝑑 and 𝐷 (with 𝑑 ≤ 𝐷) correspond to the projected and original spaces, respectively. Furthermore, let 𝑝 1, 𝑝 2 ∈ (0, 1). We then have: • If 𝛿 (p, q) < 𝜏, then Pr(∥A(p − q) ∥ 22 < (1 + 𝜖)𝑑/𝐷 · 𝜏) > 𝑝 1 . • If 𝛿 (p, q) > 𝜏, then Pr(∥A(p − q) ∥ 22 < (1 + 𝜖)𝑑/𝐷 · 𝜏) < 
  Theorem 5.1: Let x′, y′ be the (𝐷+2)-dimensional vector transformed according to MS-transformation ′ in Definition 2.3. We apply PCA to subvector xsub = [𝑥 1′ , 𝑥 2′ , . . . , 𝑥 𝐷′ ] to get x′′ = [𝜇𝑥 , 𝜎𝑥 , 𝑥 1′′, 𝑥 2′′, . . . , 𝑥 𝐷′′ ]. (7) We do the same for y′ to get y′′ . The MS-distance 𝑀𝑆𝐿(x′′, y′′, 𝑚) can still serve as a lower bound and every properties of Theorem 2.5 still hold. ′′ −
  Theorem 5: 1 shows that after applying PCA to the MS transformed data, MSPCA can also be used for pruning in the search process by iteratively refining to obtain a tighter lower bound until the exact distance is obtained. 5.1.3 Improving the iteration efficiency. Our adaptive lower bound pruning method iteratively refines the lower bound by computing the distance in subspace. The inclusion of 𝜎𝑥 𝜎 𝑦 in second component of MS-distance introduces per-iteration multiplication overhead, potentially affecting p
  Proof: According to distributional JL lemma, the projected result follows a probability distribution. Let Pr(∥A(p − q) ∥ 22 < (1 + 𝜖)𝑑/𝐷 · 𝑟 ) = 𝐹 = 𝑝, where 𝐹 (·) is the cumulative distribution function. If 𝑟 = 𝛿 (p, q) < 𝜏, we have Pr(∥A(p − q) ∥ 22 < (1 + 𝜖)𝑑/𝐷 · 𝜏) > Pr(∥A(p − q) ∥ 22 < (1 + 𝜖)𝑑/𝐷 · 𝑟 ) = 𝑝 = 𝑝 1 Similarly, if 𝑟 = 𝛿 (p, q) > 𝜏, we have Pr(∥A(p − q) ∥ 22 < (1 + 𝜖)𝑑/𝐷 · 𝜏) < Pr(∥A(p − q) ∥ 22 < (1 + 𝜖)𝑑/𝐷 · 𝑟 ) = 𝑝 = 𝑝 2 . □
  Proof: The transform matrix A ∈ R𝐷 ×𝐷 from the PCA is orthogonal matrix. Let v′ = xsub ′′ , v′′ = x′′ − y′′ we have ysub sub sub ∥v′′ ∥ 22 = (Av′ ) T (Av′ ) = v′ T v′ = ∥v′ ∥ 22, (8) ′′ , y′′ ) increases monotoniwhich means distance is the same. When 𝑚 ′ < 𝑚, the function 𝛿𝑚 ′ (xsub sub ′ ′′ ′′ ′ ′ cally with 𝑚 , its maximum value being 𝛿𝑚 (xsub, ysub ) = 𝛿𝑚 (xsub, ysub ). □
