{"id": "sigmod26-264", "title": "A Unifying Algorithm for Hierarchical Queries", "from": "tex", "n_stmt": 30, "n_proof": 9, "kinds": {"proposition": 3, "definition": 13, "claim": 4, "theorem": 6, "lemma": 2, "corollary": 2}, "counts": {"np_hard": 0, "lower_bound": 0, "upper_bound": 1, "big_o": 18, "omega": 0, "theta": 0, "approx_ratio": 5, "whp": 0, "regret": 0, "dp": 0, "invariant": 4, "convergence": 0, "competitive": 0, "worst_case": 2, "sketch": 0, "cost_model": 0, "cardinality": 0, "learned": 0}, "statements": [{"kind": "proposition", "num": "Elimination Procedure for Hierarchical Queries", "text": "A $Q$ is hierarchical if and only if the following elimination procedure transforms $Q$ into a query with a single nullary atom, i.e., $Q() R()$. Repeatedly, apply either one of the following two rules, until neither rule applies: ; (Rule 1) Eliminate a ``private'' variable $Y$: Find a variable $Y (Q)$ that occurs in only one atom $R( X)$ in $Q$, and replace $R( X)$ with $R'( X \\ Y\\ )$ where $R'$ is a new relation symbol. ; (Rule 2) Eliminate a ``duplicate atom'': Find two distinct atoms $R_1( X)$ and $R_2( X)$ that share the same set of variables $ X$, and replace both atoms with a single atom $R'( X)$ where $R'$ is a new relation symbol. Note that we are not adding two copies of the new atom $R'( X)$ but only a single copy. Hence, the resulting query is still self-join-free. Moreover, the above two rules maintain the hierarchical property. In particular, after each application of Rule 1 or Rule 2, the resulting query is hierarchical if and only if the original query was hierarchical."}, {"kind": "definition", "num": "Provenance Tree", "text": "Let $Σ$ be a set of symbols. A provenance tree is a rooted tree whose leafs are labeled with symbols from $Σ \\ , \\ $ and whose internal nodes are labeled with either $ $ or $ $. A provenance tree is called decomposable if its leaves have distinct labels, including the labels $ $ and $ $. The labels $ $ and $ $ do not need to appear in any trees except for the two trivial trees: the tree that consists of a single node which is $ $, and the tree that consists of a single $ $ node. This is because whenever $ $ appears under a conjunction, it can be dropped, and whenever it appears under a disjunction, the entire disjunction becomes $ $. Similar simplifications apply for $ $, and these simplifications can always eliminate $ $ and $ $ from any node in a given tree, except for the root node. Given a provenance tree $x$, the support of $x$, denoted $ (x)$, is the set of all symbols that appear in the leaves of $x$, excluding $ $ and $ $."}, {"kind": "definition", "num": "Provenance \\twomonoid", "text": "Let $Σ$ be a set of symbols. The provenance is a $ K = ( K , , )$ defined as follows. ; The domain $ K $ is the set of all provenance trees over $Σ \\ , \\ $. ; The $ $ operator takes two provenance trees $x$ and $y$ and returns a new provenance tree $z$ that has a root labeled with $ $ and whose children are the roots of $x$ and $y$. ; The $ $ operator takes two provenance trees $x$ and $y$ and returns a new provenance tree $z$ that has a root labeled with $ $ and whose children are the roots of $x$ and $y$. In order to ensure commutativity of $ $ and $ $, we treat the children of any node as an unordered set. In order to ensure the associativity of $ $ and $ $, whenever we have a node that has the same label as its parent, we merge them into a single node. The identity for $ $ is the provenance tree with a single leaf labeled with $ $, and the identity for $ $ is the provenance tree with a single leaf labeled with $ $."}, {"kind": "definition", "num": "\\bagsetmax", "text": "The problem is an optimization problem that is parameterized by a self-join-free Boolean conjunctive query $Q$, which is interpreted under bag-set semantics ~ [cite] . The input to the problem is a tuple $( , , θ)$ where: ; $ $ is a set database instance, which is the input instance that we want to repair. ; $ $ is another set database instance, called the repair database , which contains the facts that we are allowed to add to $ $ in order to repair $ $. ; $θ$ is a natural number representing an upper bound on the number of facts we are allowed to add to $ $ in order to repair $ $. The target is to find the maximum value of $Q( ')$ under bag-set semantics over all set database instances $ '$ that are valid repairs of $ $, i.e., that satisfy: ; $ ' $ and $ ' ≤ θ$. In words, a valid repair $ '$ results from $ $ by adding at most $θ$ facts from $ $. We call the quantity $ ' $ the repair cost and denote it $ ( , ')$."}, {"kind": "theorem", "num": "Algorithm~\\ref{alg:hierarchical} is correct", "text": "Let $Q$ be a hierarchical and $ $ be a database instance where every fact is annotated with a unique symbol from a set of symbols $Σ$. Let $ K = ( K , , )$ be the provenance from Definition~ [ref] where $ K $ is the set of provenance trees over $Σ \\ , \\ $, and let $ 0 $ and $ 1 $ be the identities. Let $ K = (K, , )$ be another with identities $ 0$ and $ 1$. Suppose we are given a function $ : K K$ that maps provenance trees to values in $K$ and satisfies the following properties, for all $x$ and $y$ in $ K $ that are decomposable and have disjoint supports: (x y) &= (x) (y) \\\\ (x y) &= (x) (y) \\\\ ( 0 ) &= 0 Let $ $ be the $ K$-annotated database instance obtained by replacing every $ K $-annotation in $ $ with its $ $-mapping , i.e. its corresponding value in $K$ using $ $. Then, the output of Algorithm~ [ref] on $Q$ and $ $ is the $ $-mapping of the output of Algorithm~ [ref] on $Q$ and $ $."}, {"kind": "definition", "num": "Shapley value of a fact with respect to a query and a database", "text": "Let $Q$ be a Boolean Conjunctive Query and $ $ be a database instance. The instance $ $ is divided into two parts: ; Exogenous part , denoted $ $. These are facts that are fixed. ; Endogenous part , denoted $ $. These are facts that can be added or deleted from $ $. Suppose that we pick a permutation of the facts in $ $ uniformly at random, and we begin to insert those facts one by one into $ $. The Shapley value of a given fact $f$ with respect to $Q$ and $ $ is the probability of adding the fact $f$ flipping the answer of $Q$ from $ $ to $ $. Formally, let $ ( )$ denote the set of all permutations of facts in $ $. Given a permutation $σ ( )$ and a fact $f$ in $ $, let $σ_ <f $ denote the set of facts that appear before $f$ in $σ$. Given a fact $f$ in $ $, the Shapley value of $f$ with respect to $Q$ and $ $ is defined as: _ Q, , (f) 1 | |! Σ_ σ ( ) (Q( σ_ <f \\ f\\ ) - Q( σ_ <f ) )"}, {"kind": "definition", "num": "\\twomonoid for computing $\\sat_{Q, \\Dexo, \\Dendo}(k)$", "text": "We define a specific $ K = (K, , )$ as follows. Let $ \\ , \\ $. The domain $K$ is $ ^ × $, i.e., the set of all vectors of natural numbers that are indexed by pairs of natural numbers and Boolean values. The $ $ and $ $ operations are defined as follows for any pair of vectors $ x_1, x_2 K$: ( x_1 red x_2)(i, b) & Σ_ i_1, i_2 : i_1 + i_2 = i Σ_ b_1, b_2 : b_1 red b_2 = b x_1(i_1, b_1) × x_2(i_2, b_2), & i , b \\\\ ( x_1 red x_2)(i, b) & Σ_ i_1, i_2 : i_1 + i_2 = i Σ_ b_1, b_2 : b_1 red b_2 = b x_1(i_1, b_1) × x_2(i_2, b_2), & i , b The identities $ 0$ and $ 1$ for $ $ and $ $ are as follows: 0(i, b) 1 & if i = 0 and b = \\\\ 0 & otherwise 1(i, b) 1 & if i = 0 and b = \\\\ 0 & otherwise"}, {"kind": "definition", "num": "Size of a $\\bm K$-annotated database", "text": "Let $ K=(K, , )$ be a where $ $ and $ $ have identities $ 0$ and $ 1$ respectively. Given a $ K$-annotated relation $R( X)$, the support of $R( X)$, denoted $ (R( X))$, is the set of all facts $R( x)$ in $R( X)$ whose $ K$-annotation is not $ 0$. The size of $R( X)$ is the size of its support, i.e., $ (R( X)) $. The support of a $ K$-annotated database instance $ $, denoted $ ( )$, is the union of the supports of its relations. The size of a $ K$-annotated database instance $ $, denoted $ $, is the sum of the sizes of its relations."}, {"kind": "theorem", "num": "", "text": "Let $ K = (K, , )$ be the from Definition~ [ref] , $Q$ be a hierarchical , $( , , θ)$ be an input instance to from Definition~ [ref] , and $ ( , )$ be the $ K$-annotated database instance from Definition~ [ref] . Then using $Q$ and $ ( , )$ as inputs, Algorithm~ [ref] runs in time $O(( + )· ^2)$ and space $O(( + )· )$ and outputs a vector $q K$ that satisfies the following: ; For every $i $, the $i$-the entry, $q(i)$, is the maximum multiplicity of $Q$ that can be achieved within a repair cost at most $i$."}, {"kind": "definition", "num": "\\twomonoid", "text": "A triple $ K = (K, , )$ is a iff it satisfies the following: ; $(K, )$ is a commutative monoid A monoid $(K, )$ is a pair where $K$ is a set and $ $ is an associative binary operator over the elements of $K$ that has an identity element $ 0$, i.e., an element $ 0 K$ where $a 0 = 0 a=a$, for all $a K$. A monoid $(K, )$ is commutative if $ $ is commutative. with identity element $ 0$. ; $(K, )$ is a commutative monoid with identity element $ 1$. ; The identity element, $ 0$, of $(K, )$ satisfies $ 0 0 = 0$."}, {"kind": "lemma", "num": "Algorithm~\\ref{alg:hierarchical} produces {\\em decomposable} provenance trees", "text": "Given a hierarchical $Q$ and a database instance $ $, suppose that we annotate every fact in $ $ with a unique symbol from a set of symbols $Σ$. Consider the provenance $ K = ( K , , )$ from Definition~ [ref] where $ K $ is the set of provenance trees over $Σ \\ , \\ $. The output of Algorithm~ [ref] on $Q$ and $ $ is a provenance tree that is decomposable (Definition~ [ref] ). Moreover, every $ K $-annotation that is computed throughout the algorithm is also a provenance tree that is decomposable ."}, {"kind": "definition", "num": "Input $\\bm K$-annotated database instance for \\bagsetmax", "text": "Let $ K = (K, , )$ be the from Definition~ [ref] , $Q$ be a $ $, and $( , , θ)$ be an input instance to the problem from Definition~ [ref] . We define a function $ $ that maps a fact $R( x)$ where $R( X) (Q)$ and $ x ^ X $ to a monotonic vector in $K$ as follows: (R( x)) 1 & if $R( x) $ \\\\ & if $R( x) $ and $R( x) $ \\\\ 0 & otherwise Moreover, we define $ ( , )$ as a $ K$-annotated database instance where for every $R( X) (Q)$ and $ x ^ X $, the fact $R( x)$ is annotated with $ (R( x))$."}, {"kind": "definition", "num": "Input $\\bm K$-annotated database instance for computing $\\sat_{Q, \\Dexo, \\Dendo}(k)$", "text": "Let $ K = (K, , )$ be the from Definition~ [ref] , $Q$ be a and $ $ and $ $ be two database instances for $Q$. We define a function $ $ that maps a fact $R( x)$ where $R( X) (Q)$ and $ x ^ X $ to a vector in $K$ as follows: (R( x)) 1 & if $R( x) $ \\\\ & if $R( x) $ and $R( x) $ \\\\ 0 & otherwise Moreover, we define $ ( , )$ as a $ K$-annotated database instance where for every $R( X) (Q)$ and $ x ^ X $, the fact $R( x)$ is annotated with $ (R( x))$."}, {"kind": "definition", "num": "\\twomonoid for \\bagsetmax", "text": "We define a specific $ K = (K, , )$ as follows. The domain $K$ is the set of all vectors of natural numbers $ ^ $ that are monotonic , i.e., $K \\ x ^ i < j, x(i) ≤ x(j) \\ $. For every pair of vectors $ x_1, x_2 K$, the $ $ and $ $ operators are defined using Eq.~ [ref] and~ [ref] respectively. The identities $ 0$ and $ 1$ for $ $ and $ $ are the all-zeros and all-ones vectors respectively, i.e., $ 0(i) 0$ and $ 1(i) 1$ for all $i $."}, {"kind": "theorem", "num": "", "text": "Let $ K = (K, , )$ be the from Definition~ [ref] , $Q$ be a hierarchical , $ $ and $ $ be two database instances for $Q$, and $ ( , )$ be the $ K$-annotated database instance from Definition~ [ref] . Then using $Q$ and $ ( , )$ as inputs, Algorithm~ [ref] runs in time in $O(( + )· ^2)$ and space $O(( + )· )$ and outputs a vector $q K$ that satisfies the following: q(i, ) = _ Q, , (i),& & i"}, {"kind": "theorem", "num": "", "text": "Let $ K = (K, , )$ be the from Definition~ [ref] . Let $Q$ be a hierarchical , and $ $ be a tuple-independent probabilistic database, viewed as a $ K$-annotated database instance $ $, where each fact is annotated with its probability. Then using $Q$ and $ $ as inputs, Algorithm~ [ref] runs in time $O( )$ and outputs the probability of $Q$ evaluating to true over all possible worlds."}, {"kind": "definition", "num": "\\bagsetmaxdecision", "text": "The problem is the decision version of . It is parameterized by a self-join-free Boolean conjunctive query $Q$, and has the same inputs as and one additional input which is a natural number $ N $. The target is to decide whether the maximum value of $Q( ')$ under bag-set semantics over all valid repairs $ '$ of $ $ is at least $ $."}, {"kind": "proposition", "num": "\\cite{10.1145/2395116.2395119", "text": "A connected $Q$ is hierarchical if and only if there exists a rooted tree $T$ whose set of nodes is exactly $ (Q)$ that satisfies the following: For every atom $R( X) (Q)$, there exists a node $Y (Q)$ where the set of variables along the path from $Y$ to the root of $T$ (including $Y$ and the root) is exactly $ X$."}, {"kind": "definition", "num": "\\twomonoid for {\\sc Probabilistic Query Evaluation", "text": "We define a specific $ K = (K, , )$ where the domain $K$ is the set of all probabilities, i.e. $K [0, 1]$, and the $ $ and $ $ are defined using Eq.~ [ref] and~ [ref] respectively, for every $p_1, p_2 [0, 1]$. The identities for $ $ and $ $ are $ 0 0$ and $ 1 1$ respectively."}, {"kind": "definition", "num": "$\\sat_{Q, \\Dexo, \\Dendo}(k)$", "text": "Let $Q$ be a Boolean Conjunctive Query, and $ $ be a database instance that is divided into an exogenous part $ $ and an endogenous part $ $. Given a number $k $, we define $ _ Q, , (k)$ as the number of possible subsets $ ' $ of size $| '| = k$ such that $Q( ')$ is true."}, {"kind": "theorem", "num": "Algorithm~\\ref{alg:hierarchical} used a linear number of $\\oplus$ and $\\otimes$ operations", "text": "Let $ K = (K, , )$ be a , $Q$ be a , and $ $ be a $ K$-annotated database instance of size $ $. On inputs $Q$ and $ $, the total number of $ $ and $ $ operations performed by Algorithm~ [ref] is $O( )$."}, {"kind": "claim", "num": "", "text": "Given a hierarchical $Q$ that does not have the form $Q() R()$, either Rule 1 or Rule 2 must be applicable to $Q$, and the resulting query must be hierarchical."}, {"kind": "claim", "num": "", "text": "Every annotation of a fact that is produced by the first run is the $ $-mapping of the annotation of the same fact that is produced by the second run."}, {"kind": "claim", "num": "Invariant of Algorithm~\\ref{alg:hierarchical", "text": "Every fact is annotated with a provenance tree that is decomposable. Moreover, all facts have provenance trees whose supports are pairwise disjoint."}, {"kind": "corollary", "num": "", "text": "For every non-hierarchical $Q$, the parameterized problem for $Q$ (with parameters $θ$ and $ $ or with just parameter $ $) is $ $-hard."}, {"kind": "claim", "num": "", "text": "Given a non-hierarchical $Q$, if either Rule 1 or Rule 2 is applicable to $Q$, then the resulting query must be non-hierarchical."}, {"kind": "lemma", "num": "", "text": "Throughout Algorithm~ [ref] , the size of the $ K$-annotated database instance $ $ never increases."}, {"kind": "corollary", "num": "", "text": "For every non-hierarchical $Q$, the problem for $Q$ does not have an unless $ = $."}, {"kind": "theorem", "num": "", "text": "For any non-hierarchical $Q$, the problem for $Q$ is $ $-complete."}, {"kind": "proposition", "num": "", "text": "For any $Q$, the problem is in $ $."}], "proofs": [{"text": "Membership in $ $ follows from Proposition~ [ref] . We now prove $ $-hardness. Let $Q$ be an arbitrary non-hierarchical . Since $Q$ is not hierarchical, $Q$ must have the form: \\[ Q() R(A, X ), S(A,B, Y ), T(B, Z ), P_1( D _1), , P_n( D _n), \\] where $A, B$ are distinct variables and $ X $, $ Y $, $ Z $, $ D _1, , D _n$ are sets of variables, $A Z $ and $B X $, and $n≥ 0$. We give a polynomial-time reduction from to for $Q$. Let $(G, k)$ be an input instance to where $G=(V,E)$ is an undirected graph without self-loops and $k$ is a positive integer. We construct an input instance $( , , θ, )$ for the problem for $Q$ as follows: Let $ $ be the set of vertices $V$, and fix an arbitrary vertex $a V$. Let $ W (Q)$ and $ $ be the set of all tuples $ w ^ W $ such that $ w(X) = a$ for all $X W \\ A,B \\ $. The database instance $ = \\ R^ ,S^ ,T^ ,P_1^ , ,P_n^ \\ $ is defined as follows: R^ &= T^ = ,\\\\ S^ &= \\ S( w(A), w(B), w( Y )) w ~ and ~ \\ w(A), w(B)\\ E \\ ,\\\\ P_i^ &= \\ P_i( w( D_i)) w ~ and ~ \\ w(A), w(B)\\ E \\ , i [n]. In contrast, the repair database $ =\\ R^ ,S^ ,T^ , P_1 ^ , , P_n ^ \\ $ is defined as follows: R^ =\\ R( w(A), w( X )) w \\ , T^ = \\ T( w(B), w( Z )) w \\ , S^ = P_1^ = = P_n^ = . In other words, we encode the edge relation into $S^ $, and we permit repairs $ '$ of $ $ to be obtained by adding $R$ or $T$ facts where $A$ and $B$ are assigned to arbitrary vertices in $V$, while all other variables must be assigned to the fixed vertex $a$. Finally, we set $θ = 2k$ and $ = k^2$, thus specifying the input instance $( , , θ, )$ for . We claim that the following two statements are equivalent: ; $(G, k)$ is a ``yes'' instance of , i.e., $G$ has a complete bipartite subgraph in which each of the two parts has size $k$. ; There is a database $ '$ such that $ ' $, $ ( , ') ≤ 2k$, and $Q( ') ≥ k^2$. Proving $(1) (2)$: If $G$ has a complete balanced bipartite subgraph with parts $U_1$ and $U_2$, each of size $k$, then let $ ' = \\ R^ ' ,S^ ' ,T^ ' ,P_1^ ' , ,P_n^ ' \\ $ be the set database in which $S^ ' = S^ $, $P_i^ ' = P_i^ $ for each $i [n]$, and R^ ' &= \\ R( w(A), w( X )) w ~ and ~ w(A) U_1\\ , \\\\ T^ ' &= \\ T( w(B), w( Z )) w ~ and ~ w(B) U_2\\ . Then, $ ( , ')= U_1 + U_2 = 2k$. Let $ '$ denote the set of tuples $ w $ such that $ w(A) U_1$ and $ w(B) U_2$. Note that $ '$ contains exactly $k^2$ tuples, each of which represents a satisfying assignment for $Q$ in $ '$. Hence $Q( ') ≥ k^2$. Proving $(2) (1)$: Assume that $ '=\\ R^ ' ,S^ ' ,T^ ' ,P_1^ ' , ,P_n^ ' \\ $ is  …"}, {"text": "[Proof of Proposition~ [ref] ] To prove Proposition~ [ref] , we will prove the following claims: Given a hierarchical $Q$ that does not have the form $Q() R()$, either Rule 1 or Rule 2 must be applicable to $Q$, and the resulting query must be hierarchical. Given a non-hierarchical $Q$, if either Rule 1 or Rule 2 is applicable to $Q$, then the resulting query must be non-hierarchical. [Proof of Claim~ [ref] ] Claim~ [ref] follows from Proposition~ [ref] . In particular, consider a hierarchical $Q$ that does not have the form $Q() R()$. If $ (Q) = $, then all connected components of $Q$ must have the form $Q_i() R_i()$, in which case we can apply Rule 2 on them. On the other hand, if $ (Q) ≠ $, let $Q_i$ be a connected component of $Q$ that has at least one variable. Let $T_i$ be the tree that satisfies Proposition~ [ref] , let $Y (Q)$ be any leaf in the tree, and let $ X$ be the set of variables from $Y$ to the root of $T_i$. We recognize two cases: ; If $Q_i$ contains more than one atom whose variables are $ X$, then we can apply Rule 2 on them. ; Otherwise, let $R( X)$ be the unique atom in $Q_i$ with a variable set $ X$. In this case, $R( X)$ must be the only atom in $Q_i$ that contains $Y$, in which case we can apply Rule 1. In both cases above, the resulting component $Q_i$ still has a tree $T_i'$ satisfying Proposition~ [ref] , hence the resulting $Q$ is still hierarchical. This proves Claim~ [ref] ."}, {"text": "[Proof of Claim~ [ref] ] In order to prove Claim~ [ref] , consider a non-hierarchical $Q$. By definition of hierarchical queries, $Q$ must contain two distinct variables $A, B (Q)$ and three distinct atoms $R( X), S( Y), T( Z) (Q)$ such that: ; $A$ is in $ X$ and $ Y$ but not in $ Z$, and ; $B$ is in $ Y$ and $ Z$ but not in $ X$. Suppose that Rule 1 is applicable to $Q$. Note that the private variable that we eliminate in Rule 1 cannot be either $A$ or $B$ because each one of them is shared by at least two atoms. Hence after applying Rule 1, we can still use the same two variables $A$ and $B$ along with the atoms $R( X), S( Y), T( Z)$ (one of which might be the one containing the private variable) to show that the resulting query is still non-hierarchical. Now suppose that Rule 2 is applicable to $Q$. Note that the two duplicate atoms from Rule 2 that share the same set of variables cannot both belong to the set of atoms $\\ R( X), S( Y), T( Z)\\ $ because none of these three atoms share the same set of variables. Hence, even after applying Rule 2, we will still be left with three distinct atoms with variable sets $ X, Y, Z$. We can still use these atoms along with the two variables $A$ and $B$ to show that the resulting query is still non-hierarchical. This proves Claim~ [ref] ."}, {"text": "In order to prove the above lemma, we inductively prove that the algorithm maintains the following invariant. Recall the notion of support of a provenance tree from Definition~ [ref] . [Invariant of Algorithm~ [ref] ] Every fact is annotated with a provenance tree that is decomposable. Moreover, all facts have provenance trees whose supports are pairwise disjoint. Initially, the above claim holds because every fact is annotated with a provenance tree consisting of a single leaf labeled with a unique symbol from $Σ$. We now prove that applying both Rule 1 and Rule 2 maintains the above invariant. For Rule 1 (line~ [ref] of Algorithm~ [ref] ), every fact $R'( x')$ for $ x' ^ X' $ is annotated with a provenance tree which is the $ $ of provenance trees of all facts $R( x', y)$ for $y $. If all these provenance trees are decomposable and have disjoint supports, then the resulting provenance tree is also decomposable and has a support that is the union of the supports of all the children. Moreover, notice that the provenance tree of any fact $R( x', y)$ is only used in the provenance tree of a single fact $R'( x')$. Therefore, after we replace the atom $R( X)$ with $R'( X')$ (thus replacing all facts $R( x', y)$ with new facts $R'( x')$), the resulting facts will still have annotations whose supports are pairwise disjoint. For Rule 2 (line~ [ref] of Algorithm~ [ref] ), every fact $R'( x)$ for $ x ^ X $ is annotated with a provenance tree which is the $ $ of provenance trees of the facts $R_1( x)$ and $R_2( x)$. If these two provenance trees are decomposable and have disjoint supports, then the resulting provenance tree is also decomposable and has a support that is the union of their supports. Moreover, every fact $R_1( x)$ or $R_2( x)$ is only used in the provenance tree of a single fact $R'( x)$. Therefore, after we replace the two atoms $R_1( X)$ and $R_2( X)$ with $R'( X)$ (thus replacing all facts $R_1( x)$ and $R_2( x)$ with new facts $R'( x)$), the resulting facts will still have annotations whose supports are pairwise disjoint."}, {"text": "To prove the above theorem, we consider two simultaneous runs of Algorithm~ [ref] . The first run is on $Q$ and $ $, while the second is on $Q$ and $ $. We show that they maintain the invariant: Every annotation of a fact that is produced by the first run is the $ $-mapping of the annotation of the same fact that is produced by the second run. The above claim initially holds by the construction of $ $. Thanks to Lemma~ [ref] , all provenance trees that are produced by the second run are decomposable. Given two provenance trees $x$ and $y$, if $x y$ (or $x $ y) is decomposable, then $x$ and $y$ are also decomposable and have disjoint supports. Hence, both Eq.~ [ref] and Eq.~ [ref] hold, thus they inductively prove the invariant."}, {"text": "Algorithm~ [ref] updates the $ K$-annotated database instance $ $ in two places, namely lines~ [ref] and~ [ref] . We show below that neither update increases the size of $ $. ; In line~ [ref] , we replace the atom $R( X', Y)$ with $R'( X')$. We argue that the following holds: $ (R'( X'))$ $ $ $ _ X' (R( X', Y))$. This is because for any given $ x' ^ X' $, we have $R'( x') _ y R( x', y)$ and $ 0$ is the identity for $ $. ; In line~ [ref] , we replace the two atoms $R_1( X)$ and $R_2( X)$ with a single atom $R'( X)$. We argue that $ (R'( X)) (R_1( X)) (R_2( X))$, which implies $ (R'( X)) ≤ (R_1( X)) + (R_2( X)) $. This holds because for any given $ x ^ X $, we have $R'( x) = R_1( x) R_2( X)$, and a must satisfy $ 0 0 = 0$ (Definition~ [ref] )."}, {"text": "[Proof of Theorem~ [ref] ] First, we prove correctness using Theorem~ [ref] . We define the function $ : K K$ from Theorem~ [ref] as follows: Given a provenance tree $x$, $ (x)$ is the probability of the Boolean formula $F_x$ corresponding to the provenance tree $x$ evaluating to true. Given two decomposable provenance trees $x$ and $y$ with disjoint supports, consider the Boolean formulas $F_x$ and $F_y$ corresponding to $x$ and $y$ respectively. Because the supports are disjoint, the two formulas evaluating to true are independent events. Let $p_1$ and $p_2$ be the probabilities of $F_x$ and $F_y$, respectively, evaluating to true. By independence, the probability of $F_x F_y$ is given by $p_1 p_2$ from Eq.~ [ref] , while the probability of $F_x F_y$ is given by $p_1 p_2$ from Eq.~ [ref] . Therefore, our definition of $ $ satisfies Eq.~ [ref] and Eq.~ [ref] , and Theorem~ [ref] applies. By the theorem, the output of Algorithm~ [ref] on $Q$ and $ $ is the probability of the Boolean formula corresponding to the provenance tree of $Q$ evaluating to true, as desired. Finally, the linear runtime follows from Theorem~ [ref] and the fact that the $ $ and $ $ operators from Eq.~ [ref] and Eq.~ [ref] take constant time."}, {"text": "[Proof of Theorem~ [ref] ] First, we prove correctness using Theorem~ [ref] . We define the function $ $ from Theorem~ [ref] as follows: Given a provenance tree $x$, let $F_x$ be the corresponding Boolean formula. We define $ (x)$ as a monotonic vector in $ ^ $ where for every $i $, the $i$-th entry is the maximum multiplicity of $F_x$ that can be achieved with a repair cost at most $i$, i.e., by adding no more than $i$ facts from the repair database $ $. Given two decomposable provenance trees $x$ and $y$ with disjoint supports, consider the Boolean formulas $F_x$ and $F_y$ corresponding to $x$ and $y$ respectively. Because the supports are disjoints, these two formulas have to be repaired independently. In particular, to repair the formula $F_x F_y$ within a repair cost of $i$, we need to find the best way to repair $F_x$ within a repair cost of $i_1$ and $F_y$ within a repair cost of $i_2$ where $i_1 + i_2 = i$. Therefore, $ (x y) = (x) (y)$ where $ $ is given by Eq.~ [ref] . The same reasoning goes for repairing the formula $F_x F_y$, leading to $ (x y) = (x) (y)$ where $ $ is given by Eq.~ [ref] . Therefore, our definition of $ $ satisfies Eq.~ [ref] and Eq.~ [ref] , and Theorem~ [ref] applies. The theorem implies that the output of Algorithm~ [ref] on $Q$ and $ $ is a monotonic vector in $ ^ $ whose $i$-th entry is the maximum multiplicity for $Q$ that can be achieved with a repair cost at most $i$, as desired. Finally, we use Theorem~ [ref] to prove that the time complexity is $O(( + )· ^2)$ and the space complexity is $O(( + )· )$ . Given an input instance $( , , θ)$ to the problem from Definition~ [ref] , note that the maximum repair cost $θ$ is always upper bounded by the size of the repair database $ $. Let $ K = (K, , )$ be the from Definition~ [ref] , and $ x_1$ and $ x_2$ be two vectors in $K$. Note that we only need to maintain the first $θ + 1$ entries of $ x_1$ and $ x_2$. Hence, the sizes of these vectors can be assumed to be linear in $θ$, and by extension, in $| |$. As a result, the $ $ and $ $ operators from Eq.~ [ref] and Eq.~ [ref] take time $O( ^2)$ and space $O( )$. By Theorem~ [ref] , the total number of $ $ and $ $ operations performed by Algorithm~ [ref] is $O( ( , ) ) = O( + )$, where $ ( , )$ is the $ K$-annotated database instance from Definition~ [ref] . This gives the desired complexity."}, {"text": "[Proof of Theorem~ [ref] ] First, we prove correctness. In order to apply Theorem~ [ref] , we define the function $ $ as follows. For convenience, we extend Definition~ [ref] to define $ _ Q, , (k, b)$ (with an extra Boolean parameter $b $) as the number of possible subsets $ ' $ of size $| '| = k$ such that $Q( ')$ is $b$. This way, we can think of $ _ Q, , (k, b)$ as a vector in $ ^ × $ that is indexed by $(k, b) × $. In addition, we also extend Definition~ [ref] from Boolean conjunctive queries $Q$ to arbitrary Boolean formulas $F$. Given a provenance tree $x$, let $F_x$ be the corresponding Boolean formula, and $ [F_x]$ be the set of facts in $ $ that appear in $F_x$. We define $ (x)$ as follows: (x) _ F_x, , [F_x] Let $x$ and $y$ be two decomposable provenance trees with disjoint supports, $F_x$ and $F_y$ be the corresponding Boolean formulas, and $ [F_x]$ and $ [F_y]$ be the sets of facts in $ $ that appear in $F_x$ and $F_y$ respectively. Note that $ [F_x]$ and $ [F_y]$ are disjoint, and $ [F_x F_y]$ is their union. As a result we have: _ F_x F_y, , [F_x F_y] (k, b) =&\\\\ Σ_ k_1, k_2 : k_1 + k_2 = k &Σ_ b_1, b_2 : b_1 b_2 = b _ F_x, , [F_x] (k_1, b_1) × _ F_y, , [F_y] (k_2, b_2)\\\\ _ F_x F_y, , [F_x F_y] =& _ F_x, , [F_x] _ F_y, , [F_y] \\\\ (x y) =& (x) (y) where the $ $ operator above is given by Eq.~ [ref] . Using a similar reasoning for $F_x F_y$, we have: _ F_x F_y, , [F_x F_y] (k, b) =&\\\\ Σ_ k_1, k_2 : k_1 + k_2 = k &Σ_ b_1, b_2 : b_1 b_2 = b _ F_x, , [F_x] (k_1, b_1) × _ F_y, , [F_y] (k_2, b_2)\\\\ _ F_x F_y, , [F_x F_y] =& _ F_x, , [F_x] _ F_y, , [F_y] \\\\ (x y) =& (x) (y) Hence, Theorem~ [ref] applies. Finally, we prove that the time complexity is $O(( + )· ^2)$ and the space complexity is $O(( + )· )$. Note that by the definition of $ _ Q, , (k, b)$, the value of $k$ cannot exceed $ $. Hence, the $ $ and $ $ operators from Eq.~ [ref] and Eq.~ [ref] run in time $O( ^2)$ and space $O( )$. By Theorem~ [ref] , the total number of $ $ and $ $ operations performed is $O( ( , ) ) = O( + )$, where $ ( , )$ is the $ K$-annotated database instance from Definition~ [ref] ."}], "bound_sents": ["The input to the problem is a tuple $( , , θ)$ where: ; $ $ is a set database instance, which is the input instance that we want to repair. ; $ $ is another set database instance, called the repair database , which contains the facts that we are allowed to add to $ $ in order to repair $ $. ; $θ$ is …"], "flag": true, "score": 49, "n_chars": 110032}