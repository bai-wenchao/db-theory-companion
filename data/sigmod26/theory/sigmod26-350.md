# sigmod26-350 — Query Answering Under Volume-Based Diversity Functions

from=tex flag=True score=53 stmts=11 proofs=20 chars=138867
kinds: {"proposition": 1, "definition": 4, "theorem": 1, "lemma": 5}
counts: {"np_hard": 1, "lower_bound": 0, "upper_bound": 0, "big_o": 25, "omega": 0, "theta": 0, "approx_ratio": 38, "whp": 0, "regret": 0, "dp": 0, "invariant": 0, "convergence": 0, "competitive": 0, "worst_case": 0, "sketch": 4, "cost_model": 0, "cardinality": 1, "learned": 0}

## Statements
**Definition \cite{deep2025ranked.** Let $(T, )$ be a tree decomposition of a full CQ $Q$. We say that a ranking function is compatible with $(T, )$ if for every node $t$ it is $( (T_t) key (t))$-decomposable conditioned on $ key (t)$ Recall that $ (T_t)$ are all the variables that are in the bag of $v$ or a decedent of $v$ and $ key (t)$ are the variables in the bag of $t$ that it shares with its parent (or $ $ if $t$ is the root). .

**Definition \cite{deep2025ranked.** Let $ $ be a ranking function ($ T _ \ R\ R $) over $R( )$-tuples and $ $. In the following, $g$ is a arbitrary homomorphism over $ $. We say that $ $ is $ $-decomposable if there exists a total order $ $ for all homomorphisms over $ $, such that for any two homomorphisms $h,h'$ over $ $ we have: $$ h h' g, (R((h g)( ))) = (R((h' g)( ))) \\ or \\ g, (R((h g)( ))) > (R((h' g)( ))) $$

**Theorem \cite{deep2025ranked.** Let $(T, )$ be a tree decomposition of the full CQ $Q( )$ and $ $ a ranking function for $Q$-tuples that is compatible with $(T, )$. Then, enumerating $ Q (D)$ in the order induced by $ $ is possible with $O(|D|^ fhw(T, ) )$ preprocessing time and $O(log|D|)$ delay in data complexity. In particular, a solution maximizing $ $ can be found in time $O(|D|^ fhw(T, ) )$.

**Definition .** Let $ = ( , μ, β)$ be a volume assignment, $R( )$ be an atom over $Σ$ with variables $ $, and $ y x $. We say that $β$ is $ y $-decomposable (w.r.t.\ $R$) if for every pair of homomorphisms $h, h'$ over $ y $ and homomorphisms $g,g'$ over $ x y $ we have: β(R((h g)( x ))) β(R((h' g)( x ))) = β(R((h g')( x ))) β(R((h' g')( x ))).

**Lemma .** Let $Q( )$ be a full CQ and let $ =( , μ, β)$ be a volume assignment over $ $ such that $β$ is $ y $-decomposable conditioned on $ z $ (w.r.t.\ $Q$). Then, the ranking function for $Q$-tuples $ _ ,S Q (D) R , Q(h( x )) δ_ (S \ Q(h( x ))\ ))$ is $ y $-decomposable conditioned on $ z $ for every $S Q (D)$.

**Definition \cite{deep2025ranked.** Let $ $ be a ranking function over $R( )$-tuples, and $ , $ such that $ = $. We say that $rank$ is $ $-decomposable conditioned on $ $ if for every homomorphism $f$ over $ $, the ranking function $rank_ f (R_ ( h ( ))) := rank(R(( f h )( )))$ defined over $R_ ( )$-tuples is $ $-decomposable.

**Lemma .** Let $(T, )$ be a tree decomposition of the full CQ $Q( )$ and let $ =( , μ, β)$ be a volume assignment over $ $ such that $β$ is compatible with $(T, )$. Then, the ranking function $ _ ,S Q (D) R , Q(h( x )) δ_ (S \ Q(h( x ))\ ))$ is compatible with $(T, )$ for every $S Q (D)$.

**Lemma .** Let $ =( , μ, β)$ be a volume assignment over $ $ such that $β$ is $ y $-decomposable conditioned on $ z $ (w.r.t.\ $R( )$). Then, then ranking function $ T _ \ R\ R , R( f ( )) μ(β(R( f ( ))))$ is $ y $-decomposable conditioned on $ z $.

**Lemma .** Let $β$ be a ball function this is $ y $-decomposable conditioned on $ z $ (w.r.t.\ $R( )$). Then, $β_ S (R( x )):=β(R( x )) S$ is $ y $-decomposable conditioned on $ z $ (w.r.t.\ $R( )$) for every $S $.

**Lemma .** Let $ $ be a ranking function over $R$-tuples that is $ $-decomposable conditioned on $ $. Then, for any constant $c$, $ + c$ is also $ $-decomposable conditioned on $ $.

**Proposition .** Let $ $ be any volume assignment over a universe $ $ of possible solutions. Then $δ_ $ is always monotone and submodular.

## Proofs
**Proof.** [Proof sketch] For given multi-attribute diversity function $v_λ$, defining an equivalent volume-based diversity function $v_ $ is straightforward. More precisely, we set $ =( , μ, β)$ with $ = 2^ 2^X $, $β (x) = \ A X x A\ $, and $μ( B ) = Σ_ A B λ_A $ for $ B $. The other direction is more involved and only works for finite universe $ $. In particular, we set $λ_A = μ ( _ a A β(a) _ x X A β(x) )$.

**Proof.** In~ [cite] , Nemhauser, Wolsey, and Fisher showed that for every monotone submodular set function $f: ( ) $ and $k≥ 1$ one can compute in polynomial time a $k$-subset~$A$ of $ $ such that $f(A) ≥ (1- 1 e ) · max_ B : |B| = k f(B)$. Since $δ_ $ is submodular and monotone and $Q$ is fixed, one can compute the set $ Q (D)$ in polynomial time over $D$ and then apply the result in~ [cite] to retrieve a $(1- 1 e )$-approximation of $δ_ $ over $ Q (D)$ restricted to subsets of~size~$k$. In Algorithm~ [ref] , we depict this procedure for $δ_ $ which follows a greedy strategy: starting from $S = $; in every iteration it finds a tuple $t Q (D)$ that maximizes the marginal diversity of $δ_ $, namely, $δ_ (S \ t\ ) δ_ (S)$. After the $k$-th iteration, it outputs $S$. By~ [cite] , this procedure achieves a $(1- 1 e )$-approximation of $ [Σ, , Q]$ for every database $D$ and $k ≥ 1$, and runs in polynomial time.

**Proof.** [Proof sketch] The proof is by encoding the maximum coverage problem into a volume assignment $ $. It is well-known that the maximum coverage problem is hard to approximate beyond $(1- 1 e )$-approximation ratio, unless $ = $~ [cite] .

**Proof.** [Proof sketch] We illustrate the basic idea by proving $ $-hardness of the apparently simplest case $ = $. The proof is by reduction from (the directed version of) Hamiltonian path: Given an instance $G = (V(G), E(G))$ of Hamiltonian path, we define an instance $(D,Q,S)$ of $ [Σ, ]$ as follows: database $D$ consists of a single binary relation $E$ storing the edges of $G$, we set $ S= $, and, for $n = |V(G)|$, we define the ACQ $Q$ as follows: \[ Q(x_1, ,x_ n ) \ \ E(x_1,x_2), ,E(x_ n-1 ,x_ n ). \] Notice that a solution $Q(h( x )) Q (D)$ corresponds to a walk in $G$ and $δ_ $ applied to singletons (i.e., $δ_ (\ Q(h( x ))\ )$) counts the number of distinct vertices used in the corresponding walk. Hence, $G$ is a positive instance of Hamiltonian path if, and only if, the solution to this instance of $ [Σ, ]$ yields an answer $Q(h( x ))$ with $δ_ (\ Q(h( x ))\ ) = n$.

**Proof.** [Proof sketch] We explain why $ [Σ, ]$ is tractable for ACQs $Q( )$. To that end, let $D$ be a database, and $h_1, h_k$ homomorphisms from $Q$ to $D$, i.e., $S=\ Q(h_1( )), ,$ $ Q(h_k( ))\ Q (D)$. Consider the marginal diversity for a new solution $Q(h( )) Q (D)$: \[ δ_ (S \ Q(h( ))\ )-δ_ (S) \ = \ Σ_ x α_ x , with α_ x = 1 & if i h(x)≠ h_i(x),\\ 0 & if i h(x) = h_i(x).\\ \] That is, we count the number of new values. We can cast this then as a sum-product query over the tropical semi-ring $ R _ max :=( R \ ∞\ , +,max)$. Doing so shows that we can find the element that maximizes the marginal diversity in linear time. To do so, for every $x $ let us choose a covering relation $R^x:=R_i( _i)$ used in $Q$ where $x _i$. Then, we can define the $ R _ max $-relations $R^*_1, ,R^*_m$. That is, for tuple $R_j( )$ in the database, we add tuples $R^*_j( )$ to the database and annotate it with the number of new values $ $ adds at positions $x$ such that $R_j$ covers $x$. Then, as every variable $x $ is covered by exactly one relation, we have: δ_ (S \ Q(h( ))\ )-δ_ (S) = Σ_ i R_i^*(h( _i)) for homomorphism $h$ such that $Q(h( )) Q (D)$. Then, due to results on sum-product queries~ [cite] we can find a $Q(h( )) Q (D)$ maximizing Equation [ref] in time $O(|Q|· |D|)$ as this is then a scalar sum-product query. This solves $ [Σ, ]$ for ACQs. Then, due to Theorem [ref] , we can compute a $(1- 1 e )$-approximation of $ [Σ, ]$ in time $O(k·|Q|· |D|)$.

**Proof.** [Proof Sketch of Theorem~ [ref] ] Theorem [ref] cannot directly be applied since $Q$ is not necessarily a full CQ. To that end, let us consider the full CQ $Q^ ( )$ defined as the subquery of $Q$ where all body relations are projected onto $ $. Then, $(T_ , |_ V(T_ ) )$ is a tree decomposition of $Q^ $ and $fhw(T_ , |_ V(T_ ) )≤ fhw(T, )$. Now, we extend $ $ to $Q^ $-tuples via $ (Q^ (h( ))):= (Q(h( )))$. Defined as such, $ $ is compatible with $(T_ , |_ V(T_ ) )$ w.r.t.\ $Q^ $ as $Q$ and, hence, also $Q^ $ are self-join-free. Then, to compute $ rank _ , S $, we have to keep track of the which-provenance [cite] for each of the tuples in the bags of $v V(T_ )$ for what happens ``outside'' of $V(T_ )$. Thus, essentially, for each $v V(T_ )$, we have to look at its children in $T T_ $, i.e., $C:=child(v) V(T_ )$ and consider the sub-query $Q^v( (v))$ that uses the variables $ (v)$, and the ones that appear in $C$ and their descendants. Computing the provenance of these queries requires time $|D|^ fhw(T, )+1 $ (where the $+1$ is to account for the semi-ring operations)~ [cite] . However, then, to compute marginals of $δ_ $ (essentially $δ_ (S \ s\ )$), it suffices to add together the provenance of every tuple $t S \ s\ $. The provenance of a tuple $t$ can be computed by looking-up and adding together the provenance of $t$ projected to $ (v)$ in $Q^v$. Thus, as $S$ can be considered fixed, this takes $O(|D|)$ time.

**Proof.** [Proof of submodularity.] Consider an arbitrary volume assignment $ =( , μ, β)$ over some universe $ $. Let $S_1, S_2 $ and $a $ such that $S_1 S_2$. We have to show that the following condition holds: δ_ (S_1 \ a\ ) - δ_ (S_1) \ ≥ \ δ_ (S_2 \ a\ ) - δ_ (S_2). W.l.o.g., let us assume that $a S_2$ since, otherwise, $S_2 \ a\ = S_2$ and the inequality [ref] holds trivially. Given that $δ_ $ is a volume diversity function over $ $, we have that: δ_ (S_1 \ a\ ) \ &= \ μ( _ x S_1 \ a\ β(x))\\ &= \ μ( _ x S_1 β(x) β(a))\\ &= \ μ( _ x S_1 β(x) (β(a) _ x S_1 β(x)))\\ &= \ μ( _ x S_1 β(x)) + μ(β(a) _ x S_1 β(x)). Then, given that $δ_ (S_1) = μ( _ x S_1 β(x))$, we conclude that: δ_ (S_1 \ a\ ) - δ_ (S_1)\ = \ μ(β(a) _ x S_1 β(x)). In the same way, we conclude that: δ_ (S_2 \ a\ ) - δ_ (S_2)\ = \ μ(β(a) _ y S_2 β(y)). We are assuming $S_1 S_2$. Hence, we have $ _ x S_1 β(x) _ y S_2 β(y)$, from which we conclude that: β(a) _ y S_2 β(y) \ \ β(a) _ x S_1 β(x). Given that $μ$ is a measure, we conclude that: μ(β(a) _ y S_2 β(y)) \ ≤ \ μ(β(a) _ x S_1 β(x)). Then, combining this property with [ref] and [ref] , we conclude that [ref] holds.

**Proof.** First, consider a diversity function $v_λ$ over some finite set $X$ according to the multi-attribute approach of [cite] , i.e., $λ$ is a non-negative measure on $2^ 2^X $. We define a volume-based diversity function $δ_ $ with the following volume assignment $ =( , μ, β)$: ; $ = 2^ 2^X $; i.e., sets of subsets of $X$. ; $ β X R _ ≥ 0 $ with $β (x) = \ A x A\ $ ; for $ B $, we set $μ( B ) = Σ_ A B λ_A $. We verify that the resulting volume-based diversity measure $δ_ $ coincides with $v_λ$. To this end, let $S X$. Then $v_λ(S) = Σ_ A X: A S ≠ λ_A$. On the other hand, $δ_ (S) = μ ( _ x S \ A x A\ )$ and $ _ x S \ A x A\ = \ A X x S with x A\ = \ A X A S ≠ \ $. By our definition of $μ$, we thus get $δ_ (S) = μ(\ A X A S ≠ \ ) = Σ_ A X: A S ≠ λ_A = v_λ(S)$. For the other direction, suppose that we are given a volume-based diversity function $δ_ $ with $ =( , μ, β)$. From this, we define $λ$ as follows: $$λ_A = μ ( _ a A β(a) _ x X A β(x) )$$ Intuitively, we define the ``contribution'' of $A$ to the overall diversity as the volume shared by all balls contributing to $A$ minus those parts of the $σ$-algebra which are covered by the balls corresponding to the complement of $A$. It remains to show that $v_λ(S) = δ_ (S)$ holds for every $S X$. Consider an arbitrary subset $S X$. Then the following chain of equalities holds: δ_ (S) &= μ( _ s S β(s) )\\ & = μ ( _ A B = X\\ A B = ( (( _ s S β(s)) ( _ a A β (a)) ) ( _ b B β (b)) ) )\\ & = Σ_ A B = X\\ A B = μ ( (( _ s S β(s)) ( _ a A β (a)) ) ( _ b B β (b)) )\\ & = Σ_ A B = X\\ A B = μ ( (( _ s S B β(s)) ( _ a A β (a)) ) ( _ b B β (b)) )\\ & = Σ_ A B = X\\ A B = μ ( (( _ a A β (a)) ) ( _ b B β (b)) ) & S B ≠ \\ μ ( (( ) ( _ a A β (a)) ) ( _ b B β (b)) ) & S B = \\ & = Σ_ A B = X\\ A B = \\ A S ≠ μ ( (( _ a A β (a)) ) ( _ b B β (b)) )\\ & = Σ_ A B = X\\ A B = \\ A S ≠ λ_A\\ & = Σ_ A X A S ≠ λ_A\\ & = v_λ(S) The correctness of the above equalities is seen as follows: First, to see that $$( _ s S β(s)) = _ A B = X\\ A B = ( (( _ s S β(s)) ( _ a A β (a)) ) ( _ b B β (b)) )$$ holds, consider an arbitrary element $e ( _ s S β(s))$ in the set on the left-hand side. To show that it is also contained in the set on the right-hand side of the equality, we define the set $A_e X$ as $A_e = \ a X e β(a)\ $. By this definition, we clearly have $e ( _ a A_e β (a)) $. Moreover, for $B_e = X A_e$, we have that, for all $b B_e$, $e β(b)$ holds. Hence, $e ( _ b B_e β (b))$. Since we started with the assumption that $e ( _ s S β(s))$ holds …

**Proof.** Simply consider the tuples $$t_1 = R(a,b,c,d,e), t_2 = R(a,b,f,g,h),t_3=R(x,y,i,j,k),t_4=(x,y,l,m,n)$$ with Hamming distances $d(t_1,t_2) = d(t_3,t_4) = 3$ and $d(t_1,t_3)=d(t_1,t_4) = d(t_2,t_3)=d(t_2,t_4)=5$. Then, &(\ t_1,t_2,t_3,t_4\ )- (\ t_2,t_3,t_4\ )= 2 (d(t_1,t_2) + d(t_1,t_3) + d(t_1,t_4)) =26 \\ &> (\ t_1,t_2,t_3\ )- (\ t_2,t_3\ )= 2(d(t_1,t_2) + d(t_1,t_3)) =16, and &(\ t_1,t_2,t_3,t_4\ )- (\ t_2,t_3,t_4\ )= 3-3 = 0 \\ &> (\ t_1,t_2,t_3\ )- (\ t_2,t_3\ )= 3-5 = -2. Thus, both violate submodularity. Furthermore, (\ t_2,t_3\ ) = 5 > (\ t_1,t_2,t_3,t_4\ ) = 3. Thus, $ $ vioates monotonicity.

**Proof.** We can show this using $ $. To that end, note that $ $ is oblivious to constant names. Then, consider the sets of triples S_1&=\ t_1 = R(a,b,x), t_2 = R(a,y,c), t_3 = R(z,b,c)\ \\ S_2&=\ t_4 = R(a,b,x), t_5 = R(a,c,y), t_6 = R(a,d,z)\ \\ S_3&=\ t_7 = R(b,a,x), t_8 = R(c,a,y), t_9 = R(d,a,z)\ \\ S_4&=\ t_ 10 = R(b,x,a), t_ 11 = R(c,y,a), t_ 12 = R(d,z,a)\ . Then, $δ_ (S_1)=6$ while $δ_ (S_2) = δ_ (S_3) = δ_ (S_4) = 7$. Now let $d, agg $ be as required and assume $δ_ agg ,d (S_2) = δ_ agg ,d (S_3) = δ_ agg ,d (S_4)$. Then, observe that due to $d$ being oblivious to the names of constants. d(t_ 1 ,t_ 2 )&=d(t_4,t_5) = d(t_4,t_6) = d(t_5,t_6),\\ d(t_1,t_3)&=d(t_7,t_8)=d(t_7,t_9)=d(t_8,t_9),\\ d(t_2,t_3)&=d(t_ 10 ,t_ 11 )=d(t_ 10 ,t_ 12 )=d(t_ 11 ,t_ 12 ) W.l.o.g., $$d(t_ 1 ,t_ 2 )≤ d(t_1,t_3) ≤ d(t_2,t_3)$$ Thus, due to monotonicity $$ δ_ agg ,d (S_2)≤ δ_ agg ,d (S_1)≤ δ_ agg ,d (S_4)$$ Thus, $ δ_ agg ,d (S_1)= δ_ agg ,d (S_2)$ and, e.g., $ agg ,d$ cannot distinguish $S_1$ from $S_2$

**Proof.** Consider the universe $ = \ a,b,c,d\ $ with the distance function $d$ shown in Table~ [ref] . [h!] |c|c|c|c|c| & $a$ & $b$ & $c$ & $d$ \\ $a$ & 0 & 2 & 2 & 2 \\ $b$ & 2 & 0 & 1 & 1 \\ $c$ & 2 & 1 & 0 & 2 \\ $d$ & 2 & 1 & 2 & 0 \\ Distance function $d$ in the proof of Theorem [ref] It is easy to verify that $d$ is actually a metric, i.e., it satisfies the following properties: [label=( )] ; non-negativity: $d(x,y) ≥ 0 $ for every pair $x,y $; ; symmetry: $d(x,y) = d(y,x)$ for any two elements $x,y $; ; $d(x,x) = 0$ for every $x $; ; $d$ satisfies the triangle inequality, i.e., $d(x,z) ≤ d(x,y) + d(y,z)$ for any three (pairwise distinct) elements $x,y,z $. This clearly holds, since we have $d(x,y) + d(y,z) ≥ 2$ and $d(x,z) ≤ 2$ for any three pairwise distinct elements $x,y,z $. ; $d(x,y) = 0$ implies $x = y$ for all $x,y $. If Weitzman's diversity measure $ $ is defined via this distance function $d$, it is easy to veriy that the following equalities hold: $ (\ a,b\ ) = 2$, $ (\ a,b,c\ ) = 3$, $ (\ a,b,d\ ) = 3$, and $ (\ a,b,c,d\ ) = 5$. We can now can show that there exist sets $S_1,S_2 $, such that $ (S_1 S_2) + (S_1 S_2) ≥ (S_1) + (S_2).$ by setting $S_1 = \ a,b,c\ $ and $S_2 = \ a,b,d\ $. Then we have: $ (\ a,b,c,d\ ) + (\ a,b\ )= 5 + 2 = 7$ and $ (\ a,b,c\ ) + (\ a,b,d\ )= 3+3 = 6$ \\ That is, this particular diversity measure $ $ violates submodularity.

**Proof.** First, recall that an ultrametric is a distance function satisfying the conditions (1), (2), (3), and (5) of a metric recalled in Section [ref] plus (4') the strong triangle inequality: $d(a,c) ≤ max ( d(a,b), d(a,c) )$. In case of an ultrametric on $ $, the elements of $ $ can be arranged at the leaf nodes of a hierarchical (= ``taxonometric'') tree $T$, where $T$ is obtained as follows: With each inner node of $T$, we can associate the radius of the ball that contains all elements at descendants of this node. In particular, with the root node of $T$, we associate the radius $r$ of (the ball containing all of) $ $, i.e., $r = max \ d(a,b) | a,b \ $. Moreover, the length of the edges can be chosen in such a way that $d(a,b)$ for two distinct elements $a,b $ is equal to the path length from each of these nodes to their nearest common ancestor. Note that then the path from the root to any leaf node has length $r$. Now, if the distance function is an ultrametric, then the Weitzman diversity $δ_W(S)$ of a subset $S $ can be defined as follows: let $T'$ be the smallest subtree of the hierarchical tree $T$ that contains the root of $T$ and all leaf nodes corresponding to the elements in $S$. Moreover, let $r$ denote the radius of $ $. Then $δ_W(S)$ is equal to the sum of the edge-lengths in $T'$ minus $r$. We now define a volume assignment $ = ( , μ, B)$ by defining $ $ as the set of edges in the hierarchical tree $T$, $β$ maps every element $j$ of $ $ to the set of edges of the path from the root to the node corresponding to $j$ in $T$, and $μ(S)$ is defined as the sum of the lengths of the edges in $S$. Then, for every non-empty subset $S $, the resulting volume-based diversity function $δ_ $ satisfies $δ_ (S) = δ_W(S) + r$, where $r$ is the radius of $ $.

**Proof.** We provide a reduction for the problems individually. However, we always use the $ $ problem as the basis for the reductions. To that end, recall that in ~ [cite] , it was shown that the problem remains $ $-hard when restricted to $3$-regular graph. That is, we consider instances $(V,E,k)$ where $G=(V,E)$ is a graph where every vertex $v V$ has exactly 3 incident edges and $k N $. Then, $(V,E,k)$ is a yes-instance iff there exists a set of pairwise non-adjacent vertices $I V$ such that $|I|=k$. We acknowledge that the provided reductions are similar to those given in [cite] . Reduction for $ $: For the reduction, let us consider a database $D$ encoding the incident relation of the undirected graph $G=(V,E)$. That is, for every $u V$ and its 3 incident edges $uv_1,uv_2,uv_3 E$ ($v_i≠ v_j$ for $i≠ j$), the database contains the tuple $Inc(u, uv_1,uv_2,uv_3)$. However, to only include one atom per vertex, let us consider an arbitrary order $ $ over $V$. Then, we define D := \ Inc(u,uv_1,uv_2,uv_3) uv_1,uv_2,uv_3 E and v_1 v_2 v_3\ Furthermore, consider the query $Q$ defined independent of $G$: Q(x,y_1,y_2,y_3) Inc(x,y_1,y_2,y_3) Consequently, $ Q (D)$ consists exactly of tuple $Q(u, uv_1,uv_2,uv_3)$ where $u V$ and $uv_1,uv_2,uv_3$ are its 3 incident edges. Then, for a subset $S Q (D)$ of size $k$, the value $δ_ (S)$ equals the number of different vertices $v V$ used in $S$ plus the number of unique incident edges over all $v$ used. Thus, $δ_ (S)$ is at most $4k$ and it is $4k$ exactly when $S$ uses $k$ distinct vertices $u_1, ,u_k V$ such that for no two $i,j=1, ,k, i≠ j$ the vertices $u_i$ and $u_j$ have a common incident edge. Consequently, $\ u_1, ,u_k\ $ is an independent set of $G$. Conversely, when $\ u_1, ,u_k\ $ is an independent set of $G$ of size $k$, then for no two $i,j=1, ,k, i≠ j$ the vertices $v_i$ and $v_j$ have a common incident edge. Consequently, $$δ_ (\ Q(u_1,u_1v_ 1,1 ,u_1v_ 1,2 ,u_1v_ 1,3 ), , Q(u_k,u_kv_ k,1 ,u_kv_ k,2 ,u_kv_ k,3 )\ )=4k.$$ This completes the reduction for $ $. Reduction for $ $: For $ $, we have to adapt the previous reduction. The problem with using the reduction as is, is that $ $ counts the same edge twice when they appear at different positions of the tuples. Thus, essentially, we have to make sure that edges $uv$ appear at the same position for both incident vertices $u$ and $v$. To do so, we simply increase the arity of $Inc$ to 6. Then, the database $D$ encoding $G$ will be defined via a iterative process. To t …

**Proof.** We will prove this by encoding the $ $ problem. To that end, recall the definition of $ $ (where $Ω$ is simply a countable infinite universe): rl \\[-2ex] Problem: & $ $\\ Input: & A set of finite subsets $ C =\ S_1, ,S_m\ 2^ Ω $ and a $k≥ 0$. \\ Output: & $ _ C ' C , | C '|=k | _ S C ' S|$\\\\[-2.2ex] Further, recall that due to [cite] , it is know that unless $ = $, there is no algorithm to approximate $ $ in polynomial time. We can construct very naturally a volume assignment $ =( ,μ,β)$ such that for a simply CQ $Q$ the problem $ [Σ, ,Q]$ encodes $ $. To do so, we only use one relation symbol $Σ=\ Q,R_ C \ $ and data values $ D := \ S Ω S is finite \ $. Then, the universe of tuples $ $ is $ = \ R_ C (S) S Ω, S is finite \ $. We then define the volume assignment via := \ S Ω S is finite \ , μ= , β R_ C (S) S, Q(S) S That is, the data values are finite subsets of the universe and intuitively, we measure the diversity of a set of subsets by the number of distinct elements covered. Further, we use the single atom query $$Q(x) R_ C (x)$$ and encode an instance $( C ,k)$ in the database $$D:=\ R_ C (S) S C \ $$ Then, clearly, $$δ_ (\ Q(S_1), , Q(S_k)\ ) = δ_ (\ R_ C (S_1), , R_ C (S_k)\ ) = | _ i=1, ,k S_i|$$ and $\ Q(S_1), , Q(S_k)\ Q (D)$ iff $\ S_1, , S_k\ C $. Hence, finding maximal diverse solutions of $Q$ over these databases is equivalent to finding subsets of $ C $ with maximal coverage.

**Proof.** This theorem can essentially be proven in the same way as Theorem [ref] . That is, it immediately follows from the results of~ [cite] as the considered diversity functions $δ_ $ are submodular. We simply have to notice that the runtime of Algorithm~ [ref] with the additional input $Q$ is as claimed as $T$, in particular, accounts for the time spent in line [ref] .

**Proof.** We provide a reduction for the problems individually. However, we always use the $ $ problem as the basis for the reductions. Recall that a Hamiltonian path of a directed graph $G=(V,E)$ is a sequence of distinct vertices $(v_1, ,v_n)$ such that $v_iv_ i+1 E$ for $i=1, ,|V|-1$ Then, $(V,E)$ is a yes-instance iff there exists a Hamiltonian path of $(V,E)$. Reduction for $ $: Given an instance $G = (V(G), E(G))$ of $ $, we define an instance $(D,Q,S)$ of $ [Σ, ]$ as follows: the database $D$ consists of a single binary relation $E$ storing the edges of $G$. That is, $$D=\ E(u,v) uv E(G)\ $$ Further, we set $ S= $, and, for $n = |V(G)|$, we define the ACQ $Q$ as follows: \[ Q(x_1, ,x_ n ) \ \ E(x_1,x_2), ,E(x_ n-1 ,x_ n ). \] Notice that a solution $Q(h( x )) Q (D)$ corresponds to the walk $h( )$ (a sequence of not necessarily distinct vertices) in $G$. Applying $δ_ $ to the singleton set $\ Q(h( x ))\ $, i.e., $δ_ (\ Q(h( x ))\ )$, counts the number of distinct vertices used in the corresponding walk. Hence, $G$ is a positive instance of Hamiltonian path, if and only if the solution to this instance of $ [Σ, ]$ yields an answer $Q(h( x ))$ with $δ_ (\ Q(h( x ))\ ) = n$. Reduction for $ $: We can simply reuse the reduction for $ $ by simply assigning all constants the weight $1$. Reduction for $ $: Given an instance $G = (V, E)$ of $ $, we define an instance $(D,Q,S)$ of $ [Σ, ]$ as follows: the database $D$ consists of the binary relation $E$ storing the edges of $G$ as well as a relation $V$ storing the vertices. That is, $$D=\ E(u,v) uv E\ \ V(v) v V(G)\ $$ Further, we set $ S= $, and, for $n = |V(G)|$, we define the ACQ $Q$ as follows: \[ Q(x_1, ,x_ n ) \ \ E(x_1,x_2), ,E(x_ n-1 ,x_ n ), V(x_1), , V(x_n). \] Notice that a solution $Q(h( x )) Q (D)$ corresponds to the walk $h( )$ (a sequence of not necessarily distinct vertices) in $G$. Applying $δ_ $ to the singleton set $\ Q(h( x ))\ $, i.e., $δ_ (\ Q(h( x ))\ )$, counts the number of distinct vertices and edges used in the corresponding walk. Hence, $G$ is a positive instance of Hamiltonian path, if and only if the solution to this instance of $ [Σ, ]$ yields an answer $Q(h( x ))$ with $δ_ (\ Q(h( x ))\ ) = 2n-1$.

**Proof.** Since $ $ is strictly more general than $ $ we simply provide the proof for $ $. That is, we show that $ [Σ, ]$ is tractable for ACQs. To that end, let $$Q( ) \ \ R_1( _1), … R_ m ( _ m )$$ be a ACQ over some schema $Σ$, $D$ a database, and $h_1, ,h_k$ homomorphisms from $Q$ to $D$, i.e., $S=\ Q(h_1( )), , Q(h_k( ))\ Q (D)$ a $k$-set of solutions. Further, let $ =(x_1, ,x_ | | )$. Consider the marginal diversity for a new solution $Q(h( )) Q (D)$: \[ δ_ (S \ Q(h( ))\ )-δ_ (S) \ = \ Σ_ x_l α_ x_l , where α_ x_l = w(h(x_l),l) & if i h(x_l)≠ h_i(x_l)\\ 0 & if i h(x_l) = h_i(x_l)\\ \] namely, the marginal diversity counts the number of new values (at new positions). We can cast this then to a sum-product query over the tropical semi-ring $ R _ max :=( R \ +∞\ , +,max)$. Doing so shows that we can find the element that maximizes the marginal diversity in linear time. To do so, for every $x_l $ let us choose a covering relation $R^ x_l :=R_i$ where $x_l _i$ and $1, ,m$. Then, we can define the $ R _ max $-annotated relations $R^*_1, ,R^*_m$. That is, for $h X $ such that $R_j(h( _j)) D$ we add to $D$ the annotated tuple $$R_j^*(h( _j)) Σ_ x_l R^ x_l =R_j α_ x_l , where α_ x_l = w(h(x_l),l) & if i=1, ,k h(x_l)≠ h_i(x_l),\\ 0 & if i=1, ,k h(x_l) = h_i(x_l). \\ $$ Note that we only use information from $h( _j)$ and the relation name $R_j$. Then, as every variable $x $ is covered by exactly one relation, we have: δ_ (S \ Q(h( ))\ )-δ_ (S) = Σ_ j=1 ^ m R_m^*(h( _m)), for homomorphism $h$ form $Q$ to $D$ where we add up the annotations on the right hand side. Note that we can see minimizing the right hand side of Eq. [ref] as a sum-product query of the following form: Q^*() _ j=1 ^m R_m^*( _m) With the semantics $$ Q^*() (D)= _ h hommomorphism from Q to D _ j=1 ^m R_m^*(h( _m))$$ Note that over the tropical semi-ring $ = max$ while $ = +$. Thus, actually, Q^*() (D) = max_h Σ_ j=1 ^m R_m^*(h( _m)). Then, due to results on sum-product queries~ [cite] we can determine the value of $ Q^*() $ as well as a $h$ witnessing this maximum in time $O(|Q|· |D|)$. Note that $Q(h( )) Q (D)$ finding $h$ is possible by tracing how $ Q^*() $ is computed as it is always only necessary to trace one argument of $ =max$. Consequently, solving $ [Σ, ]$ for ACQs is possible in time $O(|Q|· |D|)$. Then, due to Theorem [ref] , we can compute a $(1- 1 e )$-approximation of $ [Σ, ]$ in time $O(k·|Q|· |D|)$.

**Proof.** Let $f$ be a homomorphism over $ z $. Then, let us extend $β_ S ,β$ to $R_ ( )$-tuples as $$β_ S (R_ ( h ( ))) = β_ S (R((f h )( ))), β(R_ ( h ( ))) = β(R((f h )( ))).$$ Notice, $β_ S =β S$ also for $R_ $-tuples. Then, let $h,h'$ be homomorphisms over $ y $, and $g,g'$ homomorphisms over $ x ( y z )$. Then, β_ S &(R_ ((h g)( x z ))) β_ S (R_ ((h' g)( x z ))) \\ &= (β(R_ ((h g)( x z ))) S ) (β(R_ ((h' g)( x z )) S )\\ &= (β(R_ ((h g)( x z ))) β(R_ ((h' g)( x z ))) ) S \\ &= (β(R_ ((h g')( x z ))) β(R_ ((h' g')( x z ))) ) S\\ &= (β(R_ ((h g')( x z ))) S ) (β(R_ ((h' g')( x z )) S )\\ &=β_S(R_ ((h g')( x z ))) β_S(R_ (h' g')( x z ))) This completes the proof

**Proof.** Let $f$ be a homomorphism over $ z $, let $h,h'$ be homomorphisms over $ y $ and let $g$ be a homomorphism over $ x ( y z )$. We then define h h' & iff μ(β(R((h g f)( x )))) < μ(β(R((h' g f)( x ))))\\ h h' & iff μ(β(R((h g f)( x )))) = μ(β(R((h' g f)( x ))))\\ h h' & iff μ(β(R((h g f)( x )))) > μ(β(R((h' g f)( x )))) Let $g'$ be a further homomorphism over $ x ( y z )$. Then ($β$ extended to $R_ $ as above), μ (β(R((h g f)&( x ))) ) - μ (β(R((h' g f)( x ))) )\\ &= μ (β(R((h g f)( x ))) β(R((h' g f)( x ))) ) \\ & a - μ (β(R((h g f)( x ))) β(R((h' g f)( x ))) )\\ &= μ (β(R_ ((h g)( x z ))) β(R_ ((h' g)( x z ))) ) \\ & a - μ (β(R_ ((h g)( x z ))) β(R_ ((h' g)( x z ))) )\\ &= μ (β(R_ ((h g')( x z )) β(R_ ((h' g')( x z ))) ) \\ & a - μ (β(R_ ((h g')( x z ))) β(R_ ((h' g')( x z ))) ) \\ &= μ (β(R((h g' f)( x ))) β(R((h' g' f)( x ))) ) \\ & a - μ (β(R((h g' f)( x ))) β(R((h' g' f)( x ))) ) \\ & = μ (β(R((h g' f)( x ))) ) - μ (β(R((h' g' f)( x ))) ) Thus, for every homomorphism $g'$ over $ x ( y z )$ _f(R_ ((h g')( x z )))&=μ(β(R((h g f)( x )))) \\ &= μ(β(R((h' g f)( x )))) \\ &= _f(R_ ((h' g')( x z ))) holds if $h h'$, while _f(R_ ((h g')( x z )))&=μ(β(R((h g f)( x )))) \\ &> μ(β(R((h' g f)( x )))) \\ &= _f(R_ ((h' g')( x z ))) holds if $h h'$.

**Proof.** Let $S Q (D)$. Then, we need to show that $$ _ ,S (Q(h( x )))=δ_ (S \ Q(h( x ))\ ) = δ_ (S)+μ(β(Q(h( x ))) _ s S β(s))$$ is $ y $-decomposable conditioned on $ z $. Due to Lemma [ref] , $$β_ _ s S β(s) Q(h( x )) β(Q(h( x ))) _ s S β(s)$$ is $ y $-decomposable conditioned on $ z $. Then, due to Lemma [ref] , $$ Q(h( x )) μ(β_ _ s S β(s) (Q(h( x ))))$$ is $ y $-decomposable conditioned on $ z $. Lastly, due to Lemma [ref] , $$ _ ,S Q(h( x )) δ_ (S \ Q(h( x ))\ ) =δ_ (S)+ (Q(h( x )))$$ is $ y $-decomposable conditioned on $ z $ as required.

## Bound sentences
- Nevertheless, one must compare the approximation ratio obtained for volume-based diversity functions with that of the best approximation found for distance-based analogs.
- For $ $ under the Hamming or Euclidean metrics, the best approximation ratio is $(1 - ( 1 2 + ε))$, and the running time of the algorithms depends on $ε$.
- For $ $, the best approximation ratio is $(1 - 2 k )$ for Hamming distance (for Euclidean distance it is $(1- 1 2 )$) but the running time depends on $k^3$.
- Instead, the approximation ratio for volume-based diversity functions is $(1- 1 e )$ and works for every volume assignment that can be computed in polynomial time (in particular, for most of the examples presented in Section~ [ref] ).
- It is well-known that the maximum coverage problem is hard to approximate beyond $(1- 1 e )$-approximation ratio, unless $ = $~ [cite] .
- Nevertheless, even in the cases where $ $ is NP-hard, one might still be able to get a reasonable approximation algorithm.
