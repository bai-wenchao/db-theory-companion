# sigmod26-011 — Acyclic Conjunctive Regular Path Queries are no Harder than Corresponding Conjunctive Queries

flag=True score=47 stmts=14 proofs=16 chars=83224
kinds: {"definition": 2, "proposition": 9, "theorem": 2, "lemma": 1}
counts: {"np_hard": 0, "lower_bound": 3, "upper_bound": 1, "big_o": 0, "omega": 0, "theta": 1, "approx_ratio": 0, "whp": 0, "regret": 0, "dp": 0, "invariant": 0, "convergence": 0, "competitive": 0, "worst_case": 1, "sketch": 0, "cost_model": 0, "cardinality": 0, "learned": 0}

## Statements
**Definition 2.** 1 (Edge-Labeled Graph). An edge-labeled graph 𝐺 = (𝑉 , 𝐸, Σ) consists of a finite set 𝑉 of vertices, a finite set Σ of edge labels, and a set 𝐸 ⊆ 𝑉 × Σ × 𝑉 of labeled directed edges. Every triple (𝑣, 𝜎, 𝑢) in 𝐸 denotes an edge from vertex 𝑣 to vertex 𝑢 labeled with 𝜎. The size of 𝐺 is |𝑉 | + |𝐸|, i.e., the total number of its vertices and edges.10 7 Combinatorial algorithms is an under-defined concept in the algorithms literature that basically refers to algorithms that don’t use fast matrix multiplication, e.g. Strassen’s algorithm [34]. 8 The free-connex fractional hypertree width for acyclic queries takes only integer values [24]. See Proposition 2.16. 9 In particular, when 𝑘 ≥ 2, we have fn-fhtw(𝑄 1/2 +OUT). 𝑘-path ) = 2, hence the runtime from Eq. (5) becomes 𝑂 (𝑁 +𝑁 ·OUT 10We neglect the size of Σ by assuming that it does not contain any symbol that does not appear as an edge label in 𝐺. Proc. ACM Manag. Data, Vol. 4, No. 2 (PODS), Article 95. Publication date: May 2026. Acyclic Conjunctive Regular Path Queries are no Harder than Corresponding Conjunctive Queries 95:5

**Proposition 2.11.** A CRPQ 𝑄 is acyclic if and only if its 3-expansion is an acyclic CQ. The reason we use 3-expansions above (and not just 2-expansions) is because a self-loop will only result in a cyclic CQ after a 3-expansion. A CRPQ 𝑄 is called connected if its query graph is connected. A variable in 𝑄 is called a leaf if it is a leaf in the query graph of 𝑄. A query 𝑄 ′ is a subquery of 𝑄 if atoms(𝑄 ′ ) ⊆ atoms(𝑄) and free(𝑄 ′ ) = free(𝑄) ∩ vars(𝑄 ′ ). We state some basic observations on evaluating RPQs and CRPQs. ⇝

**Proposition 2.12.** (The CRPQ 𝑄 (𝑋 ) = 𝑅(𝑋, 𝑌 ) is evaluatable in linear time.) Given an edge-labeled ⇝ graph of size 𝑁 , the CRPQ 𝑄 (𝑋 ) = 𝑅(𝑋, 𝑌 ) can be evaluated in 𝑂 (𝑁 ) time. The following proposition follows immediately from Definitions 2.3 and 2.4. It basically says ⇝ that to produce the output of an RPQ 𝑅(𝑌 , 𝑋 ) where 𝑋 and 𝑌 are swapped, we can simply take the ⇝ inverse of 𝑅 and evaluate it on the transpose graph. Proc. ACM Manag. Data, Vol. 4, No. 2 (PODS), Article 95. Publication date: May 2026. Acyclic Conjunctive Regular Path Queries are no Harder than Corresponding Conjunctive Queries ⇝ 95:7 ⇝

**Proposition 2.13.** (The CRPQ 𝑄 (𝑋, 𝑌 ) = 𝑅(𝑌 , 𝑋 ) is equivalent to an RPQ 𝑅 −1 (𝑋, 𝑌 ).) The answer ⇝ of the CRPQ 𝑄 (𝑋, 𝑌 ) = 𝑅(𝑌 , 𝑋 ) on an edge-labeled graph 𝐺 is the same as the answer of the RPQ ⇝ 𝑅 −1 (𝑋, 𝑌 ) on the transpose graph 𝐺𝑇 . ⇝ ⇝

**Proposition 2.14.** (The query 𝑄 (𝑋, 𝑌 ) = 𝑅(𝑋, 𝑌 ) ∧ 𝑆 (𝑌 ) is equivalent to an RPQ.) Let 𝑅(𝑋, 𝑌 ) be an RPQ over an edge-labeled graph 𝐺 = (𝑉 , 𝐸, Σ), and 𝑆 ⊆ 𝑉 be a set of vertices viewed as a unary ⇝ relation. The answer of the query 𝑄 (𝑋, 𝑌 ) = 𝑅(𝑋, 𝑌 ) ∧ 𝑆 (𝑌 ) on 𝐺 is the same as the answer of an RPQ ⇝ 𝑅 ′ (𝑋, 𝑌 ) on an edge-labeled graph 𝐺 ′ = (𝑉 , 𝐸 ′, Σ′ ) that can be constructed from 𝐺 in linear time. 2.3 Width Measures The following width measures were originally defined for conjunctive queries (CQs), but they extend naturally to CRPQs. Given a CRPQ (or CQ) 𝑄, a tree decomposition of 𝑄 is a pair (𝑇 , 𝜒), where 𝑇 is a tree and 𝜒 : nodes(𝑇 ) → 2vars(𝑄 ) is a map from the nodes of 𝑇 to subsets of vars(𝑄) that satisfies the following properties: ⇝ • For every atom 𝑅(𝑋, 𝑌 ) ∈ atoms(𝑄), there is a node 𝑡 ∈ nodes(𝑇 ) such that 𝑋, 𝑌 ∈ 𝜒 (𝑡). (In case of a CQ 𝑄, for every atom 𝑅(𝑿 ) ∈ atoms(𝑄), there is a node 𝑡 ∈ nodes(𝑇 ) such that 𝑿 ⊆ 𝜒 (𝑡).) • For every variable 𝑋 ∈ vars(𝑄), the set {𝑡 | 𝑋 ∈ 𝜒 (𝑡)} forms a connected sub-tree of 𝑇 . Each set 𝜒 (𝑡) is called a bag of the tree decomposition. Let TD(𝑄) denote the set of all tree decompositions of 𝑄. A tree decomposition (𝑇 , 𝜒) of 𝑄 is called free-connex [11, 33] if it contains a (possibly empty) connected subtree 𝑇𝑓 with nodes nodes(𝑇𝑓 ) that contains all and only the free Ð variables of 𝑄, i.e., 𝑡 ∈nodes(𝑇𝑓 ) 𝜒 (𝑡) = free(𝑄). Let FTD(𝑄) denote the set of all free-connex tree decompositions of 𝑄. Note that if the query is Boolean (i.e. free( …

**Proposition 2.16.** For any acyclic CRPQ (or acyclic CQ) 𝑄, the following claims hold: • fn-fhtw(𝑄) is an integer. In particular, fn-fhtw(𝑄) = fn-ghtw(𝑄). • fn-fhtw(𝑄) is 1 if and only if 𝑄 is free-connex.

**Proposition 2.** 18 (fn-fhtw of 𝑘-expansion). Given an acyclic CRPQ 𝑄 and a natural number 𝑘 ≥ 2, let 𝑄𝑘′ be the 𝑘-expansion of 𝑄 (Definition 2.10). The following holds: ( 1 if 𝑄 is trivial, ′ fn-fhtw(𝑄𝑘 ) = (10) max(fn-fhtw(𝑄), 2) if 𝑄 is non-trivial. ⇝ As an example, the CRPQ 𝑄 (𝑋, 𝑌 ) = 𝑅(𝑋, 𝑌 ) has fn-fhtw(𝑄) = 1, but its 𝑘-expansion is 𝑄𝑘-path (Eq. (4)), which has fn-fhtw(𝑄𝑘-path ) = 2 for all 𝑘 ≥ 2. 2.4 Data Statistics Given a set of variables 𝑿 , we refer to a tuple 𝒙 over 𝑿 as an 𝑿 -tuple.

**Theorem 3.1.** Let 𝑄 be any acyclic CRPQ, and 𝐺 be an input edge-labeled graph of size 𝑁 . Then, 1 𝑄 over 𝐺 can be answered in time 𝑂 (𝑁 + 𝑁 · OUT1− max(fn-fhtw(𝑄 ),2) + OUT) in data complexity, where OUT is the output size, and fn-fhtw(𝑄) is the free-connex fractional hypertree width of 𝑄 (Definition 2.15). Our algorithm for acyclic CRPQs is combinatorial, i.e., it does not use fast matrix multiplication. In order to assess how good its runtime is, we compare it below to the best known runtime for acyclic CQs over all combinatorial algorithms [18, 24].

**Theorem 3.** 2 ([24]). Let 𝑄 be any acyclic CQ and 𝐷 be an input database instance of size 𝑁 . Then, 1 𝑄 over 𝐷 can be answered in time 𝑂 (𝑁 + 𝑁 · OUT1− fn-fhtw(𝑄 ) + OUT) in data complexity, where OUT is the output size, and fn-fhtw(𝑄) is the free-connex fractional hypertree width of 𝑄. Proc. ACM Manag. Data, Vol. 4, No. 2 (PODS), Article 95. Publication date: May 2026. Acyclic Conjunctive Regular Path Queries are no Harder than Corresponding Conjunctive Queries 95:9 There are two differences between the above two theorems (besides the obvious fact that one is for CRPQs while the other is for CQs). The first (subtle) difference lies in the definition of acyclicity for CRPQs versus CQs. See Remark 2.9 for a discussion. The other difference is in the runtime expression, where in the CRPQ case, we take the maximum of fn-fhtw(𝑄) and 2. Taking this maximum is unavoidable by Proposition 2.18. This is because solving a CRPQ 𝑄 is at least as hard as solving its 𝑘-expansion 𝑄𝑘′ (which is a CQ) for every 𝑘 ≥ 2. Excluding trivial11 CRPQs, Proposition 2.18 says fn-fhtw(𝑄𝑘′ ) = max(fn-fhtw(𝑄), 2). This shows that it is not possible to improve upon our runtime for acyclic CRPQs without improving the runtimes of the state-of-the-art combinatorial algorithms for acyclic CQs. In other words, our result links the fate of output-sensitive evaluation of acyclic CRPQs to that of acyclic CQs. We now prove our Theorem 3.1. To that end, we will break it down into two steps: In the first step, we reduce the prob …

**Definition 3.** 3 (A free-leaf CRPQ). A CRPQ 𝑄 is called free-leaf if it is acyclic, connected, and has a set of free variables which is exactly the leaves of its query graph.

**Lemma 3.** 4 (Reduction from an acyclic CRPQ to free-leaf CRPQs). Let 𝑄 be any acyclic CRPQ, and 𝐺 be an input edge-labeled graph of size 𝑁 . Then, in 𝑂 (𝑁 )-time in data complexity, we can construct 𝑘 free-leaf CRPQs 𝑄 1, . . . , 𝑄𝑘 and 𝑘 corresponding edge-labeled graphs 𝐺 1, . . . , 𝐺𝑘 for some constant 𝑘, satisfying the following: • |free(𝑄𝑖 )| ≤ max(fn-fhtw(𝑄), 2), for each 𝑖 ∈ [𝑘]. • The output size of each 𝑄𝑖 over 𝐺𝑖 is at most OUT, where OUT is the output size of 𝑄 over 𝐺. • The answer of 𝑄 over 𝐺 can be computed from the answers of 𝑄 1, . . . , 𝑄𝑘 over 𝐺 1, . . . , 𝐺𝑘 in time 𝑂 (OUT).

**Proposition 3.9.** For any acyclic bound-connected CRPQ 𝑄, |free(𝑄)| ≤ max(𝜌𝑄∗ (free(𝑄)), 2).

**Proposition 3.10.** (Every acyclic bound-connected CRPQ can be transformed into free-leaf.) For any acyclic bound-connected CRPQ 𝑄 and input edge-labeled graph 𝐺 = (𝑉 , 𝐸, Σ) of size 𝑁 , we can construct a free-leaf CRPQ 𝑄 ′ and an edge-labeled graph 𝐺 ′ = (𝑉 , 𝐸 ′, Σ′ ) in time 𝑂 (𝑁 ) in data complexity such that free(𝑄) = free(𝑄 ′ ) and the answer of 𝑄 over 𝐺 is the same as the answer of 𝑄 ′ over 𝐺 ′ . The above proposition can be proved by repeatedly removing non-free leaf variables from 𝑄 using Propositions 2.12 and 2.14. Figure 1 (bottom-right) depicts the free-leaf CRPQs obtained by applying Prop. 3.10 to each bound-connected component of 𝑄 from Eq. (11). Our reduction in Lemma 3.4 relies on taking the bound-connected components of 𝑄, and applying Prop. 3.10 to each component. But there is a little twist: Consider for example the component 𝑄 1 from Eq. (12). The output size of 𝑄 1 could potentially be larger than the output size of 𝑄, thus violating the second bullet point in Lemma 3.4. This is because, for example, the regular path query ⇝ ⇝ 𝑅6 (𝐷, 𝐺) could produce an empty output, but 𝑅6 (𝐷, 𝐺) is not part of 𝑄 1 . To resolve this issue, we have to modify 𝑄 1 a bit. In particular, we define the following CRPQ, 𝑄 𝐷 , whose body is the same as 𝑄 but has only one free variable, 𝐷: ⇝ ⇝ ⇝ ⇝ ⇝ ⇝ ⇝ ⇝ ⇝ ⇝ ⇝ 𝑄 𝐷 (𝐷) =𝑅1 (𝐴, 𝐵) ∧ 𝑅2 (𝐴, 𝐶) ∧ 𝑅3 (𝐴, 𝐷) ∧ 𝑅4 (𝐶, 𝐸) ∧ 𝑅5 (𝐶, 𝐹 ) ∧ 𝑅6 (𝐷, 𝐺)∧ 𝑅7 (𝐸, 𝐻 ) ∧ 𝑅8 (𝐹, 𝐼 ) ∧ 𝑅9 (𝐹, 𝐽 ) ∧ 𝑅10 (𝐻, 𝐾) ∧ 𝑅11 (𝐻, 𝐿) Similarly, we define a CRPQ 𝑄 𝑋 for every free  …

**Proposition 3.** 11 ([4]). Every acyclic CRPQ 𝑄 with at most one free variable can be answered in time 𝑂 (𝑁 ) in data complexity, where 𝑁 is the size of the input edge-labeled graph. After evaluating each 𝑄 𝑋 , we extend 𝑄 1 by adding the outputs of the CRPQs 𝑄 𝐷 , 𝑄 𝐸 , and 𝑄 𝐹 as extra filters to the body of 𝑄 1 : ⇝ ⇝ ⇝ ⇝ ⇝ 𝑄 1′ (𝐷, 𝐸, 𝐹 ) = 𝑅1 (𝐴, 𝐵) ∧ 𝑅2 (𝐴, 𝐶) ∧ 𝑅3 (𝐴, 𝐷) ∧ 𝑅4 (𝐶, 𝐸) ∧ 𝑅5 (𝐶, 𝐹 )∧𝑄 𝐷 (𝐷) ∧ 𝑄 𝐸 (𝐸) ∧ 𝑄 𝐹 (𝐹 ) Now, the output of 𝑄 1′ above is guaranteed to be the projection of the output of 𝑄 on the variables (𝐷, 𝐸, 𝐹 ). By Proposition 2.14, 𝑄 1′ above is still equivalent to a CRPQ: ⇝ ⇝ ⇝ ⇝ ⇝ 𝑄 1′ (𝐷, 𝐸, 𝐹 ) = 𝑅1 (𝐴, 𝐵) ∧ 𝑅2 (𝐴, 𝐶) ∧ 𝑅3′ (𝐴, 𝐷) ∧ 𝑅4′ (𝐶, 𝐸) ∧ 𝑅5′ (𝐶, 𝐹 ) 12 The bound-connected components of an acyclic CRPQ are also acyclic. Proc. ACM Manag. Data, Vol. 4, No. 2 (PODS), Article 95. Publication date: May 2026. 95:12 Mahmoud Abo Khamis, Alexandru-Mihai Hurjui, Ahmet Kara, Dan Olteanu, and Dan Suciu The above CRPQ is acyclic bound-connected but not yet free-leaf because 𝐵 is a leaf variable and it is not free. By applying Proposition 2.12, followed by Proposition 2.14, we can eliminate 𝐵, thus making 𝑄 1′ free-leaf: ⇝ ⇝ ⇝ ⇝ 𝑄 1′ (𝐷, 𝐸, 𝐹 ) = 𝑅2′ (𝐴, 𝐶) ∧ 𝑅3′ (𝐴, 𝐷) ∧ 𝑅4′ (𝐶, 𝐸) ∧ 𝑅5′ (𝐶, 𝐹 ) Because it is free-leaf, the above query can be solved using the algorithm from Lemma 3.5 in time 1 1− |free(𝑄 ′ )| 𝑂 (𝑁 + 𝑁 · OUT1 1 ), where OUT1 is the output size of 𝑄 1′ , which satisfies OUT1 ≤ OUT by 1 construction. By Eq. (17), this is bounded by 𝑂 (𝑁 + 𝑁 · OUT1− ma …

## Proofs
**Proof.** 4 As a running example in this section, we will use the following acyclic CRPQ 𝑄, which is depicted in Figure 1 (top-left): ⇝ ⇝ ⇝ ⇝ ⇝ ⇝ ⇝ ⇝ ⇝ ⇝ ⇝ 𝑄 (𝐷, 𝐸, 𝐹, 𝐺, 𝐾, 𝐿) =𝑅1 (𝐴, 𝐵) ∧ 𝑅2 (𝐴, 𝐶) ∧ 𝑅3 (𝐴, 𝐷) ∧ 𝑅4 (𝐶, 𝐸) ∧ 𝑅5 (𝐶, 𝐹 ) ∧ 𝑅6 (𝐷, 𝐺)∧ 𝑅7 (𝐸, 𝐻 ) ∧ 𝑅8 (𝐹, 𝐼 ) ∧ 𝑅9 (𝐹, 𝐽 ) ∧ 𝑅10 (𝐻, 𝐾) ∧ 𝑅11 (𝐻, 𝐿) (11) First, we introduce some concepts.

**Proof.** 5 In this section, we demonstrate our proof of Lemma 3.5. First, we introduce a definition and some auxiliary propositions.

**Proof.** 12 ⇝

**Proof.** Assume that the CRPQ 𝑄 is defined over an alphabet Σ, and let 𝐺 = (𝑉 , 𝐸, Σ) be an input edge-labeled graph for 𝑄. Let 𝑀𝑅⇝ = (𝑉𝑅⇝, 𝐸𝑅⇝, Σ) be an edge-labeled graph representing the ⇝ nondeterministic finite automaton (NFA) for the regular expression 𝑅. Let 𝐺 ′ = (𝑉 ′, 𝐸 ′ ) be the product graph of 𝐺 and 𝑀𝑅⇝ (Definition 2.5). Recall that 𝑉 ′ = 𝑉 × 𝑉𝑅⇝. Let 𝑇 ⊆ 𝑉 ′ be the set of all vertices in 𝑉 ′ of the form (𝑣, 𝑞 𝑓 ) where 𝑣 ∈ 𝑉 and 𝑞 𝑓 is an accepting state of 𝑀𝑅⇝. Similarly, let 𝑆 be the set of all vertices in 𝑉 ′ of the form (𝑢, 𝑞 0 ) where 𝑢 ∈ 𝑉 and 𝑞 0 is the initial state of 𝑀𝑅⇝. To compute the answer of 𝑄 on 𝐺, we do a backward traversal of 𝐺 ′ starting from all vertices in 𝑇 , until we compute the subset 𝑆 ′ ⊆ 𝑆 of vertices in 𝑆 reachable from 𝑇 . The answer of 𝑄 on 𝐺 is then obtained by projecting each vertex in 𝑆 ′ onto its first component. ⇝ Since the size of 𝑄, and hence of 𝑅, is constant, the product graph 𝐺 ′ can be constructed in 𝑂 (|𝑉 | + |𝐸|) = 𝑂 (𝑁 ) time. Traversal takes 𝑂 (𝑁 ) time as well. □

**Proof.** 14 ⇝ ⇝

**Proof.** Let 𝜎 ′ be a fresh symbol not in Σ, and Σ′ = Σ ∪ {𝜎 ′ }. We construct 𝐺 ′ = (𝑉 , 𝐸 ′, Σ′ ) by extending the edge set 𝐸 with self-loops at every vertex 𝑠 ∈ 𝑆 with label 𝜎 ′ . Now, let the regular ⇝ ⇝ expression 𝑅 ′ be the concatenation of the regular expression 𝑅 and the symbol 𝜎 ′ . Then, answering ⇝ the RPQ 𝑅 ′ (𝑋, 𝑌 ) over 𝐺 ′ is equivalent to answering the original query 𝑄 (𝑋, 𝑌 ) over 𝐺. □

**Proof.** 18

**Proof.** Let 𝑄 be an acyclic CRPQ and 𝑘 ≥ 2 be any natural number. Let 𝑄 ′ be the 𝑘-expansion of 𝑄 (Definition 2.10). If 𝑄 is trivial, then it is free-connex, hence 𝑄 ′ is also free-connex. By

**Proof.** 8

**Proof.** Consider an acyclic CRPQ 𝑄. Let (𝑋 1, . . . , 𝑋𝑛 ) be a bound-leading variable elimination order for 𝑄 with induced variable set sequence (V𝑋1 , . . . , V𝑋𝑛 ) and induced hypergraph sequence (G0, G1, . . . , G𝑛 ). Let (𝑋 1, . . . , 𝑋𝑚 ) be the bound variables in 𝑄. The main observation is that for any bound variable 𝑋 𝑗 , it holds: if V𝑋 𝑗 contains two free variables, then they must be contained in the same bound-connected component of 𝑄. Consider the hypergraph G𝑚 , which we obtain after eliminating all bound variables. Each hyperedge in G𝑚 is a set in the induced variable set sequence and must consist of the free variables of a bound-connected component of 𝑄. Hence the free variables of each bound-connected component of 𝑄 must be contained in a set of the induced variable set sequence. □

**Proof.** Let 𝑄 be an acyclic CRPQ and 𝑄 ′ a bound-connected component of 𝑄. Since atoms(𝑄) ⊇ atoms(𝑄 ′ ), it holds 𝜌𝑄∗ (free(𝑄 ′ )) ≤ 𝜌𝑄∗ ′ (free(𝑄 ′ )). Hence, it suffices to show that 𝜌𝑄∗ (free(𝑄 ′ )) ≥ 𝜌𝑄∗ ′ (free(𝑄 ′ )). For the sake of contradiction, assume that 𝜌𝑄∗ (free(𝑄 ′ )) < 𝜌𝑄∗ ′ (free(𝑄 ′ )). This ⇝ ⇝ means that 𝑄 contains an atom 𝑅(𝑋, 𝑌 ) such that 𝑅(𝑋, 𝑌 ) is not contained in 𝑄 ′ and 𝑋 and 𝑌 are two distinct free variables in 𝑄 ′ . Since each pair of variables in 𝑄 ′ is connected, this implies that 𝑄 is cyclic, which is a contradiction to our initial assumption. □

**Proof.** Since 𝑄 is acyclic, bound-connected, where all atoms are binary, 𝑄 must be a tree where free variables can occur only at the leaves. In general, we need a distinct edge to cover each free leaf, i.e., 𝜌𝑄∗ (free(𝑄)) = |free(𝑄)|, unless there are two free leaves that are directly connected by an edge, which can only happen when the query consists of a single edge. In this case, we have 𝜌𝑄∗ (free(𝑄)) = 1 and |free(𝑄)| = 2. □

**Proof.** 13

**Proof.** Let 𝑀𝑅⇝ = (𝑉𝑅⇝, 𝐸𝑅⇝, Σ) be an edge-labeled graph representing the NFA for the regular ⇝ expression 𝑅. Let 𝐺 ′ = (𝑉 ′, 𝐸 ′ ) be the product graph of 𝐺 and 𝑀𝑅⇝ (Definition 2.5). For each vertex (𝑣, 𝑞) ∈ (𝑉 ′ = 𝑉 × 𝑉𝑅⇝) in 𝐺 ′ , we compute a list, list(𝑣, 𝑞), of up to Δ distinct 𝒁 -tuples 𝒛 such that there is a path in 𝐺 ′ from (𝑣, 𝑞) to some (𝑦, 𝑞 𝑓 ) ∈ 𝑉 ′ where 𝑞 𝑓 is an accepting state of 𝑀𝑅⇝ and def (𝑦, 𝒛) ∈ 𝑆. This computation can be done in time 𝑂 (𝑁 · Δ) as follows: • For each vertex (𝑦, 𝑞 𝑓 ) ∈ 𝑉 ′ where 𝑞 𝑓 is an accepting state of 𝑀𝑅⇝, we initialize list(𝑦, 𝑞 𝑓 ) to be the set of all 𝒁 -tuples 𝒛 such that (𝑦, 𝒛) ∈ 𝑆. • Every time we add a 𝒁 -tuple 𝒛 to list(𝑣, 𝑞) for some vertex (𝑣, 𝑞) ∈ 𝑉 ′ , we go through all incoming edges ((𝑣 2, 𝑞 2 ), (𝑣, 𝑞)) and we add 𝒛 to list(𝑣 2, 𝑞 2 ) assuming 𝒛 is not already in this list and |list(𝑣 2, 𝑞 2 )| < Δ. We repeat this process until no more tuples can be added to any list. During the above process, each edge ((𝑣 2, 𝑞 2 ), (𝑣, 𝑞)) ∈ 𝐸 ′ is traversed once every time a tuple 𝒛 is added to list(𝑣, 𝑞), and there can only be at most Δ such tuples. Hence, the overall runtime is 𝑂 (𝑁 · Δ). Finally, we set 𝑄 (𝑋, 𝒁 ) as follows, where 𝑞 0 is the initial state of 𝑀𝑅⇝: 𝑄 (𝑋, 𝒁 ) def = {(𝑥, 𝒛) | 𝑥 ∈ 𝑉 and 𝒛 ∈ list(𝑥, 𝑞 0 )} □

**Proof.** 5

**Proof.** Let 𝑄 be any free-leaf CRPQ with ℓ leaves, i.e., ℓ = |free(𝑄)|, and let 𝐺 be an input edge-labeled graph of size 𝑁 . We assume for now that the output size OUT is known upfront; we will later explain how to get rid of this assumption. Let Δ = OUT1−1/ℓ , and Δ′ = Δ + 1. Pick any free variable 𝑊 ∈ free(𝑄), and re-orient the query tree so that 𝑊 becomes the root. For each variable 𝑌 ∈ vars(𝑄), let 𝑭𝑌 ⊆ free(𝑄) be the set of free variables in the subtree rooted at 𝑌 excluding 𝑊 (which can happen if 𝑌 is the root 𝑊 ). We do a bottom-up traversal of the query tree, from the leaves to the root, 𝑊 , where for each variable 𝑌 , let 𝑋 be its parent variable (assuming 𝑌 ⇝ is not the root), and let 𝑅(𝑋, 𝑌 ) be the RPQ associated with the edge (𝑋, 𝑌 ) in the query graph. For each variable 𝑌 , we define subqueries without actually computing them yet, as follows: def def • If 𝑌 is a leaf, we define the following subquery: (Recall that 𝑌 must be a free variable in this case because 𝑄 is free-leaf.) 𝑇𝑌 (𝑋, 𝑌 ) def = ⇝ 𝑅(𝑋, 𝑌 ) (Prop. 3.14) • Otherwise, Let 𝑍 1, . . . , 𝑍𝑘 be the children of 𝑌 in the query tree. We define the following two subqueries: (Note that in this case, 𝑭𝑍 1 , . . . , 𝑭𝑍𝑘 must be disjoint and their union must be 𝑭𝑌 .) 𝑆𝑌 (𝑌 , 𝑭𝑌 ) def = 𝑇𝑍 1 (𝑌 , 𝑭𝑍 1 ) ∧ · · · ∧ 𝑇𝑍𝑘 (𝑌 , 𝑭𝑍𝑘 ) 𝑇𝑌 (𝑋, 𝑭𝑌 ) def ⇝ = 𝑅(𝑋, 𝑌 ) ∧ 𝑆𝑌 (𝑌 , 𝑭𝑌 ) (Prop. 3.15) (Prop. 3.13) By induction, it is straightforward to verify the following two claims: Claim B.5. For each variable 𝑌 , 𝑇𝑌 (𝑋, 𝑭𝑌 ) is equivalent to a subquery of the CRPQ 𝑄 with free variables {𝑋 } ∪ 𝑭𝑌 and with edges corresponding to the subtree rooted at 𝑌 in addition to the edge (𝑋, 𝑌 ). Claim B.6. For each non-leaf variable 𝑌 , 𝑆𝑌 (𝑌 , 𝑭𝑌 ) is equivalent to a subquery of the CRPQ 𝑄 with free variables {𝑌 } ∪ 𝑭𝑌 and with edges corresponding to the subtree rooted at 𝑌 . As a result, 𝑆𝑊 (𝑊 , 𝑭𝑊 ) is equivalent to the original query 𝑄. (Note that {𝑊 } ∪ 𝑭𝑊 = free(𝑄).) By applying Prop. 3.14, we can compute an (𝑋, Δ′ )-restriction 𝑇 𝑌 (𝑋, 𝑌 ) of 𝑇𝑌 (𝑋, 𝑌 ) for each leaf variable 𝑌 in time 𝑂 (𝑁 · Δ′ ). Then, by repeatedly alternating Props. 3.15 and 3.13, we can compute a (𝑌, Δ′ )-restriction 𝑆 𝑌 (𝑌 , 𝑭𝑌 ) of 𝑆𝑌 (𝑌 , 𝑭𝑌 ) and an (𝑋, Δ′ )-restriction 𝑇 𝑌 (𝑋, 𝑭𝑌 ) of 𝑇𝑌 (𝑋, 𝑭𝑌 ) for each non-leaf variable 𝑌 in time 𝑂 (𝑁 · Δ′ ). At the very end, we compute a (𝑊 , Δ′ )-restriction 𝑄 (𝑊 , 𝑭𝑊 ) of 𝑄 (𝑊 , 𝑭𝑊 ) in time 𝑂 (𝑁 · Δ′ ). A 𝑊 -value 𝑤 is called light if deg𝑄 (𝑭𝑊 |𝑊 = 𝑤) ≤ Δ, and heavy otherwise (i.e., if

## Bound sentences
