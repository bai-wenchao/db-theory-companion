{"id": "sigmod26-104", "title": "Faster Relational Algorithms Using Geometric Data Structures", "from": "tex", "n_stmt": 30, "n_proof": 18, "kinds": {"definition": 4, "theorem": 8, "lemma": 19}, "counts": {"np_hard": 0, "lower_bound": 2, "upper_bound": 2, "big_o": 249, "omega": 13, "theta": 9, "approx_ratio": 90, "whp": 15, "regret": 0, "dp": 0, "invariant": 0, "convergence": 0, "competitive": 0, "worst_case": 1, "sketch": 0, "cost_model": 0, "cardinality": 0, "learned": 0}, "statements": [{"kind": "lemma", "num": "\\cite{esmailpour2024improved", "text": "Let $ $ be a database instance of $N$ tuples (with numerical attributes) and let $ $ be an acyclic query over $ $. Let $ $ be a box in $ ^d$. There exists an algorithm ; $ CountRect ( , , )$ to count $| ( ) |$ in $O(N)$ time, ; $ SampleRect ( , , , z)$ to sample $z$ points from $ ( ) $ in $O(N+zlog N)$ time, ; $ ReportRect ( , , )$ to report all points in $ ( ) $ in $O(N+| ( )|)$ time, ; $ ReprRect ( , , )$ to find a representative tuple from $ ( ) $ in $O(N)$ time. The running times of the oracles hold assuming perfect hashing. We assume perfect hashing for simplicity. A standard randomized hash table yields the same asymptotic bounds with high probability; perfect hashing assumption is used only to avoid carrying additional (routine) randomness through the analysis."}, {"kind": "theorem", "num": "", "text": "Given a set $P$ of $n$ points in $ ^d$ a parameter $ (0,1)$, and a constant parameter $ >1$, the RBBD tree has height $O(log n)$, it can be constructed in $O(nlog n + n·(f(n, )+ ^ -2 log n))$ time with space $O(n)$, where $f(n, )$ denotes the running time to sample $Θ( ^ -2 log n^ )$ points from a box (or a box with a hole), such that given a query ball $ (p,r)$ for a point $p ^d$ and a radius $r$, a set $ $ of $O(log n + ^ -d+1 )$ canonical nodes can be found in $O(log n + ^ -d+1 )$ time such that $ (p,r) P _ u _u (p,(1+ )r)$. The height, the space, the construction time of the tree, the size of $ $, and the query time hold with probability at least $1- 1 n^ -1 $."}, {"kind": "theorem", "num": "", "text": "Given an acyclic join query $ $ over $d$ attributes, a database instance $ $ of size $N$, a parameter $k$, and an arbitrarily small constant parameter $ (0,1)$, there exists an algorithm that computes a set $ ( )$ of $O(klog^3 N)$ points and a number $r_ $ such that $ _ ( ( ))≤ r_ ≤ (84+ )· _ ( ( )) ( ( ))$, with probability at least $1- 1 N $. The running time of the algorithm is $O(kNlog^5 N + klog^7 N)$ with probability at least $1- 1 N $. An algorithm with the same theoretical guarantees and running time exists for relational $k$-median clustering."}, {"kind": "theorem", "num": "", "text": "Given an acyclic join query $ $, a database instance $ $ of size $N$, a parameter $k$, and an arbitrarily small constant parameter $ (0,1)$, there exists an algorithm that computes a set $ ( )$ of $k$ points such that $ _ ( ( ))≤ (4+ )γ _ ( ( )) ( ( ))$, with probability at least $1- 1 N $. The running time of the algorithm is $O(kNlog^5 N + klog^ 7 N+ _ γ (klog^4 N))$ with probability at least $1- 1 N $. An algorithm with approximation factor $(2+ )γ$ and the same running time exists for the relational $k$-median clustering problem."}, {"kind": "theorem", "num": "", "text": "Given an acyclic join query $ $, a database instance $ $ of size $N$, and a parameter $k$, there exists an $O(1)$-approximation algorithm for the RRE, RRT, and RRC problem over $ ( )$ in $O(kNlog^2 N +klog^4 N)$ time. Both the aproximation factor and the running time hold with probability at least $1- 1 N $. Furthermore, there exists an $O(log k)$-approximation algorithm for the RRP, and RRM problem over $ ( )$ in $O(kNlog^2 N +klog^4 N)$ time. The running times hold with probability at least $1- 1 N $."}, {"kind": "theorem", "num": "", "text": "Given an acyclic join query $ $, a database instance $ $ of size $N$, and parameters $k_1,…, k_g$, such that $k=k_1+…+k_g$, there exists a $O(1)$-approximation algorithm for the RFRE problem over $ ( )$ in $O(kNlog^2 N +klog^4 N + T_ FRE (gk^2))$ time, where $T_ FRE $ is the running time of an $O(1)$-approximation algorithm in the standard computational setting for the fair remote edge problem over $gk^2$ points. The running time holds with probability at least $1- 1 N $."}, {"kind": "theorem", "num": "", "text": "Given an acyclic join query $ $ with $d_n$ numerical and $d_c$ categorical attributes, a database instance $ $ of size $N$, and a parameter $k$, there exists an algorithm that computes a set $ ( )$ of $k$ points such that $ _ ( ( ))≤ ((√ 10 +1)√ γ +√ 10 γ )^2 \\!\\! _ ( ( )) ( ( ))$ in $ (k^ d_c+1 N+ _γ(k^ d_c+1 ))$ time, with probability at least $1- 1 N $."}, {"kind": "theorem", "num": "", "text": "Given an acyclic query $ $, a database instance $ $ of size $N$, and an arbitrarily small constant parameter $ (0,1)$, there exists an $ $-approximate version of the Gonzalez algorithm on $ ( )$ for $k$ steps in $O(kNlog^2 N +klog^4 N)$ time. The running time holds with probability at least $1- 1 N $."}, {"kind": "lemma", "num": "", "text": "Assume that we can get a random sample set $ _u$ from a box or a box with a hole, in $f(n, )$ time, where $f$ is a positive real function that depends on $n$ and $ $, then the RBBD tree can be constructed in $O (nlog (n)+n· (f(n, )+ ^ -2 log^2 n ) )$ time, with probability at least $1- 1 n^ -1 $."}, {"kind": "theorem", "num": "", "text": "Given an acyclic query $ $, a database instance $ $ of size $N$, an arbitrarily small constant parameter $ $, and a set $S ( )$ with $|S|<k$, a point $p ( ) S$ can be computed such that $ (p,S)≥ (1- )max_ t ( ) S (t,S)$ in $O(kNlog^2 N + klog^4 N)$ time, with probability at least $1- 1 N $."}, {"kind": "lemma", "num": "", "text": "The RBBD tree has the same properties and the same asymptotic query complexity as the BBD tree, with high probability. The height of the RBBD tree is $O(log n)$, the number of nodes is $O(n)$ and the query complexity is $O(log n + ^ -d+1 )$, with probability at least $1- 1 n^ -1 $."}, {"kind": "lemma", "num": "", "text": "$ . (x,r)$ makes all points in $ (x,r) Q'$ inactive and might make some points in $ (x,(1+ )r) Q'$ inactive. Furthermore, in the end of the execution of the oracle, for every node $u $ the values $u.rep, u.c$ are correct. The running time is $O(( ^ -d+1 +log N)(N+ ^ -2 log^2 N))$."}, {"kind": "lemma", "num": "", "text": "Let $ $ be an acyclic join query over a database instance $ $ of size $N$. Given a node $u$ of an RBBD tree over $ ( )$, the fair split procedure is executed in $O(N)$ time, while the randomized centroid shrink procedure is executed in $O(N+ ^ -2 log^2 N)$ time."}, {"kind": "definition", "num": "", "text": "[Relational $k$-center/median/means clustering] Given a database instance $ $, a join query $ $, and a positive integer parameter $k$, the goal is to compute a set $ ( )$ of size $| |≤ k$ such that $ _ ( ( ))$ (resp. $ _ ( ( ))$, $ _ ( ( ))$ is minimized."}, {"kind": "definition", "num": "Relational implementation of Gonzalez algorithm", "text": "Given a database instance $ $, a join query $ $, and a positive parameter $k$, the goal is to implement the Gonzalez algorithm for $k$ iterations. Namely, given a set $ $ with $| |<k$, compute a point $p ( )$ such that $ (p, )=max_ q ( ) (q, )$."}, {"kind": "lemma", "num": "", "text": "For any arbitrary small constant $ \\! \\!(0,1)$, it holds that $ _ ( ( ))≤ r_ ≤ (84+ )· _ ( ( )) ( ( ))$, with probability at least $1- 1 N $. The algorithm runs in $O(kNlog^5 N + klog^7 N)$ time with probability at least $1- 1 N $."}, {"kind": "definition", "num": "", "text": "[Relational $k$-means clustering] Given a database instance $ $, a join query $ $, and a positive integer parameter $k$, the goal is to compute a set $ ( )$ of size $| |≤ k$ such that the $ _ ( ( ))$ is minimized."}, {"kind": "definition", "num": "", "text": "[Relational $k$-median clustering] Given a database instance $ $, a join query $ $, and a positive integer parameter $k$, the goal is to compute a set $ ( )$ of size $| |≤ k$ such that $ _ ( ( ))$ is minimized."}, {"kind": "lemma", "num": "", "text": "The randomized centroid shrink procedure on a node $u$ computes a midpoint box $ $ such that $ 1 3 - ≤ | P_u| |P_u| ≤ 2 3 + $, with probability at least $1- 1 Ω(n^ ) $."}, {"kind": "lemma", "num": "", "text": "For every iteration $i$, $| Q _i|≥ _i 2 $, so the algorithm executes $O(log | ( )|)=O(log N)$ iterations, and $| |=O(klog^3 N)$, with probability at least $1- 1 N^2 $."}, {"kind": "lemma", "num": "", "text": "Using the binary search, the main algorithm for the relational $k$-center problem returns a set $ $ of $k$ points such that $ _ ( ( ))≤ 2√ d (1+ ) _ ( ( )) ( ( ))$."}, {"kind": "lemma", "num": "", "text": "For every iteration $i$, the family of sets $Q_ i,0 ',…, Q_ i,1 ',…, Q_ i,M ',Q_ i,∞ '$ satisfy the three properties in the third bullet."}, {"kind": "lemma", "num": "", "text": "$ . (x,r)$ returns a number $c$ such that $| (x,r) Q'|≤ c≤ | (x,(1+ )r) Q'|$ in $O(( ^ -d+1 +log N)(N+ ^ -2 log^2 N))$ time."}, {"kind": "lemma", "num": "", "text": "The running time of the algorithm is $O (k( ^ -d+1 +log N)(N+ ^ -2 log^2 N)log N )$, with probability at least $1- 1 N $."}, {"kind": "lemma", "num": "", "text": "Given a point $p ^d$, there exists an algorithm $ NearNeighbor ( , ,p)$ to compute $ NN (p, ( ))$ in $O(N)$ time."}, {"kind": "lemma", "num": "", "text": "$ . ()$ returns $Q'$ in time $O(| | N + |Q'|)$, where $| |$ is the number of nodes in $ $."}, {"kind": "lemma", "num": "", "text": "For every iteration $i$, $| |≤ _i 20log| ( )| $, with probability at least $1- 1 N^ 3 $."}, {"kind": "lemma", "num": "", "text": "$ . ()$ returns a point in $Q'$ if $Q' ≠ $, and $ $ otherwise, in $O(1)$ time."}, {"kind": "lemma", "num": "", "text": "$ . (z)$ returns $z$ samples uniformly at random from $Q'$ in $O(z· N)$ time."}, {"kind": "lemma", "num": "", "text": "$Σ_ t ( ) ^2(t, (t)) ≤ (1+ ) _ ( ( )) ( ( ))$."}], "proofs": [{"text": "The recursive approach in the randomized centroid shrink is similar to the standard centroid shrink, but instead of executing it on $P_u$ it is executed in $ _u$. Hence, the randomized centroid shrink procedure will finish after $O( ^ -2 log n^ )=O( ^ -2 log n^ )$ recursive calls. The algorithm stops the first time it finds a majority midpoint box $ $ such that $| _u|≤ 2 3 | _u|$. By definition, the set $ _u$ is an $ $-sample with probability at least $1- 1 Ω(n^ ) $. Hence, $ | P_u| |P_u| ≤ | _u| | _u| + ≤ 2 3 + $. It remains to show the other direction of the inequality. Let $ '$ be the midpoint box that we executed the last fair split (to find $ $). By definition, we had $ | _u '| | _u| > 2 3 $. Notice that by definition of $ $, it holds that $| _u |≥ | _u '| 2 $. Hence, $ | _u | | _u| ≥ 1 2 · | _u '| | _u| ≥ 1 3 $. Since $ _u$ is an $ $-sample with probability at least $1- 1 Ω(n^ ) $, we conclude $ | P_u| |P_u| ≥ | _u| | _u| - ≥ 1 3 - $. The lemma follows."}, {"text": "The construction of the RBBD tree is similar to that of the BBD tree, except for the centroid shrink. Using the randomized centroid shrink, for a node $u$ we find a box $ $ such that $ 1 3 - ≤ | P_u| |P_u| ≤ 2 3 + $, with probability at least $1- 1 Ω(n^ ) $. For now, let us assume that the inequality of Lemma~ [ref] holds for every execution of the randomized centroid shrink. Next, we show that under this assumption, asymptotically, there is no difference between the BBD tree and the RBBD tree for sufficiently small constant $ $. In~ [cite] the authors show that every $4$ levels of descent in the BBD tree, the number of points associated with the nodes decreases by at least a factor $ 2 3 $, so the height of the tree is $O(log n)$ and the BBD tree has $O(n)$ nodes. Similarly, in our case, observe that each randomized centroid shrink introduces three new levels into the tree, and we alternate this with fair splits, so it follows that with each four levels the number of points decreases by at least a factor of $ 2 3 - $. For a small $ $, for example $ <0.1$, the height of the tree is $O(log n)$ by the same argument. Equivalently we can argue that every $8$ levels of descent in the RBBD tree, the number of points associated with the nodes decreases by at least a factor $ 2 3 $, the height of the tree is $O(log n)$ and the number of nodes is $O(n)$. Next, the authors in~ [cite] argue that every $4d$ levels of descent in the BBD tree, the sizes of the associated cells decrease by at least a factor of $ 2 3 $. Similarly, in our case, note that to decrease the size of a cell, we must decrease its size along each of $d$ dimensions. Since fair splits are performed at least at every fourth level of the tree, and each such split decreases the longest side by at least a factor of $2/3$, it follows that after at most $d$ splits (that is, at most $4d$ levels of the tree) the size decreases by this factor. All proofs of the BBD tree~ [cite] also hold for the RBBD tree assuming that all randomized centroid shrink operators are executed correctly, i.e., assuming that Lemma~ [ref] holds with probability $1$. Given that the inequality of Lemma~ [ref] holds with probability at least $1- 1 Ω(n^ ) $ and using the union bound over $O(n)$ nodes, we have that the probability that all randomized centroid shrink operators are executed correctly is at least $1- 1 n^ -1 $. Hence, we conclude that all proofs and properties of the BBD tree also hold (asymptotically) for the RBBD tree wi …"}, {"text": "Since the fair split procedure is identical to the standard BBD tree, all fair split procedures are executed in $O(nlog n)$ time using the machinery from the standard BBD tree. Next, we focus on the randomized centroid shrink procedure. For a node $u$ we get the set $ _u$ in $f(n, )$ time. We find the smallest midpoint box $ '$ that contains $ _u$ along with the inner box $ _u^-$ in $O( ^ -2 log n)$ time since we explicitly know all points in $ _u$ and the corners of $ _u^+$, using the highest and lowest values in each coordinate as described in~ [cite] . After the computation of $ '$, we execute $O(1)$ fair splits in order to find the first non-trivial fair split. The hyperplane in every fair split is computed straightforwardly in $O(1)$ time. Using a simple binary search tree over each coordinate of the points in $ _u$, we can check whether a fair split is trivial in $O(log ( ^ -2 log n))$ time. After finding the first non-trivial fair split, we compute $ $ in $O(log ( ^ -2 log n))=O(log n)$ time using the search binary tree that corresponds to the last non-trivial fair split by checking whether $| _u|>|( ' ) _u|$. After each non-trivial split, the box $ $ contains at most $| ' _u|-1$ samples from $ _u$. Hence, this recursive approach runs for at most $O(| _u|)=O( ^ -2 log n)$ times. Thus, each randomized centroid shrink runs in $O(f(n, )+ ^ -2 log^2 n)$ time. From Lemma~ [ref] , the RBBD tree has height $O(log n)$ and $O(n)$ nodes with probability at least $1- 1 n^ -1 $, so the running time of all randomized centroid shrink procedures is $O(n(f(n, )+ ^ -2 log^2 n))$, with probability at least $1- 1 n^ -1 $."}, {"text": "[Proof of Lemma~ [ref] ] If all nodes of $ $ were active then we would get a set of canonical nodes $ '(x,r)$ such that $B(x,r) ( ) _ u '(x,r) ( _u ( )) B(x,(1+ )r) ( )$. Since there are inactive nodes, the query procedure on $ $ skips branches of the tree that are already inactive. Hence $ (x,r) '(x,r)$. Let $p$ be an inactive point, i.e., $p ( ) Q'$. Point $p$ will remain inactive because our procedure only makes new nodes inactive (it does not make any node active). Then, assume an active point $p Q'$ such that $ (p,x)>(1+ )r$. By definition, there is no node $u (x,r)$ such that $p _u$, so $p$ will remain active. Finally, let $p Q'$ be an active point such that $ (p,x)<r$. Then there would be a node $u' '(x,r)$ such that $p _ u' $. Since $p$ is active there is no node $v $ such that $v.a=0$ and $p _v$. Node $u'$ becomes inactive, so $p$ will be inactive. We conclude that $B(x,r) Q' _ u (x,r) ( _u Q') B(x,(1+ )r) Q'$. Then we show that the values $u.rep$ and $u.c$ are correct after the update operation. This is shown by induction. Let $u$ be a node and $v,w$ be its children. If both $v.a=w.a$ then our algorithm correctly sets $u.a=0$ because $ _u= _v _w$. If $v.a=1$, $w.a=0$ and $v.rep, v.c$ are correct then our algorithm correctly sets $u.rep=v.rep$ and $u.c=v.c$ since $ _u Q'= _v Q'$. If $v.a=0$ and $w.a=1$, we argue equivalently. Finally, if $v.a=w.a=1$, then if the counts and representative points in $v,w$ are correct then indeed $v.rep _u Q'$, and by definition $| _u Q'|=| _v Q'|+| _w Q'|=v.c+w.c$. Every new node is constructed in $O(N+ ^ -2 log^2 N)$ time as shown in Lemma~ [ref] . The oracle runs a query on the RBBD tree to compute the canonical nodes $ (x,r)$. Since the number of samples we get in order to implement a randomized centroid shrink is $O( ^ -2 log N^ m+4 )$, the query procedure in the RBBD tree has the same properties as the query procedure in the BBD tree with probability at least $1- 1 N^ m+3 $ (as shown in Lemma~ [ref] ). Hence $| (x,r)|=O( ^ -d+1 +log N)$ and $ (x,r)$ is computed in $O( ^ -d+1 +log N)$ with probability at least $1- 1 N^ m+3 $. Thus, the running time is $O(( ^ -d+1 +log N)((N+ ^ -2 log^2 N)))$ with probability at least $1- 1 N^ m+3 $. The same holds for the proofs of the next lemmas in this section."}, {"text": "[Proof of Lemma~ [ref] ] From the proof of Lemma~ [ref] , we have that the query procedure computes a set of nodes $ (x,r)$ such that $B(x,r) Q' _ u (x,r) ( _u Q') B(x,(1+ )r) Q'$. Since the counters on the active (constructed) nodes are correct, the lemma follows."}, {"text": "[Proof of Lemma~ [ref] ] If $Q'= $ then indeed it must be the case that $ .a=0$. Otherwise, there would be a path of active nodes from the root of $ $ to a node $u$ (without constructed children) where $ _u ( )≠ $. If $Q'≠ $, and given that the representative points in each node are correct (Lemma~ [ref] ), we have that the returned point $ .rep$ is a point from $Q'$. In order to access $ .a$ and $ .rep$ we spend $O(1)$ time."}, {"text": "[Proof of Lemma~ [ref] ] Let $U$ be a subset of the nodes in $ $ constructed as follows: a node $u$ belongs in $U$ if and only if i) $u $, ii) there is no inactive node from the root to $u$ (including the roor and $u$), and iii) the children of $u$ have not been constructed in $ $ or $u$ is a leaf node of a fully constructed RBBD tree. Notice that the $ ReportRect ( , , _u)$ procedure is executed exactly for every $u U$. By definition of the active points $Q'$ we have that $Q'= _ u U ( _u ( ))$. Indeed, for each point $p Q'$ there is no inactive node $w $ such that $p _w$. Hence the procedure $ ReportRect ( , , _u)$ for every $u U$ returns exactly $Q'$. For the running time, we note that $|U|≤ | |$ and each procedure $ ReportRect ( , , _u)$ runs in $O(N+| _u ( )|)$ time. Hence the total running time is $O(| |· N+|Q'|)$."}, {"text": "[Proof of Lemma~ [ref] ] Our goal is to show that in each iteration, a point $p Q'$ is selected with probability $ 1 |Q'| $. Let $p Q'$ be a point stored in a leaf node $u$ of a complete RBBD tree (such a node may not have been constructed in $ $ yet). Recall that a point $p$ belongs in $Q'$ if and only if there is no inactive node in $ $ from the root to $u$. Assume that $v$ is the last node in the path from $ $ to $u$ that was constructed in $ $. Let $ v_1 … v_ v$. Let $w_1,…, w_ $ be the siblings of nodes $v_1,…, v_ $, respectively. From Section~ [ref] , we know that a point in a cell $ _ v $ is sampled with probability $ 1 | _v ( )| $. The probability that $p$ is selected is $Π_ j [ ] v_j.c v_j.c+w_j.c · 1 | _v ( )| $. Notice that $v$ is active and no children of $v$ have been constructed in the tree, so $ _v ( )= _v Q'$. Furthermore, by the correctness of the counts, $v_1.c+w_1.c=|Q'|$ and $v.c=| _v Q'|$. Moreover, $v_j.c=v_ j+1 .c+w_ j+1 .c$, for every $j [ ]$. We conclude that the probability of selecting $p Q'$ is $ 1 |Q'| $. For every sample, we spend $O(log N)$ time to get a node $v$ of $ $ which is either a leaf node or its children have not been constructed, because the height of $ $ is $O(log N)$ with high probability. After we find $v$, the sampling procedure in $ _v Q'= _v ( )$ is executed in $O(N)$ time as shown in Lemma~ [ref] . Since we get $z$ samples, the total running time is $O(zN)$."}, {"text": "[Proof of Theorem~ [ref] ] We notice that for any pair $t, p ( )$, $||t-p||_∞=max_ j [d] | _ A_j (t)- _ A_j (p)|$. Hence, the $ _∞$ distance between two points can be found as the absolute difference between two of the input tuples' coordinates. Using this observation, we construct a set $V$ of $O(N)$ points in $ ^1$ such that all pairwise $ _∞$ distances in $ ( )$ are included in the pairwise distances of points in $V$. Initially, $V= $. For every $A_j $, and every $R_i $ such that $A_j _i$, we add in $V$ the projections of tuples in $ _i$ with respect to $A_j$, i.e., $V=V \\ _ A_j (R_i)\\ $. After considering all attributes and relations, notice that for every $ _∞$ pairwise distance between two tuples in $ $, there exist two points in $V$ with the same distance. After we constructed $V$, we run a binary search on the pairwise distances of the points in $V$. In order to do it, we use the result from~ [cite] to return the $z$-th smallest pairwise distance of a set $V$ of points in $ ^1$ in $O(|V|· log |V|)=O(Nlog N)$ time. Hence, we run a binary search requesting the $z$-th smallest distance for the selected $O(log N)$ values of $z$ in the range $[0,1,…, |V× V|]$. Let $r'$ be the $z$-th smallest distance returned by~ [cite] . We set $r=√ d · r'$ and we run the main algorithm described in Section~ [ref] . In the end, we return the last non-empty set $ $ returned by the binary search. Using the binary search, the main algorithm for the relational $k$-center problem returns a set $ $ of $k$ points such that $ _ ( ( ))≤ 2√ d (1+ ) _ ( ( )) ( ( ))$. For any pair of points $t, p ^d$, $||t-p||_∞ ≤ ||t-p||_2≤ √ d ||t-p||_∞$. Let $r^*= _ ( ( )) ( ( ))$ and let $t^*, p^*$ be two tuples in $ ( )$ such that that $||t^*-p^*||_2=r^*$. Let $ r =||t^*-p^*||_∞$. By the definition of $V$ there will be two points in $V$ with distance exactly $ r $. By definition, we have $ r ≤ r^*≤ √ d r $. For every $r'≥ r $, we have $r=√ d r'≥ √ d r ≥ r^*$. Hence, from the analysis of Section~ [ref] , the main algorithm returns a non-empty set $ '$ of (at most) $k$ points such that $ _ ' ( ( ))≤ 2(1+ )r$. Let $r'≤ r $ be the distance in the last valid iteration of the binary search where a set $ ≠ $ was returned. We have $r=√ d r'$. In this case, after at most $k$ iterations, $ . ()= $ (equivalently, all points in $ ( )$ are inactive). Since in each iteration we make all points within distance $2r$ (and some points within distance at most $2(1+ )r$) inactive around every $p_i $, it means th …"}, {"text": "[Proof of Lemma~ [ref] ] From Lemma~ [ref] , we note that the set $X_i$ is a set of $ (k)$ samples selected from $Q'_i$ uniformly at random. For every $j \\ 0,1,2,…, M, ∞\\ $, we execute $ '. (x,b_j)$ over every $x X_i$. Let $p Q'_i$ be a point that was active at the moment that $c_ i,j $ was defined and became inactive after applying $ '. (x,b_j)$ for every $x X_i$. By the correctness of the $ '. (x,b_j)$ oracle (Lemma~ [ref] ) $p _ <j Q_ i, '$ and $p Q_ i,j '$. Furthermore, $p _ >j Q_ i, '$ because a point becomes inactive once. Hence for every pair $j_1,j_2 [M] \\ 0,∞\\ $, $Q_ i,j_1 ' Q'_ i,j_2 = $. Furthermore, when $j=∞$, all remaining active points, by definition, will become inactive. Hence $ _ j [M] \\ 0,∞\\ Q_ i,j '=Q'_i$. So property (1) holds. Next, if we fix an index $j [M] \\ 0,∞\\ $, for every $x X_i$, we execute $ _ i,j = _ i,j + '. (x,b_j)$ and then $ '. (x,b_j)$. By the proof of Lemma~ [ref] and Lemma~ [ref] , the $ '. (x,b_j)$ oracle returns exactly the number of points in $Q'_i$ that become inactive when we execute $ '. (x,b_j)$. Hence, it also holds that $ _ i,j =|Q_ i,j '|$, and property (3) holds. Finally, we show property (2). Let $j=0$, and let $x_1 X_i$ be the first sample we process. We execute $ '. (x_1,b_0)$. By Lemma~ [ref] , this oracle makes all points in $Q'_i$ within distance $b_0$ from $x_1$ inactive (it might also make some points within distance $(1+ )b_0$ inactive). Similarly, for any other $x_s X_i$, the oracle $ '. (x_s,b_0)$ makes inactive all currently active points within distance $b_0$ from $x_s$ (it might also make some points within distance $(1+ )b_0$ inactive). Hence, by the end of the process for $j=0$, all points in $Q'_ i,0 $ within distance $b_0$ from a point in $X_i$ become inactive with respect to $ '$. Some points within distance $(1+ )b_0$ from $X_i$ also might have become inactive. Hence $Q_ i,0 '$ satisfies property (2). By strong induction assume that every set $Q_ i,j '$ for each $j<j^*$ satisfies property (2), where $j^* [M] \\ ∞\\ $. For every $x X_i$, the oracle $ '. (x,b_ j^* )$ makes inactive all currently active points in $Q'_i$ that are within distance $b_ j^* $ from $x$ (and might make some points inactive within distance $(1+ )b_ j^* $). Let $Q_ i,j^*,x '$ be this set of points and hence we have $Q_ i,j^* '= _ x X Q_ i,j^*,x '$. From property (1), we have that $Q_ i,j^* ' ( _ j<j^* Q_ i,j ')= $. By the induction hypothesis the set $ _ j<j^* Q_ i,j '$ contains all points in $Q'_i$ within distance $b_ …"}, {"text": "[Proof of Lemma~ [ref] ] We fix the iteration $i$ of the algorithm. For every $p_s ( ( ))$, we place the smallest ball $B_s$ with center $p_s$ that contains $ _i 20· k· log| ( )| $ points from $Q'_i$. Since $X_i Q_i'$ and $|X_i|=Θ(klog^2| ( )|)$, we have that there is at least one point of $X_i$ inside every ball $B_s$, with probability at least $1- 1 N^3 $~ [cite] . Namely, $X_i B_s≠ $ for each $s [k]$. Let $x_s X_i$ denote the point that lies in the ball $B_s$, for each $s [k]$. Let $p Q'_i (( _ [k] B_ ) Q'_i)$ be a point outside of the balls $ _ [k] B_ $. Let $p_s$ be the closest center of $ ( ( ))$ to $p$, i.e., $ (p, ( ( )))= (p,p_s)$. Since $x_s B_s Q'_i$, notice that $ (p,x_s)≤ 2 (p,p_s)$, so $p Q'_i $, i.e., $p$ is a good point. Thus, with probability at least $1- 1 N^3 $, all bad points in $Q'_i$ lie inside the balls $B_1,…, B_k$. But every one of those balls, contain at most $ _i 20klog | ( )| $ points of $Q'_i$. It follows that $| |≤ k· _i 20klog | ( )| = _i 20log | ( )| $, with probability at least $1- 1 N^3 $."}, {"text": "[Proof of Lemma~ [ref] ] Clearly, $| Q _i|≥ _i- _ i,∞ -M· _i 10log | ( )| $, since $β_i$ is the largest index such that $Q'_ i,β_i > _i 10log | ( )| $. Notice that for every point $p Q'_ i,∞ $, it holds $ (p,X_i)≥ 2L| ( )|$. However $ _ ( ( )) ( ( ))≤ | ( )| L^2$ so $ (p, ( ( )))≤ √ | ( )| L$ and hence $p $. Thus, $Q'_ i,∞ $. Furthermore, from Lemma~ [ref] we have $| |≤ _i 20log| ( )| $ with probability at least $1- 1 N^3 $. Hence, $| Q _i|≥ _i - _i 20log | ( )| - (2log | ( )| +3) _i 10log| ( )| ≥ _i 2 $. So, in every iteration, at least half of the remaining points become inactive. We conclude that the algorithm runs for $O(log | ( )|)=O(log N)$ iterations with probability at least $1- 1 N^2 $. In each iteration we add in $ $ the set $X_i$ and $|X_i|=O(klog^2 N)$, so $| |=O(klog^3 N)$, with probability at least $1- 1 N^2 $."}, {"text": "[Proof of Lemma~ [ref] ] We first prove the approximation factor. We fix the iteration $i$ of the algorithm. If $β_i>0$, then we argue as follows. Let $ =Q_ i,β_i ' $. For any point $p Q _i $ and $q $, we have $ (p,X_i)≤ 2(1+ ) (q,X_i)$. This inequality follows from the definition of the sets $Q'_ i,j $ and the $ '. (·,·)$ oracle. Furthermore, $| |> _i 10log | ( )| - _i 20log | ( )| = _i 20log | ( )| ≥ | Q _i |$, with probability at least $1- 1 N^2 $. By definition, every point in $ $ is a good point with respect to $X_i$ so $ _ X_i ( )≤ 4 _ ( ( )) ( )$. Furthermore, since $ Q _i$ we have that $ _ ( ( )) ( )≤ _ ( ( )) ( Q _i)$. Thus, $$ _ X_i ( Q _i )≤ 4(1+ )^2 _ X_i ( )≤ 16(1+ )^2 _ ( ( )) ( )≤ 16(1+ )^2 _ ( ( )) ( Q _i).$$ So, _ X_i ( Q _i)&= _ X_i ( Q _i )+ _ X_i ( Q _i )≤ 16(1+ )^2 _ ( ( )) ( Q _i)+4 _ ( ( )) ( Q _i)\\\\&≤ 20(1+ )^2· _ ( ( )) ( Q _i). If $β_i=0$, then for every point $q Q _i$ we have $ (q,X_i)≤ (1+ ) L 4| ( )| $. Thus, for $ ≤ 0.1$, $$ _ X_i ( Q _i)=Σ_ q Q _i ^2(q,X_i)≤ (1+ )^2 L^2 16| ( )| < L^2 9| ( )| ≤ 1 | ( )| _ ( ( )) ( ( )).$$ In any case, we have $ _ X_i ( Q _i)≤ max\\ 20(1+ )^2· _ ( ( )) ( Q _i), 1 | ( )| _ ( ( )) ( ( ))\\ $. By definition, recall that $ Q _ i_1 Q _ i_2 = $ for every $i_1≠ i_2$. Overall, $ _ ( ( ))≤ 21(1+ )^2 _ ( ( )) ( ( ))$, with probability at least $1- 1 N $. Finally, for every point $p Q _i$ such that $p Q_ i,j' '$ for an index $j'≤ β_i$ we charge $((1+ )b_ j' )^2$ in $r_ $. By definition, if $j'>0$, $ (p,X_i)≤ (1+ )b_ j' ≤ 2(1+ ) (p,X_i)$. Hence the $k$-means cost of $p$ with respect to $X_i$ is charged at most $4(1+ )^2 ^2(p,X_i)$ and at least $ ^2(p,X_i)$ in $r_ $. If $j'=0$, then by the bound of $ _ X_i ( Q _i)$ when $β_i=0$, we charge a value which is at most $ 1 | ( )| _ ( ( )) ( ( ))$ and at least $ _ ( Q )$ in $r_ $. Generally, for all points in $ Q _i$, we charge a value at most $Σ_ p Q _i 4(1+ )^2 ^2(p,X_i)≤ 84(1+ )^4 _ ( ( )) ( Q _i)$ and at least $Σ_ p Q _i (p,X_i)≥ _ ( Q _i)$ in $r_ $. Overall, $ _ ( ( ))≤ r_ ≤ 84(1+ )^4 _ ( ( )) ( ( ))$. By setting $ /400$, we get $ _ ( ( ))≤ r_ ≤ (84+ ) _ ( ( )) ( ( ))$, wuit probability at least $1- 1 N $. Next, we proceed with the running time. First, we run the $k$-center algorithm from Theorem~ [ref] in $O (kNlog^2 N + klog^4 N )$ time with probability at least $1- 1 10N $. Then the algorithm runs for $O(log N)$ iterations with probability at least $ 1 N^2 $. For every iteration $i$, we get the set $X_i$ in $O(kNlog^2 N)$ time, with probability at least $1- …"}, {"text": "[Proof of Lemma~ [ref] ] The algorithm runs for $O(log N)$ iterations. For every iteration, we run the $k$-center algorithm from Theorem~ [ref] in $O (kNlog^2 N + klog^4 N )$ time with probability at least $1- 1 N^ Ω(1) $. We get the set $X$ in $O(kNlog^2 N)$ time, with probability at least $1- 1 N^ Ω(1) $, using the result of Lemma~ [ref] . There are $O(log N)$ different values of $ [M] \\ 0,∞\\ $, and for each $ $ we compute $ _ $ in $O(kNlog N +klog^3 N)$ time using Lemma~ [ref] . Furthermore, the time we need to construct a copy of $ $ is $O(kNlog^2 N +klog^4 N)$. Thus, in each iteration the $k$-center algorithm from Theorem~ [ref] dominates the running time. In the end, we run $ . ()$. The size of the constructed tree $ $ is $O(klog^3 N)$. From Lemma~ [ref] , and the fact that $| |=O(klog^2 N)$, the oracle $ . ()$ runs in $O(kNlog^3 N)$ time, with probability at least $1- 1 N^ Ω(1) $. Overall, our algorithm runs in $O(kNlog^3 N + klog^5 N)$ time, with probability at least $1- 1 N^ Ω(1) $."}, {"text": "Let $t ( )$ be a tuple assigned to a center in $C$ for some radius $r > L 9| ( )| $, and let $ _1( ) ( )$ be the set of such tuples. By the correctness of the RBBD-tree, $ _ _ (n) (t) _ c' C' B (c', r/(1+ ))$, implying $ ( _ _ (n) (t), _ _ (n) ( (t))) ≤ (1+ )^2 ( _ _ (n) (t), C')$. Moreover, by construction, for every $A _ (c) $, $ _A( (t)) C_A$ is the closest center of $ _A(t)$ in $C_A$. Thus, $ (t, (t)) ≤ (1+ )^2 (t, C)$. Now let $t ( )$ be a tuple assigned to a center in $C$ for $r = L 9| ( )| $, and let $ _2( ) ( )$ denote these tuples. Then, we have $ (t, (t)) ≤ (1+ ) L 9| ( )| $. Combining the two cases, we have $Σ_ t ( ) ^2(t, (t)) = Σ_ t _1( ) ^2(t, (t)) + Σ_ t _2( ) ^2(t, (t)) ≤ (1+ )^4 Σ_ t ( ) ^2(t, C) + (1+ )^2 L^2 81| ( )| ≤ (1+2 )^4 _ ( ( )) ( ( ))$. Setting $ /12$ yields the desired bound $Σ_ t ( ) ^2(t, (t)) ≤ (1+ ) _ ( ( )) ( ( ))$."}, {"text": "From Appendix~ [ref] , we have that $Σ_ t ( ) ^2(t, (t)) ≤ 2 \\, _ ( ( )) ( ( ))$. This is equivalent to the process of first clustering $ ( )$ according to Theorem~ [ref] with an approximation error of $2 · 5γ$ (instead of $5γ$) and then computing the exact weights $ (p)$. Following the analysis in~ [cite] , the overall approximation factor becomes $((√ 10 + 1)√ γ + √ 10 γ)^2$. For each categorical attribute $A _ (c) $, the set of centers $C_A$ is computed in $ (N)$ time, as discussed before. The algorithm from Theorem~ [ref] , when applied to $ _ _ (n) ( ( ))$, runs in $ (kN + _γ(k))$ time with high probability. Since $|C| = O(k^ d_c + 1 )$, all weights $ (p)$ for $p C$ is computed in $ (k^ d_c + 1 )$ time (see Appendix~ [ref] ). Finally, the $k$-means clustering on the weighted set $C$ is executed in $ ( _γ(k^ d_c + 1 ))$ time. Overall, the total running time of our algorithm is $ (k^ d_c + 1 N + _γ(k^ d_c + 1 ))$, with high probability."}, {"text": "The algorithm executes $ (k)$ oracles from Section~ [ref] . If $r'$ was the largest distance found by the binary search such that $ . ≠ $, we have that $ (p,S)>r'$. Recall, that the binary search (as defined in Appendix~ [ref] ) is executed on the $ _∞$ pairwise distances of $ ( )$ and then discretize the ranges $[r,√ d r]$ by a factor $(1+ )$. By definition, in the execution of binary search, for $r''=(1+ )r'$ we had that $ . ()= $, otherwise $r'$ would not be the largest distance we found with $ . ()≠ $. By the properties of RBBD tree from Section~ [ref] , we have that every point $q ( )$ is inactive, i.e., there exists $s S$ such that $q _ u (s,r'') ( _u ( ))$. Since the RBBD tree approximates the balls within a factor $(1+ )$, we have that for every $q ( )$, $ (q,S)≤ (1+ )r''$. Hence, $max_ t ( ) S (t,S)≤ (1+ )r''$. We conclude that $ (p,S)>r'= r'' 1+ ≥ 1 (1+ )^2 max_ t ( ) S (t,S)$."}, {"text": "The binary search runs in $O(log N)$ iterations. In order to compute a distance in each step of the binary search we spend $O(Nlog N)$ time as shown in Appendix~ [ref] . Hence all values for the binary search is computed in $O(Nlog^2 N)$ time. For each step of the binary search, the $ . (s,r)$ oracle runs in $O(( ^ -d+1 +log N)(N+ ^ -2 log^2 N))$ with probability at least $1- 1 N^ m+2 $ and the $ . ()$ runs in $O(1)$ time. Hence, the overall running time of our algorithms is $O(Nlog^2 N+k( ^ -d+1 +log N)(N+ ^ -2 log^2 N)log N)=O(k( ^ -d+1 +log N)(N+ ^ -2 log^2 N)log N)$, with probability at least $1- 1 N $."}], "bound_sents": ["Relational algorithms for $k$-center/median/means clustering Overall, $O(N^ )$ is an upper bound on the number of join results materialized for each node in $ $.", "We first execute our relational $k$-center algorithm on $ ( )$ in order to get a polynomial upper and lower bound on the optimum $k$-means/median cost.", "In Subsection~ [ref] , we got the factor $√ d $ in the approximation ratio because for every $ _∞$ distance $r'$ in the binary search we set $r=√ d r'$.", "While this is a loose upper bound in the standard computational setting, we use it to bound the query procedure in the relational setting."], "flag": true, "score": 68, "n_chars": 260327}