# sigmod26-289 — Clustering with Set Outliers and Applications in Relational Clustering

from=tex flag=True score=66 stmts=29 proofs=18 chars=195157
kinds: {"definition": 3, "theorem": 12, "lemma": 13, "corollary": 1}
counts: {"np_hard": 0, "lower_bound": 0, "upper_bound": 0, "big_o": 182, "omega": 3, "theta": 5, "approx_ratio": 87, "whp": 2, "regret": 0, "dp": 1, "invariant": 0, "convergence": 0, "competitive": 0, "worst_case": 2, "sketch": 0, "cost_model": 0, "cardinality": 1, "learned": 0}

## Statements
**Theorem .** Given a set $P$ of $n$ points in $ ^d$, a set $ $ of $m$ hyper-rectangles in $ ^d$, where $d$ is a constant, such that every point in $P$ belongs to one hyper-rectangle from $ $, positive integer parameters $k, z$, and a small constant $ (0,1)$, there exists a $(2+ , 2,O(1))$-approximation algorithm for the $ (P, ,k,z)$ problem that runs in $O(((n+m)log^ d-1 n +kmlog n+β_1log(β_1)+(k+z)(β_2+β_3)log^ d+2 (β_2)) n)$ time, where $β_1=min\ n,km\ $, $β_2=min\ n,kz\ $, and $β_3=min\ m,kz\ $.

**Definition $\gprob(P,\setrects,k,z)$.** Given a set $P$ of $n$ points in $ ^d$, a set $ $ of $m$ hyper-rectangles in $ ^d$, where $d$ is a constant and every point in $P$ lies in at least one hyper-rectangle in $ $, and two integer positive parameters $k, z$, the goal is to compute a set of centers $C P$ with $|C|≤ k$ and a set $ $ of hyper-rectangles as outliers with $| |≤ z$ and $C ( _ P)= $, such that the cost excluding points covered by outliers $ (C,P ( _ P))$ is minimized.

**Theorem .** For an acyclic join query $ $ of $d$ attributes, a database $ $ of input size $N$ with $| ( )|= (N)$, integer positive parameters $k, z$, and a small constant $ (0,1)$, there exists an $(1,1+ ,3+ )$-approximation for the $ ( , ,k,z)$ problem with probability at least $1- 1 N $ that runs in $O (Nlog N + k^2 δ log^3 N )$ time, where $δ= z | ( )| $. If $| ( )|=O(N)$, then the running time is bounded by $O(kNlog^2 N)$.

**Definition $\prob(P,\mathcal{H},k,z)$.** Given a set $P$ of $n$ elements, a family $ H $ of $m$ subsets of $P$, where every element in $P$ belongs to at least one set in $ H $, and two integer positive parameters $k, z$, the goal is to compute a set of centers $C P$ with $|C|≤ k$ and a family $H H $ of outlier sets with $|H|≤ z$ and $C ( _ h H h)= $ such that the cost excluding the points covered by outliers $ (C,P ( _ h H h))$ is minimized.

**Theorem \cite{arora2012multiplicative.** Given a feasibility problem as defined above, a parameter $ $, a $ $- ORACLE implemented in procedure $ Oracle (·)$, and an update procedure $ Update (·)$, there is an algorithm which either finds an $ $ such that $ i$, $A_i _i≥ b_i- $ or correctly concludes that the system is infeasible. The algorithm makes $O( (m')/ ^2)$ calls to procedures $ Oracle (·)$ and $ Update (·)$.

**Corollary .** Given a set of $n$ elements $P$ in a metric space with bounded doubling dimension, a family of $m$ disjoint subsets $ H $ over $P$, and positive integer parameters $k,z$, there exists a $(2,2,O(1))$-approximation algorithm for the $ (P, H ,k,z)$ that runs in $O(nk+k^2m+β_1^2+(β_2+β_3)^ log β_1)$ time, where $β_1=min\ n,km\ $, $β_2=min\ n,kz\ $, and $β_3=min\ m,kz\ $.

**Theorem .** For an acyclic join $ $ of $d$ attributes, a database $ $ of input size $N$, parameters $ , k, z$, a set $S ( )$ of size $k$ and a set $C ( )$ of size $|C|≤ (1+ )^2δ | ( )|$ can be computed with probability at least $1- 1 | ( )|^2 $, such that $ (S, ( ) C)≤ 3√ d _ k,z ( ( ))$, where $δ= z | ( )| $. The running of the algorithm is $ O ( k ^2δ N+ k^3 ^4δ^2 )$.

**Theorem .** For a database schema $ $ with $g$ tables $R_1,…, R_g$, an acyclic join query $ $ of $d$ attributes, a database $ $ with $| |=N$, integer positive parameters $k, z$, and a small constant $ (0,1)$, there exists a $(2+ ,2,O(1))$-approximation algorithm for the $ ( , ,k,z)$ problem, with probability at least $1- 1 N $, that runs in $O(k^2N^2log N)$ time.

**Theorem .** Given a set of $n$ elements $P$, a family of $m$ disjoint subsets $ H $ over $P$, and positive integer parameters $k,z$, there exists a $(2,2,O(1))$-approximation algorithm for the $ (P, H ,k,z)$ problem that runs in $O ( (nk+k^2m+β_1^2+(β_2+β_3)^ )· log β_1 )$ time, where $β_1=min\ n,km\ $, $β_2=min\ n,k^2z, km\ $, and $β_3=min\ m,kz\ $.

**Theorem \cite.** For an acyclic join $ $ of $d$ attributes, a database $ $ of input size $N$, and parameters $ (0,1)$ and $k>0$, there exists a $(2+ )$-approximation algorithm for the $k$-center clustering in $ ( )$ under the Euclidean metric that runs in time $$O (min \ k^2N^ + k ^d N^ , N^ log^2N + k ^d N^ log(N) + k^ O(d) \ )$$.

**Theorem .** For an acyclic join query $ $ of $d$ attributes and $g$ tables, a database $ $ with $| |=N$, and integer parameters $k, z\!\!>\!\!0$, there exists an $(1,g,O(1))$-approximation algorithm for the $ ( , ,k,z)$ problem, with probability at least $1- 1 N $, that runs in $O(2^ g· k+z · (z+ k^d)· N· log^3 N)$ time.

**Theorem .** Given a set $P$ of $n$ points in $ ^d$, a set $ $ of $m$ hyper-rectangles in $ ^d$, where $d=O(1)$, integer parameters $k, z>0$, and a small constant $ (0,1)$, there exists a $(2+ ,2f,2+ )$-approximation algorithm for the $ (P, ,k,z)$ problem that runs in $O ( (k+z )· (n+m ) ^ d+2 n )$ time.

**Lemma \cite{esmailpour2024improved.** Let $ $ be a hyper-rectangle in $ ^d$. For an acyclic join query $ $, there exists an oracle $ CountRect ( , , )$ to count $| ( ) |$ in $O(Nlog N)$ time. Furthermore, there exists an oracle $ SampleRect ( , , , z)$ to sample $z$ samples from $ ( ) $ in $O((N+z)log N)$ time.

**Definition $\relclustertwo(\Q,\I, k, z)$.** Given a database schema $ $, a database instance $ $ of size $O(N)$, a join query $ $, and two integer positive parameters $k, z$, the goal is to compute a set $S ( )$ with $|S|≤ k$ and a set $T $ with $|T|≤ z$ and $S ( T)$, such that $ (S, ( T))$ is minimized.

**Theorem .** runtime Given an instance $(P,L,k,z)$ of , there exists an algorithm running in time $O(mk^3)$ that computes sets $P' P$ and $L' L$ such that $ (P',P L'(P))≤ 3 ^*_ k,z (P,L)$ and either (a) $|P'|≤ k(k+1)$ and $L'≤ z$ or (b) $|P|≤ k$ and $|L'|<z(z+1)$.

**Theorem .** If the Unique games conjecture is true, there exists no $(1,f- ,γ)$-approximation algorithm that runs in polynomial time for the $ $ problem, where $f = o(log n)$, $γ=o(| H |)$ is any sublinear value and $ $ any positive real number.

**Lemma \cite{agarwal2024computing.** For an acyclic join query $ $ and a small constant $ (0,1)$, there exists an oracle $ RelCluster ( , ,k)$ that returns a set $S ( )$ and a number $r_S$, with $|S|≤ k$, such that $ (S, ( ))≤ r_S≤ (2+ ) ^*_k( ( ))$ in $O(k^2· N)$ time.

**Theorem .** Given a set of $n$ elements $P$, a family of $m$ subsets $ H $ over $P$, and positive integer parameters $k, z$, there exists a $(2,2f,2)$-approximation algorithm for the $ (P, H ,k,z)$ problem that runs in $O((n+m)^ log n)$ time.

**Lemma .** If $ Alg $ is a polynomial time $(1,f- ,γ)$-approximation algorithm for the $ $ problem, where $γ=o(| H |)$ is any sublinear value, then there exists a polynomial time $(f- )$-approximation algorithm for the $ SC $ problem.

**Lemma .** It holds that: i) If $r≥ ^*_ k,z (P, H )$ then ($ LP $ [ref] )\ is feasible. ii) $|H|≤ 2fz$. iii) If ($ LP $ [ref] )\ is feasible for a distance $r$, then $ (C,P ( _ h_j H h_j))≤ 2r$. iv) $|C|≤ 2k$.

**Lemma .** It holds i) If $r≥ ^*_ k,z (P, )$ then ($ LP $ [ref] ) is feasible. ii) $| |≤ 2fz$, and iii) If ($ LP $ [ref] ) is feasible for a distance $r$ then $ (C,P ( _ _j _j))≤ 2(1+ )r$.

**Lemma .** If $r≥ ^*_ k,z (P, H )$, then $|P'|=O(min\ n,k^2z, km\ )$, $| H '|=O(min\ m,kz\ )$, and $ ^*_ k',z' (P', H ')≤ 18r$.

**Lemma .** It holds that $|S|≤ k$, $|T|≤ g· z$ and $ (S, ( T))≤ O(1)· ^*_ k,z ( ( ))$, with probability at least $1- 1 N $.

**Lemma .** $ (S, ( ) T)≤ 3(1+ )^2 ^*_ k,z ( ( ))$ and $|T|≤ (1+ )^2δ| ( )|$ with probability at least $1- 1 N $

**Lemma .** The running time of the algorithm is $O(Nlog N + ^ -d + k( + ^ -d+1 )log )$.

**Lemma .** If the LP is feasible for a distance $r$, then $ (C,P ( _ h_j H h_j))≤ 2r$.

**Lemma .** The algorithm runs in $O(2^ g· k+z · (z+ k^d)· Nlog^3 N)$ time.

**Lemma .** If $r≥ r^*$ then the LP is feasible.

**Lemma .** $|C|≤ 2k 1-2 $.

## Proofs
**Proof.** [Proof of Lemma~ [ref] ] Starting from the input $X, Y $ of the $ SC (X, Y )$ problem we construct in polynomial time a set $P$ and a family of sets $ H $ that will serve as input to the $ $ problem. For every $x_i X$, we place a point $p_i$ on the real line at coordinate $i$. We define $ P = _ x_i X p_i$. Then for an arbitrary positive integer number $k$ such that $k≤ O(n'^2)$, we add $k$ additional points $ Q =\ q_1, …, q_k\ $ in $P$. Each point $q_j$ is placed at coordinate $2n'+j$. Overall $P= P Q $. For every set $y Y $, we create the set $ y =\ p_i x_i y\ $. Let $ Y =\ y y Y \ $. Furthermore, for every $j [k]$ we define a set $h_j=\ q_j\ $. Let $ H =\ h_j j [k]\ $. Overall $ = Y H $. Let $n=|P|=n'+k$ and $m=| H |=m'+k$, and let $γ=o(m)$ be any sublinear value. We notice that for any two elements $p,q P$, we have $ (p,q)=|p-q|$, since $p,q$ are points in $ ^1$. Given $X, Y $ we construct in polynomial time the instance of the $ $ problem $P, $ along with a number $k$ as described above. If $ Alg $ is an $(1,f- ,γ)$-approximation algorithm for the $ $ problem, then for any $z [m]$, without loss of generality, $ Alg $ returns a valid solution $C_z, H_z$ with $|C_z|=k$ and $|H_z|≤ (f- )z$ such that $ (C_z,P ( _ h H_z h))≤ γ · ^*_ k,z (P, )$. We first prove some important properties. First, if $ ^*_ k,z (P, )=0$ then by definition, $ (C_z,P ( _ h H_z h))=0$. Furthermore, if $ ^*_ k,z (P, )=0$, then without loss of generality we can assume that $C_z= Q $ and $H_z Y $. Indeed, assume that $ Alg (P, ,k,z)$ returns a valid solution $C_z', H_z'$ with $ (C_z',P ( _ h H_z' h))=0$. Then it follows that for every point $p P$, $p C_z'$ or $p _ h H_z' h$. Assume that $C_z'≠ Q $ or/and $H_z' Y $. In this case, let $C_z'=Q' P'$, where $Q' Q $ and $P' P $. Notice that $|Q'|+|P'|≤ k$. Similarly, let $H_z'= Y ' H'$, where $ Y ' Y $ and $H' H $. Notice that $| Y '|+|H'|≤ (f- )z$. We note that there is a $1:1$ relationship between $ H H_z'$ and $ P C_z'$. Using $C_z', H_z'$ we construct $C_z= Q $ and $H_z Y $ such that $ (C_z,P ( _ h H_z h))=0$. Initially, $C_z=C_z'$ and $H_z=H_z'$. For every point $q_j Q $, if $q_j C_z'$, then it means that $h_j H_z'$. By the $1:1$ relationship, there exists a point $p_i P C_z'$. Hence, we add $q_j$ in $C_z$, we remove $p_i$ from $C_z$, we remove $h_j$ from $H_z$, and we add any set $ y Y $ that contains $p_i$ in $H_z$. In the end of this process notice that $C_z= Q $, $H_z Y $, $|C_z|=|C_z|≤ k$, $|H_z|=|H_z'|≤ (f- )z$, and every point in …

**Proof.** [Proof of Lemma~ [ref] ] For simplicity, in the correctness proof we consider that $x_i^*, y_j^*$ are the values of the variables $x_i, y_j$ in the last feasible LP solution we found. We first show i). Equivalently, we will prove that if $r≥ ^*_ k,z (P, H )$ then the corresponding IP is feasible. Since $r≥ ^*_ k,z (P, H )$, there exists the optimum set of $k$ centers $C^* P$ and a family of $z$ sets $H^* H $ such that $ (C^*,P _ h H^* h)= ^*_ k,z (P, H )$. Based on this optimum solution, we construct a solution to the IP as follows. If $p_i C^*$ then we set $ x _i=1$, otherwise we set $ x _i=0$. If $h_j H^*$ we set $ y _j=1$, otherwise $ y _j=0$. Since $|C^*|=k$ and $|H^*|=z$, the first two constraints are satisfied in the IP. Next, we focus on the third family of constraints in the IP. If $p_i _ h_j H^* h_j$ then there exists a set $h_ j_i L_i$ such that $h_ j_i H^*$ and $ y _ j_i =1$. Hence $Σ_ h_j L_i y_j ≥ 1$ and the inequality is satisfied. If $p_i _ h_j H^* h_j$, then $p_i$ must be covered within distance $r$ from a center in $C^*$. Hence there exists an element $p_ i^* C^*$ such that $ (p_i,p_ i^* )≤ r$ where $ x _ i^* =1$, so the inequality is satisfied. Then, we show ii). Assume that $|H|>2fz$. Then there would be more than $2fz$ variables $y_j$ with value $y_j^*≥ 1 2f $, so $Σ_ j [m] y_j^*>z$, which is a contradiction. Next, we prove iii). By definition, the algorithm only finishes when no elements in $ Act $ remain. Every time we add an element $p_i$ in $C$ we remove all points in (current) $ Act $ within distance $2r$ from $p_i$. So all elements in $P ( _ h_j H h_j)$ are within distance $2r$ from $C$. Finally, we prove iv). Let $p_i$ be an element that does not belong in any outlier set in $H$. By definition, $Σ_ h_j L_i y_j^*≤ 1 2 $ so from the third family of inequalities in the LP, we have $Σ_ p_ B(p_i,r) x_ ^*≥ 1 2 $. We prove it by an assignment argument. Every time we add a point $p$ in $C$ we assign the weight of all elements within distance $r$ to $p$. Assume that an element $p_i$ is added in $C$ in one of the iterations of the algorithm. The weight $w_ p_i =Σ_ p_ B(p_i,r) x_ ^*$ is assigned to $p_i$. We have that $w_ p_i ≥ 1 2 $ while $Σ_ i [n] x_i^*≤ k$. Furthermore, for two different elements $p_i, p_j C$, we have that $B(p_i,r) B(p_j,r)= $ because $ (p_i,p_j)>2r$ since the algorithm removes all active elements within distance $2r$ from a selected element in $C$. Hence, each time that we add a new element $p_i$ in $C$ a weight $w_ p_ …

**Proof.** [Proof of Lemma~ [ref] ] Equivalently, we will prove that if $r≥ r^*$ then the corresponding IP is feasible. Since $r≥ r^*$, there exists the optimum set of $k$ centers $C^* P$ and a family of $z$ sets $H^* H $ such that $ (C^*,P _ h H^* h)=r^*$. Based on this optimum solution, we construct a solution to the IP as follows. If $p_i C^*$ then we set $ x _i=1$, otherwise we set $ x _i=0$. If $h_j H^*$ we set $ y _j=1$, otherwise $ y _j=0$. Since $|C^*|=k$ and $|H^*|=z$, the first two constraints are satisfied in the IP. Next, we focus on the third family of constraints in the IP. If $p_i _ h_j H^* h_j$ then there exists a set $h_ j_i L_i$ such that $h_ j_i H^*$ and $ y _ j_i =1$. Hence $Σ_ h_j L_i y_j ≥ 1$ and the inequality is satisfied. If $p_i _ h_j H^* h_j$, then $p_i$ must be covered within distance $r$ from a center in $C^*$. Hence there exists an element $p_ i^* C^*$ such that $ (p_i,p_ i^* )≤ r$ where $ x _ i^* =1$, so the inequality is satisfied.

**Proof.** [Proof of Lemma~ [ref] ] Assume that $|H|>2fz$. Then there would be more than $2fz$ variables $y_j$ with value $y_j^*≥ 1 2f $, so $Σ_ j [m] y_j^*>z$, which is a contradiction.

**Proof.** [Proof of Lemma~ [ref] ] By definition, the algorithm only finishes when no elements in $ Act $ remain. Every time we add an element $p_i$ in $C$ we remove all points in (current) $ Act $ within distance $2r$ from $p_i$. So all elements in $P ( _ h_j H h_j)$ are within distance $2r$ from $C$.

**Proof.** [Proof of Lemma~ [ref] ] Let $p_i$ be an element that does not belong in any outlier set in $H$. By definition, $Σ_ h_j L_i y_j^*≤ 1 2 $ so from the third family of inequalities in the LP, we have $Σ_ p_ B(p_i,r) x_ ^*≥ 1 2 $. We prove the lemma by an assignment argument. Every time we add a point $p$ in $C$ we assign the weight of all elements within distance $r$ to $p$. Assume that an element $p_i$ is added in $C$ in one of the iterations of the algorithm. The weight $w_ p_i =Σ_ p_ B(p_i,r) x_ ^*$ is assigned to $p_i$. We have that $w_ p_i ≥ 1 2 $ while $Σ_ i [n] x_i^*≤ k$. Furthermore, for two different elements $p_i, p_j C$, we have that $B(p_i,r) B(p_j,r)= $ because $ (p_i,p_j)>2r$ since the algorithm removes all active elements within distance $2r$ from a selected element in $C$. Hence, each time that we add a new element $p_i$ in $C$ a weight $w_ p_i ≥ 1/2$ is assigned to $p$. The total amount of weight among all elements is at most $k$, and no fraction of weight is assigned to two different elements in $C$. Hence $Σ_ p C w_p≤ k$ and $Σ_ p C w_p≥ |C|· 1 2 $ leading to $|C| 1 2 ≤ k |C|≤ 2k$.

**Proof.** [Proof of Lemma~ [ref] ] We first show that if $r≥ ^*_ k,z (P, H )$, then $ ^*_ k',z' (P', H ')≤ 10r$. We have that $ ^*_ k, z ( P , H )≤ 5r$. Notice that $P' P $ and $ H ' H $. Let $ C P $ and $ H H $ with $| C |≤ k$ and $| H |≤ z $, such that $ ( C , P _ h H )≤ 5r$. For each $h H $, if $h H '$ then we remove it from $ H $. Since $P' P $ and $ H ' H $ we have that $ ( C ,P' _ h H )≤ 5r$. However $ C $ is not a valid solution for $ _ k',z' (P', H ')$ because it is not necessary that $ C P'$ and $| C |$ might be larger than $k'$. If $X= $, then the result follows because $ P =P'$ and $z=z'$. If $X$ is not empty then it contains some balls of radius $10r$. For every $B_ X$, there exists a center $c_ C $ such that $ (c_ ,p_ )≤ 5r$. Since $B_ $ has radius $10r$ it completely covers all points assigned to $c_ $. Hence, every $B_ X$ fully contains at least one cluster. Let $ C C _ B_ X c_ $. Notice that $| C |=k'$ now. If there is still a center $c$ from $ C $ in a ball $B_ X$ then we replace $c$ with the closest point in $P'$ outside of every ball in $X$. By definition, now $ C P'$, it contains $k'$ points and every point in $P' _ h H $ is within distance $2· 5· r$ from the closest center in $ C $. Hence, $ ^*_ k',z' (P', H ')≤ 10r$. Let $C' P'$ and $H' H '$ are selected such that $|C'|≤ k'$, $|H'|≤ z'$, and $ (C',P' _ h H' h)≤ 10r$. Since $ _ c' C' (B(c',10r) P')=P' _ h H' h$ and every set $B(c',10r) P'$ contains points from at most $z$ sets, we have that $| H '|=min\ m,kz+|H'|\ =O(min\ m,kz\ )$. Every set $h H '$ contains at most $k$ points so $|P'|=O(min\ n,k^2z,km\ )$.

**Proof.** [Proof of Theorem~ [ref] ] Given a radius $r$, the running time of our procedure is $O(nk+k^2m+β_1^2+(β_2+β_3)^ )$ because of our analysis in Section~ [ref] and Section~ [ref] . In order to run a binary search over all pairwise distances we need $O(n^2log n)$ time to sort all distances. Next we focus on the approximation factors. By the proof of Lemma~ [ref] , we have that $| C |≤ 2k'$. Furthermore, $|X|=k-k'$. Hence, $|C|≤ 2k'+k-k'≤ 2k$. Similarly, from Lemma~ [ref] , $| H |≤ 2z'$; notice that $f=1$ in this case. We have that $|H_0|=z-z'$. Hence, $|H|≤ 2z'+z-z'≤ 2z$. Finally, we show that $ (C,P _ h H h)≤ 72· ^*_ k,z (P, H )$. From the proofs of Lemma~ [ref] and Lemma~ [ref] , we have that the ($ LP $ [ref] ) is feasible for every $r≥ ^*_ k,z (P, H )$ so it holds that the last radius $r'$ that our algorithm will encounter in the binary search is at most $ ^*_ k,z (P, H )$. From the proof of Theorem~ [ref] , we have that $ ( C ,P' _ h H h)≤ 36r'≤ 36 ^*_ k,z (P, H )$. Every ball $B_ '$ has radius $36r'≤ 36 ^*_ k,z (P, H )$ so the point $p_ '$ that is added to $ C $ covers all points in the ball within distance $72 ^*_ k,z (P, H )$. Hence $ (C, P _ h H h)≤ 72 ^*_ k,z (P, H )$. If $km<n$, then we can improve the $O(n^2log n)$ running time to construct a sorted array of the distances to run the binary search on. After computing the $k$-center clustering using the Gonzalez algorithm in every $h_j H $, we consider all pairwise distances among points in $ _ j [m] C_j$. This leads to sorting $O(k^2m^2)$ distances in $O(k^2m^2log(km))$ time. The approximation factor of the algorithm will increase by a constant factor. Notice that for each distance $r$, the running time of our procedure has a term $β_1^2$, where $β_1=min\ n,km\ $. Based on the comparison of $n$ and $km$ we decide whether we sort all pairwise distances or only the pairwise distances in $ _ j [m] C_j$. The result follows.

**Proof.** i) and ii) follow from Lemma~ [ref] , straightforwardly. Then we focus on iii). By definition of range tree $ $ we identify the subset of $P$ that does not belong to any hyper-rectangle $ _j $. Hence, $ Act $ is computed correctly. Every time we add an element $p_i$ in $C$ we make inactive all nodes of $ $ that contains points from $ Act $ within distance $2r$ from $p_i$. By the approximation of the query procedure on $ $ we might also make inactive some nodes that contain points within distance $2(1+ )r$. Hence all elements in $P ( _ _j h)j)$ are within distance $2(1+ )r$ from $C$.

**Proof.** Let $p_i$ be an element that does not belong in any outlier set in $ $. By definition, $Σ_ _j L_i _ n+j ≤ 1 2 $ so from the third family of inequalities in the LP, we have $Σ_ p_ S_ p_i ^ _ ≥ 1 2 - $. We prove the lemma by an assignment argument. Every time we add a point $p$ in $C$ we assign the weight of all elements within distance $r$ from $p$. Assume that an element $p_i$ is added in $C$ in one of the iterations of the algorithm. The weight $w_ p_i =Σ_ p_ S_ p_i ^ _ $ is assigned to $p_i$. We have that $w_ p_i ≥ 1 2 $ while $Σ_ i [n] _i=k$, and $Σ_ i [n], p_i Act ≤ k$. Furthermore, for two different elements $p_i, p_j C$, we have that $B(p_i,r) B(p_j,r)= $ because $ (p_i,p_j)>2r$ since the algorithm removes all active elements within distance at least $2r$ from a selected element in $C$. Hence, each time that we add a new element $p_i$ in $C$ a weight $w_ p_i ≥ 1/2- $ is assigned to $p$. The total amount of weight among all elements is $k$, and no fraction of weight is assigned to two different elements in $C$. Hence $Σ_ p C w_p≤ k$ and $Σ_ p C w_p≥ |C|· ( 1 2 - )$ leading to $|C|( 1 2 - )≤ k |C|≤ 2k 1-2 $.

**Proof.** By definition for every center in $S_ ^* $ we add a point in $S$. Recall that $S_ ^* $ is the set of $k$ centers returned by the relational $k$-center clustering algorithm from Lemma~ [ref] , so indeed $|S|≤ k$. Next, we note that in every iteration of the algorithm we visit at most $z$ points from $ ( ) M (G)$. For each such points we add at most $g$ tuples from $ $ in $T'$. Thus for every $ [ ]$, $|T_ |≤ g· z$, and hence $|T|≤ g· z$. Next, we show the approximation guarantee that holds with high probability. The probability that all tuples from $ $ that are joined to generate $S^*$ are included in $ _1$ and all tuples in $T^*$ are included in $ _2$ is $Π_ t T 1 2 Π_ s Tuples (S^*) 1 2 $, where $ Tuples (S^*) $ is the set of tuples such that are needed to joined to generate all points in $S^*$. By definition $|S^*|≤ k$ and there are $g$ tables in $ $ so $| Tuples (S^*)|≤ g· k$. Hence the probability that all tuples from $ $ that are joined to generate $S^*$ are included in $ _1$ and all tuples in $T^*$ are included in $ _2$ is bounded by $ 1 2^ g· k+z $. Since the algorithm is repeated for $Θ(2^ g· k+z log N)$ iterations, there will be at least one iteration where $S^* ( _1)$ and $T^* _2$ with probability at least $1- 1 N $. Without loss of generality, assume that this is the $ $-th iteration. Let $r^*= ^*(S^*, ( T^*))$. By the definition of the binary search on $L_∞$ distances, it will guess a value $r$ such that $r≤ r^*≤ √ d r$. We show that this iteration of the binary search indeed computes $T_ $ (a valid step of the binary search). Let $t ( ) M (G)$. We show that at least one tuple that is joined to construct $t$ belongs in $T^*$. We have $ (t,S^*)>r_ S_1 +√ d r - r_ S_1 ≥ r^*$. Hence $t ( _2)$ and indeed every tuple in $ ( ) M (G)$ contains at least one tuple outlier from $T^*$. Every time we visit a tuple $t ( T') M (G)$ we remove all the tuples in $ $ that are joined to construct it. Thus after visiting $z$ tuples we have removed at least $z$ tuple outliers, and by definition no other tuple should remain in $ ( T') M (G)$. We conclude that $T_ =T'$ is set in our algorithm. From our discussion above the set $T_ ^* $ is well-defined. By definition, all points in $ ( T_ ^* )$ are within (Euclidean) distance at most $√ d r_ ^* $ from the points in $S_ ^* $. In the end, the set $S$ is constructed by adding one point in $ ( T_ ^* )$ for every $ _ p S_ ^* _p$. So all points in $ ( T_ ^* )$ are actually within distance $2√ d r_ ^* $ from the points in $S$ …

**Proof.** The algorithm is repeated for $O(2^ g· k+z log N)$ iterations. For each iteration we run the relational $k$-center clustering from Lemma~ [ref] on $ , _1$ in $O(k^2N)$ time. Then the binary search takes $O(Nlog^2 N)$ time to generate all $O(log N)$ distances that our algorithm checks~ [cite] . For every step of the binary search, we construct the complement of the arrangement $ M (G)$ in $O(k^d)$ time~ [cite] . For every cell $ M (G)$, we run the oracle in Lemma~ [ref] in $O(Nlog N)$ time to count $| ( )|$ or get a point from $ ( )$. In total, we allow to visit at most $z$ points over $\ M (G)\ ( )$ Furthermore, notice that it is straightforward to check whether $t ( _2)$ in $O(glog N)=O(log N)$ time. The running time follows.

**Proof.** [Theorem~ [ref] ] Let $r= (P^*,P L^*(P))$, we first show that running the algorithm with $(P,L,k,z)$ and $r$ returns sets $(P',L')$ satisfying the properties mentioned in the theorem statement. Note that $P'$ is the set of cluster center points and $L'$ is the set of outlier lines. Let $P' = \ p_1,p_2,…,p_ |P'| \ $ and $L'=\ _i,…, _ |L'| \ $ be the order in which the elements were added to the sets by the algorithm. Further let $P^*$ and $L^*$ be a pair of optimal sets with $ (P^*,P L^*(P)) = ^*_ k,z (P,L)$. Let $x$ be the total number of points added to $P'$ in step (2). We now show that there are $x$ points $X=\ p^*_1,…,p^*_x\ $ in $P^*$ that satisfies $cov(p^*_i,r,P) cov(p_i,3r,P)$ for each $i [x]$. That is we show that each $p_i$ $3r$-covers all points that are $r$-covered by $p^*_i$ in the optimum solution. We will prove the claim by induction on $i [x]$. Suppose the claim is true $ j<i$. We now prove it for $i$. Since point $p_i$ $(r,U)$-hits more than $z$ lines, there is an uncovered point $u U$ that does not lie on an outlier line in $L^*$. Thus $u$ is $r$-covered by $p$ and also $r$-covered by some $p^*_i P^*$. Note that $ j<i$, $p^*_i≠ p^*_j$ -- if not $u$ must have been $3r$-covered by some $p_j$ and thus $u U$. Since the ball of radius $r$ around $p_i^*$ and $p_i$ intersect, $p_i$ $3r$-covers all points that are $r$-covered by $p^*_i$ and so $cov(p^*_i,r,P) cov(p_i,3r,P)$. Note that at the end of step (2), $|P'|≤ k$. Consider $U_2$ to be the set $U$ of uncovered points after step (2). Each uncovered point in $U_2$ either lies on an outlier line in $L^*$ or is covered by a center $p^* P^* X$ that $(r,U_2)$-hits at most $z$ lines. Thus $| (U)|<z+z.k=z(k+1)$. If $k≤ z$, then $| (U)|<z(z+1)$ and the algorithm in step (3) returns $(P',L')$ such that $ (P',P L'(P))≤ 3 ^*_ k,z (P,L)$ and $|P|≤ k$ and $|L'|< z(z+1)$ proving the Theorem. We are now left to prove the Theorem when $k>z$. Recall that $k'=k-|X|$. Step (4) identifies obvious outlier lines in $L^*$, those that need more than $k'$ centers and includes them in $L'$. Observe that no point in $U_2$ is $r$-covered by any point in $X$ and thus if a point in $U_2$ is $r$-covered in the solution, it is covered by some point in $P^* X$. If for a line $ $, $ _ k' ( (U))>r$, then points in $ (U)$ cannot be $r$-covered by a set of $k'$-points in $P^* X$ and so $ $ must be an outlier. Thus every line $ $ added to $L'$ in step (4) must be an outlier in $L^*$ as well. At the end of step (4), every line $ L  …

**Proof.** We first show that if $r≥ ^*_ k,z (P, H )$, then $ ^*_ k',z' (P', H ')≤ 18r$. We have that $ ^*_ k, z ( P , H )≤ 9r$. Notice that $P' P $ and $ H ' H $. Let $ C P $ and $ H H $ with $| C |≤ k$ and $| H |≤ z $, such that $ ( C , P _ h H )≤ 9r$. For each $h H $, if $h H '$ then we remove it from $ H $. Since $P' P $ and $ H ' H $ we have that $ ( C ,P' _ h H )≤ 9r$. However $ C $ is not a valid solution for $ _ k',z' (P', H ')$ because it is not necessary that $ C P'$ and $| C |$ might be larger than $k'$. If $X= $, then the result follows because $ P =P'$ and $z=z'$. If $X$ is not empty then it contains some balls of radius $36r$. For every $B_ ' X$, notice that the subset $B_ B_ '$ must contain at least one point $q_ B_ $ such that there exists a center $c_ C $ with $ (c_ ,q_ )≤ 9r$. This is because the set $B_ $ intersects points from more than $ z $ sets so it is not possible that all of these sets are selected as outliers. Hence, every $B_ ' X$ fully contains at least one cluster (all points assigned to center $c_ $). Let $ C C _ B_ ' X c_ $. Notice that now, $| C |=k'$. If there is still a center $c$ from $ C $ in a ball $B_ ' X$ then we replace $c$ with one point in $P' _ h H h$ outside of every ball in $X$ that had $c$ as the closest center in $ C $ (if such a point exists). By definition, $ C P'$, it contains at most $k'$ points and every point in $P' _ h H h$ is within distance $2· 9· r$ from the closest center in $ C $. Hence, $ ^*_ k',z' (P', H ')≤ 18r$. Let $C' P'$ and $H' H '$ are selected such that $|C'|≤ k'$, $|H'|≤ z'$, and $ (C',P' _ h H' h)≤ 18r$. Since $ _ c' C' (B(c',18r) P')=P' _ h H' h$ and every set $B(c',18r) P'$ contains points from at most $z$ sets, we have that $| H '|≤ min\ m,zk'+|H'|\ =O(min\ m,kz\ )$. Every set $h H '$ contains at most $k$ points so $|P'|=O(min\ n,k^2z,km\ )$.

**Proof.** We first show i). Equivalently, we prove that if $r≥ ^*_ k,z (P, H )$ then the corresponding IP is feasible. Since $r≥ ^*_ k,z (P, H )$, there exists the optimum set of $k$ centers $C^* P$ and a family of $z$ sets $H^* H $ such that $ (C^*,P _ h H^* h)= ^*_ k,z (P, H )$. Based on this optimum solution, we construct a solution to the IP as follows. If $p_i C^*$ then we set $ x _i=1$, otherwise we set $ x _i=0$. If $h_j H^*$ we set $ y _j=1$, otherwise $ y _j=0$. Since $|C^*|=k$ and $|H^*|=z$, the first two constraints are satisfied in the IP. Next, we focus on the third family of constraints in the IP. If $p_i _ h_j H^* h_j$ then there exists a set $h_ j_i L_i$ such that $h_ j_i H^*$ and $ y _ j_i =1$. Hence $Σ_ h_j L_i y_j ≥ 1$ and the inequality is satisfied. If $p_i _ h_j H^* h_j$, then $p_i$ must be covered within distance $r$ from a center in $C^*$. Hence there exists an element $p_ i^* C^*$ such that $ (p_i,p_ i^* )≤ r$ where $ x _ i^* =1$, so the inequality is satisfied. Then, we show ii). Assume that $|H|>2fz$. Then there would be more than $2fz$ variables $y_j$ with value $y_j^*≥ 1 2f $, so $Σ_ j [m] y_j^*>z$, which is a contradiction. Next, we prove iii). By definition, the algorithm only finishes when no elements in $ Act $ remain. Every time we add an element $p_i$ in $C$ we remove all points in (current) $ Act $ within distance $2r$ from $p_i$. So all elements in $P ( _ h_j H h_j)$ are within distance $2r$ from $C$. Finally, we prove iv). Let $p_i$ be an element that does not belong in any outlier set in $H$. By definition, $Σ_ h_j L_i y_j^*≤ f 1 2f = 1 2 $ so from the third family of inequalities in the LP, we have $Σ_ p_ B(p_i,r) x_ ^*≥ 1 2 $. We use an assignment argument. Every time we add a point $p$ in $C$ we assign the weight of all elements within distance $r$ to $p$. Assume that an element $p_i$ is added in $C$ in one of the iterations of the algorithm. The weight $w_ p_i =Σ_ p_ B(p_i,r) x_ ^*$ is assigned to $p_i$. We have that $w_ p_i ≥ 1 2 $ while $Σ_ i [n] x_i^*≤ k$. Furthermore, for two different elements $p_i, p_j C$, we have that $B(p_i,r) B(p_j,r)= $ because $ (p_i,p_j)>2r$ since the algorithm removes all active elements within distance $2r$ from a selected element in $C$. Hence, each time that we add a new element $p_i$ in $C$ a weight $w_ p_i ≥ 1/2$ is assigned to $p_i$. The total amount of weight among all elements is at most $k$, and no fraction of weight is assigned to two different elements in $C$. Hence $Σ_ p C w_p≤ k$ a …

**Proof.** [Sketch of the proof] The full proof is shown in Appendix~ [ref] . Given $X, Y $, we create a set $P$ and a family of sets $ $ as the input for out $ $ problem. For each $x_i X$ we create a point $p_i$ on the real line with coordinate $i$. Let $ P $ be these points on the real line. For an arbitrary value $k=o(|X|)$, let $ Q =\ q_1,…, q_k\ $ be $k$ points on the real line such that each point $q_j Q $ is a point on the real line with coordinate $2|X|+j$. The set $P$ is defined as $P= P Q $. For every set $y Y $, we create a set $ y =\ p_i x_i y\ $ and let $ Y =\ y y Y \ $. Finally, for every $q_j Q $ we create a set $ h _j=\ q_j\ $ and let $ H =\ h _j q_j Q \ $. We define $ = Y H $. We show that if $z≥ $, then $ ^*_ k,z (P, )=0$. Since $γ$ is a relative approximation factor, the solution $C_z, H_z$ returned by $ Alg $ should also satisfy $ (C_z,P ( _ h H_z h))=0$. Furthermore, for $z≥ $, without loss of generality, $C_z= Q $ and $H_z Y $, i.e., $C_z$ contains all points in $ Q $ and sets in $H_z$ are sets in $ Y $ that cover al points in $ P P$. For each $z=\ 1,…, | Y |\ $, we run $ Alg $ for $ (P, ,k,z)$ until we find the smallest $z$, say $z'$, such that $ (C_ z' ,P ( _ h H_z h))=0$. We have $z'≤ $. By definition, $|C_ z' |≤ k$, and $|H_ z' |≤ (f- ) $. For every set in $H_ z' $ there exists a corresponding set in $ Y $ and the union of them cover all points in $X$. Hence, we return a $(f- )$-approximation for the $ SC $ problem.

**Proof.** The correctness proof of our algorithm follows from the properties of the BBD tree. Every time that a node $u$ becomes inactive the points in $ _u$ do not contribute in the counts of the active nodes, i.e., the update operation of the counts is correct. In each iteration, it finds a point $p_ i^* $ with the maximum number of points in a region that contains $B(p_ i^* ,r)$ and might contain some parts of $B(p_ i^* ,(1+ )r) B(p_ i^* ,r)$. Furthermore, every time that $p_ i^* $ is added in $S$ all points in $Q$ within distance $3r$ and some within distance $3(1+ )r$ are explicitly removed from $Q$, i.e., the canonical nodes that contain these points become inactive. The analysis is the same as in~ [cite] ($3$-approximation), however we have a multiplicative $(1+ )$ error due to the BBD tree approximation. Furthermore, because of the WSPD we might not get $r= $. Instead, we guarantee that we will find at least a distance at most $(1+ ) $. Hence the overall approximation is $3(1+ )^2$ with probability at least $1- 1 N^2 $. The second part of the lemma $|T|≤ (1+ )^2δ| ( )|$ follows directly from~ [cite] .

**Proof.** Using Lemma~ [ref] we can sample $ $ points from $ ( )$ in $O(Nlog N+ N)$ time. The BBD tree $ $ is constructed in $O( )$ time and the set $ $ in $O( ^ -d )$ time. The binary search is executed for at most $O(log ( ^ -d ))=O(log )$ times. The next steps are executed at most $k$ times. We find all points that are not contained in any inactive node in $O( )$ time. For each such point $p_i$ we compute $ (B(p_i,r))$ in $O(log + ^ -d+1 )$ time. Furthermore the set $ (B(p_ i^* ,3r))$ and the update procedure is executed in $O(log + ^ -d+1 )$ time, from the definition of the BBD tree.

## Bound sentences
- The bound on $C$ and the approximation ratio hold with probability at least $1- 1 | ( )|^2 $.
