# sigmod26-298 — Differentially Private Explanations for Clusters

from=tex flag=True score=61 stmts=26 proofs=17 chars=170506
kinds: {"proposition": 4, "definition": 14, "theorem": 2, "lemma": 4, "corollary": 2}
counts: {"np_hard": 0, "lower_bound": 1, "upper_bound": 5, "big_o": 7, "omega": 1, "theta": 1, "approx_ratio": 0, "whp": 0, "regret": 0, "dp": 2, "invariant": 0, "convergence": 0, "competitive": 0, "worst_case": 0, "sketch": 0, "cost_model": 0, "cardinality": 0, "learned": 0}

## Statements
**Proposition .** The following holds for DP [cite] : ; Sequential Composition: Let $ _1: $ and $ _2: × $. Suppose $ _1$ satisfies $ _1$-DP and for every $y $, $ _2(·,y)$ satisfies $ _2$-DP (as a function of its first input). Define $ _3: $ by $ _3(D) = _2(D, _1(D))$. Then, $ _3$ satisfies $( _1+ _2)$-DP ; Parallel Composition: Suppose $ _1: $ satisfies $ _1$-DP and $ _2: $ satisfies $ _2$-DP. Let $ _1, _2 (R)$ be disjoint subsets of the input domain. Define $ ': × $ by $ '(D)=( _1(D _1), _2(D _2))$. Then, $ '$ satisfies $max _1, _2 $-DP. ; Post-processing: Suppose $ : $ satisfies $ $-DP. Then, for any function $g: $ (deterministic or randomized), The mechanism defined by $g( (D))$ satisfies $ $-DP.

**Definition .** An HBE mechanism $ (D,f)$ is an algorithm that takes a dataset $D$ and a clustering function $f: (R) C$, and outputs a global HBE ( def:global-candidate ) for the clustering of $D$ defined by $f$. We say that $ $ satisfies $ _ exp $-DP if for any clustering function $f$, for any pair of neighboring datasets $D D'$, and any set of explanations $S Range( )$, it holds $ [ (D,f) S] e^ _ exp · [ (D',f) S]$.

**Definition Pair Diversity.** For a dataset $D$ and a clustering function $f$, the diversity score of a pair of attributes $A_c, A_ c' $, where $A_c$ (respectively $A_ c' $) is a candidate attribute for explaining $D_c = t f(t)=c $ (respectively $D_ c' $), is &d(D, f, c, c' , A_c, A_ c' ) = min |D_c|, |D_ c' | × \\ & 1 & A_c ≠ A_c \\ 1 2 Σ_ a (A) _ A=a (D_c) max D_c ,1 - _ A=a (D_ c' ) max |D_ c' |,1 & A_c=A_ c'

**Definition Global Score.** Let $ λ = (λ_ , λ_ , λ_ )$ be non-negative parameters that sum to $1$. For a dataset $D$ and a clustering function $f$, define the overall quality score of an attribute combination $ $ as _ λ (D,f, ) &= λ_ · (D, f, )+λ_ · (D, f, ) \\ & + λ_ · (D, f, ) where we extend $ (D, f, ) = 1 Σ_ c C ( D, f, c, (c))$ and $ (D, f, ) = 1 Σ_ c C ( D, f, c, (c)). $

**Definition Jensen-Shannon Divergence \cite{lin1991divergence.** For two distributions $p$ and $q$ over the same domain, their Jensen–Shannon divergence is defined as \[ JSD(p,q) = H ( p+q 2 )- 1 2 H(q) - 1 2 H(q) \] where $H(·)$ is the Shannon entropy and $ p+q 2 $ is the mixture distribution of $p$ and $q$. The Jensen–Shannon distance , denoted $d_ JS $, is the square root of the Jensen–Shannon divergence.

**Corollary .** Let $D$ be a dataset, $f$ a clustering function, $A$ an attribute, and $D_ c , D_ c' $ non-empty clusters such that $ D_c D_ c' $. The pairwise-diversity score ( def:pairwise_diversity ) is \[ d(D, f, c, c' , A, A) = 1 2 _A(D_c) - |D_c| |D_ c' | _A (D_ c' ) _1 \]

**Definition Single-Cluster Score.** Let $γ = (γ_ ,γ_ )$ be a pair of non-negative parameters that sum to $1$. For a dataset $D$ and a clustering function $f: (R) C$, define the quality score $A $ as a candidate attribute for a cluster $c C$, as _ γ (D,f,c,A) = γ_ · (D,f,c,A) +γ_ · (D,f,c,A) .

**Definition Global Diversity.** For a dataset $D$ and a clustering function $f: (R) C$, define the diversity score of an attribute combination $ $ as (D, f, ) = 1 2 Σ_ c,c' C d(D, f,c,c', (c), (c')) where the sum is over all distinct pairs of cluster labels.

**Definition Low Sensitivity Interestingness.** For a dataset $D$ and a clustering function $f: (R) C$, define the interestingness of an attribute $A$ for explaining the cluster $D_c = t D f(t)=c $ as $$ ( D, f, c, A) = 1 2 Σ_ a (A) _ A=a (D_c) - |D_c| D · _ A=a (D) . $$

**Definition $\eps$-Differential Privacy (DP) \cite{dwork2006differential, dwork2014algorithmic.** A randomized mechanism $ $ is said to satisfy $ $-DP if for any neighboring datasets $D D'$ and any set of possible outputs $S Range( )$, \[ [ (D) S] e^ · [ (D') S], \] where the probability is over the randomness of $ $.

**Definition The Exponential Mechanism (EM) \cite{mcsherry2007mechanism.** Given $D $, a set of candidates $ $, a quality function $q: × $, and a privacy parameter $ $, the exponential mechanism $ _E$ selects and outputs $r $ with probability proportional to $exp ( · q(D,r) 2 _q )$

**Corollary .** For a dataset $D$ and a clustering function $f: (R) C$, a cluster label $c$, and an attribute $A$, the interestingness score ( def:low-sens-interest ) is $$ ( D, f, c, A) = 1 2 _A(D_c) - |D_c| D _A(D) _1 $$

**Definition Low Sensitivity  Sufficiency.** For a dataset $D$ and a clustering function $f: (R) C$, define the sufficiency of an attribute $A$ for explaining the cluster $D_c$ as $$ ( D, f, c, A) = Σ_ a _ D_c (A) _ A=a (D_c)^2 _ A=a (D) . $$

**Theorem .** Given a clustering function $f: (R) C$, number of candidates $k$, privacy parameters $ _ CandSet , _ TopComb , _ Hist $, alg:gen_global_explanation is $( _ CandSet + _ TopComb + _ Hist )$-DP.

**Lemma .** For $i=1, , m$ let $α_i $ be a scalar and $f_i: $ be a function with sensitivity $ _i$. Then, the function $f = Σ_ i=1 ^m α_i · f_i$ has sensitivity bounded by $Σ_ i=1 ^m α_i _i$.

**Definition Sensitivity~\cite{dwork2014algorithmic.** For a set of candidates $ $, Let $q: × $ be a quality function that given $D $, defines a score for every $r $. The sensitivity of $q$ is $$ _q = _ r _ D D' q(D)-q(D') $$

**Definition Global HBE candidate \cite{TabEE.** Given $D$, clustered into $ D_c c C $, a global HBE candidate is a set $ e_c c C $, where $e_c$ is a single-cluster HBE candidate for $D_c$.

**Definition Single cluster HBE candidate \cite{TabEE, FEDEX.** For a dataset $D$ and a cluster label $c C$, a single-cluster HBE candidate $e_c$ is a tuple $(c, A, _ A (D D_c), _ A (D_c))$ where $A $.

**Definition Neighboring Datasets~\cite{dwork2006differential.** Two datasets $D$ and $D'$ are called neighboring (denoted $D D'$) if $D'$ can be obtained from $D$ by removing or adding one tuple.

**Lemma .** For two clusters $D_c,D_ c' D$ and attributes $A_c, A_ c' $, it holds that $d(D, f, c, c', A_c, A_ c' ) [0, min |D_c|, |D_ c' | ] $

**Lemma .** Let $a,b $ such that $b>0$ and $0 a b$. Then [label=( ),font= ] ; $ a^2 b - (a+1)^2 b+1 1 $, ; $ a^2 b - a^2 b+1 1 $

**Theorem \cite{mcsherry2007mechanism.** The exponential mechanism satisfies $ $-DP. Moreover, \[ [ _E(D, ,q, ) max_ r q(D,r) - 2 _q (ln + t ) ] e^ -t . \]

**Proposition .** The sensitivity of $ (D,f, ) $ is at least $ 1 2 $ and its range is $[0, C ]$.

**Proposition .** The sensitivity of $d_ JS $ is at least $ 1 2 $ and its range is $[0,1]$.

**Lemma .** The range of $ ( D, f, c, A) $ is $ [0, |D_c|]$.

**Proposition .** $d$ has sensitivity $1$.

## Proofs
**Proof.** [Proof Sketch] The execution of alg:topk-single satisfies $ _ CandSet $-DP by prop:topk-single privacy . Since the function $ _ λ $ has sensitivity $1$ ( prop:global_score-sensitivity ), the execution of the exponential mechanism in gen_global_explanation:line 5 guarantees $ _ TopComb $-DP, applying thm:exponential_mech . We assume that each execution of $ _ hist (·, )$ satisfies $ $-DP. Therefore, by sequential composition, the computation of $ h _A$ for all $A '$ satisfies overall $ _ Hist /2$-DP. Since the clusters are disjoint, the computation of all noisy histograms $ h ^c$ satisfies overall $ _ Hist /2$-DP by parallel composition. Note that the computation of each $ h^ -c $ in gen_global_explanation:line 13 is post-processing, and therefore incurs no additional privacy loss. Overall, by sequential composition, the entire algorithm guarantees $( _ CandSet + _ TopComb + _ Hist )$-DP.

**Proof.** Let $D D'$ be two neighboring datasets. By the triangle inequality, f(D) - f(D') &= Σ_ i=1 ^m α_i · f_i(D) - Σ_ i=1 ^m α_i · f_i(D') \\ & Σ_ i=1 ^m α_i · f_i(D) -f_i(D') \\ & Σ_ i=1 ^m α_i _i.

**Proof.** Since the range bound is standard (see, e.g., [cite] ), we focus on the sensitivity lower bound. Let $ D$ be a dataset of size $n 1$, and $A$ an attribute. Suppose that for all tuples $t D$ it holds that $t[A]=a$ for some $a (A)$. That is, $ _ A=a (D)=n$. Let $D_c D$ be a cluster of size $1$. In this case $ ( _A(D), _A(D_c)) = 0$, as $ _A(D)$ and $ _A(D_c)$ define the same distribution (the value $a$ has probability $1$). Now, let $ D' = D t' $ and $D'_c = D_c t' $ for a tuple $t'$ with $t'[A]=a'$ for $a'≠ a$. We now have 2· ( _A(D'), _A(D'_c)) &= _ A=a (D') D' - _ A=a (D'_c) D'_c \\ & + _ A=a' (D') D' - _ A=a' (D'_c) D'_c \\ & = n n+1 - 1 2 + 1 n+1 - 1 2 \\ & = n n+1 - 1 2 + 1 2 - 1 n+1 \\ & = 1 - 2 n+1 Therefore, ( _A(D'), _A(D'_c)) = 1 2 - 1 n+1 Since sensitivity is defined as the supremum over all datasets and dataset sizes, the lemma follows.

**Proof.** The proof for the range bound can be found in [cite] . We proceed with the sensitivity analysis. Let $D$ be a dataset of size $n$, and $A$ an attribute. Suppose that for all tuples $t D$ it holds that $t[A]=a$ for some $a (A)$. Let $D_c = t $ be a cluster of size $1$. In this case, $ d_ JS (P_A(D),P_A(D_c)) = 0$, as $P_A(D)$ and $P_A(D_c)$ are the same distribution (the value $a$ has probability $1$). Now, let $D' = D t' $ and $D_c' = D_c t' $ for a tuple $t'$ with $t'[A]=a'$ where $a'≠ a$. We now have \[ P_A(D') = a & w.p $ n n+1 $ \\ a' & w.p $ 1 n+1 $ \\ \] and \[ P_A(D_c') = a & w.p $ 1 2 $ \\ a' & w.p $ 1 2 $ \] therefore, the mixture distribution satisfies \[ P_A(D') +P_A(D_c') 2 = a & w.p $ n 2(n+1) + 1 4 $ \\ a' & w.p $ 1 2(n+1) + 1 4 $ \] Since all three distributions are supported on $ a,a' $, we have \[ JSD(P_A(D'),P_A(D_c')) = H_b ( 1 2(n+1) + 1 4 )- H_b ( 1 n+1 ) + H_b ( 1 2 ) 2 \] where $H_b$ is the binary entropy function. Note that by continuity of $H_b$ we have _ n ∞ JSD(P_A(D'),P_A(D_c')) &= H_b ( 1 4 ) - 1 2 \\ & ≈ 0.311 In particular, for sufficiently large $n$, $JSD(P_A(D'),P_A(D_c')) > 0.3$, and hence $d_ JS (P_A(D'),P_A(D_c')) > 1 2 $. Therefore, we find that $d_ JS $ has sensitivity greater than $ 1 2 $.

**Proof.** Let $f: (R) C$ be a clustering function, and $c C$ be a cluster label. Let $A $ and $ D D'$ be neighboring datasets such that $ D' = D t $. Denote $a = t[A]$, and let $ _a = _A(t)$ be the $ (A) $-dimensional histogram with $1$ in its $a$'th entry and $0$ elsewhere. Let $D_c$ (respectively, $D'_c$) denote the set of tuples in $D$ (respectively, $D'$) that are mapped to $c$ by the function $f$, and note that $D'_c$ is either $D_c$ or $D_c t $. We first consider the case that $D_c = D'_c$. By lemma:interest_vector and the triangle inequality, we have & ( D',f, c, A) - ( D,f, c, A) \\ &= 1 2 _A(D_c) - |D_c| D' _A(D') _1- _A (D_c) - |D_c| D _A(D) _1 \\ & 1 2 |D_c| D _A(D)- |D_c| D' _A(D') _1 Substituting $ _A(D') = _A(D) + _a$ into the inequality above and applying the triangle inequality again, we obtain &= 1 2 |D_c| D +1 _a - |D_c| D ( D +1) _A(D) _1 \\ & 1 2 ( _a _1 + 1 D _A(D) _1 ) where for the equality, we used the fact that $ D' = D + 1$, which implies $ D_c D - D_c D' = D_c D ( D + 1) . $ For the inequality, we also bound $ D_c ≤ D + 1$. Next, we substitute $ _a _1 =1$ and $ _A(D) _1=|D|$ to obtain = 1 2 (1 + D D ) = 1 Now, consider the case that $D'_c = D_c t $. Applying the triangle inequality, we have & ( D',f, c, A) - ( D,f, c, A) \\ & 1 2 _A(D'_c) - D_c' D' _A(D')- _A(D_c)+ |D_c| D _A(D) . Substituting $ _A(D') = _A(D) + _a$ and $ _A(D'_c) = _A(D_c) + _a$, and rearranging, we obtain &= 1 2 _a - D'_c D' _A(D) - D'_c D' _a + |D_c| D _A(D) \\ & = 1 2 ( 1 - D'_c D' ) _a - ( D'_c D' - |D_c| D ) _A(D) \\ Recall that $ D'_c =|D_c|+1$ and $ D' = D +1$. Therefore, $ D'_c / D' - |D_c| / D =( D -|D_c|) / ( D ( D +1))$, which is non-negative, as $|D_c| D $. Since $ D'_c D' $ holds as well, we have & 1 2 ( 1 - D'_c D' ) _a _1 + 1 2 ( D -|D_c| D ( D +1) ) _A(D) \\ & 1 2 ( 1 - D'_c D' ) _a _1 + 1 2 ( 1 D +1 ) _A(D) .\\ Since $ _a _1 =1$ and $ _A(D) _1=|D|$, we conclude & = 1 2 ( 1 - D'_c D' ) + \ 1 2 ( 1 D +1 ) |D| \\ & 1 2 + 1 2 = 1. For the range upper bound, we use lemma:interest_vector to obtain ( D,f, c, A) &= 1 2 _A(D_c)- |D_c| D _A(D) _1 \\ & 1 2 ( _A(D_c) _1 + |D_c| D _A(D) _1 ) \\ &= D_c

**Proof.** To see that the range is $[0,1]$, note that by definition in~ [ref] , $m^s_ (t) [0,1]$ for every tuple~$t$, and~ [ref] averages these values across all tuples. We proceed with the sensitivity analysis. Consider a dataset $D= t_1 $ and two clusters $D_1= t_1 $ and an empty cluster $D_2= $. Let $ $ be an attribute combination, and suppose denote $ (i)=A$ for $i=1,2$. Let us denote $a=t_1[A]$. In this case $ ( D, f, 1, A)=1$, as the value $a$ appears only inside the cluster $D_1$. Hence, using the the equality from item (1) of prop:suff-sensitivity , (D, , f)= 1 D Σ_ c C (D, f, c, (c)) = 1. Now, suppose a tuple $t_2$ is added to the cluster $D_2$. That is, $D'= t_1,t_2 $, $D'_1= t_1 $ and $D'_2= t_2 $. Suppose further that $t_2[A]=a$. In this case, for $i 1,2 $ (D', f, i, A) &= Σ_ b _ D'_i (A) ( _ A=b (D'_i))^ 2 _ A=b (D') = 1 2 . Hence, we find that (D', , f) &= 1 D' Σ_ c 1,2 (D', f, c, (c)) = 1 2 · ( 1 2 + 1 2 )\\ & = 1 2 .

**Proof.** [Proof of (1) ] Let $ $ be an attribute combination. Denote $A_c= (c)$. Observe that the right-hand-side of eq:suff-old-local depends only on the cluster assignment $f(t)$, therefore it is equal across all tuples $t$ of the same cluster. Thus, plugging in the definition of $r(t',A)$, Σ_ t D m^s_E(t) = Σ_ c C D_c · Σ_ t' D_c _ A_c=t'[A_c] (D_c) _ A_c=t['A_c] (D) Σ_ t' D _ A_c=t'[A_c] (D_c) _ A_c=t['A_c] (D) . Let us consider each cluster separately, and observe that each value $ _ A_c=a (D_c) / _ A_c=a (D)$ appears in the numerator exactly $ _ A_c=a (D_c)$ times, and $ _ A_c=a (D)$ times in the denominator. Hence, by changing the order of summation & Σ _ t D_c _ A_c=t'[A_c] (D_c) _ A_c=t['A_c] (D) Σ _ t' D _ A_c=t'[A_c] (D_c) _ A_c=t['A_c] (D) \\ & = Σ _ a _ D_c (A) _ A_c=a (D_c)· _ A_c=a (D_c) _ A_c=a (D) Σ _ a _D(A_c) _ A_c=a (D)· _ A_c=a (D_c) _ A_c=a (D) \\ & = Σ _ a _ D_c (A_c) ( _ A_c=a (D_c))^2 _ A_c=a (D) Σ _ a _D(A_c) _ A_c=a (D_c) Since the denominator equals $|D_c|$, the following equality holds: & = 1 |D_c| Σ_ a _ D_c (A_c) ( _ A_c=a (D_c))^ 2 _ A_c=a (D) \\ & = 1 |D_c| ( D, f, c, A). The proposition follows by substituting the equality into eq:equiv_form_suff .

**Proof.** We have ( D, f, c, A) &= Σ_ a _ D_c (A) _ A=a (D_c)^2 _ A=a (D) \\ & Σ_ a _ D_c (A) _ A=a (D_c)\\ & = |D_c| where the inequality holds since $ _ A=a (D_c) _ A=a (D)$.

**Proof.** For item (i) , observe that our assumption $a b$ implies that $ (a+1)^2 b+1 a^2 b $. Indeed, a^2 b &= a^2(b+1) b(b+1) \\ &= a^2b+a^2 b(b+1) a^2b+2ab+b b(b+1) = b(a^2+2a+1) b(b+1) = (a+1)^2 b+1 . where the in the inequality we used $a^2 ab 2ab+b$. Now, we have (a+1)^2 b+1 - a^2 b &= b(a+1)^2-a^2(b+1) b(b+1) \\ &= 2ab+b-a^2 b(b+1) b^2+b b(b+1) = 1 where the inequality follows from $2ab-a^2 b^2$ which holds for any $a,b $. For (ii) , note that $$ a^2 b - a^2 b+1 = a^2 b(b+1) < 1$$ as $a b$.

**Proof.** [Proof of prop:suff-sensitivity item (2) ] Let $f: (R) C $ be a clustering function, and $c C$ be a cluster label. Fix an attribute $A$. Let $ D D'$ be two neighboring datasets such that $ D'= D t $. Let $D_c$ (respectively, $D'_c$) denote the set of tuples in $D$ (respectively, $D'$) that are mapped to $c$ by the function $f$, and note that $D'_c$ is either $D_c$ or $D_c t $. First, note that if $D_c$ is empty, \[ ( D, f, c, A)- ( D', f, c, A) 1. \] Indeed, in this case $ D'_c 1$, and by claim:suff_properties we have $ ( D, f, c, A) = 0$ and $ ( D', D'_c, A) 1$. Second, if $ _ A=t[A] (D)=0$, then clearly $ _ A=t[A] (D_c)=0$ and $ _ A=t[A] (D'_c) 1$. In this case & ( D, f, c, A)- ( D', f, c, A) \\ &= Σ_ a _ D_c (A) _ A=a (D_c)^2 _ A=a (D) - Σ_ a _ D'_c (A) _ A=a (D'_c)^2 _ A=a (D') \\ &= _ A=t[A] (D'_c)^2 _ A=t[A] (D') \\ & 1. where the second equality holds because all summands are identical in both sums, except for the term corresponding to $a = t[A]$, which appears only in the second sum. The inequality holds since the numerator is at most $1$. Hence, we assume that $D_c$ is not empty and that $ _ A=t[A] (D)>0$. We consider two cases according to whether or not $t$ is added to $D_c$. First assume that $D'_c = D_c$. We have & ( D, f, c, A)- ( D', f, c, A) \\ &= _ A=t[A] (D_c)^2 _ A=t[A] (D) - _ A=t[A] (D_c)^2 _ A=t[A] (D)+1 1 where in the equality we have used that $ _ A=a (D_c)= _ A=a (D'_c)$ for all $a$ since $D'_c=D_c$. We also used that the only summand in the definition of $ $ that changes is the one corresponding to $t[A]$. The inequality uses item (i) of lem:suff . If $D'_c = D_c t $, we use item (ii) of lem:suff to obtain & ( D, f, c, A)- ( D', f, c, A) \\ &= _ A=t[A] (D_c)^2 _ A=t[A] (D) - ( _ A=t[A] (D_c)+1)^2 _ A=t[A] (D)+1 1.

**Proof.** Let $D$ be a dataset of size $n$ and $f: (R) C$ be a clustering function such that $D_1 = t_1 $ is a cluster of size 1. Let $A$ be an attribute and suppose that there exists $a (A)$ such that $t[A]=a$ for all tuples $t D$. Let $ $ be an attribute combination mapping all cluster labels to $A$. In this case, we have $ExpBy( , A) = C$. Moreover, for any $c,c' C$ we have that $ ( _A(D_ c ), _A(D_ c' )) = 0$, as the distributions of values of the column $A$ is identical among all clusters. Therefore, for any permutation $p Perm(C)$, we find that $PermDiv_A(p) = 0$. Hence, $ (D,f, ) = 0$. Now, suppose a new tuple is added to cluster 1. Denote $D' = D t_2 $ and $D_1' = t_1, t_2 $. Suppose further that $t_2[A]=a'$ for $a'≠ a$. Now, for every $c≠ 1$ we have ( _A(D_ 1 ), _A(D_ c ))= 1 2 and for every $c,c' C 1 $ we have ( _A(D_ c ), _A(D_ c' ))=0 as for other clusters the distribution of values is unchanged. Hence, for any permutation $p Perm(C)$, we find that \[ PermDiv_A(p) = Σ_ i=1 ^ C min_ j<i ( _A(D_ p(i) , _A(D_ p(j) )) = 1 2 , \] where we used that exactly one of the summands has the form ( [ref] ) and the rest have the form ( [ref] ). Therefore, we conclude that $ (D,f, ) =1/2$. For the range bound, observe that $ (D, f, )$ is maximized when all pairwise distances equal $1$. This can occur, for instance, when $ $ maps each cluster to a distinct attribute. Alternatively, when any two clusters assigned with the same attribute $A$ satisfy $ ( _A(D_ 1 ), _A(D_ c )) = 1$. Assuming this is the case, we have \[ PermDiv_A(p) = Σ_ i=1 ^ ExpBy( , A) 1 = ExpBy( , A) . \] Thus, (D,f, ) &= Σ_ A Σ_ p Perm(ExpBy( , A)) PermDiv_A(p) ExpBy( , A) ! \\ &= Σ_ A Σ_ p Perm(ExpBy( , A)) ExpBy( , A) ExpBy( , A) ! \\ &= Σ_ A ExpBy( , A) \\ &= C .

**Proof.** If one of the two clusters is empty, or if $A_c ≠ A_ c' $, then the claim trivially holds. Hence, we assume that both are not empty and that $A_c= A_ c' = A$. In this case & 1 2 Σ_ a (A) _ A=a (D_c) max D_c ,1 - _ A=a (D_ c' ) max |D_ c' |,1 \\ & 1 2 Σ_ a (A) _ A=a (D_c) D_c + 1 2 Σ_ a (A) _ A=a (D_ c' ) D_ c' \\ & = 1 2 + 1 2 = 1 and therefore $d(D, f, c, c', A_c, A_ c' ) min |D_c|, |D_ c' | $.

**Proof.** Let $f: (R) C$ be a clustering function, and $c,c' C$ be two cluster labels. Fix attributes $A_c, A_ c' $, and let $ D D'$ be neighboring datasets such that $ D' = D t $. Let $D_c$ and $D_ c' $ (respectively, $D'_c$ and $D'_c$) denote the sets of tuples in $D$ (respectively, $D'$) that are mapped to $c$ and $c'$ by the function $f$. First, observe that when $A_c ≠ A_ c' $, we have $d(D, f, c, c', A_c, A_ c' ) = min |D_c|, |D_ c' | $, which changes by at most $1$ when we add or remove one tuple from $D$. Hence, we proceed with the assumption that $A_c=A_ c' = A$. To simplify notation, we let $d(D, c, c', A) =d(D, f, c, c', A, A)$. If $t$ has not been added to either cluster, i.e, $D_c=D'_c$ and $D_ c' =D'_ c' $, then clearly $$ d(D, c, c', A)- d(D', c, c', A) = 0.$$ Otherwise, consider the case that $t$ has been added to one of the clusters. Without loss of generality, suppose that $D'_ c' = D_ c' t $ and $D_c' = D_c$. If either of $D_c$ or $D_ c' $ is empty, then by claim:divers-upperbound , we have $d(D_c,D_ c' , A) = 0$, and $d(D_c,D'_ c' , A) 1$, and so \[ d(D, c, c', A)- d(D', c, c', A) 1. \] Hence, we proceed with the assumption that both are not empty. Denote $a = t[A]$, and let $ _a = _A(t)$ be the $ (A) $-dimensional histogram with $1$ in its $a$'th entry and $0$ elsewhere. First, consider the case that $|D_c| |D_ c' |$. Recalling that $|D'_ c' |= |D_ c' |+1$, we have & d(D, c, c', A)- d(D', c, c', A) \\ &= 1 2 _A(D_c) - |D_c| |D_ c' | _A (D_ c' ) _1 - _A (D_c) - |D_c| |D_ c' |+1 _A(D'_ c' ) _1 By the triangle inequality, & 1 2 _A(D_c) - |D_c| |D_ c' | _A (D_ c' ) - _A (D_c) + |D_c| |D_ c' |+1 _A(D'_ c' ) Substituting $ _A(D'_ c' )= _A(D_ c' ) + _a$ into the inequality above, we obtain &= 1 2 |D_c| |D_ c' |+1 _a - ( |D_c| |D_ c' | - |D_c| |D_ c' |+1 ) _A (D_ c' ) _1 \\ Applying the triangle inequality and noting that $ |D_c| |D_ c' | - |D_c| |D_ c' |+1 = |D_c| |D_ c' |(|D_ c' |+1) $, we have & 1 2 ( _a _1 + |D_c| |D_ c' |(|D_ c' |+1) (D_ c' ) _1 ) substituting $ _a _1 =1$ and $ (D_ c' ) _1=|D_ c' |$, = 1 2 (1 + |D_c|· |D_ c' | |D_ c' |(|D_ c' |+1) ) 1 where we have also used our assumption that $|D_c| |D_ c' |$. Now, consider the case that $|D_c| > |D_ c' |$, and therefore also $|D_c| |D'_ c' | = |D_ c' |+1$. We have & d(D, c, c', A)- d(D', c, c', A) \\ &= 1 2 |D_ c' | |D_c| _A(D_c) - _A(D_ c' ) _1 - |D_ c' |+1 |D_c| _A (D_c) - _A(D'_ c' ) _1 By the triangle inequality, & 1 2 |D_ c' | |D_c| _A(D_c) - _A (D_ c' ) - |D_ c' |+1 |D_c| _A (D_c) + _A(D'_ …

**Proof.** The sensitivity bound follows from lem:pairwise-diverse-sensitivity using lemma:convex-comb , as $ $ is a convex combination of sensitivity-1 functions. For the range bound, note that $d(D, f, c, c', A_c, A_ c' )$ attains its upper-bound ( claim:divers-upperbound ), in particular, when $A_c≠ A_ c' $. Hence, Let $ $ be an attribute combination assigning a different attribute for each cluster. It follows that $ $ is maximized, attaining value 1 2 Σ_ c,c' C min |D_c|, |D_ c' | . Now, let $|D_ c_1 | |D_ c_ |C| |$ be an ordering of the clusters by increasing size. For each $i=1, , |C|$, the value $|D_ c_i |$ appears as a summand in ( [ref] ) $|C|-i$ times, each for a coupling of $c_i$ with $c_j$ for $j < i$. By changing the order of summation, we obtain that ( [ref] ) equals 1 2 Σ_ i=1 ^ ( -i) D_ c_ i

**Proof.** The proposition follows directly from lemma:convex-comb and the sensitivity bounds of the sufficiency and interestingness functions ( prop:interest-sensitivity , prop:suff-sensitivity ), as $ _γ$ is a convex combination of the two.

**Proof.** The proposition follows directly from lemma:convex-comb and the sensitivity bounds of the sufficiency, interestingness and diversity functions ( prop:interest-sensitivity , prop:suff-sensitivity , prop:sens-div ), as $ _λ$ is a convex combination of the three. For the range bounds, note that $ 1 C Σ_ c C ( D, f, c, A)$ and $ 1 C Σ_ c C ( D, f, c, A)$ both have range $[0, 1 C Σ_ c C D_c ]$. The range upper bound for $ $, $R_ $, is given by $ prop:sens-div $.

**Proof.** (1) Privacy. For each cluster, the distribution of the selected top-$k$ is equivalent to iteratively applying $k$ exponential mechanisms [cite] , where each satisfies $ = _ CandSet /( · k)$. Overall, we have $ $ applications of Top-$k$, hence the output distribution is equivalent to $ · k$ exponential mechanisms. by sequential composition ( prop:composition ), alg:topk-single satisfies overall $ _ CandSet $-DP. (2) Utility Bound. Let $c C$, and $ _ $ be the set of remaining attributes after the $( -1)$-th selection for this cluster. The distribution over the selected sequence $A^ (1) _c, ,A^ (k) _c$ is equal to applying $k$ exponential mechanisms [cite] where $A_c^ ( ) $ is selected from $ _ $. Note that we always have $max_ A _ (c,A) _c^ ( ) $. Hence, for $ = _ CandSet /( · k)$, we apply the utility theorem of exponential mechanism (Theorem 3.11 in [cite] ) to obtain & [ (c,A_c^ ( ) ) _c^ ( ) - 2 (ln +t) ] \\ & [ (c,A_c^ ( ) ) max_ A _ (c,A) - 2 (ln _ +t) ] e^ -t where we have used that $ _ 1$.

## Bound sentences
- By the triangle inequality, [display] Interestingness We prove next the high sensitivity of the total variation distance interestingness measure [ref] , restated below for convenience. [display] Since the range bound is standard (see, e.g., [cite] ), we focus on the sensitivity lower bound.
- Since $ D'_c D' $ holds as well, we have [display] Since $ _a _1 =1$ and $ _A(D) _1=|D|$, we conclude [display] For the range upper bound, we use lemma:interest_vector to obtain [display] Sufficiency We revisit the notion of sufficiency from prior work and provide its sensitivity analysis.
- If either of $D_c$ or $D_ c' $ is empty, then by claim:divers-upperbound , we have $d(D_c,D_ c' , A) = 0$, and $d(D_c,D'_ c' , A) 1$, and so \[ d(D, c, c', A)- d(D', c, c', A) 1. \] Hence, we proceed with the assumption that both are not empty.
- For the range bound, note that $d(D, f, c, c', A_c, A_ c' )$ attains its upper-bound ( claim:divers-upperbound ), in particular, when $A_c≠ A_ c' $.
- The range upper bound for $ $, $R_ $, is given by $ prop:sens-div $.
