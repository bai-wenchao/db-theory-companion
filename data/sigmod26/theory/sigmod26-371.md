# sigmod26-371 — The Space-Time Complexity of Sum-Product Queries

from=tex flag=True score=71 stmts=30 proofs=20 chars=255835
kinds: {"definition": 14, "theorem": 1, "conjecture": 1, "lemma": 18}
counts: {"np_hard": 0, "lower_bound": 9, "upper_bound": 4, "big_o": 113, "omega": 1, "theta": 0, "approx_ratio": 0, "whp": 0, "regret": 0, "dp": 0, "invariant": 0, "convergence": 0, "competitive": 0, "worst_case": 7, "sketch": 0, "cost_model": 0, "cardinality": 11, "learned": 0}

## Statements
**Definition .** A recursive pseudo-tree (RPT) of an SPQ with input variables $(Q( X ), I )$ is recursively defined as follows: ; Base case. A PTCR $(P,C, )$ of $(Q( X ), )$ is an RPT of $(Q( X ), )$. ; Recursion. Let $(P,C, )$ be a PTCR of $(Q( X ), )$, and let $A_1, , A_l$ be vertices in $ V (P) $, such that $A_i (A_j) \, i≠ j$. Let $Q_i$ be the subquery of $Q( X )$ restricted to the variables $ (A_i) (A_i)$ and with head variables $ (A_i) (A_i)$. Let $( _i, _i, _i)$ be RPTs of $(Q_i, (A_i))$. Then, $( , , ):=((P,( _i)_i), (C,( _i)_i), ( , ( _i)_i))$ is an RPT of $(Q( X ), )$. The class of query plan $ (Q)$ consist of RPTs $( , , )$ of $(Q, )$ and their space and time exponents are defined recursively via: s( , , ) &= max(max_ A _i (A_i) ^*( (A) (A));max_i s( _i, _i, _i)),\\ t( , , ) &= max(max_ A _i (A_i) ^*( (A) (A));max_i t( _i, _i, _i)).

**Definition .** Fix a query $Q$ and cardinality constraints $(cc)_i$, and let $ $ be a class of query plans for evaluating SPQs. The class of plans $ $ consists all pairs $(TD, )$, where $TD$ is a tree decomposition of $Q$, and $ $ is a function that maps each vertex $v$ in $TD$ to a plan $$ ^v (Q^v, ((cc_i / m)_i, ( ^*( Z ^w, Q, (cc_i)_i)/m)_w)$$ where $$m=max(\ cc_i i\ \ ^*( Z ^w, Q, (cc_i)_i) w\ ).$$ I.e., $ ^*( Z ^w, Q, (cc_i)_i)$ is the maximal possible size of the relation $Q^w( Z ^w)$ and $ ^v$ respects these in normalized cardinality constraints. The space and time exponents are then s(TD, ) = max_v s( ^v)· m, t(TD, ) = max_v t( ^v)· m.

**Definition (Plan Domination)..** Let $ _1, _2$ be two plans for the same query $Q$. We say $ _1$ improves over $ _2$, or that $ _1$ dominates $ _2$, denoted $ _1 _2$, if $t( _1) ≤ t( _2)$ and $s( _1) ≤ s( _2)$. We say that $ _1$ strictly dominates $ _2$, denoted $ _1 _2$, if $ _1 _2$ but not vice versa. A class of plans $ _1$ improves over ( dominates ) another class $ _2$, denoted $ _1 _2$, if for every query $Q$, it holds that $ \, _2 _2(Q)$, $ \, _1 _1(Q)$ such that $ _1 _2$. Notice that $ $ over classes of plans forms a preorder. We say that $ _1$ strictly dominates $ _2$, denoted $ _1 _2$, if $ _1 _2$ holds but not vice versa.

**Lemma .** Let $( , , )$ be a RPT of the SPQ $Q$ and $A V ( )$ arbitrary. Then, during the execution of Algorithm [ref] , the function call $ solve (A, x )$ returns $$ Q[ (A) | x [ (A)] ]( (A)) .$$ Furthermore, after the call to $ fillCache (A, x [ (A) (A)])$ the cache $M_A$ is of the form \ ( , ) Q[ (A) | , ]( (A)) Q[ (A) | x [ (A)] ] \ . where $ = x [ (A)]$. Further, the function call $ solveReplaced (A,A', x )$ returns $$ Q[ (A') | x [ (A')] ]( (A')) .$$ Furthermore, after Line [ref] , the cache $M_A$ is of the form \ ( , ) Q[ (A') | , ]( (A')) Q[ (A') | x [ (A')] ] \ . where $ = x [ (A)]= x [ (A)]$.

**Definition \cite{DBLP:conf/ijcai/FreuderQ85,DBLP:series/synthesis/2019Dechter.** A pseudo-tree (PT) of $Q$ is a directed tree $P=( V ,E)$, satisfying: ; Every atom $R_i( X _i)$ is contained in a branch: formally, $ A V$ such that $ X _i (A)$. The term pseudo-tree was introduce by Freuder and Quinn in the context of constraint optimization~ [cite] , and studied extensively by Dechter~ [cite] . Pseudo-trees are a generalization of normal trees , also called Tr\'emaux trees used in graph theory~ [cite] , which are pseudo-trees where every tree edge is also an edge in the graph.

**Lemma .** Let $( , , )$ be a RPT of the SPQ $Q$ and $A V ( )$ arbitrary. Then, for a run of Algorithm [ref] , let $ solve (A, x _1), solve (A, x _2)$ be two calls where the first happens before the latter. Then, $ x _1[ (A)]$ comes lexicographically before $ x _2[ (A)]$ (or they are equal). Further, let $ solveReplaced (A,A', x '_1), solveReplaced (A,A', x _2)$ be two calls where the first happens before the latter. Then, $ x '_1[ (A)]$ comes lexicographically before $ x '_2[ (A)]$ (or they are equal).

**Lemma .** Let $( , , )$ be a RPT of the SPQ $Q$ and $A V ( )$ arbitrary. Let $Q( X ) _i R_i( X _i)$ be a SPQ and $( , , )$ a RPT of $Q( X )$. Then, for a run of Algorithm [ref] , the function $ solve (A, x )$ is only called for arguments $A V ( )$ that do not replace a $A'$ and $ x ^ (A) $ where $ x [ (A)] Q[ (A) ] $. Further, the function $ solveReplaced (A,A', x )$ is only called for arguments $A V ( )$ that replaced $A'$, $ x ^ (A') $ where $ x [ (A')] Q[ (A') ] $ and $ x [ (A)] Q[ (A) ] $.

**Definition .** The class of query plans $ (Q)$ consists of pseudo-trees with caching $(P, C )$ of $Q$ and their space and time exponents are defined as: s(P, C ) := max_ A C ^*( (A) (A)), t(P, C ) := max_ A V(P) ^*( (B_A) [A, B_A] (A)), where Given a nonempty set $ S $ on some branch of the tree, i.e. $ S (A)$ for some $A$, we denote by $min( S )$ its smallest element, i.e. $min( S ) S $ and $ S (min( S ))$. $B_A:=min( C (A))$.

**Lemma .** Let $( , , )$ be a RPT of the SPQ $Q$ and $A V ( )$ arbitrary. Then for a run of the Algorithm [ref] , the collective time spent in function calls $ solve (A, x )$ and $ solveReplaced (A,A', x )$ (excluding cache hits - counting starts from lines [ref] and [ref] - and excluding recursive calls - lines~ [ref] to~ [ref] ) over all values for $ x $ is bounded by $O(|D|^ ^*( (A) (A)) )$.

**Definition .** A tree decomposition of $Q( X )$ is a tuple $TD = (T, )$ where $T=(V,E)$ is a directed tree and $ : V 2^ (Q) $ is a function from the nodes to sets of variables, satisfying: ; $ X ( (T))$, ; $ A (Q)$, the nodes $v$ with $A (v)$ must form a connected subset of $V$, ; $ X _i v V s.t. X _i (v)$. We pick an arbitrary such $v$ and say $R_i( X_i)$ is covered by $v$.

**Lemma .** Let $(P,C)$ be a PTCR of the SPQ $Q$ and $A (Q)$ arbitrary. Then, during the execution of Algorithm [ref] , the function call $ solve (A, x )$ returns $ Q[ (A) | x [ (A)] ]( (A)) $. Furthermore, after a call to $ fillCache (A, x [ (A) (A)])$ the cache $M_A$ is of the form \ ( x [ (A)], ) Q[ (A) | x [ (A)], ]( (A)) Q[ (A) | x [ (A)] ] \ .

**Definition .** A Generic Join Plan of a query $Q$ is a total order on its variables, $ = (A_1, …, A_k)$. We denote by $ $ (resp. $ (Q)$) the set of all Generic Join plans (of $Q$). The space-time exponents of any $ (Q)$ are $e( ) = ( ^*( X ), ^*( (Q)))$; in particular, if $Q$ is a scalar query, then the space-time exponents are $(0, ^*( (Q)))$.

**Lemma .** Let $(P,C)$ be a PTCR of the SPQ $Q$ and $A (Q)$ arbitrary. Then for a run of the Algorithm [ref] , the collective time spent in function calls $ solve (A, x )$ (excluding cache hits - counting starts from line [ref] - and excluding recursive calls - line [ref] ) over all values for $ x $ is bounded by $O(|D|^ ^*( (A) (A)) )$.

**Lemma .** Let $Q( X ) _i R_i( X _i)$ be a SPQ, $ Y Z (Q)$ be sets of variables and $A (Q) Z $ a variable. Further, for every $ y Q[ Y ] $, let $ z ( y ) ^ Z $ be a arbitrary extended tuple, i.e., such that $ y = z ( y )[ Y ]$. Then, we can iterate through $a s Q[A| z ( y ) ] $ over all $ y Q[ Y ] $ in time $O(|D|^ ^*( Y \ A\ ) )$.

**Definition .** For a PTCR $(P,C)$ and variable $A V (P)$, we define the (closed) relevant (instantiated) ancestors $ (A), (A), (A)$ --- where $ (A) (A) (A), (A) (A) (A),$ and (for $ (A)≠ $) $ (A) (A) (min( (A)))$ --- recursively as follows: (A) &= (parent(A)) (min( (A))) & (A)≠ , \\ & (A)= , \\ (A) & = (A) (A), (A) = (A) \ A\ .

**Lemma .** Let $P$ be a PT of the SPQ $Q$ and $A (Q)$ arbitrary. Then, for a run of the Algorithm [ref] , the collective time spent in function calls $ solve (A, x )$ (excluding the time spent in recursive calls made in line [ref] that exit the function call) over all values for $ x $ is bounded by $O(|D|^ ^*( (A) (A)) )$.

**Definition .** Let $ $ be a class of query plans for evaluating SPQs. The class of plans $ (Q)$ consists of pairs $(TD, )$, where $TD$ is a tree decomposition of $Q$, and $ $ is a function that maps each vertex $v$ in $TD$ to a plan $ ^v (Q^v)$. The space and time exponents are s(TD, ) = max_v s( ^v), t(TD, ) = max_v t( ^v).

**Lemma .** Let $(P, C )$ be a PTC of the SPQ $Q$ and $A (Q)$ arbitrary. Further, let $B_A=min( C (A))$ be the closest ancestor of $A$ with a cache. Then, for a run of Algorithm~ [ref] algptc, the function call $ solve (A, x )$ has a cache miss (reaches line [ref] ) at most once per argument pair $(A, x [ (A) (A, B_A]])$.

**Lemma .** Let $(P,C)$ be a PTCR of the SPQ $Q$ and $A (Q)$ arbitrary. Then, for a run of Algorithm [ref] , let $ solve (A, x _1), solve (A, x _2)$ be two calls where the first happens before the latter. Then, $ x _1[ (A)]$ comes lexicographically before $ x _2[ (A)]$ (or they are equal).

**Conjecture The Triple $k$-Clique Conjecture.** For $k:= 2 , ≥ 2$, consider the query: Q() _ i < j [1,k] E(A_i,A_j) _ i < j [k- +1,k+ ] E(A_i,A_j) _ i < j [k+1,2k] E(A_i,A_j) Then, for any $ > 0$, no algorithm can solve this problem over the tropical semi-ring in space $S=O(|E|^ k 4 -ε )$ and time $T=O(|E|^ k 2 )$.

**Lemma .** Let $(P, C )$ be a PTC of the SPQ $Q$ and $A C $ be an arbitrary variable with cache. Then, for a run of Algorithm [ref] algptc, the function call $ solve (A, x )$ has a cache miss (reaches line [ref] ) at most once per argument pair $(A, x [ (A)])$.

**Lemma .** Let $Q$ be a SPQ, $(P, C )$ a PTC of $Q$, and $A (Q)$ an arbitrary variable. Then, for any set of variables $ Y $ such that $ (A) Y (Q) (A)$ and tuple $ y Q[ Y ] $ we have $ Q[ (A) | y ] = Q[ (A) | y [ (A)] ] $.

**Lemma .** Let $(P,C)$ be a PTCR of the SPQ $Q$ and $A (Q)$ arbitrary. Then, for a run of Algorithm [ref] , the function $ solve (A, x )$ is only called for arguments $ x ^ (A) $ where $ x [ (A)] Q[ (A) ] $.

**Lemma .** Let $Q( X ) _i R_i( X _i)$ be a SPQ, $P$ a PT of $Q( X )$ and $A (Q)$ arbitrary. Then, for an $ x ^ (A) $ Q[ (A) | x ]( (A)) = _ a s Q[A| x ] \ a[ X ] s\ _ B child(A) Q[ (B) | ( x ,a) ]( (B)) .

**Lemma .** Let $P$ be a PT of the SPQ $Q$ and $A (Q)$ arbitrary. Then, for a run of Algorithm~ [ref] , the function $ solve (A, x )$ is called at most once per argument pair $(A, x )$ and $ x Q[ (A) ] $.

**Lemma .** Let $Q( X ) _i R_i( X _i)$ be a SPQ, $ Y (Q)$ a set of variables and $A (Q) Y $ a variable. Then, we can iterate through $a s Q[A| y ] $ over all $ y Q[ Y ] $ in time $O(|D|^ ^*( Y \ A\ ) )$.

**Definition .** The class of query plans $ (Q)$ consists of PTCR $(P,C)$ of $Q$ and their space and time exponents are defined as: s( P,C) = max_ A V (P) ^*( (A) (A)), t( P,C) = max_ A V (P) ^*( (A) (A)).

**Definition .** The class of query plans $ (Q)$ consists of pseudo-trees $P$ of $Q$ and their space and time exponents are defined as: $$s(P) := max_ A (Q) ^*( (A)), t(P) := max_ A (Q) ^*( (A) (A)).$$

**Definition .** A pseudo-tree with caching (PTC) of $Q$ is a pair $(P, C )$, where $P=( V ,E)$ is a of $Q$, and $ C V $ is a subset of the variables for which we add a cache. We require $ (P) C $.

**Lemma .** Let $(P, C )$ be a PTC of the SPQ $Q$ and $A (Q)$ arbitrary. Then, for an $ x Q[ (A) ] $, the function $ solve (A, x )$ of Algorithm [ref] algptc returns $ Q[ (A) | x ]( (A)) $.

## Proofs
**Proof.** [Proof Sketch.] $ $ follows from the fact that the 4-path query $R(AB) S(BC) T(CD)$ admits a $ $ query plan $ $ with $e( ) = (1,1)$, while time exponent 1 is not feasible in . $ $ is proved by showing that the 3-path query in Example~ [ref] has a plan $ $ with $e( ) = (0,1)$, while allows for plans $ '$ with $e( ') = (1,1)$ or $e( ') = (0,2)$ but not $(0,1)$; none dominate $e( )=(0,1)$. $ ^ $ follows as $Q^v=Q$ for the single-bag TD.

**Proof.** [Proof Sketch.] Given a plan $ := ((T, ), ) ^ $, we construct a plan $ _0 := (P, C) $ such that $ _0 $. The construction is based on the variable elimination procedure for a tree decomposition~ [cite] , and proceeds by induction on the number of bags in $T$. If $T$ has a single bag, then $ $ is essentially a $ $ plan, and the claim follows from $ $. Otherwise, let $v$ be a leaf of $T$, and $p:= (v)$. We eliminate all variables $ A_1, …, A_k := (v) (p)$. Let $ Z$ be their neighbors, $ Z := B atom R_i( X_i), j, s.t. A_j, B X_i $. Let $Q'$ to be the query obtained from $Q$ by removing all variables $A_1, …, A_k$, and adding a new atom $R( Z)$. Let $ ' = ((T', ), )$ be the plan obtained from $ $ by removing the leaf $v$. By induction hypothesis, $ '$ can be converted to a $ $ plan $ _0' = (P', C')$ such that $ _0' ≤ '$. All variables $ Z$ belong to a branch of $ _0'$ (because of the atom $R( Z)$). Construct the pseudo-tree $P$ from $P'$ by adding a branch $A_1 - A_2 - … - A_k$ as a child of the last variable in $ Z$. Finally, define $ _0 := (P, C A_1 )$ (only $A_1$ receives a cache). It can be checked that $ _0 $, which proves $ ^ $.

**Proof.** [Proof Sketch] For $ $, we use the query $ ()$ depicted in Fig.~ [ref] and the $ $ in given Example~ [ref] , with space-time exponents $(1,2)$. Because no edge separates the query, any TD with $2+$ bags uses superlinear space, and a single bag is equivalent to a , which takes more than quadratic time. For $ $, we use the query $ ()$ depicted in Fig.~ [ref] and a TD with three bags, separated by $DE$ and $GH$; its space-time exponents are $(1,2)$. The best $ $ has space-time exponents of $(1.5,2)$ or $(1,2.5)$.

**Proof.** [Proof Sketch] The RPT $( , , )$ describes a tree structure (e.g., see black parts of Fig.~ [ref] ) and we can use this tree structure together with bags $ (A) (A)$ at nodes $A$ to construct a tree decomposition of $Q$. The width of this tree decomposition is the same as the time exponent of the RPT.

**Proof.** [Proof Sketch] To achieve this, we first perform a heavy-light partitioning of the input relations where a heavy join value $a_j$ is one which appears in at least $√ |E_ i | $ tuples of $E_ i $. As there cannot be more than $√ |E_ i | $ heavy values, we can iterate over them, instantiate them, and solve the reaming linear query in linear time and constant space. Thus, in space $O(1)$ and time $O(|D|^ 3 2 )$ we can handle all heavy values. For the case where all values are light, a different technique is needed. There, the aim is to perform a merge join on the fly, essentially using the decomposition $$ _ a_1,a_3 ( _ a_2 E_ 1 (a_1,a_2) E_ 2 (a_2,a_3) ) ( _ a_4 E_ 4 (a_1,a_4) E_ 3 (a_4,a_3) ).$$ We explain how to iterate through $E_ 1 (A_1,A_2) E_ 2 (A_2,A_3)$ projected to $A_1,A_3$ in lexicographic order (the other side is symmetric). To that end, we iterate through $a_1$ at the top level and compute the $≤ √ |E_ 1 | $ values $a_2 (E_1[A_2|a_1])$ that extend $a_1$. For every $a_2 (E_1[A_2|a_1])$ we spawns a separate process that iterates (in an ordered manner) through $a_3 (E_2[A_3|a_2])$ -- hence we use $O(|D|^ 1 2 )$ processes that all require $O(1)$ space. Merging the loops of the different $a_2$ results in an ordered stream of $a_3$ values. Thus, we go over pairs $a_1,a_3$ in lexicographic order. Doing the same for $ E_ 4 (a_1,a_4) E_ 3 (a_4,a_3)$ allows us to perform a merge join. In total, this is done in space $O(|D|^ 1 2 )$ and time $O(|D|^ 3 2 )$.

**Proof.** First note that Q[A| y ] _ i A = X _i Y R_i[A| y ] _ i A X _i Y (R_i[A| y ]) _ i A X _i (R_i[ y ]) Thus, to iterate through $a s Q[A| y ] $ we can do the following: ; We can ignore the last part of the product of Eq. [ref] by assuming $ y Q[ Y ] $. ; We can iterate through the possible $a Q[A| y ] $ by taking the smallest (cardinality wise) $R$ of the relations $R_i[A| y ], (R_i[A| y ])$ in the first and second product of Eq. [ref] , iterate through the domain $a \1 R$ and check that $a$ is present in every other relation $R_i[A| y ], (R_i[A| y ])≠ R$ in the first and second product of Eq. [ref] . ; The annotation $s$ of $a$ in $ Q[A| y ] $ is then the product of the annotations of $a$ in the relations $R_i[A| y ]$ in the first product of Eq. [ref] Iterating through $ Q[A| y ] $ in this way allows us to do it in time (look-ups take logarithmic time which we ignore) O(min_ i A X _i Y | (R_i[A| y ])). Note the index $i$ goes over the relations of the first and second product in Eq. [ref] . Thus, to compute the overall time, we simply have to sum this up over all $ y Q[ Y ] $. To that end, we proof that the claim of the lemma holds for an arbitrary fractional edge cover and, thus, it also holds for the minimal. Therefore, let $(w_i)_i$ be a fractional edge cover of $ Y \ A\ $. Then, clearly, $ B Y Σ_ i B X _i w_i≥ 1$ and $Σ_ i A X _i w_i≥ 1$. Furthermore, let us fix an arbitrary order $ Y = B_1, ,B_l$ and the shorthand $ y _j^ j' :=(b_j, , b_ j' ))$ for $j≤ j'$. &Σ_ y Q[ Y ] min_ i A X _i Y | (R_i[A| y ])| \\ &≤ Σ_ y Q[ Y ] Π_ i A X _i Y | (R_i[A| y ])|^ w_i \\ &≤ Σ_ y Q[ Y ] Π_ i | (R_i[A| y ])|^ w_i \\ & ≤ Σ_ b_1 Q[ B_1 ] Σ_ b_2 Q[B_2| y ^1_1 ] Σ_ b_l Q[B_l| y _1^ l-1 ] Π_ i | (R_i[A| y _1^ l ])|^ w_i \\ & = Σ_ b_1 Q[ B_1 ] Σ_ b_ l-1 Q[B_ l-1 | y _1^ l-2 ] Π_ i B_l X _i | (R_i[A| y _1^ l-1 ])|^ w_i Σ_ b_l Q[B_l| y _1^ l-1 ] Π_ i B_l X _i | (R_i[A| y _1^ l ])|^ w_i \\ & ≤ Σ_ b_1 Q[ B_1 ] Σ_ b_ l-1 Q[B_ l-1 | y _1^ l-2 ] Π_ i B_l X _i | (R_i[A| y _1^ l-1 ])|^ w_i Π_ i B_l X _i (Σ_ b_l Q[B_l| y _1^ l-1 ] | (R_i[A| y _1^ l ])|)^ w_i \\ & ≤ Σ_ b_1 Q[ B_1 ] Σ_ b_ l-1 Q[B_ l-1 | y _1^ l-2 ] Π_ i B_l X _i | (R_i[A| y _1^ l-1 ])|^ w_i Π_ i B_l X _i | (R_i[AB_l| y _1^ l-1 ])|^ w_i \\ & = Σ_ b_1 Q[ B_1 ] Σ_ b_ l-1 Q[B_ l-1 | y _1^ l-2 ] Π_ i | (R_i[AB_l| y _1^ l-1 ])|^ w_i \\ & \\ & ≤ Σ_ b_1 Q[ B_1 ] Π_ i | (R_i[AB_2… B_l|b_1])|^ w_i \\ & = Π_ i B_1 X_i | (R_i[AB_2… B_l)|^ w_i Σ_ b_1 Q[ B_1 ] Π_ i B_1 X_i | (R_i[AB_2… B_l|b_1])|^ w_i \\ & ≤ Π_ i B_1 X_i | (R_i[AB_2… B …

**Proof.** Let us denote the children as $\ B_1, , B_l\ =child(A)$. Further, let us consider an $R_i( X _i)$ with $ (A) X _i≠ $. Then, note that due to the property of a PT, either $ X _i (A)$ or for exactly one $j=1, ,l$ we have $ (B_j) X _i ≠ $. Thus, we have Q&[ (A) | x ]( (A)) \\ & _ i (A) X _i≠ \\ X _i (A) R_i[A| x ] _ j _ i (B_j) X _i ≠ R_i[ (B_j) \ A\ | x ] _ i (A) X _i = (R_i[ x ] ). Note that $ Q[ (A) | x ]( (A)) =\ y \0 y \ $ if $ x Q[ X ] $ and the same for the right hand side of Eq. [ref] due to convention. Thus, let us consider the case $ x Q[ X ] $. Then, we can ignore the relations in the last product of Eq. [ref] . Let us consider the cases $A X $ and $A X $ individually. Case $A X $: Then, for $ y ^ (A) $, we can compute $ Q[ (A) | x ]( (A)) ( y )=$ _ i (A) X _i≠ \\ X _i (A) R_i( x [ X _i], y [A]) _ j _ y _j ^ (B_j) _ i (B_j) X _i ≠ R_i(( x , y , y _j)[ X _i]) . However, we can rewrite this to Q[A| x ] ( y [A]) _ j Q[ (B_j)|( x , y [A])] ( y [ (B)]). Note that this just introduces additions and redundant checks if $( x , y [A])$ is in the support of all relations. Thus, this completes this case. Case $A X $: Then, for $ y ^ (A) $ ($A (A)$), we can compute $ Q[ (A) | x ]( (A)) ( y )=$ _ a ^ A _ i (A) X _i≠ \\ X _i (A) R_i( x [ X _i],a) _ j _ y _j ^ (B_j) _ i (B_j) X _i ≠ R_i(( x ,a, y , y _j)[ X _i]) . However, we can rewrite this to _ a Q[A| x ] Q[A| x ] (a) _ j Q[ (B_j)|( x ,a)] ( y [ (B)]). Note that this just introduces additions and redundant checks if $( x ,a)$ is in the support of all relations. Thus, this completes this case and with it the whole proof.

**Proof.** First, let us consider the structure of $Q[ (A) | y ] $ _ i ≠ X _i Y ,\\ X _i Y (A) R_i[ (A)| y ] _ i = X _i Y (R_i[ y ]) _ i X _i Y (A) (R_i[ (A)| y ]). Then, let us consider a relation $R_i$ considered in the last product of Eq. [ref] and assume there exists a $C X _i (A)$. By definition, there is a $B X _i ( Y (A))$. Then, by the definition of a PT, $B (A)$ and by the definition of context $B (A)$. Thus, actually, such a $B$ cannot exist and as a consequence, $ X _i (A)= $ for every relation $R_i$ in the last product of Eq. [ref] . Thus, we can rewrite it to Q[ (A) | y ] & _ i ≠ X _i Y ,\\ X _i Y (A) R_i[ (A)| y ] _ i = X _i Y \\ X _i Y (A) (R_i[ y ]). Further, let us consider the structure of $Q[ (A) | y [ (A)] ]$ (note that $ (A)$ could have also been such a set of variables $ Y $): Q&[ (A) | y [ (A)] ] \\ & _ i ≠ X _i (A), \\ X _i (A) (A) R_i[ (A)| y [ (A)]] _ i = X _i (A) \\ X _i (A) (A) (R_i[ y [ (A)]]). First, note that for any $i$ the relation $ (R_i[ y [ (A)]])$ is the same as $ (R_i[ y ])$ since both are $\ () \1\ $ as $ y Q[ Y ] $. Moreover, if $ X _i Y (A)$, clearly $R_i[ (A)| y [ (A)]]$ is the same as $R_i[ (A)| y ]$. Hence, if we can show that Eq. [ref] and [ref] actually partition the $i$'s in the same way and that $ X _i Y (A)$ always holds for relations in the first part, we can be sure that the expressions are actually the same. To that end, consider a $i$ such that $ ≠ X _i Y (A)$. Then, similarly to above, using the definition of PTs and contexts, we get $ X _i (A) (A)$. Hence, $ X _i Y (A)$ and $ ≠ X _i (A) (A)$. Proving the converse is even simpler. To that end, consider a $i$ such that $ ≠ X _i (A) (A)$. Hence, $ X _i (A) (A)$ and $ ≠ X _i Y (A)$. As the expressions are the same, naturally, $ Q[ (A) | y ] = Q[ (A) | y [ (A)] ] $.

**Proof.** We prove this by induction on the size of $ (A)$. The base case is $ (A) = \ A\ $ which we will consider now. What Algorithm [ref] does to compute $ Q[ A | x ]( (A)) $ is to allocate a $ $-relation $ OUT $ over the same schema and iteratively considers $a s Q[A| x ] $. As $ (A)= $, the $ $-relation $ TMP $ allocated next is then also of the same schema. Thus, Algorithm [ref] simply computes OUT = _ a s Q[A| x ] \ a[ (A)] s\ Due to Lemma [ref] , this equals $ Q[A| x ]( (A)) $ as required. Now for the induction step, let us consider an $A$ such that $ (A)≠ $. What Algorithm [ref] does to compute $ Q[ A | x ]( (A)) $ is to allocate a $ $-relation $ OUT $ over the same schema and iteratively considers $a s Q[A| x ] $. Then, $ TMP $ is instantiated as the relation $\ a[ (A)] s\ $. Depending on whether $A (A)$, the schema is either $A$ or $ $. In either case, we iterative multiply to it the calls to the children $B child(A)$ Thus, in the end OUT = _ a s Q[A| x ] \ a[ (A)] s\ _ B child(A) solve (B,( x ,a)) Due to the induction hypothesis and Lemma [ref] , this equals $ Q[ (A)| x ]( (A)) $ as required.

**Proof.** [Proof of Lemma [ref] ] First notice that the first call is made to $ (P)$ together with the only possible tuple $() ^ ( (P)) = ^ .$ Then, the algorithm iterates through $a Q[ ] $ and for every $A child( (P))$ calls $ solve (A,a)$. Then, we can argue inductively. To that end, let $A$ be such that the induction hypothesis holds. I.e., all calls gets a tuple $ x $ together with a variable $A$ as input such that $ x Q[ (A) ] $ and such that $(A, x )$ is unique. Then the call simply iterates through domain elements $a Q[A| x ] $ and extends $ x $ by that element to get $ x '=( x ,a)$. Thus, by construction $ x ' Q[ (A) ] $. Then, we call $ solve (B, x ')$ for every $B child(A)$. Note that $ (B)= (A)$ and, thus, $ x ' Q[ (B) ] $. Importantly, domain elements are never removed from the input tuple and, thus, we can trace back the call of $ solve (B, x ')$ to $ solve (parent(B), x '[ (parent(B))])$. Hence, as $(A, x )$ was unique, also $(B, x ')$ is unique.

**Proof.** Due to Lemma [ref] , we only have to sum up the time spent in call $ solve (A, x )$ over all $ x Q[ (A) ] $. For each call, we simply have to go through all $a s Q[A| x ] $ and then, as we have seen in the proof of Lemma [ref] , compute $ Q[ (A) | x ]( (A)) $ by computing the huge sum $$ Q[ (A) | x ]( (A) \ A\ ) = Q[A| x ] _ B child(A) Q[ (B) | ( x ,a) ] ( (B)) ,$$ Possibly also marginalizing away the values for $A$ when $A (A)$. However, each annotated tuple in the sum is unique and as we materialize $ TMP $ and $ OUT $, the time for the computation can be bound by $$O(| Q[A| x ](A) | Π_ B child(A) | Q[ (B) | ( x ,a) ] ( (B)) |),$$ i.e., in output optimal time where the output is $\ A\ (A)$. Thus, over all $ x Q[ (A) ] $, the time required is $O(|D|^ ^*( (A) (A)) )$ (due to $O(|D|^ ^*( (A) (A)) )$ being a bound on the size of $ Q[ (A) (A) ]( (A) (A)) $; see [cite] ) while the time to iterate through $a s Q[A] $ is bound by $O(|D|^ ^*( (A)) )$ due to Lemma [ref] .

**Proof.** The correctness follows directly by applying Lemma [ref] to the call $ solve ( ,())$ which consequently returns $ Q( X ) $ as $ ( )= (Q)$ and $ ( )= X $. To bound the runtime of Algorithm [ref] , we can attribute each timestep to the current call of the function $ solve (A, x )$. To that end, for each variables $A$ we sum up the time spent in function calls $ solve (A, x )$ (excluding the time spent in recursive calls made in line 12 that exit the function call) over all values for $ x $. Due to Lemma [ref] , this can be bound by $O(|D|^ ^*( (A) (A)) )$. Thus, by taking the maximum over all $A$, we arrive at a overall bound of $O(|D|^ t(P) )$ for Algorithm [ref] . Lastly, the bound on the space consumption of Algorithm [ref] is rather immediate. First, note that the recursion depth is constant (bounded by $| (Q)|$). Second, note that for every variable $A$, the $ $-relations stored in calls to $ solve (A, x )$ are essentially subsets of the results of $ Q[ (A) ] $ and, thus, their sizes never exceed $O(|D|^ ^*( (A)) )$. Moreover, $ (A) ( (P)) = X $.

**Proof.** To prove $ $, take the 4-path query $Q() = R_1(A,B) R_2(B,C) R_3(C,D)$. A $ $ query plan $P$ can evaluate this query with $e(P) = (1,1)$. Just take a TD consisting of 3 nodes whose bags are $\ A,B\ $, $\ B,C\ $, and $\ C,D\ $. On the other hand, a $ $ query plan can achieve $e(P') = (0,2)$ via a PT that takes one of $B$ or $C$ as root. But linear time complexity is out of reach for $ $, since we cannot avoid a branch consisting of 2 edges. Also additional space does not help. To prove $ $, we consider the 3-path query $Q() = R_1(A,B) R_2(B,C)$. It can be evaluated by a plan $P$ in $ $ with $e(P) = (0,1)$. Just take the with root $B$ and two child nodes $A,B$. On the other hand, a TD either has two nodes with bags $\ A,B\ $ and $\ B,C\ $ or with a single bag that contains all 3 variables. This leads to query plans $P'$ with either $e(P') = (1,1)$ or $e(P') = (0,2)$. That is, in $ $, we can reach space exponent = 0 and time exponent = 1 separately, but not at the same time. For arbitrary classes of query plans $ $ and queries $Q$, we can simply us the TD $(T, )$ consisting of a single node $\ v\ =V(T)$ and bag $ (v)= (Q)$. Then, $Q^v=Q$ and $e( ^v)=e(T, , )$ for all $ ^v $.

**Proof.** Formally, we can prove this by induction on the size of $ (A)$. However, we have already seen this in the proof of Lemma [ref] for variables without a cache. Thus, we only provide the arguments for the induction step for $A C $ here. To that end, let $ y Q[ (A) ] $ with $ y [ (A)]= x [ (A)]$ be the first tuple of this form such that a function call $ solve (A, y )$ is made. Then, $ Q[ (A) | y ] $ is stored in the cache $M_A$ and returned for the function call $ solve (A, x )$. This is correct as due to Lemma [ref] $$ Q[ (A) | y ] = Q[ (A) | y [ (A)] ] = Q[ (A) | x [ (A)] ] = Q[ (A) | x ] .$$ This completes the proof.

**Proof.** Let $ y ^ (A) $ with $ y [ (A)]= x [ (A)]$ be the first tuple of this form such that a function call $ solve (A, y )$ is made. Then, this call produces a cache miss but $ y [ (A)] Q[ (A) | y ] $ is then added to the cache (using Lemma~ [ref] and Lemma~ [ref] ) for subsequent calls that agree on $ (A)$, i.e., also for the call $ solve (A, x )$.

**Proof.** We proceed to prove this by induction on the size of $(A,B_A]$. The base case $B_A=A$ is taken care of by Lemma [ref] . To that end, note that every call $ solve (A, x )$ of a run of Algorithm [ref] algptc has to also be a call $ solve (A, x )$ of a run of Algorithm [ref] . Then, we can argue inductively. To that end, let $A$ be such that the induction hypothesis holds. I.e., all calls that incur a cache miss get a tuple $ x $ together with a variable $A$ as input such that $ x Q[ (A) ] $ and such that $(A, x [ (B_A) (A,B_A]])$ is unique (recall also Lemma~ [ref] ). Then, the call simply iterates through domain elements $a Q[A| x ] $ and extends $ x $ by that element to get $ x '=( x ,a)$. Thus, by construction $ x ' Q[ (A) ] $. Then, we call $ solve (B, x ')$ for every $B child(A)$. Note that $ (B)= (A)$ and, thus, $ x ' Q[ (B) ] $. Importantly, domain elements are never removed from the input tuple and, thus, we can trace back the call of $ solve (B, x ')$ to $ solve (parent(B), x '[ (parent(B))])$. Hence, as $(A, x [ (B_A) (A, B_A]])$ was unique, also $(B, x '[ (B_A) [A, B_A]])$ is unique.

**Proof.** An almost verbatim copy of the proof of Lemma [ref] also works for this lemma. However, up to Equation [ref] , $ Z $ plays the roll of $ Y $ while $ z ( y )$ plays the role of $ y $. Then, for the long equation, we start with &Σ_ y Q[ Y ] min_ i A X _i Y | (R_i[A| z ( y )])| \\ & ≤ Σ_ y Q[ Y ] min_ i A X _i Y | (R_i[A| y ])| I.e., we simply fix less variables. From that point onwards, we simply continue the proof verbatim.

**Proof.** Due to Lemma [ref] , we only have to sum up the time spent in calls $ solve (A, x )$ over all $ x [ (B_A) (A,B_A]] Q[ (B_A) (A,B_A] ] $. For each call, we simply have to go through all $a s Q[A| x ] $. We can use Lemma [ref] , to calculate how much time this takes (excluding the time spent in the loop body). That is, we use $ Y := (B_A) (A,B_A]$ and $ Z = (A)$ with $ y Q[ (B_A) (A,B_A] ] $ mapping to the unique $ z ( y ):= x ^ ( A ) $ that caused the cache miss. I.e., in total, this takes at most $O(|D|^ ^*( (B_A) [A, B_A]) )$ time. Moreover, in the loop body $( x [ (B_A) (A,B_A]],a) Q[ (B_A) [A,B_A] ] $. Then, as we have seen in the proof of Lemma $ [ref] $, we compute $ Q[ (A) | x ]( (A)) $ in output optimal time where the output is $\ A\ (A)$. Thus, over all $ x Q[ (A) ] $ (more importantly actually, over all $ x [ (B_A) (A,B_A]] Q[ (B_A) (A,B_A] ] $), the time required is $O(|D|^ ^*( (B_A) [A,B_A] (A)) )$.

**Proof.** The correctness follows directly by applying Lemma [ref] to the call $ solve ( (P),())$ which consequently returns $ Q $ as $ ( (P))= (Q)$ and $ ( (P))= X $. To bound the runtime of Algorithm [ref] algptc, we can attribute each step past line [ref] (cache miss) to the current call of the function $ solve (A, x )$ and the steps before line [ref] to the calling function. As the number of steps before line [ref] is constant, we can actually simply ignore them. To that end, for each variables $A$ we sum up the time over all values for $ x $. Due to Lemma [ref] , this can be bound by $O(|D|^ ^*( (B_A) [A,B_A] (A)) )$ where $B_A$ is the closest ancestor of $A$ with a cache. Thus, by taking the maximum over all $A$, we arrive at a overall bound of $O(|D|^ t(P, C ) )$ for Algorithm [ref] algptc. Lastly, the bound on the space consumption of Algorithm [ref] algptc is rather immediate. For every variable $A$, the $ $-relations $ TMP , OUT $ stored in calls to $ solve (A, x )$ never exceed $O(|D|^ ^*( (A)) )$. For variables $A$ with caches, the caches $M_A$ are essentially a collection of subsets of $ Q[ (A) | y ] $ for $ y Q[ (A) ] $. Thus, they are collectively essentially a subset of $ Q[ (A) (A) ] $ and its size can, therefore, never exceed $O(|D|^ ^*( (A) (A)) )$.

**Proof.** Given a plan $ := ((T, ), ) ^ $ of the query $Q( X )$, we construct a plan $ _0 := (P, C) $ such that $ _0 $. The construction is based on the variable elimination procedure for a tree decomposition~ [cite] , and proceeds by induction on the number of bags in $T$. If $T$ has a single bag, then $ $ is essentially a $ $ plan, and the claim follows from $ $. Otherwise, let $v$ be a leaf of $T$, and $p:= (v)$. We eliminate all variables $ A_1, …, A_k := (v) (p)$. Let $ Z$ be their neighbors, $ Z := B A_1, …, A_k atom R_i( X_i), j, s.t. A_j, B X_i $. Then, $ Z $ must be in $ (v) (p)$. Let $Q'$ be the query obtained from $Q$ by removing all variables $A_1, …, A_k$, and adding a new atom $R( Z)$. That is, let $$Q' Q[ (Q) A_1, …, A_k ]( X ) R( Z).$$ Note that $ A_1, …, A_k X = $ as $ X ( (T))$. Let $ ' = ((T', ), )$ be the plan obtained from $ $ by removing the leaf $v$. Notice that $ '$ is also a plan of $Q'$. By induction hypothesis, $ '$ can be converted to a $ $ plan $ _0' = (P', C')$ such that $ _0' '$. All variables $ Z$ belong to a branch of $ _0'$ (because of the atom $R( Z)$). Construct the pseudo-tree $P$ from $P'$ by adding a branch $A_1 - A_2 - … - A_k$ as a child of the last variable in $ Z$. Finally, define $ _0 := (P, C A_1 )$ (only $A_1$ receives a cache). It can be checked that $ _0 $, which proves $ ^ $. Strictness, $ ^ $, follows from Theorem~ [ref] and the trivial relationship $ $. That is, notice that $$ , ,$$ while also $$ , .$$ Thus, neither $ $ nor $ $ can hold.

## Bound sentences
- For a full CQ (query without projection), a worst-case optimal join (WCOJ) algorithm runs in time $O(|D|^ ^* )$, where $ ^*$ is the fractional edge cover number of $Q$; this time is proportional to the worst-case output size of the query~ [cite] .
- GJ can easily be adapted to compute conjunctive queries with projections (i.e. non-full queries), however it is no longer guaranteed to be worst-case optimal.
- The Generic Join (GJ) algorithm~ [cite] computes $Q(D)$ in worst-case optimal time given by the AGM bound $O(|D|^ ^*( (Q)) )$~ [cite] .
- Pseudo-trees For a full query, any GJ plan is worst-case optimal, because the output size can be as large as $O(|D|^ ^*( (Q)) )$~ [cite] .
- Readers familiar with free-connex tree decomposition may notice that condition ( [ref] ) is more restrictive, but that's OK for our purpose, because we only consider worst-case optimal algorithms, and do not consider constant-delay algorithms~ [cite] .
- The latter can be asymptotically larger: if the bags are computed using Generic Join, or Pseudo-Trees, then we need to use the tighter upper bound expression $O(Π_i N_i^ w_i^* )$ rather than $O(|D|^ ^* )$, see Sec.~ [ref] .
- When all cardinalities are equal $|R_1|=|R_2|=…$ then the simplified formula $O(|D|^ ^* )$ still gives an upper bound on the time and space complexity, assuming that we ignore the messages $Q^w$ when computing $ ^*$; equivalently, we assign each of them the weight $0$.
- For example, the set difference and distinct element problems have been proven to have a lower bound $ST = Ω(n^2)$~ [cite] .
- We propose a problem called the Triple $k$-Clique Problem , and conjecture a lower bound on its space-time complexity.
