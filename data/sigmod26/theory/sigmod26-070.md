# sigmod26-070 — Deterministic Lower Bounds for k-Edge Connectivity in the Distributed Sketching Model

flag=True score=30 stmts=13 proofs=8 chars=67391
kinds: {"theorem": 3, "lemma": 6, "claim": 1, "corollary": 1, "fact": 1, "definition": 1}
counts: {"np_hard": 0, "lower_bound": 45, "upper_bound": 5, "big_o": 11, "omega": 0, "theta": 5, "approx_ratio": 0, "whp": 1, "regret": 0, "dp": 0, "invariant": 0, "convergence": 0, "competitive": 0, "worst_case": 10, "sketch": 75, "cost_model": 0, "cardinality": 1, "learned": 0}

## Statements
**Theorem 1.** Every deterministic algorithm that decides k-edge connectivity on n-node graphs in the distributed sketching model has a worst case message length of Ω(k) bits, for any super-constant √ k = k(n) ≤ γ n, where γ > 0 is a suitable constant. The proof of Theorem 1 requires us to overcome several technical challenges: • To obtain a graph that is hard for deciding k-edge connectivity in the distributed sketching model, we need to find a suitable graph construction. We cannot directly extend the constructions used for deciding graph connectivity in [Yu21], as this crucially rests on the assumption that the neighborhood of every node in V m (see Sec. 1.1) must be chosen from the same distribution. In fact, the straightforward generalization of this idea to k-edge connectivity would yield a graph that is very likely to be k-edge connected, for large values of k. In this work, we propose a new construction that does not preserve the uniformity property of [Yu21] regarding the input distribution. Instead, there is a special node vσ , with a somewhat larger degree than its peers, which we connect in a way such that the k-edge connectivity of the graph entirely rests on the choice of neighborhood for this node. Nevertheless, we ensure that its neighbors have no easy way of distinguishing vσ from the vast majority of unimportant nodes. This approach introduces additional technical difficulties that we address in our simulation. • As elaborated in more detail in Section 1.2, we need to depa …

**Theorem 2.** For any sufficiently large m, there exists an s = Θ (m) such that the deterministic one-way communication complexity of UniqueOverlapm,s in the simultaneous 3-party model is Ω (m), where each m-length input vector has support s. 1.4 Additional Related Work The distributed graph sketching model was first introduced by Becker, Matamala, Nisse, Rapaport, Suchan, and Todinca in [BMN+ 11], where they proved the hardness of several fundamental graph problems for deterministic sketching algorithms: In particular, they showed that problems including deciding whether the diameter is at most 3, and whether the graph contains a certain subgraph such as a triangle or a square, require sketches of large (i.e., super-polylogarithmic) size. Their approach works by showing that the existence of an efficient protocol for any of these problems can be leveraged for reconstructing graphs with certain properties using similar-sized sketches. However, as pointed out in [BMN+ 11], their approach does not extend to graph connectivity problems. The model was further explored by Becker, Montealegre, Rapaport, and Todinca in [BMRT14], where they show separations between the power of deterministic, private randomness and public randomness algorithms. Apart from the aforementioned works of [NY19, Yu21, Rob23]on connectivity related problems, Assadi, Kol, and Oshman [AKO20] showed a lower bound of Ω n1/2−ε bits on the sketch size for computing a maximal independent set. For the distributed sketching mod …

**Lemma 1.** Every node vi ∈ V is either vσ , A-restricted, or B-restricted. Moreover, vi knows this information as part of its initial state.

**Lemma 2.** Graph G ∈ Gk,n is k-edge connected if and only if (C1) holds.

**Lemma 3.** Consider a set family W , any small ε > 0, and any positive integer d ≤ 2eε|W 2+4/ε . There  εd exists a set family S ⊆ W d such that, for any two distinct S1 , S2 ∈ S, we have |S1 ∩ S2 | ≤ 2 , and d−1 |S| ≥ e . Instantiating Lemma 3 with d = 2k − 1 and a suitable small constant ε > 0, shows that |S| ≥ e2k−2 . (4) Given an arbitrary set D, we say that {B1 , . . . , Bℓ } is a partition P of D and each set Bi ⊆ D is S called a block of P, if ℓi=1 Bi = D and all blocks are pairwise disjoint. For a fixed partition {A, B} of W and any S ∈ S, we define the projections ϕA (S) = S ∩ A and ϕB (S) = S ∩ B, and we also define the resulting image sets ΦA = {ϕA (S) | S ∈ S} and ΦB = {ϕB (S) | S ∈ S}. Consider the algorithm executed by some node vi . Let us assume that, for the time being, σ = i, and that node vi is equipped with ID i. Together with the node IDs in its neighborhood, this fully determines the message vi sends to the referee. In particular, vi can send at most 2L distinct messages, and hence the given algorithm induces a partition Pi,σ of S into at most 2L distinct blocks, where each block is a set of W -neighborhoods for which vi sends the same message to Charlie. Let Bi,σ denote the largest one of these blocks, and observe that Bi,σ ≥ |S| . We emphasize that the Bi,σ depends on 2L the ID i of vi ; it may be the case that the resulting set Bj,σ contains a completely different set of neighborhoods for node vj (i ̸= j). Recall from Lemma 1 that a node vi ∈ V may deduce wheth …

**Lemma 4.** Suppose that we obtain the partition {A, B} by assigning each vertex in W uniformly at random to either A or B. Consider any vi ∈ V . Then, with probability at least 34 , there exist distinct S0 , S1 ∈ S that form an indistinguishable separated pair for vi .

**Claim 1.** Let D be a set of size s, and let P1 , . . . , Pr be r partitions of D, such that the number of blocks in each of the partitions is at most n′ < s1/r+1 . Then, for every i ∈ [r], there exists a block Bi in Pi that contains at least s1−i/(r+1) elements that are in the same block in each of the partitions P1 , . . . , Pi .

**Corollary 1.** For a given k-edge connectivity algorithm, let Vgood ⊆ V be the set of nodes for which there exist indistinguishable separated pairs (see Def. 2 on page 9). If each node sends at most o(k) bits, then there exists a partition {A∗ , B ∗ } of W such that |Vgood | ≥ 43 |V |. 4 The UniqueOverlap Problem We now introduce a communication complexity problem in a 3-party model with simultaneous messages, whose hardness turns out to be crucial for obtaining a lower bound for deciding k-edge connectivity in the sketching model. In the UniqueOverlapm,s problem, we are given two positive integers m and s, where s ≤ ⌈m/2⌉, and there are three players Alice, Bob, and Charlie. We focus on one-way communication protocols, where Alice and Bob each send a single message to Charlie, who outputs the result. Alice’s input is a ternary vector of length m, denoted by X ∈ {0, 1, ⊥}m , and, similarly, Bob gets a vector Y ∈ {0, 1, ⊥}m . We use supp(Z) to denote the support of a vector Z, i.e., the set of indices in [m], for which Zi ̸=⊥. Charlie does not know anything about X and Y , except supp(X) and supp(Y ). 11 Figure 2: An instance of UniqueOverlap8,3 . Since (Xσ , Yσ ) = (1, 0), Charlie needs to answer “no”. Definition 3 (Valid Instance for UniqueOverlapm,s ). The tuple (X, Y ) is a valid instance for UniqueOverlapm,s , if |X| = |Y | = s and the following properties hold: (P1) There is exactly one index σ ∈ [m] such that Xσ ⊕ Yσ = 1, where ⊕ denotes the XOR operator. (P2) For every i ∈ [m] \ {σ}, …

**Fact 1.** Set families R̂Alice and R̂Bob form a cross-intersecting family, which means that i i ∀I1′ ∈ R̂Alice ∀I2′ ∈ R̂Bob : |I1′ ∩ I2′ | ≥ 1. i i (11) Next, we employ an upper bound on the sum of the cardinalities of any two cross-intersecting set families: Lemma 6 ([HM67]). Let A and B be such that each is a family of ℓ-element sets on the same underlying set of size p and assume that A and B are cross-intersecting. If p ≥ 2ℓ, then |A| + |B| ≤ p p−ℓ 1+ ℓ − ℓ . Choosing f = m 3 + 1 and recalling (9), implies that m , 2 s=C +f ≤ (12) and therefore 2(s − 1) ≤ m − 1. Thus, we can apply Lemma 6 with parameters p = m − 1 and ℓ = s − 1 to obtain m−1 m−s − . s−1 s−1 ! |R̂Alice | + |R̂Bob |≤1+ i i ! (13) Since |R̂i | = |Ri |, it follows from (10) and (13) that m 2f s ! m−1 m−s ≤1+ − . s−1 s−1 ! ! (14) However, it also holds that m 2f s (by (12)) ! m m m−1 +1 3 s s−1 =2  ≥4    m −1 +1 3  4 m−1 = m 3 s−1 ! m−1 s−1  ! ! m−1 >m 1+ s−1 !! , which provides a contradiction to (14), thus completing the proof of Theorem 2. 14 4.3 A Deterministic Algorithm for UniqueOverlap The argument developed for our lower bound proof in Section 4.2 inspires the design of a simple deterministic algorithm that allows Alice and Bob to save a single bit by sending messages of length s − 1 under certain conditions. As this is not needed for proving our main result, we relegate the details to Appendix B.

**Theorem 3.** If ⌈ m 3 ⌉ < s, then there exists a deterministic one-way protocol for UniqueOverlapm,s in the simultaneous 3-party model, such that Alice and Bob send at most s − 1 bits to Charlie. 5 Simulation In this section, we show how Alice, Bob, and Charlie can jointly simulate a given k-edge connectivity algorithm Ak-conn in the 3-party model to solve the UniqueOverlapm,s problem, for some given integers m and s, assuming that the sketches are sufficiently small for Corollary 1 to hold. We now describe the details of the simulation and how the players can create the lower bound graph (see Sec. 2), given a valid instance of UniqueOverlapm,s . First, each player locally computes n = 2 ⌈4m/3⌉ , (15) which will correspond to the number of nodes in the graph G ∈ Gk,n that the players construct. According to Corollary 1, there is a partitioning of W into {A∗ , B ∗ }, as well as a set Vgood of size at least m vertices, with the property that there exist indistinguishable separated pairs (see Def. 2), for ≤m each of the nodes in Vgood . We use Vgood to denote the first m nodes in Vgood , ordered by IDs. Note that the players know, in advance, the IDs in the sets V , Vgood , A∗ , B ∗ , as these are fixed for all possible lower bound graphs in Gk,n . Furthermore, they can also pre-compute the indistinguishable ≤m separated pair (S0i , S1i ) , for every vi ∈ Vgood , as these are fully determined by fixing k, n, and the algorithm at hand. Alice’s and Bob’s Simulation: Alice simulates all nodes i …

**Definition 4.** Consider any graph G ∈ Gk,n and recall the existence of set Vgood , the ≤m partition (A∗ , B ∗ ), and the separating pairs (S0i , S1i ) for each vi ∈ Vgood , guaranteed by k-conn applying Corollary 1 to algorithm A . We say that G is compatible with a valid instance (X, Y ) of UniqueOverlapm,s if |Vgood | ≥ m and, for every i ∈ [m]: 1. If Xi = 0, then E(vi , A∗ ) = A∗ ∩ S1i . 2. If Xi = 1, then E(vi , A∗ ) = A∗ ∩ S0i . 3. If Yi = 0, then E(vi , B ∗ ) = B ∗ ∩ S0i . 4. If Yi = 1, then E(vi , B ∗ ) = B ∗ ∩ S1i . 5. If i ∈ supp(X) or i ∈ / (supp(X) ∪ supp(Y )), then vi has k edges to uA . 6. If i ∈ supp(Y ) \ supp(X), then vi has k edges to uB .

**Lemma 7.** Let G be a graph that is compatible with an instance I = (X, Y ) of UniqueOverlap. It holds that G is k-edge connected if and only if the solution to I is “yes”, i.e., Xσ = 0 and Yσ = 1.

**Lemma 9.** Suppose there exists a deterministic k-edge connectivity algorithm, with the property that every node sends a sketch of length at most L = o (k) bits, when executing on Gk,n . Then, √ UniqueOverlapm,s has a deterministic communication complexity of o (k · m) bits.

## Proofs
**Proof.** Let VA ⊆ V be the subset of nodes that are A-restricted, i.e., satisfy (E3), and also include vσ in VA . Similarly, we define VB ⊆ V to contain all B-restricted nodes, which satisfy (E4). Every vj ∈ VA has k parallel edges to uA , which itself has an edge to each node in A. Since A is a clique of size at least k, it follows that there are at least k edge-disjoint paths between every pair of nodes in G[A ∪ {uA } ∪ VA ], which implies that G[A ∪ {uA } ∪ VA ] is k-edge connected. A similar argument shows that B, uB , and VB induce a k-edge connected subgraph. By properties (E1)-(E5), any edge in the cut (A ∪ {uA } ∪ VA , B ∪ {uB } ∪ VB ) must be in E(vσ , B). Thus, the graph is k-edge connected if and only if Case (C1) occurs. 7 Figure 1: An instance of a lower bound graph for k = 3. Notice that vσ is the only vertex with edges in both A and B. In this example, condition (C0) holds, since vσ has k edges to A. Thus, the graph is not k-edge connected. 3 Existence of Indistinguishable Separated Pairs As our overall goal is to prove Theorem 1, we assume towards a contradiction that each node sends at most L = o (k) bits throughout this section. For a node vi ∈ V , we call the subset of its neighbors in W its W -neighborhood. Here, our main focus will be to show that there is a way to choose the partition {A, B} of W that has properties suitable for our simulation in Section 5. Definition 1 (Separated Pair). We say that two W -neighborhoods S0 and S1 form a separated pair for vσ with respect to A and B, when the following are true: • if vσ ’s W -neighborhood corresponds to S0 , then condition (C0) holds. In other words, |S0 ∩ A| ≥ k and |S0 ∩ B| ≤ k − 1. • if vσ ’s W -neighborhood is given by S1 , then condition (C1) holds. Thus, |S1 ∩ A| ≤ k − 1 and |S1 ∩ B| ≥ k. Hence, if vσ ’s W -neighborhood is S1 , the graph is k-edge connected whereas if it is S0 , then the graph is not k-edge connected. Since node vσ has exactly 2k − 1 neighbors in W according to our lower bound graph description, |W |  we know that the set of all W -neighborhoods of vσ is of size 2k−1 . For technical reasons, however, we need to ensure that any pair of possible W -neighborhoods of vσ have an intersection that is at most a small constant fraction of k. The proof of the next lemma follows via the probabilistic method 8 and similar to Lemma 6 in [KNP+ 17a].4 We include a full proof in Appendix A for completeness. |

**Proof.** We start by defining additional partitions P̃i,A and P̃i,B that we obtain from Pi,A and Pi,B , respectively, as follows: For each block B ∈ Pi,A , we define ϕ−1 A (B) = [ {S ∈ S | ϕA (S) = T }. T ∈B 4 This is Lemma 7 in the full version of their paper on arXiv [KNP+ 17b]. 9 Partition P̃i,A has exactly the same number of blocks as Pi,A , and we obtain B̃ ∈ P̃i,A from B ∈ Pi,A by defining it as the set containing all elements in ϕ−1 A (B). Analogously, we construct P̃i,B , which will have the same number of blocks as Pi,B , and, for each B ′ ∈ Pi,B , we get block B̃ ′ ∈ P̃i,B , which S ′ contains all elements in ϕ−1 B (B ) = T ∈B′ {S ∈ S | ϕB (S) = T }. Thus, the number of blocks in P̃i,A , P̃i,B , and Pi,σ is at most 2L , and all three of them partition the set S given by Lemma 3. To show the existence of the sought sets S0 and S1 , we need the following combinatorial statement:

**Proof.** The case i = 1 is immediate by the pigeonhole principle. For the inductive step, assume that the claim is true for some i < r. That is, there is some block Bi ∈ Pi containing at least s1−i/(r+1) elements that are also in the same block in each of the partitions P1 , . . . , Pi . Again, by the pigeonhole principle, we conclude that there exists a block Bi+1 ∈ Pi+1 that has at least s1−(i+1)/(r+1) elements of Bi , as required. Instantiating Claim 1 with n′ = 2L = 2o(k) and partitions P̃i,A , P̃i,B , and Pi,σ , it follows that there exists a set T ⊆ S, such that all sets in T are in the same block in all three partitions. Moreover, (4) tells us that |T | ≥ |S|1/4 ≥ e(k−1)/2 . Without loss of generality, assume that |T | is even, and pair up the sets in T in some arbitrary way. Let T̂ denote this set of pairs, and observe that |T̂ | ≥ e(k−1)/2−log 2 . (5) Consider any pair (S0 , S1 ) ∈ T̂ , and note that we have already shown that Property (i) of Definition 2 holds. To show Properties (ii) and (iii), suppose that S0 , S1 ∈ B̃A ∈ P̃i,A . By construction of P̃i,A , we know that there exist some T0 , T1 ∈ BA where BA is a block in Pi,A , such that T0 = ϕA (S0 ) and T1 = ϕA (S1 ), and, analogously, it follows that ϕB (S0 ) and ϕB (S1 ) are in a common block of Pi,B . To complete the proof, we need to bound the probability that S0 and S1 form a separated pair. Note that this will also imply ϕA (S0 ) ̸= ϕA (S1 ) and ϕB (S0 ) ̸= ϕB (S1 ), by Definition 1 (see page 8). Since |S0 | = |S1 | = 2k − 1, it must be true that either |S0 ∩ A| ≥ k or |S0 ∩ B| ≥ k. Without loss of generality, we can assume the former inequality holds—otherwise, simply exchange S0 and S1 . Recalling that S0 , S1 ∈ S, we know that |S0 ∩ S1 | ≤ εk, which means that, conditioned on |S0 ∩ A| ≥ k, there are still at least |S1 \ S0 | ≥ 2k − 1 − ⌈εk⌉ (6) elements of S1 , whose random assignment is not fixed by the conditioning. Let ℓ be the largest even integer such that ℓ ≤ 2k − 1 − ⌈εk⌉ , and note that k(2 − ε) − 2 ≤ ℓ ≤ k(2 − ε) − 1. (7) Fix some order of the elements in S1 \ S0 , and define Zi (i ∈ [ℓ]) to be the indicator random P variable that is 1 if and only if the i-th element is in B. Let Z = ℓi=1 Zi . Our goal is to show that |(S1 \ S0 ) ∩ B| ≥ Z ≥ k. In the worst case, all ⌈εk⌉ elements in S1 ∩ S0 lie in A, and hence E [Z | |S0 ∩ A| ≥ k] < k. Therefore, we need to make use of the following anti-concentration result to show that there is a sufficiently large probability for Z ≥ k: 10 Lemma 5 …

**Proof.** Consider any protocol for UniqueOverlapm,s and let C be the worst case length of any message sent. Assume towards a contradiction that m C≤ − 1. 6   (9) Suppose that we fix Alice’s support to be a set I of C + f indices from [m], where f is an integer parameter described below. We can partitions her 2C+f possible input vectors into 2C blocks such that the i-th block contains all inputs on which Alice sends the number i to Charlie. Clearly, the largest block B in this partition contains at least 2f inputs. Observe that there must exist a set FAlice (I) ⊆ I of f indices in I such that, for each i ∈ FAlice (I), there are inputs X, X̂ ∈ B such that Xi ̸= X̂i , i.e., Alice sends the same message for two inputs in which the bit at index i is 0 and 1, respectively. We say that index i is flipped for I, and we refer to FAlice (I) as Alice’s flipped indices for support I. Similarly, we can identify a set of flipped indices FBob (I) for Bob by partitioning all possible input vectors with support I into blocks. Note that FAlice (I) and FBob (I) need not be the same since Alice and Bob can execute different algorithms. A crucial observation is that the algorithm fails, if there exist support sets I1 and I2 such that (a) |I1 ∩ I2 | = 1, and (b) (I1 ∩ I2 ) ⊆ (FAlice (I1 ) ∩ FBob (I2 )). These conditions imply that the important index σ ∈ I1 ∩ I2 is flipped for Alice’s as well as Bob’s support. In more detail, this means that there are two inputs X and X̂ for Alice that are in the same block BAlice , where Xσ ̸= X̂σ and Alice sends the same message π(BAlice ), and there are two inputs Y and Ŷ in the same block BBob , where Yσ ̸= Ŷσ , for which Bob sends π(BBob ), thus causing Charlie to fail in one of the (two) valid input combinations. We show that if an algorithm manages to avoid (a) and (b) for all possible pairs of support sets,  then it must send at least C = Ω (m) bits. Let I = [m] be the set family of all possible s-element s support sets for the inputs of Alice and Bob. For each i ∈ [m], define RAlice = {I ∈ I | i ∈ FAlice (I)} , i which means that RAlice contains every support set I where i is a flipped index for I of Alice, and i we define RBob analogously. i For the rest of the proof, if a statement holds for both RAlice and RBob , we simply omit the i i superscript and write Ri instead. Recall that each support I ∈ I contains at least f flipped indices, i.e., |FAlice (I)| ≥ f and |FBob (I)| ≥ f . This tells us that I must be a member of at least f of the …

**Proof.** Since G is a compatible graph, we know that G ∈ Gk,n . Thus, it follows from Lemma 2 that G is k-edge connected if and only condition (C1) holds, i.e., |E(vσ , A∗ )| ≤ k − 1 and |E(vσ , B ∗ )| ≥ k. Since (S0σ , S1σ ) form a separated pair, we know from Definition 1 that this is satisfied for S1σ . According to Definition 4, we use S1σ to determine the neighbors of vσ in A∗ ∪ B ∗ if and only if Xσ = 0 and Yσ = 1, which proves the correspondence between k-edge connectivity and the solution to UniqueOverlap. Equipped with Definition 4, we are ready to show that the sketches produced by the simulation indeed correspond to an actual execution of Ak-conn on a suitable graph in Gk,n . 17 Lemma 8. Suppose that algorithm Ak-conn has a maximum sketch length of L = o(k) bits. Then, Charlie provides as input to the referee in the simulation the same set of sketches that the referee receives when executing algorithm Ak-conn on a graph compatible with the instance (X, Y ).

**Proof.** Since the players construct a graph G ∈ Gk,n , where n = 2 page 6) that |V | ≥ l 4m 3 l 4m 3 m , it follows from (2) (on m . According to Corollary 1, this implies that the set Vgood must have a size ≤m of c · m for some constant c ≥ 1, which ensures that the set Vgood ⊆ Vgood of size m exists. Recall that all players can compute Vgood , since this is fully determined by the algorithm at hand (i.e., Ak-conn ) and the parameters n, k, and L. We now argue that the simulation computes the correct sketch for every type of node in graph G: • Nodes uA and uB : These nodes are only simulated by Charlie who knows supp(X), supp(Y ), and thus also σ. By the description of the simulation, Charlie creates their incident edges in a way that satisfies Points 5 and 6 of Definition 4. • Node wj ∈ A∗ ∪ B ∗ : Without loss of generality, assume that wj ∈ A∗ ; the case wj ∈ B ∗ is symmetric. In the execution on the compatible graph G, the edges in the cut E(V, wj ) are fully determined by the separating pairs (S01 , S11 ), . . . , (S0m , S1m ), which corresponds to how Alice creates the edges incident to wj in the simulation. Moreover, Alice will also add an edge {uA , wj }, which ensures the same sketch for wj as in the actual execution. ≤m • Node vi ∈ Vgood : According to the simulation, Charlie will select one of the blocks Bσ , B̃A , B̃B , where Bσ ∈ Pi,σ , B̃A ∈ P̃i,A∗ and B̃B ∈ P̃i,B ∗ , with the property that each of the blocks contains (S0i , S1i ). Recall that these blocks exist due to Corollary 1. Let BA ∈ Pi,A∗ be the associated block of B̃A , and BB ∈ Pi,B ∗ be the associated block of B̃B , see Page 9. If i = σ, then he computes π(Bσ ) where π(Bσ ) corresponds to the message sent by vi for the inputs in Bσ ; otherwise, if i ∈ supp(X) \ supp(Y ), then he computes π(BA ). Finally, in the case that i ∈ supp(Y ) \ supp(X), he computes π(BB ) instead. A crucial observation is that, even though Charlie does not know whether Alice used S0i ∩ A∗ or S1i ∩ A∗ to create the edges in E(vi , A∗ ), this does not prevent him from computing the correct sketch for vi , since S0i and S1i are guaranteed to be in the same block in each of the partitions; a similar argument applies to the edges in E(vi , B ∗ ). ≤m • Node vi ∈ V \ Vgood : These nodes do not have any edges to A∗ ∪ B ∗ . Instead, Charlie only creates k parallel edges to uA , which matches the neighborhood of vi in the compatible graph G.

**Proof.** By Lemma 8, Charlie invokes the referee with the same set of sketches as in the execution of algorithm Ak-conn on the compatible graph G ∈ Gk,n . Thus, correctness follows directly from

**Proof.** Consider an input vector X with supp(X) = {i1 , i2 , . . . , is } where i1 < i2 < . . . < is . We can obtain a binary string of length s = | supp(X)| from X by simply removing all ⊥ from the vector. In other words, the binary string of X is Xi1 Xi2 . . . Xis . For the rest of the proof, we use X to refer to the input vector or its binary string representation depending on the context. We define πia (X) to be the mapping of the bit string of X to a (s − 1)-length bit string that we get by removing Xia . That is, πia (Xi1 Xi2 . . . Xia . . . Xis ) = Xi1 Xi2 . . . Xia−1 Xia+1 . . . Xis . 20 Our algorithm determines the message sent by a party given the input vector X to be πia (X), whereby ia is chosen in a way that guarantees that Charlie can compute the output correctly. Note that, given X and πia (X), the only bit values of X that Charlie can not deduce with certainty is the value at index ia , i.e., Xia . Now, consider a valid input pair supp(X) = {i1 , . . . , is } and supp(Y ) = {j1 , . . . , js }, where σ = supp(X) ∩ supp(Y ). Upon receiving the messages πia (X) and πjb (Y ) sent by Alice and Bob respectively, for some values ia and jb , Charlie can deduce Xσ and Yσ only if at least one of ia or jb is not σ. In the following, we describe a protocol on how to choose the value ia ∈ supp(X) so that Charlie  can compute correctly: We construct a partition of [m] into blocks R1 , . . . , Rm such that for each s i ∈ [m]: (a) For all I ∈ Ri , we have i ∈ I. (b) For all I, J in Ri , it holds that |I ∩ J| ≥ 2.5 To see why Properties (a) and (b) are sufficient, consider I = supp(X) and J = supp(Y ) such that I ∩ J = {σ}. Let i and j are such that I ∈ Ri and J ∈ Rj . The message sent by Alice and Bob given input X and Y is thus πi (X) and πj (Y ), respectively. By the properties of the partition, it follows that i ̸= j. Hence, at least one of them is not equal to σ, which allow Charlie to deduce the correct value of Xσ and Yσ . To complete the proof, we give a concrete construction of the blocks R1 , . . . , Rm : We partition [m] into ⌈ m 3 ⌉ intervals, each of length 3, except possibly one interval, where the length is either 1 or 2. We define Φ(b) to be the successor of b in the interval containing b, following the cyclic order. For instance, if the interval is [b, b + 1, b + 2], then Φ(b + i) = b + ((i + 1) mod 3), for i = 0, 1, 2. Since we are considering cyclic order, the intervals are referred to as cycles. Note that if a cycle has only one element, say

## Bound sentences
