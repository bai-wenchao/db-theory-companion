# sigmod26-363 — SplineSketch: Even More Accurate Quantiles with Error Guarantees

from=tex flag=True score=21 stmts=7 proofs=6 chars=140127
kinds: {"definition": 2, "lemma": 4, "observation": 1}
counts: {"np_hard": 0, "lower_bound": 7, "upper_bound": 4, "big_o": 91, "omega": 10, "theta": 11, "approx_ratio": 3, "whp": 0, "regret": 0, "dp": 0, "invariant": 5, "convergence": 0, "competitive": 0, "worst_case": 27, "sketch": 218, "cost_model": 0, "cardinality": 0, "learned": 0}

## Statements
**Definition .** We say that a bucket $i$ is splittable if one of the following conditions holds: [label=( )] ; bucket $i$ violates the bucket bound~ [ref] , i.e., $b_i > C_b· n/k$, or ; there are at least $k/3 + 2$ joinable pairs of buckets, $b_i > 2n/k$, and the heuristic error~ [ref] of bucket $i$ is $γ > 1$ times larger than the heuristic error of a joinable pair of buckets (after joining) not overlapping bucket $i$.

**Definition .** We say that a pair $i, i+1$ of adjacent buckets is joinable if both of the following conditions hold: [label=( )] ; the counter of the bucket resulting from the join satisfies $b_i + b_ i+1 0.75· C_b · n/k$, and ; threshold $ _i$ is not protected .

**Lemma .** Let \( P(x) \) be a cubic polynomial such that \( P(0) = 0 \), \( P(1) = 1 \), and \( P(x) \) is non-decreasing on the interval \( [0,1] \). Then, $P ( 1 2 ) [β, 1 - β] ≈ [0.067, 0.933],$ where $β = 1 2 - √ 3 4 ≈ 0.067$.

**Lemma .** For any $j$-node $t$ of the merge tree $T_0$ with $j > 0$ (i.e., containing buckets), the sketch represented by $t$ contains a joinable pair of adjacent buckets, according to Definition~ [ref] .

**Lemma .** For any time $t$ during processing the stream after initialization of buckets by Algorithm~ [ref] , SplineSketch contains a joinable pair of adjacent buckets, according to Definition~ [ref] .

**Lemma See \Cref{lem:existsBucketToMergeStreaming,lem:existsBucketToMerge} in Appendix~\ref{sec:analysis} for proofs in the streaming and mergeability settings, respectively..** After initialization of buckets by Algorithm~ [ref] , SplineSketch always contains a joinable pair of adjacent buckets, according to Definition~ [ref] .

**Observation .** For any epoch $j > 0$, the $j$-nodes of $T_0$ form a disjoint union of paths.

## Proofs
**Proof.** We maintain buckets in a doubly linked list $L$. We use a queue $Q_S$ to keep track of buckets that need to be split due to violating bucket bound. We also use two heaps: a max-heap $H_S$ to keep track of buckets that might get split due to the heuristic error~ [ref] , ordered by their heuristic errors, and a min-heap $H_J$ for joinable pairs of buckets, ordered by their heuristic errors after temporarily joining the pair. In both heaps, every node contains a pointer to the corresponding bucket (or pair of buckets) in $L$. Conversely, every bucket in $L$ contains a (constant size) list of pointers to nodes of $H_S$ and $H_J$ containing that bucket. Both heaps can be initialized in $O(k)$ time. After every split and join performed by , we update $Q_S, H_S$, and $H_J$ according to the new bucket sizes. Since every split and join affects only the sizes and heuristic errors of a constant number of adjacent buckets, the update can be done in $O(1)$ time using the pointers that we maintain. The modifications of the keys and deletions of arbitrary nodes in the heaps can be done in $O(log k)$ time using standard decrease/increase key operation. Every split introduces a constant number of new protected thresholds and every join is accompanied with a corresponding split. Since the number of possible protected thresholds is bounded by $k$, the total number of splits and joins performed by is also $O(k)$. Finally, note that the buffer items can be merged into buckets in $O(klog k)$ time. Indeed, we sort the buffer and start adding items from the smallest to the largest, maintaining the index of the bucket containing the current buffer item; if the processing of the buffer is done in batches (as described in Appendix~ [ref] ), we just perform the necessary splits after each batch. Therefore, the total time complexity of the heap-based implementation of is $O(k log k)$.

**Proof.** We prove that $P( 1 2 ) ≤ 1 2 + √ 3 4 $. The other inequality then holds by symmetry, that is, considering the polynomial $Q(x) = 1-P(1-x)$ which is also non-decreasing and $Q( 1 2 ) = 1-P( 1 2 )$. From now on, we focus on proving that $P( 1 2 ) ≤ 1 2 + √ 3 4 $. Let us denote the coefficients of $P$ by $a, b, c$ and $d$, that is, \[ P(λ) = a λ^3 + b λ^2 + c λ + d \,. \] Because $P(0) = 0$, we have that $d=0$. At the same time, because $P(1) = 1$, we have that $a+b+c = 1$. From the assumption that the polynomial is non-decreasing, we also have that its derivative is non-negative, meaning that for any $λ [0,1]$, \[ 3 a λ^2 + 2b λ + c ≥ 0\,. \] At $1/2$, the polynomial evaluates to \[ f(a,b,c) = a/8 + b/4 + c/2 \,. \] We need to prove that under the constraints we mentioned, it holds that \[ f(a,b,c) ≤ 1 2 + √ 3 4 \] First, using the constraint \( a + b + c = 1 \), we express \( c \) as \( c = 1 - a - b \). Substituting this into the inequality \( 3aλ^2 + 2bλ + c ≥ 0 \), we obtain $3aλ^2 + 2bλ + (1 - a - b) ≥ 0$. Simplifying this expression gives: (3λ^2 - 1)a + (2λ - 1)b + 1 ≥ 0\,. Next, we choose \( λ = 3 + √ 3 6 \), which lies within \( [0, 1] \). We compute the coefficients: \[ 3λ^2 - 1 = √ 3 2 , 2λ - 1 = √ 3 3 \,. \] Substituting these values into inequality~ [ref] gives: \[ √ 3 2 a + √ 3 3 b + 1 ≥ 0\,, \] which simplifies to: √ 3 ( a 2 + b 3 ) + 1 ≥ 0\,. We now express \( f(a, b, c) \) in terms of \( a \) and \( b \). Since \( c = 1 - a - b \), we have: f(a, b, c) = a 8 + b 4 + 1 - a - b 2 = 1 2 - 3a 8 - b 4 \,. To relate inequality~ [ref] to \( f(a, b, c) \), observe that inequality~ [ref] implies: \[ a 2 + b 3 ≥ - 1 √ 3 . \] Multiplying both sides by \( - 3 4 \) gives: \[ - ( 3a 8 + b 4 ) ≤ 3 4√ 3 . \] From Equation~ [ref] , we have: \[ f(a, b, c) = 1 2 - ( 3a 8 + b 4 )\,. \] Using the inequality derived above, we obtain: \[ f(a, b, c) ≤ 1 2 + 3 4√ 3 = 1 2 + √ 3 4 \,. \]

**Proof.** Suppose for a contradiction that there is no joinable pair at time $t$. Note that a pair of adjacent buckets is not joinable for one of two reasons: (1) due to bucket bounds, namely that the resulting bucket would have counter exceeding $0.75· C_b · t/k$ (as the current value of $n$ is equal to $t$) or (2) due to protected thresholds. Observe that there are at most $2t / (0.75· C_b · t/k) = 83 k / C_b k/6$ pairs of buckets satisfying case (1), where we use $C_b 16$. Thus, the main part of the proof is bounding the number of protected thresholds. Let $j$ be the epoch at time $t$, i.e., the smallest $j > 0$ such that $t n_j$. (For $j=0$, there is nothing to prove as there are no buckets in the sketch.) Since there is no joinable pair at time $t$, we take the last time $t' < t$ during the $j$-th epoch such that the sketch at $t'$ has at least $k/3 + 2$ joinable pairs of buckets before performing (the number of joinable pairs does gets below $k/3 + 2$ during the execution of ). Time $t'$ is well-defined, since at the beginning of the $j$-th epoch before performing any split, the sketch has at least $k - 1 - k/6$ joinable pairs as there are no protected thresholds and since $k - 1 - k/6 k/3 + 2$ by the assumption that $k 6$. After time $t'$, the sketch has always less than $k/3 + 2$ joinable pairs of buckets, and thus the algorithm does not perform splits due to the heuristic error, that is, it only splits due to exceeding the bucket bound. To bound the number of splits at times $(t', t]$, we consider a suitable potential of each sketch state $S$ at time $t'' (t', t]$ with $k(S)$ buckets, including the intermediate states during the consolidate methods (after performing some splits or joins of buckets; thus, its number of buckets $k(S)$ may also be $k-1$ or $k+1$). The potential is defined as follows: (S) := Σ_ i = 1 ^ k(S) max \ 0, b_i - 0.75· C_b· t'' k \ \,, where $b_i$ is the counter of bucket $( _ i-1 , _i]$ of the sketch $S$. Clearly, $ (S) [0, t'']$ and moreover, the total increase of the potential during $(t', t]$ is at most $t - t'$. Note that the potential does not increase by joining buckets by Definition~ [ref] . We now analyze the decrease of the potential by a split at time $t'' (t', t]$; such a split is thus due to exceeding the bucket bound. In particular, we now show that the potential decreases by at least $β· C_b· t / (2.5· k)$. First, suppose that both buckets resulting from the split have counters at least $0.75· C_b· t''/k$; then, the pote …

**Proof.** First, we assume w.l.o.g.\ that there are no items of frequency above $n/k$ after processing $n$ items by running the Misra-Gries sketch~ [cite] of size $O(k)$ in parallel, as described in Section~ [ref] . Using such MG filtering, we maintain the invariant that at any time $t$, every item inserted into the buckets of SplineSketch has frequency at most $t/k$. Second, we show an upper bound on the error only for the thresholds of the final sketch. The error bound then extends to any rank or quantile query by the fact that the counter of each bucket has $O(n/k)$ items and that the interpolation is monotone. Thus, in the following we consider a threshold $ _i$ in the final sketch after processing $n$ items and analyze the rank error at $ _i$. To this end, we define the error for any threshold $ _a$ in the sketch at time $t$ as follows: Let $ I _B^t$ be the multiset of items that were added to the buckets by time $t$, let $ ^t( _a) = |\ y I _B^t : y _a\ |$ be the $ _a$'s rank with respect to items $ I _B^t$, and let $ ^t( _a) := Σ_ j = 1 ^i b_j$ be the estimated rank of $ _a$ in $ I _B^t$ solely based on the buckets. Then we define the rank error of $ _a$ in the sketch at $t$ as $ ^t( _a) := ^t( _a) - ^t( _a)$. We show that for any threshold $ _i$ of the final sketch, $ ^n( _i) O(log α· n/k)$; equivalently, we aim to show that only $O(log α· n/k)$ input items $y _i$ are accounted for in buckets with items greater than $ _i$. The lower bound, i.e., $ ^n( _i) -O(log α· n/k)$, can be obtained in a symmetric way. First note that the error at a threshold $ _a$ does not change when items are added to the buckets or when performing a join of buckets (as the removed threshold is no longer relevant). Also, thresholds created by the equally spaced selection of initial buckets have zero error as they are exact at the bucket thresholds, and similarly, thresholds created by new minima or maxima in the buffer have zero error as they are exact at the buffer items and overlap no items in the buckets. So the error may increase only due to splitting a bucket. For the threshold $ _i$ of the final sketch, we consider the following sequence of times $t_0, t_1, t_2, $ and thresholds $σ_0, σ_1, σ_2, $: Let $σ_0 := _i$ and let $t_0$ be the last time of the split that creates $ _i$; if $ _i$ is created by the equally spaced selection of initial buckets (Algorithm~ [ref] ) or by a new minimum or maximum in the buffer, then $ ^n( _i) = 0$ and we are done. Note that the rank error at $ _i …

**Proof.** The proof is similar to the streaming setting (Lemma~ [ref] ), only using the merge tree instead of time. Suppose for a contradiction that there is no joinable pair in the sketch represented by a node $t$ of the merge tree $T_0$. Note that a pair of adjacent buckets is not joinable for one of two reasons: (1) due to bucket bounds, namely that the resulting bucket would have counter exceeding $0.75· C_b · n_t/k$ or (2) due to protected thresholds. Observe that there are at most $2n_t / (0.75· C_b · n_t/k) = 83 k / C_b k/6$ pairs of buckets satisfying case (1), where we use $C_b 16$. Thus, the main part of the proof is bounding the number of protected thresholds. By obs:jNodesFormPath , $j$-nodes in the subtree of $t$ form a path $P_j$. If the sketches represented by nodes on $P_j$ all have at least $k/3 + 2$ joinable pairs of buckets, then the lemma holds. Consider the highest node $t'$ on the path $P_j$ such that the sketch represented by $t'$ has at least $k/3 + 2$ joinable pairs of buckets; we have that $t' ≠ t$, i.e., $t'$ is below $t$, otherwise the lemma holds. Note that the sketch of the lowest node $u$ on $P_j$ has at least $k - 1 - k/6$ joinable pairs as there are no protected thresholds (the merge operation still resets the protection bitvector when the epoch changes and taking the union of buckets does not introduce new protected thresholds). It follows that $t'$ is well-defined since $k - 1 - k/6 k/3 + 2$ by $k 6$. Let $P'_j$ be the subpath of $P_j$ consisting of nodes above $t'$. By the definition of $t'$, all of them have less than $k/3 + 2$ joinable pairs of buckets. Thus, the algorithm does not perform splits on sketches on $P'_j$ due to the heuristic error, that is, it only splits due to exceeding the bucket bound. To bound the number of split-nodes on $P'_j$, we consider the following potential of each sketch represented by a node $v$ that has $k(v) \ k-1, k, , 2k\ $ buckets: (v) := Σ_ i = 1 ^ k(v) max \ 0, b_i - 0.75· C_b· n_v k \ \,, where $b_i$ is the counter of bucket $( _ i-1 , _i]$ of the sketch of $v$. Clearly, $ (v) [0, n_v]$ and moreover, the total increase of the potential on path $P'_j$ from merging with smaller sketches is at most $n_t$. The potential does not increase by joining buckets by Definition~ [ref] . Using the same argument as in the streaming setting, every split at node $v$ of a bucket $i$ with counter $b_i > C_b· n_v/k$ decreases the potential by at least $β· C_b· n_ t / (2.5· k)$. Hence, the number of split-nodes  …

**Proof.** [Proof sketch.] As in the streaming setting, we maintain the invariant that at any node $t$ of the merge tree summarizing $n_t$ items, every item inserted into the buffer and buckets of SplineSketch has frequency at most $n_tt/k$, by running the Misra-Gries sketch~ [cite] of size $O(k)$ in parallel, as described in Section~ [ref] . We note that the Misra-Gries sketch is fully mergeable~ [cite] . We again analyze the rank error for any threshold $ _i$ of the final sketch. We consider the binary tree $T_0$ corresponding to merging sketches and operations with buckets, as described in Section~ [ref] . To this end, we define the error for any threshold $ _a$ in the sketch represented by a node $t$ as follows: Let $ I _B^t$ be the multiset of items at leaves in the subtree of $t$ (i.e., the items summarized by the sketch at $t$) that are not in the buffer of the sketch represented by $t$ (i.e., they are accounted for in the buckets), let $ ^t( _a) = |\ y I _B^t : y _a\ |$ be the $ _a$'s rank with respect to items $ I _B^t$, and let $ ^t( _a) := Σ_ j = 1 ^i b_j$ be the estimated rank of $ _a$ in $ I _B^t$ solely based on the buckets. Then we define the rank error of $ _a$ in the sketch represented by $t$ as $ ^t( _a) := ^t( _a) - ^t( _a)$. We show that for any threshold $ _i$ of the final sketch at the root $r$, $ ^r( _i) O(log (n/k)· n/k)$. The lower bound, i.e., $ ^r( _i) -O(log (n/k)· n/k)$, can be obtained in a symmetric way. Note that the error may increase only due to splitting a bucket or due to taking the union of buckets when merging sketches. First, observe that the total error from merge nodes due to taking the union of buckets is $O(n log (n/k) / k)$. Indeed, the error at a threshold of the sketch represented by a merge-node $t$, with children $t'$ and $t''$, is at most the sum of errors at corresponding thresholds of $t'$ and $t''$ plus $O(n_t / k)$ due to the split induced by the merge operation (i.e., rank query inside a bucket of one of the source sketches). Summing $n_t$ over all merge-nodes $t$ that have buckets (i.e., summarize $Ω(k)$ items) gives a bound of $O(n log (n/k) / k)$, which follows from that the merges are balanced; in more detail, consider a binary tree $T'_0$ where we consider only merge-nodes and note that sketches on each level of $T'_0$ contain $n$ items in total and the number of levels of $T'_0$ with sketches that have buckets is bounded by $O(log (n/k))$. Second, we consider splits due to the heuristic error. The rank error …

## Bound sentences
- For deterministic comparison-based algorithms, there is a gap of $O(log n)$ between the merge-and-prune algorithm using space $O( ^ -1 · log^3 n)$~ [cite] and the lower bound of $Ω( ^ -1 · log^2 n)$~ [cite] .
- Efficient randomized sketches with relative error have only appeared recently, namely, ReqSketch with space $O( ^ -1 · log^ 1.5 n)$~ [cite] and its ``elastic`` version achieving space close the information-theoretic lower bound of $Ω( ^ -1 · log n)$~ [cite] ; however, mergeability of the ``elastic'' …
- We also depict an upper bound on the theoretically optimal error bound of $O(1/k)$ in purple (loosely dotted), where $k$ is the sketch size; the hidden constant factor is based on the maximum error of KLL and GKAdaptive.
- Combining the lower bound on the decrease due to a split with the upper bound on the total potential, it follows that the number of splits during $(t', t]$ is at most $$ t β· C_b· t 2.5· k = 2.5· k β· C_b \,.$$ We choose $C_b$ based on $β$ so that $2.5· k / (β· C_b) k/18$.
- Second, we show an upper bound on the error only for the thresholds of the final sketch.
- The lower bound, i.e., $ ^n( _i) -O(log α· n/k)$, can be obtained in a symmetric way.
- The lower bound, i.e., $ ^r( _i) -O(log (n/k)· n/k)$, can be obtained in a symmetric way.
