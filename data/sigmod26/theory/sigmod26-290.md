# sigmod26-290 — Codd's Theorem for Databases over Semirings

from=tex flag=True score=39 stmts=25 proofs=7 chars=199440
kinds: {"definition": 17, "theorem": 3, "lemma": 1, "proposition": 4}
counts: {"np_hard": 0, "lower_bound": 0, "upper_bound": 0, "big_o": 0, "omega": 0, "theta": 0, "approx_ratio": 0, "whp": 0, "regret": 0, "dp": 0, "invariant": 0, "convergence": 0, "competitive": 0, "worst_case": 0, "sketch": 0, "cost_model": 0, "cardinality": 0, "learned": 0}

## Statements
**Definition Operations on $\mathbb{K}$-relations.** Let\/ $ K =(K,+,·, - , s, 0,1)$ be a semiring with monus and support. We define the following operations on $ K $-relations. ; Union: If $R_1$ and $R_2$ are two $n$-ary $ K $-relations, then the union $R_1 R_2$ is the $n$-ary $ K $-relation defined by $(R_1 R_2)( t ) = R_1( t ) + R_2( t )$, for all $ t D^n$. ; Difference: If $R_1$ and $R_2$ are two $n$-ary $ K $-relations, then the difference $R_1 R_2$ is the $n$-ary $ K $-relation defined by $(R_1 R_2)( t ) = R_1( t ) - R_2( t )$, for all $ t D^n$. ; Cartesian Product: If $R_1$ is an $m$-ary $ K $-relation and $R_2$ is an $n$-ary $ K $-relation, then the Cartesian product $R_1 × R_2$ is the $(m+n)$-ary $ K $-relation defined by $(R_1 × R_2)( t ) = R_1( t [v_1]) · R_2( t [v_2])$, for all $ t D^ m+n $, where $v_1=(1,…,m)$ and $v_2=(m+1,…,m+n)$. ; Projection: ; If $R$ is an $n$-ary $ K $-relation with $n≥ 1$ and $v=(i_1,…,i_k)$ is a non-empty sequence of distinct indices from $\ 1,…,n\ $, then the projection $ _v(R)$ is the $k$-ary $ K $-relation defined by $$ _v(R)( t ) = Σ_ w [v]= t ~ and ~R( w ) ≠ 0 R( w ), $$ for all $ t D^n$. (We take the empty sum to be $0$.) ; If $R$ is a $ K $-relation, then the projection of $R$ on the empty sequence is the $0$-ary relation $ _ [ ]> (R)$ defined by $ _ [ ] > (R)(< >) = Σ_ R( t )≠ 0 R( t )$. ; Selection: If $R$ is an $n$-ary $ K $-relation and $θ$ is an $n$-ary selection condition, then the selection $σ_θ(R)$ is the $n$-ary $ K $-relation defined by $σ_θ(R) ( t ) = R( t ) · P_θ ( t )$,  …

**Definition .** Let $ =( R_1 ,… R_p )$ be a schema. The basic relational calculus language $ BRC ( )$ over $ $ is the set of all expressions defined recursively as follows: ; If $y_i$ and $y_j$ are variables, then the expression $y_i=y_j$ is a $ BRC ( )$-formula with $y_i$ and $y_j$ as its free variables. ; If $ R_i $ is a relation symbol of arity $r_i$ and if $y_1,…,y_ r_i $ are variables, then the expression $ R_i (y_1,…,y_ r_i )$ is a $ BRC ( )$-formula with $y_1,…,y_ r_i $ as its free variables. ; If $ $ is a $ BRC ( )$-formula with $y_1,…,y_n$ as its free variables and if $ $ is a $ BRC ( )$-formula with $y_ n+1 ,…,y_ n+k $ as its free variables, then the expressions $ $, $ $, and $ $ are $ BRC ( )$-formulas, and each of them has $y_1,…,y_n,y_ n+1 ,…,y_ n+k $ as its free variables. Note that the sets $\ y_1,…,y_n\ $ and $\ y_ n+1 ,…, y_ n+k \ $ need not be disjoint, so when we write $y_1,…,y_n,y_ n+1 ,…,y_ n+k $ we assume that repeated occurrences have been eliminated from the list. ; If $ $ is a $ BRC ( )$-formula with $y_1, ,y_n$ as its free variables, then the expression $ $ is a $ BRC ( )$-formula with $y_1, ,y_n$ as its free variables. ; If $ $ is a $ BRC ( )$-formula with $y_1, ,y_n$ as its free variables, then for every $i$ with $1≤ i≤ n$, then the expression $ y_i $ is a $ BRC ( )$-formula with the variables $y_1,…,y_n$ other than $y_i$ as its free variables. From now on, the notation $ (y_1,…,y_n)$ denotes a $ BRC ( )$-formula $ $ whose free variables are $y_1,…,y_n$.

**Definition Semantics of Basic Relational Algebra.** Let $ K =(K,+,·, -, s , 0,1)$ be a semiring with monus and support, and let $ = ( R_1 , …, R_p )$ be a schema. ; A $ K $-database over $ $ is a finite sequence $ I =(R_1,…,R_p)$ of\/ $ K $-relations such that the arity of each $ K $-relation $R_i$ matches the arity of the relation symbol $ R_i $, $1≤ i≤ p$. We say that $ I$ is non-trivial if at least one of the relations $R_i$ of $ I$ has non-empty support. In what follows, we make the blanket assumption that all $ K$-databases considered are non-trivial. ; If $E$ is a $ BRA ( )$-expression of arity $n$ and\/ $ I = (R_1,…,R_p)$ is a $ K $-database, then $E^ I $ is the $n$-ary $ K $-relation obtained by evaluating $E$ on $ I $ according to the definitions of the operations on $ K $-relations. For the sake of completeness, we spell out the precise definition of the semantics of $ BRA $-expressions: ; $( R_i )^ I = R_i$, where $1≤ i≤ p$. ; $(E_1 E_2)^ I = E_1^ I E_2^ I $ and $(E_1 E_2)^ I = E_1^ I E_2^ I $, where $E_1$ and $E_2$ are $ BRA $-expressions of the same arity. ; $(E_1× E_2)^ I = E_1^ I × E_2^ I $. ; $( _v(E))^ I = _v(E^ I ) $ and $( _ [] (E))^ I = _ [] (E^ I )$. ; $(σ_θ(E))^ I = σ_θ(E^ I )$. ; $( (E))^ I = (E^ I )$.

**Proposition .** Let $ =( R_1 ,… R_p )$ be a schema. The following statements are true. ; Consider the atomic formula $ R_i (y_1,…,y_n)$, where $ R_i $ is one of the relation symbols of the schema $ $, $n$ is the arity of $ R_i $, and the $y_i$'s are distinct variables. If $ A =(A,R_1…,R_p)$ is a $ K $-structure, then $( R_i (y_1,…,y_n))^ A = R_i$. ; Let $ _1(y_1,…,y_n)$ and $ _2(y_1,…,y_n)$ be two $ BRC ( )$-formulas with the same free variables. If $ A =(A,R_1…,R_p)$ is a $ K $-structure, then ; $( _1 _2)^ A = _1^ A _2^ A $. ; $( _1 _2)^ A = _1^ A _2^ A $. ; Let $ _1(y_1,…,y_n)$ and $ _2(y_ n+1 ,…,y_ n+m )$ be two $ BRC ( )$-formulas, where $y_1,…,y_ n+m $ are distinct variables. If $ A $ is a $ K $-structure, then $( _1 _2)^ A = _1^ A × _2^ A $. ; Let $ _1(y_1,…,y_n)$ be a $ BRC ( )$-formula. If $ A =(A,R_1…,R_p)$ is a $ K $-structure, then $( )^ A = ( ^ A ) $. ; Let $ (y_1,…,y_n)$ be a $ BRC ( )$-formula, let $i$ be an index with $1≤ i≤ n$, and let $v=(i_1,…,i_ n-1 )$ be the sequence of distinct indices from $\ 1,…,n\ \ i\ $ ordered by the standard ordering of the natural numbers. If $ A =(A,R_1…,R_p)$ is a $ K $-structure, then $( y_i )^ A = _v( ^ A )$.

**Definition Basic Relational Algebra.** Let $ = ( R_1 ,…, R_p )$ be a schema. The basic relational algebra language $ BRA ( )$ over $ $ is the set of all expressions defined recursively as follows: ; Each relation symbol $ R_i $ is a $ BRA ( )$-expression of arity $r_i$, $1≤ i≤ p$. ; If $E_1, E_2$ are $ BRA ( )$-expressions of the same arity $n$, then $E_1 E_2 $ and $E_1 E_2$ are $ BRA ( )$-expressions of arity $n$. ; If $E_1, E_2$ are $ BRA ( )$-expressions of arities $n, m$, respectively, then $E_1 × E_2 $ is a $ BRA ( )$-expression of arity $n+k$. ; If $E$ is a $ BRA ( )$-expression of arity $n≥ 1$ and $v=(i_1,…,i_k)$ is a sequence of distinct indices from $\ 1,…,n\ $, then $ _v(E) $ is a $ BRA ( )$-expression of arity $k$. Furthermore, if $E$ is a $ BRA ( )$-expression of arity $n≥ 0$, then $ _ [] (E)$ is a $ BRA ( )$-expression of arity $0$. ; if $E$ is a $ BRA ( )$-expression with arity $n$ and $θ$ is an $n$-ary selection condition, then $σ_θ(E) $ is a $ BRA ( )$-expression of arity $n$, ; if $E$ is a $ BRA ( )$-expression of arity $n$, then $ (E) $ is a $ BRA ( )$-expression of arity $n$.

**Definition .** Let\/ $ K =(K,+, ·, -, s , 0,1)$ be a semiring with monus and support, let $ =( R_1 ,…, R_p )$ be a schema, and let $ $ be a $ BRC ( )$-formula. The semantics of $ $ on $ K $ is a function $\| \|$ that takes as input a $ K $-structure $ A =(A,R_1,…,R_p)$ and an assignment $α$ on $ A $, and returns a value $\| \|( A , α)$ in $K$ that is defined recursively and simultaneously for all assignments as follows: ; $\|x_i=x_j\|( A ,α) = 1 & if α(x_i) = α(x_j) ; \\ 0 & otherwise. $ ; $ \| R_i (x_ i_1 ,…,x_ i_n )\|( A , α) = R_i(α(x_ i_1 ), … α(x_ i_n ))$, for $1≤ i≤ p$. ; $\| _1 _2\|( A ,α) = \| _1\|( A , α) · \| _2\|( A , α)$. ; $\| _1 _2\|( A ,α) = \| _1\|( A , α)+ \| _2\|( A , α)$. ; $| _1 _2\|( A ,α) = \| _1\|( A , α) - \| _2\|( A , α)$. ; $\| \|( A ,α) = s (\| \|( A , α)) $. ; $\| x_i \| ( A , α) = Σ_ b A \| \|( A , α[x_i/b])$.

**Definition $\mathbb{K}$-structures and assignments.** Let\/ $ K =(K,+,·,-, s ,0,1)$ be a semiring with monus and support, and let $ = ( R_1 , …, R_p )$ be a schema. ; A $ K $-structure over $ $ is a tuple $ A = (A,R_1, , R_p)$, where $A$ is a non-empty subset of $D$, called the universe of $ A $, and each $R_i$ is a $ K $-relation on $A$ whose arity matches the arity of the relation symbol $ R_i $, $1≤ i≤ p$. We say that $ A $ is a finite $ K $-structure if its universe $A$ is a finite set. ; An assignment on $ A $ is a function $α V A$, assigning to every variable an element in $A$. ; If $α$ is an assignment on $ A $ and $b$ is an element of $A$, then we write $α[x_i/b]$ for the assignment $β$ on $ A $ such that $β(x_i)=b$ and $β(x_j) = α(x_j)$, for every variable $x_j ≠ x_i$.

**Proposition .** Let $ =( R_1 ,… R_p )$ be a schema. The following statements are true. ; Let $ _1(y_1,…,y_n)$ and $ _2(y_1,…,y_n)$ be two $ BRC ( )$-formulas with the same free variables. If $ _1$ and $ _2$ are domain independent $ BRC $-formulas, then $ _1 _2$ and $ _1 _2$ are domain independent. ; If $ _1(y_1,…,y_n)$ and $ _2(y_ n+1 ,…,y_ n+m )$ are two domain independent $ BRC ( )$-formulas,\\ where $y_1,…,y_ n+m $ are distinct variables, then $ _1 _2$ is domain independent. ; If $ _1(y_1,…,y_n)$ is a domain independent $ BRC ( )$-formula, then $ $ is domain independent. ; If $ (y_1,…,y_n)$ is a $ BRC ( )$-formula that is domain independent, then $ y_i $ is domain independent.

**Definition .** Let\/ $ K $ be a semiring with monus and support, and let $ $ be a schema. ; If $ (y_1,…,y_n)$ is a $ BRC ( )$-formula whose free variables are $y_1,…,y_n$ and if $ A $ is a $ K $-structure, then $ ^ A D^n K$ is the $n$-ary $ K $-relation defined as follows: $ ^ A (a_1,…,a_n) = \| \|( A ,(a_1,…,a_n))& if $(a_1,…,a_n) A^n$ \\ 0 & if $(a_1,…,a_n) A^n$ $ Note that $ ^ A $ is actually a $ K $-relation on $A$ since, obviously, $ ( ^ A ) A^n$. ; If $ $ is a $ BRC ( )$-sentence and if $ A $ is a $ K $-structure, then $ ^ A D^0 K$ is the $0$-ary $ K $-relation such that $ ^ A (<>) = \| \|( A ,<>)$.

**Theorem .** Let\/ $ K $ be a zero-sum-free semiring with monus and support, let $ =( R_1 ,… R_p )$ be a schema, and let $q$ be an $n$-ary query. The following statements are equivalent: ; [1.] There is a $ BRA ( )$-expression $E$ of arity $n$ such that $q^ I = E^ I $, for every $ K $-database $ I $. ; [2.] There is a domain independent $ BRC ( )$-formula $ (y_1,…,y_n)$ such that $q^ I = ^ A ( I ) $, for every $ K $-database $ I $. ; [3.] There is a $ BRC ( )$-formula $ (y_1,…,y_n)$ such that $q^ I = ^ A ( I ) $, for every $ K $-database $ I $.

**Theorem .** Let\/ $ K $ be a positive semiring with monus and support, let $ =( R_1 ,… R_p )$ be a schema, and let $q$ be an $n$-ary query. The following statements are equivalent: ; [1.] There is an $ RA ( )$-expression $E$ of arity $n$ such that $q^ I = E^ I $, for every $ K $-database $ I $. ; [2.] There is a domain independent $ RC ( )$-formula $ (y_1,…,y_n)$ such that $q^ I = ^ A ( I ) $, for every $ K $-database $ I $. ; [3.] There is an $ RC ( )$-formula $ (y_1,…,y_n)$ such that $q^ I = ^ A ( I ) $, for every $ K $-database $ I $.

**Proposition .** Let\/ $ K = (K, +, ·, 0, 1)$ be a naturally ordered semiring and let $-'$ be a binary operation on $K$. Then, the following statements are equivalent: ; For all $a,b K$, we have $a -' b = a-b$, i.e., $a -' b$ is the smallest element c such that $a b+c$. ; For all $a,b, c K$, we have: $a -' b c$~ if and only if~ $a b+c$. ; The following identities hold for $-'$: ; $a -' a = 0$ ; $0 -' a = 0$ ; $a + (b -' a) = b + (a -' b)$ ; $a -' (b + c) = (a -' b) -' c$.

**Definition .** Let $ K =(K, +, ·, 0,1)$ be a semiring and let $n≥ 0$ be a non-negative integer. ; An $n$-ary $ K $-relation $R$ is a function $R D^n K$ of finite support, that is to say, the set $ (R) := \ (d_1,…,d_n) D^n: R(d_1,…,d_n) ≠ 0\ $ is finite. We call $n$ the arity of $R$. ; A $ K $-relation is an $n$-ary $ K $-relation $R$, for some $n≥ 0$. ; If $n≥ 1$ and $A D$, then an $n$-ary $ K $-relation on $A$ is an $n$-ary $ K $-relation $R$ with $ (R) A^n$.

**Definition Semiring.** A semiring is a structure $ K =(K, +, ·,0,1)$, where addition $+$ and multiplication $·$ are binary operations on $K$, and $0$, $1$ are elements of $K$ such that the following properties hold: ; $(K, +, 0)$ is a commutative monoid, $(K, ·, 1)$ is a monoid, and $0≠ 1$; ; (distributivity): for all elements $a,b,c$ in $K$, we have that $a· (b+c) = a· b + a · c$; ; (annihilation): for every element $a$ in $K$, we have that $a · 0 = 0 · a = 0$.

**Theorem .** Let $ N =(N, +, ·, , s , 0, 1)$ be the bag semiring expanded with the truncated subtraction $ $ as monus and with support $ s $. Then there is no expression of basic relational algebra $ BRA $ that defines the division operation $ $ on $ N $-databases (i.e., division is not expressible using union, difference, Cartesian product, projection, selection, and support on $ N $-databases).

**Definition Division.** Let $R_1 D^ n_1 K$ and $R_2 D^ n_2 K$ be two $ K $-relations with $n_1 > n_2$. The division of $R_1$ and $R_2$ is the $ K $-relation $R_1 R_2 D^ n_1 - n_2 K$ defined by $$ R_1 R_2( a ) = s (Σ_ b D^ n_2 R_1( a , b ))~ · Π_ b ~:~ R_2( b ) = 0 R_1( a , b ). $$ We stipulate that the empty product is equal to $1$. Observe that $ R_1 R_2 = R_1 supp ( R_2)$.

**Definition Semiring with monus.** A semiring with monus is a structure $ K = (K, +, ·, -, 0, 1)$ such that the following hold: ; $(K, +, ·, 0, 1)$ is a naturally ordered commutative semiring; ; For all elements $a,b K$, there is a smallest (in the sense of the natural order $ $) element $c$ in $K$, denoted by $a-b$, such that $a b+c$.

**Lemma Relevance Lemma.** Let $ $ be a schema and $ (y_1,…,y_n)$ be a $ BRC ( )$-formula whose free variables are $y_1,…,y_n$, and let $ A $ be a $( )$-structure. If $α$ and $β$ are two assignments on $ A $ such that $α(y_i)= β(y_i)$ for every free variable $y_i$ of $ $, then $\| \|( A ,α)= \| \|( A ,β)$.

**Definition .** Given a $ K $-database $ I =(R_1,…,R_p)$, we write $ A ( I )$ to denote the $ K $-structure with universe the active domain of\/ $ I $ and with relations those of\/ $ I $. In symbols, we have $ A ( I )= ( adom ( I ),R_1,…,R_p)$.

**Proposition .** Let $ K $ be a zero-sum-free semiring with monus and support, and let $ $ be a schema. Then there is a $ BRA ( )$-expression $E_ adom $ such that for every $ K $-database $ I $, we have that $E_ adom ^ I = adom ( I )$.

**Definition Semiring with support.** A semiring with support is a structure $ K =(K, +, ·, s ,0,1)$ such that $(K, +, ·,0,1)$ is a semiring and $s$ is the unary operation on $K$ such that for every $a K$, $ s (a) = 1 & if a ≠ 0; \\ 0 & otherwise. $

**Definition Active domain.** If\/ $ I =(R_1,…,R_p)$ is a $ K $-database, then the active domain of\/ $ I $, denoted by $ adom ( I )$, is the set of all elements of $D$ that occur in the support of at least one relation $R_i$ of\/ $ I $.

**Definition Domain Independence.** A $ BRC $-formula $ $ is domain independent if for every $ K $-database $ I = (R_1,…,R_p)$ and every $ K $-structure $ B =(B,R_1,…,R_p)$ with $ adom ( I ) B D$, we have $ ^ B = ^ A ( I ) $.

**Definition Query.** Let $n≥ 0$. A $n$-ary query $q$ is a mapping defined on $ K $-databases and such that on every $ K $-database $ I $, $q$ returns an $n$-ary $ K $-relation $q^ I $ on $ adom ( I )$.

**Definition Semiring with monus and  support.** A semiring with monus and support is a structure $ K =(K, +, ·,-, s ,0,1)$ such that $(K, +,·,-,0,1)$ is a semiring with monus and $(K, +, ·, s ,0,1)$ is a semiring with support.

## Proofs
**Proof.** The first part about the atomic formula $ R_i (y_1,…,y_n)$ follows easily from Definitions~ [ref] and~ [ref] . For the second part, let $ _1$ and $ _2$ be two $ BRC ( )$-formulas with $y_1,…,y_n$ as their free variables, and let $ A $ be a $ K $-structure. To show that $( _1 _2)^ A = _1^ A _2^ A $, we have to show that for every tuple $(a_1,…,a_n) D^n$, we have $( _1 _2)^ A (a_1,…,a_n) = ( _1^ A _2^ A )(a_1,…,a_n) $. We distinguish two cases for the tuple $(a_1,…,a_n) D^n$, namely, whether or not $(a_1,…,a_n) A^n$, where $A$ is the universe of $ A $. If $(a_1,…,a_n) A^n$, then we have that $$ ( _1 _2)^ A (a_1,…,a_n)= \| _1 _2\|( A ,(a_1,…,a_n)) = \| _1\| ( A , (a_1,…,a_n))+ \| _2\| ( A , (a_1,…,a_n)) =$$ $$ _1^ A (a_1,…,a_n) + _2^ A (a_1,…,a_n)= ( _1^ A _2^ A )(a_1,…,a_n),$$ where the first equation follows from Definition~ [ref] , the second follows from the semantics of $ BRC ( )$-formulas in Definition~ [ref] , the third follows from Definition~ [ref] , and the last follows from the definition of operations on $ K $-relations in Definition~ [ref] . If $(a_1,…,a_n) A^n$, then $ ( _1 _2)^ A (a_1,…,a_n)=0$ and also $ _1^ A (a_1,…,a_n)= 0 = _1^ A (a_1,…,a_n)$ by Definition~ [ref] . Therefore, $$( _1 _2)^ A (a_1,…,a_n) = 0 = _1^ A (a_1,…,a_n) + _2^ A (a_1,…,a_n) = ( _1^ A _2^ A )(a_1,…,a_n) .$$ The proof that $( _1 _2)^ A = _1^ A _2^ A $ is entirely analogous to the preceding one, but with the connective $ $ in the role of the connective $ $, and with the connective $ $ in the role of the connective $ $. This completes the proof of the second part. For the third part, assume that $ _1(y_1,…,y_n)$ and $ _2(y_ n+1 ,…,y_ n+m )$ are two $ BRC ( )$-formulas, where $y_1,…,y_ n+m $ are distinct variables and let $ A $ be a $ K $-structure. To show that $( _1 _2)^ A = _1^ A × _2^ A $, we have to show that for every tuple $(a_1,…,a_ n+m ) D^n$, we have that $$( _1 _2)^ A (a_1,…,a_ n+m )= ( _1^ A × _2^ A )(a_1,…,a_ n+m ).$$ We distinguish two cases for the tuple $(a_1,…,a_n) D^n$, namely, whether or not $(a_1,…,a_ n+m ) A^ n+m $, where $A$ is the universe of $ A $. If $(a_1,…,a_ n+m ) A^ n+m $, then $$( _1 _2)^ A (a_1,…,a_ n+m )= \| _1 _2\|( A , (a_1,…,a_ n+m ))= $$ $$\| _1\|( A , (a_1,…,a_ n )) · \| _2\|( A , (a_ n+1 ,…,a_ n+m ))=$$ $$ ^ A (a_1,…,a_n) · ^ A (a_ n+1 ,…,a_ n+m )= ( _1^ A × _2^ A )(a_1,…,a_ n+m ) ,$$ where the first equation follows from Definition~ [ref] , the second follows the semantics of $ BRC ( )$-formulas in Definition~ [ref] and from the Relevan …

**Proof.** For concreteness, let us assume that $ $ consists of a single binary relation symbol $ R $. The argument presented below extends easily to arbitrary schemas with only slight modifications. Let $E_ adom $ be the $ BRA ( )$-relational algebra expression $ ( _1( R ) _2( R ))$. We claim that for every $ K $-database, we have that $E_ adom ^ I = adom ( I )$. Let $ I =(R)$ be a $ K $-database and let $a$ be an element of the domain $D$. First, observe that, using the definitions of the semantics of $ BRA $-expressions, we have that $$( _1( R ) _2( R ))^ I (a) = (( _1( R ))^ I ( _2( R ))^ I )(a) = Σ_ R(a,b) ≠ 0 R(a,b)~~ + Σ_ R( b,a) ≠ 0 R(b,a).$$ If $a$ is in the active domain $ adom ( I )$ of $ I $, there is at least one element $b D$ such that $R(a,b) = 0$ or $R(b,a) = 0$. Therefore, at least one summand in at least one of the two sums above is different from $0$. Since $ K $ is a zero-sum-free semiring, it follows that $Σ_ R(a,b) ≠ 0 R(a,b) ~+~ Σ_ R( b,a) ≠ 0 R(b,a) =0$, hence $( _1( R ) _2( R ))^ I (a) = 0$. Consequently, $a ( ( _1( R ) _2( R )))^ I = E_ adom ^ I $. If $a$ is not in the active domain of $ I $, then each summand in both of the two sums above is equal to $0$, hence $( _1( R ) _2( R ))^ I (a)=0$ and so $a ( ( _1( R ) _2( R )))^ I = E_ adom ^ I $. Note that in this step we did not use the hypothesis that $ K $ is a zero-sum-free semiring.

**Proof.** For the first part, we give the proof for the case of the connective $ $. The proof for the case of the connective $ $ is entirely analogous. Let $ I =(R_1,…,R_p)$ be a $ K $-database and let $ B =(B,R_1,…,R_p)$ be a $ K $-structure with $ adom ( I ) B D$. By the domain independence of $ _1$ and $ _2$, we have that $ _1^ B = _1^ A ( I ) $ and $ _2^ B = _2^ A ( I ) $. By combining these facts with the second part of Proposition~ [ref] , we have that $( _1 _2)^ B = _1^ B _2^ B = _1^ A ( I ) _2^ A ( I ) = ( _1 _2)^ A ( I ) $, which establishes the domain independence of the formula $ _1 _2$. For the second part, let $ I =(R_1,…,R_p)$ be a $ K $-database and let $ B =(B,R_1,…,R_p)$ be a $ K $-structure with $ adom ( I ) B D$. By the domain independence of $ _1$ and $ _2$, we have that $ _1^ B = _1^ A ( I ) $ and $ _2^ B = _2^ A ( I ) $. By combining these facts with the third part of Proposition~ [ref] , we have that $( _1 _2)^ B = _1^ B × _2^ B = _1^ A ( I ) × _2^ A ( I ) =( _1 _2)^ A ( I ) $. For the third part, let $ I =(R_1,…,R_p)$ be a $ K $-database and let $ B =(B,R_1,…,R_p)$ be a $ K $-structure with $ adom ( I ) B D$. By the domain independence of $ $, we have that $ ^ A ( I ) = ^ B $. By the domain independence of $ $, we have that $ ^ A ( I ) = ^ B $. By combining these facts with the fourth part of Proposition~ [ref] , we have that $( )^ B = ( ^ B ) = ( ^ A ( I ) ) = ( )^ A ( I ) $. For the fourth part, let $ I =(R_1,…,R_p)$ be a $ K $-database and let $ B =(B,R_1,…,R_p)$ be a $ K $-structure with $ adom ( I ) B D$. By the domain independence of $ $, we have that $ ^ A ( I ) = ^ B $. By combining these facts with the fifth part of Proposition~ [ref] , we have that $( y_i )^ B = _v( ^ B ) = _v( ^ A ( I ) ) = ( y_i )^ A ( I ) $, where $v=(i_1,…,i_ n-1 )$ is the sequence of distinct indices from $\ 1,…,n\ \ i\ $ ordered by the standard order on $N$.

**Proof.** The implication $(2) (3)$ is obvious. The implications $(1) (2)$ and $(3) (1)$ are established by induction on the construction of $ BRA ( )$-expressions and of $ BRC ( )$-formulas, respectively. In some steps in the proof of the implication $(3) (1)$, we will make certain simplifying assumptions about the arity of the relational algebra expressions and the pattern of free variables of the relational calculus formulas. In each such step, the proof extends to the general case with minor modifications only. $(1) (2):$ We proceed by induction on the construction of $ BRA ( )$-expressions. For every such expression $E$, we produce a $ BRC ( )$-formula $ _E$ such that $E^ I = ^ A ( I ) _E$, for every $ K $-database $ I $. Moreover, in each case, we show that the $ BRC ( )$-formula $ _E$ is domain independent. ; Assume that $E$ is one of the relation symbols $ R_i $, $1≤ i≤ p$. In this case, we take $ _E$ to be the atomic formula $ R_i (x_1,…,x_n)$, where $n$ is the arity of $ R_i $. To see why this works, let $ I =(R_1,…,R_p)$ be a $ K $-database. From Definition~ [ref] of the semantics of $ BRA ( )$-expressions and the first part of Proposition~ [ref] , we have $E^ I =R_i= ( R_i (x_1,…,x_n))^ A ( I ) = _E^ A ( I ) $, which establishes the correctness of the formula $ _E$. Furthermore, the first part of Proposition~ [ref] immmediately implies the domain independence of the formula $ _E$. ; Assume that $E$ is a $ BRA ( )$-expression of the form $E_1 E_2$ or of the form $E_1 E_2$, where $E_1$ and $E_2$ are $ BRA ( )$-expressions of the same arity, say $n≥ 0$. By induction hypothesis, there are domain independent $ BRC ( )$-formulas $ _1$ and $ _2$ each with $n$ free variables such that for every $ K $-database $ I $, we have that $E_i^ I = _i^ A ( I ) $, for $i=1,2$. If $E$ is $E_1 E_2$, we take $ _E$ to be the $ BRC ( )$-formula $ _1 _2$; if $E$ is $E_1 E_2$, we take $ _E$ to be the $ BRC ( )$-formula $ _1 _2$. The correctness of the formula $ _E$ follows from the semantics of $ BRA ( )$-expressions in Definition~ [ref] , the inductive hypothesis, and Proposition~ [ref] . Specifically, if $ I $ is a $ K $ database, then $E^ I = (E_1 E_2)^ I = E_1^ I E_2^ I = _1^ A ( I ) _2^ A ( I ) = ( _1 _2)^ A ( I ) = _E^ A(I ) $, as desired. The proof for the case of the difference operation $ $ is entirely analogous. The domain independence of the formula $ _E$ follows from the inductive hypothesis and the first part of Proposition~ [ref] . ; Assume that $E$ is a $ BRA ( )$- …

**Proof.** Let $ =(R,S)$ be a schema consisting of a binary relation symbol $R$ and a unary relation symbol $S$. For every $n≥ 1$, let $a, b_1,…,b_n$ be distinct elements from the domain $D$ and let $ I (n)=(R_n,S_n)$ be the bag database ($ N $-database), where $R_n$ and $S_n$ are the following bags ($ N $-relations): ; $R_n(a,b_i) = 2$, for $1≤ i≤ n$, while $R_n(c,d)=0$, for all other pairs of elements of $D$. ; $S_n(b_i)=1$, for $1≤ i≤ n$, while $S_n(c)=0$, for every other element of $D$. Clearly, for every $n≥ 1$, the division $R_n S_n$ is the bag such that $(R_n S_n)(a) = 2^n$, while ($R_n S_n)(c) = 0$, for all other elements of $D$. We will show that no $ BRA $-expression defines $R S$ by showing that, for sufficiently large $n$, no such expression involving $R_n$ and $S_n$ can have an element of multiplicity $2^n$. Let $E$ be an expression of the basic relational calculus $ BRC $. ; The length $l(E)$ of $E$ is defined by induction as follows: if $E$ is $R$ or $S$, then $l(E)=1$; if $E$ is $E_1 ~ op ~ E_2$, where $ op $ is union, difference, or Cartesian product, then $l(E)=l(E_1)+l(E_2)+1$; if $E$ is $ op (F)$, where $ op $ is projection, selection, or support, then $l(E)=l(F)+1$. ; We write $| (E^ I (n) )|$ to denote the number of tuples in the support of the bag $E^ I (n) $ obtained by evaluating $E$ on the bag database $ I (n)$. ; We write $ (E^ I (n) )$ to denote the highest multiplicity of a tuple in the bag $E^ I (n) $. Claim: The following statements are true: ; $ | (E^ I (n) )| ≤ n^ l(E) $, for all $n≥ 2$. ; There is a polynomial $p_E(n)$ such that $ (E^ I (n) )≤ p_E(n)2^ l(E) $, for all $n≥ 2$. Both statements are proved by induction on the construction of the $ BRC $-expression $E$; the proof of the second statement uses also the first statement. For the first statement, if $E$ is $R$ or $E$ is $S$, then $| (E^ I (n) )| = n = n^ l(E) $, since in this case $l(E)=1.$ For the induction steps, the more interesting cases those of union and Cartesian product. If $E=E_1 E_2$, then $| (E^ I (n) )|≤ | (E_1^ I (n) )|+ | (E_2^ I (n) )| ≤ n^ l(E_1) + n^ l(E_2) ≤ 2n^ l(E_1)+l(E_2) ≤ n^ l(E_1)+l(E_2)+1 =n^ l(E) ,$ where the second inequality follows from the induction hypothesis, while the one before the last one follows from the assumption that $n≥ 2$. If $E=E_1 × E_2$, then $| (E^ I (n) )|= | (E_1^ I (n) )| · | (E_2^ I (n) )| ≤ n^ l(E_1) · n^ l(E_2) = n^ l(E_1)+l(E_2) ≤ n^ l(E_1)+l(E_2)+1 =n^ l(E) ,$ where the first inequality follows from the induction hypothesi …

**Proof.** The proof expands that of Theorem~ [ref] . The direction $(2) (3)$ is obvious, so we only prove the other two directions. We focus on the case of the division operation in direction $(1) (2)$, and on the case of universal quantification in direction $(3) (1)$. All other cases are identical to the ones in the proof of Theorem~ [ref] . $(1) (2):$ Assume that $E$ is an $ RA ( )$-expression of the form $E_1 E_2$, where $E_1$ and $E_2$ are $ RA ( )$-expressions of arities $n_1$ and $n_2$ with $n_1 > n_2$. By induction hypothesis and by renaming variables if necessary, there are domain independent $ RC ( )$-formulas $ _ 1 (y_1, , y_ n_1 )$ and $ _2(y_ n_1-n_2+1 ,…, y_ n_1 )$, such that for every $ K$-database $ I $, we have that $E_1^ I = _1^ A ( I ) $ and $E_2^ I = _2^ A ( I ) $. We will construct a domain independent $ RC ( )$-formula $ _E$ such that for every $ K$-database $ I$, we have that $E^ I = _E^ A ( I ) $. Let $ $ be the $ RC ( )$-sentence $ y_1(y_1=y_1)$. It is easy to see that $ $ has the following property: ; For every $ K$-database $ I =(R_1, , R_p)$ and for every $ K$-structure $ B =(B,R_1, , R_p)$ with the same relations as $ I$ and with $ adom ( I ) B D$, we have that $ ^ B (<>) = 1$. Indeed, this property holds because $$ ^ B (<>)= \| y_1(y_1=y_1)\|( B ,<>)= s (Σ_ b B (\|y_1=y_1\|( B ,b)))=1, $$ where the first two equalities follow from Definitions [ref] and [ref] , while the third equality holds for the following reason: from the blanket assumption that $ I$ is non-trivial, we have that $ adom ( I ) = $, hence $B = $, and so $Σ_ b B (\|y_1=y_1\|( B ,b))= Σ_ b B 1 = 0$, since $ K$ is a zero-sum free semiring. Clearly, the $ RC $-sentence $ $ uses equality $=$. There are equality-free $ RC $-sentences that have the properties of $ $. For example, it is easy to verify that $ ( _ i=1 ^p y_1… y_ r_i R_i (y_1,…,y_ r_i ))$ is such a sentence. In addition to having the $ RC ( )$-formulas $ _1$ and $ _2$ as building blocks, the formula $ _E$ will also have another $ RC ( )$-formula as a building block. We describe that other formula next. We claim that there is a domain independent $ RC ( )$-sentence $θ_ $ (i.e., $θ$ has no free variables) such that if $ I =(R_1, , R_p)$ is a $ K$-database and $ B =(B,R_1, , R_p)$ is a $ K $-structure with $ adom ( I ) B D$, then $θ_ ^ B (< >) = 1 & if $ adom ( I ) ≠ $ \\ 0 & otherwise. $ Let $θ_ $ be the $ RC ( )$-sentence $ ( _ i=1 ^p y_1… y_ r_i R_i (y_1,…,y_ r_i ))$. By Definitions [ref] and [ref] , we have $$θ_  …

**Proof.** If $ K $ is zero-sum-free, just reason as in Theorem~ [ref] to get that the equivalences in Codd's Theorem hold. Suppose now that $ K $ is not zero-sum-free, that is, there are $k_1, k_2 K$ such that $k_1+k_2=0$, $k_1≠ 0$, and $k_2≠ 0$. We will show that there is a $ K $-database $ I $, and a $ BRA $-expression $E$ such that for no $ BRC $-formula

## Bound sentences
