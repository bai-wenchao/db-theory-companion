# sigmod26-015 — AeroSketch: Near-Optimal Time Matrix Sketch Framework for Persistent, Sliding Window, and Distributed Streams

from=tex flag=True score=42 stmts=20 proofs=10 chars=209192
kinds: {"proposition": 3, "theorem": 8, "fact": 1, "lemma": 5, "corollary": 3}
counts: {"np_hard": 0, "lower_bound": 10, "upper_bound": 13, "big_o": 100, "omega": 1, "theta": 3, "approx_ratio": 23, "whp": 2, "regret": 0, "dp": 0, "invariant": 0, "convergence": 2, "competitive": 0, "worst_case": 2, "sketch": 79, "cost_model": 0, "cardinality": 0, "learned": 0}

## Statements
**Theorem Probabilistic error bounds and time complexity of Simultaneous Iteration, the main lemma proven in~\cite{musco2015randomized.** Given a matrix $ A R ^ d × $, a target rank $k$, and an error parameter $ _ SI $, after executing Algorithm~ [ref] , we obtain matrices $ Z R ^ d× k $ and $ Σ R ^ k× k $. With high probability (at least $99/100$), the following guarantees hold: ; $\| A - Z Z ^ A \|_F (1 + _ SI ) \| A - A _k\|_F$, where $ A _k$ is the best rank-$k$ approximation of $ A $. If the singular value decomposition of $ A $ is $ U Σ V ^ $, then $ A _k = U _k Σ _k V _k^ $. Specifically, for $k = 0$, we define $ A _0 = 0 $. ; $\| A - Z Z ^ A \|_2 (1 + _ SI ) \| A - A _k\|_2$. ; $ i,\ | u _i^ A A ^ u _i - z _i^ A A ^ z _i | _ SI · σ_ k+1 ^2$, where $ u _i$ is the $i$-th left singular vector of $ A $ and $ z _i$ is the $i$-th column of $ Z $. The comments in Algorithm~ [ref] analyze the time cost of each line. The time complexity of Algorithm~ [ref] is dominated by line 2, which takes $Θ(q d k)$ time, where $q = Θ ( log d _ SI )$. If $ _ SI $ is a constant, then the total time complexity of Algorithm~ [ref] becomes $Θ(d k log d)$.

**Theorem .** Algorithm~ [ref] solves the Tracking Approximate Matrix Multiplication over Sliding Window (Problem~ [ref] ) with space complexity $O ( d_x+d_y log R )$ and amortized time complexity $O ( d_x+d_y log d log R )$ per update, with success probability at least that given in Eq.~ [ref] . Alternatively, using the similar like probability amplification of Algorithm~ [ref] , it achieves amortized time complexity $O\! ( d_x+d_y log d log^2 1 δ )$ per update while guarantee success probability at least $1-δ$, where $δ$ is a tunable parameter that can be chosen arbitrarily in the interval $(0, 1/100)$.

**Theorem .** Algorithm~ [ref] solves the Tracking Distributed Matrix Sketch over Sliding Window (Problem~ [ref] ) with communication complexity $O ( md log \| A \|_F^2 )$ and amortized time complexity $O ( d log d )$ per update, with success probability at least that given in Eq.~ [ref] . Alternatively, using the probability amplification of Algorithm~ [ref] , it achieves amortized time complexity $O\! ( d log d log^2 1 δ )$ per update to guarantee success probability at least $1-δ$, where $δ$ is a tunable parameter that can be chosen arbitrarily in the interval $(0, 1/100)$.

**Theorem .** (Algorithm~ [ref] ) solves the Distributed Matrix Sketch problem (Problem~ [ref] ) with communication complexity $O ( md log \| A \|_F^2 )$ and amortized time complexity $O ( d log d )$ per update, with success probability at least that given in Eq.~ [ref] . Alternatively, using the probability amplification of Algorithm~ [ref] , it achieves amortized time complexity $O\! ( d log d log^2 1 δ )$ per update while guarantee success probability at least $1-δ$, where $δ$ is a tunable parameter that can be chosen in the interval $(0, 1/100)$.

**Theorem .** Algorithm~ [ref] solves the ATTP Matrix Sketch (Problem~ [ref] ) with space complexity $O\! ( d log \| A \|_F^2 )$ and amortized time complexity $O\! ( d log d )$ per update, with success probability at least that given in Eq.~ [ref] . Alternatively, using the probability amplification of Algorithm~ [ref] , it achieves amortized time complexity $O\! ( d log d log^2 1 δ )$ per update while guarantee success probability at least $1-δ$, where $δ$ is a tunable parameter that can be chosen arbitrarily in the interval $(0, 1/100)$.

**Proposition .** Let $ B $ be the sketch of the current sliding window $(T - N,T]$ returned at line 6 of Algorithm~ [ref] (i.e., from $ Query (lb = T - N,\ ub = T)$). This is our target sketch. We show that, with success probability at least \( 99 100 (1- 2 √ e · 1 √ d log_2 d )\), the following error bound holds: \| A _ T - N, T ^ A _ T - N, T - B ^ B \|_2 O(1)\, \, \| A _ T - N, T \|_F^2, where $ A _ T - N, T $ is the matrix formed by stacking the vectors $ a _i$ for $i = T - N+1,…,T$.

**Lemma Decomposability.** Given a matrix $ A R ^ d× n $, we decompose $ A $ into $m$ submatrices, i.e., $ A ^ = [ A _1, A _2, , A _m]$, where $ A _i R ^ d× n_i $ and $Σ_ i=1 ^m n_i = n$. If we construct a matrix sketch with an $ε_i$ covariance error guarantee for each submatrix $ A _i$, denoted as $ B _i$, such that A _i A _i^ - B _i B _i^ _2 ε_i· A _i _F^2. Then $ B = [ B _1, B _2,…, B _m]$ is an approximation of $ A $ and the error bound is A A ^ - B B ^ _2 Σ_ i=1 ^mε_i· A _i _F^2.

**Corollary Probabilistic error bound and time complexity of Power Iteration, a simplified corollary of Theorem~4.1(a) in~\cite{kuczynski1992estimating.** For Algorithm~ [ref] , if we set $k = _ 2 d +1$, then the probability that the estimated top squared singular value $ σ _1^2$ exceeds half of the true value $σ_1^2 / 2$ is bounded by: [ σ _1^2 σ_1^2/2 ] 1- 2 √ e · 1 √ d log_2 d . Power Iteration requires only matrix-vector multiplications. The total computational time cost of Algorithm~ [ref] is $O(d k)$. If we set $k = _2 d +1$, then the time cost becomes $O(d log d)$.

**Corollary .** If Algorithm~ [ref] is applied, then ML- \ (Algorithm~ [ref] ) solves the Tracking Approximate Matrix Sketch over Sliding Window (Problem~ [ref] ) with space complexity $O\! ( d log R )$ and amortized time complexity $O ( d log d log R log^2 1 δ )$ per update, with success probability at least $1-δ$, where $δ$ is a tunable parameter that can be chosen arbitrarily in the interval $(0, 1/100)$.

**Theorem Theorem 4.1(a) in~\cite{kuczynski1992estimating.** Suppose that $ A ^ A R ^ d× d $. Let $f^ prob ( A , k, )$ be the probability that after $k$ iterations, the Power Iteration Algorithm fails to approximate the squared largest singular value of $ A $ with relative error at most $ $, i.e., $ | σ _1^2-σ_1^2 σ_1^2 |> $. For any $k 2$, we have f^ prob ( A , k, ) min \ 0.824, 0.354 √ (k-1) \ √ d (1- )^ k-1 / 2 .

**Theorem .** ML- (Algorithm~ [ref] ) solves the Tracking Approximate Matrix Sketch over Sliding Window (Problem~ [ref] ) with space complexity $O ( d log R )$ and amortized time complexity $O ( d log d log R )$ per update, with success probability (the probability that the output sketch satisfies the covariance error bound) at least 99 100 (1- 2 √ e · 1 √ d log_2 d ).

**Theorem Weyl's inequality~\cite{franklin2000matrix.** Let $ X , Y $ be Hermitian on inner product space $ V $ with dimension $n$, with spectrum ordered in descending order $λ_1 ≥ ≥ λ_n$. Note that these eigenvalues can be ordered, because they are real (as eigenvalues of Hermitian matrices). Then we have the following inequality: λ_ i+j-1 ( X + Y ) ≤ λ_i( X )+λ_j( Y ) ≤ λ_ i+j-n ( X + Y ).

**Fact .** There always exists one level $j= log_2 \| A _ T-N,T \|_F^2 N $ such that \[ θ = 2^j N \| A _ T-N,T \|_F^2 2θ, \] and we can find the required $j$ using line~14 of Algorithm~ [ref] , in the same way as Algorithm~7 does in~ [cite] .

**Corollary .** As Algorithm~ [ref] uses $ _2 R $ levels of the \ framework, its overall space complexity is $O ( d log R )$, and its amortized time complexity is $O ( d log d log R )$, according to Propositions~ [ref] and~ [ref] .

**Lemma .** Given Eq.~ [ref] , we have the following inequality: \| A _i \|_F^2 k_1 2 Π_ j=2 ^ i (1- k_j 2 ) \| A _1 \|_F^2, where $0< k_j 2 <1$ for $j=1,…,i$ and $k_1=1$.

**Lemma .** We have \| A _ T-N B _ T-N ^ \|_2 ≤ 4θ ≤ 4 \| X _ W \|_F \| Y _ W \|_F, with success probability at least \( 99 100 (1- 2 √ e · 1 √ d log_2 d )\).

**Lemma .** We have \| C _ T-N ^ C _ T-N \|_2 ≤ 4θ ≤ 4 \| A _ T-N,T \|_F^2, with success probability at least \( 99 100 (1- 2 √ e · 1 √ d log_2 d )\).

**Proposition .** The amortized time cost of each update procedure of (Algorithm~ [ref] ) is $O ( d log d )$.

**Lemma Lemma 3 of~\cite{ghashami2014continuous.** After $r$ rounds, at most $O(m· r)$ element update messages have been sent.

**Proposition .** The space complexity of \ is $O ( d )$.

## Proofs
**Proof.** [Proof of Corollary~ [ref] ] We set the number of iterations $k= _2 d + 1$. Plugging $k$ in [ref] , we have [ | σ _1^2-σ_1^2 σ_1^2 |> ] 0.354 √ · log_2 d √ d (1- )^ log_2 d + 1 / 2 . Then we set $ = 1/2$, we have & = [ σ _1^2<σ_1^2/2 ] = [ σ _1^2-σ_1^2 σ_1^2 <- 1 2 ] [ | σ _1^2-σ_1^2 σ_1^2 |> 1 2 ] \\ & = [ | σ _1^2-σ_1^2 σ_1^2 |> ] 0.354 √ _2 d √ d (1- )^ log_2 d + 1 / 2 \\ & = 0.354 √ (log_2 d)/2 √ d · ( 1 2 )^ log_2 d + 1 / 2 = 0.354 √ d log_2 d , where the numerical constant corresponds to the numerical evaluation of $ 192 105 · 1 √ e ≈ 0.354$, and it holds that $ 192 105 · 1 √ e 2 √ e $. Therefore, we have [ σ _1^2<σ_1^2/2 ] 2 √ e · 1 √ d log_2 d . Hence, we have [ σ _1^2 σ_1^2/2 ] 1- 2 √ e · 1 √ d log_2 d , which is the desired result in Corollary~ [ref] .

**Proof.** We denote the matrix $ B $ before the execution of eigen decomposition at line~5 of Algorithm~ [ref] over the window $(T-N, i]$ as $ B _i$. Formally, B _i= C _i^ C _i +Σ_ j=T-N+1 ^ i ( Z _j Z _j^ C _j^ ^ C _j^ + C _j^ ^ C _j^ Z _j Z _j^ - Z _j Z _j^ C _j^ ^ C _j^ Z _j Z _j^ ). In fact, not every index \( j \) explicitly corresponds to the matrices \( Z _j \) and \( Z _j^ C _j^ ^ C _j^ \) stored in the queue \( S \); in such cases, we set \( Z _j = 0 \) here. We introduce the per-update error as: _ i = a _ i ^ a _ i + B _ i-1 - B _ i . \\ Here $ a _i R ^ 1× d $ is a row vector. Then summing over the window yields \|Σ_ i=T-N+1 ^ T _i \|_2 & Eq.~ [ref] = \| (Σ_ i=T-N+1 ^ T a _ i ^ a _ i )- B _ T + B _ T-N \|_2 \\ & = \| A _ T-N,T ^ A _ T-N,T - B _T+ B _ T-N \|_2 \\ & ≥ \| A _ T-N, T ^ A _ T-N,T - B _T \|_2 - \| B _ T-N \|_2 \\ & Eq.~ [ref] = \| A _ T-N, T ^ A _ T-N,T - B _T \|_2 - \| C _ T-N ^ C _ T-N \|_2 . \\ Rearranging terms yield \| A _ T-N,T ^ A _ T-N,T - B _T \|_2 Eq.~ [ref] ≤ \|Σ_ i=T-N+1 ^ T _i \|_2 _ Term 1 + \| C _ T-N ^ C _ T-N \|_2 _ Term 2 . Bounding Term 1 Substituting Eq.~ [ref] into Eq.~ [ref] , we obtain: _ i = a _ i ^ a _ i + C _ i-1 ^ C _ i-1 - C _i ^ C _ i - ( Z _i Z _i^ C ^ _i ^ C ^ _i + C ^ _i ^ C ^ _i Z _i Z _i^ - Z _i Z _i^ C ^ _i ^ C ^ _i Z _i Z _i^ ). \\ We utilize two update relationships: $ C _i^ C _i = ( C _i^ - C _i^ Z _i Z _i^ )^ ( C _i^ - C _i^ Z _i Z _i^ )$ (from line 16 of Algorithm~ [ref] , with $ Z _i= 0 $ if line 19 is triggered), and $ C _i^ C _i = C _ i-1 ^ C _ i-1 + a _i^ a _i$ (line 1). Substituting these into the preceding expression yields the following property: _ i = & C _ i ^ C _ i - ( C _ i ^ - C _ i ^ Z _i Z _i^ )^ ( C _ i ^ - C _ i ^ Z _i Z _i^ ) \\ & - ( Z _i Z _i^ C ^ _i ^ C ^ _i + C ^ _i ^ C ^ _i Z _i Z _i^ - Z _i Z _i^ C ^ _i ^ C ^ _i Z _i Z _i^ ) \\ = & C _ i ^ C _ i - C ^ _i ^ C ^ _i . \\ Lines 2-4 of Algorithm~ [ref] yield the following expression: _ i = V _imin ( Σ _i^2, I (σ_ ^2)_i ) V _i^ & rows ( C _i) ≥ 2 , \\ 0 & else , \\ where $ Σ _i^2$ is the squared singular value matrix from line 3 of the $i$-th update of Algorithm~ [ref] , and $(σ_ ^2)_i$ denotes the $ $-th largest squared singular value of $ Σ _i^2$. (If $ rows ( C _i) < 2 $, we set $(σ_ ^2)_i = 0$.) Next, summing the matrices $ _i$ from Eq.~ [ref] over the window $(T - N,T]$ and analyzing its $ _2$-norm, we derive: \|Σ_ i=T-N+1 ^ T _i \|_2 ≤ Σ_ i=T-N+1 ^ T \| _i \|_2 = Σ_ i=T-N+1 ^T (σ_ ^2)_i. \\ We now aim to bound $Σ_ i=T-N+1 ^T (σ_ ^2)_ …

**Proof.** If the number of rows in $ C _i$ reaches $2 $, we perform SVD at line 3, which takes $O(d ^2)$ time. After this step, the number of rows in $ C _i^ $ is reduced to $ $. Thus, SVD is executed once every $ $ updates, and the amortized time cost of lines 2-6 is $O(d ^2)/ = O(d )$. According to the time complexity result in Corollary~ [ref] , the power iteration at line 7 takes $O(d k) = O(d log d)$ time, where $k= _2 d + 1$ as defined in Algorithm~ [ref] . The analysis of the time cost for the for-loop (lines~9-17) of Simultaneous Iterations is more subtle. During this loop, we compute approximate singular values for increasing $k=2^j$ ($j$ is the loop index) by executing Simultaneous Iteration (line~11) on $ C _i^ ^ C _i^ $ -- specifically, the top-$k$ approximate singular values for $k=2,4,8, ,2^j, $. Suppose that in the $j$-th iteration, the if-statement (line~12) finds that the (approximate) $k = 2^j$-th singular value of $ C _i^ ^ C _i^ $ is smaller than $θ$. In that case, we execute lines 13-17 and terminate the for-loop. The following property holds for the approximate singular values obtained from Simultaneous Iteration on $ C _i^ $ as we do not trigger the if-statement in line~12 for $k/2$ but trigger it for $k$: σ _ k/2 ^2( C _i^ ) = σ _ 2^ j-1 ^2( C _i^ )≥ σ _ ^2( C _i^ ) ≥ θ . We further get the following bound: \| C _i^ \|_F^2- \| C _i^ - C _i^ Z _i Z _i^ \|_F^2 = & ( C _i^ ^ C _i^ ) - ( ( I - Z _i Z _i^ ) C _i^ ^ C _i^ ( I - Z _i Z _i^ ) ) \\ = & ( C _i^ ^ C _i^ ) - ( C _i^ ^ C _i^ ( I - Z _i Z _i^ ) ( I - Z _i Z _i^ ) ) \\ = & ( C _i^ ^ C _i^ ) - ( C _i^ ^ C _i^ ( I - Z _i Z _i^ ) ) \\ = & ( C _i^ ^ C _i^ Z _i Z _i^ ) = ( C _i^ ^ C _i^ Z _i Z _i^ Z _i Z _i^ ) \\ = & ( Z _i Z _i^ C _i^ ^ C _i^ Z _i Z _i^ ) = Σ_ i=1 ^ σ _i^2( C _i^ ) ≥ 2^ j-1 θ. \\ Substituting $ C _i = C _i^ - C _i^ Z _i Z _i^ $ into Eq.~ [ref] , we obtain the following relationship between the two updates: \| C ^ _ i \|_F^2 - \| C _i\|_F^2 & 2^ j-1 θ, \\ \| C ^ _ i \|_F^2 & \| C _ i-1 \|_F^2 + a _ i a _ i ^ , where the “$<$” relation in the last inequality holds when the if-branch at lines 2-4 is executed. Eq.~ [ref] can then be rewritten as: \| C _ i-1 \|_F^2 + a _i a _i^ - \| C _ i \|_F^2 2^ j-1 θ = k_i θ 2 , where $k_i$ substitutes $2^j$ at line 12 of Algorithm~ [ref] , representing the upper bound on the $k$ at line~10 of the $i$-th update. Summing Eq.~ [ref] from $i = T - N + 1$ to $i = T$ (over the active sliding window; snapshots before this window have already expired v …

**Proof.** The space cost of the algorithm is dominated by the sketch matrix $ C $ and the snapshots stored in the queue $ S $. The sketch matrix $ C $ requires $O(d )$ space, as its maximum size is $d × 2 $. The queue $ S $ uses $O (dΣ_ i=T-N+1 ^ T k_i )$ space, since it stores matrices $ Z _i R ^ d × k_i $ and $ Z _i^ C _i^ ^ C _i^ R ^ k_i × d $ at line 15. Summing over all $N$ updates, and using the bound from Eq.~ [ref] , the space cost of the queue $ S $ is: O (d Σ_ i=T-N+1 ^ T k_i ) = O ( d ). Thus, the total space cost of is $O (d + d ) = O ( d )$.

**Proof.** To prove the lower bound of $ [ \| C _ T-N ^ C _ T-N \|_2 4θ ]$, we consider the upper bound of $ [ \| C _ T-N ^ C _ T-N \|_2 ≥ 4θ ]$. We only consider the scenario that the squared largest singular value of $ C ^ _i$ exceeds $4θ$, but after the update the squared largest singular value of $ C _i$ is still larger than $4θ$, i.e., $$ [ \| C _ T-N ^ C _ T-N \|_2 ≥ 4θ \| ( C ^ _ T-N )^ C ^ _ T-N \|_2 ≥ 4θ ].$$ This event can be decomposed into either (1) the power iteration at line 9 of Algorithm~ [ref] fails to converge (thus the if-statement is not triggered), more specifically, the probability that the largest singular value of $( C _ T-N ^ )^ C _ T-N ^ $ is larger than $4θ$ but the estimated largest singular value $ σ _1^2$ returned by power iteration method is smaller than $θ/2$ (i.e., $ σ _1^2 θ/2$). The probability of this event is bounded by the following bound according to Corollary~ [ref] : & [ \| C _ T-N ^ C _ T-N \|_2 ≥ 4θ σ _1^2 ≤ θ/2\ \ \| C ^ _ T-N ^ C ^ _ T-N \|_2 ≥ 4θ ] = 1 , \\ & [ σ _1^2 ≤ θ/2 \|( C ^ _ T-N )^ C ^ _ T-N \|_2 ≥ 4θ ] ≤ 2 √ e · 1 √ d log_2 d , or (2) the power iteration succeeds in converging ($ σ _1^2>θ/2$) but the simultaneous iteration at line 11 of Algorithm~ [ref] fails to converge, more specifically, the returned $ Z ^ $ of simultaneous iteration method is not a ``good'' approximation of the larger singular vectors. That is to analyze the probability: & [ \| C _ T-N ^ C _ T-N \|_2 ≥ 4θ σ _1^2 > θ/2 \ \ \|( C ^ _ T-N )^ C ^ _ T-N \|_2 ≥ 4θ ] . \\ By the Spectral norm error of the simultaneous iteration (Theorem~ [ref] (2) and (3)), we have the following bound, where $σ_i$ and $ σ _i$ denote the $i$-th largest exact and approximate singular values (given by Simultaneous Iteration algorithm) of $ C ^ _ T-N $, respectively: [ \| C _ T-N ^ ( I - Z Z ^ ) \| _ 2 (1+ _ SI )σ_ +1 \ |σ_ +1 ^2- σ _ +1 ^2 | _ SI ·σ_ +1 ^2 ] 99/100, which can be rewritten as [ \| C _ T-N ^ ( I - Z Z ^ ) \| _ 2 1+ _ SI √ 1- _ SI σ _ +1 ] 99/100. Because line 13 of Algorithm~ [ref] guarantees $ σ _ +1 ^2< θ$, we have & = [ \| C _ T-N ^ C _ T-N \|_2 ≤ (1+ _ SI )^2 1- _ SI θ ] \\ & = [ \| ( I - Z Z ^ ) ( C _ T-N ^ )^ C _ T-N ^ ( I - Z Z ^ ) \| _ 2 (1+ _ SI )^2 1- _ SI θ ]\\ & = [ \| C _ T-N ^ ( I - Z Z ^ ) \| _ 2 1+ _ SI √ 1- _ SI √ θ ]\\ & [ \| C _ T-N ^ ( I - Z Z ^ ) \| _ 2 1+ _ SI √ 1- _ SI σ _ +1 ]\\ & 99/100. Take $ _ SI =0.4$, then we have $4 (1+ _ SI )^2 1- _ SI $ and Eq.~ [ref] can be rewritten as [ \| C _ T-N ^ C _ T-N \|_2 ≤ 4θ ] [ \| C _ T-N ^ …

**Proof.** Error bound analysis: The proof of the error bound is roughly the same as that of Proposition~ [ref] . Eq.~ [ref] in the proof of Proposition~ [ref] becomes: B _i=Σ_ j=0 ^ i ( Z _j Z _j^ C _j^ ^ C _j^ + C _j^ ^ C _j^ Z _j Z _j^ - Z _j Z _j^ C _j^ ^ C _j^ Z _j Z _j^ ). We first define the single-update error as: _ i = a _ i ^ a _ i + C _ i-1 ^ C _ i-1 + B _ i-1 - C _ i ^ C _ i - B _ i . Then we have \|Σ_ i=1 ^ T _i \|_2 & Eq.~ [ref] = \| (Σ_ i=1 ^ T a _ i ^ a _ i )- C _ T ^ C _ T - B _ T \|_2 \\ & = \| A _ T ^ A _ T - C _T^ C _T- B _T \|_2 \\ & ≥ \| A _ T ^ A _ T - B _T \|_2 - \| C _ T ^ C _ T \|_2 . \\ Rearranging the terms yields \| A _ T ^ A _ T - B _T \|_2 Eq.~ [ref] ≤ \|Σ_ i=1 ^ T _i \|_2 _ Term 1 + \| C _ T ^ C _ T \|_2 _ Term 2 . \\ Bounding Term 1 is similar to the proof of Proposition~ [ref] , except that from Eq.~ [ref] : ( B _ T + C _T^ C _T ) & =Σ_ i=1 ^ T [ ( B _i+ C _i^ C _i ) - ( B _ i-1 + C _ i-1 ^ C _ i-1 ) ] \\% ) + \| C _ T-N \|_F^2. \\ & = Σ_ i=1 ^ T [ ( a _i^ a _i)- ( _i) ] ≤ \| A _ T \|_F^2 - Σ_ i=1 ^T (σ_ ^2)_i. \\ Therefore, we derive: Σ_ i=1 ^T (σ_ ^2)_i ≤ 1 ( \| A _ T \|_F^2 - ( B _T+ C _T^ C _T ) ) 1 \| A _ T \|_F^2. Finally, Term 1 is bounded by Term 1 1 \| A _T \|_F^2. Bounding Term 2 is similar to the proof of Proposition~ [ref] . Considering line 6 of Algorithm~ [ref] and Lemma~ [ref] yields (w.h.p.): Term 2 = \| C _T^ C _T \| ≤ 4θ 4 \| A _T \|_F^2. In conclusion, we have (w.h.p.) Term 1 + Term 2 O(1) \| A _ T \|_F^2. After that, the remainder of the proof is similar to that of Theorem~ [ref] . Space Complexity Analysis: The proof of the space complexity is roughly the same as the proof of Proposition~ [ref] . Eq.~ [ref] in the proof of Proposition 3.4 becomes: \| C _i^ \|_F^2- \| C _i^ - C _i^ Z _i Z _i^ \|_F^2 2^ j-1 θ_i . \\ Eq.~ [ref] becomes: \| C ^ _ i \|_F^2 - \| C _i \|_F^2 & 2^ j-1 θ_i, \\ \| C ^ _ i \|_F^2 & \| C _ i-1 \|_F^2 + a _ i a _ i ^ , Eq.~ [ref] becomes: \| C _ i-1 \|_F^2 + a _i a _i^ - \| C _ i \|_F^2 2^ j-1 θ_i = k_i θ_i 2 , Plugging $θ_i = \| A _i\|_F^2$ into Eq.~ [ref] and summing from the beginning of the epoch to time $i$, we obtain \| A _i \|_F^2 - \| C _i \|_F^2 Σ_ j=1 ^i k_j \| A _j \|_F^2 2 . Before proceeding, we need the following lemma. Given Eq.~ [ref] , we have the following inequality: \| A _i \|_F^2 k_1 2 Π_ j=2 ^ i (1- k_j 2 ) \| A _1 \|_F^2, where $0< k_j 2 <1$ for $j=1,…,i$ and $k_1=1$. The proof of Lemma~ [ref] is deferred to Section~ [ref] . Taking the logarithm of both sides of Eq.~ [r …

**Proof.** We are given: for each \(i\), Σ_ j=1 ^i \| A _j\|_F^2 Σ_ j=1 ^ i ( k_j 2 Σ_ t=1 ^ j \| A _t\|_F^2 ), where \(\| A _j\|_F^2 > 0\) and \(0 k_j 2 < 1\) (i.e., \(0 k_j < 2\)). Define \(S_i = Σ_ j=1 ^i \| A _j\|_F^2\), then the inequality becomes: S_i Σ_ j=1 ^ i ( k_j 2 ) S_j. We need to prove: S_i k_1 2 Π_ j=2 ^ i (1 - k_j 2 ) \| A _1\|_F^2. Since \(\| A _1\|_F^2 = S_1\), this is equivalent to: S_i k_1 2 Π_ j=2 ^ i (1 - k_j 2 ) S_1. Define \(α_j = k_j 2 \), so \(0 α_j < 1\). Define \(B_m = Π_ j=2 ^ m (1 - α_j)\) for \(m 2\), and \(B_1 = 1\) (empty product). We will prove by induction on \(m 1\), Σ_ j=1 ^ m α_j S_j α_1 B_m S_1. Base case (\(m = 1\)): Σ_ j=1 ^ 1 α_j S_j = α_1 S_1 = α_1 B_1 S_1 holds. Inductive step: Assume that for \(m = k-1\), inequality~ [ref] holds, i.e., Σ_ j=1 ^ k-1 α_j S_j α_1 B_ k-1 S_1. Consider \(m = k\). From the given inequality for \(i = k\): S_k Σ_ j=1 ^ k α_j S_j, so S_k (1 - α_k) Σ_ j=1 ^ k-1 α_j S_j α_1 B_ k-1 S_1. Therefore, S_k α_1 B_ k-1 (1 - α_k) S_1 = α_1 B_k S_1, where \(B_k = B_ k-1 (1 - α_k)\). Hence, α_k S_k α_k · α_1 B_k S_1. Now, Σ_ j=1 ^ k α_j S_j = Σ_ j=1 ^ k-1 α_j S_j + α_k S_k α_1 B_ k-1 S_1 + α_k · α_1 B_k S_1. Since \(B_ k-1 = B_k / (1 - α_k)\), we have α_1 B_ k-1 S_1 = α_1 (1 - α_k) B_k S_1. Thus, Σ_ j=1 ^ k α_j S_j α_1 (1 - α_k) B_k S_1 + α_k · α_1 B_k S_1 = α_1 B_k S_1 (1 - α_k + α_k) = α_1 B_k S_1. Therefore, inequality (1) holds for \(m = k\). By induction, (1) holds for all \(m 1\). Now, for any \(i\), from the given inequality: S_i Σ_ j=1 ^ i α_j S_j α_1 B_i S_1 = α_1 Π_ j=2 ^ i (1 - α_j) S_1. Substituting \(α_j = k_j 2 \), we obtain: S_i k_1 2 Π_ j=2 ^ i (1 - k_j 2 ) S_1 = k_1 2 Π_ j=2 ^ i (1 - k_j 2 ) \| A _1\|_F^2. Hence, Σ_ j=1 ^i \| A _j\|_F^2 k_1 2 Π_ j=2 ^ i (1 - k_j 2 ) \| A _1\|_F^2, as required.

**Proof.** The proof of Theorem~ [ref] is similar to the proof in Appendix~ [ref] . Error Analysis: We denote the matrix $ A ^ B $ at line 31 of Algorithm~ [ref] over the window $(T-N, i]$ as $ A _i^ B _i$. That is, A _i B _i^ = A _i B _i^ +Σ_ j=T-N+1 ^ i ( Z _j Z _j^ A _j^ B _j^ ^ + A _j^ B _j^ ^ H _j H _j^ - Z _j Z _j^ A _j^ B _j^ ^ H _j H _j^ ). We first define the single-update error as: _ i = x _ i y _ i ^ + A _ i-1 B _ i-1 ^ - A _ i B _ i ^ . Then we have \|Σ_ i=T-N+1 ^ T _i \|_2 & Eq.~ [ref] = \| (Σ_ i=T-N+1 ^ T x _ i y _ i ^ )- A _ T B _ T ^ + A _ T-N B _ T-N ^ \|_2 \\ & = \| X _ T-N,T Y _ T-N,T ^ - A _ T B _ T ^ + A _ T-N B _ T-N ^ \|_2 \\ & ≥ \| X _ T-N, T Y _ T-N,T ^ - A _ T B _ T ^ \|_2 - \| A _ T-N B _ T-N ^ \|_2 \\ & Eq.~ [ref] = \| X _ T-N, T Y _ T-N,T ^ - A _ T B _ T ^ \|_2 - \| A _ T-N B _ T-N ^ \|_2 , which can be rewritten as \| X _ T-N,T Y _ T-N,T ^ - A _T B _T^ \|_2 Eq.~ [ref] ≤ \|Σ_ i=T-N+1 ^ T _i \|_2 _ Term 1 + \| A _ T-N B _ T-N ^ \|_2 _ Term 2 . \\ Bounding Term 1 Substituting Eq.~ [ref] into Eq.~ [ref] , we have: _ i = & x _ i y _ i ^ + A _ i-1 B _ i-1 ^ - A _i B _ i ^ \\ & - ( Z _i Z _i^ A ^ _i ^ B ^ _i + A ^ _i B ^ _i ^ H _i H _i^ - Z _i Z _i^ A ^ _i B ^ _i ^ H _i H _i^ ). \\ We take the relationship between the two updates: $ A _i B _i^ = ( I - Z _i Z _i^ ) A _i^ B _i^ ^ ( I - H _i H _i^ )$ (from line 23 of Algorithm~ [ref] ; if line 25 is triggered, then $ Z _i$ and $ H _i$ are considered to be zero), and $ A _i B _i^ = A _ i-1 B _ i-1 ^ + x _i y _i^ $ (line 7), and substitute these into the previous expression to derive the following property: _ i = & A _ i B _ i ^ - ( I - Z _i Z _i^ ) A _i^ B _i^ ^ ( I - H _i H _i^ ) \\ & - ( Z _i Z _i^ A ^ _i ^ B ^ _i + A ^ _i B ^ _i ^ H _i H _i^ - Z _i Z _i^ A ^ _i B ^ _i ^ H _i H _i^ ) \\ = & A _ i B _ i ^ - A ^ _i B ^ _i ^ . \\ Consider lines~8-11 of Algorithm~ [ref] ; from these steps, we have: _ i = U _imin ( Σ _i^2, I (σ_ ^2)_i ) V _i^ & rows ( A _i^ ) ≥ 2 , \\ 0 & else , \\ where $ Σ _i^2$ denotes the squared singular value matrix computed at line 3 of the $i$-th update, and $(σ_ ^2)_i$ represents the $ $-th largest squared singular value in $ Σ _i^2$. (If $ rows ( A _i^ ) < 2 $, we set $(σ_ ^2)_i = 0$.) Next, summing the matrices $ _i$ from Eq.~ [ref] over the interval $(T - N,T]$ and analyzing their $\|·\|_2$ norm, we obtain: \|Σ_ i=T-N+1 ^ T _i \|_2 ≤ Σ_ i=T-N+1 ^ T \| _i \|_2 = Σ_ i=T-N+1 ^T (σ_ ^2)_i. \\ We now aim to bound $Σ_ i=T-N+1 ^T (σ_ ^2)_i$. Let $\|·\|_*$ be the 1-Schatten norm . …

**Proof.** The error bound guarantee is ensured by the decomposability of the $m$ Frequent Directions sketches~ [cite] , meaning that: [Decomposability] Given a matrix $ A R ^ d× n $, we decompose $ A $ into $m$ submatrices, i.e., $ A ^ = [ A _1, A _2, , A _m]$, where $ A _i R ^ d× n_i $ and $Σ_ i=1 ^m n_i = n$. If we construct a matrix sketch with an $ε_i$ covariance error guarantee for each submatrix $ A _i$, denoted as $ B _i$, such that A _i A _i^ - B _i B _i^ _2 ε_i· A _i _F^2. Then $ B = [ B _1, B _2,…, B _m]$ is an approximation of $ A $ and the error bound is A A ^ - B B ^ _2 Σ_ i=1 ^mε_i· A _i _F^2. We let the vectors observed at site $S_i$ form the submatrix $ A _i$. In lines 19-20, the portion of the matrix $ B ^ B $ maintained by the coordinator that comes from site $S_i$ is denoted as $ B _i^ B _i$. In the procedure of each site (lines 1-13 of Algorithm~ [ref] ), apart from replacing ``storing the dumped vector in the queue'' with ``sending it to the coordinator'', the other operations are equivalent to those in Algorithm~ [ref] . Therefore, by Theorem~ [ref] , we obtain: \| A _i ^ A _i - B _i^ B _i \|_2 m \| A \|_F^2. From Lemma~ [ref] , we can obtain: \| A ^ A - B ^ B \|_2 \| A \|_F^2. We then bound the communication cost. We define the periods between two consecutive broadcasts of $ F $ (line 18 of Algorithm~ [ref] ) as a round. The $ F $ of the beginning of some round is $(1+ )$ times the $ F $ of the beginning of the previous round. As the current $ F = \| A \|_F^2$, we have $(1+ )^r \| A \|_F^2$. Therefore, at most $r=O (log_ (1+ ) \| A \|_F^2 )=O ( 1 log \| A \|_F^2 )$ rounds are possible. Then, according to Lemma 3 of~ [cite] : [Lemma 3 of~ [cite] ] After $r$ rounds, at most $O(m· r)$ element update messages have been sent. Instead of changing the weight using a single element, we use the Rayleigh quotient change along each corresponding estimated singular vector in $ Z $. In this setting, the update message for each element is represented by two $d$-dimensional vectors: one taken from a row of $ Z ^ C ^ C $ and the other from a column of $ Z $. Therefore, the total communication cost is $O ( md log \| A \|_F^2 )$. For each site, the procedure is similar to that of Algorithm~ [ref] . The amortized time complexity of Algorithm~ [ref] is $O ( d log d )$, which is the same as that of Algorithm~ [ref] .

**Proof.** The error bound and communication cost guarantees follow from~ [cite] , similarly to those for DA2. As for the time complexity, each site in Algorithm~ [ref] uses as a module, whose amortized time complexity is $O ( d log d )$.

## Bound sentences
- It is also difficult to derive an upper bound for this error.
- At initialization, requires the input vector dimension $d$, the upper bound on the relative covariance error $ $, and a dump threshold $θ$.
- ML- (Algorithm~ [ref] ) solves the Tracking Approximate Matrix Sketch over Sliding Window (Problem~ [ref] ) with space complexity $O ( d log R )$ and amortized time complexity $O ( d log d log R )$ per update, with success probability (the probability that the output sketch satisfies the covariance  …
- We plot the function graph of the success probability lower bound Eq.~ [ref] with respect to the vector dimension \(d\) in Figure~ [ref] .
- As the dimension $d$ increases, the success probability lower bound of the algorithm quickly approaches \( 99 100 \).
- This indicates that when \( d \) is not very small, the success probability lower bound is close to \( 99 100 \).
- The derivation of the upper bound for Term~1 is similar to the idea of FD~ [cite] , and we can conclude that: [display] For the upper bound of Term 2 to hold, both the Power Iteration (line 7) and the Simultaneous Iteration (line 11) at time $T-N$ need to have converged successfully.
- Under the condition of successful convergence and based on Fact~ [ref] , we can always find one level $j$ of Algorithm~ [ref] that satisfies: [display] Finally, we have the probabilistic upper bound of covariance error: [display] (Time complexity) In Algorithm~ [ref] , the update function executes t …
- Moreover, the error may accumulate over updates, and the norm of the restoration error over the sliding window is [display] Proof of Theorem~ [ref] thm:sw ML- (Algorithm~ [ref] ) solves the Tracking Approximate Matrix Sketch over Sliding Window (Problem~ [ref] ) with optimal space complexity $O ( d  …
- Eq.~ [ref] can then be rewritten as: [display] where $k_i$ substitutes $2^j$ at line 12 of Algorithm~ [ref] , representing the upper bound on the $k$ at line~10 of the $i$-th update.
- To prove the lower bound of $ [ \| C _ T-N ^ C _ T-N \|_2 4θ ]$, we consider the upper bound of $ [ \| C _ T-N ^ C _ T-N \|_2 ≥ 4θ ]$.
- For all algorithms, common parameters are the dimension of the vector stream, denoted as $d$, and the upper bound on the relative covariance error, $ $.
