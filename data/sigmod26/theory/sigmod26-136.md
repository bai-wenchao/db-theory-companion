# sigmod26-136 — Improved Lower Bounds for Privacy under Continual Release

flag=True score=72 stmts=30 proofs=20 chars=175182
kinds: {"theorem": 10, "lemma": 13, "corollary": 5, "definition": 2}
counts: {"np_hard": 0, "lower_bound": 114, "upper_bound": 24, "big_o": 86, "omega": 0, "theta": 5, "approx_ratio": 32, "whp": 1, "regret": 0, "dp": 32, "invariant": 3, "convergence": 0, "competitive": 0, "worst_case": 1, "sketch": 3, "cost_model": 0, "cardinality": 1, "learned": 0}

## Statements
**Theorem 1.** Let ε ∈ [0, 1] and δ ∈ [0, 1/3]. Any (ε, δ)-DP incremental mechanism in the exact setting for maintaining the size of the maximum matching, degree histogram, or core number of a vertex0 must have additive error α = Ω(min{T a , nb }), for problem-specific constants a, b ∈ [1/8, 1/2] given in Table 1. Our polynomial lower bounds for the real-valued graph problems are tight in the sense that relaxing to either the static setting or to small multiplicative approximations admit private mechanisms with polylogarithmic additive error. Our bounds exponentially improve the previous lower bounds of Fichtenberger, Henzinger and Ost [FHO21] for maximum matching and degree histogram, and are the first lower bounds for k-core estimation under continual release. We also give improved upper bounds for these problem, significantly reducing the gap between lower and upper bounds in the item-level setting. More specifically, when T was much larger than n, the best mechanisms in the exact setting given by Raskhodnikova and Steiner [RS25] in the fully dynamic case (for the problems whose lower bound reduction was from inner product queries) was the trivial mechanism √ that returns 0 as the answer at all time steps. This led to a n gap between the lower and upper bounds for the problems in Table 1. We present new incremental upper bounds that match our lower bounds up to √ a 12 n factor, drastically reducing the gap.

**Theorem 2.** There exist (ε, δ)-DP incremental mechanisms in the exact setting for maintaining the size of the eε,δ (min{T c , nd }) maximum matching, degree histogram, or core number of a vertex with additive error1 α = O for problem-specific constants c, d ∈ [1/6, 1/2] which are given in Table 1. Norm estimation problem. We show that our lower bound techniques also extend from graph mechanisms to a standard problem for data streams, namely the incremental norm estimation problem. This problem requires maintaining a data structure whose output estimates various norms of the frequency vector under continual insertions of elements. It is especially important in the private setting to estimate multiple norms simultaneously, since querying for each norm separately using composition theorems requires privacy budget proportional to the square root of the number of norms queried, which is prohibitive as the number of queries grow. This problem has been studied extensively under differential privacy in the streaming setting [MMNW11, BBDS12, SST20, WPS22, BMWZ23], and there exist mechanisms with polylogarithmic additive error at the cost of a small (1 + ζ) multiplicative error. Since the goal of these mechanisms is to minimize space usage in addition to the additive error, most works use a variant of a bucketing technique which inherently loses some multiplicative error. Towards discovering the limits of the problem in the continual setting even without space constraints, we show that such multip …

**Theorem 3.** Let ε ∈ [0, 1] and δ ∈ [0, 1/3]. Any incremental (ε, δ)-DP mechanism in√the exact setting for √ TopK that runs with n elements for T timesteps must have additive error α = Ω(min{ 4 T , n}). This is the first lower-bound for this problem, even in the fully-dynamic setting. We complement this result by showing that the problem admits polylogarithmic additive error both in the static setting and 0 1 These problems are formally defined in Section 2. e Here Oε,δ hides poly(1/ε, log(1/δ)) factors, and O(X) = O(X polylog(X)). 4 when allowing small multiplicative error. The static problem can be reduced to computing prefix sums of the sorted frequency vector. For the incremental setting, we show a strong upper bound on estimating all monotone symmetric norms simultaneously (SNE) with small multiplicative error and polylogarithmic additive error (See Definition 1 for a formal definition). As a special case, this problem consists of estimating all the ℓp -norms and the Top-k norms of the frequency vector simultaneously. As point of comparison, there is no known way of getting a mechanism for estimating all ℓp norms from a mechanism that just estimates one, since you cannot use composition theorems over all possible ℓp norms. For Top-k norms, √ using a composition theorem would have an accuracy loss that is at least n, while our result has additive error polylog(n).

**Theorem 4.** Let ε >  0 and ζ ∈ (0, 1/2].  There exists an ε-DP (1 + ζ)-approximate mechanism for IncSNE 6 1 e with additive error O ε2 ζ 5 · log (nT ) · L(e1 ) for any monotone symmetric norm L, where L(e1 ) is the norm of the first standard basis vector.2 Question 2. As our final result, we show that the item-level lower bounds of [RS25] cannot be circumvented by allowing multiplicative error: we give a polynomial lower bound on the product of the additive and multiplicative errors. This implies that the item-level fully dynamic setting is too strong to admit mechanisms with small additive error, even with polylog multiplicative error.

**Theorem 5.** Let ε, δ ∈ [0, 1]. Any (ε, δ)-DP mechanism in the item-level fully dynamic setting with multiplicative approximation η and additive error α for maintaining the size of the maximum matching, subgraph counts, degree histogram, mincut, s-t mincut, or core number of a vertex on n-node graphs for T ≥ Ω(1/δ) time steps must have ( √ Ω(min{ T , n}) when δ = 0 √ η·α= e Ω(min{ 3 T , n}) when 0 < δ < o(1/T ) √ √ for edge count, the lower bounds are Ω(min{ T , n2 }) for δ = 0 and Ω(min{ 3 T , n2 }) for δ ∈ (0, o(1/T )). Even in the exact setting, these constitute the first lower bounds for mincut, s-t-mincut and k-core. Our results show that even relaxing to prohibitive o(n/ polylog(n))-multiplicative approximations √ still does not result in polylogarithmic additive error, and any O( n)-approximate mechanism still needs √ to have Ω( n) additive error for long streams (with T ≫ n). For all the fully dynamic graph problems discussed above, while the item-level setting offers high privacy guarantees, we show that no private mechanism can offer competitive accuracy guarantees. To achieve these bounds we extend the fingerprinting lower bound technique of [HT10, BUV18] to work for mechanisms for 1-Way-Marginals that have both a multiplicative and an additive error. 1-Way-Marginals has served as a central hard problem in differential privacy and reductions from this problem have been used repeatedly to show lower bounds [BST14, JRSS23, JKR+ 23, RS25]. Thus, our strengthened lower bound is lik …

**Lemma 1.** Let ε ∈ [0, 1] and δ ∈ [0, 1/3], and let g be a graph problem that admits an extendable bitwise AND gadget with v(d) vertices and t(d) edges. For any (ε, δ)-DP mechanism for the event-level setting that is (i) solving g in the exact setting and (ii) running on n-node graphs for T timesteps with additive error α, it holds that  np o p α = Ω min t− (T ), v − (n) , where f − is the generalized inverse of a function f : N → N defined as f − (x) = sup{d | f (d) ≤ x}.

**Lemma 2.** There exist extendable bitwise AND gadgets for the following problems and parameters: IncMatching : v(d) = O(d2 ) and t(d) = O(d2 ), IncDegHist : v(d) = O(d) and t(d) = O(d2 ), IncKCore : v(d) = O(d2 ) and t(d) = O(d4 ). We prove this lemma in Section 3.1. Combining Lemma 2 with Lemma 1, we get Theorem 1. 17 Figure 1: Lower bound for IncMatching. The core idea of the reduction is to note that in Qjx , there is an augmenting path for the pink matching if and only if both xi and qij are set to 1. 3.1 Lower Bound Gadgets for Incremental Graph Problems

**Lemma 3.** There exist extendable bitwise AND gadgets for IncMatching with v(d) = O(d2 ) and t(d) = O(d2 ).

**Lemma 4.** There exist extendable bitwise AND gadgets for IncKCore with v(d) = O(d2 ) and t(d) = O(d4 ).

**Lemma 5.** There exist extendable bitwise AND gadgets for IncDegHist with v(d) = O(d) and t(d) = O(d2 ).

**Lemma 6.** Let g : G → R be a monotone function on graphs with constant sensitivity such that for every incremental stream of length T on graphs with n vertices, the output of g lies in the range [L(n, T ), R(n, T )] of length ℓ = R(n, T ) − L(n, T ). Then, for ε > 0 and δ ∈ [0, p1) there is an (ε, δ)-DP mechanism√M that 1/3 e e ℓ/ε). answers g with additive error α, where for δ > 0, α = O(ℓ ln(1/δ)/ε) and for δ = 0, α = O(

**Corollary 1.** For ε > 0 and δ ∈ [0, 1) there we have the following upper bound p for the additive er1/3 ln(1/δ)/ε) and for ror of (ε, δ)-DP mechanisms for IncMatching: for δ > 0, α = Õ(min(n, T ) p δ = 0, α = Õ( min(n, T )/ε). Also, for IncKCore(v), q we have√the following bounds: for δ > 0, √ 1/3 p α = Õ(min(n, T ) ln(1/δ)/ε) and for δ = 0, α = Õ( min(n, T )/ε)

**Lemma 7.** For any ε, δ ∈ (0, 1), there is an (ε, δ)-differentially private mechanism in the exact setting that p e solves IncDegHist with additive error α, where α = O( n log(1/δ)/ε).

**Definition 2.** A set of counting queries Q has sample complexity n∗ for (ε, δ)-DP and (η, α)-accuracy, resp., (η, α, β)-accuracy if n∗ is the smallest number of samples such that there exists an (ε, δ)-DP and (η, α)-accurate, resp. (η, α, β)-accurate mechanism for Q. Definition 3 (1-Way-Marginals(n, d)). In this problem, given a private matrix Y ∈ {0, 1}n×d , the 1 P th mechanism must output an estimation of the average of the j column ( n i Yi,j ) for each j ∈ [d]. 5.1 Pure Differential Privacy Now, we prove the following bound on the sample complexity of approximating (with multiplicative error η and additive error α) this problem under pure differential privacy. This is an extension of the proofs of Hardt and Talwar [HT10] and De [De12] to allow multiplicative approximations. Definition 4 ((η, α)-Accuracy for 1-Way-Marginals(n, d)). Let η ≥ 1 and α ∈ [0, 1] be parameters. An output sequence (a1 , . . . , ad ) is an (η, α)-accurate answer to 1-Way-Marginals(n, d) on database D ∈ ({0, 1}d )n if n n 1 1X 1X · Di,j − α ≤ aj ≤ η · Di,j + α η n n i=1 i=1 for all columns j ∈ [d]. A mechanism M is (η, α)-accurate for 1-Way-Marginals(n, d) if for every database D ∈ ({0, 1}d )n , Pr[M(D) is an (η, α)-accurate answer to 1-Way-Marginals(n, d) for D] ≥ 2/3,

**Theorem 9.** Let η ≥ 1 and α > 0 such that η · α < 21 . For every ε-DP mechanism M : {0, 1}n×d → [0, 1]d for 1-Way-Marginals(n, d) that is (η, α)-accurate, it holds that n = Ω(d/ε). 22 Proof. We prove this theorem by a packing argument. For every d-length binary string a ∈ {0, 1}d , define a = a for all the database Y a ∈ {0, 1}n×d to be an n × d matrix with all of its rows being a, i.e, Yi,j j a i, j ∈ [n] × [d]. For every database Y , the answer to the 1-Way-Marginals(n, d) problem on Y a is the vector a itself. Towards defining the set of acceptable accurate answers of the mechanism, we define the 0-accurate and 1-accurate intervals in R to be: Iz = [η −1 z − α, ηz + α] for z ∈ {0, 1} which implies that I0 = [−α, α] and I1 = [η −1 − α, η + α] In the general case, we define the (asymmetric) accuracy ball Ba around a ∈ {0, 1}d as n o Ba = x = (x1 , . . . , xd ) ∈ Rd xj ∈ Iaj , ∀j ∈ [d] which is the set of all acceptable answers for Y a under the accuracy guarantee of the mechanism. When η · α < 12 , it holds that α < η −1 − α, so I0 and I1 are disjoint. Thus, when η · α < 12 , the sets Ba are disjoint for all a ∈ {0, 1}d . Thus, for every a ∈ {0, 1}d , we have: X Pr[M(Y a ) ∈ Ba′ ] ≤ 1 =⇒ ∃ a′ ∈ {0, 1}d : Pr[M(Y a ) ∈ Ba′ ] ≤ 2−d a′ ∈{0,1}d From the accuracy guarantee of the mechanism we have: Pr[M(Ya′ ) ∈ Ba′ ] ≥ 2 3 The two inequalities above imply the following: Pr[M(Ya′ ) ∈ Ba′ ] 2 ≥ · 2d Pr[M(Ya ) ∈ Ba′ ] 3 However, since M is ε-DP, we have by group privacy that: Pr[M(Ya′ ) ∈ Ba′ ]  …

**Corollary 2.** There exist constants c1 , c2 > 0, such that if n ≤ c1 · d/ε, then for every ε-DP mechanism M : {0, 1}n×d → [0, 1]d that is (η, α)-accurate it holds that η · α > c2 . 5.2 Approximate Differential Privacy We show that the lower bound for approximate DP given by [BUV18] can be extended to mechanisms with multiplicative errors. We follow their proof structure, and recall two definitions that will be used in this section. We first introduce the definition of accuracy that we will be using in this subsection, which allows for a β fraction of the queries to be answered with unbounded error. 23 Definition 5 ((η, α, β)-Accuracy for 1-Way-Marginals(n, d)). Let η ≥ 1 and α, β ∈ [0, 1] be parameters. An output sequence (a1 , . . . , ad ) is an (η, α, β)-accurate answer to 1-Way-Marginals(n, d) on database D ∈ ({0, 1}d )n if n n 1 1X 1X · Di,j − α ≤ aj ≤ η · Di,j + α η n n i=1 i=1 for at least a 1 − β fraction of columns j ∈ [d]. A mechanism A is (η, α, β)-accurate for 1-WayMarginals(n, d) if for every database D ∈ ({0, 1}d )n , Pr[A(D) is an (η, α, β)-accurate answer to 1-Way-Marginals(n, d) for D] ≥ 2/3 Note that if a mechanism M is (η, α, β)-accurate, then for every β ′ such that β ≤ β ′ ≤ 1, M is also (η, α, β ′ )-accurate. Thus, if there is no (η, α, β ′ )-accurate mechanism for 1-Way-Marginals(n, d), then there does not exist an (η, α, β)-accurate mechanism for 1-Way-Marginals(n, d) (for 0 ≤ β ≤ β ′ ≤ 1). This means proving an impossibility result for (η, α, β)-accurate mechanisms  …

**Lemma 8.** Let α, β, γ ∈ [0, 1], ε > 0, δ ∈ (0, 1], η ≥ 1 be parameters and let d, n ≥ 1 be integers. If there exists a γ-reidentifiable distribution D from (η, α, β)-accurate answers to 1-Way-Marginals(n, d), then there is no (η, α, β)-accurate (ε, δ)-DP mechanism for 1-Way-Marginals(n, d) such that   2 −ε 1 e · · − γ − δ > γ. n 3 In particular, for ε = 1, this holds for any δ < 2/(3en) − γ − γ/(en), and so choosing γ = 1/(100n) and δ ≤ 1/(5n) suffices. 24 Proof. Assume to the contrary that there exists an A with such properties. First, by the accuracy guarantee of A we have: Pr[A(D) is not (η, α, β)-accurate for 1-Way-Marginals(n, d)] ≤ 1/3 and from the first property of Definition 6, Pr[A(D) is (η, α, β)-accurate for 1-Way-Marginals(n, d) ∧ (B(D, A(D)) = ⊥)] ≤ γ Taking the complement of this event and a union bound, we get Pr[A(D) is not (η, α, β)-accurate for 1-Way-Marginals(n, d) ] + Pr[(B(D, A(D)) ̸= ⊥)] ≥ 1 − γ which when combined with the accuracy guarantee gives Pr [B(D, A(D)) ̸= ⊥] ≥ D←R D 2 −γ 3 By an averaging argument, there exists an i∗ ∈ [n] such that the following holds:   2 1 ∗ −γ Pr [B(D, A(D)) = i ] ≥ · D←R D n 3 On the other hand, since B is a post-processing of A, it is (ε, δ)-DP. So we have: ∗ Pr [B(D, A(D−i∗ )) = i ] ≥ e −ε D←R D ∗ −ε · Pr [B(D, A(D)) = i ] − δ ≥ e D←R D 1 · · n  2 −γ 3  −δ However, from the second property of Definition 6, we have: Pr [B(D, A(D−i∗ )) = i∗ ] ≤ γ D←R D which leads to a contradiction when −ε e 1 · · n  2 −γ 3  − δ > γ. Now,  …

**Lemma 9.** Given n, d ∈ N and β, γ ∈ [0, 1], if there exists an (n, d)-fingerprinting code with security parameter γ robust to a β fraction of errors, then there exists a distribution D on databases D ∈ ({0, 1}d )n that is γ-reidentifiable from (η, α, β)-accurate answers to 1-Way-Marginals(n, d) if η · α < 12 .

**Lemma 10.** Let α, β ∈ [0, 1], 1 ≤ η, let constant ε0 > 0 and 0 < ε ≤ ε0 . If 1-Way-Marginals(n, d) has sample complexity n∗ for (η, α, β)-accuracy and (ε0 , δ)-differential privacy for δ = O(1/n), then 1-Way∗ Marginals(kn, d) (for k = ⌊ εε0 ⌋) has sample complexity Ω( nε ) for (η, α, β)-accuracy and (ε, δ ′ )-differential privacy where δ ′ (kn) = O(1/(kn)) = O(ε/n).

**Theorem 12.** For ε ≤ 1, d ∈ N, n = O( 2 (ε, O(1/n))-DP mechanism for 1-Way-Marginals that is (η, α, 1/75)-accurate. As (η, α, 1/75)-accuracy implies (η, α)-accuracy, the corresponding statement also holds for (η, α)accuracy. 6 Item-Level Lower Bounds for Fully Dynamic Graph Mechanisms In this section, we will use reductions from 1-Way-Marginals to show strong lower bounds on fully dynamic item-level graph problems. We first introduce the framework of graph problems for which we show our lower bounds. Definition 8 (Marginals Solving Family). Let n0 ∈ N and g be a real-valued graph function. We call a family of tuples {(Hn = (Vn , En ), (e1 , . . . , en ))}n>n0 a marginals solving family for function g with weight w and size (ν(n), ξ(n)) (where ν : N → N is an increasing function of n, ξ : N → N is an increasing polynomial functions of n, and w is a positive number) if the following holds for all n > n0 : • Hn is a graph with ν(n) vertices and ξ(n) edges, i.e., |Vn | = ν(n), |En | = ξ(n), • One of the following cases happen: (Note that the same case should hold for all values of n > n0 ) – e1 , . . . , en ∈ / En and for every subset S ⊆ [n], g((Vn , En ∪ {ei | i ∈ S})) = w · |S|, or – e1 , . . . , en ∈ En and for every subset S ⊆ [n], g((Vn , En \ {ei | i ∈ S})) = w · |S| If there exists such a sequence for a problem g, we say that g has a marginal solving family with weight w and size (ν, ξ). Recall that inserting ⊥ is equivalent to a no-op and has no influence on the graph (and thus the v …

**Corollary 3.** Let ε, δ ∈ [0, 1] and η ≥ 1. For every large enough T and N , if there is a mechanism that is (ε, δ)-DP and (η, α)-accurate for the functions below, then the bound in front of them holds for η and α: • Every g that has a 0-based 1-edge distinguishing gadget of size (ng , mg ) and weight w: q If 0 < δ < √ 3 T T N o(1/T ), then η · α = e Ω(w · min( 2/3 , , )), and if δ = 0, then η · α = e Ω(w · min( T , N , T )). ε ng mg ε √ 3 T e • f≥τ and KCore: If 0 < δ < o(1/T ), then η · α = Ω(min( , N, ε2/3 q T e η · α = Ω(min( ε , N )). √ 3 T e • MinCut: If 0 < δ < o(1/T ), then η · α = Ω(min( , N, ε2/3 q T e Ω(min( ε , N )). √ √ ng mg T )), and if δ = 0, then T )), and if δ = 0, then η · α = √ 3 T • s-t-MinCut: If 0 < δ < o(1/T ), then η · α = e Ω(min( ε2/3 , N, T )), and if δ = 0, then η · α = q e Ω(min( Tε , N, T )). √ 3 T e , N 2 )), and if δ = 0, then η · α = • EdgeCount: If 0 < δ < o(1/T ), then η · α = Ω(min( ε2/3 q T 2 e Ω(min( ε , N )).

**Corollary 4.** Let ε ∈ [0, 1] and δ ∈ [0, 1/3]. Any incremental (ε, δ)-DP mechanism in the exact setting for the data structure √ √ version of SNE that runs with n elements for T time steps must have additive error α = Ω(min{ 4 T , n}). 7.2 Static TopK Upper Bound Next, we show that no lower bound that is polynomial in n can exist for the static TopK estimation problem. Specifically we show how it can be solved using a prefix-sum estimation mechanism as subroutine, thereby giving a mechanism with error polylogarithmic in n. 35 Prefix sum estimation. In the prefix sum estimation problem, the input is a stream u = u1 , . . . , un with ui ∈ R and the output are estimates a1 , . . . , an of the prefix sums. The error is calculated as i α = max a − i∈[n] i X uj j=1 and two streams u and v are said to be neighboring if ∥u − v∥1 ≤ 1. Offline problems. Like in the static setting, all the updates are provided up front in the offline setting. The goal in the offline setting however is to estimate the property after each update, while in the static setting, only one estimate sufficed at the end of the stream.

**Lemma 12.** Any private mechanism for offline prefix-sum estimation can be used to solve the static TopK problem privately with the same additive error guarantees. vectors f u and f v . Let su

**Corollary 5.** There exists an ε-DP mechanism that solves static TopK with additive error O(log2 n/ε). 7.3 Incremental Upper Bound for Monotone Symmetric Norm Estimation with Multiplicative Approximation We show next that no lower bound polynomial in T and n can exist in the incremental setting when a multiplicative error is allowed. More specifically, we show that in the incremental setting there exists 36 an (ε, δ)-differentially private η-multiplicative approximation mechanism with polylogarithmic in T and n additive error for TopK estimation, and even for the harder problem of monotone symmetric norm estimation.

**Theorem 14.** Let ε > 0 and ζ ∈ (0, 1/2]. There exists an ε-DP (1  + ζ)-approximate  mechanism for the 4 1 e 2 5 · log (nT ) · L(e1 ) for a single vector version of IncSNE with the additive error upper bounded by O ε ζ fixed time step t ∈ [T ], where L(e1 ) is the norm of the first standard basis vector. The high-level intuition of our mechanism, is the following: the continual histogram of Lemma 14 are very precise for estimating the size of each large set – more precisely, the estimation is correct up to an (1 ± ζ) factor for each set that has size at least E(n)/ζ, where E(n) is whp the maximum error of the respective continual n-dimensional histogram mechanism H1 over all time steps simultaneously and ζ > 0. Thus, we use a first histogram mechanism with privacy parameter ε/2 to estimate each frequency: let fˆ be the outcome of the histogram. fˆi is correct up to a factor 1 ± ζ for every element i with fi ≥ E(n)/ζ. Let CL = 4 · E(n)/ζ 2 , CU = (4 + 2ζ) · E(n)/ζ 2 and let τ f be a number chosen uniformly at random in [CL , CU ]. Using fˆ, we can construct a private vector which captures the elements with “high" frequency. To do ˆ this formally, we define H f := {j : fˆj > τ f } to be the set of elements with estimated frequency more ˆ than τ f (i.e., the frequencies that are correctly estimated by fˆ). The vector V f is defined as follows: the ˆ ˆ first n − |H f | coordinates are 0, and the next ones are the elements of the multiset {fˆj , j ∈ H f }. Formally, ˆ let j1 , ..., j|H fˆ| be …

**Definition 12.** (Levels, definition 3.1 in [BBC+ 17]) For i ∈ {1, ..., ⌈log1+ζ τ f ⌉}, the i-th level is Bi := f {j ∈ [n] : (1 + ζ)i−1 ≤ fj < min((1 + ζ)i , τ f + 1)}. The vector b ∈ R⌈log1+ζ τ ⌉ records the size of each level: bi := |Bi |. 5 4 L(e1 ) is allowed to not be O(1) here, since this holds for any monotone symmetric norm L, not only a fixed one. Note that in [BBC+ 17], there is no upperbound such as τ f on the frequencies of elements in a level and the levels are simply defined as Bi := {j : (1 + ζ)i−1 ≤ fj < (1 + ζ)i }. However, to answer (1), we only need to care about elements with frequency less than τ f . 5 37 Importantly, note that the definitions above do not include elements with frequency larger than τ f . At each update from the input, the frequency of a single element changes: hence, the size of at most one level increases by 1, and the size of at most one decreases by 1. Thus, we can use a second continual histogram mechanism H2 , this one for estimating privately the size of each level: The input universe of the histogram mechanism consists of ⌈log1+ζ τ f ⌉ many different levels, each new element in the input stream u of the norm estimation mechanism creates up to two entries in the input stream for the histogram mechanism, being +1 when the size of a level increases, −1 when it decreases, and 0 when it is unchanged. Note, however, that the privacy guarantee decreases: two neighboring input streams for the SNE problem can create streams of updates for the level sizes d …

**Theorem 15.** Let ε > 0 and δ > 0. For any 0 < ζ ≤ 1/2, there exist an (ε, δ)-DP mechanism for the vector version of IncSNE such that at any time step t it holds with probability at least 2/3 that  the multiplicative 2.5 0.5 1 approximation is 1 + ζ and the additive error Õ ε2 ζ 5 · log (T ) · log (nT ) log(1/δ) · L(e1 ). The probability in this result can also be boosted for the data structure version, see Section B.5. We note that our mechanisms assume the knowledge of T , the number of time steps: this is for sake of simplicity, and we can use the technique of [CSS11] to extend our mechanism to the case where T is unknown at a multiplicative increase by O((log T )0.1 ) in the additive error bound.6 6 the constant 0.1 can be chosen an arbitrarily small a > 0, and the error becomes O(log T )a /a). See Theorem 4.1 in [CSS11]. 38 Algorithm 4: Output a vector E such that L(E) is a good approximation of L(f ) Input: Stream u1 , . . . , ut of updates Output: At each time step, a vector E ∈ Rn f 2 2 1 Choose τ uniformly at random from the range [4 · E(n)/ζ , (4 + 2ζ) · E(n)/ζ ] f 2 for i ∈ {1, . . . , ⌈log1+ζ τ ⌉} do 3 Bi ← ∅  // Bi consists of elements with frequency in (1 + ζ)i−1 , min((1 + ζ)i , τ f + 1) b f f 4 τ ← 2⌈log1+ζ τ ⌉E(⌈log1+ζ τ ⌉)/ζ ε 5 Initialize two private continual histogram mechanisms, H1 for fˆ (with privacy parameter 2 ) and H2 for b̂ (with ε privacy parameter 2⌈log τ f ⌉ ) 1+ζ foreach update ut do 7 fut ← fut + 1 8 Feed the update into H1 to update fˆ 9 Feed the chan …

**Theorem 19.** There exists an ε-DP mechanism such that given an input stream u = (u1 , . . . , uT ) where ut ∈ {−1, 0, 1} for all t ∈ [T ], at any time step t outputs yt such that with probability 1 − β and some constant c (depending on ε): p  X p p |yt − ut′ | ∈ c log(T ) log(1/β) log(T ) + log(1/β) /ε. t′ ≤t With this neighboring definition, running d binary tree mechanisms [DNPR10, CSS11] for the Continuous Counting Problem concurrently, one per column, gives the following result which we prove for completeness.

**Lemma 13.** There exists an ε-differentially private mechanism for continual n-dimensional histogram such that for any individual with probability 1 − β, the maximum error over all n columns is at most p time step, p  p c log T log(n/β) log(T ) + log(n/β) /ε, for some constant c > 0 (depending on ε), and over all T time steps and all n columns simultaneously with probability 1 − 1/T d , for any constant d > 0, it is O(log(nT ) log T )/ε), i.e., max yjt − j,t X ut′ ≤ t′ ≤t:xt′ =j c(d + 1) log(nT ) log T , ε

**Lemma 14.** Let ε > 0. There exists an ε-differentially private mechanism for continual n-dimensional histogram such that given an input stream u = (u1 , . . . , uT ) where for every t ∈ [T ], ut = (xt , st ) where xt ∈ [n] and st ∈ {−1, 0, 1}, at every time step outputs yjt for every j ∈ [n] such that with constant probability over all time steps t ∈ [T ] and all elements j ∈ [n] simultaneously, the following holds: X X st′ − E(n) ≤ yjt ≤ st′ . t′ :xt′ =j t′ :xt′ =j Typical examples of monotone symmetric norms include the ℓp norms (defined as ℓp (x) := (|x1 |p + 1 . . . |xn |p ) p ), the Top-k norms (the sum of the k largest absolute values of entries of x), and weighted top-k norms. The following fact is a direct consequence of the triangle inequality and the symmetry property of the norms.

## Proofs
**Proof.** Let A be a mechanism that is solving g in the exact setting with additive error α. We denote by A(G) the query value from the mechanism on the graph G. The accuracy guarantee tells us that with probability 2/3, for any graph G generated during the execution of the mechanism, ∥g(G) − A(G)∥ ≤ α. We condition on the event that the accuracy guarantee holds, and given an instance of InnerProduct of dimension d, we show how to privately answer it with an additive error of at most 2α using the 3 This is because t(d) is a positive and increasing function. 16 mechanism for solving g in the exact setting. The reduction is given in Algorithm 1. We choose d to be d = min {t− (T ), v − (n)}, so n′ = v(d) ≤ n and T ′ = t(d) ≤ T . The reduction uses the extendable bitwise AND gadget and starts by creating an empty graph with n′ vertices, inserting the edges of Hinit in arbitrary order. Then, we embed the dataset by inserting edge ei if the corresponding bit xi = 1, and inserting ⊥ otherwise. This leads to the graph Hx1 . Then iteratively for each query q j , we first query the mechanism for A(Hxj ) and store it after evaluation with decj as e h. Then, j j we insert the edges in S in any arbitrary order, resulting in the graph Qx , and query the mechanism again for A(Qjx ) and store it after evaluation decj as qe. Finally, we estimate the inner product ⟨x, q j ⟩ as qe − e h, j+1 j then insert the edges in T in any arbitrary order, resulting in the graph Hx . This creates an instance with n′ vertices and ≤ T ′ timesteps. Neighboring inputs for InnerProduct reduce to event-level neighboring streams for solving g in the exact setting, since changing the value of one bit of x, say the ith bit, changes the insertion of edge ei to the insertion of a ⊥ or vice-versa for solving g in the exact setting. Fix a value of j, and let qe and e h be the values of the corresponding variables after the j th query is inserted and queried. Let q and h be the true query values, namely decj (g(Qjx )) and decj (g(Hxj )). By guarantee of the gadget, we have q − h = ⟨x, q j ⟩ By Lipschitzness of decj and the accuracy of the mechanism, we have |q − qe| ≤ ∥g(Qjx ) − A(Qjx )∥ ≤ α and |h − e h| ≤ ∥g(Hxj ) − A(Hxj )∥ ≤ α and h−α≤e h≤h+α which together give that q − α ≤ qe ≤ q + α Subtracting these two accuracy guarantees and noting that inprodj is as set in Algorithm 1 after the j th iteration is inprodj = qe − e h, we get that ⟨x, q j ⟩ − 2α ≤ inprodj ≤ ⟨x, q j ⟩ + 2α for all j, giving a mechanism fo …

**Proof.** We present the gadget in Figure 1. Formally, Gadget construction. 1. Hinit has vertex set (U j ∪V j )j∈{0}∪[m] ∪U m+1 with |V j | = |U j | = d for all j, and a perfect matching between V 0 and U 1 given by ∪i∈[d] (vi0 , u1i ). 2. ei = (u0i , vi1 ) for all i ∈ [d]. 3. S j = ∪i:qj =1 (uji , vij ), which consists of a matching of all edges between U j and V j whose index i corresponds to the index of a query bit that is set to 1.     j j j , which consists of a perfect matching between V j 4. T j = ∪i∈[d] (vij , uj+1 ) ∪ ∪ (u , v ) \ S i∈[d] i i i and U j+1 and additionally edges between U j and V j to form a perfect matching between them as well. Properties of Hxj and Qjx . Note that for any j, both graphs Hxj and Qjx are the union of (i) vertex-disjoint paths consisting of vertices with the same subscript and (ii) isolated vertices, and for each i ∈ [d] there exists at most one such path. Denote the path corresponding to index i by Pi . Since the size of a matching is not affected by removing isolated vertices, we focus on the paths in both graphs. For each j ≥ 0, note that for each path Pi in Hxj , • if xi = 0, the length of the path Pi is 2j − 1, • if xi = 1, the length of the path Pi is 2j. In both cases, the size of the maximum matching within the path is j. Thus the size of the maximum matching in Hxj is j · d. In Qjx , each path has the following property: • if xi = 0 and qij = 0, then the length of the path is 2j − 1, • if only one of xi and qij is 1, then the length of the path is 2j, 18 • if xi = 1 and qij = 1, then the length of the path is 2j + 1. In the first two cases, the size of the maximum matching of all edges on Pi is j, while in the last case, the size is j + 1. Thus, the size of the maximum matching in Qjx is j · d + ⟨x, q j ⟩ as desired. Retrieving the inner product. Since the size of the matching is j · d in Hxj and j · d + ⟨x, q j ⟩ in Qjx , the difference between them gives the inner desired inner product ⟨x, q j ⟩. Thus, the claim follows. Parameters of the gadget. The number of vertices in the graph is O(d2 ) and the number of edges in the final graph is O(d2 ).

**Proof.** Define the k-core of a graph to be the subset of all vertices of the graph with core number at least k. Note that any vertex with degree < k does not belong to the k-core, and removing all the vertices of degree < k does not change the identity of all k ′ cores for k ′ ≥ k. Gadget construction. 1. Hinit has vertex set {v} ∪ U ∪ (V j ∪ W j )j∈{0}∪[m] with |U | = |V j | = |W j | = d for all j, and a complete graph K2d(m+1) embedded inside ∪j∈{0}∪[m] (V j ∪ W j ), along with a star {v} × (V 0 ∪ W 0 ) and a complete bipartite graph U × V 0 . Thus the core number of all vertices outside {v} ∪ U is at least 2d(m + 1). 2. ei = (v, ui ) for all i ∈ [d]. 3. S j = ∪i:qj =1 ({ui } × (W j−1 ∪ V j )) is a star from ui to all vertices in W j−1 ∪ V j for each query bit i i set to 1. 4. T j = ((U × (W j−1 ∪ V j )) \ S j ) ∪ ({v} × (V j ∪ W j )) is the remaining edges in the complete bipartite graph along with a star from v to V j ∪ W j . Properties of Hxj and Qjx . Define S = ∪s∈{0}∪[j−1] V s ∪ W s as a shorthand. We first discuss Hxj . Note that v has edges only to S ∪ U and the degree of v in Hxj is 2jd + ∥x∥1 , which upper bounds its core number. Since v has edges to all of S, and since S is a complete graph, the core number of v is at least 2jd. Since the degree of all vertices in U is at most 2(j − 1)d + d + 1 < 2jd, they do not belong to any k-core for k > 2jd. Thus the core number of v is exactly 2jd. Next, we discuss Qjx . The degree of v is still 2jd + ∥x∥1 , and we will show that the core number of v is 2jd + ⟨x, q j ⟩. Note that all vertices ui in U with qij = 0 have degree ≤ 2(j − 1)d + d + 1 and do not contribute to any larger cores. Thus the core number of v is at most 2jd + ⟨x, q j ⟩ since none of its neighbors with qij = 0 contribute to its core number. Note that every vertex ui in U with qij = 1 has edges to all of S ∪ V j in Qjx , and thus has degree ≥ 2jd + d. Let us denote this set of vertices of U by U j . Thus, if |U j | ̸= 0, every vertex in U j ∪ S ∪ V j has induced degree ≥ 2jd + d. Thus the subgraph {v} ∪ U j ∪ S ∪ V j of Qjx has minimum degree at least 2jd + ⟨x, q j ⟩ for all vertices, which shows that the core number of v is the same as well. 19 Figure 2: Lower bound for IncDegHist. The core idea of the reduction is to note that in Qjx , the ith vertex in the middle layer has degree j + 1 if and only if both xi and qij are set to 1. Retrieving the inner product. Since the core value of v is 2jd in Hxj and 2jd + ⟨x, q j ⟩ in Qjx , the difference …

**Proof.** Recall that we are reducing from the inner product queries problem with parameters d and m. We present the gadget in Figure 2. Gadget construction. 1. Hinit has vertex set U ∪ V ∪ W ∪ X ∪ Y with |U | = m − d + 3, |V | = d, |W | = d, |X| = m, and |Y | = 3. The initial graph consists of a complete graph Km+3 on U ∪ V and of a complete graph Km+3 on X ∪ Y . 2. ei = (vi , wi ) for all i ∈ [d]. 3. S j = ∪i:qj =1 (wi , xj ) is a star at xj to all vertices in W with the query bit set to 1. i 4. T j = W × {xj } \ Sj is the set of remaining vertices in the star. 5. gj = Πj+1 is the projection onto the (j + 1)st coordinate of the degree histogram as the decoding function for the j th query. Properties of Hxj and Qjx . Note that all vertices in U, V, X, and Y have degree at least m + 2 throughout the stream. We will focus on the degree of vertices in W , which remain smaller than m + 1 at all times. After the j th query is inserted (recall that 1 ≤ j ≤ m), the number of vertices with degree exactly j + 1 in Qjx is equal to ⟨x, q j ⟩. At the same time, the number of vertices with degree j + 1 in Hxj is 0, which means that gj works as the decoding function as stated. 20 Retrieving the inner product. Qjx , the claim follows. Since the number of vertices of degree j + 1 is 0 in Hxj and ⟨x, q j ⟩ in Parameters of the gadget. The number of vertices in the graph is O(d) and the number of edges in the final graph is O(d2 ). 4 Insertions-only Graph Upper Bounds In this section we first use the sparse vector technique (Section C) to show an insertions-only upper bound for a set of graph problems including IncMatching and IncKCore, then we use another mechanism that composes multiple continual counting mechanisms to give an upper bound for IncDegHist (hence for event-level privacy). 4.1 Upper Bound for Monotone Functions using SVT

**Proof.** To this end, we set a parameter k (to be determined later), then we run the SVT mechanism as mentioned in Section C, by setting the query to be the value of the function g itself for the graph of the first i time steps (Gi ) for all i ∈ [T ] and setting ti ’s adaptively from the sequence τj = L(n, T ) + k.j for j ∈ [ kℓ ] ∪ {0}. Note that τ0 = L(n, T ) and τ ℓ = R(n, T ). We start by setting t1 = τ0 and then after each k positive output we set the next threshold to be the next threshold on the τ sequence (ti+1 = ti + k if the answer to the i-th query was positive, ti+1 = ti otherwise). √ Now, in both cases of δ = 0 and δ > 0 there exist allocations for k where k = θ̃( ℓ) for δ = 0 and k = θ̃(ℓ1/3 ) for δ > 0, such that√ since the above threshold condition in the statement of Theorem 25 occurs 2/3 Õ(ℓ ) times for δ > 0 and Õ( ℓ) times for δ = 0. Thus, applying Theorem 25 with setting c according to the mentioned upper bounds, will imply the accuracy guarantees in the statement of this theorem. Note that since the sensitivity of g can be greater than 1, we might need to apply Theorem 25 with scaled privacy parameter; however, this will not change our bounds asymptotically (since we assume constant sensitivity for g).

**Proof.** This follows directly from Lemma 6 since all of the problems are monotone (IncCC is decreasing and the other two are increasing) and the range for the answers of these problems are [0, min( n2 , T )] for Matching, [max(n √ − T, 1), n] for IncCC and [0, min(n, κ)] where κ is the largest integer such that  κ ≤ T (κ = O( T )) for IncKCore(v). 2 4.2 Upper Bound for DegHist In this subsection, we use n separate counters (one for each degree) to calculate the number of vertices with each degree. Then we use the advanced composition theorem (Theorem 18) to find the right privacy 21 parameter for the counters so that the whole mechanism is private. Then, using those parameters, we discuss the accuracy of this mechanism.

**Proof.** For each possible degree d ∈ {0, . . . , n − 1}, we build an ε′ -DP counter fˆd with ε′ = √ ε 2 2n ln 1/δ . Then after receiving an update which inserts an edge (v, u), for each endpoint we insert -1 on its previous degree’s counter, 1 on its new degree’s counter, and 0 on the other counters. Advanced composition theorem implies that the whole mechanism is (ε, δ)-DP. Note that since we are considering the insertions-only case, each vertex can enter each degree once, and exit it once; thus, the sensitivity of each counter is 4 (2 for each ′ endpoint of the edge). Therefore, by applying Theorem 19 with β = θ(n−1 T −1 ), and privacy parameter ε4 (for some constant c, where c comes from the sensitive of degree counters), there exists an ε′ -DP counter e √n/ε). Thus, which except with probability O(1/n), at all time steps simultaneously, has additive error O( the accuracy guarantee for all of the counters hold with a constant probability. 5 Lower Bounds for 1-Way-Marginals In this section, we will first review the definition of sample complexity and 1-Way-Marginals and then prove two different lower bounds for its sample complexity. The first lower bound is for ε-DP mechanism and uses a more restrictive notion of accuracy, namely (η, α)-accuracy than the second lower bound that applies for (ε, δ)-DP mechanisms and uses (η, α, β)-accuracy.

**Proof.** First, we define the distribution D and adversary B. Let A be the (η, α, β)-accurate mechanism for 1-Way-Marginals(n, d). In this construction, D will be Gen itself and B will be Trace(D, roundα (A(D))) where roundα (A(D)) is the output of the mechanism A(D) after doing the following: for each entry of A(D), we round it to 0 if it is less than α, and to 1 otherwise. Also, the common state of D and B will be the same as the common state of Gen and Trace. Now, we will prove the properties of Definition 6. Note that an all 0s (resp. 1s) column in CS corresponds to a bit that must be set to 0 (resp. 1) in c′ . Every mechanism A for 1-Way-Marginals(n, d) that is (η, α, β)-accurate satisfies that for at least (1 − β) · d many of the columns, n n i=1 i=1 1 1X 1X · Di,j − α ≤ aj ≤ η · Di,j + α η n n In particular, for any column that is all 0s or all 1s, the accuracy guarantee η · α < 21 also implies that aj (0) < α < aj (1), where aj (i) is the output on the all is column. Thus, by the definition of Fβ (except for a β fraction of the columns), every entry corresponding to an all 0s or all 1s column is decoded by roundα 26 as 0 or 1, respectively. Thus, by definition of Fβ (D), we have that roundα (A(D)) ∈ Fβ (D). This gives that the first property of error-robust fingerprinting codes implies the first property of Definition 6. Finally, we show that the second property of fingerprinting codes (of Trace wrongly returning an index that does not belong to S) also implies the second property of reidentifiable distributions. For every i ∈ [n]: Pr [B(D, A(D−i )) = i] = D←D Pr [Trace(D, roundα (A(D−i ))) = i] ≤ γ D←Gen where the equation is from the constructions above and the inequality comes from the second property in Definition 7 by applying S = [n] \ i. which completes the proof. Now, we are ready to prove the main result of this section. √ e d), α ∈ [0, 1] and η ≥ 1 such that η · α < 1 , there exists no (1, O(1/n))Theorem 11. For d ∈ N, n = O( 2 DP mechanism for 1-Way-Marginals(n, d) that is (η, α, 1/75)-accurate.

**Proof.** For anyp d ∈ N and γ ∈ (0, 1], it follows from Theorem 10 and Lemma 9 that there exists a function n′ (d, γ) = e θ( d/ log 1/γ) such that for any n ≤ n′ (d, γ) there exists a distribution D on databases D ∈ ({0, 1}d )n that is γ-reidentifiable from (η, α, 1/75)-accurate d), √ answers to 1-Way-Marginals(n, 2 ). We can e d), we have that γ = O(1/n e whenever η · α < 12 . Now, for γ = dc and n ≤ n′ (d, γ) = θ( 1 choose c such that γ ≤ 100n . Thus, we can use Lemma 8 to conclude that for every α ∈ [0, 1], η ≥ 1 √ and every d ∈ N, n ≤ n′ (d, d1 ) = e θ( d), and η · α < 12 , there is no (1, O(1/n))-DP mechanism for 1-Way-Marginals(n, d) that is (η, α, 1/75)-accurate. In particular, this also shows that any (η, α)-approximation mechanism must have η · α ≥ 1/2 = Ω(1). Now, to extend the result above from (ε0 , δ)-DP for constant ε0 to (ε0 , δ)-DP for any ε ≤ 1, we use the following lemma which is similar to Lemma 2.6 of [BUV18] implied by [KLN+ 11]. However, since they only showed the lemma for the case of η = 1, we prove it here.

**Proof.** Let k = ⌊ εε0 ⌋ ≥ 1. We prove the contrapositive. We show that for every n, if there exists an (ε, δ ′ )-DP (for a suitable δ ′ (n) = O(1/n) to be chosen later), (η, α, β)-accurate mechanism M′ (·) for 1-Way-Marginals(kn, d) with k · n = ⌊ εε0 ⌋n = O( nε ) samples, then there exists an (ε0 , δ)-DP (η, α, β)accurate mechanism M(·) with δ(n) = O(1/n) that solves 1-Way-Marginals(n, d) with n samples. Now, we assume that M′ exists and show how to construct M(X). Let X ∈ ({0, 1}d )n be the private input dataset with n samples that M receives. M simply outputs M′ (f (X)), where f (X) ∈ ({0, 1}d )kn is a dataset constructed by k copies of X. Since the correct answers of 1-Way-Marginals(·, d) are equal for X and f (X), M is (η, α, β)-accurate. Now, we prove that M is (ε0 , δ)-DP. For every pair of neighboring datasets X, X ′ and subset of outputs R, we have the following: Pr[M(X) ∈ R] = Pr[M′ (f (X)) ∈ R] ≤ ekε · Pr[M′ (f (X ′ )) ∈ R] + (1 + eε + . . . + e(k−1)ε ) · δ ′ (kn) ekε · δ ′ (kn) eε − 1 ekε ′ ≤ ekε · Pr[M′ (f (X ′ )) ∈ R] + · δ (kn) ε eε0 ′ ≤ eε0 · Pr[M′ (f (X ′ )) ∈ R] + · δ (kn), ε ≤ ekε · Pr[M′ (f (X ′ )) ∈ R] + 27 where the first inequality holds since M′ is (ε, δ ′ )-DP and f (X) and f (X ′ ) are different in k rows and the last inequality holds since kε ≤ ε0 . ε To show that M is (ε0 , δ)-DP, for each n′ ∈ N and δ(n) = O(1/n), we set δ(n) = eε0 · δ ′ (kn), which ′ implies that δ ′ (n′ ) = eεε0 · δ( nk ). Since δ(n′ /k) = O(k/n′ ), and since ε0 is a constant, we get     ε  k 1 0 ′ ′ δ (n ) = O ε · ′ = O =O ′ n n n′ as well, as required. Thus, from the inequalities above we have: Pr[M(X) ∈ R] ≤ eε0 · Pr[M′ (f (X ′ )) ∈ R] + δ(n) = eε0 · Pr[M(X)) ∈ R] + δ(n) Therefore, M is (ε0 , δ)-DP. Note that the above arguments also hold for any set of counting queries. Now, by Lemma 10 and Theorem 11, we have the following: √ e d/ε), η ≥ 1 and α ∈ [0, 1] such that η · α < 1 , there exists no

**Proof.** Assume we are given an instance Y of 1-Way-Marginals(n, d) with d columns and n rows. First, we show how the graph sequence is built and then we show the two properties mentioned in the lemma. Assume without loss of generality that the marginals solving family (Hn = (Vn , En ), (e1 , . . . , en )) considered for g is in the first case of the definition (e1 , . . . , en ∈ / En ). Recall that the number of vertices is ν(n) and the number of edges in the initial graph is ξ(n). The graph of this instance will be exactly the graph Hn from the marginals solving family. Intuitively, each one of edges ei is corresponding to the ith row. We are going to first add the edges En , and then in d rounds we are going to use this graph to answer the column sum for each one of the columns in Y by modeling the bits set in the column by the subset of edges from {e1 , . . . , en } that are present in the graph. Formally, in the time steps 1 ≤ t ≤ ξ(n) the edges of En are inserted. Then, in the j th round for each j ∈ [d] (tj−1 < t ≤ tj ), in time steps tj−1 + i for each i ∈ [n], if Yi,j = 1 we insert ei and otherwise we insert ⊥. Then, in the second half of this round (for the next n time steps), at time step tj−1 + n + i we undo what we did in time step tj−1 + i. The reduction is formalized in Algorithm 2. Note that inserting or deleting edge e (for any edge e) means inserting (+, e) or (−, e) in the input stream, respectively. For the first property of the lemma, note that all of the updates in the graph sequence corresponding to the ith row are done to the edge ei . Thus, the graph sequences corresponding to the neighboring databases Y, Y ′ are item-level edge-neighboring. Regarding the second item, note that after time stepPt0 the graph is Hn and after time step tj − n the graph is (Vn , En ∪ {ei | Yi,j = 1}, thus g(Gtj −n ) = w ni=1 Yi,j . Now we are ready to prove Theorem 13.

**Proof.** Given a mechanism that is (ε, δ)-DP for solving g, we will first choose integer values for n and d such that ν(n) ≤ N , ξ(n) + 2nd ≤ T√ and (1) for δ = 0, n ≤ c1 d/ε, where c1 is the e d/ε) as needed for Theorem 12. Then we will use constant from Corollary 2, and (2) for δ > 0, n = O( the reduction from Lemma 11 to construct a fully dynamic graph stream on ν(n)-node graphs and length 29 Algorithm 2: Reduction from 1-Way-Marginals to g with a Marginals Solving Family {(Hn (Vn , En ), (e1 , . . . , en ))}n>n0 of size (ν(n), ξ(n)) and weight w = d n Input: A private dataset Y ∈ ({0, ) P1} n 1 Output: Marginal sums yi = n i=1 Yi,j 1 Set N = ν(n) and T = ξ(n) + 2dn. 2 Initialize an (ε, δ)-DP mechanism A solving g with N nodes and T time steps with multiplicative error η and additive error α. 3 Insert edges of En into the graph of A in any arbitrary order. 4 for j = 1, . . . , d do 5 for i = 1, . . . , n do 6 if Yi,j = 1 then 7 Insert ei to the graph of A 8 else 9 Insert ⊥ to the graph of A 10 yej ← A on the current graph divided by wn. 11 for i = 1, . . . , n do 12 if Yi,j = 1 then 13 Delete ei from the graph of A 14 else 15 Insert ⊥ to the graph of A ξ(n)+2nd such that any (η, α)-accurate algorithm for the function g on this stream gives an (η/(n·w), α)accurate algorithm for 1-Way-Marginals(n, d). Note that an (η/(n · w), α)-accurate mechanism is also an (η/(n · w), α, 0.75)-accurate mechanism. Then, since we show that a mechanism to solve g for N and T can solve any instance of 1-Way-Marginals(n, d) (with the privacy and accuracy guarantees mentioned above), we apply the lower bounds from Section 5 to finish the proof. The details are as follows. Case 1: δ > 0: We set $√ % $√ %!! j k d d 2/3 d = (T ε) and n = min p(d) , ν − (N ), ξ − T − 2d p (d) ε ε √ where p(d) hides the polylogarithmic factors in Theorem 12 (so Theorem 12 holds for n ≤ dp(d)/ε). As discussed in Section 5.2, we have that there exist√constants c1 > 0 and c2 < 0 such that p(d) ≤ c1 logc2 d. Now, we show that the conditions n = e O( d/ε) (according to Theorem 12), ν(n) ≤ N and ξ(n) + 2nd ≤ T hold. The first condition trivially follows from the first argument in the minimum used in construction of n and the second condition follows from the second argument of the √ minimum and since − ν is increasing. For the third condition, we know that since n ≤ ξ T − 2d⌊ dp(d)/ε⌋ , we have √ ξ(n) ≤ T − 2d⌊ dp(d)/ε⌋. Thus, j√ k ξ(n) + 2d dp(d)/ε ≤ T (1) On the other hand, we have that ξ(n) + 2nd ≤ ξ(n) + 2  …

**Proof.** We will prove the bounds by showing marginals solving families for each one of the problems mentioned above. The lower bounds follow directly from Theorem 13. • g that has a 0-based 1-edge distinguishing gadget (H, e) of size (ng , mg ) and weight w: We will make n copies of H and call the copies of e on them e1 , . . . , en . One can see that this is a marginals solving family for function g with weight w and size (θ(nng ), θ(nmg )). • f≥τ : Let Hn = (Vn , En ) be as follows: Vn = {v1 , . . . , vn } ∪ {u1 , . . . , un } ∪ {z1 , . . . , zn }. Then, we give the vertices v1 , . . . , vn a cyclic order (so v1 is right after vn ) and for each i ∈ [n] put an edge τ −1 between vi and all of the ⌊ τ −1 2 ⌋ vertices before and all of the ⌊ 2 ⌋ vertices after it. So now every vertex vi (for all i ∈ [n]) has degree τ − 1 if τ is odd, and has degree τ − 2 if τ is even. Then, if τ is even, we add the edge (vi , ui ) for all i ∈ [n]. Thus, in the graph Hn , every vertex vi (for i ∈ [n]) has degree τ − 1 and all other vertices have degree 0 or 1. Now, we set the edges e1 , . . . , en to be ei = (vi , zi ) for each i ∈ [n]. It can be seen that for every τ ≥ 2, after adding any subset S of edges {e1 , . . . , en }, there will be exactly |S| vertices with degree τ and other vertices’ degree will be less. For τ = 1, it will be 2|S| such vertices and others will have degree 0. Therefore, we have a marginals solving family for f≥τ of size (θ(n), O(n2 )) (since τ < n) and weight 1 for τ ≥ 2 and weight 2 for τ = 1. 32 • MinCut: Let Hn consist of a vertex v and n vertices that form a Kn . Also, let the edges e1 , . . . , en be the edges between v and the n vertices of the Kn . The minimum cut of the graph after adding each subset of the edges {e1 , . . . , en } will be ({v}, Vn \ {v}). Thus, Hn is a marginals solving family for this problem with weight 1 and size (θ(n), θ(n2 )). • s-t-MinCut: Hn contains 2 distinguished vertices s and t, and n vertices such that t has an edge to all of them and e1 , . . . , en will be the edges between these vertices and s. After adding any subset of the edges {e1 , . . . , en }, the minimum (s, t)-cut will be ({s}, Vn \ {s}). So Hn is a marginals solving family for this problem with weight 1 and size (θ(n), θ(n)). • KCore(v): We will take exactly the same family as s-t-MinCut. Here also the answer will be the number of edges incident to v (e1 , . . . , en ) existing in the graph. Thus, it has a marginals solving family with weight 1 and size (θ …

**Proof.** Let A be a mechanism that solves the data structure version of IncTopK. We denote by A(k) the query value from the mechanism for the value of the Top-k norm of the current frequency vector f t . The accuracy guarantee tells us that with probability at least 2/3, at all times t ∈ [T ] and for all k ∈ [n], |A(k) − ∥f t ∥Top-k | ≤ α. We condition on the event that the accuracy guarantee holds, and given an instance of InnerProduct of dimension d, we show how to privately answer it with an additive error of at most 2α + 1 using the mechanism for IncTopK. The reduction is given in Algorithm 3. We maintain the invariant that before query j is inserted, element i is present j times if xi = 1, and j − 1 times if xi = 0. The reduction starts by inserting element i if xi = 1, and inserting ⊥ otherwise. 33 Algorithm 3: Reduction from InnerProduct to IncTopK Input: A secret dataset x ∈ {0, 1}d , and m = ψd queries q1 , . . . , qm with q j ∈ {0, 1}d . Output: Inner product answers ⟨x, q 1 ⟩, . . . , ⟨x, q m ⟩ 2 1 Set n = d and T = d + 2ψd 2 Initialize an (ε, δ)-DP mechanism A for IncTopK with n elements for T timesteps with additive error α. 3 for t = 1, . . . , n do 4 if xt = 1 then insert element t else insert ⊥ ▷ insert dataset x 5 for j = 1, . . . , m do 6 for t′ = 1, . . . , n do 7 if qtj′ = 1 then insert element t′ else insert ⊥ ▷ insert query j 8 Set inprodj as the smallest k ∈ {1, . . . , n} such that A(k) < (j + 1)k − α 9 for t′ = 1, . . . , n do 10 if qtj′ = 0 then insert element t′ else insert ⊥ ▷ neutralize query j Figure 3: Idea for IncTopK lower bound reductions. This shows the structure of TopK values after the j − 1st query has been inserted. It increases by j until k ∗ = ⟨x, q j−1 ⟩, then reduces to at most j − 1 later. The left figure shows the accuracy guarantee, while the right figure shows how kAlg = inprodj−1 is chosen. The goal is to accurately estimate k ∗ by kAlg . The worst case choice of kAlg occurs at the latest k ′ when the red-shaded region on the left figure is completely disjoint from the purple-shaded region on the right figure. For query q j , we perform a sequence of n insertions, by inserting element i if qij = 1, and inserting ⊥ otherwise. After these insertions, we have that • if xi = 0 and qij = 0, then element i is present j − 1 times, • if only one of xi and qij is 1, then element i is present j times, • if xi = 1 and qij = 1, then element i is present j + 1 times, We use this to estimate the inner product value as explained bel …

**Proof.** Let u and v be two neighboring inputs for static TopK with frequency P v u and s be the frequency vectors sorted in decreasing order. Thus ∥f ∥Top-k = ki=1 sui for any k ∈ [n]. Since u and v are neighboring inputs for TopK, there exists one index k ∗ such that their frequency vectors agree on all indices except for k ∗ , and it holds that |fku∗ − fkv∗ | = 1. Thus the frequency vectors in particular satisfy ∥f u − f v ∥1 ≤ 1. Assume without loss of generality that fku∗ + 1 = fkv∗ . u . Note that swapping the indices of different Let σ be the permutation used to sort f u , i.e., sui = fσ(i) elements with the same frequency do not affect the sorted frequency vector. To this end, consider the permutation π that swaps the index σ −1 (k ∗ ) to be the first index with frequency fku∗ . Thus, i′ = min{i ∈ u = f u }, and π swaps the positions of i′ and σ −1 (k ∗ ). Then π ◦ σ is still a sorting permutation [n] | fσ(i) k∗ u for f . We will show that π ◦ σ is a sorting permutation for v as well. The sorting property holds for all pairs i, j ̸= σ −1 (k ∗ ) since the streams u and v coincide on all u coordinates other than k ∗ . Fix j = σ −1 (k ∗ ). For all i with i > σ −1 (k ∗ ), it held that fσ(i) > fku∗ (since v ≥ f v . For all i with σ −1 (k ∗ ) is the first index in su with frequency fku∗ ), and thus it still holds that fσ(i) k∗ −1 ∗ u u v v i < σ (k ), it held that fσ(i) ≤ fk∗ , and thus it still holds that fσ(i) ≤ fk∗ . Since the ℓ1 -norm is permutation invariant, and since the s vectors are obtained by applying π ◦ σ to the corresponding f vectors, we have that ∥su − sv ∥1 ≤ 1 as well. Thus, since estimating all the prefix-sums of s accurately is the same as estimating all the TopK norms of f accurately, using an α-additive error mechanism for prefix sum estimation on the streams su (with answers ak ) gives that for all k ∈ [n], k k a − ∥f ∥Top-k = a − k X sui ≤ α. i=1 Using the continual counting mechanism of Dwork et al. [DNPR10] and Chan et al. [CSS11] with additive error O(log2 n/ε), we get the following corollary.

**Proof.** Consider the following procedure A for ε-differentially private histograms under continual observation. During initialization A starts n ε-DP continual counting mechanisms A1 , ..., An be n that use independent randomness and then starts processing the input stream u. At each time step t given the input ut = (j, st ) for some j ∈ [n] the mechanism creates n streams of input, one for each column of the histogram, and feeds st to Aj and feeds 0 to all other counting mechanisms. In this way A creates n input streams. Let us denote by Ii the stream for column i. We show first that if each Ai is ε-DP, then Q A is ε-DP. For a dataset I and any potential solution s = (s1 , ..., sn ) we have Pr[A(I) = s] = ni=1 Pr[Ai (Ii ) = si ] as the random choices of the Ai are independent. Now consider two neighboring streams u, u′ . Note that, by assumption, only one of the streams Ii differs from Ii′ , and only at a single time step. Thus the ratio of probability is n Y Pr[Ai (Ii ) = si ] Pr[A(I) = s] = . ′ Pr[A(I ) = s] Pr[Ai (Ii′ ) = si ] i=1 44 The ratio inside the product is equal to 1 for all Ii where Ii = Ii′ , and since the Ai are ε-DP, the ratio is at most exp(ε) for the remaining one. Therefore, Pr[A(I) = s] ≤ exp(ε), Pr[A(I ′ ) = s] which shows that A is ε-differentially private. We use the continual counting mechanism from Theorem 19 with parameters β ′ = β/(nT f +1 ) as mechanism Ai for each i ∈ [n]. time step t and p Thus, at any  each i ∈ [n] the additive error is at most p p cε−1 log(T ) log(nT f +1 /β) log(T ) + log(nT f +1 /β) = c(f +1)ε−1 log(T ) log(nT /β) with probability 1 − β/(nT ). Thus with probability 1 − β/T f simultaneously over all time steps and all i ∈ [n], the additive error is at most c(f + 1)ε−1 log(T ) log(nT /β). Throughout this section, we will use E(n) to denote the error bound of a private n-dimensional histogram mechanism. Subtracting E(n) from the output frequency of the previous lemma gives Lemma 14. B.2 Differentially Private Histogram Differentially private histogram refers to the problem of privately estimating the column sum of an ndimensional table with entries from {−1, 0, 1}, while at every time step a new n-dimensional row is added to the table containing at most one non-zero entry. Formally, we are given an input stream u = (u1 , . . . , uT ) where for every t ∈ [T ], ut = (xt , st ) where xt ∈ [n] is the updated column and st ∈ {−1, 1}. The goal is t to, at every time step , ynt ) such that for every j ∈ [n], yjt is a goo …

**Proof.** To show this lemma, we will essentially show that there are few non-zero entries in fi L and then note that, by the definition of RL , all of them have frequency at most τ f . Using Fact 1 we will conclude. Recall that RL contains all elements j with (a) fj ≤ τ f and (b) that belong to a level i with b̂i < τ b . Note that (a) implies fˆj ≤ τ f , as fˆj ≤ fj . Thus for each level Bi either all of its elements are in RL or none of them are, i.e., either Bi ⊂ RL or Bi ∩ RL = ∅. Thus, for each element j ∈ RL , the level Bi to which j belongs does not change its cardinality, when the set of elements is restricted to the elements in RL . Let B RL be the set of the levels of the elements in RL , which is equivalent to say that it is the set of levels computed for the frequency vector f |RL . As for every i ∈ B RL , b̂i < τ b , Lemma 14 implies that bi ≤ (1 + ζ)τ b . Recall that the upper bound on the frequency of the elements in Bi is at most (1 + ζ)i . Therefore, we have the following: ⌈log1+ζ τ f ⌉ ℓ1 (f |RL )≤ X i |Bi |(1 + ζ) ≤ X (1 + ζ)τ b · (1 + ζ)i i=1 i∈RL f (1 + ζ)⌈log1+ζ τ ⌉+1 − 1 ≤ (1 + ζ)τ · (1 + ζ) − 1 3 (1 + ζ) ≤ τfτb ζ b 3 Therefore, Fact 1 implies that L(f |RL ) ≤ τ f τ b (1+ζ) · L(e1 ). ζ As RH may consist of many elements, we cannot show that f RH has a tiny norm. However, the random choice of τ f ensures that each element is in RH with only a small probability: therefore, we can show that the norm of f |[n]\RH is very similar to the norm of f , as each element is preserved with large probability.

**Proof.** The right-hand-side inequality is a direct consequence of the monotonicity of the norm. In the rest of the proof we show the left-hand-side. The elements in RH are exactly the ones with true frequency fj > τ f and estimated frequency fˆj ≤ τ f . Thus they are all contained in the set of potentially 2) dropped elements D = {j : fj ∈ [ ζ42 · E(n), (4+4ζ+ζ · E(n)]}: since fj − E(n) ≤ fˆj ≤ fj (by E t ), RH is ζ2 a subset of {j : fj ∈ [τ f , τ f + E(n)]}. The choice of τ f ∈ [ ζ42 · E(n), (4+4ζ) · E(n)] ensures that RH ⊆ D. ζ2 We proceed as follows: (1) We show that, with probability at least 1/2, RH is only an ζ fraction of the elements in D. (2) We show that when this happens, for any monotone symmetric norm, all elements in D contribute roughly the same amount to the norm, and that this implies that replacing frequencies of RH by 0 does not change the norm significantly. (1) From Lemma 14 we have that for any element |fj − fˆj | ≤ E(n). Recall that elements in RH are exactly the ones with fˆj ≤ τ f < fj . Furthermore, τ f is chosen uniformly at random in an interval of length E(n)/ζ. Thus, for any j ∈ D, the probability that j ∈ RH equals the probability that τ f belongs to the interval [fˆj , fj ]. This probability is at most ζ/4, as the interval [fˆj , fj ] has size at most E(n) and τ f is chosen in an interval of size 4E(n)/ζ, i.e., 4/ζ times bigger. Thus, the probability that j ∈ D belongs to RH is at most ζ/4. Therefore, in expectation over the choice of τ f , it holds that Eτ f [|RH |] ≤ ζ|D|/4, and Markov’s inequality ensures that, with probability at least 3/4 over the choice of τ f , |RH | ≤ ζ|D|. We condition on this event for the rest of the proof. 47 2 ) (2) Let f ′ be the frequency vector where each frequency in D is replaced by (4+4ζ+ζ · E(n) and the ζ2 vector f ′′ = f ′ |[n]\RH – i.e., f ′ where we zero out the coordinates in RH . Formally,  ( /D  fj if j ∈ fj if j ∈ /D (4+4ζ+ζ 2 ) ′′ ′ fj = fj = (4+4ζ+ζ 2 ) · E(n) if j ∈ D \ RH ζ2  · E(n) if j ∈ D  ζ2 0 otherwise. Note that for any j, it holds that fj ≤ fj′ ; and additionally, |[n]\RH fj′′ ≤ (1 + 2ζ)fj (4) Indeed, for j ∈ / D, we directly have fj = fj′′ , and for j ∈ D \ RH , it holds that fj ≥ ζ42 · E(n), which 2 2 ) ) implies fj′′ = (4+4ζ+ζ · E(n) ≤ (4+4ζ+ζ · fj ≤ (1 + 2ζ)fj as ζ ≤ 1/2. 4 ζ2 Next order the values in D in increasing order and consider the permutation σ : Rn → Rn that performs a cyclic over the indices of D, (i.e., that shifts each index of D to the next one, and the la …

**Proof.** The upper-bound is a direct consequence of monotonicity. For the lower bound, we first observe that f |[n]\RH = f |[n]\R + f |RL . Using the definition of norms it follows that L(f |[n]\RH ) ≤ L(f |[n]\R )+L(f |RL ). Conditioned on Et , Lemma 18 shows that L(f |[n]\RH ) ≥ (1 − 4ζ)L(f ) with probability at least 3/4. Thus, by adding the two equations we have that the lower bound holds with probability at least 3/4. 48 B.3.3 Estimating the norm for the important elements fˆ b̂ It remains to show how to estimate privately the norm of f |(H ∪H ) . For this, we simplify the frequency vector even further, rounding all coordinates of heavy levels and rounding the number of elements in each level. To formalize this idea, we use the idea of level vectors, defined as follows. Recall that hb̂ = {i : b̂i ≥ c log0.5 (nT ) log T }. ζ·ε Definition 14 (Level Vectors and Estimated Level Vectors, see Definition 3.3 in [BBC+ 17]). For any level i ∈ hb̂ , the level vector Vi is Vi := ( 0, . . . , 0 , (1 + ζ)i , . . . , (1 + ζ)i , 0, . . . , 0) ∈ Rn | {z } | {z } P bk k<i,k∈hb̂ bi P i.e., it is a n-dimensional vector consisting of k≤i,k∈hb̂ bk many zeros, followed by bi many ones, followed by all zeros. The estimated private level vector Ei is: Ei := ( 0, . . . , 0 , (1 + ζ)i , . . . , (1 + ζ)i , 0, . . . , 0) ∈ Rn | {z } | {z } P b̂k k<i,k∈hb̂ b̂i Also, we define the following auxiliary vector Wi (where the number of zeros depends on b instead of b̂) as follows: Wi := ( 0, . . . , 0 , (1 + ζ)i , . . . , (1 + ζ)i , 0, . . . , 0) ∈ Rn | {z } | {z } P bk k<i,k∈hb̂ b̂i P Note that (conditioned on E t ) these vectors are indeed in Rn , since bi ≤ n, and Lemma 16 ensures b̂i ≤ bi , so the non-zero indices in the definitions above will not exceed n. These vectors are used to represent the frequency of the elements in the corresponding levels, i.e., Vi (respectively Wi and Ei ) represents the rounded frequencies based on bi (respectively estimated b̂i ) of elements in Bi . We showed in Lemma 15 that Ei is private, but Wi is not (because the initial number of zeros is defined with the true level sizes bk ). We introduced Wi only to simplify the proof of utility. The vectors Ei are helpful for representing items that have low frequency; we now introduce vectors ˆ that will serve as proxy for the high frequency elements. Recall that H f = {j : fˆj > τ f } is the set of ˆ indices of high frequency elements. Let {j1 , . . . , j|H fˆ| } be the elements of H f . We next define two vectors ˆ …

**Proof.** Since for every element in j ∈ H f , a unique coordinate of V ′ is equal to fj and for every level ˆ i ∈ hb , bi coordinates of V ′ are equal to (1 + ζ)i and since H f and H b̂ are disjoint, there exists a one to ˆ |(H b̂ ∪H f ) one mapping σ : [n] 7→ [n] such that fi of monotone symmetric norms implies that b̂ ˆ |(H b̂ ∪H f ) ′ ≤ Vσ(i) ≤ (1 + ζ)fi fˆ b̂ fˆ . Therefore, the monotonicity L(f |(H ∪H ) ) ≤ L(V ′ ) ≤ (1 + ζ)L(f |(H ∪H ) ) (6) We first show the first inequality of the lemma: Corollary 6 shows that conditioned on Et it holds with b̂ fˆ probability at least 3/4 that (1 − 3ζ)L(f ) − L(f |RL ) ≤ L(f |H ∪H ). Combined with the left-hand-side of (6) this concludes the proof of the first inequality. b̂ fˆ For the second inequality, simply apply the inequality L(f |(H ∪H ) ) ≤ L(f ) into the right-hand-side of (6). As mentioned earlier, the vector V ′ is non-private for several reasons: in particular, in Vi , the number of non-zero elements is bi , which is not private; and the high frequencies are exactly released in V f . To make a step towards privacy, we define W as follows: X ˆ W := Wi + V f i∈hb̂ The next lemma shows that L(W ) is a good approximation of L(V ′ ). 1 L(V ′ ) ≤

## Bound sentences
