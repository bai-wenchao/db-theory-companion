# sigmod26-306 — Efficient Defective Clique Enumeration and Search with Worst-Case Optimal Search Space

from=tex flag=True score=38 stmts=18 proofs=9 chars=281974
kinds: {"lemma": 5, "theorem": 4, "claim": 2, "observation": 1, "definition": 6}
counts: {"np_hard": 3, "lower_bound": 2, "upper_bound": 19, "big_o": 45, "omega": 7, "theta": 1, "approx_ratio": 0, "whp": 0, "regret": 0, "dp": 0, "invariant": 0, "convergence": 0, "competitive": 0, "worst_case": 29, "sketch": 0, "cost_model": 0, "cardinality": 0, "learned": 0}

## Statements
**Definition Colorful degeneracy ordering.** Given a graph $G=(V, E)$ and a coloring $ $ of $G$, an ordering $(v_1, v_2, …, v_n)$ of its vertices $V$ is a colorful degeneracy ordering with respect to $ $ if, for each $1 i n$, $v_i$ is a vertex with the smallest colorful degree with respect to $ $ in the subgraph of $G$ induced by $\ v_i, v_ i+1 , …, v_n\ $.

**Definition Degeneracy ordering~\cite{matula1983smallest.** Given a graph $G=(V, E)$, an ordering $(v_1, v_2, …, v_n)$ of its vertices $V$ is a degeneracy ordering if, for each $1 i n$, $v_i$ is a vertex with the smallest degree in the subgraph of $G$ induced by vertices $\ v_i, v_ i + 1 , …, v_n\ $.

**Lemma .** Given a graph $G=(V, E)$, a coloring $ $ of $G$, and positive integers $k$ and $q$, let $S$ be a $k$-defective clique of size at least $q$. Then, every vertex $u S$ has colorful degree $d^ _ S (u) q - k - 1$ in the subgraph induced by $S$.

**Definition Colorful degree.** Given a graph $G=(V, E)$ and a coloring $ $ of $G$, the colorful degree of $u V$ in $G$ with respect to $ $, denoted by $d^ _G(u)$, is the number of distinct colors among $u$'s neighbors. Formally, $d^ _G(u) = |\ (v): v N_G(u) \ |$.

**Lemma $k$-defective clique pivoting.** Given an instance $(S, C, X)$ of a recursive call where $N_C(S) ≠ $, let $p$ be a vertex in $N_C(S)$. Every maximal $k$-defective clique that includes $S$ and is included by $S C$ must contain at least one vertex in $ _C[p]$.

**Definition Colorful $s$-core.** Given a graph $G=(V, E)$, a coloring $ $ of $G$ and an integer $s$, the colorful $s$-core of $G$ with respect to $ $ is the maximal subgraph $g$ of $G$ in which every vertex $u$ in $g$ has colorful degree $d^ _ g (u) s$.

**Lemma .** Given a graph $G=(V, E)$, a coloring $ $ of $G$, and positive integers $k$ and $q$, every $k$-defective clique of size at least $q$ is contained within the colorful $(q - k - 1)$-core of $G$ with respect to $ $.

**Definition $k$-defective clique.** Given a graph $G=(V, E)$ and an integer $k$, a vertex subset $S$ of $V$ is a $k$-defective clique in $G$ if $G[S]$ misses at most $k$ edges (i.e., $G[S]$ has at least $ |S| 2 - k$ edges).

**Theorem .** Given a graph $G=(V, E)$ with $n$ vertices and a non-negative integer $k$, the number of search tree nodes generated by Algorithm~ [ref] is $ O (3^ n 3 · n^k)$.

**Claim Diameter-two property~\cite{chen2021computing.** For any $k$-defective clique $S$ in $G$ with $|S| k + 2$, its diameter is at most two (i.e., any two non-adjacent vertices must have a common neighbor in $S$).

**Definition $s$-core~\cite{seidman1983network.** Given a graph $G=(V, E)$ and an integer $s$, the $s$-core of $G$ is the maximal subgraph $g$ of $G$ in which every vertex in $g$ has degree $d_ g (u) s$.

**Theorem .** The worst-case output size of maximal $k$-defective clique enumeration is $Ω( n · n^k)$ when $k$ is a constant.

**Lemma .** The number of search tree nodes generated by Algorithm~ [ref] is bounded by $ O (n · 3^ δ 3 · (δ )^ k )$.

**Lemma .** For any instance $I = (S, C, X)$ of the search tree $ T $, T(S, C, X) & 2 · |C_0| · f^ (|C_1|, …, |C_ |).

**Claim Hereditary.** For any $k$-defective clique $S$ in $G$, every subset $H$ of $S$ is also a $k$-defective clique in $G$.

**Observation .** A $k$-defective clique of size $q$ must contain a clique of size $q - k$.

**Theorem .** Algorithm~ [ref] runs in $ O (n · 3^ δ 3 · (δ )^ k+2 )$ time.

**Theorem .** Algorithm~ [ref] runs in $ O (m · 3^ n 3 · n^k)$ time.

## Proofs
**Proof.** Let $S$ be a $k$-defective clique of size $q$. By definition, $S$ contains at most $k$ missing edges. Consider the set of vertices incident to these missing edges. By removing one endpoint from each missing edge, we remove at most $k$ vertices from $S$, and the remaining vertices form a clique of size at least $q - k$. Therefore, a $k$-defective clique of size $q$ contains a clique of size $q - k$.

**Proof.** Suppose not. Then, there exists a maximal $k$-defective clique $S'$ that includes $S$ but does not contain any vertex in $ _C[p]$. Since $p$ is in $N_C(S)$, all the vertices in $S'$ are adjacent to $p$. Therefore, $S' \ p\ $ is also a $k$-defective clique, and thus $S'$ is not maximal. This contradiction proves the lemma.

**Proof.** We prove the lemma by showing that the search space size of each subproblem is bounded by $ O (3^ δ 3 · (δ )^k)$. This follows from the facts that $|N_ C_i (v_i)| δ$ and $|C_i| δ $~ [cite] for all $1 i n$. Then, applying Lemma~ [ref] , the overall bound follows.

**Proof.** In Algorithm~ [ref] , the running time of Line~ [ref] is bounded by $ O (m)$. Additionally, for each branching vertex $v_i$, the total running time of Lines~ [ref] -- [ref] is bounded by the number of edges in $G[V_i]$. Since we have $|V_i| |N(v_i)| + |N^+(v_i)| · + δ = O (δ )$, the number of edges in $G[V_i]$ is bounded by $ O ((δ )^2)$. Furthermore, the recursive call in Line~ [ref] runs in $ O (3^ δ 3 · (δ )^ k + 2 )$ time because the number of search tree nodes rooted at $(\ v_i\ , C_i, X_i)$ is $ O (3^ δ 3 · (δ )^k)$, and each search tree node takes $ O ((δ )^2)$ time. Therefore, the total running time is $ O (n · 3^ δ 3 · (δ )^ k + 2 )$.

**Proof.** We only consider the case when $q k + 2$, as the lemma holds trivially otherwise. Suppose not, i.e., there is a vertex $u S$ with colorful degree $d^ _S(u) < q - k - 1$. The set $S$ is then partitioned into two disjoint subsets: $S_1 = _S(u)$ and $S_2 = N_S(u)$. We have $| E (S_1)| q - d_S(u) - 1$ because $|S| q$, and $| E (S_2)| d_S(u) - d^ _ S (u)$ since the vertices of the same color must not be adjacent. Therefore, $| E (S)| | E (S_1)| + | E (S_2)| q - 1 - d^ _ S (u) > k$ holds. This contradicts the assumption that $d^ _S(u) < q - k - 1$ holds, thereby proving the lemma.

**Proof.** We prove that the Moon-Moser graph with $n$ vertices, where $n$ is a multiple of 3 and $ n 3 k$, contains at least $3^ n 3 · n/3 k $ maximal $k$-defective cliques; note that $3^ n 3 · n/3 k = Ω( n · n^k)$ when $k$ is a constant. To construct a maximal $k$-defective clique in the Moon-Moser graph, we first select $k$ triangles in the complement graph of the Moon-Moser graph. Then, we choose a missing edge from each of the $k$ selected triangles and one vertex from each of the remaining $ n 3 - k$ triangles; for example in Figure~ [ref] , we first choose two missing edges from the second and third triangles and then choose two vertices from the remaining triangles. The resulting set of vertices forms a maximal $k$-defective clique in the Moon-Moser graph, since the set contains exactly $k$ missing edges, and adding another vertex to the set introduces a missing edge. The total number of such maximal $k$-defective cliques is $3^ n 3 · n/3 k $.

**Proof.** We prove the inequality by induction. For the base case where $I$ is a leaf node, it is trivial that $T(S, C, X) = 1 2 · 3^ |C_0| 3 · f^ (|C_1|, …, |C_ |) = 2$ because $C_0 C_1 … C_ = C = $ (that is, $|C_0| = |C_1| = … = |C_ | = 0$). We now consider an arbitrary non-leaf node $I$. There are two cases as follows. Case I: $C_0 = $. In this case, we can observe that adding a candidate vertex in $C$ to the partial solution always increases the number of missing edges. From the observation, the height of the subtree of $ T $ rooted at $I$ is bounded by $ $. Let the depth of the root node of the subtree be zero. Consider a node $I$ in the subtree at depth $i$ ($0 i < $). Then, the number of allowed missing edges at $I$ is at most $ - i$ from the observation, and branches at $I$ can be made only with vertices in $C_ - i $. Therefore, the number of nodes at depth $i + 1$ is at most $|C_ - i |… |C_ |$, and thus the total number of nodes in the subtree is at most $1 + |C_ | + |C_ - 1 ||C_ | + … + |C_1| … |C_ | = f^ (|C_1|, …, |C_ |)$. Case II: $C_0 ≠ $. When $ = 0$ (i.e., no missing edge is allowed), the problem degenerates to maximal clique enumeration. In this case, our pivot selection strategy coincides with that of Tomita et al.~ [cite] , and therefore we obtain $T(S, C, X) 2 · |C_0| $~ [cite] . Thus, we consider the case $ > 0$. Since $C_0 = N_C(S)$, our pivot selection strategy selects a pivot vertex $p N_C(S)$ that has the fewest non-neighbors in $N_C(S)$, and then sets $B$ to $ _C[p]$ (Line~ [ref] ). For every integer $0 i $, let $B_i$ be the set of vertices in $B$ that introduce exactly $i$ missing edges when added to $S$ (i.e., $B_i = \ u B: | _S(u)| = i\ $); see Figure~ [ref] for illustration. We refer to an instance of a recursive call from $I$ as a child instance of $I$. Consider $I' = (S' = S \ b\ , C', X')$, a child instance of $I$ with the branching vertex $b B = B_0 B_1 … B_ $. Define $ ' = - | _S(b)|$, the number of missing edges that can be added to $S' = S \ b\ $. For $0 j '$, define $C'_j = \ u C': | _ S' (u)| j\ $, the set of vertices in $C'$ that introduce at most $j$ missing edges when added to $S'$. For example, in Figure~ [ref] , consider a child instance of $I$ with the branching vertex $u_2$, which results in $(S', C', X') = (\ u_1, u_2\ , \ u_3, u_4, u_5, u_8\ , )$. In this instance, we have $ ' = 1$, $C'_0 = \ u_5\ $, and $C'_1 = \ u_3, u_4, u_5, u_8\ $. We assume as the induction hypothesis that the following inequality holds for every …

**Proof.** Consider the root node $(S, C, X) = ( , V, )$ of the search tree. Since $S = $, we have $C_0 = C_1 = … = C_k = V$. Therefore, $T( , V, ) 2 · 3^ n 3 · f^k(n, …, n) = 2 · 3^ n 3 · (1 + n + n^ 2 + … + n^ ) = O ( n · n^ k )$.

**Proof.** In Algorithm~ [ref] , Line~ [ref] can be done in $ O (m)$ time. For each branching vertex, the upper bound in Line~ [ref] can be computed in $ O (m)$ time~ [cite] . Additionally, Lines~ [ref] , [ref] , and [ref] also run in $ O (m)$ time. That is, each search tree node takes $ O (m)$ time. Since Algorithm~ [ref] generates at most $ O (3^ n 3 · n^k)$ search tree nodes, its total running time is $ O (m · 3^ n 3 · n^k)$.

## Bound sentences
- myproc Procedure clique reduce reduce KwIn Input KwOut Output Continue continue output output report report update refine upperbound upper-bound A graph $G=(V, E)$ and two integers $k, q$ Every maximal $k$-defective clique in $G$ of size $ q$ $ , V, $ \; $S, C, X$ $C X = $ and $|S| q$ $S$ If $N_C(S) …
- Since we aim to find $k$-defective cliques of size at least $q$, we compute an upper bound on the size of a $k$-defective clique that includes $S$ and is included by $S C$ (Line~ [ref] ).
- Whenever this upper bound is less than $q$, the remaining recursive calls are pruned, as they cannot generate any solution of size at least $q$.
- This upper-bound-based pruning is applied to $(S, C)$ in Line~ [ref] , in order to reduce the search space efficiently.
- However, the recursive calls of $M_2$ are pruned since the upper bound of $(\ u_1, u_2\ , \ u_3, u_4, u_8\ )$ is $|S| + c + r = 2 + 0 + 1 = 3$, which is less than $q=4$.
- After backtracking to $M_1$, $u_2$ is moved from $C$ to $X$, and the upper bound of $(\ u_1\ , \ u_3, u_4, …, u_8\ )$ is computed in Line~ [ref] .
- Since the upper bound is $3$, the remaining recursive calls of $M_1$ are pruned.
- A New Pivoting Technique for Defective Cliques Explain the classical pivoting technique Challenges for the worst-case optimal algorithms for relaxed clique problem A technique called pivoting was originally developed to reduce the search space of the BK algorithm~ [cite] to enumerate all maximal cli …
- Tomita et al.~ [cite] proved that the BK algorithm with the pivot selection strategy that minimizes the number of branches at each node leads to a worst-case optimal algorithm for maximal clique enumeration.
- However, to the best of our knowledge, no other worst-case optimal backtracking algorithms have been developed for enumerating maximal relaxed cliques (Section~ [ref] ).
- This enables our algorithm to explore a worst-case optimal search space when $k$ is a constant.
- These three characteristics together lead to the worst-case optimal search space (Section~ [ref] ).
