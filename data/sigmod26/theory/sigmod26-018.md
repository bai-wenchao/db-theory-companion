# sigmod26-018 — An Efficient Streaming Algorithm for Approximating Graphlet Distributions

from=tex flag=True score=26 stmts=12 proofs=6 chars=118805
kinds: {"definition": 1, "theorem": 4, "fact": 4, "lemma": 3}
counts: {"np_hard": 0, "lower_bound": 6, "upper_bound": 2, "big_o": 77, "omega": 5, "theta": 9, "approx_ratio": 4, "whp": 6, "regret": 0, "dp": 0, "invariant": 0, "convergence": 0, "competitive": 0, "worst_case": 0, "sketch": 0, "cost_model": 0, "cardinality": 0, "learned": 0}

## Statements
**Theorem .** There is a two-phase semi-streaming algorithm with the following guarantees. Given $c>0$, a simple graph $G$, a positive integer $k 3$, an error parameter $α (0,1)$ and a failure probability $δ (0,1)$, the algorithm returns a distribution $ $ such that: ; The first phase (``preprocessing'') uses $O (1/c )$ passes and $ O (n^ 1+c )$ bits of memory , with probability at least $1- 1 n $. ; The second phase (``sampling'') always uses $O ( k^ O(k) α^4 M ln 1 δ )$ passes and $M$ bits of memory. ; The $L_ ∞ $ distance between $ $ and the $k$-graphlet distribution of $G$ is at most $α$, with probability at least $1-δ$.

**Definition (Approximate) DD-ordering.** Given a simple graph $G=(V,E)$, a total order $ $ over $V$ is degree-dominating if, for each $v V$, the maximum degree in $G(v)$ is achieved by $v$ itself; in other words, $d(v|G(v)) d(u|G(v))$ for all $u v$. For $ > 0$, a $ $-DD order $ $ is a total order over $V$ such that, for all $v V$ with $d(v|G(v)) 1$ and all $u v$, it holds that $d(v|G(v)) · d(u|G(v))$.

**Fact Chernoff Bound Variant \Rmnum{2.** Let $D$ be the distribution given by $ X=r_i = p_i$ for $i = 1, …, m$ where $Σ_ i=1 ^m p_i = 1$. A variable following $D$ has expectation $E = Σ_ i=1 ^m p_ir_i$. Let $X_1, …, X_T$ be i.i.d. variables following $D$. For any $0 < ε < 1$, we have the following inequality: | Σ_ i=1 ^T X_i T - E | ε E 2exp ( - ε^2TE 3max_ i=1 ^m r_i ).

**Theorem .** For any $ 1 1+ε $-DD order, $k$ and $0 < α, δ < 1$, let $T$ be a positive integer satisfying T 12 ((k-1)!)^2 (k-1)^ 2k-2 (1+ε)^ 2k-2 α^2 (1 - α)^2 ln 4m_k δ , then alg:gd computes $ μ_1, …, μ_ m_k $ such that with probability at least $1-δ$, $| μ_i - μ_i| α$ for each $1 i m_k$.

**Lemma .** For any $ 1 1+ε $-DD order, $k$ and $0 < ε_0, δ < 1$, let $T$ be a positive integer satisfying T 3 (k-1)! (k-1)^ k-1 (1+ε)^ k-1 ε_0^2 ln 2 δ , then alg:gd computes $ C$ such that $ | C T - L | < ε_0 L$ with probability at least $1-δ$.

**Fact Chernoff Bound Variant \Rmnum{1.** Let $X ~ B(m, p)$ and $0 < ε < 1$. Then, we have the following: ; If $m ≥ N$, $ X ≤ (1 - ε) N p ≤ exp (- 1 3 ε^2 N p )$. ; If $m ≤ N$, $ X ≥ (1 + ε) N p ≤ exp (- 1 3 ε^2 N p )$.

**Theorem .** With probability at least $1 - δ$, Algorithm~ [ref] returns a $ 1 1+ε $-DD order using $O ( 1 c )$ passes and $O ( n^ 1+c ε^3 log n ε δ )$ words of memory.

**Theorem .** With probability at least $1 - δ$, Algorithm [ref] returns a $ 1 1+ε $-DD order using $O ( log n ε )$ passes and $O ( n ε^2 log n ε δ )$ words of memory.

**Fact .** ( [cite] ) For every $v V$ such that $N_v > 0$, we have N_v d(v|G(v))^ k-1 (k-1)^ k-1 .

**Fact .** ( [cite] ) For each $k$-graphlet $S$, it holds that 1 p(S) (k-1)! (1+ε)^ k-1 Z.

**Lemma Stochastic Dominance.** Let $n m$, let $X ~ B(n,p)$ and $Y ~ B(m,p)$. For any $c$, $ X > c Y > c $.

**Lemma .** It holds that L Z (k-1)^ k-1 .

## Proofs
**Proof.** Proof structure. The proof separates the probabilistic part from the deterministic part. In the probabilistic part, we first define a small set of bad events that, if they occur, could violate either (i) the memory bound, (ii) the pass bound, or (iii) correctness of the returned order. We then bound the probability of each bad event using Chernoff bounds ( fact:Chernoff ) and take a union bound over all vertices and all iterations. In the deterministic part, we condition on the event that none of these bad events occur in the first $T$ iterations, and prove deterministically that the algorithm (a) makes geometric progress in $ $ and thus terminates in $T$ iterations, (b) outputs a $ 1 1+ε $-DD order, and (c) never exceeds the claimed space. Bad Events in One Iteration. Suppose the maximum degree of $G[U]$ at the beginning of is at most~$ $. To simplify the notation, for a vertex $v U$ at the moment it is considered in , we use $d_v$ to denote its true degree in the remaining graph $G[H]$ and $ d _v$ to denote the unbiased estimation of $d_v$, which has the distribution $ 1 p × B(d_v, p)$. The algorithm makes decisions using only the sampled graph $ G$ and the estimates $ d_v$. Thus, there are two distinct failure modes to control: (1) $ G$ can be too large (breaking the memory budget), and (2) $ d_v$ can deviate enough to change a peel/keep decision (breaking progress or correctness). Accordingly, we consider the following bad events. ; [Type 1 :] The sampled directed graph $ G$ has more than $(1 + ε )· n p = Θ ( n ε^2 log n ε δ )$ edges. By Fact~ [ref] , this happens with probability at most $exp (- 1 3 ε ^2 n p ) ≤ δ 2 T $. Intuition. The number of sampled directed edges is a sum of independent Bernoulli variables. At the start of the iteration, the total number of directed edges in $G[U]$ is at most $n $, and each is kept with probability $p$, so the expectation is at most $n p$. A multiplicative Chernoff bound then shows that exceeding $(1+ ε)n p$ has exponentially small probability, which is why choosing $p$ so that $n p=Θ\! ( n ε^2 log n εδ )$ suffices for a union bound over $T$ iterations. ; [Type 2 :] For a vertex~$v$, if $d_v ≥ K $, the bad event is $ d _v < (1 - ε ) · d_v$; if $d_v < K $, the bad event is $ d _v ≥ (1 + ε ) · K $. By Fact~ [ref] , the bad event for each vertex is at most $exp (- 1 3 ε ^2 p K ) ≤ δ 2nT $. Intuition. We split Type~ 2 into two cases because the harmful direction of estimation error depends on the true degree. If $d_v …

**Proof.** Observe that the difference between Algorithms~ [ref] and~ [ref] is that in Algorithm~ [ref] , during one pass of the edge lists, random directed graphs from several iterations are sampled and saved. Correctness and the Number of Passes. The key idea is a coupling argument via the directed edge sampling in each iteration: even though alg:approxdd-fewer-2 samples multiple graphs in one pass, it can be seen as using exactly the same randomness that alg:approxdd-fewer-1 would have used over the corresponding iterations. Therefore, the two algorithms produce exactly the same order~$ $, and the correctness follows from theorem:approxdd-fewer-1 . As before, given that there is no bad event in the first $T$ iterations, the algorithm must terminate and uses $O( T q ) = O( 1 c )$ passes. Memory. Recall that in each pass, we sample and save $q$ directed subgraphs. Assuming that no bad event has occurred in previous passes, the maximum degree of $G[U]$ is at most $ $ at the beginning of the current pass. For the $i$-th graph, the expected number of sampled directed edges is on the order of $n · p_i$. Hence, in this pass, the expected number of sampled edges in all the $q$ subgraphs is at most: Σ_ i=0 ^ q-1 n p_i O ( n^ 1+c ε^3 log n ε δ ). To get a high probability statement for memory usage, observe that we are no worse than the situation in theorem:approxdd-fewer-1 . First, the expected number of saved edges is larger in each pass, which means Chernoff Bound will give a smaller failure probability for each pass. Second, the number of passes are fewer, which means there are fewer terms in the union bound. In conclusion, with probability at least~$1-δ$, all the required properties as stated are satisfied.

**Proof.** [Proof of lemma:bound-on-L ] By lemma:b23 , we have L= Σ_ v V, \ N_v>0 N_v Σ_ v V, \ N_v>0 d(v|G(v))^ k-1 (k-1)^ k-1 = Z (k-1)^ k-1 .

**Proof.** Let $X_1, …, X_T$ be the value of $ 1 p(S) $ of each iteration. Then we have $ C = Σ_ i=1 ^T X_i$. In fact, $X_1, …, X_T$ are independent random variables such that $X_i$ takes value $ 1 p(S) $ with probability $p(S)$ for each $k$-graphlet $S$ in $G$. Each $X_i$ is at most $(k-1)!(1+ε)^ k-1 Z$ by lemma:bound-on-pS and has expectation $L$. By fact:Chernoff2,lemma:bound-on-L we have | C T - L | ε_0 L & 2exp ( - ε_0^2 L T 3 (k-1)! (1+ε)^ k-1 Z ) \\ & 2exp ( - ε_0^2 T 3 (k-1)! (k-1)^ k-1 (1+ε)^ k-1 ) \\ &= δ.

**Proof.** We want to bound the deviation of each normalized counter $ μ_i = c_i/ C$ from the true proportion $μ_i = l_i/L$, uniformly over all isomorphism classes $i [m_k]$. The main technical issue is that $ μ_i$ is a ratio with random denominator $ C$. To handle this cleanly, we first condition on the event that $ C$ concentrates around its mean (so the denominator is well-controlled), and then apply a concentration bound to each numerator $c_i$. Finally we union bound over the $m_k$ classes and combine with the probability that the conditioning event holds. Assume that the condition $| C T - L| < α 2 L$ holds. Then $ C$ lies in the interval $[(1-α/2)LT,\ (1+α/2)LT]$. For each $1 i m_k$, recall that $μ_i = l_i L $ where $l_i$ is the number of $k$-graphlets in $G$ which are isomorphic to $ _i$. We have | μ_i - μ_i| α = | c_i C - l_i L | α c_i (1- α 2 )LT - l_i L α + l_i L - c_i (1+ α 2 )LT α . We analyze $ c_i (1- α 2 )LT - l_i L α $, and the other side is similar. We rewrite the deviation in terms of $ c_i T $: c_i (1- α 2 )LT - l_i L α &= c_i T - (1- α 2 )l_i α (1- α 2 ) L \\ &= c_i T - l_i α (1- α 2 ) L - α 2 l_i \\ & c_i T - l_i α 2 (1 - α) L , where the last inequality is due to $l_i L$. Similarly to the proof of lemma:bound-on-hatC , let $X_1, …, X_T$ be the increment of $c_i$ after each iteration, i.e., $c_i = Σ_ j=1 ^T X_j$. In fact, $X_1, …, X_T$ are independent random variables such that $X_j$ takes value $ 1 p(S) $ with probability $p(S)$ for each $k$-graphlet $S$ in $G$ that is isomorphic to $ _i$, and $0$ with the rest probability. Each $X_i$ is at most $(k-1)!(1+ε)^ k-1 Z$ by lemma:bound-on-pS and has expectation $l_i$. By fact:Hoeffding,lemma:bound-on-L , c_i T - l_i α 2 (1 - α) L & 2exp ( - Tα^2 (1 - α)^2 L^2 2 ((k-1)! (1+ε)^ k-1 Z)^2 ) \\ & 2exp ( - Tα^2 (1 - α)^2 2 ((k-1)!)^2 (1+ε)^ 2k-2 (k-1)^ 2k-2 ) δ 4m_k . One can argue similarly that l_i L - c_i (1+ α 2 )LT α δ 4m_k . Recall that the computation above is conditioned on $| C T - L| < α 2 L$. We denote this condition by $ C$. Applying a union bound to both sides of eq:mui for each $1 i m_k$, we obtain that _ i [m_k] | μ_i - μ_i| > α \ | \ C δ 2 . Also note that T 12 (k-1)! (k-1)^ k-1 (1+ε)^ k-1 α^2 ln 4 δ , which gives $ C δ 2 $ by lemma:bound-on-hatC . Therefore, we have the upper bound for overall failure probability: _ i [m_k] \!\!\!| μ_i - μ_i| > α \ \ C + _ i [m_k] \!\!\!| μ_i - μ_i| > α \ | \ C \ \ δ.

**Proof.** Let $x_1, …, x_m$ be Bernoulli variables with positive probability $p$. Let $A = Σ_ i=1 ^n x_i$, $B = Σ_ i=n+1 ^m x_i$, and $C = A + B$. Thus $A$ and $X$ have the same distribution, and $C$ and $Y$ have the same distribution. Now observe that, for any $c$, X > c = A > c C > c = Y > c .

## Bound sentences
- As a consequence of their lower bound, our algorithm is optimal up to a factor of $ O (n^c)$ in the memory usage.
- This result is nearly optimal, as a lower bound rules out $O(1)$-pass algorithms with sublinear memory.
- We also provide a space lower bound showing that the tradeoff between memory and number of passes of our algorithms is near-optimal.
- The Chernoff bound is applied to the binomial $B(d_v,p)$, and we upper bound the failure probability uniformly by lower bounding the relevant mean by $( /K)p$, enabling a union bound over all vertices and all iterations. fig:bad-events helps to illustrate the area of type 2 bad events. [display] The …
- Therefore, we have the upper bound for overall failure probability: [display] Now consider the number of passes of alg:gd .
- We remark that, by a lower bound of~ [cite] , no algorithm using $O(1)$ passes and $o(n)$ memory can even decide if the input graph contains some $k$-graphlet, for a fixed $k 2$.
