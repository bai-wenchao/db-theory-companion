# sigmod26-385 — WoW: A Window-to-Window Incremental Index for Range-Filtering Approximate Nearest Neighbor Search

from=tex flag=True score=14 stmts=8 proofs=2 chars=104429
kinds: {"definition": 6, "theorem": 2}
counts: {"np_hard": 0, "lower_bound": 6, "upper_bound": 2, "big_o": 19, "omega": 0, "theta": 0, "approx_ratio": 3, "whp": 0, "regret": 0, "dp": 0, "invariant": 0, "convergence": 0, "competitive": 0, "worst_case": 4, "sketch": 0, "cost_model": 0, "cardinality": 6, "learned": 0}

## Statements
**Theorem .** In the landing layer $l_d$, the expected fraction $f_R$ of in-range neighbors at a single hop on the path is bounded by f_R= ( 1 √ o , 1 2 ) & (a) l (l'-1,l'- 1 2 ), o>4, \\ [ √ 2 2 - 1 4o^ l+1 , 3 4 - 1 4o^ l+1 ) & (b) l (l'-1,l'- 1 2 ), o ≤ 4, \\ [ 3 4 - 1 4o^ l , 1- o^l + 1 4o^ l + 1 2 ] & (c) l [l'- 1 2 ,l'], where $n'$ is the number of in-range vectors for filter $R=[x,y]$, $l'=log_o n' 2 $, and $l = l_h = _o n' 2 $, which is the highest layer defined in Line~2 of Algorithm~ [ref] , whose window size is less than $R$.

**Definition Window Graph.** A window graph is a directed graph $G=\ P , E ,w\ $, where $ P $ is the vertex set, $ E $ is the edge set, and $w$ is the half window size. Each vertex $v_i$ denotes a vector-attribute pair, where $v$ is the vector and $i$ is the attribute value. $ E $ satisfies: (1) RNG property: $ (v_i,v'_j) E , v''_k P \ v_i,v'_j\ $, then $δ(v_i,v'_j) < δ(v_i,v''_k) δ(v_i,v'_j)<δ(v'_j,v''_k)$; (2) Window property: $ (v_i,v'_j) E $, then $|rank(i)-rank(j)| < w$, where $rank(i)$ counts the number of unique values less than $i$.

**Definition Range-Filtering ANNS.** A hybrid dataset $ D = \ V , A \ $ consists of a vector set $ V $ and an attribute set $ A $ with equal size $n$. Given a range-filtering query $(q, R)$, where $q$ is the query vector and $R = [x, y]$ is the range (window) filter over $ A $, with $x$ and $y$ as its left and right boundaries, RFANNS aims to find a subset $ S D $ with $k$ vector-attribute pairs to minimize $Σ_ (v,a) S δ(v,q), x a y$. We call a pair $(v,a)$ in-range only when $x a y$ (i.e. $a R$).

**Definition Local Intrinsic Dimensionality, LID.** Given a hybrid dataset $ D =\ V , A \ $ and a range-filtering query workload $Q = \ V _Q, R \ $, the LID of $Q$ is estimated by each query $(v,R)$'s top-$k$ in-range nearest neighbors $(u_i,a_i) \ u_i N(v) a_i R \ $: LID@k= E _ (v,R) Q [ - ( 1 k Σ_ i=1 ^ k log ( δ(v, u_i) δ(v,u_k) ) )^ -1 ].

**Definition Query Selectivity.** For a range filter $R=[x,y]$, let $n'$ denote the number of attributes in $ A $ that are in range $R$, the fraction of in-range attributes over the dataset cardinality is $f= n' n $, and the selectivity of $R$ is defined by $s= 1 f $.

**Definition Approximate Nearest Neighbor Search.** A vector dataset $ V $ is defined in vector space with a distance metric $δ$, where each vector has dimension $d$. Given a query vector $q$, ANNS aims to find a subset $ S V $ with $k$ vectors to minimize $Σ_ v S δ(v,q)$.

**Definition Hierarchical Window Graphs.** Hierarchical window graphs are a set of window graphs $ H = _ l [0, top ] G_l$. Define a window boosting base $o≥ 2$, then $G_l.w=o^l$ and $ top = _o | A |_u 2 $, where $| A |_u$ is the total number of unique values.

**Theorem .** Without pruning dominated vertices in Line 11 and Line 17, for vertex $v_a$, we have $Σ_ s N_ v_a ^ l+1 δ(s,v_a) Σ_ t N_ v_a ^l δ(t, v_a)$.

## Proofs
**Proof.** Without RNGPrune, $N_ v_a ^l$ and $N_ v_a ^ l+1 $ contain the nearest vertices to $v_a$ covered by $W_a^l$ and $W_a^ l+1 $, respectively. As window $W_a^l$ is a subset of window $W_a^ l+1 $, there exists a subset $W' W_a^ l+1 W_a^l$ s.t. $ j W', δ(v_j,v_a) < min_ i W_a^l δ(v_i,v_a)$. In addition, $ SearchCandidates $ traverses on the graph in a greedy manner, thus for layer $l+1$ it first expands the candidate list from $W'$ and later from $W_a^l$ before the list is full. The inequality holds. $Σ_ u N_ v_a ^l δ(v_a,u) = Σ_ u N_ v_a ^ l+1 δ(v_a, u)$, if and only if $W' = $. The conclusion also holds with a high probability when RNGPrune is not omitted, as the pruning algorithm runs in a greedy manner and the nearest neighbors are often reserved [cite] .

**Proof.** The landing layer $l_d$ is determined by comparing $ 2o^l n' $ and $ n' 2o^ l+1 $. Range filter $[x,y]$ can be rewritten to $[x, x+n'-1]$ by assuming all attribute values are sequential. Moreover, vectors have an equal probability to be selected as neighbors in a certain range. There are two situations: $ $ Situation 1. $2o^ l + 1 2 < n' < 2o^ l + 1 $, then $ 2o^l n' < n' 2o^ l+1 $ and $l_d = l + 1$, the half window size is $o^ l+1 $. For a vector with attribute value $x + i$ in range $[x, x + n' - 1]$ where $i [0, n'-1]$, the window of a vector with attribute value $x + i$ is $W_ x + i ^ l+1 = [x + i-o^ l+1 + 1, x + i + o ^ l+1 - 1]$. Case (a). When $n' < o^ l+1 $, the range filter is always covered by the windows. In this case, $2o^ l+ 1 2 < o^ l+1 $ and $o > 4$. The fraction can be calculated by $f_R= n' 2o^ l+1 ( 1 √ o , 1 2 )$. Case (b). When $n' ≥ o^ l+1 $, some windows fail to cover the whole range. If the window left boundary is less than the filter left boundary, i.e. $x+i-o^ l+1 < x i o^ l+1 -1$ The fraction of in-range vectors is $f_1= (x + i + o^ l+1 - 1 )- x + 1 2o^ l+1 $, and the expectation is f_1 = E _ i [0,o^ l+1 -1] [ i + o^ l+1 2o^ l+1 ], which is $ f_1 = 1 4 (3-o^ l+1 )$. On the other hand, if $i [o^ l+1 , n'-1]$, the window right boundary is greater than the filter right boundary. The fraction is $f_2= (x+n'-1) - (x+i-o^ l+1 +1) + 1 2o^ l+1 $, and f_2 = E _ i [o^ l+1 ,n'-1] [ n'+o^ l+1 -1-i 2o^ l+1 ], which is $ f_2 = n'+o^ l+1 -1 4o^ l+1 $. Combining two parts, $f_R=w_1 f_1 +w_2 f_2 $, where $w_1= o^ l+1 n' $ and $w_2= n'-o^ l+1 n' $, the total fraction is f_R&= 3o^ l+1 -1 4o^ l+1 · o^ l+1 n' + n'+o^ l+1 -1 4o^ l+1 · n'-o^ l+1 n' \\ &= o^ l+1 2n' + n'-1 4o^ l+1 [ 1 4 (2√ 2 -o^ -(l+1) ), 1 4 (3-o^ -(l+1) ) ), and the lower bound is at $ n'=√ 2 o^ l+1 [2o^ l+ 1 2 ,2o^ l+1 )$, while the upper bound is determined by $n'=2o^ l+1 $. $ $ Situation 2. Case (c). $ 2o^l n' 2o^ l+ 1 2 $, then $ 2o^l n' n' 2o^ l+1 $ and $l_d = l$, the half window size is $o^l$. For a vector with attribute value $x + i$ in range $[x, x + n' - 1]$ where $i [0, n'-1]$, its window in Layer $l$ is $W_ x+i ^l = [x + i-o^l + 1, x + i + o ^l - 1]$. When $i o^l - 1$, we have $x +i-o^l + 1 < x$ that the left boundary of the window is less than that of the filter. The fraction of in-range vectors over the window size is $f_1= (x + i + o^l - 1)-x+1 2o^l $. The expected fraction of this situation is f_1 = E _ i [0,o^l-1] [ i + o^l 2o^l ], which is $ f_1 = 1 4 (3-o^ -l )$. Whe …

## Bound sentences
- In this setting, the lower bound is 74.97\% and the upper is 82.30\%, leading to 12--13 in-range neighbors at a single hop when $m=16$.
- If $o$ is a large value, the construction would be fast, but the lower-bound in Case (a) would be small.
- To achieve the optimal fraction lower bound in all cases, while maintaining good indexing performance, setting $o=4$ is recommended, as verified in Section~ [ref] .
- For landing layer selection, the cardinality of the filtering subset by the range filter $[x,y]$ can be calculated by the rank $i,j$ of the upper bound of $x$ and the lower bound of $y$ from WBT, which requires $O(log n)$ time separately.
- The number of distance computations performed on these oracle HNSWs represents the lower bound achievable by the most accurate RFANNS indices.
- Combining two parts, $f_R=w_1 f_1 +w_2 f_2 $, where $w_1= o^ l+1 n' $ and $w_2= n'-o^ l+1 n' $, the total fraction is [display] and the lower bound is at $ n'=√ 2 o^ l+1 [2o^ l+ 1 2 ,2o^ l+1 )$, while the upper bound is determined by $n'=2o^ l+1 $. $ $ Situation 2.
