# sigmod26-259 — A Bouquet of Results on Maximum Range Sum: General Techniques and Hardness Reductions

from=tex flag=True score=46 stmts=14 proofs=15 chars=82280
kinds: {"definition": 1, "claim": 1, "theorem": 6, "lemma": 6}
counts: {"np_hard": 0, "lower_bound": 14, "upper_bound": 3, "big_o": 66, "omega": 20, "theta": 19, "approx_ratio": 33, "whp": 14, "regret": 0, "dp": 0, "invariant": 0, "convergence": 0, "competitive": 0, "worst_case": 0, "sketch": 0, "cost_model": 0, "cardinality": 0, "learned": 0}

## Statements
**Lemma .** Let $C$ be a $d$-ball, and let $ ' $ be a collection of $k$ weighted $d$-balls, such that the $(d -1 )$-dimensional measure of $ C B$ relative to the $(d -1 )$-dimensional measure of $C$ is $ 1 2 -Θ( )$, for all $B '$ In $ ^2$, $C$ will be a disk and any disk in $ '$ will cover at least $ ( 1 2 -Θ( ) )$ fraction of $C$'s circumference. We also remark that the lemma holds in a more general setting. . Let $S=\ p_1,p_2,…,p_t\ $ be a collection of points such that each point in $S$ is independently and uniformly at random picked from $ C $, where $t=c ^ -2 log n$ (for a sufficiently large constant $c$). Then with high probability (i.e., $≥ 1-n^ -Ω(1) $) there exists a point in $S$ whose weighted depth is at least $ ( 1 2 -Θ( ) )W$, where $W=Σ_ B ' w_B$.

**Theorem .** For dynamic MaxRS with $Q$ being a $d$-ball, there is a randomized $( 1 2 - )$-approximation algorithm with amortized update time $O( ^ -2d-2 log n)$, for $0 < < 1/2$. The approximation factor holds with high probability, i.e., $1-n^ -Ω(1) $. Here $n$ is the number of points of $P$ in a given round.

**Definition Monotone $(\min,+)$-convolution.** Given two sequences \( D = (D_0, D_1, , D_ n-1 ) \) and \( E = (E_0, E_1, , E_ n-1 ) \), such that \( D_0>D_1>…> D_ n-1 \) and \( E_0> E_1> …> E_ n-1 \), the monotone $(min,+)$-convolution produces a new sequence \( F \), where for each $k \ 0,…,n-1\ $, \[ F_k = min_ i + j = k (D_i + E_j). \]

**Lemma .** Let $B$ be a unit radius $d$-ball and $C$ be a $d$-ball of radius $ $. Suppose $ B $ passes through a point that lies at distance $ ^2$ from the center of $C$. Then the $(d - 1)$-dimensional measure of $B C $ relative to the $(d - 1)$-dimensional measure of $C$ is at least $ 1 2 -Θ( )$.

**Theorem .** For colored MaxRS with $Q$ being a $2$-ball, there is a randomized $(1- )$-approximation algorithm with expected running time $O( ^ -2 nlog n)$, for $0 < < 1$. The approximation factor holds with high probability.

**Theorem .** For static MaxRS with $Q$ being a $d$-ball, there is a randomized $( 1 2 - )$-approximation algorithm with running time $O( ^ -2d-2 nlog n)$, for $0 < < 1/2$. The approximation factor holds with high probability.

**Lemma .** (Theorem 2 in [cite] ) The trapezoidal map a set of $n$ circular $x$-monotone arcs with $k$ intersection points can be computed in $O(nlog n + k)$ expected time. The map has $O(n+k)$ vertices, edges and faces.

**Theorem .** Assuming that $(min,+)$-convolution on sequences of length $n$ requires $Ω(n^2)$ time, the batched MaxRS problem on $n$ points and $m$ query interval lengths requires $Ω(mn)$ time.

**Theorem .** Assuming that $(min,+)$-convolution on sequences of length $n$ requires $Ω(n^2)$ time, the batched smallest k-enclosing interval problem on $n$ points requires $Ω(n^2)$ time.

**Lemma .** Consider a collection of grids $ = \ G_s ( √ d z ) \, \, z \ 0,1, , s√ d -1\ ^d \ $. For any point $p ^d$, there exists at least one grid in $ $ in which $p$ is $ $-near.

**Claim .** The weight covered by an interval $I_ i,j =[i,(2n-1)-j]$ for some $i,j \ 0,…,n-1\ $, is given by $w(I_ i,j )=A_i+B_j$.

**Theorem .** There is a randomized algorithm for exact colored disk MaxRS with $O(nlog n + n. )$ expected running time.

**Lemma .** With high probability, the approximation factor of the algorithm is $ 1 2 - $.

**Lemma .** The amortized update time of the algorithm is $O( ^ -2d-2 log| |)$.

## Proofs
**Proof.** We will use an indirect argument to establish the lemma. Consider any ball $B '$. We will first establish that with high probability at least $ ( 1 2 -Θ( ) )t$ points of $S$ will lie inside $B$. For $1≤ i ≤ t$, let $X_i$ be the indicator random variable which is equal to $1$ if $p_i B$. Let $Y_B=Σ_ i=1 ^t X_i$ be the number of points in $S$ contained in $B$. Then, $ E [Y_B] ≥ ( 1 2 -Θ( ) )t$. Since $X_1,X_2,…, X_t$ are independent random variables, via standard application of Chernoff bound, with high probability (w.h.p.), we have $Y_B ≥ (1- ) ( 1 2 -Θ( ) )t ≥ ( 1 2 -Θ( ) )t$. Next, by union bound on $k≤ n$ balls, $Y_B ≥ ( 1 2 -Θ( ) )t$ holds w.h.p. for all $B '$. Now we will connect the random variables $Y_B$ with the weighted depth of points in $S$. Let $ (p)$ be the weighted depth of any point $p S$ w.r.t. $ '$. Then, observe that $Σ_ p S (p)=Σ_ B ' w_BY_B$, which by the previous argument is at least $W ( 1 2 -Θ( ) )t$ w.h.p. This finishes the proof.

**Proof.** Via suitable rotations of the $d$-balls, we can assume that the center of $C$ is at the origin and the center of $B$ is $(0,0,…,0,1+ ^2)$. See Figure~ [ref] (a). For any point $x = (x_1, ,x_d) B C $, $ x = ^2$ and $ x-(0, ,0,1+ ^2) = 1$. Expanding these two quantities, we get the following equations: Σ_ i=1 ^ d x_i^2 = ^2 and Σ_ i=1 ^ d-1 x_i^2 + (x_d - 1 - ^2)^2 = 1, which solves to $x_d = 3 ^2 + ^4 2+2 ^2 $. We denote the expression on the right by $b$. It can be verified that $ ^2 ≤ b ≤ 2 ^2$ for all $ (0,1)$. In $d=2$, suppose $B C $ subtends an angle $2θ$ at the center of $C$ (ref Figure~ [ref] (a)). Since $ θ = b/r$, the arc length of $B C $ relative to the circumference of $C$ is 2θ r 2 r = 1 ( 3 + ^3 2+2 ^2 ) ≥ 1 (2 ) ≥ 1 2 - 2 , for all $0 < < 1/2$. For $d≥ 3$, we use a known result of computing the surface area of a spherical cap as follows. Let $h$ denote the height of the spherical cap $B C $ determined by the hyperplane $x_d = b$. Define $q = 1 - h/r = b/r = Θ( )$, since $b = Θ( ^2)$. By an estimate of [cite] , the surface area of $B C $ relative to the surface area of $C$ is given by 1 2 - G_ d-2 (q) 2\,G_ d-2 (1) , where $G_d(x) = _ 0 ^ x (1-t^2)^ (d-1)/2 \,dt$ for all $x [0,1]$. The quantity $G_ d-2 (q)$ can be trivially upper bounded by $q=Θ( )$, and the quantity $G_ d-2 (1)$ can be lower bounded by $ _ 0 ^ 1 (1-t^2)^ (d-3)/2 \,dt ≥ _ 0 ^ 1/2 (3/4)^ (d-3)/2 \,dt =Ω(1)$. This finishes the proof.

**Proof.** Consider the grid in $ G $ in which the optimal point $p^*$ is $ ^2$-near (guaranteed by Lemma~ [ref] ). Let $X^*$ be the cell in the grid containing $p^*$. W.l.o.g., assume that the center of $X^*$ is at the origin $o$. Consider a $d$-ball $A$ of radius $ ^2$ centered at the origin. Then $p^*$ will lie inside $A$ (see Figure~ [ref] (b)). The circumsphere $C := C(X^*)$ of the grid cell has radius $ $. Let $B' $ be a ball containing $p^*$ with center $q$. Translate $B'$ in the direction $ oq $ to the ball $B$ until it is tangential to $A$. In this case, the spherical cap $B C B' C $, and therefore, the $(d - 1)$-dimensional measure of the cap can only decrease. This is the setting considered in Lemma~ [ref] , which implies that the minimum measure is at least $ 1 2 -Θ( )$ relative to the measure of $C$. We then invoke Lemma~ [ref] on the ball $C$ and the set of balls $ '$ which cover $p^*$, which shows the existence of a randomly sampled point in $ C $ with weighted depth at least $ opt ( 1 2 - Θ( ))$ with high probability. Scaling $ $ appropriately gives the desired approximation factor.

**Proof.** Consider the $j$-th epoch. First consider the cost of an insertion or deletion of a ball. In any fixed grid $G G $, a unit ball intersects at most \( (2/s )^d = ( 2√ d )^d = O( ^ -d ) \) cells and the collection $ G $ contains \( ( s√ d )^d = O( ^ -d ) \) grids. Therefore, the number of sample points in $S$ updated is \( O( ^ -2d ) × O( ^ -2 log | _j|) = O( ^ -2d-2 log | _j|) \). Hence each update costs \( O_ (log | _j|). \) We now consider the sampling step that will happen in the $(j +1 )$-th epoch. The number of points sampled will be $ (| _ j+1 |log| _ j+1 |)$. Inserting the balls in $ _ j+1 $ one-by-one, and updating the weighted depth of $ (log | _j|)$ sampled points per ball, takes $ (| _ j+1 |log| _ j+1 |)$ time. We will charge the cost of this sampling step to the updates which happened in the $j$-th epoch. Since $| _j|=Θ(| _ j+1 |)$ and the number of updates in the $j$-th epoch are at least $| _j|/2$, each update in the $j$-th epoch is charged only $ (log | _j|)$.

**Proof.** Every unit $d$-ball, intersects $O( ^ -2d )$ cells across all grids in $ G $ (as shown earlier in proof of Lemma~ [ref] ), and for each cell we maintain a sample of $O( ^ -2 log n)$ points. Hence corresponding to each ball, there are $O( ^ -2d-2 log n)$ points that need to be considered. Sorting the set of balls takes $O(nlog n)$ time. Since corresponding to each ball, we update the colored depth of $O_ (log n)$ points, and each update takes $O(1)$ time, we get an overall running time of $O( ^ -2d-2 log n)$.

**Proof.** For any color $c [m]$, the time taken to construct $U_c$ is $O(| _c|log | _c|)$ via power diagrams~ [cite] . Hence, the time taken to construct $U_1,…, U_m$ will be $O(nlog n)$. The expected time to construct the trapezoidal map is $O(nlog n + k)$. The traversal of the map can be performed in $O(n+k)$ time by using the standard doubly-connected edge list (DCEL) data structure~ [cite] . The check of $C D$ or not can be performed in constant time by testing if an arbitrary point in $C$ lies inside $D$. Overall, the expected time is $O(nlog n +k)$.

**Proof.** Consider a disk $D$ and a cell $C^*$ as shown in Figure~ [ref] . Since $|bd| = |cd|$, we have $|bd| ≤ 1/2$. Now $|ad|=√ |ab|^2-|bd|^2 ≥ √ 1-(1/2)^2 ≥ √ 3 2 $. Finally, $|ad| + |de| = 1$ implies $|de|≤ 1- √ 3 2 < 0.15$. [h] [scale=0.8] disk-missing-corners.pdf A disk $D$ not containing any corner of cell $C^*$, cannot contain the optimal point $p^*$. This establishes that $D$ cannot contain $p^*$, and completes the proof of (1). Proof of (2) follows from the fact that the depth of each corner of $C$ is at most $ $.

**Proof.** Consider any point $p I( _R, _B)$. Let $D_R _R$ and $D_B _B$ be the disks corresponding to the arcs in $ U_R $ and $ U_B $, respectively, intersecting at $p$. Since $p U_R U_B $, it implies that $p$ is at a distance of at least one from the center of any disk in $ _R _B$. Therefore, by definition of a boundary point, we claim that $p ( _R _B) $. For the sake of contradiction assume that $p$ is not a vertex on $ ( _R _B) $. By general position assumption, only $D_R$ and $D_B$ contain $p$ and hence, either the arc of disk $D_R$ or $D_B$ on $ ( _R _B) $ contains $p$. W.l.o.g. assume it is disk $D_R$. Then $ ( _R _B) $ will have a point infinitesimally close to $p$ on either side which lie on $D_R$ (as shown in Figure~ [ref] ). Label them $p_a$ and $p_b$. [h] [scale=0.8] boundary-vertex.pdf Proof of Lemma~ [ref] Now either $p_a$ or $p_b$ will lie at a distance of less than one from the center of $D_B$ (in Figure~ [ref] it is $p_b$). Therefore, one among $p_a$ and $p_b$ cannot be on $ ( _R _B) $. This is a contradiction. Therefore, $p$ is a vertex on $ ( _R _B) $. Finally, notice that since $ _R _B$ is also a collection of disks, the number of vertices on $ ( _R _B) $ will be bounded by $O(| _R| + | _B|)$~ [cite] .

**Proof.** By Lemma~ [ref] the number of intersection points, $|I( _i, _j)|$, among the boundaries of two regions $ U_i $ and $ U_j $ is $O(| _i|+| _j|)$. Summing this quantity over all the pairs, we get \[Σ_ i=1 ^ t Σ_ j=1 ^ t O(| _i| + | _j|) = Σ_ i=1 ^t O(| _i|· + n) = O(n· ). \]

**Proof.** Consider a cell $C$ and let $n_C$ be the number of disks intersecting $C$. The expected time taken to run the first algorithm (Lemma~ [ref] ) on these disks is $O(n_Clog n_C + k_C)$, where $k_C=O(n_C· )$ (by Lemma~ [ref] ). Note that we can apply Lemma~ [ref] since by Lemma~ [ref] , there are at most $4· $ colored disks remaining in each cell. Since each disk intersects at most constant number of cells in the grid, the running time over all non-empty cells is $O(nlog n + n· )$. Now we argue about the correctness. Consider the grid in $ G $ where $p^*$ is $0.25$-near. Let $C$ be the cell containing $p^*$. By Lemma~ [ref] , throwing away the disks will not change the colored depth of $p^*$. One subtle point is that when the first algorithm (Lemma~ [ref] ) is run on the remaining disks in $C$, then a point not lying inside $C$ might get reported. However, by definition of $p^*$ being the optimal point, the reported point will also have the same colored depth as $p^*$.

**Proof.** If $ ' ≤ c_1 ^ -2 log n$, then the expected running time is $O(n· )=O( ^ -2 nlog n)$. Now consider the case where $ ' > c_1 ^ -2 log n$. For each color $j [m]$, let $ _j$ be the disks of color $j$. Let $X_j$ be an indicator random variable such that $X_j=1$ if color $j$ is sampled, and $X_j=0$ otherwise. So we have $ [X_j] = λ$. The size of $R$ can be written as $|R| = Σ_ j [m] X_j·| _j|$. So, its expected size is \[ [|R|] = Σ_ j [m] | _j|· [X_j] = λ·Σ_ j [m] | _j| = c_1nlog n ^ 2 ' .\] We run the exact algorithm on the set $R$, giving the desired runtime $ [|R|· _R] · [|R|] = O( ^ -2 nlog n)$ in expectation.

**Proof.** Let $M$ be the set of colors which have a disk covering $p^*$, and for $j M$ let $X_j$ be the random variable indicating whether color $j$ was sampled. So, the colored depth of $p^*$ w.r.t. $R$ will be given by $X:= _R(p^*)=Σ_ j M X_j$, and we can compute its expectation as \[μ:= [ _R(p^*)]= [Σ_ j M X_j ]=Σ_ j M [X_j]=λ· . \] The concentration of the depth around the expectation can be given by, \[ [X (1- 2 )μ ] exp (- μ( /2)^2 2 ) exp ( - c_1 8 ' n ) 1 n^k ,\] which holds for $k= c_1 8 ' ≥ c_1 8 Ω(1)$. So with the high probability of ($1-1/n^k$) we have that $ _R(p^*)>(1-( /2))μ c n$. This proves (1). Consider some point $p P'$. Let $M'$ be the set of colors which have a disk covering $p$, and for $j M'$, let $Y_j$ be the indicator random variable representing whether color $j$ was sampled. So, the colored depth of $p$ w.r.t. $R$ will be given by $Y:= _R(p)=Σ_ j M' Y_j$, and we can compute its expectation as \[μ':= [ _R(p)]= [Σ_ j M' Y_j ] =Σ_ j M' [Y_j]<λ(1- ) . \] The concentration of the depth around the expectation can be given by, \[ [Y (1+ 2 )μ' ] exp (- μ'( /2)^2 3 ) exp ( - c_1(1- ) 12 ' n ) 1 n^ k' ,\] which holds for some $k'= c_1(1- ) 12 ' c_1(1- ) 12 = Ω(1)$. So with the high probability of ($1-1/n^ k' $) we have that $ _R(p)<(1+( /2))μ' < (1-( /2))μ c n$. We can now union bound over all $O(n^2)$ points $p P'$ to get that $ [ p P', _R(p)<c n] 1- 1 n^ k'-2 $. But for a large enough constant $c_1$, we will have that $k'>2$, and hence with high probability, for all points $p P'$ their colored depth w.r.t. $R$ is $ _R(p) < c n$. This proves (2).

**Proof.** [Proof Sketch.] Since every point has an associated guard point which negates its weight, any placement of an interval can cover the weight of at most one $A$-point, and at most one $B$-point. Since placing an interval of length $L_s$, starting at the coordinate of some $A$-point $p^A_i$, covers weight $A_i+B_j$ for $j=k_s-i$ (proved in Lemma~ [ref] ), we can discard all other placements of such a segment (since they will either cover negative weight, zero weight, or weight of a single element $A_i$ or $B_j$). Using the batched MaxRS oracle gives us the maximum possible such sum, which is hence the maximum possible sum of $A_i+B_j$ where the indices satisfy $i+j=k_s$.

**Proof.** Since $I_ i,j $ starts at x-coordinate $i$, it includes point $p_ i ^A=(i, A_ i )$, but not the corresponding guard point $p'^ A _ i =(i-0.5, -A_ i )$. Similarly, since $I_ i,j $ ends at $(2n-1)-j$, it includes the point $p_ j ^B = ((2n-1)-j, B_ j )$, but not its corresponding guard point $p'^ B _ j = ((2n-1)-j+0.5, -B_ j )$. Further for all $i<i' n-1$ and $0 j'<j$, the interval $I_ i,j $ covers the points $p_ i' ^A$, $p'^A_ i' $, $p_ j' ^B$, $p'^B_ j' $. By construction, if a point and its corresponding guard point are picked, then their weights sum to zero. Hence we get that $w(I_ i,j )=A_ i +B_ j $.

**Proof.** [Proof of Lemma~ [ref] ] Since $A_i, B_j 0$ for all $i,j \ 0, , n-1\ $, it follows that $C_ k_s ^* 0$. Let $(i_0, j_0)$ be a pair of indices such that $i_0, j_0 \ 0, , n-1\ $, $i_0+j_0=k_s$, and $A_ i_0 +B_ j_0 =C_ k_s ^*$. Consider an interval $I_ i_0,j_0 =[i_0, (2n-1)-j_0]$. The length of this interval is $x_ end -x_ start =(2n-1)-(i_0+j_0)=(2n-1)-k_s$, which is precisely $L_s$, the query interval length for the target index $k_s$. Further from Claim~ [ref] we know that $w(I_ i_0,j_0 )=A_ i_0 +B_ j_0 =C_ k_s ^*$ Since the batched MaxRS oracle returns $W_s^*$, the maximum possible sum for an interval of length $L_s$, we have $W_s^* C_ k_s ^*$. To show that $W_s^* = C_ k_s ^*$, we argue that there exists an optimal interval $I^* = [x^*, x^*+L_s]$ which is of the form $[i, (2n-1)-j]$ for some $i,j \ 0,…,n-1\ $ such that $i+j=k_s$. ; If $x^*+L_s<n-0.5$ (i.e., $I^*$ covers no $B$-points ): then the interval covers all points starting from $p'^A_0$ up to some point $p^A_a$ or $p'^A_a$ for some $a \ 0, ,n-1\ $ (this is because $L_s n$). This interval hence either covers zero weight (in the former case, when all points covered are paired with their corresponding guard points), or negative weight (in the latter case, where only $p'^A_a$ is picked but not $p^A_a$). ; If $x^* n-0.5$ (i.e., $I^*$ covers no $A$-points ): then the interval covers all points starting from some point $p^B_b$ or $p'^B_b$ for some $b \ 0, ,n-1\ $ up to the point $p'^B_0$. This interval hence either covers zero weight (in the former case, when all covered are paired with their corresponding guard points), or negative weight (in the latter case, where only $p'^B_b$ is picked but not $p^B_b$). ; We now know that the left endpoint lies to the left of $n-0.5$, and the right end point lies to the right of $n-0.5$. If the left endpoint satisfied $x^*<-0.5$ (i.e., $I^*$ covers all $A$-points ), then all points $p^A_a$ for $a \ 0, ,n-1\ $ will be paired with their guard points, and hence contribute a total weight of zero. Further, at most one point $p^B_b$ for $b \ 0, ,n-1\ $ can be left unpaired, and hence the weight covered by the interval is either zero or $B_b$ for some $b$. Similarly, if the right endpoint satisfied $x^*+L_s>2n-0.5$ (i.e., $I^*$ covers all $B$-points ), then the cost covered by the interval can be either zero or $A_a$ for some $a$. ; We are now left with the case where $I^*$ covers some $A$-points, and some $B$-points . Consider an interval starting at an integral coordinate  …

## Bound sentences
- We establish a conditional lower bound of $Ω(mn)$ time for this problem, assuming the hardness of $(min,+)$-convolution problem.
- Interestingly, this implies that the trivial upper bound of $O(mnlog n)$ for batched MaxRS in $ ^2$ is almost-tight.
- A similar lower bound is established for a related problem of batched smallest $k$-enclosing interval . ; In the colored MaxRS problem in $ ^d$, each point in $P$ is assigned a color from $\ 1,2,…,m\ $ and the goal is to find the placement of a $d$-ball $Q$ that maximizes the number of uniquely colo …
- Specifically, we establish an $Ω(mn)$ time conditional lower bound for batched MaxRS in $ ^1$ assuming hardness of the popular $(min,+)$-convolution problem.
- We establish a conditional lower bound of $Ω(n^2)$ for this problem as well.
- Recall that even (uncolored) MaxRS for $Q$ being a $2$-ball has a conditional lower bound of $Ω(n^2)$~ [cite] (and in fact we conjecture a conditional lower bound of $Ω(n^d)$ for colored MaxRS for $Q$ being a $d$-ball).
- Lower bound for batched MaxRS Consider the batched maximum range sum problem (batched MaxRS), where we are given a set of $n$ weighted points $P=\ p_1,p_2,…,p_n\ $, and a set of $m$ intervals of length $L_1,L_2,…,L_m$.
- In this section, we show a $Ω(mn)$ lower bound for this problem, conditioned on the conjectured hardness of the $(min,+)$-convolution problem.
- But the latter problem is conjectured to be $Ω(n^2)$ hard, and hence the $(min,+,M)$-convolution problem has a conditional lower bound of $Ω(nm)$.
- Lower bound for batched smallest k-enclosing interval In this section, we study a closely related problem, that is the batched version of the smallest $k$-enclosing interval (BSEI).
- There is a quadratic time algorithm for the problem, and we show that there is also a matching quadratic lower bound conditioned on the conjectured hardness of the $(min,+)$-convolution problem.
- An immediate open problem is to extend this technique to answer colored MaxRS for boxes and balls in $ ^3$. ; For the batched MaxRS problem in $ ^1$, the conditional hardness result lower bound of $Ω(mn)$ holds for the weighted set of points (Theorem~ [ref] ).
