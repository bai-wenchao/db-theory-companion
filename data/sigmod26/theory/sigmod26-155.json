{"id": "sigmod26-155", "title": "Maintaining Queries under Updates Using Heavy-Light Partitioning of the Input Relations", "from": "tex", "n_stmt": 22, "n_proof": 8, "kinds": {"proposition": 5, "definition": 13, "lemma": 4}, "counts": {"np_hard": 0, "lower_bound": 1, "upper_bound": 18, "big_o": 2, "omega": 3, "theta": 2, "approx_ratio": 11, "whp": 0, "regret": 0, "dp": 0, "invariant": 2, "convergence": 0, "competitive": 0, "worst_case": 10, "sketch": 0, "cost_model": 0, "cardinality": 0, "learned": 0}, "statements": [{"kind": "definition", "num": "(Delta) View Tree", "text": "A view tree $T$ for a query $Q$ is a rooted tree with the properties: ; There is a one-to-one mapping between the leaves of $T$ and the atoms of $Q$. ; Each inner node is a view over some variables of $Q$. ; If a node $V'( Y )$ has a single child node $V( X )$, then it is a projection view defined by marginalizing variables of $V( X )$, i.e., $V'( Y ) = Σ_ X Y V( X )$. Furthermore, every atom of $Q$ with a variable from $ X Y $ occurs in the subtree rooted at $V( X )$. ; If a node $V( X )$ has several children $V_1( X_1 ), … , V_1( X_n )$, for $n ≥ 2$, then it is a join view defined by the natural join of the child views, i.e., $V( X ) = V_1( X_1 )·…· V_k( X_n )$. Under an update to a relation $R$, the view tree $T$ becomes a delta view tree , denoted by $δ T_R$, where $R$ is replaced by $δ R$, and each view $V$ along the path from $δ R$ to the root view is replaced by $δ V$."}, {"kind": "definition", "num": "Guarding Query", "text": "Let $ d$ be a degree configuration, $δ T_R$ be a delta view tree for an update $δ R_j$, $δ V( X)$ be a delta view in $δ T_ R_j $, and $ = ( (δ V( X)), d)$. For any set $ Y$ of variables such that $ X Y ( )$ and any acyclic set $ ' A ( [ Y])$ of degree constraints, we define the $ '$-guarding query of $δ V$ as the join query: Q_ ' ( Y) = R'_1( X'_1)·…· R'_k( X'_k) where $ R_1( X_1), ,R_k( X_k) $ is the set of atoms at the leaves of $δ V( X)$ in $δ T_ R_j $ that guard the constraints in $ '$ and includes $δ R_j( X_j)$, and $R'_i( X'_i) = Σ_ X_i Y R_i( X_i)$ and $ X_i' = X_i Y$ for $i [k]$."}, {"kind": "definition", "num": "Degree Constraint", "text": "[cite] A degree constraint on a database of size $N$ is a tuple \\(( Z| Y, N^ p_ Z| Y )\\), where $ Y Z$ and \\(p_ Z| Y Q _ ≥ 0 \\). Let \\(Q\\) be a query and $R_i( X_i)$ an atom of \\(Q\\). Then $R_i( X_i)$ guards the degree constraint \\(( Z| Y, N^ p_ Z| Y )\\) if \\( Z X_i\\) and $max_ t | _ Z (σ_ Y= t. Y R_i)|≤ N^ p_ Z| Y $. We denote by \\( _Q(c)\\) the set of atoms of $Q$ that guard the degree constraint \\(c\\)."}, {"kind": "definition", "num": "Relation Part", "text": "Given a query $Q$ with join variable sets $( Y_1 , ..., Y_n )$, degree configuration $ d = (d_1, ..., d_n)$, and atom $R( X )$ in $Q$ mapped to the database relation $ R $, the $ d $-restriction of $ R $ is given by \\[Π_ i [n]: Y_i X ( 1 _ d_i L ·Σ_ y Light( Y_i ) σ_ Y_i = y R + 1 _ d_i H ·Σ_ y Heavy( Y_i ) σ_ Y_i = y R ) \\] where the indicator $ 1 _ $ is 1 in case $ $ holds and 0 otherwise."}, {"kind": "lemma", "num": "", "text": "Let $Q$ be a join query, $ d$ a degree configuration, $δ R_j$ a single-tuple update, and $δ T$ a delta view tree for $Q$ and $δ R_j$. The time to compute all the delta views $δ V_1( X_1),…,δ V_k( X_k)$ along the path from $δ R_j$ to the root of $δ T$ is $ (N^w)$, where \\[ w = max_ i [k] ~~~~~min_ A ( ( (δ V_i( X_i), d,δ R_j)\\\\ X_i X'_i ( ) ~~~~ ( [ X'_i]). \\]"}, {"kind": "definition", "num": "Polymatroid Bound under Degree Constraints", "text": "[cite] Given a set \\( =\\ c_1,…,c_m\\ \\) of degree constraints, we define the Polymatroid Bound under Degree Constraints , denoted by \\( ( )\\), as the optimal value of the following linear program: minimize & Σ_ c_i=( Z| Y,N^ p_ i ) w_ i · p_ i \\\\ subject to &Σ_ c_i=( Z| Y,N^ p_ i ) \\\\ A Z Y w_ i ≥ 1 && A ( )\\\\ &w_ i ≥ 0 && c_i=( Z| Y,N^ p_ i )"}, {"kind": "definition", "num": "Acyclic Sets of Degree Constraints", "text": "For any set $ $ of degree constraints, associate a directed graph \\(G_ \\) with a node for every variable in $ ( )$ and with a directed edge \\((y,z) Y× ( Z- Y)\\) for every degree constraint \\(( Z Y,N^ p_ Z| Y ) \\) \\( \\). If \\(G_ \\) is acyclic, then \\( \\) is called acyclic . We denote by $ A ( )$ the set of all maximal acyclic subsets of \\( \\)."}, {"kind": "lemma", "num": "", "text": "For any delta view $δ V( X)$ and any of its $ [ Y]$-guarding queries $Q_ [ Y] ( Y)$, where $ $ is the set of constraints that are guarded by the leaves of $δ V( X)$ and $ X Y$, it holds: For any tuple $ x$ in the output of $δ V$ over any database, there is a tuple $ y$ in the output of $Q_ [ Y] $ over the same database such that $ x = y. X$."}, {"kind": "lemma", "num": "", "text": "Given a database of size $N$, a delta view tree $δ T_R$, a delta view $δ V( X)$ in $δ T_R$, and any of the $ $-guarding queries of $δ V( X)$, where $ $ is an acyclic set of constraints, then $δ V$ can be computed in time $ (N^ ( ) )$ given that the child views of $δ V( X)$ in $δ T_R$ are already computed."}, {"kind": "lemma", "num": "", "text": "Let $Q$ be a connected conjunctive query and let $(T, )$ be a non-redundant tree decomposition of $Q$. Then there exists a view tree $ T$ of $Q$ such that the non-redundant tree decomposition obtained from $ T$ by the construction above is isomorphic to $(T, )$."}, {"kind": "definition", "num": "Relation Restriction", "text": "Given a query $Q$ with the tuple of join variables $(Y_1, …, Y_n)$, degree configuration $ d = (d_1, ..., d_n)$, and atom $R( X )$ in $Q$, the $ d $-restriction of relation $R$ is: \\[ \\ r r R i [n]: Y_i X (d_i=L r. Y_i Light(Y_i) d_i=H r.Y_i Heavy(Y_i))\\ . \\]"}, {"kind": "definition", "num": "Degree        Configuration", "text": "Given a query $Q$ with join variables $( Y_1,…, Y_n)$, a degree configuration is a tuple $(d_1,…,d_n)$, where $d_i L,H $, for $i [n]$. For a join variable $ Y_i$ we say that $ Y_i$ is light if $d_i=L$ and heavy otherwise."}, {"kind": "definition", "num": "Maintenance Width", "text": "For a join query \\(Q\\), the maintenance width of \\(Q\\) is (Q) & min_ ε [0,1] ~ max_ d D(Q) min_ T (Q) ~ max_ V( X) T\\\\ R( Y ) (V( X ), T) ~~~ min_ ( (δ V( X), δ T_R), d)\\\\ X X' ( )\\\\ ' A ( [ X'])\\\\ w S _ ' ( w, ')(ε)."}, {"kind": "definition", "num": "Database Part", "text": "Given a query $Q$, a database $D$, ordered join variable sets $ Y $, and a degree configuration $ d $, the corresponding database part is the set of relation parts defined by $ Y $ and $ d $ for each relation in $D$."}, {"kind": "definition", "num": "Degree Configuration", "text": "Given a query $Q$ with the tuple of join variables $(Y_1,…, Y_n)$, a degree configuration is a tuple $(d_1,…,d_n)$, where $d_i=L$ in case $Y_i$ is light and $d_i=H$ in case $Y_i$ is heavy ($i [n]$)."}, {"kind": "proposition", "num": "", "text": "For any join query $Q$ and sequence of single-tuple updates, the amortized major and minor rebalancing time is \\( (2^ (Q) )\\) per single-tuple update, where $ (Q)$ is the maintenance width of $Q$."}, {"kind": "definition", "num": "Degree Configuration", "text": "Given a query $Q$ with join variables $(Y_1,…,Y_n)$, a degree configuration is a tuple $(d_1,…,d_n)$, where $d_i=L$ in case $Y_i$ is light and $d_i=H$ in case $Y_i$ is heavy, for $i [n]$."}, {"kind": "proposition", "num": "", "text": "If $Q$ is a Loomis-Whitney $k$ query where $k ≥ 3$, then $Q$ admits $ (N^ 1/2 )$ (amortized) update time and (non-amortized) $ (1)$ enumeration delay using our approach."}, {"kind": "proposition", "num": "", "text": "For any join query $Q$ and view tree set $ $ constructed by our approach for $Q$, it holds that the output of $Q$ can be enumerated from $ $ with constant delay."}, {"kind": "proposition", "num": "", "text": "If $Q$ is a hierarchical query, then $Q$ admits $ (1)$ (amortized) update time and (non-amortized) $ (1)$ enumeration delay using our approach."}, {"kind": "definition", "num": "Dynamic Width", "text": "For any join query \\(Q\\), the dynamic width of \\(Q\\) is (Q) min_ T T (Q) ~ max_ V( X ) T\\\\ R( Y ) (V( X ),T) ~~~ ^*_ Q_ (V( X ),T) ( X Y )."}, {"kind": "proposition", "num": "", "text": "For any join query $Q$, it holds $ (Q) ≤ (Q)$."}], "proofs": [{"text": "Consider the Loomis-Whitney $k$ query $Q$, where $k ≥ 3$, and set the partitioning threshold to $ε = 1/2$. Then there are $2^k$ unique degree configurations which fall into two cases: Either the degree configuration indicates at least one light variable, or all variables are heavy. Consider the first case and wlog, let the light variable be $X_k$, which appears in the schema of all relations except $R_k$. We construct the view tree for this degree configuration by joining $R_1(X_2, ..., X_k)$ and $R_2(X_1, X_3, ..., X_k)$ to create the view $V_3(X_1, ..., X_k)$. The view $V_ i+1 (X_1, ..., X_k)$ is created by joining $V_ i (X_1, ..., X_k)$ and $R_ i (X_1, ..., X_ i-1 , X_ i+1 , ..., X_k)$ for all $i [3, k-1]$. We create $V_ k+1 $ by projecting $V_ k $ onto $ (R_k)$, and we create $V_ k+2 $ by intersecting $V_ k+1 $ and $R_k$. This view tree is shown in Fig.~ [ref] . We now show the update time is $ (N^ 1/2 )$. Consider an update $δ R_1$. The first delta view to be computed is \\[δ V_3(X_1,...,X_k) = δ R_1(X_2,...,X_k) · R_2(X_1,X_3,...,X_k).\\] The number of $X_1$-values for a given $X_k$-value is at most $N^ 1/2 $, so the compute time is $ (N^ 1/2 )$. Each $V_4, ..., V_k$ is simply the semi-join reduction of $V_ i-1 $ with $R_ i-1 $ and can be inductively shown to have the same compute time. $V_ k+1 $ is a projection and $V_ k+2 $ is an intersection and achieve the same compute time. An update to $R_2$ is symmetric and achieves the same update time. Now consider an update $δ R_3$. \\[δ V_4(X_1,...,X_k) = V_3(X_1,...,X_k) · δ R_3(X_1,X_2,X_4,...,X_k)\\] and the number of $X_3$-values for a given $X_k$-value is at most $N^ 1/2 $, and so $δ V_4$ has compute time $ (N^ 1/2 )$. As before, the semi-joins $V_5, ..., V_k$, the projection $V_ k+1 $, and the intersection $V_ k+2 $ have the same compute time. Updates to $R_4, ..., R_ k-1 $ are symmetric and have the same update time. Now consider an update $δ R_k$. Because $V_ k+2 $ is simply an intersection of $V_ k+1 $ and a single tuple, the compute time is $ (1)$. Thus, the update time is $ (N^ 1/2 )$ for such degree configurations. Now consider the case where all variables are heavy. Construct the same view tree as in the first case, and choose $X_k$ arbitrarily. Consider an update $δ R_1$. There are at most $N^ 1/2 $ different $X_1$-values, so the compute time is $ (N^ 1/2 )$. As before, the semi-joins $V_4, ..., V_k$, the projection $V_ k+1 $, and the intersection $V_ k+2 $ have the same compute time. Thus, the u …"}, {"text": "[Proof of Proposition~ [ref] ] For any hierarchical query, there is a view tree that contains for each atom $R( X)$ with $ X = \\ X_1, …, X_n \\ $ a root-to-leaf path of the form \\[V_1'( X_1') V_1( X_1) … V_n'( X_n') V_n( X_n) R( X)\\] such that for each $i [n]$, it holds: (1) $V_i( X_i)$ is a join view such that all views and atoms containing $X_i$ are in the subtree rooted at $V_i( X_i)$; (2) $V_i'( X_i')$ is a projection view that projects away $X_i$ from $V_i( X_i)$. Given the structural properties of hierarchical queries, this implies that any two sibling views in the view tree must be over the same set of variables. For any single-tuple update to relation $R$, each of the views $V_i$ and $V_i'$ on the path from the atom $R( X)$ to the root of the view tree can be updated as follows: if $V_i$ is a join view, we do constant-time look-ups in the child views of $V_i$ (corresponding to an intersection); if $V_i'$ is a projection view, we project the update tuple onto the variables of $V_i'$, which takes only constant time. The constant-delay enumeration is a general property of the view trees considered in this paper. Our approach will explore the space of view trees and for each degree configuration will pick the above view tree as it has the smallest update time."}, {"text": "We say that two views in a view tree are independent if they do not appear on a root-to-leaf path in the view tree. Consider a view tree $ $ and a variable $X ( )$. For the sake of contradiction, assume that $ $ does not contain a unique view owning $X$. This means that $ $ has two independent views $V_1( X_1 )$ and $V_2( X_2 )$ such that (i) $X X _1$, (ii) $X X _2$, (iii) for any view $V_1'( X _1')$ above $V_1( X _1)$, it holds $X X _1'$, and (iv) for any view $V_2'( X _2')$ above $V_2( X _2)$, it holds $X X _2'$. This implies that the parent view $ V _1( X _1 )$ of $V_1( X _1)$ is a projection view that projects away $X$, which means $X X _1$. The definition of view trees requires that $V_2$ must be in the subtree rooted at $V_1$ (Definition~ [ref] ). This means that $V_1$ and $V_2$ cannot be independent, which is a contradiction."}, {"text": "[Proof of Proposition~ [ref] ] As explained at the beginning of Section~ [ref] , it suffices to show that for any view tree $ $, the set of tuples in the join of the views of $ $ can be enumerated with constant delay. The procedure Enumerate in Fig.~ [ref] describes our enumeration strategy for any given view tree. First, we explain the details of the procedure. Then, we show its correctness, i.e., we explain why it enumerates all distinct tuples represented by the view tree. Finally, we show that it enumerates with constant delay. Enumeration Procedure Let $ $ be the set of views in the input view tree $ $ that own at least one variable in $ $. For each $i [n]$, let $ O _i$ be the variables owned by $V_i$ and let $ N _i = X _i O _i$. The procedure Enumerate creates a strict ordering $V_1( X_1 ), …, V_n( X_n )$ of the views in $ $ that is consistent with the partial ordering given by $ $ (Line~2), i.e., for any $i, j [n]$ it holds: if $V_j$ is on the path from $V_i$ to the root of $ $, then $i <j$. The procedure constructs the first value tuple over $ ( )$ by traversing the views in the strict order as follows (Lines~4--11). All variables in $ X_1 $ must be owned by $V_1$, hence, $ X _1 = O _1$. The procedure retrieves a tuple $ t _1$ over $ O _1$ from $V_1$. For any $i \\ 2, … , n\\ $ it proceeds as follows. Let $ t _ i-1 $ be the tuple constructed before visiting the view $V_ i $ and let $ t _ i-1 ' = _ N _i t _ i-1 $. The procedure retrieves a tuple $ t _i$ from $V_i( t _ i-1 ', O _i)$. After all views in $ $ are visited, we have a complete tuple $ t = t _1 … t _n$, which is reported by the procedure. Then, the procedure iterates over the remaining tuples in $V_n( t _ n-1 ', O _n)$ and outputs for each such tuple $ t _n$, the tuple $ t = t _1 … t _ n-1 · t _n$. After all tuples in $V_n$ are exhausted, the procedure backtracks, i.e., it retrieves the next tuple in $V_ n_1 $ and iterates again over the tuples in $V_n$. The procedure stops after each view in $ $ is exhausted. Correctness The correctness of the enumeration procedure follows from two observations. Firstly, the values of any variable $X$ are retrieved from the view that owns $X$, which by Proposition~ [ref] is unique. This means that the values are retrieved from a view that joins all views containing $X$. Secondly, it follows from Proposition~ [ref] that for each view $V_i( X _i)$, it holds: all variables in $ N _i$ have their owning views above $V_i$ in the view tree. This means that at the t …"}, {"text": "The proof is implied by the following chain of (in)equalities. We explain each step below. (Q) & min_ ε [0,1] ~ max_ d D(Q) min_ T (Q) ~ max_ V( X) T\\\\ R( Y ) (V( X ),T) ~~~ min_ ( (δ V( X), δ T_R), d)\\\\ X X' ( )\\\\ ' A ( [ X'])\\\\ w S _ ' ( w, ')(ε) \\\\ & (1) ≤ max_ d D(Q) min_ T (Q) ~ max_ V( X) T\\\\ R( Y ) (V( X ), T) ~~~ min_ ( (δ V( X), δ T_R), d)\\\\ X X' ( )\\\\ ' A ( [ X'])\\\\ w S _ ' ( w, ')(1) \\\\ & (2) = min_ T (Q) ~ max_ V( X) T\\\\ R( Y ) (V( X ),T) ~~~ min_ ( (δ V( X), δ T_R), L)\\\\ X X' ( )\\\\ ' A ( [ X'])\\\\ w S _ ' ( w, ')(1) \\\\ & (3) ≤ min_ T (Q) ~ max_ V( X) T\\\\ R( Y ) (V( X ),T) ~~~ min_ w S _ ( w, )(1) \\\\ & (4) ≤ min_ T (Q) ~ max_ V( X) T\\\\ R( Y ) (V( X),T) ^*_ Q_ (V( X),T) ( X Y) \\\\ & \\ (Q), where $ L = (L)_ | J _Q| $ is the degree constraint where all join variables are light and $ = [ X ]$ with $ = \\ ( Z | , N) | S( Z )$ $ $ $ (δ V( X ), δ T_R) S ≠ R\\ $ $ $ $\\ (B| ,1)|B Y)\\ $. Inequality~(1) is obtained by fixing $ε$ to $1$. Equality~(2) is implied by the following observation. Consider a degree configuration $ d D(Q)$ and a vector $ w S _ ' $ in the definition of $ (Q)$. Assuming that $ε = 1$, any degree constraint resulting from a variable $A$ that is heavy in $ d $ is of the form $(A , N^ 1-ε = N^0)$. This implies that for any vector $ w S _ ' $, the additive factor added to $ ( w, ')(1)$ by such a constraint is of the form $w_i · (1-ε) = 0$. Hence, instead of maximizing $ ( w, ')(1)$ over all possible degree configurations, it suffices to restrict to the configuration $ L = (L)_ | J _Q| $, where all variables are light. Inequality (3) holds because $ ( (δ V( X), δ T_R),$ $ L)$ and both $ $ and $ = [ X ]$ are acyclic, since the graph associated with $ $ does not have any edge. For Inequality (4), consider a view $δ V( X )$ in a delta view tree for an update $δ R( Y )$. Assume that $ C = \\ c_1, … , c_m\\ \\ c^B\\ _ B X Y $, where each $c_i$ is of the form $( Z | , N)$ and each $c^B$ is of the form $(B| , 1)$. The linear program determining $min_ w S _ ( w, )(1)$ is as follows: minimize & Σ_ c_i = ( Z| ,N) w_i · 1 + Σ_ B X Y w^B · 0 \\\\ subject to & Σ_ c_i = ( Z| ,N) \\\\ A Z w_i ≥ 1 && A X Y \\\\ & Σ_ c_i = ( Z| ,N) \\\\ B Z w_i + w^B≥ 1 && B X Y \\\\ & w_ i ≥ 0 && i [m] \\\\ & w^ B ≥ 0&& B X Y The above program simplifies to the linear program determining $ ^*_ Q_ (V( X),T) ( X Y )$ due to the following two observations. Firstly, each constraint in $ $ is of the form $( Z | , N)$ or of the form $(B| ,1)$ with $B X Y $. Secondly, the weights $w^B$ associated  …"}, {"text": "Let $\\ R_1( X_1),…, R_k( X_k)\\ = (δ V( X))$. The delta view is thus defined by $$δ V( X) = Σ_ ( _ i [k] X_i) X R_1( X_1)·…· R_k( X_k).$$ Let $ $ be the set of constraints that are guarded by the database relations $R_1,…,R_k$. Since $ $ always contains the size constraints of these relations, it holds that $ ( ) = _ i [k] X_i$. By definition of the $ [ Y]$-guarding query $Q_ [ Y] ( Y)$ (Def.~ [ref] ), we have that $ X Y ( )$ and: $$Q_ [ Y] ( Y) = R_1'( X_1')·…· R_k'( X_k'), where R_i'( X_i') = Σ_ X_i Y R_i( X_i) and X_i' = X_i Y for i [k].$$ The set of tuples in the output of $δ V$ is: t. X Σ_ t ( ( )) R_1 ( t. X_1)·…· R_k( t. X_k)>0 , whereas the set of tuples in the output of $Q_ [ Y] $ is: y ( Y) R'_1( y. X'_1)·…· R'_k( y. X'_k)>0 . If the output of $δ V$ is empty, then the statement of the lemma follows trivially. Otherwise, consider a tuple \\( x\\) in the output of $δ V$ given in Eq.~ [ref] . Since the sum in Eq.~ [ref] is positive, it must contain at least one positive term. Thus, there exists a tuple \\( t ( ( ))\\) with \\( t. X = x\\) such that \\(R_1( t. X_1), , R_k( t. X_k)\\) are all positive. It then also follows that \\(R'_1( t. X'_1)·…· R'_k( t. X'_k)>0\\), since $ X'_i X_i$ and $R'_i$ is defined by marginalizing $ X_i Y$ from $R_i$, for $i [k]$. We conclude that for any tuple \\( x\\) such that \\(δ V( x)>0\\) there exists at least one tuple \\( y ( Y)\\) with \\( y. X= x\\) such that \\(Q_ [ Y] ( y)>0\\)."}, {"text": "We compute $δ V$ in two steps. In the first step, we compute the join query $Q_ $ in time $ (N^ ( ) )$ using the algorithm from prior work~ [cite] . Its output is, however, an over-approximation of the output of $δ V$ as stated in Lemma~ [ref] . In the second step, we first create a relation $V'$ that is the projection of $Q_ $'s output onto the set of variables of the child views of $δ V$ in $T$. We next semi-join reduce $V'$ with each of the child views of $δ V$. The multiplicity of each tuple $ t$ in $V'$ becomes the product of the multiplicities of the tuples in the child views whose join make $ t$ in $V'$. By performing the computation of the delta views bottom-up in the delta view tree, we ensure that the correct multiplicities of the child views are computed before those of the parent views. Finally, we marginalize out all variables of $V'$ except $ X$ to obtain the output of $δ V$. The second step also takes time proportional to the size of $Q_ $'s output, so in $ (N^ ( ) )$ time."}, {"text": "Dan: Proof needs to be adjusted, there is some name clashing. We bound the total computation cost by the cost of updating the most expensive delta view. Let \\(δ V( X) = V_1( X_1)·…· V_k( X_k) = Σ_ Y X R_1( Y_1)·…· δ R_j( Y_j)·…· R_l( Y_l)\\) be the most expensive delta view along the path from \\(R_j\\) to the root of \\(T\\), where \\( Y = Y_1 Y_l\\) and \\( R_1( Y_1), ,R_l( Y_l) = (V( X))\\). The view \\(V( X)\\) must be a join views since the update cost of a projection view is bounded by the update cost of its child. It follows from lemma:pbd-for-conjunctive-queries that for any set of degree constraints \\( ( (V( X), d,δ R_j) \\) and any set of variables \\( X'\\) with \\( X X' Y\\), the \\( [ X']\\)-guarding query \\(Q_ [ X'] ( X')\\) is an over approximation of \\(δ V\\). Furthermore, \\(Q_ [ X'] ( X')\\) can be computed in time \\( (N^ ( [ X']) )\\) by thm:hung-algo . To compute the exact value of \\(δ V\\), we assume inductively that all the children \\(V_1( X_1), ,V_k( X_k)\\) have already been computed. Then, we can compute \\(δ V\\) in time \\( (N^ ( [ X']) )\\) by computing the join \\(Q_ [ X'] ( X)· V_1( X_1)·…· V_k( X_k)\\)."}], "bound_sents": ["The join query $Q_ _1 $ that over-approximates $δ V_3$ gives the best upper bound of $ (N^ ε )$ on the compute time of $δ V_3$.", "The constraint set \\[ _5[ABC] = \\ (BC| ,N), (C| ,N),(A| ,N),(BC|B,N^ε),(BC|C,N^ε),(A, ,1), (B| ,1)\\ \\] has the guarding query $Q_ _5[ABC] = δ R(A,B) · S(B,C) · T'(C) · U'(A)$, where $T'$ is the projection of $T$ onto $C$ and $U'$ is the projection of $U$ onto $A$. $Q_ _5[ABC] $ over-approximates $δ  …", "The machinery developed in this paper to upper bound the sizes and compute times for delta views already works for arbitrary conjunctive queries. ; We can extend our approach to richer constraints beyond degree constraints: Using $ _p$-norms on the degree sequences of join columns, we can obtain tig …", "The following degree constraints are guarded by the leaves of $δ V_5(A,C)$: [display] The \\( \\)-guarding query is \\(Q_ (A,B,C,D) = δ R(A,B)· S(B,C)· T(C,D)· U(A,D)\\) and its output can be computed in worst-case optimal time \\( (N^ ( ) )\\)~ [cite] .", "Second, it gives an upper bound on the update time for a delta view: This is the minimum of the computation time over all its guarding queries.", "A question remains: Why should we consider all guarding queries of a delta view in order to upper bound the compute time for the delta view?", "As shown in Ex.~ [ref] , this gives the upper bound of \\( (N^ min(1, 2ε) )\\), which is also worse than $ (N^ ε )$ for all $ε (0, 1)$.", "We first consider $δ V_1(A,B,C) = δ R(A,B)· S(B,C).$ The degree constraints that hold at the leaves of $δ V_1(A,B,C)$ in the delta view tree are: $$ _1= ( (δ V_1(A,B,C)), d) = \\ (BC| , N), (BC| B, N^ε), (C| , N^ 1-ε ), (A| , 1), (B| , 1)\\ .$$ We obtain the exponent $s$ of an upper bound $ (N^s)$ on  …", "The delta view $δ V_1$ is a join query that can be computed using an adaptation of a worst-case optimal join algorithm~ [cite] in time $ (N^ min(ε, 1-ε) )$.", "In the paper, we introduce a systematic approach to upper bound the time to compute conjunctive queries with bound variables, such as $δ V_2$.", "This requires computing: (1) the worst-case optimal size of each delta view of a materialized view in the view tree; (2) the delta view in worst-case optimal time, so proportional to its worst-case optimal size.", "There are two challenges here: (i) we need to do this in the context of a degree configuration; (ii) we do not know a priori which threshold parameter is best for the query, so we need to express the worst-case optimal size of the delta view as a function of the threshold parameter and later obtain  …"], "flag": true, "score": 40, "n_chars": 256702}