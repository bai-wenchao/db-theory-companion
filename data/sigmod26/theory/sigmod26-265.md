# sigmod26-265 — Accelerating High-Dimensional ANN Search via Skipping Redundant Distance Computations

flag=True score=11 stmts=6 proofs=2 chars=103504
kinds: {"definition": 1, "lemma": 1, "theorem": 4}
counts: {"np_hard": 0, "lower_bound": 67, "upper_bound": 1, "big_o": 3, "omega": 0, "theta": 0, "approx_ratio": 0, "whp": 1, "regret": 0, "dp": 0, "invariant": 0, "convergence": 1, "competitive": 0, "worst_case": 0, "sketch": 0, "cost_model": 0, "cardinality": 2, "learned": 0}

## Statements
**Definition 2.** 1 (k-Nearest Neighbor Search). Given a set of points 𝑃 with each points p ∈ R𝐷 , a query point q ∈ R𝐷 , and distance function 𝛿 (p, q), find a set 𝐾 ⊂ 𝑃 with 𝑘 points, where for any point p ∈ 𝐷 \ 𝐾, 𝛿 (p, q) ≥ maxp∈𝐾 𝛿 (p, q). Proc. ACM Manag. Data, Vol. 3, No. 6 (SIGMOD), Article 288. Publication date: December 2025. Accelerating High-Dimensional ANN Search via Skipping Redundant Distance Computations 288:5 Approximate nearest neighbor search allows for retrieval of approximate, rather than exact nearest neighbors. In practice, we do not specify constant error as discussed in [27]. We use recall to measure the accuracy of the result [69], which definition is as: |𝑅𝑜 ∩ 𝑅𝑘 | 𝑟𝑒𝑐𝑎𝑙𝑙 (𝑅𝑜 ) = (1) |𝑅𝑘 | where| · | denotes the number of elements in a set, 𝑅𝑜 denotes the search result, and 𝑅𝑘 is the correct 𝑘 nearest neighbors. 2.2 Johnson-Lindenstrauss Lemma The Johnson-Lindenstrauss lemma (JL lemma) [42] is a foundational result in dimensionality reduction that guarantees the approximate preservation of pairwise distances between points when projected into a lower-dimensional space. The proof of the JL lemma rely on the distributional Johnson-Lindenstrauss lemma [45], which shows in following.

**Lemma 2.** 2 (distributional JL lemma [42, 45]). Let 𝜖, 𝛽 ∈ (0, 1), there exists a probability  distribution 𝐹 over linear functions 𝑓 : R𝐷 → R𝑑 , where 𝑑 = Θ 𝜖 −2 log 𝛿1 , such that for any fixed x ∈ R𝐷 ,   Pr ∥𝑓 (x) ∥ 22 − ∥x∥ 22 ≤ 𝜖 ∥x∥ 22 ≥ 1 − 𝛽. (2) 𝑓 ∼𝐹 Let S be a random d-dimensional subspace, let xi′ be the projection of xi ∈ 𝑃 into S, we set 𝜌 = ∥xi′ − xj′ ∥ 22 to be the square 𝑙 2 norm, and 𝜇 = (𝑑/𝐷) ∥xi − xj ∥ 22 , then we have the following inequality [21]:   𝑑 (𝜖 2 /2 − 𝜖 3 /3) . (3) Pr [𝜌 ≥ (1 + 𝜖)𝜇] ≤ exp − 2 The distributional JL lemma provides probabilistic bounds on the distortion of distances between points when data are projected into a lower-dimensional space. This theoretical foundation guides our approach to dimensionality reduction in candidate search of Section 4, where it helps us design the basic search method. 2.3 MS-distance The MS-distance [36] is a measure of distance between two vectors in the Euclidean space, transformed using their mean and standard deviation. M stands for mean and S stands for standard deviation. The MS-distance aims to use mean and standard deviation to approximately represent the overall information of a vector. It provides lower and upper bounds for the Euclidean distance, with the lower bound form being the focus of this work.

**Theorem 2.5.** For any two points x and y, we have the following properties [36]: • 𝑀𝑆𝐿(x′, y′, 𝑚) monotonically increases with 𝑚. • 𝛿 (x, y) ≥ 𝑀𝑆𝐿(x′, y′, 𝑚) for all 0 ≤ 𝑚 ≤ 𝐷. • 𝛿 (x, y) == 𝑀𝑆𝐿(x′, y′, 𝑚) when 𝑚 = 𝐷. Those properties enable us to use the MS-distance as a lower bound for the Euclidean distance. We can gradually increase dimensions to obtain a better lower bound until exact distance is calculated. 2.4 DCO and Lower Bound Pruning Distance comparison operation (DCO) [29] is a key step in the search process. Alg. 1 outlines a common step in search procedure, within which the DCO plays a pivotal role. Specifically, DCO computes the distance between a candidate point x and the query q, then compares it against a dynamic threshold 𝜏. If 𝑑𝑖𝑠𝑡 (x, q) < 𝜏, then the actual distance will be returned. The DCO result will be used to update the result set and the threshold 𝜏. Previous studies [29] have demonstrated that distance computation constitutes a significant portion of search overhead, and most points will exceed the threshold, providing an opportunity to improve performance. Algorithm 1: Common Step in Search Input: query q, data structure T, result set R, threshold 𝜏 Output: result set R, new threshold 𝜏 1 Let x be a point from T; 2 𝑑𝑖𝑠𝑡 x = 𝛿 (x, q); 3 if 𝑑𝑖𝑠𝑡 x < 𝜏 then 4 Update result set R; 5 Update 𝜏; 6 // calculate distance return R, 𝜏; The lower bound is widely used in index design to enable efficient pruning. If a lower bound 𝐿𝐵(q, p) ≥ 𝜏, p can be pruned. Lower bound c …

**Theorem 4.1.** Given a point p ∈ 𝑃 from the dataset, a query point q, and a parameter 𝜖 ∈ (0, 1). Let A ∈ R𝑑 ×𝐷 be the first 𝑑 rows of a random orthogonal matrix A𝐷 ×𝐷 , and let 𝜏 represent the farthest square distance in the ef-queue. The dimensions 𝑑 and 𝐷 (with 𝑑 ≤ 𝐷) correspond to the projected and original spaces, respectively. Furthermore, let 𝑝 1, 𝑝 2 ∈ (0, 1). We then have: • If 𝛿 (p, q) < 𝜏, then Pr(∥A(p − q) ∥ 22 < (1 + 𝜖)𝑑/𝐷 · 𝜏) > 𝑝 1 . • If 𝛿 (p, q) > 𝜏, then Pr(∥A(p − q) ∥ 22 < (1 + 𝜖)𝑑/𝐷 · 𝜏) < 𝑝 2 .

**Theorem 5.1.** Let x′, y′ be the (𝐷+2)-dimensional vector transformed according to MS-transformation ′ in Definition 2.3. We apply PCA to subvector xsub = [𝑥 1′ , 𝑥 2′ , . . . , 𝑥 𝐷′ ] to get x′′ = [𝜇𝑥 , 𝜎𝑥 , 𝑥 1′′, 𝑥 2′′, . . . , 𝑥 𝐷′′ ]. (7) We do the same for y′ to get y′′ . The MS-distance 𝑀𝑆𝐿(x′′, y′′, 𝑚) can still serve as a lower bound and every properties of Theorem 2.5 still hold. ′′ −

**Theorem 5.** 1 shows that after applying PCA to the MS transformed data, MSPCA can also be used for pruning in the search process by iteratively refining to obtain a tighter lower bound until the exact distance is obtained. 5.1.3 Improving the iteration efficiency. Our adaptive lower bound pruning method iteratively refines the lower bound by computing the distance in subspace. The inclusion of 𝜎𝑥 𝜎 𝑦 in second component of MS-distance introduces per-iteration multiplication overhead, potentially affecting performance. We optimize this part to improve efficiency while preserving correctness. The iteration process involves threshold comparison, formally expressed as: 𝐷 · ((𝜇x − 𝜇y ) 2 + (𝜎x − 𝜎y ) 2 ) + 𝜎x𝜎y 𝑘 ∑︁ (𝑥𝑖′ − 𝑦𝑖′ ) 2 > 𝜏 . (9) 𝑖=0 We apply the following one-time transformation to the threshold, 𝑑𝑖𝑠 ′ = 𝑑 ∑︁ 𝑖=0 (𝑥𝑖′ − 𝑦𝑖′ ) 2 ≤ 𝜏 − 𝐷 ((𝜇x − 𝜇y ) 2 + (𝜎x − 𝜎y ) 2 ) = 𝜏 ′. 𝜎x𝜎y (10) Consequently, we compare the distance between subspaces directly with the modified threshold 𝜏 ′ instead of the original threshold 𝜏. This approach requires only one multiplication and one division operation: one during initialization and another when returning the actual distance. Crucially, the iterative process itself becomes multiplication-free, reducing computational overhead. 5.1.4 Lower bound based candidate search. In this subsection, we present our lower bound based candidate search method. Accurate distance estimation is crucial for query performance, as inaccuracies may falsely exclude valid …

## Proofs
**Proof.** According to distributional JL lemma, the projected result follows a probability distribution. Let Pr(∥A(p − q) ∥ 22 < (1 + 𝜖)𝑑/𝐷 · 𝑟 ) = 𝐹 = 𝑝, where 𝐹 (·) is the cumulative distribution function. If 𝑟 = 𝛿 (p, q) < 𝜏, we have Pr(∥A(p − q) ∥ 22 < (1 + 𝜖)𝑑/𝐷 · 𝜏) > Pr(∥A(p − q) ∥ 22 < (1 + 𝜖)𝑑/𝐷 · 𝑟 ) = 𝑝 = 𝑝 1 Similarly, if 𝑟 = 𝛿 (p, q) > 𝜏, we have Pr(∥A(p − q) ∥ 22 < (1 + 𝜖)𝑑/𝐷 · 𝜏) < Pr(∥A(p − q) ∥ 22 < (1 + 𝜖)𝑑/𝐷 · 𝑟 ) = 𝑝 = 𝑝 2 . □

**Proof.** The transform matrix A ∈ R𝐷 ×𝐷 from the PCA is orthogonal matrix. Let v′ = xsub ′′ , v′′ = x′′ − y′′ we have ysub sub sub ∥v′′ ∥ 22 = (Av′ ) T (Av′ ) = v′ T v′ = ∥v′ ∥ 22, (8) ′′ , y′′ ) increases monotoniwhich means distance is the same. When 𝑚 ′ < 𝑚, the function 𝛿𝑚 ′ (xsub sub ′ ′′ ′′ ′ ′ cally with 𝑚 , its maximum value being 𝛿𝑚 (xsub, ysub ) = 𝛿𝑚 (xsub, ysub ). □

## Bound sentences
