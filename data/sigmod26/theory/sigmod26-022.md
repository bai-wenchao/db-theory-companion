# sigmod26-022 — Analyzing Deviations from Monotonic Trends through Database Repair

from=tex flag=True score=13 stmts=2 proofs=4 chars=327761
kinds: {"definition": 1, "theorem": 1}
counts: {"np_hard": 6, "lower_bound": 3, "upper_bound": 6, "big_o": 6, "omega": 3, "theta": 0, "approx_ratio": 4, "whp": 0, "regret": 0, "dp": 0, "invariant": 0, "convergence": 0, "competitive": 0, "worst_case": 2, "sketch": 0, "cost_model": 0, "cardinality": 5, "learned": 0}

## Statements
**Definition .** A monotonic subset of $r$ is a subset $r'$ of $r$ such that $r' G α(A)$. A of $r$ is a monotonic subset of maximum size. Hence, $r'$ is obtained from $r$ by deleting the minimum possible number of tuples needed to satisfy $G α(A)$.

**Theorem .** Let $G α(A)$ be an AOD. ; For $α , , , , $, a can be found in polynomial time. ; For $α , $, a can be found in pseudo-polynomial time.

## Proofs
**Proof.** We begin with $α= $. We will show a reduction from the NP-complete Subset-Sum problem in the variant of natural numbers. An instance of this problem consists of a set $X= x_1, ,x_n $ of natural numbers, along with a natural number $s$; the goal is to decide whether $X$ has a subset $Y$ with $Σ_ x Y x=s$. Given $X$ and $s$, we construct the relation $r$ that consists of the following tuples over $(G,A)$: ; $(i,s)$ for $i=0, ,n-1$; ; $(n,x_j)$ for $j=1, ,n$; ; $( ,s)$ for $ =n+1, ,2n$. We claim that $(X,s)$ is a ``yes'' instance (i.e., the above $Y$ exists) if and only if $r$ has a monotonic subset (having the aggregate value $s$ for each group) that requires deleting fewer than $n$ tuples: ; If the subset $Y$ exists, then we remove from $r$ all tuples $(n,x_j)$ with $x_j X Y$, and obtain a monotonic subset. ; Conversely, if $r$ has a monotonic subset achieved by deleting fewer than $n$ tuples, then it must leave at least one tuple of each of the above three types. In particular, the tuples $(n,x_j)$ that remain in $r$ must have their $x_j$s summing up to $s$ for the result to satisfy the . In particular, it is NP-hard to find a deleting a minimum number of tuples from $r$. For $α= $, we use a similar reduction, with a slightly different construction of $r$, which now consists of the following tuples: ; $(i,0)$ for $i=0, ,n-1$; ; $(n,-s)$ and, additionally, $(n,x_j)$ for $j=1, ,n$; ; $( ,0)$ for $ =n+1, ,2n$. The same reasoning of shows that $(X,s)$ is a ``yes'' instance if and only if $r$ has a monotonic subset that requires deleting fewer than $n$ tuples. In this case, the aggregate value of each group is zero.

**Proof.** Let $ OPT $ denote the optimal solution: $ OPT [s]$ will be the size of the largest subset of $r_i$ with sum $s$. We first observe that $ OPT [s] ≥ OPT [s-v_j]+1$, since a solution with sum $s-v_j$ can be completed into a solution for $s$ by adding another $v_j$ tuple to the subset. We divide into two cases. Case 1: $ OPT [s] > OPT [s-v_j]+1$. We will now show that $v_j$ items are not used in the optimal solution for $s$. Assume by way of contradiction that $D[j,s]≠ 0$. We can remove a $v_j$ tuple from the optimal solution for $s$, and get a solution with sum $s-v_j$ with $ OPT [s]-1$ tuples. Then $ OPT [s-v_j]≥ OPT [s]-1$. However, by the assumption in case 1, this means: $ OPT [s] > OPT [s-v_j]+1 ≥ OPT [s]$ (contradiction). Case 2: $ OPT [s] = OPT [s-v_j]+1$. Meaning that the solution with sum $s$ contains one more tuple than the solution for $s-v_j$. Adding a $v_j$ tuple to the optimal solution for $s-v_j$ would yield a solution for $s$ with $D[j,s-v_j]+1$ tuples of value $v_j$. Since the algorithm searches for an optimal solution with a minimal number of instances of the largest value, we get: D[j,s]≤ D[j,s-v_j]+1 Assume by way of contradiction: $D[j,s] 0,D[j,s-v_j]+1 $. Then: 0 < D[j,s] < D[j,s-v_j]+1 Then removing a $v_j$ tuple from the optimal solution with sum $s$ would yield a solution with sum $s-v_j$, with $OPT[s]-1$ tuples in total, and with $D_j[s]-1$ instances of $v_j$. From eq:sum_knapsack_2 we get $D[j,s]-1 < D[j,s-v_j]$, in contradiction to the minimality of $D[j,s-v_j]$.

**Proof.** For each case we show a database with schema $(G,A)$, where will reach a repair by removing at least $Ω(n)$ times more tuples than the optimal solution. . Consider the following database with schema $(G,A)$, composed of the following tuples: $ (1,1),(1,3),(2,1),(2,3),(3,2) $ and also $n-5$ tuples $(3,1)$. Initially, the averages of the 3 groups are $ (r_1) = (r_2)=2$ and $ (r_3)=(2+n-5)/n-4$, meaning $1< (r_3)<2$, and the $G (A)$ is not satisfied. A possible monotonic subset is achieved by removing the two tuples $(1,3)$ and $(2,3)$, resulting in an average of $1$ for $r_1, r_2$. would not choose to remove these tuples. Removing $(1,3)$ has impact 0 on the $ MVI $, and removing $(2,3)$ has a negative impact. The only tuples with a positive impact are the $n-5$ copies of $(3,1)$, which will remove one by one. . Consider the following database with schema $(G,A)$, composed of the following tuples: $n-5$ copies of $(1,1)$, as well as $(2,2n)$, 2 copies of $(2,n/3)$ and 2 copies of $(3,n)$. Initially, the sums of the 3 groups are $ (r_1) = n-5$, $ (r_2)=8n/3$ and $ (r_3)=2n$, and the $G (A)$ is not satisfied since $ (r_2)> (r_3)$. Removing the two tuples $(2,n/3)$ will yield a monotonic subset, since $ (r_2)$ will be $2n$. In the first iteration of , tuples from $r_1$ have impact $0$, and tuples from $r_3$ will have a negative impact. Therefore the algorithm will remove a tuple from $r_2$: specifically, $(2, 2n)$ has the maximal impact of $2n/3 - (n-5-2n/3)=5+n/3$ (while the other two tuples in $r_2$ have impact $n/3$). The new sums are $n-5, 2n/3, 2n$, and the is still not satisfied. Next, will remove only tuples from $r_1$, as the others have impact $0$. Since each tuple contributes $1$ to $ (r_1)$, $n/3-5$ additional tuples will be removed. . Consider the following database with schema $(A,G)$, composed of the following tuples: $(1,1)$, $3$ copies of $(1,n)$, and $(2,1),(2,2), ,(2,n-4)$. Initially, the medians are $ (r_1) = n$, $ (r_2) 1, ,n-4 $, and the $G (A)$ is not satisfied since $ (r_1)> (r_2)$. Removing the three tuples of the form $(1,n)$ yields a monotonic subset, since $ (r_1)$ will be $1$. However, the impact of the $(1,n)$ tuples is $0$, since it does not change $ (r_1)$. Instead, will remove all $n-4$ tuples from $r_2$, starting with the lower $A$ values, since each one will increase $ (r_2)$ and have a positive impact.

**Proof.** Let $r'$ be a monotonic subset of $r$ w.r.t.~an $G α(A)$, that uses $x_1$ for group $i$. Then we have $α(r'_i[A])=x_1$ and $α(r'_j[A])≥ x_1$ for $j>i$. Since $x_1 > x_2$, $α(r'_j[A])≥ x_2$ for $j>i$. Let $r'_ >i = r'_i r'_m$. Then $H_ i-1 [x_2] r'_ >i $ is a monotonic subset for the , and $|H_ i-1 [x_2] r'_ >i | ≥ |H_ i-1 [x_1] r'_ >i | = |r'|$.

## Bound sentences
- Hence, we begin with a complexity analysis of the problem, showing that while the general case is NP-hard, several practically relevant aggregates admit polynomial-time solutions.
- However, in the case of sum and average, where the number of possible aggregation values can be exponential in the number of tuples, the resulting algorithm is only weakly polynomial, that is, it runs in polynomial time if we assume that the input represents the numbers in a unary encoding; we show  …
- This heuristic serves two key purposes: (1) providing a practical alternative when computational resources are limited, and (2) yielding an upper bound on the number of deletions required.
- This number provides an upper bound on the number of tuples needed to be removed, but it does not give any information about the minimum amount of deletion needed to fulfill the AOD; hence, it cannot support a claim as the one closing ex:so_example_cont .
- In summary, our contributions are as follows: ; We introduce the notion of an AOD to express a monotonic trend in the result of an aggregate query over a table; ; We provide a complexity analysis of for AODs, and present a general algorithmic framework that reduces the global repair problem to local …
- In sec:tuple_del_hardness , we will show that this weakening is necessary, since the problem is NP-hard if we assume a binary representation.
- It is NP-hard to find a (under the conventional binary representation of numbers).
- Pruning the DP using a heuristic algorithm To accelerate the search, we can use a lower bound on the size of the repair (i.e., an upper bound on the number of removed tuples).
- Second, it accelerates via pruning, by providing an upper bound on the number of tuples to remove in both the generic algorithm (as described in sec:dp_pruning_greedy ) and the implementation of (described in sec:opt_greedy_aggpack ), without sacrificing the optimality guarantee.
- The following proposition states that the approximation ratio of can be arbitrarily high. proposition thmGreedyRatio For $α , , $, there are classes of instances where the approximation ratio of is $Ω(n)$.
- Despite this large worst-case approximation ratio, the use of to prune the search of can still lead to a reduction in the runtime of finding a , as we show in sec:exp_optimizations .
- Due to the optimality of the algorithm, it provides a lower bound on the number of tuples to be removed to satisfy the trend in each direction.
