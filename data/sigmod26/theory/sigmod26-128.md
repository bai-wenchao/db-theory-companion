# sigmod26-128 — High-Throughput k Nearest Neighbors Search in Road Networks

flag=True score=17 stmts=17 proofs=0 chars=93918
kinds: {"definition": 3, "theorem": 6, "lemma": 6, "corollary": 2}
counts: {"np_hard": 0, "lower_bound": 0, "upper_bound": 0, "big_o": 0, "omega": 0, "theta": 0, "approx_ratio": 0, "whp": 0, "regret": 0, "dp": 0, "invariant": 0, "convergence": 0, "competitive": 0, "worst_case": 2, "sketch": 0, "cost_model": 0, "cardinality": 1, "learned": 0}

## Statements
**Definition 2.** 1 (𝑘NN Query). Given a road network 𝐺 = (𝑉 , 𝐸), an integer 𝑘, a query vertex 𝑞, and a set of candidate objects M with |M | > 𝑘, a 𝑘NN query is to find the set of 𝑞’s 𝑘 nearest neighbors in M, i.e., M𝑘 (𝑞) ⊂ M such that: (1) |M𝑘 (𝑞)| = 𝑘; and (2) ∀𝑣 ∈ M𝑘 (𝑞), 𝑣 ′ ∈ M \ M𝑘 (𝑞), dist(𝑞, 𝑣) ≤ dist(𝑞, 𝑣 ′ ). Following previous work [35], we assume that the shortest distance between every pair of vertices is unique. This assumption ensures that the result of any 𝑘NN search is also unique and thus simplifies our presentation. Nevertheless, our algorithms guarantee that the query result Proc. ACM Manag. Data, Vol. 4, No. 1 (SIGMOD), Article 42. Publication date: February 2026. High-Throughput 𝑘 Nearest Neighbors Search in Road Networks 42:5 Table 1. Frequently used notations Notation M𝑘 (𝑞) 𝑇𝐺 ≺ 𝑁 (𝑣) S(𝑣) P(𝑣) 𝐺 ⪰𝑣 M𝑘⪰ (𝑣) lsd(𝑣, 𝑢) dist(𝑣, 𝑢) cn(𝑣) Description top-𝑘 nearest neighbor set of 𝑞 cut tree of 𝐺 partial order over 𝑉 defined by the cut tree 𝑇𝐺 the tree node in 𝑇𝐺 that contains vertex 𝑣 the set of vertices 𝑢 s.t. 𝑣 ⪯ 𝑢 the set of vertices 𝑢 s.t. 𝑢 ⪯ 𝑣 the local subgraph of 𝑣 𝑣’s local 𝑘 nearest neighbors in 𝐺 ⪰𝑣 local shortest distance from 𝑣 to 𝑢 computed in 𝐺 ⪰𝑣 (global) shortest distance between 𝑣 and 𝑢 computed in 𝐺 the convex neighbors of 𝑣 Table 2. Parameters for throughput Notation 𝑇 𝑅∗ 𝜌 = 𝜆𝑚ˆ𝑢 Description duration of periods (in seconds) response requirement (ms) object update rate (per second) Values 1, 4, 8, 16, 20, 24, 32 0.2, 0.4, 0.8, 1.6, 3.2, 6.4 4, 2, 1, 0 …

**Definition 4.** 1 (Vertex Cut). Given a graph 𝐺 = (𝑉 , 𝐸), a vertex subset 𝐶 ⊂ 𝑉 is a vertex cut if removing them disconnects the graph, i.e., 𝐺 \ 𝐶 consists of at least two connected components. Proc. ACM Manag. Data, Vol. 4, No. 1 (SIGMOD), Article 42. Publication date: February 2026. 42:8 Yu Kong, Lijun Chang, Dong Wen, and Dian Ouyang N0 v5 v13 N1 N2 v8 v10 N3 v3 v4 v6 N6 v15 v9 N9 N8 N7 N5 N4 v1 v2 𝑣1 𝑣2 𝑣3 𝑣4 𝑣5 𝑣6 𝑣7 𝑣8 𝑣9 𝑣 10 𝑣 11 𝑣 12 𝑣 13 𝑣 14 𝑣 15 N10 v11 v14 v7 v12 (a) Cut tree 𝑇𝐺 G⪰v8 1 v8 1 v2 v10 1 v6 G⪰v10 v1 1 4 v7 2 3 1 v3 1 3 v12 lsd 0, 4, 2, 1, 4 0, 1, 5, 1, 2, 5 0, 2, 2, 5, 6 0, 4, 2 0 0, 1, 1, 1, 4, 5 0, 4, 2, 4, 1, 2 0, 3, 6 0, 1, 3, 1 0, 2, 3, 4 0, 1, 3, 7, 1 0, 3, 1, 3, 3, 4, 5 0, 3 0, 2, 3, 7, 4 0, 4, 8, 2 (b) Two local subgraphs M𝑘⪰ (·) 𝑣2 𝑣2 𝑣 6, 𝑣 7 𝑣 4, 𝑣 9, 𝑣 14 𝑣 9, 𝑣 4, 𝑣 7 𝑣6 𝑣7 𝑣 2, 𝑣 6, 𝑣 7 𝑣9 𝑣 6, 𝑣 7, 𝑣 2 ∅ ∅ 𝑣 13, 𝑣 7, 𝑣 2 𝑣 14 𝑣 14 (c) Labels (𝑘 = 3) Fig. 2. Our index constructed for the graph in Fig. 1 ￼ It is straightforward that, for any two vertices 𝑣 and 𝑢 that are in different connected components of 𝐺 \ 𝐶, every path between them must have at least one vertex in 𝐶. As a result, dist(𝑣, 𝑢) = min dist(𝑣, 𝑐) + dist(𝑐, 𝑢) (3) 𝑐 ∈𝐶 The cut tree constructs a hierarchical representation of vertex cuts.

**Theorem 4.1.** The size of our CtLd-index is 𝑂 ( 𝑣 ∈𝑉 |S(𝑣)|) = 𝑂 ( 𝑣 ∈𝑉 |P(𝑣)|). Proc. ACM Manag. Data, Vol. 4, No. 1 (SIGMOD), Article 42. Publication date: February 2026. 42:10 Yu Kong, Lijun Chang, Dong Wen, and Dian Ouyang Algorithm 1: CtLd-Cons(𝑔, 𝑘, M) Input: A graph 𝑔, an integer 𝑘 and a set of candidate objects M Output: A cut tree 𝑇𝑔 of 𝑔, and associated local shortest distances and local 𝑘 nearest neighbors (𝐿, 𝐶, 𝑅) ← BalancedCut(𝑔); foreach 𝑐 ∈ 𝐶 in increasing ID order do 3 Run Dijkstra’s algorithm in 𝑔, starting from 𝑐, to compute local shortest distances   𝑐, 𝑣, lsd(𝑐, 𝑣) | 𝑣 ∈ 𝑉 (𝑔) and local 𝑘 nearest neighbors M𝑘⪰ (𝑐); 4 Remove 𝑐 from 𝑔; 1 2 if |𝐿| > 2 then 𝑇𝑔[𝐿] ← CtLd-Cons(𝑔[𝐿], 𝑘, M); else 𝑇𝑔[𝐿] ← 𝐿; 7 if |𝑅| > 2 then 𝑇𝑔[𝑅 ] ← CtLd-Cons(𝑔[𝑅], 𝑘, M); 8 else 𝑇𝑔[𝑅 ] ← 𝑅; 9 Create a node for 𝐶 and set it as the parent of 𝑇𝑔[𝐿] and 𝑇𝑔[𝑅 ] in 𝑇𝑔 ; 10 return 𝑇𝑔 5 6 Index Construction Algorithm. Our index construction algorithm is a recursive algorithm, as shown in Algorithm 1. Each recursion consists of three phases. The first phase identifies a vertex cut 𝐶 of the given graph 𝑔 (Line 1). The second phase computes the local shortest distances between each 𝑐 ∈ 𝐶 and all vertices 𝑣 ∈ S(𝑐), as well as local 𝑘-nearest neighbors M𝑘⪰ (𝑐) (Lines 2–4). The third phase (Lines 5–9) conducts recursion for 𝑔[𝐿] and 𝑔[𝑅], the subgraphs of 𝑔 induced by vertex subsets 𝐿 and 𝑅. We adopt the techniques of [21] for computing the vertex cut in Line 1. The computed vertex cut is not an arbitrar …

**Theorem 4.2.** The time complexity of our index construction (i.e., CtLd-Cons(𝐺, 𝑘, M)) is 𝑂 ((𝑚 log 𝑛+ 𝑤 (𝑚 + 𝑛 log 𝑛)) log 𝑛), where 𝑤 is the largest number of vertices contained in any node of 𝑇𝐺 . Remarks. Although we adopt the techniques of [21] for computing the vertex cut in Line 1 of Algorithm 1, our cut tree is different from and is structurally more efficient than the one constructed by [21]. This is because the vertex cuts computed in [21] are global shortest distance preserving. That is, after obtaining the cut (𝐿, 𝐶, 𝑅) by BalancedCut, they will construct two subgraphs, 𝑔𝐿 for 𝐿 and 𝑔𝑅 for 𝑅, such that the shortest distance between any two vertices computed in 𝑔𝐿 (resp. 𝑔𝑅 ) is the same as the global distance computed in 𝑔. To achieve this, they need to add shortcuts to 𝑔[𝐿] and 𝑔[𝑅] to retain the global shortest distances. In contrast, we simply pass 𝑔[𝐿] and 𝑔[𝑅] to the subroutines in Algorithm 1. Our design offers two advantages. First, it eliminates the need to compute and add shortcut edges to preserve global distances in subgraphs, thereby simplifying the indexing process and improving the construction time. Second, 𝑔[𝐿] (resp. 𝑔[𝑅]) is typically sparser than 𝑔𝐿 (resp. 𝑔𝑅 ), and thus our cut tree is likely to be more compact. We will empirically evaluate these advantages of our design in Section 7. Despite these simplifications, we will show in Section 4.2 that local shortest distances and local 𝑘 nearest neighbors computed based on our cut tree are sufficient for process …

**Lemma 4.1.** For any path 𝑃 in 𝐺, there exists a vertex 𝑐 ∈ 𝑃 such that 𝑃 is contained in 𝐺 ⪰𝑐 . Specifically, let 𝑣, 𝑢 be 𝑃’s two end-points. The highest-ranked vertex in 𝑃 ∩ P(𝑣) ∩ P(𝑢) is such a vertex 𝑐. The general idea is as follows. Let 𝑐 ∗ be the highest-ranked vertex in 𝑃 ∩ P(𝑣), which is welldefined since (1) 𝑃 ∩ P(𝑣) is non-empty (because 𝑣 ∈ 𝑃 ∩ P(𝑣)) and (2) the vertices of P(𝑣) are totally ordered. One can show that 𝑃 is entirely contained within 𝐺 ⪰𝑐 ∗ . Consequently, 𝑐 ∗ is also the highest-ranked vertex in 𝑃 ∩ P(𝑢). Then, the shortest distance between any two vertices 𝑣 and 𝑢 in 𝐺 can be computed from our CtLd-index, as follows.

**Theorem 4.** 3 (Shortest Distance Computation). For any two vertices 𝑣, 𝑢 ∈ 𝑉 , their shortest distance in 𝐺 can be computed from our CtLd-index as, dist(𝑣, 𝑢) = min 𝑐 ∈P(𝑣)∩P(𝑢 ) lsd(𝑐, 𝑣) + lsd(𝑐, 𝑢) (4) where lsd(𝑐, 𝑣) and lsd(𝑐, 𝑢) are stored in our index. Furthermore, the 𝑘 nearest neighbors for any query vertex 𝑞 can be computed based on the theorem below. Proc. ACM Manag. Data, Vol. 4, No. 1 (SIGMOD), Article 42. Publication date: February 2026. 42:12 Yu Kong, Lijun Chang, Dong Wen, and Dian Ouyang

**Theorem 4.5.** The time complexity of Algorithm 2 for finding the first 𝑖 nearest neighbors is Í 𝑂 ( 𝑐 ∈P(𝑞) 𝑙𝑖𝑐 · log |P(𝑞)|). As 𝑙𝑖𝑐 ≤ 𝑖, the time complexity is also bounded by 𝑂 (|P(𝑞)| · 𝑖 · log |P(𝑞)|), which is polynomial in 𝑖. Thus,ÍAlgorithm 2 has an incremental polynomial time complexity [16]. We remark that although 𝑐 ∈P(𝑞) 𝑙𝑘𝑐 can reach |P(𝑞)| · 𝑘 and thus the time complexity of Algorithm 2 can reach 𝑂 (|P(𝑞)| · 𝑘 · log |P(𝑞)|) in the worst case, our empirical studies in Section 7 show that Algorithm 2 finds the 𝑘 nearest neighbors by examining only a small portion of C. 5 Online Index Updating In this section, we introduce techniques for efficiently updating our CtLd-index when objects are dynamically inserted or removed; object movements can be simulated as a removal followed by an insertion. Note that we consider the online updating (in contrast to batch updating) setting in this paper, i.e., the index is updated after each object insertion or deletion. Since the graph structure and edge weights remain fixed, both the cut tree and the precomputed local shortest distances remain unaffected. Consequently, only the local 𝑘 nearest neighbors, M𝑘⪰ (·), require updating. We propose algorithms to efficiently update M𝑘⪰ (·) for object deletion and insertion in Sections 5.3 and 5.4, respectively. We first describe a naive baseline in Section 5.1, and then introduce the concept of convex neighbor in Section 5.2 which plays a key role in the efficiency of our index updating. 5.1 A Naive  …

**Definition 5.** 1 (Convex Path). A path 𝑃 (𝑣, 𝑢) = (𝑣, 𝑥 1, . . . , 𝑥 𝑗 , 𝑢) is a convex path if each intermediate vertex ranks lower than both terminal vertices, i.e., 𝑣 ≺ 𝑥𝑖 and 𝑢 ≺ 𝑥𝑖 for each 1 ≤ 𝑖 ≤ 𝑗. Note that for a path 𝑃 (𝑣, 𝑢) to be a convex path, it must be contained within the local subgraph of the higher-ranked vertex between 𝑣 and 𝑢. For example, assuming 𝑣 ≺ 𝑢, any convex path between 𝑣 and 𝑢 must be contained in 𝐺 ⪰𝑣 .

**Theorem 5.** 1 (Local 𝑘NN Computation). For any vertex 𝑐 ∈ 𝑉 such that 𝑐 ∉ M, its local 𝑘 nearest neighbors are the top-𝑘 objects with the smallest distances in Ø   C𝐿 = 𝑜, lsd(𝑐, 𝑣) + lsd(𝑣, 𝑜) | 𝑜 ∈ M𝑘⪰ (𝑣) (6) 𝑣 ∈𝑐𝑛 (𝑐 ) after removing duplicate objects and preferring shorter distances. Note that, if 𝑐 ∈ M, then M𝑘⪰ (𝑐) \ {𝑐} is the top-(𝑘 − 1) objects with the smallest distances in C𝐿 . Following Theorem 5.1, we also precompute and store the convex neighbors of all vertices in 𝑉 , which constitute the fourth component of our CtLd-index. Example 5.1. Reconsider the example given at the beginning of this subsection. The convex neighbors Ð of 𝑣 8 are cn(𝑣 8 ) = {𝑣 1, 𝑣 2, 𝑣 3, 𝑣 6, 𝑣 10 }. We have M𝑘⪰ (𝑣 8 ) ⊆ 𝑣 ∈cn(𝑣8 ) M𝑘⪰ (𝑣) since M𝑘⪰ (𝑣 3 ) = {𝑣 6, 𝑣 7 }. Convex Neighbor Computation. Now, we present an algorithm to compute the convex neighbors. First, we prove an important property of convex neighbor in the following lemma.

**Lemma 5.1.** Let 𝑣 ≺ 𝑢. 𝑢 is a convex neighbor of 𝑣 if and only if either 𝑢 is neighbor of 𝑣 in 𝐺 ⪰𝑣 or there exists another vertex 𝑥 such that 𝑢 ≺ 𝑥 and 𝑥 is a convex neighbor of both 𝑣 and 𝑢.

**Lemma 5.** 1 implies that the convex neighbor relationships can be inferred incrementally through intermediate vertices of lower ranks. However, it remains nontrivial to directly utilize this property for efficiently computing convex neighbors. To achieve that, we introduce the concept of convex predecessor, the inverse of convex neighbor. Specifically, 𝑣 is a convex predecessor of 𝑢 if 𝑢 is a convex neighbor of 𝑣. Denote the set of convex predecessors of a vertex 𝑣 by cp(𝑣). Convex predecessor makes the relationship easier to be utilized, as shown in the corollary below.

**Corollary 5.1.** Let 𝑣 ≺ 𝑢. 𝑣 is a convex predecessor of 𝑢 if and only if either 𝑢 is a neighbor of 𝑣 in 𝐺 ⪰𝑣 or there exists another vertex 𝑥 such that 𝑢 ≺ 𝑥 and both 𝑣 and 𝑢 are convex predecessors of 𝑥. Based on this insight, we propose a bottom-up approach (i.e., in reverse order of ≺) to efficiently computing convex predecessors and convex neighbors. The pseudocode is shown in Algorithm 3. For each vertex 𝑢 ∈ 𝑉 , we initialize its convex neighbor as empty (Line 2) and set its initial convex Proc. ACM Manag. Data, Vol. 4, No. 1 (SIGMOD), Article 42. Publication date: February 2026. High-Throughput 𝑘 Nearest Neighbors Search in Road Networks 42:15 Algorithm 3: ConvexNeighbor-Cons Input: A graph 𝐺 = (𝑉 , 𝐸) and its cut tree 𝑇𝐺 Output: Convex neighbors cn(𝑣) for each 𝑣 ∈ 𝑉 foreach 𝑢 ∈ 𝑉 do cn(𝑢) ← ∅; 3 cp(𝑢) ← {𝑣 ∈ 𝑉 | 𝑣 ∈ nbr(𝑢) ∧ 𝑣 ≺ 𝑢}; 1 2 𝑄 ← an empty first-in-first-out queue; foreach 𝑣 ∈ 𝑉 s.t. |S(𝑣)| = 1 do Push 𝑣 into 𝑄; 6 while 𝑄 ≠ ∅ do 7 𝑥 ← pop a vertex from 𝑄; 8 foreach 𝑣, 𝑢 ∈ cp(𝑥) s.t. 𝑣 ≺ 𝑢 do cp(𝑢) ← cp(𝑢) ∪ {𝑣 }; 9 foreach 𝑣 ∈ cp(𝑥) do cn(𝑣) ← cn(𝑣) ∪ {𝑥 }; 10 𝑥 ′ ← the immediate predecessor of 𝑥 according to ≺; 11 if 𝑥 ′ ∉ 𝑄 then Push 𝑥 ′ into 𝑄; 4 5 predecessors cp(𝑢) to be the neighbors 𝑣 ∈ nbr(𝑢) satisfying 𝑣 ≺ 𝑢 (Line 3). We also initialize a FIFO queue 𝑄 (Line 4) to process vertices in a bottom-up manner. All vertices without lower-ranked successors (i.e., |S(𝑣)| = 1) are added to 𝑄 (Line 5) as starting points. In each iteration, we pop a vertex 𝑥 from 𝑄 (Line 7) and p …

**Lemma 5.2.** Algorithm 3 correctly computes the convex neighbors for all vertex of 𝑉 in 𝑂 𝑣 ∈𝑉 |cp(𝑣)| 2 total time. 5.3 Handling Object Deletion When an object 𝑜 is removed from M, we may need to update {M𝑘⪰ (𝑐)}𝑐 ∈𝑉 stored in our index. For a vertex 𝑐 ∈ 𝑉 , if 𝑜 ∉ M𝑘⪰ (𝑐), it is obvious that M𝑘⪰ (𝑐) does not need to be updated. If 𝑜 ∈ M𝑘⪰ (𝑐), then we need to try to replace 𝑜 in M𝑘⪰ (𝑐) with another object of M ⪰ (𝑐) \ M𝑘⪰ (𝑐). However, scanning all objects of M ⪰ (𝑐) \ M𝑘⪰ (𝑐) may result in a substantial overhead for index updating. By leveraging Theorem 5.1, we only need to consider objects in {M𝑘⪰ (𝑣)} 𝑣 ∈cn(𝑐 ) . Moreover, by the same observation as utilized in designing our query processing Algorithm in Section 4.2, for each 𝑣 ∈ cn(𝑐), we only need to consider the first object of M𝑘⪰ (𝑣) (as ranked by their distances) that is not in M𝑘⪰ (𝑐). As updating M𝑘⪰ (𝑐) requires {M𝑘⪰ (𝑣)} 𝑣 ∈cn(𝑐 ) to be up-to-date, the deletion update needs to be applied in a bottom-up manner regarding ≺. Furthermore, rather than processing all potential vertices 𝑐 ∈ P(𝑜) whose local 𝑘 nearest neighbors may need to be updated, we restrict our attention to only those satisfying 𝑜 ∈ M𝑘⪰ (𝑐). To support this, we maintain a doubly linked list that connects all occurrences of the same object 𝑜 across different M𝑘⪰ (·) sets. This allows us to efficiently process only the affected vertices in a bottom-up manner, significantly reducing unnecessary computations. Our index updating algorithm for object deletion is p …

**Lemma 5.3.** Given the CtLd-index and a deleted object 𝑜, the time complexity of Algorithm 4 is 𝑂 (|P(𝑜)| · 𝑘 · ℓ𝑚𝑎𝑥 ), where ℓmax denotes the maximum number of convex neighbors among the vertices of 𝐺. Proc. ACM Manag. Data, Vol. 4, No. 1 (SIGMOD), Article 42. Publication date: February 2026. 42:16 Yu Kong, Lijun Chang, Dong Wen, and Dian Ouyang Algorithm 4: Object-Deletion Input: Our CtLd-index and a deleted object 𝑜 Output: The updated CtLd-index foreach vertex 𝑐 ∈ P(𝑜) s.t. 𝑜 ∈ M𝑘⪰ (𝑐) in bottom-up order do Remove 𝑜 from M𝑘⪰ (𝑐); 3 𝐶 ← ∅; 4 foreach 𝑣 ∈ cn(𝑐) do 5 𝑜 ′ ← the first object in M𝑘⪰ (𝑣) that is not in M𝑘⪰ (𝑐); 6 if 𝑜 ′ exists then 𝐶 ← 𝐶 ∪ {𝑜 ′ }; 1 2 9 if 𝐶 ≠ ∅ then 𝑜 ′ ← arg min𝑥 ∈𝐶 lsd(𝑐, 𝑥); Append 𝑜 ′ to the end of M𝑘⪰ (𝑐); 5.4 Handling Object Insertion 7 8 Updating our CtLd-index for object insertion is much easier than for object deletion. The main observations are as follows. When an object 𝑜 is inserted, only those vertices 𝑐 ∈ P(𝑜) may update their M𝑘⪰ (·) sets. Moreover, M𝑘⪰ (𝑐) is updated only if (1) |M𝑘⪰ (𝑐)| < 𝑘; or (2) |M𝑘⪰ (𝑐)| = 𝑘 and lsd(𝑐, 𝑜) < lsd(𝑐, 𝑜𝑘 ), where 𝑜𝑘 is the object with the largest local shortest distance in M𝑘⪰ (𝑐). Following these ideas, our index updating algorithm for object insertion is described in Algorithm 5, which is self-explanatory. Algorithm 5: Object-Insertion Input: Our CtLd-index and an inserted object 𝑜 Output: The updated CtLd-index foreach vertex 𝑐 ∈ P(𝑜) in bottom-up order do if |M𝑘⪰ (𝑐)| < 𝑘 then Insert 𝑜 into M𝑘⪰ (𝑐); 3 e …

**Lemma 5.4.** Given the CtLd-index and an inserted object 𝑜, the time complexity of Algorithm 5 is 𝑂 (|P(𝑜)| · 𝑘). We may also leverage Theorem 5.1 to stop the updating early. That is, for a vertex 𝑐, if none of the vertices of cn(𝑐) updated their M𝑘⪰ (·) set, then M𝑘⪰ (𝑐) remains intact. Thus, we can use a queue to track the vertices whose M𝑘⪰ (·) may change, and only process vertices of the queue; the queue is initialized with 𝑜 and updated by using convex predecessors. However, our empirical studies show that Algorithm 5 performs better than the early stop version, probably due to better cache efficiency. Proc. ACM Manag. Data, Vol. 4, No. 1 (SIGMOD), Article 42. Publication date: February 2026. High-Throughput 𝑘 Nearest Neighbors Search in Road Networks 6 42:17 Faster Index Construction With the convex neighbors computed and the property proved in Theorem 5.1, we propose a faster index construction algorithm than Algorithm 1 in this section, by utilizing the convex neighbors. We observe that convex neighbors are independent to the candidate object set M and the integer 𝑘. That is, convex neighbors remain the same if we change M or 𝑘. As a result, we could set M to be 𝑉 and 𝑘 to be 𝑛 in Theorem 5.1 to get the following corollary.

**Corollary 6.1.** For any vertex 𝑐 ∈ 𝑉 , the local shortest distances {lsd(𝑐, 𝑢) | 𝑢 ∈ S(𝑐)} can be computed from Ø   𝑢, lsd(𝑐, 𝑣) + lsd(𝑣, 𝑢) | 𝑢 ∈ S(𝑣) (7) 𝑣 ∈cn(𝑐 ) by removing duplicate vertices and preferring shorter distances. Thus, we can compute {lsd(𝑐, 𝑢) | 𝑢 ∈ S(𝑐)} for vertices 𝑐 ∈ 𝑉 in a bottom up manner regarding ≺. Then, when computing {lsd(𝑐, 𝑢) | 𝑢 ∈ S(𝑐)} by Equation (7), the distance lsd(𝑣, 𝑢) is already known. However, lsd(𝑐, 𝑣) used in Equation (7) cannot be computed in this way. Fortunately, we can compute such lsd(𝑐, 𝑣) during the process of obtaining convex neighbors by following the proof of Lemma 5.1. Algorithm 6: Index-Cons+ Input: A graph 𝐺 = (𝑉 , 𝐸), an integer 𝑘 and candidate objects M Output: A cut tree 𝑇𝐺 of 𝐺, convex neighbors, and associated local shortest distances and local 𝑘 nearest neighbors 1 Run Algorithm 1 (without Lines 2–4) to build the cut tree 𝑇𝐺 ; foreach 𝑣 ∈ 𝑉 and 𝑢 ∈ S(𝑣) do lsd(𝑣, 𝑢) ← ∞; 3 foreach 𝑣 ∈ 𝑉 do lsd(𝑣, 𝑣) ← 0; 4 Run Lines 1–3 of Algorithm 3; 5 foreach 𝑢 ∈ 𝑉 and 𝑣 ∈ cp(𝑢) do lsd(𝑣, 𝑢) ← 𝜙 (𝑣, 𝑢); 6 Run Lines 4–11 of Algorithm 3, and at Line 8 of Algorithm 3 also execute lsd(𝑣, 𝑢) ← min{lsd(𝑣, 𝑢), lsd(𝑣, 𝑥) + lsd(𝑢, 𝑥)}; 7 foreach 𝑐 ∈ 𝑉 in bottom up manner regarding ≺ do 8 foreach 𝑣 ∈ cn(𝑐) and 𝑢 ∈ S(𝑣) do 9 lsd(𝑐, 𝑢) ← min{lsd(𝑐, 𝑢), lsd(𝑐, 𝑣) + lsd(𝑣, 𝑢)} 2 10 Compute M𝑘⪰ (𝑐) by using a similar algorithm as Algorithm 2 but leveraging Theorem 5.1; The pseudocode of our faster index construction algorithm is shown in Algorithm 6. Line …

**Theorem 6.1.** The time complexity of Algorithm 6 is 𝑂 𝑚 log 𝑛 · (log 𝑛 + 𝑤) + 𝑣 ∈𝑉 |cp(𝑣)| ·   (|cp(𝑣)| + |S(𝑣)|) + |cn(𝑣)| · 𝑘 · log |cn(𝑣)| . 7 Experiments We conduct extensive experiments to evaluate our proposed methods against the state-of-the-art methods in this section. Datasets. We evaluate the approaches on 13 real-world road networks, including 10 undirected and 3 directed graphs. Table 3 summarizes the details about these datasets. The first 10 datasets are available from DIMACS1 , and others are sourced from SpatialDataset2 . Most of our experiments focus on the 10 undirected graphs, while the 3 directed graphs are used in Exp-13 and 14. In each road network, vertices represent intersections between roads, and edges correspond to roads or road segments. Each edge has an associated weight, representing the distance between endpoints. DIMACS also provides the vertex coordinates required by GLAD. Environment. All algorithms are implemented in C++ and compiled with g++13.3.0 using the -O3 flag. Experiments on the CTR and USA datasets are conducted on a Linux machine with dual Intel(R) Xeon(R) Gold 6342 2.8GHz CPUs and 200GB of RAM, while the remaining experiments are performed on a Linux machine with an Intel(R) Xeon(R) W5-2445 CPU and 64GB of RAM. Baseline Methods. We compare our algorithms with three state-of-the-art solutions GLAD [24], TEN (TEN-index) [36] and KNN (KNN-index) [41] that were discussed in Section 3. Their code was kindly provided by their authors. For GLAD, we  …

## Proofs
## Bound sentences
