# sigmod26-307 — Efficient Exact Resistance Distance Computation on Small-Treewidth Graphs: A Labelling Approach

from=tex flag=True score=64 stmts=23 proofs=20 chars=149997
kinds: {"definition": 3, "theorem": 1, "lemma": 19}
counts: {"np_hard": 0, "lower_bound": 0, "upper_bound": 4, "big_o": 54, "omega": 0, "theta": 0, "approx_ratio": 2, "whp": 0, "regret": 0, "dp": 0, "invariant": 0, "convergence": 0, "competitive": 0, "worst_case": 1, "sketch": 4, "cost_model": 0, "cardinality": 0, "learned": 0}

## Statements
**Lemma .** $ S _ | U _2 U _1| = [ L _ U _1 U _1 ^ -1 & 0 \\ 0 & 0 ]$. We will prove this by mathematical induction on the number of eliminated nodes. Base case: When no nodes have been eliminated ($i=0$), we have $ S _0 = L ^ -1 _ U _2 U _2 $. Inductive hypothesis: Assume that after eliminating $k$ nodes, the resulting matrix $ S _k$ has the form where all rows and columns corresponding to the eliminated nodes are zero, and the submatrix corresponding to the remaining nodes correctly represents their Schur complement. Inductive step: Consider the elimination of node $v_ k+1 $. Let's partition the matrix $ S _k$ as $ S _k = A & b \\ b^T & c $, where $c = S _k[v_ k+1 ,v_ k+1 ]$ is a scalar, $b = S _k[:,v_ k+1 ]$ excluding the element $c$, and $A$ is the remaining submatrix. The elimination step gives: S _ k+1 = S _k - S _k[:,v_ k+1 ] S _k[:,v_ k+1 ]^T S _k[v_ k+1 ,v_ k+1 ] = A - bb^T c & 0 \\ 0 & 0 . This is precisely the Schur complement operation. According to the block matrix inversion formula, if we have a matrix $M = A & B \\ C & D $ and its inverse $M^ -1 = E & F \\ G & H $, then $E = (A - BD^ -1 C)^ -1 $, which is the inverse of the Schur complement of $D$ in $M$. In our case, we're performing the elimination in the inverse matrix $ L ^ -1 _ U _2 U _2 $, and each elimination step corresponds to computing the Schur complement with respect to one node. After eliminating all nodes in $ U _2 U _1$, by Lemma~ [ref] , the remaining submatrix corresponding to $ U _1$ is exactly $ L _ U _1 …

**Lemma Non-zero structure of resistance distance labelling.** The resistance distance labelling $ S [v,u]$ has the following non-zero structures: (i) For each node $v$, all nodes $u$ in the subtree of $v$ are non-zero; (ii) For each node $u$, all nodes $v$ in the path from $u$ to the root node are non-zero. The non-zero structure of the resistance distance labelling can be derived from the vertex hierarchy property of the tree decomposition (Lemma~ [ref] ) and the cut property of resistance distance (Lemma~ [ref] ). For property (i), consider a node $v$ and any node $u$ in its subtree. When $v$ is eliminated during the process, all nodes in its subtree (including $u$) are still present in the remaining graph. According to Definition~ [ref] , $ S [v,u]$ represents the $u$-th element of the $v$-th column of $ L ^ -1 _ U U $, where $ U $ is the set of remaining nodes after $v$ is eliminated. Since $u U $, the corresponding entry in the inverse Laplacian is non-zero due to the connectivity between $v$ and nodes in its subtree. All other entries in the resistance distance labelling are zero because they correspond to pairs of nodes that are separated by vertex cut formed by ancestors of $v$ in $ T _ min $, as established by the tree decomposition structure and the cut property of resistance distance. For property (ii), it essentially provides the reverse perspective of property (i). Since $ S [v,u]$ is non-zero for all $u$ in the subtree of $v$, if $v$ lies on the path from $u$ to the root, we can conclude that $ S [v,u]$ is non-zero for all …

**Lemma Time Complexity Analysis.** Algorithm~ [ref] has a time complexity of $O(n· d_ max · h_ G ^2)$. We analyze the time complexity by examining each component of the algorithm: First, initialization and DFS order computation (lines 1-2) require $O(n)$ time. The main outer loop (line 3) executes exactly $n$ iterations, processing each node $v_i$ sequentially. For each node $v_i$, the inner loop (line 5) iterates through its already processed neighbors, bounded by $| N (v_i) U | ≤ d_ v_i ≤ d_ max $. Within this loop, the while loop (lines 7-12) traverses from each processed neighbor $v_k$ up to $v_i$ in the tree, updating the labelling for all nodes in the subtree rooted at $v_k$. This operation takes at most $O(| T _ min . (v_k)|)$ steps for each $v_k$. Since Lemma~ [ref] establishes that $v_i$ is an ancestor of each $v_k$, we can bound the total work: for each node $v_i$, the combined size of all subtrees rooted at its processed neighbors cannot exceed $v_i$'s own subtree size multiplied by $d_ max $. This gives us an upper bound of $O(d_ max · n · h_ G ^2)$ operations across all nodes, since $Σ_ i=1 ^n | T _ min . (v_i)| = O(n · h_ G )$. The final update operations (lines 13-16) require $O(| T _ min . (v_i)|)$ time for each node, summing to $O(n · h_ G )$ across all nodes. Combining these analyses, we conclude that the overall time complexity of Algorithm~ [ref] is $O(n · d_ max · h_ G ^2)$.

**Lemma .** Let $ U _1 U _2 V $ be two subsets of the nodes of $ G $, then $ L ^ -1 _ U _1 U _1 $ is the Schur complement of $ L ^ -1 _ U _2 U _2 $ with respect to the node set $ U _1$. According to the block matrix decomposition formula, Let $S=A-BD^ -1 C$ be the Schur complement of $D$ with respect to $A$, we have: A & B \\ C & D ^ -1 = S^ -1 & -S^ -1 BD^ -1 \\ -D^ -1 CS^ -1 & D^ -1 +D^ -1 CS^ -1 BD^ -1 . We can prove this lemma using block matrix decomposition. First, let's partition $ L _ U _2 U _2 $ according to $ U _1$ and $ U _2 U _1$: L _ U _2 U _2 = L _ U _1 U _1 & L _ U _1( U _2 U _1) \\ L _ ( U _2 U _1) U _1 & L _ ( U _2 U _1)( U _2 U _1) . Using the formula for the inverse of a block matrix, we have: ( L _ U _2 U _2 ^ -1 )^ -1 = A & B \\ C & D ^ -1 = L _ U _1 U _1 & L _ U _1( U _2 U _1) \\ L _ ( U _2 U _1) U _1 & L _ ( U _2 U _1)( U _2 U _1) . The top-left block is precisely the inverse of the Schur complement of $D$ with respect to $A$. Therefore, we have: S= L _ U _1 U _1 ^ -1 = & ( L _ U _2 U _2 ^ -1 )_ U _1 U _1 - ( L _ U _2 U _2 ^ -1 )_ U _1( U _2 U _1) \\ & ( L _ U _2 U _2 ^ -1 )_ ( U _2 U _1)( U _2 U _1) ^ -1 ( L _ U _2 U _2 ^ -1 )_ ( U _2 U _1) U _1 . This is exactly the Schur complement of $ L _ U _2 U _2 ^ -1 $ with respect to the node set $ U _1$, which completes the proof.

**Lemma .** Define $ U _i$ as the set of nodes that remain uneliminated at the point when node $v_i$ is being eliminated. Suppose that $ S [:,v_k]$ is the $v_k$-th column of $ S _i$ at the moment of $v_i$'s elimination, then we have: $ [ L _ U _ i U _ i ^ -1 & 0 \\ 0 & 0 ]=Σ_ k=1 ^i S [:,v_k] S [:,v_k]^T S [v_k,v_k] $. Specifically, we have: $ L _ U _ 2 U _ 2 ^ -1 - [ L _ U _ 1 U _ 1 ^ -1 & 0 \\ 0 & 0 ]=Σ_ k=| U _1|+1 ^ | U _2| S [:,v_k] S [:,v_k]^T S [v_k,v_k] $. The proof follows directly from the Gaussian elimination process. When we eliminate a node $v_i$, we perform the operation $ S _ i = S _ i-1 - S _ i-1 [:,v_i] S _ i-1 [:,v_i]^T S _ i-1 [v_i,v_i] $. This means we're subtracting a rank-1 matrix from $ S _ i-1 $. Each elimination step removes exactly one rank-1 matrix of the form $ S [:,v_k] S [:,v_k]^T S [v_k,v_k] $. Since we start with $ L _ U _2 U _2 ^ -1 $ and end with $ [ L _ U _ 1 U _ 1 ^ -1 & 0 \\ 0 & 0 ]$ after eliminating all nodes in $ U _2 U _1$, the difference between these matrices must be the sum of all the rank-1 matrices we subtracted during elimination. Therefore, $ L _ U _ 2 U _ 2 ^ -1 - [ L _ U _ 1 U _ 1 ^ -1 & 0 \\ 0 & 0 ]=Σ_ k=| U _1|+1 ^ | U _2| S [:,v_k] S [:,v_k]^T S [v_k,v_k] $.

**Lemma Dependence property of resistance distance.** In $ T _ min $ obtained from the heuristic tree decomposition, the resistance distance $r(s,t)$ depends only on the labels stored along the paths from nodes $s$ and $t$ to the root of $ T _ min $. Specifically, r(s,t) &= Σ_ v P _ s (s,t) ( S [v,s])^2 S [v,v] +Σ_ v P _ t (s,t) ( S [v,t])^2 S [v,v] \\ &+Σ_ v P _ (s,t) root ( S [v,s]- S [v,t])^2 S [v,v] . According to Lemma~ [ref] , we have $ L _v^ -1 = Σ_ v S [:,v] S [:,v]^T S [v,v] $. Substituting this into the resistance distance formula, we obtain: r(s,t) &= ( e _s- e _t)^T (Σ_ v S [:,v] S [:,v]^T S [v,v] )( e _s- e _t) \\ &= Σ_ v ( e _s- e _t)^T S [:,v] S [:,v]^T( e _s- e _t) S [v,v] \\ &= Σ_ v ( S [v,s]- S [v,t])^2 S [v,v] . Based on the sparsity structure of the resistance distance labelling and the properties of the tree decomposition, we can partition this sum into three parts: (i) For $v P _ s (s,t) $, $ S [v,t] = 0$, so $ ( S [v,s]- S [v,t])^2 S [v,v] = ( S [v,s])^2 S [v,v] $. (ii) For $v P _ t (s,t) $, $ S [v,s] = 0$, so $ ( S [v,s]- S [v,t])^2 S [v,v] = ( S [v,t])^2 S [v,v] $. (iii) For $v P _ (s,t) root $, both $ S [v,s]$ and $ S [v,t]$ may be nonzero. Therefore, the sum of the three parts is equal to the resistance distance $r(s,t)$.

**Lemma .** $ S [u]. $ contains exactly $| T _ min . [u]|$ elements, where $ T _ min . [u]$ denotes the subtree rooted at $u$ in $ T _ min $. $ S [v,u]$ can be visited via $ S [u]. [ T _ min . [u] - T _ min . [v]]$. This result follows from the properties of traversal on trees. During a traversal, all nodes in the subtree rooted at any node $u$ are visited consecutively before the traversal backtracks to nodes outside this subtree. When performing a traversal on $ T _ min $, each node $v$ is assigned a position $ T _ min . [v]$ in the traversal sequence. For any node $v$ in the subtree of $u$, the values $ T _ min . [v]$ form a contiguous range starting from $ T _ min . [u]$. $ T _ min . [u] - T _ min . [v]$ indicates the relative position of node $u$ within the subtree rooted at $v$, and is unique for each node $v$ in this subtree. In our index structure, $ S [v]. $ stores the resistance distance labelling for all nodes in the subtree of $u$, arranged according to their relative positions in the ordering. Therefore, $ S [v]. [ T _ min . [u] - T _ min . [v]]$ directly retrieves the value of $ S [v,u]$ for any node $u$ in the subtree of $v$.

**Lemma Time Complexity of Single-Pair Queries.** Algorithm~ [ref] has a time complexity of $| P _ s root |+| P _ t root |$, where $ P _ s root $ and $ P _ t root $ are the paths from $s$ and $t$ to the root in the tree decomposition, respectively. This complexity can further be bounded by $O(h_ G )$. The time complexity of Algorithm~ [ref] is determined by the number of nodes visited during the three traversal phases. In the first phase, we traverse from node $s$ to $ (s,t)$, which requires $| P _ s (s,t) |$ steps. Similarly, the second phase traverses from $t$ to $ (s,t)$, requiring $| P _ t (s,t) |$ steps. The final phase traverses from $ (s,t)$ to the root, taking $| P _ (s,t) root |$ steps. Since each step performs only constant-time operations (arithmetic calculations using precomputed labels), the total time complexity is $| P _ s root | + | P _ t root |$. Since the length of any path from a node to the root is bounded by the height of the tree $h_ G $, the overall time complexity is $O(h_ G )$.

**Lemma Time Complexity of Single-Source Queries.** Algorithm~ [ref] has a time complexity of $n+Σ_ u P _ s root | (u)|$, where $ P _ s root $ is the path from $s$ to the root and $| (u)|$ is the size of the subtree rooted at $u$ in the tree decomposition. This complexity can further be bounded by $O(n· h_ G )$. The time complexity of Algorithm~ [ref] can be derived by analyzing the algorithm's operations: First, the algorithm traverses the path from node $s$ to the root, which has length at most $h_ G $. At each node $w$ along this path, it updates values for all nodes in $ (w)$, requiring $| (w)|$ operations. Thus, the total cost for these updates is $Σ_ u P _ s root | (u)|$. Additionally, the final loop (Lines 8-9) computes resistance distances for all $n$ nodes, contributing an $O(n)$ term. In the worst case, when the tree is highly unbalanced, each subtree could contain up to $O(n)$ nodes, and the path length could be $O(h_ G )$, resulting in an upper bound of $O(n · h_ G )$ for the time complexity.

**Lemma .** For each node $w N (v_i) U $, $v_i$ must be the parent of $w$ in $ T _ min $. Consider any node $w N (v_i) U $. By the algorithm's processing order, $w$ has been processed before $v_i$ since $w U $ when we process $v_i$. In a tree decomposition, for any edge $(u,v)$ in the original graph, there must exist at least one tree node containing both $u$ and $v$. Since $w$ and $v_i$ are neighbors in $ G $, they must share at least one tree node in $ T _ min $. Given that we process nodes according to the reverse order, when we process $v_i$, all nodes in its subtree (including $w$) have already been processed. The tree structure ensures that $v_i$ is the parent of $w$ in $ T _ min $, as this is the only arrangement that maintains the required connectivity property while preserving the properties of the tree decomposition. Therefore, for each node $w N (v_i) U $, $v_i$ must be the parent of $w$ in $ T _ min $.

**Lemma Correctness of Algorithm~\ref{algo:index-building.** Algorithm~ [ref] computes the resistance distance labelling of the graph $ G $. The correctness follows from the rank-1 update formula presented in Lemma~ [ref] . For each node $v_i$, the algorithm implements this formula by: (i) Computing $ L _ U _i U _i ^ -1 1 _ N (v_ i+1 ) U _i $ through the traversal and update process (Lines 5-12); (ii) Computing the denominator $d_ v_i - 1 ^T_ N (v_ i+1 ) U _i L _ U _i U _i ^ -1 1 _ N (v_ i+1 ) U _i $ (Line 13); (iii) Normalizing by this denominator (Lines 14-16). Line 15 specifically stores the diagonal elements of $ L _v^ -1 $ to support single-source queries. According to Lemma~ [ref] , the while loop will terminate as $v_i$ is guaranteed to be an ancestor of each $w N (v_i) U $ in the tree. By processing nodes according to the order of the tree decomposition, the algorithm correctly builds the complete resistance distance labelling.

**Lemma .** Let $s,t,v V $. If $v$ is a cut vertex that the removal of $v$ splits $s$ and $t$ into different connected components. The resistance distance $r(s,t)$ satisfies: $r(s,t)=r(s,v)+r(v,t)$. According to the resistance computation formula, we have: r(s,t)&=( e _s- e _t)^T L _v^ -1 ( e _s- e _t)\\ &= e _s^T L _v^ -1 e _s+ e _t^T L _v^ -1 e _t-2 e _s^T L _v^ -1 e _t\\ &= e _s^T L _v^ -1 e _s+ e _t^T L _v^ -1 e _t\\ &=r(s,v)+r(v,t). Here, the third equality holds because $ e _s^T L _v^ -1 e _t=0$. This is because $ e _s^T L _v^ -1 e _t= _v[s,t] d_t $ [cite] , where $ _v[s,t]$ is the expected number of passes to $t$ in a random walk starts from $s$ and terminates when it hits $v$. Since $v$ is a vertex cut of $s$ and $t$, it is impossible for a random walk from $s$ to pass $t$ before it hits $v$. Thus, $ _v[s,t]=0$. The Lemma is established.

**Lemma .** Suppose $ U _ i+1 = U _i \ v_ i+1 \ $, the inverse Laplacian submatrix $ L _ U _ i+1 U _ i+1 ^ -1 $ and $ L _ U _i U _i ^ -1 $ satisfy: e _ v_ i+1 L _ U _ i+1 U _ i+1 ^ -1 e _ v_ i+1 = 1 d_ v_ i+1 - 1 ^T_ N (v_ i+1 ) U _i L _ U _i U _i ^ -1 1 _ N (v_ i+1 ) U _i , L _ U _ i+1 U _ i+1 ^ -1 e _ v_ i+1 = L _ U _i U _i ^ -1 1 _ N (v_ i+1 ) U _i d_ v_ i+1 - 1 ^T_ N (v_ i+1 ) U _i L _ U _i U _i ^ -1 1 _ N (v_ i+1 ) U _i . According to the block matrix inverse formula, we have: [ A & B \\ C & D ]^ -1 = [ A^ -1 +A^ -1 BS^ -1 CA^ -1 & -A^ -1 BS^ -1 \\ -S^ -1 CA^ -1 & S^ -1 ] where $S = D - CA^ -1 B$ is the Schur complement. Applying this formula to our case with $A= L _ U _i U _i $ and $B=- 1 _ N (v_ i+1 ) U _i $ yields the desired result.

**Lemma .** The label size of is $O(n· h_ G )$. For each node $u$ in the graph, we need to store: (i) The parent pointer in $ T _ min $, which requires $O(1)$ space per node; (ii) The ordering information, which also requires $O(1)$ space per node; (iii) The resistance distance labelling $ S [u]. $, which stores values for all nodes in the path from $u$ to the root of $ T _ min $. Since the height of $ T _ min $ is at most $h_ G $, the path from any node to the root contains at most $h_ G $ nodes. Therefore, the size of $ S [u]. $ is bounded by $O(h_ G )$ for each node $u$. With $n$ nodes in total, the overall space complexity is $O(n) + O(n) + O(n · h_ G ) = O(n · h_ G )$.

**Definition .** (Tree decomposition) A tree decomposition of a graph $ G = ( V , E )$ consists of a set of subsets (called bags) $ X _ G = X _1, X _2, , X _ | X _ G | $ of the node set $ V $, and a tree $ T _ X _ G $ with node set $ X _ G $, satisfying the following three properties: (i) Every node $v V $ appears in at least one bag, i.e., $ v V , X _i X _ G $ such that $v X _i$; (ii) For every edge $(u, v) E $, there exists a bag $ X _i X _ G $ such that $u, v X _i$; (iii) For every node $v V $, the bags containing $v$ form a connected subtree in $ T _ X _ G $.

**Theorem .** [cite] Let $ p _ u $ be the $u$-th row of the matrix $ L _ U _i U _i ^ -1 L _ U _i V _i $ for $u U $. The resistance distance can be formulated as: ; For $u_1,u_2 U _i$, we have r(u_1,u_2)&=( e _ u_1 - e _ u_2 )^T( L _ U _i U _i ^ -1 )( e _ u_1 - e _ u_2 )\\ & +( p _ u_1 - p _ u_2 )^T( L / V _i)^ ( p _ u_1 - p _ u_2 ); ; For $u U _i, v V _i$, we have r(u,v)&= e _ u ^T L _ U _i U _i ^ -1 e _ u +( p _ u - e _ v )^T( L / V _i)^ ( p _ u - e _ v ); ; For $v_1,v_2 V _i$, we have r(v_1,v_2)&=( e _ v_1 - e _ v_2 )^T( L / V _i)^ ( e _ v_1 - e _ v_2 ).

**Lemma .** Let $ U = V V _ cut $, we have: r(s,t)-r(s, _1)-r(t, _2)=( e _ s - e _ t )^T( L _v^ -1 - [ L _ U U ^ -1 & 0 \\ 0 & 0 ])( e _ s - e _ t ). According to the definition of resistance distance, we can obtain that $r(s,t)=( e _ s - e _ t )^T L _v^ -1 ( e _ s - e _ t )$, $r(s, _1)= e _ s ^T L _ V _1 V _1 ^ -1 e _ s $, $r(t, _2)= e _ t ^T L _ V _2 V _2 ^ -1 e _ t $. Moreover, $ L _ U U ^ -1 $ has the block structure: $ [ L _ V _1 V _1 ^ -1 & & \\ & L _ V _2 V _2 ^ -1 & \\ & & … ]$, which establishes the lemma.

**Lemma .** The eletrical flow, when a unit current comes in through node $s$ and comes out through $t$, can be represented as: f = L ^ ( e _s- e _t)= [ L _v^ -1 & 0 \\ 0 & 0 ]( e _s- e _t), where the eletrical flow along edge $(e_1,e_2)$ is $ f _ e_1 - f _ e_2 $. According to [cite] , $ [ L _v^ -1 & 0 \\ 0 & 0 ]$ is a g-inverse of $ L $. The lemma can be established according to the property of g-inverse [cite] and given that $ e _s- e _t$ is orthogonal to $ 1 $.

**Lemma Vertex hierarchy property of tree decomposition \cite{LiJunChang2012exact.** For a tree decomposition $ T _ min $ obtained using the heuristic, we can derive: For any bag $ X _u$ in $ T _ min $, all nodes in $ X _u$ except $u$ itself are ancestors of $u$ in $ T _ min $. Consequently, for any two nodes $s$ and $t$, their lowest common ancestor ( ) and its ancestor nodes in $ T _ min $ form a vertex cut that partitions the graph into distinct connected components containing $s$ and $t$, respectively.

**Definition Resistance distance labelling.** Given a graph $ G $ and a tree decomposition $ T _ min $, suppose that we apply Gaussian elimination following the reverse ordering of the nodes processed in the heuristic tree decomposition. The resistance distance labelling can be represented as $ S [v,u]$, which stores the $u$-th element of the $v$-th column of $ L ^ -1 _ U U $, $ U $ is the remaining set of nodes when $v$ is eliminated.

**Definition Contraction graph.** Given a graph $ G $ and a node set $ V _1 V $, a contraction graph $ G _ V _1 $ is defined as the graph obtained by contracting $ V _1$ into a single node, such that all edges from nodes in $ V _1$ to nodes outside $ V _1$ are redirected to this new node.

**Lemma .** Let $ U = V V _ cut $, $ p _s$ and $ p _t$ be the $s$-th and the $t$-th row of the matrix $- L _ U U ^ -1 L _ U V _ cut $. Then, we have: $ r(s,t) = r_ G _ V _1 (s, _1)+r_ G _ V _2 (t, _2)+( p _ s - p _ t )^T( L / V _ cut )^ ( p _ s - p _ t )$.

**Lemma Cut property of resistance distance.** Let $ S [v_i,s]$ be the $s$-th element of $ L _ U _i U _i ^ -1 $. We have: r(s,t)=r_ G _ V _1 (s, _1)+r_ G _ V _2 (t, _2)+Σ_ v_i V _ cut ( S [v_i,s]- S [v_i,t])^2 S [v_i,v_i] .

## Proofs
**Proof.** According to the resistance computation formula, we have: r(s,t)&=( e _s- e _t)^T L _v^ -1 ( e _s- e _t)\\ &= e _s^T L _v^ -1 e _s+ e _t^T L _v^ -1 e _t-2 e _s^T L _v^ -1 e _t\\ &= e _s^T L _v^ -1 e _s+ e _t^T L _v^ -1 e _t\\ &=r(s,v)+r(v,t). Here, the third equality holds because $ e _s^T L _v^ -1 e _t=0$. This is because $ e _s^T L _v^ -1 e _t= _v[s,t] d_t $ [cite] , where $ _v[s,t]$ is the expected number of passes to $t$ in a random walk starts from $s$ and terminates when it hits $v$. Since $v$ is a vertex cut of $s$ and $t$, it is impossible for a random walk from $s$ to pass $t$ before it hits $v$. Thus, $ _v[s,t]=0$. The Lemma is established.

**Proof.** According to the resistance distance formula in Theorem~ [ref] . We have: r(s,t) &= e _ s ^T L _ U U ^ -1 e _ s + e _ t ^T L _ U U ^ -1 e _ t -2 e _ s ^T L _ U U ^ -1 e _ t \\ & +( p _ s - p _ t )^T( L / V _ cut )^ ( p _ s - p _ t )\\ &= e _ s ^T L _ U U ^ -1 e _ s + e _ t ^T L _ U U ^ -1 e _ t \\ & +( p _ s - p _ t )^T( L / V _ cut )^ ( p _ s - p _ t )\\ &= e _ s ^T L _ V _1 V _1 ^ -1 e _ s + e _ t ^T L _ V _2 V _2 ^ -1 e _ t \\ & +( p _ s - p _ t )^T( L / V _ cut )^ ( p _ s - p _ t )\\ &=r_ G _ V _1 (s, _1)+r_ G _ V _2 (t, _2)\\ & +( p _ s - p _ t )^T( L / V _ cut )^ ( p _ s - p _ t ). Similar to the proof of Lemma~ [ref] , we can obtain that $ e _ s ^T L _ U U ^ -1 e _ t =0$ for $s$ and $t$ in different connected components. $ L _ U U ^ -1 $ has a structure: $ [ L _ V _1 V _1 ^ -1 & & \\ & L _ V _2 V _2 ^ -1 & \\ & & … ]$. Thus, the third and the fourth equality holds. The Lemma is established.

**Proof.** According to the definition of resistance distance, we can obtain that $r(s,t)=( e _ s - e _ t )^T L _v^ -1 ( e _ s - e _ t )$, $r(s, _1)= e _ s ^T L _ V _1 V _1 ^ -1 e _ s $, $r(t, _2)= e _ t ^T L _ V _2 V _2 ^ -1 e _ t $. Moreover, $ L _ U U ^ -1 $ has the block structure: $ [ L _ V _1 V _1 ^ -1 & & \\ & L _ V _2 V _2 ^ -1 & \\ & & … ]$, which establishes the lemma.

**Proof.** According to the block matrix decomposition formula, Let $S=A-BD^ -1 C$ be the Schur complement of $D$ with respect to $A$, we have: A & B \\ C & D ^ -1 = S^ -1 & -S^ -1 BD^ -1 \\ -D^ -1 CS^ -1 & D^ -1 +D^ -1 CS^ -1 BD^ -1 . We can prove this lemma using block matrix decomposition. First, let's partition $ L _ U _2 U _2 $ according to $ U _1$ and $ U _2 U _1$: L _ U _2 U _2 = L _ U _1 U _1 & L _ U _1( U _2 U _1) \\ L _ ( U _2 U _1) U _1 & L _ ( U _2 U _1)( U _2 U _1) . Using the formula for the inverse of a block matrix, we have: ( L _ U _2 U _2 ^ -1 )^ -1 = A & B \\ C & D ^ -1 = L _ U _1 U _1 & L _ U _1( U _2 U _1) \\ L _ ( U _2 U _1) U _1 & L _ ( U _2 U _1)( U _2 U _1) . The top-left block is precisely the inverse of the Schur complement of $D$ with respect to $A$. Therefore, we have: S= L _ U _1 U _1 ^ -1 = & ( L _ U _2 U _2 ^ -1 )_ U _1 U _1 - ( L _ U _2 U _2 ^ -1 )_ U _1( U _2 U _1) \\ & ( L _ U _2 U _2 ^ -1 )_ ( U _2 U _1)( U _2 U _1) ^ -1 ( L _ U _2 U _2 ^ -1 )_ ( U _2 U _1) U _1 . This is exactly the Schur complement of $ L _ U _2 U _2 ^ -1 $ with respect to the node set $ U _1$, which completes the proof.

**Proof.** We will prove this by mathematical induction on the number of eliminated nodes. Base case: When no nodes have been eliminated ($i=0$), we have $ S _0 = L ^ -1 _ U _2 U _2 $. Inductive hypothesis: Assume that after eliminating $k$ nodes, the resulting matrix $ S _k$ has the form where all rows and columns corresponding to the eliminated nodes are zero, and the submatrix corresponding to the remaining nodes correctly represents their Schur complement. Inductive step: Consider the elimination of node $v_ k+1 $. Let's partition the matrix $ S _k$ as $ S _k = A & b \\ b^T & c $, where $c = S _k[v_ k+1 ,v_ k+1 ]$ is a scalar, $b = S _k[:,v_ k+1 ]$ excluding the element $c$, and $A$ is the remaining submatrix. The elimination step gives: S _ k+1 = S _k - S _k[:,v_ k+1 ] S _k[:,v_ k+1 ]^T S _k[v_ k+1 ,v_ k+1 ] = A - bb^T c & 0 \\ 0 & 0 . This is precisely the Schur complement operation. According to the block matrix inversion formula, if we have a matrix $M = A & B \\ C & D $ and its inverse $M^ -1 = E & F \\ G & H $, then $E = (A - BD^ -1 C)^ -1 $, which is the inverse of the Schur complement of $D$ in $M$. In our case, we're performing the elimination in the inverse matrix $ L ^ -1 _ U _2 U _2 $, and each elimination step corresponds to computing the Schur complement with respect to one node. After eliminating all nodes in $ U _2 U _1$, by Lemma~ [ref] , the remaining submatrix corresponding to $ U _1$ is exactly $ L _ U _1 U _1 ^ -1 $, and all other elements are zero. Therefore, $ S _ | U _2 U _1| = L _ U _1 U _1 ^ -1 & 0 \\ 0 & 0 $.

**Proof.** The proof follows directly from the Gaussian elimination process. When we eliminate a node $v_i$, we perform the operation $ S _ i = S _ i-1 - S _ i-1 [:,v_i] S _ i-1 [:,v_i]^T S _ i-1 [v_i,v_i] $. This means we're subtracting a rank-1 matrix from $ S _ i-1 $. Each elimination step removes exactly one rank-1 matrix of the form $ S [:,v_k] S [:,v_k]^T S [v_k,v_k] $. Since we start with $ L _ U _2 U _2 ^ -1 $ and end with $ [ L _ U _ 1 U _ 1 ^ -1 & 0 \\ 0 & 0 ]$ after eliminating all nodes in $ U _2 U _1$, the difference between these matrices must be the sum of all the rank-1 matrices we subtracted during elimination. Therefore, $ L _ U _ 2 U _ 2 ^ -1 - [ L _ U _ 1 U _ 1 ^ -1 & 0 \\ 0 & 0 ]=Σ_ k=| U _1|+1 ^ | U _2| S [:,v_k] S [:,v_k]^T S [v_k,v_k] $.

**Proof.** By applying Lemma~ [ref] , we can express the difference between the inverse Laplacian matrices. Since $s V _1$ and $t V _2$, and considering that the elements of the matrix are zero outside their respective blocks, we can decompose the resistance distance calculation as follows: e _s^T L _ U U ^ -1 e _t= e _s^T L _ V _1 V _1 ^ -1 e _t+ e _s^T L _ V _2 V _2 ^ -1 e _t+Σ_ v V _ cut S [v,s] S [v,t] S [v,v] . The Lemma is established since $r_ G _ V (s,t)= e _s^T L _ U U ^ -1 e _s+ e _t^T L _ U U ^ -1 e _t-2 e _s^T L _ U U ^ -1 e _t$ and Lemma~ [ref] .

**Proof.** The non-zero structure of the resistance distance labelling can be derived from the vertex hierarchy property of the tree decomposition (Lemma~ [ref] ) and the cut property of resistance distance (Lemma~ [ref] ). For property (i), consider a node $v$ and any node $u$ in its subtree. When $v$ is eliminated during the process, all nodes in its subtree (including $u$) are still present in the remaining graph. According to Definition~ [ref] , $ S [v,u]$ represents the $u$-th element of the $v$-th column of $ L ^ -1 _ U U $, where $ U $ is the set of remaining nodes after $v$ is eliminated. Since $u U $, the corresponding entry in the inverse Laplacian is non-zero due to the connectivity between $v$ and nodes in its subtree. All other entries in the resistance distance labelling are zero because they correspond to pairs of nodes that are separated by vertex cut formed by ancestors of $v$ in $ T _ min $, as established by the tree decomposition structure and the cut property of resistance distance. For property (ii), it essentially provides the reverse perspective of property (i). Since $ S [v,u]$ is non-zero for all $u$ in the subtree of $v$, if $v$ lies on the path from $u$ to the root, we can conclude that $ S [v,u]$ is non-zero for all nodes $v$ that appear on the path from $u$ to the root in the tree decomposition. Conversely, for any node $v$ that is not on the path from $u$ to the root, $ S [v,u]$ will be zero. This includes two categories: (1) nodes in the subtree rooted at $u$ (i.e., children of $u$ and their descendants), and (2) nodes in other branches of the tree. For children of $u$ and their descendants, when these nodes are eliminated, $u$ has already been eliminated earlier according to the reverse ordering of the process, so $u$ is not in the remaining set $ U $ when computing $ L ^ -1 _ U U $, resulting in zero entries. For nodes in other branches, the vertex hierarchy property ensures that the lowest common ancestor of $u$ and such nodes (along with its ancestors) forms a vertex cut that separates them in the original graph. By the cut property of resistance distance, this separation leads to zero entries in the resistance distance labelling.

**Proof.** [Proof sketch] red The proof relies on the vertex hierarchy property and the cut property of resistance distance. For property (i), when a node $v$ is eliminated, any node $u$ in its subtree remains connected in the graph, resulting in a non-zero $ S [v,u]$. Property (ii) is the dual of (i): if $v$ is an ancestor of $u$, then $u$ is in $v$'s subtree, making $ S [v,u]$ non-zero. All other entries are zero because ancestral nodes form vertex cuts that separate unrelated node pairs.

**Proof.** According to Lemma~ [ref] , we have $ L _v^ -1 = Σ_ v S [:,v] S [:,v]^T S [v,v] $. Substituting this into the resistance distance formula, we obtain: r(s,t) &= ( e _s- e _t)^T (Σ_ v S [:,v] S [:,v]^T S [v,v] )( e _s- e _t) \\ &= Σ_ v ( e _s- e _t)^T S [:,v] S [:,v]^T( e _s- e _t) S [v,v] \\ &= Σ_ v ( S [v,s]- S [v,t])^2 S [v,v] . Based on the sparsity structure of the resistance distance labelling and the properties of the tree decomposition, we can partition this sum into three parts: (i) For $v P _ s (s,t) $, $ S [v,t] = 0$, so $ ( S [v,s]- S [v,t])^2 S [v,v] = ( S [v,s])^2 S [v,v] $. (ii) For $v P _ t (s,t) $, $ S [v,s] = 0$, so $ ( S [v,s]- S [v,t])^2 S [v,v] = ( S [v,t])^2 S [v,v] $. (iii) For $v P _ (s,t) root $, both $ S [v,s]$ and $ S [v,t]$ may be nonzero. Therefore, the sum of the three parts is equal to the resistance distance $r(s,t)$.

**Proof.** This result follows from the properties of traversal on trees. During a traversal, all nodes in the subtree rooted at any node $u$ are visited consecutively before the traversal backtracks to nodes outside this subtree. When performing a traversal on $ T _ min $, each node $v$ is assigned a position $ T _ min . [v]$ in the traversal sequence. For any node $v$ in the subtree of $u$, the values $ T _ min . [v]$ form a contiguous range starting from $ T _ min . [u]$. $ T _ min . [u] - T _ min . [v]$ indicates the relative position of node $u$ within the subtree rooted at $v$, and is unique for each node $v$ in this subtree. In our index structure, $ S [v]. $ stores the resistance distance labelling for all nodes in the subtree of $u$, arranged according to their relative positions in the ordering. Therefore, $ S [v]. [ T _ min . [u] - T _ min . [v]]$ directly retrieves the value of $ S [v,u]$ for any node $u$ in the subtree of $v$.

**Proof.** For each node $u$ in the graph, we need to store: (i) The parent pointer in $ T _ min $, which requires $O(1)$ space per node; (ii) The ordering information, which also requires $O(1)$ space per node; (iii) The resistance distance labelling $ S [u]. $, which stores values for all nodes in the path from $u$ to the root of $ T _ min $. Since the height of $ T _ min $ is at most $h_ G $, the path from any node to the root contains at most $h_ G $ nodes. Therefore, the size of $ S [u]. $ is bounded by $O(h_ G )$ for each node $u$. With $n$ nodes in total, the overall space complexity is $O(n) + O(n) + O(n · h_ G ) = O(n · h_ G )$.

**Proof.** [Proof sketch] red The total label size is the sum of storage required for all nodes. For each node $u$, due to its non-zero structure, only needs to store values for the path from $u$ to the root. Since this path has a length at most the tree height $h_ G $, the total size is $O(n· h_ G )$.

**Proof.** According to the block matrix inverse formula, we have: [ A & B \\ C & D ]^ -1 = [ A^ -1 +A^ -1 BS^ -1 CA^ -1 & -A^ -1 BS^ -1 \\ -S^ -1 CA^ -1 & S^ -1 ] where $S = D - CA^ -1 B$ is the Schur complement. Applying this formula to our case with $A= L _ U _i U _i $ and $B=- 1 _ N (v_ i+1 ) U _i $ yields the desired result.

**Proof.** Consider any node $w N (v_i) U $. By the algorithm's processing order, $w$ has been processed before $v_i$ since $w U $ when we process $v_i$. In a tree decomposition, for any edge $(u,v)$ in the original graph, there must exist at least one tree node containing both $u$ and $v$. Since $w$ and $v_i$ are neighbors in $ G $, they must share at least one tree node in $ T _ min $. Given that we process nodes according to the reverse order, when we process $v_i$, all nodes in its subtree (including $w$) have already been processed. The tree structure ensures that $v_i$ is the parent of $w$ in $ T _ min $, as this is the only arrangement that maintains the required connectivity property while preserving the properties of the tree decomposition. Therefore, for each node $w N (v_i) U $, $v_i$ must be the parent of $w$ in $ T _ min $.

**Proof.** The correctness follows from the rank-1 update formula presented in Lemma~ [ref] . For each node $v_i$, the algorithm implements this formula by: (i) Computing $ L _ U _i U _i ^ -1 1 _ N (v_ i+1 ) U _i $ through the traversal and update process (Lines 5-12); (ii) Computing the denominator $d_ v_i - 1 ^T_ N (v_ i+1 ) U _i L _ U _i U _i ^ -1 1 _ N (v_ i+1 ) U _i $ (Line 13); (iii) Normalizing by this denominator (Lines 14-16). Line 15 specifically stores the diagonal elements of $ L _v^ -1 $ to support single-source queries. According to Lemma~ [ref] , the while loop will terminate as $v_i$ is guaranteed to be an ancestor of each $w N (v_i) U $ in the tree. By processing nodes according to the order of the tree decomposition, the algorithm correctly builds the complete resistance distance labelling.

**Proof.** [Proof sketch] red The correctness hinges on the iterative application of the rank-1 update formula for the inverse Laplacian from Lemma~ [ref] . For each node $v_i$, the nested loops correctly compute the vector and scalar components of this formula. By processing nodes in the specified order, these incremental updates correctly build the final resistance distance labeling for the entire graph.

**Proof.** We analyze the time complexity by examining each component of the algorithm: First, initialization and DFS order computation (lines 1-2) require $O(n)$ time. The main outer loop (line 3) executes exactly $n$ iterations, processing each node $v_i$ sequentially. For each node $v_i$, the inner loop (line 5) iterates through its already processed neighbors, bounded by $| N (v_i) U | ≤ d_ v_i ≤ d_ max $. Within this loop, the while loop (lines 7-12) traverses from each processed neighbor $v_k$ up to $v_i$ in the tree, updating the labelling for all nodes in the subtree rooted at $v_k$. This operation takes at most $O(| T _ min . (v_k)|)$ steps for each $v_k$. Since Lemma~ [ref] establishes that $v_i$ is an ancestor of each $v_k$, we can bound the total work: for each node $v_i$, the combined size of all subtrees rooted at its processed neighbors cannot exceed $v_i$'s own subtree size multiplied by $d_ max $. This gives us an upper bound of $O(d_ max · n · h_ G ^2)$ operations across all nodes, since $Σ_ i=1 ^n | T _ min . (v_i)| = O(n · h_ G )$. The final update operations (lines 13-16) require $O(| T _ min . (v_i)|)$ time for each node, summing to $O(n · h_ G )$ across all nodes. Combining these analyses, we conclude that the overall time complexity of Algorithm~ [ref] is $O(n · d_ max · h_ G ^2)$.

**Proof.** [Proof sketch] red The algorithm's time complexity is dominated by the main loop iterating through $n$ nodes. For each node $v_i$, the algorithm processes its neighbors (at most $d_ max $) and performs updates on their respective subtrees. The total work for these updates, when summed across all nodes, is proportional to the sum of all subtree sizes in the decomposition tree, which is bounded by $O(n· h_ G )$, resulting in a total complexity of $O(n· d_ max · h_ G )$.

**Proof.** The time complexity of Algorithm~ [ref] is determined by the number of nodes visited during the three traversal phases. In the first phase, we traverse from node $s$ to $ (s,t)$, which requires $| P _ s (s,t) |$ steps. Similarly, the second phase traverses from $t$ to $ (s,t)$, requiring $| P _ t (s,t) |$ steps. The final phase traverses from $ (s,t)$ to the root, taking $| P _ (s,t) root |$ steps. Since each step performs only constant-time operations (arithmetic calculations using precomputed labels), the total time complexity is $| P _ s root | + | P _ t root |$. Since the length of any path from a node to the root is bounded by the height of the tree $h_ G $, the overall time complexity is $O(h_ G )$.

## Bound sentences
- Given the labelling structure, the following lemma gives an upper bound on the label size: The label size of is $O(n· h_ G )$.
- In practice, the tree height $h_ G $ is typically not very large, and the actual label size is often much smaller than the theoretical upper bound (see Section~ [ref] , Table~ [ref] ).
- This gives us an upper bound of $O(d_ max · n · h_ G ^2)$ operations across all nodes, since $Σ_ i=1 ^n | T _ min .
- In the worst case, when the tree is highly unbalanced, each subtree could contain up to $O(n)$ nodes, and the path length could be $O(h_ G )$, resulting in an upper bound of $O(n · h_ G )$ for the time complexity.
