{"id": "sigmod26-255", "title": "Workload-Aware Incremental Reclustering in Cloud Data Warehouses", "from": "tex", "n_stmt": 5, "n_proof": 3, "kinds": {"theorem": 2, "lemma": 3}, "counts": {"np_hard": 0, "lower_bound": 0, "upper_bound": 3, "big_o": 22, "omega": 2, "theta": 2, "approx_ratio": 0, "whp": 0, "regret": 0, "dp": 0, "invariant": 0, "convergence": 0, "competitive": 0, "worst_case": 2, "sketch": 0, "cost_model": 21, "cardinality": 0, "learned": 0}, "statements": [{"kind": "theorem", "num": "", "text": "For a sequence of batched ingestions and range queries, the adjusted greedy algorithm achieves a total cost of at most \\[ O((n + q) log n) + Σ [ ] | output _i| / m + (warm starting cost) , \\] where $ output _i$ is the number of keys returned by the $i$-th range query, and the warm starting phase lasts for at most $O(n)$ operations."}, {"kind": "lemma", "num": "", "text": "We say an output $P[c_j, d_j]$ is matched to an input $P[a_i, b_i]$ if $[c_j, d_j] [a_i, b_i]$. Then there exists a matching of size $k - 3$ between the input and output micro-partitions (i.e., there are at most 3 unmatched output micro-partitions)."}, {"kind": "theorem", "num": "", "text": "For a sequence of batched ingestions and range queries, the algorithm achieves a total cost of less than \\[ O((n + q) log q) + Σ [ ] | output _i| / m , \\] where $ output _i$ is the number of keys returned by the $i$-th range query."}, {"kind": "lemma", "num": "", "text": "A reclustering operation of $\\,k$ micro-partitions decreases the potential $ $ by at least $4k - O(log q)$."}, {"kind": "lemma", "num": "", "text": "For all but $O(log q)$ matched input-output pairs, $ d (c_j, d_j) (a_i, b_i) - 4 $."}], "proofs": [{"text": "Construct a bipartite graph $G$ whose left vertices represent the $k$ input micro-partitions and right vertices represent the $k$ output micro-partitions. Draw an edge from input $P[a_i, b_i]$ to output $P[c_j, d_j]$ if and only if $[c_j, d_j] [a_i, b_i]$. We apply Hall's theorem [cite] to show that $G$ has a matching of size $k - 3$. Let $S$ be a subset of the input micro-partitions, and let $N(S)$ be the neighbors of $S$ in the output micro-partitions. We only need to show that $|N(S)| |S| - 3$ for all $S$. Let $A = min_ i S a_i$ and $B = max_ i S b_i$. Since each input micro-partition covers $x$, there is an input $P[a_i, b_i] [A, x]$ and an input $P[a_j, b_j] [x, B]$. Moreover, the input micro-partitions in $S$ provide a total of $m · |S|$ keys in the range $[A, B]$, which implies that there are at least $|S|$ output micro-partitions whose ranges intersect with $[A, B]$. Among these outputs, at most one can have its left endpoint smaller than $A$, and at most one can have its right endpoint greater than $B$; the remaining $|S| - 2$ output micro-partitions must be subintervals of $[A, B]$. Furthermore, at most one output micro-partition $P[c_i, d_i]$ can strictly contain $x$ (i.e., $c_i < x < d_i$), and the remaining $|S| - 3$ outputs must be subintervals of either $[A, x]$ or $[x, B]$, which implies that they are the neighbors of $S$. Therefore, $|N(S)| |S| - 3$ for all $S$, and the lemma follows."}, {"text": "We first prove this claim for output micro-partitions $P[c_j, d_j]$ with $c_j x + 12$. Assume there are $r k$ such outputs $P[c_1, d_1], …, P[c_r, d_r]$ satisfying $d_i c_ i+1 $ for $1 i < r$. Let $d_0 = x + 12$ for convenience, and let $P[a_i, b_i]$ be the input micro-partition matched with $P[c_i, d_i]$. Each of these $r$ outputs falls into either case: ; $(d_i - x) 3/2 · (d_ i-1 - x)$. Since the maximum $ d_i $ is at most $ d 2q $, there can only be $O(log q)$ such output micro-partitions. ; $(d_i - x) < 3/2 · (d_ i-1 - x)$. This simplifies to $3(d_i - d_ i-1 ) < d_i - x$, which further implies $$ d_i - c_i d_i - d_ i - 1 d_i - x 3 d_i - x 2 - 2 < b_i - a_i - 1 2 - 1, $$ where the second-to-last inequality holds because $d_i - x 12$; the last inequality holds because $P[a_i, b_i]$ covers both $x$ and $P[c_i, d_i]$. Therefore, d $$ (c_i, d_i) 4 + 4 log (d_i - c_i + 1) 4 + 4 log ( b_i - a_i - 1 2 ) (a_i, b_i) - 4. $$ That is, for all but $O(log q)$ output micro-partitions $P[c_i, d_i]$ with $c_i x + 12$, we have d $ (c_i, d_i) (a_i, b_i) - 4$. By symmetry, the same holds for output micro-partitions $P[c_i, d_i]$ with $d_i x - 12$. For the remaining partitions, either: ; $P[c_i, d_i]$ is non-degenerate and covers an integer in $[x - 12, x + 12]$. There are only $O(1)$ such micro-partitions, since at most two non-degenerate outputs cover any given integer. ; $P[c_i, d_i]$ is degenerate or does not cover any integer. In either case, its potential is 0 and is d at least 4 less than its matched input partition (which is at least 4). Putting all cases together completes the proof."}, {"text": "We analyze the potential change $ $ of a reclustering operation as follows: ; At most 3 unmatched output micro-partitions are created, which increase the potential by at most $O(log q)$. ; At most 3 unmatched input micro-partitions are removed, which do not increase the potential. ; For each matched pair of an input $P[a_i, b_i]$ and an output $P[c_j, d_j]$, we have $ (c_j, d_j) (a_i, b_i)$. For all but $O(log q)$ matched pairs, we have d $ (c_j, d_j) (a_i, b_i) - 4$. Taking a summation over all these changes yields the lemma."}], "bound_sents": ["With these distinctions in mind, we clarify and summarize our key contributions as follows: [leftmargin=*] ; a policy-key decoupled taxonomy and framework for the table clustering problem ( sec:preliminaries ), ; the notion of boundary micro-partitions and the logarithmic theoretical upper bound on  …", "Second, we introduce the concept of boundary micro-partitions that dominate scan-set pruning efficiency, and prove a theoretical upper bound for the cost of reclustering all these boundary micro-partitions.", "We then present , a workload-aware algorithm to identify and recluster only boundary micro-partitions most critical for pruning efficiency. achieves near-optimal (with respect to fully sorted table layouts) query performance but incurs significantly lower reclustering cost with a theoretical upper b …"], "flag": true, "score": 11, "n_chars": 121087}