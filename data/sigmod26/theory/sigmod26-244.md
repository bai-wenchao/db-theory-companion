# sigmod26-244 — Towards Parameterized Hardness on Maintaining Conjunctive Queries

from=tex flag=True score=57 stmts=30 proofs=13 chars=135980
kinds: {"proposition": 2, "definition": 12, "theorem": 9, "conjecture": 5, "lemma": 13}
counts: {"np_hard": 0, "lower_bound": 35, "upper_bound": 9, "big_o": 120, "omega": 15, "theta": 0, "approx_ratio": 0, "whp": 1, "regret": 0, "dp": 0, "invariant": 0, "convergence": 0, "competitive": 0, "worst_case": 2, "sketch": 0, "cost_model": 0, "cardinality": 0, "learned": 0}

## Statements
**Definition Chordless Path on Hypergraph.** Given a hypergraph $ = ( , )$, we define the chordless path on the hypergraph $ $ to be a sequence of distinct vertices $P$, and an edge sequence $P_e$ of the chordless path $P$ to be a sequence of hyperedges. A chordless path $P$ is [leftmargin=*] ; a length $1$ chordless path if $P \ \ $, and $P_e \ e_1\ $ as an edge sequence of $P$ with an arbitrary hyperedge $e_1$\; ; a length $2$ chordless path if $P \ v_1\ $ and there exists two hyperedges $e_1, e_2$ with $v_1 e_1 e_2$. All such pair of hyperedges form an edge sequence $P_e \ e_1, e_2\ $ of $P$\; ; a length $k$ chordless path for $k 3$ if $P \ v_1, …, v_ k-1 \ $ such that [leftmargin=*] ; (Adjacency) For every $i [1, k-2]$, there exists a hyperedge $e_ i+1 E$ with both $v_i, v_ i+1 e$; ; (No shortcuts) For any $i, j [1, k-1]$ with $|i-j| > 1$, there is no $e E$ with both $v_i,v_j e$; ; (Endpoints) there exist $e_1, e_k E$, such that $v_1 e_1$ and $v_2 e_1$, and $v_ k-1 e_k$ and $v_ k-2 e_k$ . Any $P_e \ e_1, …, e_k\ $ that satisfies the above conditions is an edge sequence of $P$. Note that all hyperedges in an edge sequence are also distinct due to the ``No shortcuts" requirement, and such an edge sequence may not be unique for a given hypergraph. A chordless path $P$ is maximum if there is no strictly longer chordless path $P'$ with $P P'$. This definition aligns with the standard graph definition when all hyperedges are standard edges, and the sequence $P$ is defined as the path's internal vertices $v_2, ..., v_n$. T …

**Definition Dimension.** Let $Q = ( , , y )$ be a conjunctive query. Let $ _c $ be a connected subset with induced relations $ _c$. Let the remaining relations be $ _r = _c$. A subset of relations $ N _r$ is called a distinct neighbor set if there exists an injective mapping $ : N _ $ such that for every $R( x ) N $, $ (R) x _ $. We refer to the image of this mapping, $ _k( N ) = \ (R) R N \ $, as a key set . Note that multiple key sets may exist for a fixed $ N $. The local dimension of $ _c$, denoted $ ( _c)$, is defined as: \[ ( _c) = max_ N _r, \ _k( N ) | _k( N )| & if _c y ≠ or _k( N ) y , \\ | _k( N ) ( y _ )| & otherwise. \] Intuitively, if $ _c$ contains output attributes, or $ _k( N )$ contains only output attributes, we only count the contribution of $ _k( N )$. Otherwise, we additionally count any other active output attributes. The dimension of the query $Q$ is the maximum local dimension over all connected subsets: \[ (Q) = max_ _c ( _c). \]

**Definition Generalized H-index.** Consider a star query $Q$ with center relation $R_1(x_1, , x_d)$ and dimension $d = (Q)$. For any attribute $x_i \ x_1, , x_d\ $, let $ _ R_1 (v)$ denote the frequency of a value $v$ in the projection $ _ x_i R_1$. We define the H-index of a specific attribute $x_i$, denoted $h_i(D)$, as the largest integer such that there are at least $h_i(D)$ distinct values in $ _ x_i R_1$ with frequency at least $h_i(D)^ d-1 $. Formally: \[ h_i(D) = max \ k N \; |\; | \ v _ x_i R_1 : _ R_1 (v) k^ d-1 \ | k \ . \] The Generalized H-index of the query $Q$ over database $D$ is defined as $h(D) = max_ i=1 ^d h_i(D)$.

**Definition Relational Hypergraph.** The relational hypergraph associated with a conjunctive query $Q = ( , , y )$ is $ = ( , )$, where $ $ is the set of hyperedges (relations) and $ $ is the set of vertices (attributes). Each hyperedge $R_i $ contains the set of vertices $ x _i$. A primal graph $G = (V, E)$ of $ $ is a graph with $V = $, and an edge $(v, u) E$ if and only if there exists $R( x ) $, such that $v, u x $. A primal graph over $V $ is a graph $G(V) = (V, E)$, and an edge $(v, u) E$ if and only if there exists $R( x ) $, such that $v, u V x $.

**Definition Connected Subset.** Given a conjunctive query $Q = ( , , y )$, let $ _c _ $ be a subset of join attributes. We define the induced active relations $ _c = \ R( x ) x _c ≠ \ $. To refine the definition, when $ _c = \ \ $, every $R_i( x _i) $ can be the active relation $ _c = \ R_i\ $. We say $ _c$ is a connected subset if the primal graph $G( _c)$ is connected. Let $ ( _c) = _ R( x ) _c x $ denote the set of attributes appearing in $ _c$. We define the set of active attributes as $ _ = ( _c) _c$.

**Lemma .** Given a CQ $Q = ( , , y )$, let $P \ v_1, …, v_ k-1 \ $ be a length $k$ chordless path on $Q$'s relational hypergraph $ $, for any join tree $ $ of $Q$, let $n_1$ be a node with $v_1 n_1$ while $v_i n_1$ for all $i [2, k-1]$, and $n_l$ be a node with $v_ k-1 n_l$ while $v_i n_l$ for all $i [1, k-2]$, then the path $P_ $ between $n_1$ and $n_l$ on the join tree includes all attributes in $P$. In addition, such a path has a length of at least $k$.

**Definition Star Queries.** A query $Q = ( , , y )$ is a star query if it is of the form: \[ Q(x_ d-j+1 , , x_d) R_1(x_1, …, x_d), R_2(x_1), , R_ k+1 (x_k) \] for any $0 d-j k d$, where $R_1$ is the center relation, $R_2, , R_ k+1 $ are satellite relations, $d$ is the dimension of the query and $j$ is the number of output attributes. There are two special cases: $j = d, k=d$ (Full query) or $j = 0, k = d$ (Boolean query with no output attributes).

**Theorem \textbf{Algorithm for Star Queries.** Let $Q$ be a star query with dimension $d 1$, and $j [0, d]$ output attributes in $ x _1$, $D$ be the current database instance, and $h(D)$ be its generalized $H$-index (a measure of database densities, see Section~ [ref] ). There is a dynamic algorithm with linear space $O(|D|)$ that maintains $Q$ with an amortized update time of $O(h(D)^ d-1 )$ and supports $O(h(D)^ d-j )$-delay~enumeration.

**Definition .** Given a conjunctive query $Q$, let $ = ( , )$ be $Q$'s relational hypergraph, query $Q$ is a height $k$ query if $ $ contains no chordless path with length greater than $2k$, $ $ contains no q-chordless path with length greater than $k$, and [leftmargin=*] ; $ $ contains a length $2k$ or length $2k-1$ chordless path; and/or ; $ $ contains a length $k$ q-chordless path.

**Lemma .** Consider the query with arbitrary output attributes $ y $: \[ Q_ 2k-1 ( y ) R_1(x_1), R_2(x_1, x_2), …, R_ 2k-2 (x_ 2k-3 ,x_ 2k-2 ), R_ 2k-1 (x_ 2k-2 ). \] If $Q_ 2k-1 $ can be maintained in amortized $O(|D|^ (k-1)/k-ε )$ time while supporting $O(|D|^ 1-ε )$-delay enumeration for any constant $ε > 0$, the combinatorial $k$-clique conjecture failed.

**Lemma .** Consider the query with arbitrary output attributes $ y $: \[ Q_ 2k ( y ) R_1(x_1), R_2(x_1, x_2), …, R_ 2k-1 (x_ 2k-2 , x_ 2k-1 ), R_ 2k (x_ 2k-1 ). \] If $Q_ 2k $ can be maintained in amortized $O(|D|^ (k-1)/k-ε )$ time while supporting $O(|D|^ 1-ε )$-delay enumeration for any constant $ε > 0$, the combinatorial $k$-clique conjecture failed.

**Definition $(n, k)$-Perfect Hash Family.** A $(n, k)$-perfect hash family is a set of hash functions $H$ mapping a universe of $n$ items to a range of $k$ values such that for every subset $S$ of items with $|S| = k$, there exists at least one hash function $f H$ that is injective on $S$ (i.e., $f$ maps the $k$ distinct items in $S$ to $k$ distinct values).

**Theorem Odd Cycle~\cite{lincoln_et_al:LIPIcs.ITCS.2020.11}, Theorem 15.** Given a directed graph $G = (V, E)$ with $|V| = n$ and $|E| = m$, and $m = n^ 1+2/(k-1) $ , no combinatorial algorithm can detect the existence of an odd $k$-cycle for any odd integer $k 3$ in $G$ in time $O(m^ 2k/(k+1)-ε )$ for any constant $ε > 0$, assuming combinatorial $k$-clique conjecture.

**Theorem Even Cycle~\cite{lincoln_et_al:LIPIcs.ITCS.2020.11}, Corollary 16.** Given a directed graph $G = (V, E)$ with $|V| = n$ and $|E| = m$, and $m = n^ k/(k-2) $, no combinatorial algorithm can detect the existence of a even $k$-cycle for any even integer $k 4$ in $G$ in time $O(m^ (2k-2)/k-ε )$ for any constant $ε > 0$, assuming combinatorial $k$-clique conjecture.

**Lemma .** Consider the star query \( Q_d'(x_ d-j+1 , …, x_d) R_1(x_1, x_2, …, x_d), R_2(x_1), …, \\ R_ k+1 (x_ k ), \) for any $0 d-j k d$. Unless OuMv$_k$ Conjecture failed, $Q_d'$ cannot be maintained in $O(|D|^ (d-1)/d-ε )$ time while supporting $O(|D|^ (d-j)/d-ε )$-delay enumeration for any $ε > 0$.

**Definition Residual Query.** Given a free-connex query $Q$ with its skeleton $ _*$, where $ _* = \ x R( x ) _*, x x \ $ is the set of attributes in $ _*$, and $ y _* = \ x x y R( x ) _*, x x \ $ is the set of output attributes in $ _*$, the residual query $Q_*$ for $Q$ is $ _*$-induced query $Q_* ( _*, _*, y _*)$.

**Lemma .** Consider the query \[ Q_ k (x_k) R_1(x_1), R_2(x_1, x_2), …, R_k(x_ k-1 , x_ k ). \] If $Q_ k $ can be maintained in amortized $O(|D|^ (k-1)/k-ε )$ time while supporting $O(|D|^ 1/k-ε )$ delay enumeration for any constant $ε > 0$, the combinatorial $k$-clique conjecture failed.

**Lemma .** Consider the star query: \( Q_d() R_1(x_1, , x_d), R_2(x_1), , R_ d+1 (x_d). \) This query has a dimension of $d$. Unless the OuMv$_k$ Conjecture fails, $Q_d$ cannot be maintained with amortized $O(|D|^ (d-1)/d - ε )$ time and $O(1)$-delay enumeration for any constant $ε > 0$.

**Lemma .** Let $Q_1 = ( _1, _1, y _1)$ and $Q_2 = ( _2, _2, y _2)$ are two residual queries for a given free-connex query $Q$, then for every $R( x ) _1$, it is either (1) $R( x ) _2$, or (2) there exists $R'( x ') _2$, such that $R'( x ') _1$, and $ x _ = x ' _ $.

**Theorem \textbf{height-based lower bound.** Let $Q$ be a height $k$ query with $k 2$ . Assuming combinatorial $k$-clique conjecture, no dynamic combinatorial algorithm can maintain $Q$ with amortized update time $O(|D|^ (k-1)/k-ε )$ and support constant delay enumeration for any constant~$ε>0$.

**Definition Ears.** In a free-connex query $Q = ( , , y )$, a relation $R( x ) $ is called an ear of the query if there exists another relation $R'( x ') $, such that either (i) $ x _ x '$ and $ x _ y $; or (ii) $ x _ x ' y $. We refer to $R'$ as an anchor of $R$.

**Theorem \textbf{Dimension-based lower bound.** Let $Q$ be a conjunctive query of dimension $d 2$ . Assuming the OuMv$_k$ conjecture, no dynamic algorithm can maintain $Q$ with amortized update time $O(|D|^ (d-1)/d-ε )$ and support constant delay enumeration for any constant $ε>0$.

**Lemma \cite{pods25}, Lemma~2.2 and Lemma~3.7.** Let $Q$ be a free-connex conjunctive query. $Q$ is q-hierarchical if and only if it admits a height $1$ free-connex join tree, and $Q$ is weak-q-hierarchical if and only if it admits a height $2$ free-connex join tree.

**Lemma .** Given a free-connex $Q=( , , y )$, let $P \ v_1, …, v_ k-1 \ $ be a length $k$ q-chordless path on $Q$'s relational hypergraph $ $, for any free-connex join tree $ $ of $Q$, $ $ has a height at least $k$.

**Theorem \cite{wang2023change, idris17:_dynam, pods25.** Given any insertion-only update sequence, a CQ $Q$ can be maintained in amortized $O(1)$ time while supporting $O(1)$-delay enumeration if and only if $Q$ is free-connex, assuming BMM and HyperClique.

**Conjecture Combinatorial $k$-Clique Conjecture~\cite{DBLP:conf/focs/AbboudBW15a.** There is no combinatorial algorithm that can detect a $k$-clique in a graph with $n$ vertices and $O(n^2)$ edges in $O(n^ k-ε )$ time, for any constant $ε > 0$ and any integer $k 3$

**Theorem \cite{berkholz17:_answer},Theorem~1.1.** Given any update sequence, a CQ $Q$ can be maintained in $O(1)$ time while supporting $O(1)$-delay enumeration if and only if $Q$ is q-hierarchical, assuming OMv~Conjecture.

**Definition .** The height of a generalized join tree $ $ is the maximum count of input relations on any path from a leaf to the root without considering the generalized relations.

**Proposition .** For any conjunctive query $Q$, the following statements are equivalent: (1) $Q$ is q-hierarchical; (2) $Q$ has height $k = 1$; and (3) $Q$ has dimension $d = 1$.

**Theorem \cite{bagan2007acyclic.** A CQ $Q$ can be enumerated with $O(1)$ delay after a linear-time $O(|D|)$ pre-processing step if and only if $Q$ is free-connex, assuming BMM and HyperClique.

## Proofs
**Proof.** [Proof of Lemma~ [ref] ]

**Proof.** [Proof of Lemma~ [ref] ]

**Proof.** The lemma is immediate for the base case when $k = 2$, i.e., the weak-q-hierarchical queries, as their residual queries are q-hierarchical. We now consider the general case $k > 2$. By definition of residual query and ears, given a free-connex join tree $ $, each leaf node corresponds to an ear, and its parent node is the anchor of that ear. Therefore, we deduce two important facts: (1) if $Q$ has a join tree of height $k$, then its residual query $Q'$ has a join tree of height at most $k-1$. This is because removing all leaf nodes (ears) from $ $ yields a join tree with height $k-1$. If necessary, further removing any remaining ears from the resulting tree can only decrease the height further. (2) Consequently, if the residual query $Q'$ admits a free-connex join tree $ '$ of height $k-1$, then we can construct a join tree for $Q$ of height at most $k$ by attaching each removed ear $R$ as a child of its anchor $R'$ in $ '$. The resulting structure remains a valid free-connex join tree, satisfying the Connect, Cover, and Connex conditions without violating the Guard or Above conditions. Since adding these ears introduces only new leaves, the tree's height increases by 1; therefore, the reconstructed join tree for $Q$ has height at most $k$. We first prove the if direction. Suppose the residual query $Q'$ is a height $k-1$ query. Assume $Q$ admits a free-connex join tree $ $ of height $≤ k-1$. Removing all ears from $ $ yields a join tree for $Q'$ of height at most $k-2$, by observation (1) above. This contradicts the assumption that $Q'$ is a height $k-1$ query, since $Q'$ would then have a join tree of height smaller than $k-1$. Therefore, $Q$ cannot have any free-connex join tree of height less than $k$. By observation (2), on the other hand, we know how to construct a height-$k$ join tree for $Q$. Taken together, the minimum possible join tree height for $Q$ is $k$. In other words, $Q$ is a height $k$ query. For the “only-if” direction, assume $Q$ is a height $k$ query. By definition, every free-connex join tree of $Q$ has height at least $k$, and there is some join tree $ $ with height $k$. Remove all the leaf nodes from this $ $ to obtain a join tree $ '$ with height $k-1$, and further removing ears from $ '$ will only decrease the height, making $ Q'$'s height at most $k-1$. Now, suppose for contradiction that $Q'$ is not a height $k-1$ query. Then there exists a free-connex join tree for $Q'$ of height at most $k-2$. By attaching the previously remo …

**Proof.** [Proof of Lemma~ [ref] ] We start by considering a length-2 path $v_1, v_2$. Because of the definition, we know that there exists $e_1$, $e_2$, $e_3$ such that $v_1 e_1$ and $v_2 e_1$, $v_1, v_2 e_2$, and $v_2 e_3$ and $v_1 e_3$. Because the Connect property of a join tree, let $T_1$ be the subtree induced by $v_1$, and $T_2$ be induced by $v_2$, since $e_2$ belongs to both $T_1$ and $T_2$, therefore, all nodes with $v_1$ or $v_2$ are also connected on $ $. We let $e_1$ be $n_1$ and $e_3$ be $n_2$; there must exist a node $n'$ on the path between $n_1$ and $n_2$ with $v_1, v_2 n'$, otherwise the path contains an addition node $v_3$ in between $v_1$ and $v_2$, combining the connect condition with $e_2$ creates a cycle, which violates the fact that $ $ is a tree. On the other hand, assuming for a length $k-1$ chordless path, the lemma holds, then for a length $k$ chordless path, we can adopt a similar idea as for the length-2 case to show the connectness of $v_1, …, v_ k-1 $-induced subtree. Therefore, let $n_1$ and $n_ k $ be two endpoints, the path between $n_1$ and $n_k$ must cover $v_1, …, v_ k-1 $, otherwise a cycle would be detected on the tree. By applying the induction, we can prove the lemma. Because there is no shortcut, covering $v_1, …, v_ k-1 $ requires at least $k-2$ nodes, and there are two endpoints for $v_1$ and $v_ k-1 $, making such a path have a length of at least $k$.

**Proof.** [Proof of Lemma~ [ref] ] We can find a corresponding path $P_ $ on any join tree $ $ for the chordless path $P$, which has a length of at least $k$. Based on the definition of height, we can minimize the height of the tree by placing the two endpoints to be leaf nodes of $ $ and requiring the path to pass the root node. Assuming the distance between the two leaf nodes and the root node is balanced, the height would be at least $ k 2 $.

**Proof.** [Proof of Lemma~ [ref] ] [cite] t pods25 shows that for a height $2$ query with $ y $ to be non-empty or non-full, it is possible there is no length $3$ or length $4$ path, but contains a length $2$ q-chordless path $P = \ x_1\ $ with $x_1 y $, $R_1( x _1), R_2( x _2)$ be two relations with $x_1 x _1 x _2$, and $ x x _1 y $. In that case, $R_1$ must be the parent node of $R_2$ on any free-connex join tree, otherwise violating the Connex Property of the join tree. On the other hand, if for a height $k$ query $Q$, its residual query $Q'$ satisfies $ [Q']$ contains such a length $k-1$ q-chordless path, by definition of q-chordless path, let $R_ k-1 ( x _ k-1 )$ be the relation with only $x_ k-2 $ in the residual query $Q'$, since $R_ k-1 $ is not an ear in $Q$ and $R_ k-1 $ contains only non-output attributes, there exists an ear $R_k( x _k)$ in $Q$ with $R_ k-1 $ be its anchor, and $ x _k x _ k-1 P = $ and $ x _k x _ k-1 ≠ $. Therefore, let $x_ k x _k x _ k-1 $ be any join attributes between $R_ k-1 $ and $R_ k $, we can find a length $k$ q-chordless path with $P' P \ x_k\ $, which completes the proof.

**Proof.** By definition, every relation that is not an ear must remain in the skeleton. Hence, any ear with a non-ear anchor must be removed. Now consider two residual queries $Q_1$ and $Q_2$. If $R( x ) _1$ but $R( x ) _2$, then $R$ must be an ear of the original query $Q$ whose anchors are also ears of $Q$. Since $R$ is removed in $Q_2$ but remains in $Q_1$, there must exist an anchor $R' _2$ of $R$ such that $R' _1$. By the same argument, because $R' _1$, it must have an anchor $R'' _1$. The anchor relation is transitive: if $R_i$ is an anchor of $R_j$ and $R_k$ is an anchor of $R_i$, then $R_k$ is also an anchor of $R_j$. Therefore, we must have $R'' = R$; otherwise, by transitivity, $R$ would also have an anchor in $ _1$ different from itself, and thus should have been removed from $ _1$, a contradiction. It follows that $R$ and $R'$ are anchors of each other, and hence $ x _ ^1 = x ' _ ^2$.

**Proof.** Given a graph $G = (V, E)$ with $m$ edges and $n$ vertices with $n = m^ (k-1)/k $, we first find an $(n, 2k-1)$-perfect hash family $H$, and for every $f H$, we color every vertex using the hash function $f$ and thus partition the graph into $2k-1$ disjoint subgraphs as $V = V^ 1 V^ 2 … V^ 2k-1 $, where $V^ x \ v V|f(v) = x\ $. Let $P$ be the set of all $(2k-1, 2k-1)$-permutations, and let $p$ be any of such permutation, for any $f, p$, we create the update sequence as follows: [leftmargin=*] ; We assign $R_i = \ (u, v) E|f(u)=p(i-1), f(v) = p(i)\ $ and we insert all such edges into $R_i$ for all $i [2, 2k-2]$\; ; For every $v V$ such that $f(v) = p(2k-1)$, we insert all $\ (u)|(v, u) E, f(u) = p(1)\ $ into $R_1$, and all $\ (u)|(u, v) E, f(u) = p(2k-2)\ $ into $R_ 2k-1 $. ; We try to enumerate $Q_ 2k-1 $ on the current database instance. If there exists any results, then there exists a $2k-2$ path $x_1, x_2, …, x_ 2k-2 $, because the color coding guarantee that for any pair of $x_i, x_j$, $x_i ≠ x_j$. In addition, we know that there exist two edges $(v, x_1)$ and $(x_ 2k-2 , v)$ with $v$ having a different color from any $x_i$. Therefore, $(v, x_1, …, x_ 2k-2 )$ form a length $2k-1$-cycle, and we can terminate by reporting $2k-1$ cycle detected. ; Otherwise, we delete all tuples from $R_1$ and $R_ 2k-1 $, and repeat (2) - (4) until no further $v$ exists. If that is the case, we can conclude a $2k-1$ cycle does not exist for the given $f, p$. The above update sequence inserts every edge into the database once, and deletes every edge from the database at most once, making a total of $O(m)$ updates; therefore, if such a query can be maintained in $O(|D|^ (k-1)/k-ε )$ time, maintaining the entire update sequence takes a total running time of $O(m^ (k-1)/k-ε × m) = O(m^ (2k-1)/k-ε )$ time. By setting $k' = 2k-1$, $O(m^ (2k-1)/k-ε )=O(m^ 2k'/(k'+1)-ε )$. Meanwhile, a total of $O(n)$ enumeration operations will be issued during the above procedures. If the enumeration delay is $O(m^ 1-ε )$, then for the enumeration procedure, we would need to spend $O(m^ 1-ε × m^ (k-1)/k ) = O(m^ 2k'/(k'+1)-ε )$ time, summing up the two parts resulting in a total running time of $O(m^ 2k'/(k'+1)-ε )$ for the above procedures. Note that we also need to verify all possible color coding and permutations to ensure we don't miss any cycles in the graph. The total running time for maintaining all $f H$ and $p P$ takes \[O( log m _ size of H × (k')! _ Number of permutations × m^ 2k'/(k …

**Proof.** Given a graph $G = (V, E)$ with $m$ edges, $n = m^ (k-1)/k $ and $k' = 2k$, we construct the same update sequence as for the odd cycle case. Now the total update time would be $O(m^ (2k-1)/k-ε )= O(m^ (2k'-2)/k'-ε )$ and the enumeration time would also be $O(m^ (2k-1)/k-ε )= O(m^ (2k'-2)/k'-ε )$ for any color coding $f H$ and permutation $p$, which would return a combinatorial algorithm for detecting even cycle in time $O(m^ (2k'-2)/k'-ε )$. Combining with Theorem~ [ref] , the existence of the maintenance algorithm suggests the combinatorial $k$-clique conjecture failed.

**Proof.** Given a graph $G = (V, E)$ with $m$ edges, we maintain two $Q_ k (x_k)$ queries as \[ Q_ k (x_k) & R_1(x_1), R_2(x_1, x_2), …, R_k(x_ k-1 , x_k)\\ Q_ k '(x_k)& R_ 2k-1 (x_ 2k-2 ), R_ 2k-2 (x_ 2k-3 , x_ 2k-2 ), …, R_k(x_ k-1 , x_k). \] The update sequence is the same as the proof for Lemma~ [ref] ; the only difference is in the enumeration procedure. Once we finish every round of updates, we enumerate both $Q_ k $ and $Q_ k '$ and check if $Q_ k Q_ k '$ is empty. If it is not empty, then a $k'=2k-1$ cycle is detected. The output size is at most $n = m^ (k-1)/k $. Therefore, the delay should be $O(|D|^ 1/k-ε )$ for some $ε > 0$ to guarantee strictly smaller overhead than re-evaluation. When the amortized update time is $O(|D|^ (k-1)/k-ε )$, the total running time becomes $O(m^ (2k-1)/k-ε ) = O(m^ 2k'/(k'+1)-ε )$, and the above procedure provides a combinatorial algorithm for odd $k'$ cycle detection problem . Combining with Theorem~ [ref] , the existence of the maintenance algorithm suggests the combinatorial $k$-clique conjecture failed.

**Proof.** We reduce the OuMv$_k$ problem to maintaining $Q_d$. Let $M$ be an $d$-dimensional Boolean tensor of size $n × × n$, and let $u_1, , u_d$ be a stream of $n^γ$ vector sets. Since $n = |D|^ 1/d $, when $γ d-1$, the total input size is bounded by $(|D|^ 1/d )×(|D|^ 1/d )^γ = |D|^ (γ+1)/d $. Given an OuMv$_k$ instance, we can solve the problem with the following reduction: [leftmargin=*] ; Encode tensor $M$ into $R_1$: insert tuple $(t_1, , t_d)$ into $R_1$ if and only if $M[t_1, , t_d] = 1$. ; For each round $j$ of vector updates and queries: [leftmargin=*] ; For each $i [1, d]$, update $R_ i+1 $ to represent vector $u_i^ (j) $ by inserting $(x_i)$ if $u_i^ (j) [x_i] = 1$. ; Enumerate the result of $Q_d$. The result is non-empty if and only if $M$ has a non-empty intersection with $u_1^ (j) × × u_d^ (j) = 1$. ; Delete tuples from $R_2, , R_ d+1 $ to prepare for round $j+1$. If $Q_d$ can be maintained in $O(|D|^ (d-1)/d - ε )$ time per update, the total update time would be $O(|D|^ (d-1)/d+(γ+1)/d - ε ) = O(|D|^ 1+γ/d-ε ) = O(n^ d+γ-dε )$, and the enumeration takes $O(1)$ per query, which thus solves OuMv$_k$ problem in time $O(n^ d+γ-ε )$ and violates OuMv$_k$ Conjecture.

**Proof.** We adapt the construction from the proof for $Q_d$. In this scenario, enumeration requires outputting slices of the tensor $M$. For every output tuple $(t_ d-j+1 , , t_d)$, a valid join result exists for the first $k$ dimensions. Consequently, for each candidate output, we only need to verify if the intersection of the remaining dimensions ($k+1$ to $d$) exists in $u_ k+1 × × u_d$. We can construct a hash index on the Cartesian product $u_ k+1 × × u_d$ to perform this check in $O(1)$ time per entry. Computing this index takes $O(|D|^ (d-k)/d )$ time per round, and performing the distinct projection on $Q'_d$ to the remaining dimensions raises an additional $O(|D|^ (k+j-d)/d )$ delay. If the amortized update time were $O(|D|^ (d-1)/d - ε )$ and enumeration delay for enumerating $ _ x_ k+1 , …, x_d Q_d'$ were $O(|D|^ (d-j)/d-ε × |D|^ (k+j-d)/d ) = O(|D|^ k/d -ε)$, since there are total $O(|D|^ (d-k)/d )$ output per round, the total running time would be $O(n^ γ+d-1-ε' )$, which again violates the conjecture.

**Proof.** By definition, there exists an attribute $x_i$ with at least $h(D)$ distinct values, each appearing in at least $h(D)^ d-1 $ tuples in $R_1$. These sets of tuples are disjoint (grouped by $x_i$), so $|R_1| h(D) × h(D)^ d-1 = h(D)^d$. Thus, $h(D) |R_1|^ 1/d |D|^ 1/d $.

## Bound sentences
- The running time suggests for this query, the width $ (Q) 1.5$, and the lower bound for dynamic evaluation from the width would be $Ω(|D|^ (Q)-1 ) = Ω(|D|^ 0.5 )$, which is dominated by the bound from the dimension of the query. [Running Example for Theorem~ [ref] ] Consider $Q_1$ from Example~ [ref …
- However, a gap remains between the best known upper bound of $O(|D|)$ and lower bounds of $Ω(|D|^ 0.5-ε )$ for any $ε 0$.
- This matches our parameterized lower bound and provides instance-specific performance in favorable cases. conjunctive query, fine-grained complexity, query maintenance, dynamic algorithms <ccs2012> <concept> <concept_id>10003752.10010070.10010111.10011711</concept_id> <concept_desc>Theory of computa …
- The optimal static exponent $ (Q)$ provides a straightforward lower bound $Ω(|D|^ (Q)-1-ε )$ on the amortized update time [cite] via an insertion-only reduction.
- Although the lower bound is tight [cite] for insertion-only update sequences on free-connex queries (i.e., acyclic queries with $ (Q) = 1$), it is not the best lower bound when considering deletions. [cite] t berkholz17:_answer introduced the notion of q-hierarchical queries, a subset of free-connex …
- On the upper-bound side, multiple efforts have been made to efficiently maintain free-connex conjunctive queries~ [cite] .
- In particular, we show: [ height-based lower bound ] Let $Q$ be a height $k$ query with $k 2$ .
- Assuming combinatorial $k$-clique conjecture, no dynamic combinatorial algorithm can maintain $Q$ with amortized update time $O(|D|^ (k-1)/k-ε )$ and support constant delay enumeration for any constant~$ε>0$. [ Dimension-based lower bound ] Let $Q$ be a conjunctive query of dimension $d 2$ .
- In particular, these results are the first to provide super-square-root lower bounds for maintaining free-connex queries , implying that a general $O(|D|^ 1-ε )$-time maintenance algorithm for all free-connex queries would violate these conjectures, resolving the open problem in the negative unless  …
- On the algorithmic side, we complement our lower bounds with a new parameterized upper bound for star queries, a subset of height $2$ queries with details given in Definition~ [ref] .
- On the upper bound side, [cite] t DBLP:journals/algorithmica/AlonYZ97 gave an algorithm running for detecting a $k$-cycle with running time $O(m^ 2-(1+ k/2 ^ -1 )/(k+1) )$.
- Although we do not impose key constraints in the query definition, our lower bound instances respect them; meanwhile, prior work shows that, under restricted update sequences such as first-in-first-out, imposing key constraints can change the hardness of maintaining these sequences~ [cite] .
