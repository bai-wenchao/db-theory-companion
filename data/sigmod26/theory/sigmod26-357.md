# sigmod26-357 — Representation Obliviousness and Pseudodeterminism in Streaming Algorithms

flag=True score=53 stmts=25 proofs=13 chars=62790
kinds: {"definition": 9, "theorem": 5, "lemma": 4, "claim": 4, "fact": 1, "observation": 2}
counts: {"np_hard": 0, "lower_bound": 32, "upper_bound": 1, "big_o": 0, "omega": 16, "theta": 0, "approx_ratio": 4, "whp": 5, "regret": 0, "dp": 0, "invariant": 1, "convergence": 0, "competitive": 0, "worst_case": 0, "sketch": 11, "cost_model": 0, "cardinality": 1, "learned": 0}

## Statements
**Definition 2.1.** Let 𝜖, 𝛿 > 0. A streaming algorithm A is said to (𝜖, 𝛿)-approximate F𝑘 if for every universe size 𝑛 ∈ N and for every stream D ∈ [𝑛] ∗ Pr (|A𝑛 (D) − F𝑘 (D)| ≤ 𝜖F𝑘 (D)) ≥ 1 − 𝛿 For simplicity, in the rest of the paper we fix 𝛿 = 1/3. We will also use the following additional notation: For a set 𝑆 ⊆ [𝑛], D (𝑆) denotes the stream representation of 𝑆 in lexicographic order. For any two streams D1 and D2 , let D1 D2 denote the concatenation of the two streams. Proc. ACM Manag. Data, Vol. 3, No. 5 (PODS), Article 282. Publication date: November 2025. Representation Obliviousness and Pseudodeterminism in Streaming Algorithms 2.2 282:5 Pseudodeterministic and Representation Oblivious Streaming Algorithms We are interested in two notions; pseudodeterminism and representation obliviousness, in the context of streaming.

**Definition 2.** 2 (Pseudodeterministic algorithms [16]). A randomized algorithm A is pseudodeterministic (PD for short) if for all inputs 𝑥, there is a canonical value 𝑔(𝑥) such that Pr(A (𝑥) = 𝑔(𝑥)) ≥ 23 . In the context of streaming algorithms for F𝑘 , a pseudodeterministic algorithm for a given input stream should output a canonical approximation for F𝑘 with high probability. We next introduce the notion of representation oblivious algorithms. Let 𝑘 ∈ N \ {0}, note that the frequency moment F𝑘 is “oblivious” to the arrangement of the input stream in the sense that for any stream D, F𝑘 (D) depends only on the frequency vector 𝑓 (D). In this sense, for example, the streams (1, 2, 2, 1, 1) and (2, 1, 1, 2, 1) are equivalent. We define this formally next. Let D1 and D2 be streams over universe [𝑛]. We say D1 and D2 are R𝑘 -equivalent if 𝑓 𝑘 (D1 ) = 𝑘 𝑓 (D2 ). This equivalence relation partitions the set of inputs to equivalence classes which we call R𝑘 -equivalence classes. When 𝑘 = 0, the condition 𝑓 0 (D1 ) = 𝑓 0 (D2 ) is equivalent to the condition that the set of unique elements in D1 is equal to the set of unique elements in D2 . When 𝑘 > 0, the condition 𝑓 𝑘 (D1 ) = 𝑓 𝑘 (D2 ) is the same as 𝑓 (D1 ) = 𝑓 (D2 ).

**Definition 2.4.** [Representation Oblivious Algorithm for F𝑘 ] Let 𝑘 ∈ N. Two streams D1 and D2 are R𝑘 -equivalent if 𝑓 𝑘 (D1 ) = 𝑓 𝑘 (D2 ). A streaming algorithm A for F𝑘 is said to be representation oblivious if for every universe size 𝑛, A𝑛 is R𝑘 -oblivious. Recall that A𝑛 is the streaming algorithm A instantiated with the universe as [𝑛]. By combining the notions of pseudodeterminism and representation obliviousness, we arrive at the following definition.

**Definition 2.5.** A streaming algorithm for F𝑘 is Representation Oblivious Pseudodeterministic (ROPD) if it is both representation oblivious and pseudodeterministic. The above definition means the following: Suppose A is an ROPD algorithm for F𝑘 . Since A is pseudodeterministic for every D there is a canonical value 𝑔(D) (which is an 𝜖-approximation of F0 (D)) such that A outputs 𝑔(D) with probability at least 2/3. Representation obliviousness imposes that if D1 and D2 belong to the same R𝑘 -equivalence class, then 𝑔(D1 ) = 𝑔(D2 ). We now state a more relaxed definition of representation obliviousness.

**Definition 2.7.** A streaming algorithm for F𝑘 is 𝛼-Representation Oblivious Pseudodeterministic (𝛼-ROPD) if it is both 𝛼-representation oblivious and pseudodeterministic. Note that 0-ROPD is the same as ROPD. We remark that our lower bound results extend to 𝛼-ROPD setting. Proc. ACM Manag. Data, Vol. 3, No. 5 (PODS), Article 282. Publication date: November 2025. 282:6 2.3 Michael Chen, Sourav Chakraborty, A. Pavan, and N. V. Vinodchandran Communication Complexity For our lower bound proofs, we will need results from communication complexity. We focus on randomized communication complexity. Let 𝑚 ∈ N and 𝜓 : {0, 1}𝑚 × {0, 1}𝑚 ⇀ {0, 1} be a partial function, called the communication problem. We often view binary strings of length 𝑚 can as subsets of [𝑚] in the standard way. In the randomized communication model, Alice receives 𝐴 ∈ {0, 1}𝑚 , and Bob receives 𝐵 ∈ {0, 1}𝑚 such that (𝐴, 𝐵) ∈ dom(𝜓 ). Both are unaware of the other’s string but have access to the same shared random bits. Alice and Bob are allowed to send messages through a communication channel (in both ways). The goal is for Bob to output 𝜓 (𝐴, 𝐵). If the input is outside the domain, Bob is allowed to output any arbitrary value. Alice and Bob are allowed to agree on a strategy before receiving the input. The cost of a protocol is the sum of the lengths of messages communicated by Alice and Bob over all inputs in dom𝜓 and over all random bits. The communication complexity of 𝜓 , denoted CC(𝜓 ), is the minimum cost over all protocols. …

**Definition 2.10.** Let 𝜓 : {0, 1}𝑚 × {0, 1}𝑚 ⇀ {0, 1} be a partial function and 𝑇 be an equivalence relation over {0, 1}𝑚 × {0, 1}𝑚 . A communication protocol P for 𝜓 is 𝑇 -oblivious pseudodeteriministic (𝑇 -OPD), if it is 𝑇 -oblivious and pseudodeterministic. We now define some communication problems that we use in this paper.

**Definition 2.13.** Let 𝑚 ∈ N. Two input instances to Promise-Union𝑚 (𝐴1, 𝐵 1 ) and (𝐴2, 𝐵 2 ) are 𝜌𝑚 -equivalent if 𝐴1 ∪ 𝐵 1 = 𝐴2 ∪ 𝐵 2 . For the rest of the paper, we are interested in the above relationship 𝜌𝑚 . From now on, an ROPD protocol for Promise-Union𝑚 refers to a 𝜌𝑚 -OPD protocol. 3 F0 Lower bound In this section, we prove an Ω(𝑛) space lower bound for estimating F0 over the universe [𝑛] for ROPD one-pass streaming algorithms.

**Theorem 3.1.** Let 𝜖, 𝛿 ≤ 31 . Any ROPD streaming algorithm that (𝜖, 𝛿)-approximates F0 requires Ω(𝑛) space (where 𝑛 is the size of the universe). Our proof approach shall be to show that if an ROPD F0 estimator using 𝑜 (𝑛)-space exists, then there is a 𝑜 (𝑚) communication protocol to solve Disj𝑚 problem via the reductions depicted below. This is a contradiction because the communication complexity of Disj𝑚 is Ω(𝑚) [26, 34]. ROPD F0 estimator ROPD communication protocol for Promise-Union Communication protocol for Disj

**Lemma 3.2.** For any 𝑛 ∈ N, if there is a ROPD streaming algorithm that ( 13 , 31 )-approximates F0 (with universe size 𝑛) using space 𝑠 then there is a ROPD protocol for the Promise-Union𝑛 with communication complexity 𝑠.

**Claim 3.3.** P𝑛 is pseudodeterministic. Proc. ACM Manag. Data, Vol. 3, No. 5 (PODS), Article 282. Publication date: November 2025. 282:8 Michael Chen, Sourav Chakraborty, A. Pavan, and N. V. Vinodchandran

**Claim 3.4.** P𝑛 is representation oblivious.

**Claim 3.5.** P𝑛 solves Promise-Union𝑛 .

**Fact 3.** 6 ([26, 34]). CC(Disj𝑚 ) = Ω(𝑚) Our goal now is to show that the existence of a ROPD protocol for Promise-Union implies the existence of a PD communication protocol for Disj over a smaller universe. Suppose we want to solve Disj𝑚 , distinguishing whether 𝐴 ∩ 𝐵 = ∅ or 𝐴 ∩ 𝐵 ≠ ∅, equivalently distinguishing between 𝐴 ∪ 𝐵 = [𝑚] or 𝐴 ∪ 𝐵 ≠ [𝑚]. We consider the problem Promise-Union4𝑚 , it distinguishes between the case |𝐴 ∪ 𝐵| ≤ 𝑚 and |𝐴 ∪ 𝐵| ≥ 3𝑚. We shall show that embedded within a ROPD protocol for Promise-Union4𝑚 is a PD protocol for Disj𝑚 . All Alice and Bob would need to do is take the complement of their input and then follow the protocol for Promise-Union4𝑚 .

**Lemma 3.7.** The communication complexity of ROPD protocol for Promise-Union𝑚 is Ω(𝑚). Proc. ACM Manag. Data, Vol. 3, No. 5 (PODS), Article 282. Publication date: November 2025. Representation Obliviousness and Pseudodeterminism in Streaming Algorithms 282:9

**Theorem 4.1.** Let 𝑡 : N → N and 𝜖, 𝛿 ≤ 13 . Any ROPD 𝑡 (𝑛)-pass streaming algorithm that 𝑛 (𝜖, 𝛿)-approximates F0 requires Ω( 𝑡 (𝑛) ) space (where 𝑛 is the size of the universe). Proc. ACM Manag. Data, Vol. 3, No. 5 (PODS), Article 282. Publication date: November 2025. 282:10 Michael Chen, Sourav Chakraborty, A. Pavan, and N. V. Vinodchandran The proof of theorem 4.1 approach is similar to a single-pass streaming algorithm. We show that a space-efficient ROPD multi-pass streaming algorithm for F0 can be used to solve Promise-Union efficiently.

**Lemma 4.2.** For any 𝑛 ∈ N, if there is a ROPD 𝑡 (𝑛)-pass streaming algorithm that ( 13 , 31 )-approximates F0 (with universe size 𝑛) using space 𝑠 then there is a ROPD protocol for the Promise-Union𝑛 with communication complexity 𝑂 (𝑠 · 𝑡 (𝑛)).

**Claim 3.** 3, Claim 3.4, and Claim 3.5, we conclude that P𝑛 is a protocol for Promise-Union𝑛 that is representation oblivious and pseudodeterministic. □

**Lemma 4.** 2 there exists ROPD protocols with 𝑜 (𝑠 · 𝑡 (𝑛)) = 𝑜 ( 𝑡 (𝑛) · 𝑡 (𝑛)) = 𝑜 (𝑛) communication cost for Promise-Union. This directly contradicts Lemma 3.7. □ Note that Theorem 4.1 is indeed tight as one can design a 𝑡 (𝑛)-pass representation oblivious 𝑛 deterministic 𝑡 (𝑛)-pass streaming algorithm that computes F0 of any stream using 𝑂 ( 𝑡 (𝑛) ) space. Let 𝑡 : N → N. We now describe a simple representation oblivious deterministic 𝑡 (𝑛)-pass streaming algorithm to that computes F0 . For simplicity’s sake, assume 𝑡 (𝑛) divides 𝑛. Partition the 𝑛 𝑛 [𝑛] into 𝐵 1, 𝐵 2, . . . , 𝐵𝑡 (𝑛) where 𝐵 1 first 𝑡 (𝑛) elements of [𝑛], 𝐵 2 are the next 𝑡 (𝑛) elements of [𝑛], 𝑛 and so on. We have |𝐵𝑝 | = 𝑡 . Algorithm 1: Representation Oblivious Deterministic 𝑡 (𝑛)-Pass Streaming Algorithm for F0 Input : Stream D = (𝑎 1, . . . , 𝑎𝑚 ) 1 𝐷 ←0 2 for 𝑝 = 1, . . . , 𝑡 (𝑛) do 𝑛 ) bits 3 Observe D and count the size of the set {𝑎𝑖 | 𝑖 ∈ [𝑚]} ∩ 𝐵𝑝 using 𝑂 ( 𝑡 (𝑛) 4 𝐷 𝑝 ← Number of distinct elements in D that belong to 𝐵𝑝 5 𝐷 ← 𝐷 + 𝐷𝑝 6 return 𝐷

**Observation 4.3.** For any 𝑡 : N → N, Algorithm 1 is a 𝑡 (𝑛)-pass representation oblivious deter𝑛 ministic 𝑡 (𝑛)-pass streaming algorithm that computes exact F0 using 𝑂 ( 𝑡 (𝑛) ) space. Proc. ACM Manag. Data, Vol. 3, No. 5 (PODS), Article 282. Publication date: November 2025. Representation Obliviousness and Pseudodeterminism in Streaming Algorithms 282:11

**Theorem 5.1.** The BJKST algorithm for F0 is representation oblivious.

**Theorem 5.2.** The NVGMPC algorithm for F0 is 13 -representation oblivious.

**Observation 5.3.** Let 𝑘 > 0. All sketching algorithms [23] for F𝑘 are representation oblivious.

**Theorem 5.4.** The AMS algorithm for F𝑘 is representation oblivious.

**Definition 6.1.** Two input instances (𝐴1, 𝐵 1 ) and (𝐴2, 𝐵 2 ) are 𝜏𝑚 -equivalent if 𝐴1 ∪ 𝐵 1 = 𝐴2 ∪ 𝐵 2 and 𝐴1 ∩ 𝐵 1 = 𝐴2 ∩ 𝐵 2 . Possibly a bit surprisingly, there exists a simple pseudodeterministic communication protocol for Promise-Union𝑚 with respect to the equivalence relation 𝜏𝑚 : Proc. ACM Manag. Data, Vol. 3, No. 5 (PODS), Article 282. Publication date: November 2025. Representation Obliviousness and Pseudodeterminism in Streaming Algorithms 282:15 𝜏𝑚 -OPD protocol for Promise-Union𝑚 : - Alice send size of her input. - If the size of Alice’s input and size of Bob’s input add to more than 3𝑚/4 then Bob outputs 1 else 0 One can verify that the above protocol is indeed an 𝜏𝑚 -OPD protocol for Promise-Union𝑚 . But the correctness of the protocol crucially hinges on the fact that the gap for the Promise-Union𝑚 (as defined in 2.12 with parameter 3𝑚/4 and 𝑚/4) is very large. Instead, let us define a version of Promise-Union𝑚 with slightly different parameters:

**Definition 6.** 2 (New-Promise-Union). The communication problem New-Promise-Union𝑚 is the partial function New-Promise-Union𝑚 : 2 [𝑚] × 2 [𝑚] ⇀ {0, 1} defined as ( 1 if |𝐴 ∪ 𝐵| ≥ 0.6𝑚 New-Promise-Union𝑚 (𝐴, 𝐵) = 0 if |𝐴 ∪ 𝐵| ≤ 0.4𝑚 The proof of Lemma 3.7 goes through to establish an Ω(𝑚) lower bound for any 𝜌𝑚 -OPD protocol for New-Promise-Union. A lower bound on the communication complexity for 𝜏𝑚 -OPD protocols for New-Promise-Union𝑚 would give a lower bound for the space complexity of any ROPD streaming algorithms for estimating F𝑘 for any 𝑘. The ultimate goal of this line of research is to show a lower bound for pseudodeterministic algorithms for F0 (without the restriction of representation obliviousness). Establishing a lower bound for New-Promise-Union against I𝑚 -OPD protocols will yield such a lower bound. Here, I𝑚 is the trivial identity relation where an input is related to only itself. In fact, a lower bound for I𝑚 -OPD protocol for New-Promise-Union𝑚 is a significant open problem in the area of communication complexity. Only recently, some progress has been made in a similar problem, but in the query complexity regime and not the communication complexity regime [17]. Compared to the problem of obtaining a lower bound for I𝑚 -OPD protocols for the problem of New-Promise-Union𝑚 , the problem of obtaining lower bounds for 𝜏𝑚 -OPD protocols for New-Promise-Union𝑚 seems easie, and proving it would be a natural first step towards proving a lower bound against I𝑚 -OPD protocols. Ackno …

## Proofs
**Proof.** Let 𝑛 ∈ N. Let A𝑛 be a ROPD streaming algorithm that ( 13 , 13 )-approximates F0 over a universe [𝑛] and uses space 𝑠. We now define the protocol P𝑛 to solve the Promise-Union𝑛 : Alice receives 𝐴 ⊆ [𝑛] and Bob receives 𝐵 ⊆ [𝑛]. They convert their input sets to streams D𝐴 = D (𝐴) and D𝐵 = D (𝐵), respectively. Alice runs the streaming algorithm A𝑛 on D𝐴 and then communicates the state at the end of her execution to Bob (requiring only 𝑠 bits of communication). On receiving this state, Bob continues the execution with his part of the stream D𝐵 . Let D = D𝐴 D𝐵 . At the end of the simulation, Bob has A𝑛 (D). Bob outputs 1 if A𝑛 (D) is at least 𝑛/2; otherwise, Bob outputs 0. We now prove that P𝑛 (1) is pseudodeterministic, (2) is representation oblivious, and (3) solves Promise-Union𝑛

**Proof.** For a stream D over [𝑛], let 𝑔(D) be the canonical value of A𝑛 , i.e., Pr(A𝑛 (D) = 𝑔(D)) ≥ 23 . For 𝐴, 𝐵 ⊆ [𝑛], define ( 1 if 𝑔(D (𝐴)D (𝐵)) ≥ 𝑛2 ℎ(𝐴, 𝐵) = , 0 if 𝑔(D (𝐴)D (𝐵)) < 𝑛2 so that, Pr(P (𝐴, 𝐵) = ℎ(𝐴, 𝐵)) ≥ 23 . Thus, ℎ testifies that P𝑛 is pseudodeterministic. We emphasize that the pseudodeterminism holds for all inputs. □

**Proof.** Let (𝐴1, 𝐵 1 ), (𝐴2, 𝐵 2 ) be inputs (they need not be part of the promised region) such that (𝐴1, 𝐵 1 ) and (𝐴2, 𝐵 2 ) are 𝜌𝑛 -equivalent, i.e., 𝐴1 ∪𝐵 1 = 𝐴2 ∪𝐵 2 . We show that the output distributions are the same. Let D1 = D𝐴1 D𝐵1 and D2 = D𝐴2 D𝐵2 . As the inputs are Promise-Union𝑛 equivalent we have that 𝑓 0 (D1 ) = 𝑓 0 (D2 ), because the unique elements in D1 and D2 are the same and as a consequence D1 and D2 are R0 -equivalent. We have A𝑛 (D1 ) = A𝑛 (D2 ) because A𝑛 is representation oblivious. Which implies that P𝑛 (𝐴1, 𝐵 1 ) = P𝑛 (𝐴2, 𝐵 2 ), as required. □

**Proof.** Let (𝐴, 𝐵) ∈ dom(Promise-Union𝑛 ) be a promised input, i.e., either |𝐴 ∪ 𝐵| ≤ 𝑛4 or |𝐴 ∪ 𝐵| ≥ 3𝑛 4 . Also, let D = D (𝐴)D (𝐵), the concatenation of the stream of the set 𝐴 and 𝐵. As discussed in Claim 3.3, the output of the protocol P𝑛 will depend upon the value of A𝑛 (D). To prove P𝑛 solves Promise-Union𝑛 it suffices to show that 𝑔(D) is less than 𝑛2 for the case when |𝐴∪𝐵| ≤ 𝑛2 and 𝑔(D) is at least 𝑛2 when |𝐴∪𝐵| ≥ 3𝑛 4 . This suffices because the event A𝑛 (D) = 𝑔(D) occurs with probability at least 2/3. Since A𝑛 is an ( 13 , 13 )-approximation algorithm for F0 we have: Case 1. If |𝐴 ∪ 𝐵| ≤ 𝑛4 then 𝑔(D) ≤ 𝑛4 (1 + 13 ) = 𝑛3 . 3𝑛 1 𝑛 Case 2. If |𝐴 ∪ 𝐵| ≥ 3𝑛 4 then 𝑔(D) ≥ 4 (1 − 3 ) = 2 . Hence, the canonical values of P𝑛 gives the correct answer, i.e., ℎ satisfies ( ℎ(𝐴, 𝐵) = 1 0 if |𝐴 ∪ 𝐵| ≥ 3𝑛 4 . if |𝐴 ∪ 𝐵| ≤ 𝑛4 Thus P𝑛 solves Promise-Union𝑛 . □

**Proof.** Assume we have ROPD protocols P = (P𝑚 )𝑚∈N that solves (Promise-Union𝑚 )𝑚∈N using 𝑜 (𝑚) communication. Using this, we shall design protocols (Q𝑚 )𝑚∈N that solve (Disj𝑚 )𝑚∈N using 𝑜 (𝑚) communication contradicting Fact 3.6. Let 𝑚 ∈ N, we design a protocol Q𝑚 to solve Disj𝑚 . Alice and Bob first agree on the communication problem Promise-Union𝑛 with input size 𝑛 = 4𝑚. They shall use the help of P𝑛 to solve Disj𝑚 . Since P𝑛 is PD there exists a canonical values for P𝑛 , for 𝐴, 𝐵 ⊆ [𝑛] let 𝑔(𝐴, 𝐵) be the canonical value of P𝑛 , i.e., 2 Pr(P𝑛 (𝐴, 𝐵) = 𝑔(𝐴, 𝐵)) ≥ 3 Furthermore, since P𝑛 is representation oblivious, we have the additional guarantee that equivalent elements (under 𝜌𝑚 equivalence relation) have the same output distribution. i.e., for any (𝐴1, 𝐵 1 ) and (𝐴2, 𝐵 2 ) if 𝐴1 ∪ 𝐵 1 = 𝐴2 ∪ 𝐵 2 then P𝑛 (𝐴1, 𝐵 1 ) = P𝑛 (𝐴2, 𝐵 2 ). In particular, their canonical values are the same, 𝑔(𝐴1, 𝐵 1 ) = 𝑔(𝐴2, 𝐵 2 ). Furthermore, the canonical value only depends on the union of the subsets. For any 𝑋 ⊆ [𝑛], define ℎ(𝑋 ) = 𝑔(𝑋, ∅). We have that ℎ satisfies the following property: for all 𝐴, 𝐵 ⊆ [𝑛], 𝑔(𝐴, 𝐵) = ℎ(𝐴 ∪ 𝐵). Observe that for any set 𝑋 ⊆ [𝑛], if |𝑋 | ≤ 𝑛/4 then ℎ(𝑋 ) = 0 and if |𝑋 | ≥ 3𝑛/4 then ℎ(𝑋 ) = 1 because P𝑛 solves Promise-Union𝑛 . Therefore, there must exist a minimal 𝑌 such that ℎ(𝑌 ) = 1 and removing any subset from 𝑌 results in an output of 0. Furthermore, it must be the case that |𝑌 | > 𝑛/4. Formally there exists 𝑌 with |𝑌 | > 𝑛/4 such that ℎ(𝑌 ) = 1 and for every 𝑍 such that 𝑍 ⊊ 𝑌 , ℎ(𝑍 ) = 0. Fix the lexicographically smallest 𝑌 so that |𝑌 | = 𝑛 ′ , without loss of generality, assume 𝑌 = [𝑛 ′ ] , as Alice and Bob can always agree to rearrangements. Note that we have 𝑛 ′ > 𝑚. Alice and Bob agree to use P𝑛 and 𝑌 to solve Disj𝑚 . For 𝑋 ⊆ [𝑛 ′ ], we denote 𝑋 = [𝑛 ′ ] \𝑋 , i.e., complement is taken over the universe [𝑛 ′ ]. Identifying disjointness can equivalently be seen as distinguishing between a “full universe” and “not a full universe” by complementing the inputs over the universe [𝑛 ′ ]. Formally, via De Morgan’s law, for all 𝐴, 𝐵 ⊆ [𝑛 ′ ], 𝐴 ∩ 𝐵 = ∅ ⇐⇒ 𝐴 ∪ 𝐵 = [𝑛 ′ ]. We now define the protocol Q𝑚 : Alice receives 𝐴 ⊆ [𝑚] and Bob receives 𝐵 ⊆ [𝑚]. Note that trivially 𝐴, 𝐵 ⊆ [𝑛 ′ ] as 𝑛 ′ > 𝑚. Alice creates an instance 𝐴′ = 𝐴, and Bob creates an instance 𝐵 ′ = 𝐵. They proceed as though this were an instance of Promise-Union𝑛 with input (𝐴′, 𝐵 ′ ). After finishing the simulation, Bob obtains P𝑛 (𝐴′, 𝐵 ′ ). If P𝑛 (𝐴′, 𝐵 ′ ) = 1 then Bob outputs …

**Proof.** Let 𝜖, 𝛿 ≤ 13 . If there exists ROPD streaming algorithm that (𝜖, 𝛿)approximates F0 using space 𝑜 (𝑛) then setting 𝑛 = 𝑚 and using Lemma 3.2 there exists ROPD protocols with 𝑜 (𝑚) communication cost for Promise-Union. This directly contradicts Lemma 3.7. □

**Proof.** Let 𝑛 ∈ N. Let A𝑛 be a ROPD 𝑡 (𝑛)-pass streaming algorithm that ( 13 , 31 )-approximates F0 over a universe [𝑛] and uses space 𝑠. We now define the protocol P𝑛 to solve the Promise-Union𝑛 : Alice receives 𝐴 ⊆ [𝑛] and Bob receives 𝐵 ⊆ [𝑛]. They convert their input sets to streams D𝐴 = D (𝐴) and D𝐵 = D (𝐵), respectively. Alice runs the streaming algorithm A𝑛 on D𝐴 and then communicates the state at the end of her execution to Bob. On receiving this state, Bob continues the execution with his part of the stream D𝐵 , he then returns the algorithm’s state to Alice. Alice then initializes the next pass of the algorithm, and then they repeat the process for a total of 𝑡 (𝑛) rounds of communication. Let D = D𝐴 D𝐵 , after 𝑡 (𝑛) rounds of communication (and therefore they have simulated 𝑡 (𝑛) passes of the stream), Bob has A𝑛 (D). Bob outputs 1 if A𝑛 (D) is at least 𝑛/2; otherwise, Bob outputs 0. The cost of this communication is 2𝑠 · 𝑡 (𝑛) = 𝑂 (𝑠 · 𝑡 (𝑛)). Using identical arguments from

**Proof.** Let 𝑡 : N → N and 𝜖, 𝛿 ≤ 13 . If there exists ROPD 𝑡 (𝑛)-pass streaming 𝑛 algorithm that (𝜖, 𝛿)-approximates F0 using space 𝑠 = 𝑜 ( 𝑡 (𝑛) ) then setting 𝑛 = 𝑚 and using 𝑛

**Proof.** Clearly, the presented algorithm is deterministic (as a consequence, pseudodeterministic) 𝑛 and uses 𝑂 ( 𝑡 (𝑛) ) space because in each pass, the space used for counting can be re-used. We now prove correctness, let D = (𝑎 1, . . . , 𝑎𝑚 ) be a stream and let 𝐴 = {𝑎𝑖 | 𝑖 ∈ [𝑚]} be the set Ð (𝑛) of unique elements. The set 𝐴 can be partitioned as 𝐴 = 𝑡𝑝=1 (𝐴 ∩ 𝐵𝑝 ). In each pass 𝑝 ∈ [𝑡 (𝑛)], 𝐷 𝑝 is computed deterministically which is exactly |𝐴 ∩ 𝐵𝑝 |. Representation Obliviousness follows from the fact that Algorithm 1 computes exact F0 deterministically, and from the fact that F0 of a stream only depends on the number of unique elements. Formally, let D1, D2 be equivalent streams, i.e., 𝑓 0 (D1 ) = 𝑓 0 (D2 ). Because of correctness of algorithm, the output is exactly F0 (D1 ) = F0 (D2 ) on either input. □

**Proof.** A version of the BJKST algorithm from [3] is presented in Algorithm 2. Let D1 = (𝑎 1, . . . , 𝑎𝑚 ), D2 = (𝑏 1, . . . , 𝑏𝑚 ′ ) be two streams such that 𝑓 0 (D1 ) = 𝑓 0 (D2 ). To prove that the output distributions are the same, it suffices to show that the outputs are the same conditioned on the same hash function being chosen. Fix a hash function ℎ ∈ H , let 𝑉1 and 𝑉2 be as described in Algorithm 2 when D1 and D2 is given as input respectively. Since 𝑓 0 (D1 ) = 𝑓 0 (D2 ) (i.e., the set of unique elements is the same) and the hash function is the same, we have 𝑉1 = 𝑉2 . Therefore, both outputs have the same value. Hence, the output distributions obtained by uniformly at random picking a hash function in H are the same. □

**Proof.** The algorithm of NVGMPC can be thought of as running log 𝑛 × Reps number of algorithms in parallel - each algorithm sampling from the stream with different probabilities. Finally, after the stream ends, the output of the algorithm is a function of the number of elements picked in the different algorithms. More specifically, for any 1 ≤ 𝑘 ≤ log 𝑛 and any 1 ≤ 𝑟 ≤ Reps, the algorithm picks each element in the stream with probability 1/2𝑘 and stores the first thresh many in the bucket X𝑟(𝑘 ) . So at the end of the stream the |X𝑟(𝑘 ) | is, in fact, a random variable which is equal to min{thresh, Z𝑟(𝑘 ) }, where Z𝑟(𝑘 ) is another random variable indicating the number of elements picked if each element is independently picked with probability 1/2𝑘 . In other words, Z𝑟(𝑘 ) is a random number picked accordingly to the distribution Bin(𝑛, 1/2𝑘 ). The main contribution of NVGMPC is how they manage to pick each element of the stream independently with probability 1/2𝑘 . When element 𝑎𝑖 comes it is removed from the stored set 𝜒𝑟(𝑘 ) and then 𝑎𝑖 is added to 𝜒𝑟(𝑘 ) with probability 1/2𝑘 . Here we must note that the process is not representation oblivious. The main issue is that if, for any 𝑘, the number of elements picked (when each element of the stream is independently with probability 1/2𝑘 ) is more than thresh then only the first thresh of them are stored in 𝜒𝑟(𝑘 ) . Thus clearly which elements are stored can be very much be dependent on the ordering in the stream and hence the process (or rather the | 𝜒𝑟(𝑘 ) |) is not representation oblivious. The proof of correctness of the algorithm goes via proving that the value of 𝑘 ∗ (in line 11) is either 𝑘 0 or 𝑘 0 − 1, where 𝑘 0 is the unique number satisfying the following equation 3 F0 (S) 3 < 𝑘 ≤ . 4 2 20 The output of the algorithm depends only on the value of the |X𝑟(𝑘 ) | for 𝑘 > 𝑘 0 − 1. Proc. ACM Manag. Data, Vol. 3, No. 5 (PODS), Article 282. Publication date: November 2025. Representation Obliviousness and Pseudodeterminism in Streaming Algorithms 282:13 From our point of view, all we need to realize is that with probability at least 2/3 if the value of ∗ 𝑘 ∗ in (line 11) remains same and the distribution of value of | 𝜒𝑟(𝑘 ) | remains same for any ordering of the stream then the the algorithm is almost representation oblivious. And this is true as for 𝑘 ≥ 𝑘 0 − 1, the number of size of | 𝜒𝑟(𝑘 ) | is always less than thresh and which mean for 𝑘 > 𝑘 0 − 1 the the distribution of the random variable |X𝑟(𝑘 ) | is inv …

**Proof.** Let 𝑘 > 0 and let (M, 𝑔) be a sketching algorithm. Let D1 and D2 be R𝑘 -equivalent streams, i.e., 𝑓 (D1 ) = 𝑓 (D2 ). Clearly, the output distributions are the same as 𝑔(M 𝑓 (D1 )) = 𝑔(M 𝑓 (D2 )). □

**Proof.** A version of the AMS algorithm for estimating F𝑘 from [1] for the general value of 𝑘 > 0 is presented in Algorithm 4. For the sake of simplicity, we assume that the algorithm also gets the stream’s length as input. This assumption can easily be lifted by reservoir sampling, as shown in [1]. It suffices to show that the single instance AMS_thread is representation oblivious, as averaging independent copies does not change the representation oblivious nature of the algorithm Let D1 = (𝑎 1, . . . , 𝑎𝑚 ), D2 = (𝑏 1, . . . , 𝑏𝑚 ′ ) such that they are R2 -equivalent, i.e., 𝑓 (D1 ) = 𝑓 (D2 ). Necessarily, we have 𝑚 = 𝑚 ′ , furthermore D1 is a rearrangement of D2 . We will show that for every 𝑝 ∈ [𝑚], there exists an ℓ ∈ [𝑚] such that the output for D1 conditioned on 𝑝 being chosen is the same as the output of D2 conditioned on ℓ being chosen. Furthermore, this mapping is bijective. Since the index is chosen uniformly at random, the output distribution is therefore the same. Proc. ACM Manag. Data, Vol. 3, No. 5 (PODS), Article 282. Publication date: November 2025. 282:14 Michael Chen, Sourav Chakraborty, A. Pavan, and N. V. Vinodchandran Toward that end, fix a 𝑝 ∈ [𝑚], let 𝑥 = 𝑎𝑝 . Let 𝑡 ∈ [𝑓𝑥 (D1 )] so that 𝑎𝑝 is the 𝑡 th occurrence of 𝑥 in D1 . Let ℓ ∈ [𝑚] be Í the index of theÍ𝑡 th occurrence of 𝑥 in D2 . Observe that this construction 𝑚 leads to the following: 𝑚 𝑖=𝑝 1 {𝑎𝑖 =𝑎𝑝 } = 𝑖=ℓ 1 {𝑏𝑖 =𝑏 ℓ } , i.e., the frequencies of 𝑥 in D1 after position 𝑝 is the same as the frequencies of 𝑥 in D2 after position ℓ. Therefore, the output for D1 when 𝑝 is chosen is the same as the output for D2 when ℓ is chosen. Thus, for every 𝑝 ∈ [𝑚], there exists a unique ℓ𝑝 ∈ [𝑚] such that the outputs are the same. Since the index 𝑝 is chosen uniformly at random, summing over all possible outputs for each 𝑝, the output distribution is the same for D1 and D2 . □

## Bound sentences
