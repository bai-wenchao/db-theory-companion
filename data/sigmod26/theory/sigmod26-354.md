# sigmod26-354 — Regular Expression Indexing for Log Analysis

from=tex flag=True score=9 stmts=3 proofs=2 chars=159362
kinds: {"lemma": 1, "observation": 2}
counts: {"np_hard": 1, "lower_bound": 0, "upper_bound": 0, "big_o": 9, "omega": 0, "theta": 0, "approx_ratio": 1, "whp": 1, "regret": 0, "dp": 0, "invariant": 0, "convergence": 0, "competitive": 0, "worst_case": 1, "sketch": 0, "cost_model": 0, "cardinality": 0, "learned": 0}

## Statements
**Observation .** Consider a trigram $s = α β γ$ with selectivity $σ$ that is picked based on its frequency in the query workload. Then, the bigrams $α β$ and $β γ$ together have selectivity $σ$.

**Observation .** Suppose a bigram $g$ has selectivity $σ$. Then, any string literal $s$ containing $g$ has selectivity at most $σ$.

**Lemma .** Algorithm~ [ref] correctly generates the output of a regex for the given log $L$.

## Proofs
**Proof.** Consider some log line $ L$. First, note that the bit index for $ $ stores a set bit for all bigrams $b G$ if $b$ is a substring of length $2$ in $ $. To show the correctness of Algorithm~ [ref] , it suffices to argue that the bit filtering on line~ [ref] does not remove any log line from consideration that may contain a potential regex match. We consider two cases: Regex has no $n$-grams In this case, the Set-BitVector on line~ [ref] returns a bit-vector with all bits as $0$ and line~ [ref] will set $m$ to bit-vector with all bits as $1$. Thus, the check on line~ [ref] will be evaluated to true and the regex will be evaluated on the log line under consideration. Regex has $n$-grams Suppose $g_1, , g_j$ are the $j$ $n$-grams present in the regex and also as keys in the inverted index hashmap $G$. Let us consider gram $g_1$. Set-BitVector will set the bit for $g_1$ as $1$ and line~ [ref] will set it to $0$. Next, for the check on line~ [ref] , the output bit corresponding to $g_1$ will be $1$ if and only if the bit in $I[j]$ for $g_1$ is set to $1$ (i.e. $g_1$ is a $n$-gram that is present in the log line $L_j$). In other words, if $I[j]$ for $g_1$ is $0$, we correctly do not process the log line. Otherwise, the output bit is correctly set to $1$. The same reasoning applies to all other $n$-grams as well. Thus, the check on line~ [ref] is passed if and only if each of the $n$-grams of the regex are also present in the index $I[j]$.

**Proof.** Consider the set of bigrams generated by $s$ (which includes $g$). The number of log lines that contains $g$ is $σ · |L|$. Clearly, by adding the additional requirement that all other bigrams of $s$ be also present in the log lines that contain $g$, the number of such log lines can only decrease from $σ · |L|$ which implies that the selectivity of $s$ is at most $σ$.

## Bound sentences
- The search for the optimal subset is shown to be NP-hard~ [cite] ; most works focus on techniques to reduce the search space and reach a good enough subset.
